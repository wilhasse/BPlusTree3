"""
Fuzz test failure reproduction
Seed: 719598
Capacity: 4
Failed at operation: 13421
"""

from bplus_tree import BPlusTreeMap
from collections import OrderedDict

def reproduce_failure():
    tree = BPlusTreeMap(capacity=4)
    reference = OrderedDict()

    # Operation 1: insert
    tree[1366] = 'value_474360'
    reference[1366] = 'value_474360'
    assert tree.invariants(), "Invariants failed at step 1"

    # Operation 2: update
    tree[1366] = 'value_580537'
    reference[1366] = 'value_580537'
    assert tree.invariants(), "Invariants failed at step 2"

    # Operation 3: delete
    del tree[1366]
    del reference[1366]
    assert tree.invariants(), "Invariants failed at step 3"

    # Operation 4: insert
    tree[5632] = 'value_506476'
    reference[5632] = 'value_506476'
    assert tree.invariants(), "Invariants failed at step 4"

    # Operation 5: update
    tree[5632] = 'value_43363'
    reference[5632] = 'value_43363'
    assert tree.invariants(), "Invariants failed at step 5"

    # Operation 6: delete
    del tree[5632]
    del reference[5632]
    assert tree.invariants(), "Invariants failed at step 6"

    # Operation 7: insert
    tree[1253] = 'value_877173'
    reference[1253] = 'value_877173'
    assert tree.invariants(), "Invariants failed at step 7"

    # Operation 8: update
    tree[1253] = 'value_155680'
    reference[1253] = 'value_155680'
    assert tree.invariants(), "Invariants failed at step 8"

    # Operation 9: insert
    tree[7283] = 'value_4454'
    reference[7283] = 'value_4454'
    assert tree.invariants(), "Invariants failed at step 9"

    # Operation 10: insert
    tree[8514] = 'value_426146'
    reference[8514] = 'value_426146'
    assert tree.invariants(), "Invariants failed at step 10"

    # Operation 11: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11"

    # Operation 12: insert
    tree[6055] = 'value_622052'
    reference[6055] = 'value_622052'
    assert tree.invariants(), "Invariants failed at step 12"

    # Operation 13: delete
    del tree[6055]
    del reference[6055]
    assert tree.invariants(), "Invariants failed at step 13"

    # Operation 14: insert
    tree[6796] = 'value_630485'
    reference[6796] = 'value_630485'
    assert tree.invariants(), "Invariants failed at step 14"

    # Operation 15: get
    assert tree.invariants(), "Invariants failed at step 15"

    # Operation 16: insert
    tree[7223] = 'value_455361'
    reference[7223] = 'value_455361'
    assert tree.invariants(), "Invariants failed at step 16"

    # Operation 17: get
    assert tree.invariants(), "Invariants failed at step 17"

    # Operation 18: insert
    tree[3164] = 'value_298951'
    reference[3164] = 'value_298951'
    assert tree.invariants(), "Invariants failed at step 18"

    # Operation 19: update
    tree[7223] = 'value_748288'
    reference[7223] = 'value_748288'
    assert tree.invariants(), "Invariants failed at step 19"

    # Operation 20: delete
    del tree[6796]
    del reference[6796]
    assert tree.invariants(), "Invariants failed at step 20"

    # Operation 21: get
    assert tree.invariants(), "Invariants failed at step 21"

    # Operation 22: batch_delete
    keys_to_delete = [8514, 6259, 1319, 7223]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 22"

    # Operation 23: update
    tree[1253] = 'value_318890'
    reference[1253] = 'value_318890'
    assert tree.invariants(), "Invariants failed at step 23"

    # Operation 24: delete
    del tree[3164]
    del reference[3164]
    assert tree.invariants(), "Invariants failed at step 24"

    # Operation 25: get
    assert tree.invariants(), "Invariants failed at step 25"

    # Operation 26: insert
    tree[1362] = 'value_854673'
    reference[1362] = 'value_854673'
    assert tree.invariants(), "Invariants failed at step 26"

    # Operation 27: update
    tree[1253] = 'value_133083'
    reference[1253] = 'value_133083'
    assert tree.invariants(), "Invariants failed at step 27"

    # Operation 28: update
    tree[1253] = 'value_974282'
    reference[1253] = 'value_974282'
    assert tree.invariants(), "Invariants failed at step 28"

    # Operation 29: insert
    tree[1010] = 'value_353580'
    reference[1010] = 'value_353580'
    assert tree.invariants(), "Invariants failed at step 29"

    # Operation 30: insert
    tree[8873] = 'value_161616'
    reference[8873] = 'value_161616'
    assert tree.invariants(), "Invariants failed at step 30"

    # Operation 31: delete
    del tree[1362]
    del reference[1362]
    assert tree.invariants(), "Invariants failed at step 31"

    # Operation 32: get
    assert tree.invariants(), "Invariants failed at step 32"

    # Operation 33: insert
    tree[1938] = 'value_62541'
    reference[1938] = 'value_62541'
    assert tree.invariants(), "Invariants failed at step 33"

    # Operation 34: insert
    tree[3167] = 'value_608038'
    reference[3167] = 'value_608038'
    assert tree.invariants(), "Invariants failed at step 34"

    # Operation 35: update
    tree[1938] = 'value_677042'
    reference[1938] = 'value_677042'
    assert tree.invariants(), "Invariants failed at step 35"

    # Operation 36: insert
    tree[4739] = 'value_708484'
    reference[4739] = 'value_708484'
    assert tree.invariants(), "Invariants failed at step 36"

    # Operation 37: delete
    del tree[7283]
    del reference[7283]
    assert tree.invariants(), "Invariants failed at step 37"

    # Operation 38: delete
    del tree[8873]
    del reference[8873]
    assert tree.invariants(), "Invariants failed at step 38"

    # Operation 39: insert
    tree[1671] = 'value_286692'
    reference[1671] = 'value_286692'
    assert tree.invariants(), "Invariants failed at step 39"

    # Operation 40: delete
    del tree[1938]
    del reference[1938]
    assert tree.invariants(), "Invariants failed at step 40"

    # Operation 41: insert
    tree[1628] = 'value_963853'
    reference[1628] = 'value_963853'
    assert tree.invariants(), "Invariants failed at step 41"

    # Operation 42: insert
    tree[8239] = 'value_228036'
    reference[8239] = 'value_228036'
    assert tree.invariants(), "Invariants failed at step 42"

    # Operation 43: insert
    tree[7906] = 'value_203002'
    reference[7906] = 'value_203002'
    assert tree.invariants(), "Invariants failed at step 43"

    # Operation 44: get
    assert tree.invariants(), "Invariants failed at step 44"

    # Operation 45: get
    assert tree.invariants(), "Invariants failed at step 45"

    # Operation 46: get
    assert tree.invariants(), "Invariants failed at step 46"

    # Operation 47: delete
    del tree[1628]
    del reference[1628]
    assert tree.invariants(), "Invariants failed at step 47"

    # Operation 48: delete
    del tree[1253]
    del reference[1253]
    assert tree.invariants(), "Invariants failed at step 48"

    # Operation 49: delete
    del tree[3167]
    del reference[3167]
    assert tree.invariants(), "Invariants failed at step 49"

    # Operation 50: get
    assert tree.invariants(), "Invariants failed at step 50"

    # Operation 51: get
    assert tree.invariants(), "Invariants failed at step 51"

    # Operation 52: delete
    del tree[7906]
    del reference[7906]
    assert tree.invariants(), "Invariants failed at step 52"

    # Operation 53: insert
    tree[7372] = 'value_170179'
    reference[7372] = 'value_170179'
    assert tree.invariants(), "Invariants failed at step 53"

    # Operation 54: update
    tree[1010] = 'value_739336'
    reference[1010] = 'value_739336'
    assert tree.invariants(), "Invariants failed at step 54"

    # Operation 55: insert
    tree[5615] = 'value_299756'
    reference[5615] = 'value_299756'
    assert tree.invariants(), "Invariants failed at step 55"

    # Operation 56: update
    tree[1671] = 'value_953531'
    reference[1671] = 'value_953531'
    assert tree.invariants(), "Invariants failed at step 56"

    # Operation 57: get
    assert tree.invariants(), "Invariants failed at step 57"

    # Operation 58: batch_delete
    keys_to_delete = [4739, 1671, 5615, 3922, 4924]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 58"

    # Operation 59: update
    tree[1010] = 'value_599795'
    reference[1010] = 'value_599795'
    assert tree.invariants(), "Invariants failed at step 59"

    # Operation 60: insert
    tree[8088] = 'value_715795'
    reference[8088] = 'value_715795'
    assert tree.invariants(), "Invariants failed at step 60"

    # Operation 61: insert
    tree[1760] = 'value_966985'
    reference[1760] = 'value_966985'
    assert tree.invariants(), "Invariants failed at step 61"

    # Operation 62: update
    tree[1760] = 'value_472779'
    reference[1760] = 'value_472779'
    assert tree.invariants(), "Invariants failed at step 62"

    # Operation 63: insert
    tree[7763] = 'value_149572'
    reference[7763] = 'value_149572'
    assert tree.invariants(), "Invariants failed at step 63"

    # Operation 64: get
    assert tree.invariants(), "Invariants failed at step 64"

    # Operation 65: delete
    del tree[8088]
    del reference[8088]
    assert tree.invariants(), "Invariants failed at step 65"

    # Operation 66: batch_delete
    keys_to_delete = [1760, 7089, 9983, 8239]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 66"

    # Operation 67: delete
    del tree[7763]
    del reference[7763]
    assert tree.invariants(), "Invariants failed at step 67"

    # Operation 68: get
    assert tree.invariants(), "Invariants failed at step 68"

    # Operation 69: insert
    tree[6143] = 'value_550413'
    reference[6143] = 'value_550413'
    assert tree.invariants(), "Invariants failed at step 69"

    # Operation 70: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 70"

    # Operation 71: get
    assert tree.invariants(), "Invariants failed at step 71"

    # Operation 72: insert
    tree[5427] = 'value_880493'
    reference[5427] = 'value_880493'
    assert tree.invariants(), "Invariants failed at step 72"

    # Operation 73: get
    assert tree.invariants(), "Invariants failed at step 73"

    # Operation 74: insert
    tree[3079] = 'value_26663'
    reference[3079] = 'value_26663'
    assert tree.invariants(), "Invariants failed at step 74"

    # Operation 75: insert
    tree[6083] = 'value_25207'
    reference[6083] = 'value_25207'
    assert tree.invariants(), "Invariants failed at step 75"

    # Operation 76: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 76"

    # Operation 77: insert
    tree[438] = 'value_598398'
    reference[438] = 'value_598398'
    assert tree.invariants(), "Invariants failed at step 77"

    # Operation 78: update
    tree[5427] = 'value_724585'
    reference[5427] = 'value_724585'
    assert tree.invariants(), "Invariants failed at step 78"

    # Operation 79: delete
    del tree[5427]
    del reference[5427]
    assert tree.invariants(), "Invariants failed at step 79"

    # Operation 80: update
    tree[7372] = 'value_639341'
    reference[7372] = 'value_639341'
    assert tree.invariants(), "Invariants failed at step 80"

    # Operation 81: get
    assert tree.invariants(), "Invariants failed at step 81"

    # Operation 82: update
    tree[3079] = 'value_928580'
    reference[3079] = 'value_928580'
    assert tree.invariants(), "Invariants failed at step 82"

    # Operation 83: delete
    del tree[6143]
    del reference[6143]
    assert tree.invariants(), "Invariants failed at step 83"

    # Operation 84: insert
    tree[5161] = 'value_35905'
    reference[5161] = 'value_35905'
    assert tree.invariants(), "Invariants failed at step 84"

    # Operation 85: get
    assert tree.invariants(), "Invariants failed at step 85"

    # Operation 86: delete
    del tree[438]
    del reference[438]
    assert tree.invariants(), "Invariants failed at step 86"

    # Operation 87: batch_delete
    keys_to_delete = [5223, 4484, 7372, 3079]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 87"

    # Operation 88: delete
    del tree[6083]
    del reference[6083]
    assert tree.invariants(), "Invariants failed at step 88"

    # Operation 89: insert
    tree[9959] = 'value_786752'
    reference[9959] = 'value_786752'
    assert tree.invariants(), "Invariants failed at step 89"

    # Operation 90: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 90"

    # Operation 91: delete
    del tree[9959]
    del reference[9959]
    assert tree.invariants(), "Invariants failed at step 91"

    # Operation 92: insert
    tree[5944] = 'value_326293'
    reference[5944] = 'value_326293'
    assert tree.invariants(), "Invariants failed at step 92"

    # Operation 93: get
    assert tree.invariants(), "Invariants failed at step 93"

    # Operation 94: insert
    tree[6057] = 'value_247304'
    reference[6057] = 'value_247304'
    assert tree.invariants(), "Invariants failed at step 94"

    # Operation 95: insert
    tree[1763] = 'value_568366'
    reference[1763] = 'value_568366'
    assert tree.invariants(), "Invariants failed at step 95"

    # Operation 96: batch_delete
    keys_to_delete = [6480, 5161, 1763, 6057]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 96"

    # Operation 97: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 97"

    # Operation 98: delete
    del tree[5944]
    del reference[5944]
    assert tree.invariants(), "Invariants failed at step 98"

    # Operation 99: update
    tree[1010] = 'value_226633'
    reference[1010] = 'value_226633'
    assert tree.invariants(), "Invariants failed at step 99"

    # Operation 100: delete
    del tree[1010]
    del reference[1010]
    assert tree.invariants(), "Invariants failed at step 100"

    # Operation 101: insert
    tree[6036] = 'value_75440'
    reference[6036] = 'value_75440'
    assert tree.invariants(), "Invariants failed at step 101"

    # Operation 102: delete
    del tree[6036]
    del reference[6036]
    assert tree.invariants(), "Invariants failed at step 102"

    # Operation 103: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 103"

    # Operation 104: insert
    tree[5295] = 'value_75524'
    reference[5295] = 'value_75524'
    assert tree.invariants(), "Invariants failed at step 104"

    # Operation 105: insert
    tree[7217] = 'value_764833'
    reference[7217] = 'value_764833'
    assert tree.invariants(), "Invariants failed at step 105"

    # Operation 106: insert
    tree[8636] = 'value_686641'
    reference[8636] = 'value_686641'
    assert tree.invariants(), "Invariants failed at step 106"

    # Operation 107: delete
    del tree[8636]
    del reference[8636]
    assert tree.invariants(), "Invariants failed at step 107"

    # Operation 108: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 108"

    # Operation 109: insert
    tree[1218] = 'value_276946'
    reference[1218] = 'value_276946'
    assert tree.invariants(), "Invariants failed at step 109"

    # Operation 110: update
    tree[7217] = 'value_932844'
    reference[7217] = 'value_932844'
    assert tree.invariants(), "Invariants failed at step 110"

    # Operation 111: insert
    tree[1334] = 'value_689691'
    reference[1334] = 'value_689691'
    assert tree.invariants(), "Invariants failed at step 111"

    # Operation 112: get
    assert tree.invariants(), "Invariants failed at step 112"

    # Operation 113: delete
    del tree[1218]
    del reference[1218]
    assert tree.invariants(), "Invariants failed at step 113"

    # Operation 114: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 114"

    # Operation 115: update
    tree[7217] = 'value_388320'
    reference[7217] = 'value_388320'
    assert tree.invariants(), "Invariants failed at step 115"

    # Operation 116: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 116"

    # Operation 117: insert
    tree[1471] = 'value_350338'
    reference[1471] = 'value_350338'
    assert tree.invariants(), "Invariants failed at step 117"

    # Operation 118: get
    assert tree.invariants(), "Invariants failed at step 118"

    # Operation 119: get
    assert tree.invariants(), "Invariants failed at step 119"

    # Operation 120: delete
    del tree[1471]
    del reference[1471]
    assert tree.invariants(), "Invariants failed at step 120"

    # Operation 121: get
    assert tree.invariants(), "Invariants failed at step 121"

    # Operation 122: get
    assert tree.invariants(), "Invariants failed at step 122"

    # Operation 123: insert
    tree[4510] = 'value_698829'
    reference[4510] = 'value_698829'
    assert tree.invariants(), "Invariants failed at step 123"

    # Operation 124: insert
    tree[7314] = 'value_193219'
    reference[7314] = 'value_193219'
    assert tree.invariants(), "Invariants failed at step 124"

    # Operation 125: update
    tree[7314] = 'value_596594'
    reference[7314] = 'value_596594'
    assert tree.invariants(), "Invariants failed at step 125"

    # Operation 126: insert
    tree[814] = 'value_883764'
    reference[814] = 'value_883764'
    assert tree.invariants(), "Invariants failed at step 126"

    # Operation 127: batch_delete
    keys_to_delete = [236, 814, 7217, 7314, 6334]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 127"

    # Operation 128: delete
    del tree[5295]
    del reference[5295]
    assert tree.invariants(), "Invariants failed at step 128"

    # Operation 129: update
    tree[4510] = 'value_580795'
    reference[4510] = 'value_580795'
    assert tree.invariants(), "Invariants failed at step 129"

    # Operation 130: insert
    tree[6673] = 'value_999893'
    reference[6673] = 'value_999893'
    assert tree.invariants(), "Invariants failed at step 130"

    # Operation 131: get
    assert tree.invariants(), "Invariants failed at step 131"

    # Operation 132: delete
    del tree[4510]
    del reference[4510]
    assert tree.invariants(), "Invariants failed at step 132"

    # Operation 133: delete
    del tree[1334]
    del reference[1334]
    assert tree.invariants(), "Invariants failed at step 133"

    # Operation 134: get
    assert tree.invariants(), "Invariants failed at step 134"

    # Operation 135: insert
    tree[9181] = 'value_481445'
    reference[9181] = 'value_481445'
    assert tree.invariants(), "Invariants failed at step 135"

    # Operation 136: delete
    del tree[6673]
    del reference[6673]
    assert tree.invariants(), "Invariants failed at step 136"

    # Operation 137: insert
    tree[7739] = 'value_570358'
    reference[7739] = 'value_570358'
    assert tree.invariants(), "Invariants failed at step 137"

    # Operation 138: insert
    tree[7594] = 'value_761548'
    reference[7594] = 'value_761548'
    assert tree.invariants(), "Invariants failed at step 138"

    # Operation 139: get
    assert tree.invariants(), "Invariants failed at step 139"

    # Operation 140: delete
    del tree[9181]
    del reference[9181]
    assert tree.invariants(), "Invariants failed at step 140"

    # Operation 141: delete
    del tree[7594]
    del reference[7594]
    assert tree.invariants(), "Invariants failed at step 141"

    # Operation 142: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 142"

    # Operation 143: insert
    tree[5774] = 'value_565004'
    reference[5774] = 'value_565004'
    assert tree.invariants(), "Invariants failed at step 143"

    # Operation 144: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 144"

    # Operation 145: insert
    tree[2571] = 'value_550863'
    reference[2571] = 'value_550863'
    assert tree.invariants(), "Invariants failed at step 145"

    # Operation 146: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 146"

    # Operation 147: delete
    del tree[2571]
    del reference[2571]
    assert tree.invariants(), "Invariants failed at step 147"

    # Operation 148: insert
    tree[3531] = 'value_462849'
    reference[3531] = 'value_462849'
    assert tree.invariants(), "Invariants failed at step 148"

    # Operation 149: delete
    del tree[3531]
    del reference[3531]
    assert tree.invariants(), "Invariants failed at step 149"

    # Operation 150: insert
    tree[9407] = 'value_257819'
    reference[9407] = 'value_257819'
    assert tree.invariants(), "Invariants failed at step 150"

    # Operation 151: delete
    del tree[5774]
    del reference[5774]
    assert tree.invariants(), "Invariants failed at step 151"

    # Operation 152: insert
    tree[1987] = 'value_411337'
    reference[1987] = 'value_411337'
    assert tree.invariants(), "Invariants failed at step 152"

    # Operation 153: insert
    tree[9156] = 'value_423118'
    reference[9156] = 'value_423118'
    assert tree.invariants(), "Invariants failed at step 153"

    # Operation 154: update
    tree[1987] = 'value_962051'
    reference[1987] = 'value_962051'
    assert tree.invariants(), "Invariants failed at step 154"

    # Operation 155: insert
    tree[5207] = 'value_782157'
    reference[5207] = 'value_782157'
    assert tree.invariants(), "Invariants failed at step 155"

    # Operation 156: get
    assert tree.invariants(), "Invariants failed at step 156"

    # Operation 157: insert
    tree[2974] = 'value_105173'
    reference[2974] = 'value_105173'
    assert tree.invariants(), "Invariants failed at step 157"

    # Operation 158: update
    tree[5207] = 'value_334932'
    reference[5207] = 'value_334932'
    assert tree.invariants(), "Invariants failed at step 158"

    # Operation 159: delete
    del tree[9407]
    del reference[9407]
    assert tree.invariants(), "Invariants failed at step 159"

    # Operation 160: get
    assert tree.invariants(), "Invariants failed at step 160"

    # Operation 161: delete
    del tree[7739]
    del reference[7739]
    assert tree.invariants(), "Invariants failed at step 161"

    # Operation 162: delete
    del tree[9156]
    del reference[9156]
    assert tree.invariants(), "Invariants failed at step 162"

    # Operation 163: insert
    tree[9340] = 'value_908082'
    reference[9340] = 'value_908082'
    assert tree.invariants(), "Invariants failed at step 163"

    # Operation 164: get
    assert tree.invariants(), "Invariants failed at step 164"

    # Operation 165: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 165"

    # Operation 166: get
    assert tree.invariants(), "Invariants failed at step 166"

    # Operation 167: delete
    del tree[5207]
    del reference[5207]
    assert tree.invariants(), "Invariants failed at step 167"

    # Operation 168: insert
    tree[1208] = 'value_38431'
    reference[1208] = 'value_38431'
    assert tree.invariants(), "Invariants failed at step 168"

    # Operation 169: delete
    del tree[1208]
    del reference[1208]
    assert tree.invariants(), "Invariants failed at step 169"

    # Operation 170: insert
    tree[1307] = 'value_604840'
    reference[1307] = 'value_604840'
    assert tree.invariants(), "Invariants failed at step 170"

    # Operation 171: update
    tree[1307] = 'value_345186'
    reference[1307] = 'value_345186'
    assert tree.invariants(), "Invariants failed at step 171"

    # Operation 172: insert
    tree[8111] = 'value_759905'
    reference[8111] = 'value_759905'
    assert tree.invariants(), "Invariants failed at step 172"

    # Operation 173: insert
    tree[4687] = 'value_924725'
    reference[4687] = 'value_924725'
    assert tree.invariants(), "Invariants failed at step 173"

    # Operation 174: insert
    tree[8882] = 'value_871465'
    reference[8882] = 'value_871465'
    assert tree.invariants(), "Invariants failed at step 174"

    # Operation 175: delete
    del tree[1987]
    del reference[1987]
    assert tree.invariants(), "Invariants failed at step 175"

    # Operation 176: get
    assert tree.invariants(), "Invariants failed at step 176"

    # Operation 177: insert
    tree[5895] = 'value_792175'
    reference[5895] = 'value_792175'
    assert tree.invariants(), "Invariants failed at step 177"

    # Operation 178: insert
    tree[9737] = 'value_358078'
    reference[9737] = 'value_358078'
    assert tree.invariants(), "Invariants failed at step 178"

    # Operation 179: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 179"

    # Operation 180: delete
    del tree[8882]
    del reference[8882]
    assert tree.invariants(), "Invariants failed at step 180"

    # Operation 181: batch_delete
    keys_to_delete = [9737, 6964, 1307, 3165, 2974]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 181"

    # Operation 182: update
    tree[8111] = 'value_155207'
    reference[8111] = 'value_155207'
    assert tree.invariants(), "Invariants failed at step 182"

    # Operation 183: update
    tree[9340] = 'value_560117'
    reference[9340] = 'value_560117'
    assert tree.invariants(), "Invariants failed at step 183"

    # Operation 184: insert
    tree[9586] = 'value_520964'
    reference[9586] = 'value_520964'
    assert tree.invariants(), "Invariants failed at step 184"

    # Operation 185: insert
    tree[81] = 'value_953930'
    reference[81] = 'value_953930'
    assert tree.invariants(), "Invariants failed at step 185"

    # Operation 186: delete
    del tree[9586]
    del reference[9586]
    assert tree.invariants(), "Invariants failed at step 186"

    # Operation 187: batch_delete
    keys_to_delete = [81, 9340, 4437, 5692]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 187"

    # Operation 188: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 188"

    # Operation 189: insert
    tree[7241] = 'value_104162'
    reference[7241] = 'value_104162'
    assert tree.invariants(), "Invariants failed at step 189"

    # Operation 190: update
    tree[8111] = 'value_10804'
    reference[8111] = 'value_10804'
    assert tree.invariants(), "Invariants failed at step 190"

    # Operation 191: delete
    del tree[5895]
    del reference[5895]
    assert tree.invariants(), "Invariants failed at step 191"

    # Operation 192: update
    tree[7241] = 'value_896792'
    reference[7241] = 'value_896792'
    assert tree.invariants(), "Invariants failed at step 192"

    # Operation 193: insert
    tree[6846] = 'value_936876'
    reference[6846] = 'value_936876'
    assert tree.invariants(), "Invariants failed at step 193"

    # Operation 194: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 194"

    # Operation 195: get
    assert tree.invariants(), "Invariants failed at step 195"

    # Operation 196: delete
    del tree[4687]
    del reference[4687]
    assert tree.invariants(), "Invariants failed at step 196"

    # Operation 197: delete
    del tree[8111]
    del reference[8111]
    assert tree.invariants(), "Invariants failed at step 197"

    # Operation 198: update
    tree[7241] = 'value_532721'
    reference[7241] = 'value_532721'
    assert tree.invariants(), "Invariants failed at step 198"

    # Operation 199: insert
    tree[9154] = 'value_115995'
    reference[9154] = 'value_115995'
    assert tree.invariants(), "Invariants failed at step 199"

    # Operation 200: insert
    tree[5957] = 'value_935204'
    reference[5957] = 'value_935204'
    assert tree.invariants(), "Invariants failed at step 200"

    # Operation 201: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 201"

    # Operation 202: insert
    tree[84] = 'value_546629'
    reference[84] = 'value_546629'
    assert tree.invariants(), "Invariants failed at step 202"

    # Operation 203: batch_delete
    keys_to_delete = [7165, 9154, 5957, 4055]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 203"

    # Operation 204: insert
    tree[1129] = 'value_569000'
    reference[1129] = 'value_569000'
    assert tree.invariants(), "Invariants failed at step 204"

    # Operation 205: delete
    del tree[84]
    del reference[84]
    assert tree.invariants(), "Invariants failed at step 205"

    # Operation 206: delete
    del tree[1129]
    del reference[1129]
    assert tree.invariants(), "Invariants failed at step 206"

    # Operation 207: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 207"

    # Operation 208: get
    assert tree.invariants(), "Invariants failed at step 208"

    # Operation 209: insert
    tree[3276] = 'value_253739'
    reference[3276] = 'value_253739'
    assert tree.invariants(), "Invariants failed at step 209"

    # Operation 210: insert
    tree[2303] = 'value_872110'
    reference[2303] = 'value_872110'
    assert tree.invariants(), "Invariants failed at step 210"

    # Operation 211: delete
    del tree[2303]
    del reference[2303]
    assert tree.invariants(), "Invariants failed at step 211"

    # Operation 212: insert
    tree[4140] = 'value_493368'
    reference[4140] = 'value_493368'
    assert tree.invariants(), "Invariants failed at step 212"

    # Operation 213: get
    assert tree.invariants(), "Invariants failed at step 213"

    # Operation 214: delete
    del tree[3276]
    del reference[3276]
    assert tree.invariants(), "Invariants failed at step 214"

    # Operation 215: delete
    del tree[7241]
    del reference[7241]
    assert tree.invariants(), "Invariants failed at step 215"

    # Operation 216: delete
    del tree[4140]
    del reference[4140]
    assert tree.invariants(), "Invariants failed at step 216"

    # Operation 217: get
    assert tree.invariants(), "Invariants failed at step 217"

    # Operation 218: insert
    tree[7715] = 'value_948690'
    reference[7715] = 'value_948690'
    assert tree.invariants(), "Invariants failed at step 218"

    # Operation 219: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 219"

    # Operation 220: insert
    tree[963] = 'value_543502'
    reference[963] = 'value_543502'
    assert tree.invariants(), "Invariants failed at step 220"

    # Operation 221: delete
    del tree[6846]
    del reference[6846]
    assert tree.invariants(), "Invariants failed at step 221"

    # Operation 222: insert
    tree[9008] = 'value_380724'
    reference[9008] = 'value_380724'
    assert tree.invariants(), "Invariants failed at step 222"

    # Operation 223: get
    assert tree.invariants(), "Invariants failed at step 223"

    # Operation 224: get
    assert tree.invariants(), "Invariants failed at step 224"

    # Operation 225: insert
    tree[3750] = 'value_949871'
    reference[3750] = 'value_949871'
    assert tree.invariants(), "Invariants failed at step 225"

    # Operation 226: delete
    del tree[3750]
    del reference[3750]
    assert tree.invariants(), "Invariants failed at step 226"

    # Operation 227: update
    tree[9008] = 'value_431038'
    reference[9008] = 'value_431038'
    assert tree.invariants(), "Invariants failed at step 227"

    # Operation 228: update
    tree[963] = 'value_31923'
    reference[963] = 'value_31923'
    assert tree.invariants(), "Invariants failed at step 228"

    # Operation 229: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 229"

    # Operation 230: delete
    del tree[9008]
    del reference[9008]
    assert tree.invariants(), "Invariants failed at step 230"

    # Operation 231: insert
    tree[6005] = 'value_825859'
    reference[6005] = 'value_825859'
    assert tree.invariants(), "Invariants failed at step 231"

    # Operation 232: insert
    tree[5362] = 'value_407760'
    reference[5362] = 'value_407760'
    assert tree.invariants(), "Invariants failed at step 232"

    # Operation 233: insert
    tree[8510] = 'value_480924'
    reference[8510] = 'value_480924'
    assert tree.invariants(), "Invariants failed at step 233"

    # Operation 234: get
    assert tree.invariants(), "Invariants failed at step 234"

    # Operation 235: delete
    del tree[8510]
    del reference[8510]
    assert tree.invariants(), "Invariants failed at step 235"

    # Operation 236: insert
    tree[2190] = 'value_322122'
    reference[2190] = 'value_322122'
    assert tree.invariants(), "Invariants failed at step 236"

    # Operation 237: delete
    del tree[2190]
    del reference[2190]
    assert tree.invariants(), "Invariants failed at step 237"

    # Operation 238: insert
    tree[2318] = 'value_961640'
    reference[2318] = 'value_961640'
    assert tree.invariants(), "Invariants failed at step 238"

    # Operation 239: insert
    tree[3992] = 'value_780091'
    reference[3992] = 'value_780091'
    assert tree.invariants(), "Invariants failed at step 239"

    # Operation 240: get
    assert tree.invariants(), "Invariants failed at step 240"

    # Operation 241: delete
    del tree[3992]
    del reference[3992]
    assert tree.invariants(), "Invariants failed at step 241"

    # Operation 242: delete
    del tree[6005]
    del reference[6005]
    assert tree.invariants(), "Invariants failed at step 242"

    # Operation 243: update
    tree[2318] = 'value_527054'
    reference[2318] = 'value_527054'
    assert tree.invariants(), "Invariants failed at step 243"

    # Operation 244: delete
    del tree[7715]
    del reference[7715]
    assert tree.invariants(), "Invariants failed at step 244"

    # Operation 245: insert
    tree[6131] = 'value_40158'
    reference[6131] = 'value_40158'
    assert tree.invariants(), "Invariants failed at step 245"

    # Operation 246: insert
    tree[5795] = 'value_701017'
    reference[5795] = 'value_701017'
    assert tree.invariants(), "Invariants failed at step 246"

    # Operation 247: insert
    tree[8703] = 'value_924339'
    reference[8703] = 'value_924339'
    assert tree.invariants(), "Invariants failed at step 247"

    # Operation 248: get
    assert tree.invariants(), "Invariants failed at step 248"

    # Operation 249: get
    assert tree.invariants(), "Invariants failed at step 249"

    # Operation 250: delete
    del tree[8703]
    del reference[8703]
    assert tree.invariants(), "Invariants failed at step 250"

    # Operation 251: update
    tree[2318] = 'value_557641'
    reference[2318] = 'value_557641'
    assert tree.invariants(), "Invariants failed at step 251"

    # Operation 252: insert
    tree[6857] = 'value_594798'
    reference[6857] = 'value_594798'
    assert tree.invariants(), "Invariants failed at step 252"

    # Operation 253: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 253"

    # Operation 254: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 254"

    # Operation 255: insert
    tree[1759] = 'value_276975'
    reference[1759] = 'value_276975'
    assert tree.invariants(), "Invariants failed at step 255"

    # Operation 256: insert
    tree[4310] = 'value_830903'
    reference[4310] = 'value_830903'
    assert tree.invariants(), "Invariants failed at step 256"

    # Operation 257: get
    assert tree.invariants(), "Invariants failed at step 257"

    # Operation 258: update
    tree[1759] = 'value_705639'
    reference[1759] = 'value_705639'
    assert tree.invariants(), "Invariants failed at step 258"

    # Operation 259: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 259"

    # Operation 260: batch_delete
    keys_to_delete = [2318, 6131, 4310, 3065, 1950, 1759]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 260"

    # Operation 261: insert
    tree[3054] = 'value_119691'
    reference[3054] = 'value_119691'
    assert tree.invariants(), "Invariants failed at step 261"

    # Operation 262: get
    assert tree.invariants(), "Invariants failed at step 262"

    # Operation 263: batch_delete
    keys_to_delete = [8513, 878, 963, 3054]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 263"

    # Operation 264: insert
    tree[6991] = 'value_305485'
    reference[6991] = 'value_305485'
    assert tree.invariants(), "Invariants failed at step 264"

    # Operation 265: insert
    tree[7767] = 'value_876714'
    reference[7767] = 'value_876714'
    assert tree.invariants(), "Invariants failed at step 265"

    # Operation 266: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 266"

    # Operation 267: batch_delete
    keys_to_delete = [6857, 5421, 278, 6991]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 267"

    # Operation 268: get
    assert tree.invariants(), "Invariants failed at step 268"

    # Operation 269: delete
    del tree[5795]
    del reference[5795]
    assert tree.invariants(), "Invariants failed at step 269"

    # Operation 270: insert
    tree[2904] = 'value_878230'
    reference[2904] = 'value_878230'
    assert tree.invariants(), "Invariants failed at step 270"

    # Operation 271: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 271"

    # Operation 272: update
    tree[2904] = 'value_176721'
    reference[2904] = 'value_176721'
    assert tree.invariants(), "Invariants failed at step 272"

    # Operation 273: insert
    tree[3463] = 'value_9213'
    reference[3463] = 'value_9213'
    assert tree.invariants(), "Invariants failed at step 273"

    # Operation 274: get
    assert tree.invariants(), "Invariants failed at step 274"

    # Operation 275: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 275"

    # Operation 276: update
    tree[7767] = 'value_872418'
    reference[7767] = 'value_872418'
    assert tree.invariants(), "Invariants failed at step 276"

    # Operation 277: delete
    del tree[7767]
    del reference[7767]
    assert tree.invariants(), "Invariants failed at step 277"

    # Operation 278: update
    tree[3463] = 'value_325434'
    reference[3463] = 'value_325434'
    assert tree.invariants(), "Invariants failed at step 278"

    # Operation 279: insert
    tree[2594] = 'value_473516'
    reference[2594] = 'value_473516'
    assert tree.invariants(), "Invariants failed at step 279"

    # Operation 280: delete
    del tree[2904]
    del reference[2904]
    assert tree.invariants(), "Invariants failed at step 280"

    # Operation 281: delete
    del tree[2594]
    del reference[2594]
    assert tree.invariants(), "Invariants failed at step 281"

    # Operation 282: delete
    del tree[3463]
    del reference[3463]
    assert tree.invariants(), "Invariants failed at step 282"

    # Operation 283: get
    assert tree.invariants(), "Invariants failed at step 283"

    # Operation 284: insert
    tree[2274] = 'value_962098'
    reference[2274] = 'value_962098'
    assert tree.invariants(), "Invariants failed at step 284"

    # Operation 285: insert
    tree[8196] = 'value_792152'
    reference[8196] = 'value_792152'
    assert tree.invariants(), "Invariants failed at step 285"

    # Operation 286: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 286"

    # Operation 287: get
    assert tree.invariants(), "Invariants failed at step 287"

    # Operation 288: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 288"

    # Operation 289: delete
    del tree[8196]
    del reference[8196]
    assert tree.invariants(), "Invariants failed at step 289"

    # Operation 290: get
    assert tree.invariants(), "Invariants failed at step 290"

    # Operation 291: insert
    tree[5610] = 'value_313459'
    reference[5610] = 'value_313459'
    assert tree.invariants(), "Invariants failed at step 291"

    # Operation 292: update
    tree[5610] = 'value_583937'
    reference[5610] = 'value_583937'
    assert tree.invariants(), "Invariants failed at step 292"

    # Operation 293: get
    assert tree.invariants(), "Invariants failed at step 293"

    # Operation 294: insert
    tree[7516] = 'value_567084'
    reference[7516] = 'value_567084'
    assert tree.invariants(), "Invariants failed at step 294"

    # Operation 295: delete
    del tree[5362]
    del reference[5362]
    assert tree.invariants(), "Invariants failed at step 295"

    # Operation 296: update
    tree[2274] = 'value_666368'
    reference[2274] = 'value_666368'
    assert tree.invariants(), "Invariants failed at step 296"

    # Operation 297: delete
    del tree[2274]
    del reference[2274]
    assert tree.invariants(), "Invariants failed at step 297"

    # Operation 298: delete
    del tree[5610]
    del reference[5610]
    assert tree.invariants(), "Invariants failed at step 298"

    # Operation 299: get
    assert tree.invariants(), "Invariants failed at step 299"

    # Operation 300: insert
    tree[1936] = 'value_256003'
    reference[1936] = 'value_256003'
    assert tree.invariants(), "Invariants failed at step 300"

    # Operation 301: delete
    del tree[7516]
    del reference[7516]
    assert tree.invariants(), "Invariants failed at step 301"

    # Operation 302: insert
    tree[5811] = 'value_292376'
    reference[5811] = 'value_292376'
    assert tree.invariants(), "Invariants failed at step 302"

    # Operation 303: get
    assert tree.invariants(), "Invariants failed at step 303"

    # Operation 304: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 304"

    # Operation 305: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 305"

    # Operation 306: update
    tree[5811] = 'value_263346'
    reference[5811] = 'value_263346'
    assert tree.invariants(), "Invariants failed at step 306"

    # Operation 307: get
    assert tree.invariants(), "Invariants failed at step 307"

    # Operation 308: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 308"

    # Operation 309: insert
    tree[3240] = 'value_645305'
    reference[3240] = 'value_645305'
    assert tree.invariants(), "Invariants failed at step 309"

    # Operation 310: delete
    del tree[3240]
    del reference[3240]
    assert tree.invariants(), "Invariants failed at step 310"

    # Operation 311: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 311"

    # Operation 312: get
    assert tree.invariants(), "Invariants failed at step 312"

    # Operation 313: insert
    tree[861] = 'value_191770'
    reference[861] = 'value_191770'
    assert tree.invariants(), "Invariants failed at step 313"

    # Operation 314: delete
    del tree[1936]
    del reference[1936]
    assert tree.invariants(), "Invariants failed at step 314"

    # Operation 315: delete
    del tree[5811]
    del reference[5811]
    assert tree.invariants(), "Invariants failed at step 315"

    # Operation 316: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 316"

    # Operation 317: update
    tree[861] = 'value_367295'
    reference[861] = 'value_367295'
    assert tree.invariants(), "Invariants failed at step 317"

    # Operation 318: delete
    del tree[861]
    del reference[861]
    assert tree.invariants(), "Invariants failed at step 318"

    # Operation 319: insert
    tree[5536] = 'value_566957'
    reference[5536] = 'value_566957'
    assert tree.invariants(), "Invariants failed at step 319"

    # Operation 320: insert
    tree[2813] = 'value_98548'
    reference[2813] = 'value_98548'
    assert tree.invariants(), "Invariants failed at step 320"

    # Operation 321: insert
    tree[9602] = 'value_657797'
    reference[9602] = 'value_657797'
    assert tree.invariants(), "Invariants failed at step 321"

    # Operation 322: delete
    del tree[9602]
    del reference[9602]
    assert tree.invariants(), "Invariants failed at step 322"

    # Operation 323: get
    assert tree.invariants(), "Invariants failed at step 323"

    # Operation 324: insert
    tree[4629] = 'value_832359'
    reference[4629] = 'value_832359'
    assert tree.invariants(), "Invariants failed at step 324"

    # Operation 325: get
    assert tree.invariants(), "Invariants failed at step 325"

    # Operation 326: delete
    del tree[5536]
    del reference[5536]
    assert tree.invariants(), "Invariants failed at step 326"

    # Operation 327: update
    tree[2813] = 'value_474205'
    reference[2813] = 'value_474205'
    assert tree.invariants(), "Invariants failed at step 327"

    # Operation 328: insert
    tree[1707] = 'value_739170'
    reference[1707] = 'value_739170'
    assert tree.invariants(), "Invariants failed at step 328"

    # Operation 329: delete
    del tree[4629]
    del reference[4629]
    assert tree.invariants(), "Invariants failed at step 329"

    # Operation 330: insert
    tree[3431] = 'value_303916'
    reference[3431] = 'value_303916'
    assert tree.invariants(), "Invariants failed at step 330"

    # Operation 331: get
    assert tree.invariants(), "Invariants failed at step 331"

    # Operation 332: get
    assert tree.invariants(), "Invariants failed at step 332"

    # Operation 333: insert
    tree[6061] = 'value_283311'
    reference[6061] = 'value_283311'
    assert tree.invariants(), "Invariants failed at step 333"

    # Operation 334: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 334"

    # Operation 335: delete
    del tree[3431]
    del reference[3431]
    assert tree.invariants(), "Invariants failed at step 335"

    # Operation 336: insert
    tree[7187] = 'value_152292'
    reference[7187] = 'value_152292'
    assert tree.invariants(), "Invariants failed at step 336"

    # Operation 337: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 337"

    # Operation 338: get
    assert tree.invariants(), "Invariants failed at step 338"

    # Operation 339: delete
    del tree[6061]
    del reference[6061]
    assert tree.invariants(), "Invariants failed at step 339"

    # Operation 340: delete
    del tree[1707]
    del reference[1707]
    assert tree.invariants(), "Invariants failed at step 340"

    # Operation 341: delete
    del tree[2813]
    del reference[2813]
    assert tree.invariants(), "Invariants failed at step 341"

    # Operation 342: insert
    tree[5012] = 'value_871765'
    reference[5012] = 'value_871765'
    assert tree.invariants(), "Invariants failed at step 342"

    # Operation 343: delete
    del tree[7187]
    del reference[7187]
    assert tree.invariants(), "Invariants failed at step 343"

    # Operation 344: get
    assert tree.invariants(), "Invariants failed at step 344"

    # Operation 345: insert
    tree[4957] = 'value_792348'
    reference[4957] = 'value_792348'
    assert tree.invariants(), "Invariants failed at step 345"

    # Operation 346: update
    tree[5012] = 'value_950257'
    reference[5012] = 'value_950257'
    assert tree.invariants(), "Invariants failed at step 346"

    # Operation 347: insert
    tree[8120] = 'value_707092'
    reference[8120] = 'value_707092'
    assert tree.invariants(), "Invariants failed at step 347"

    # Operation 348: insert
    tree[3265] = 'value_979443'
    reference[3265] = 'value_979443'
    assert tree.invariants(), "Invariants failed at step 348"

    # Operation 349: update
    tree[3265] = 'value_154924'
    reference[3265] = 'value_154924'
    assert tree.invariants(), "Invariants failed at step 349"

    # Operation 350: get
    assert tree.invariants(), "Invariants failed at step 350"

    # Operation 351: get
    assert tree.invariants(), "Invariants failed at step 351"

    # Operation 352: update
    tree[8120] = 'value_340526'
    reference[8120] = 'value_340526'
    assert tree.invariants(), "Invariants failed at step 352"

    # Operation 353: insert
    tree[2186] = 'value_93766'
    reference[2186] = 'value_93766'
    assert tree.invariants(), "Invariants failed at step 353"

    # Operation 354: get
    assert tree.invariants(), "Invariants failed at step 354"

    # Operation 355: get
    assert tree.invariants(), "Invariants failed at step 355"

    # Operation 356: get
    assert tree.invariants(), "Invariants failed at step 356"

    # Operation 357: insert
    tree[5198] = 'value_916125'
    reference[5198] = 'value_916125'
    assert tree.invariants(), "Invariants failed at step 357"

    # Operation 358: insert
    tree[4700] = 'value_323113'
    reference[4700] = 'value_323113'
    assert tree.invariants(), "Invariants failed at step 358"

    # Operation 359: insert
    tree[1821] = 'value_609499'
    reference[1821] = 'value_609499'
    assert tree.invariants(), "Invariants failed at step 359"

    # Operation 360: update
    tree[4957] = 'value_919412'
    reference[4957] = 'value_919412'
    assert tree.invariants(), "Invariants failed at step 360"

    # Operation 361: insert
    tree[7584] = 'value_524684'
    reference[7584] = 'value_524684'
    assert tree.invariants(), "Invariants failed at step 361"

    # Operation 362: insert
    tree[7249] = 'value_834164'
    reference[7249] = 'value_834164'
    assert tree.invariants(), "Invariants failed at step 362"

    # Operation 363: delete
    del tree[7249]
    del reference[7249]
    assert tree.invariants(), "Invariants failed at step 363"

    # Operation 364: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 364"

    # Operation 365: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 365"

    # Operation 366: delete
    del tree[4700]
    del reference[4700]
    assert tree.invariants(), "Invariants failed at step 366"

    # Operation 367: insert
    tree[8585] = 'value_505269'
    reference[8585] = 'value_505269'
    assert tree.invariants(), "Invariants failed at step 367"

    # Operation 368: batch_delete
    keys_to_delete = [3265, 8585, 5012, 6262, 8120, 6043]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 368"

    # Operation 369: insert
    tree[1409] = 'value_33982'
    reference[1409] = 'value_33982'
    assert tree.invariants(), "Invariants failed at step 369"

    # Operation 370: insert
    tree[6289] = 'value_692271'
    reference[6289] = 'value_692271'
    assert tree.invariants(), "Invariants failed at step 370"

    # Operation 371: get
    assert tree.invariants(), "Invariants failed at step 371"

    # Operation 372: insert
    tree[5876] = 'value_893264'
    reference[5876] = 'value_893264'
    assert tree.invariants(), "Invariants failed at step 372"

    # Operation 373: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 373"

    # Operation 374: get
    assert tree.invariants(), "Invariants failed at step 374"

    # Operation 375: update
    tree[1821] = 'value_263684'
    reference[1821] = 'value_263684'
    assert tree.invariants(), "Invariants failed at step 375"

    # Operation 376: delete
    del tree[1821]
    del reference[1821]
    assert tree.invariants(), "Invariants failed at step 376"

    # Operation 377: get
    assert tree.invariants(), "Invariants failed at step 377"

    # Operation 378: get
    assert tree.invariants(), "Invariants failed at step 378"

    # Operation 379: get
    assert tree.invariants(), "Invariants failed at step 379"

    # Operation 380: update
    tree[6289] = 'value_123551'
    reference[6289] = 'value_123551'
    assert tree.invariants(), "Invariants failed at step 380"

    # Operation 381: update
    tree[4957] = 'value_914944'
    reference[4957] = 'value_914944'
    assert tree.invariants(), "Invariants failed at step 381"

    # Operation 382: get
    assert tree.invariants(), "Invariants failed at step 382"

    # Operation 383: delete
    del tree[6289]
    del reference[6289]
    assert tree.invariants(), "Invariants failed at step 383"

    # Operation 384: insert
    tree[8775] = 'value_920515'
    reference[8775] = 'value_920515'
    assert tree.invariants(), "Invariants failed at step 384"

    # Operation 385: update
    tree[5198] = 'value_247333'
    reference[5198] = 'value_247333'
    assert tree.invariants(), "Invariants failed at step 385"

    # Operation 386: update
    tree[1409] = 'value_615694'
    reference[1409] = 'value_615694'
    assert tree.invariants(), "Invariants failed at step 386"

    # Operation 387: insert
    tree[6820] = 'value_928860'
    reference[6820] = 'value_928860'
    assert tree.invariants(), "Invariants failed at step 387"

    # Operation 388: insert
    tree[1271] = 'value_242571'
    reference[1271] = 'value_242571'
    assert tree.invariants(), "Invariants failed at step 388"

    # Operation 389: insert
    tree[178] = 'value_139809'
    reference[178] = 'value_139809'
    assert tree.invariants(), "Invariants failed at step 389"

    # Operation 390: insert
    tree[8851] = 'value_453289'
    reference[8851] = 'value_453289'
    assert tree.invariants(), "Invariants failed at step 390"

    # Operation 391: get
    assert tree.invariants(), "Invariants failed at step 391"

    # Operation 392: insert
    tree[182] = 'value_410860'
    reference[182] = 'value_410860'
    assert tree.invariants(), "Invariants failed at step 392"

    # Operation 393: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 393"

    # Operation 394: insert
    tree[8332] = 'value_550140'
    reference[8332] = 'value_550140'
    assert tree.invariants(), "Invariants failed at step 394"

    # Operation 395: delete
    del tree[6820]
    del reference[6820]
    assert tree.invariants(), "Invariants failed at step 395"

    # Operation 396: delete
    del tree[182]
    del reference[182]
    assert tree.invariants(), "Invariants failed at step 396"

    # Operation 397: get
    assert tree.invariants(), "Invariants failed at step 397"

    # Operation 398: update
    tree[2186] = 'value_161832'
    reference[2186] = 'value_161832'
    assert tree.invariants(), "Invariants failed at step 398"

    # Operation 399: delete
    del tree[2186]
    del reference[2186]
    assert tree.invariants(), "Invariants failed at step 399"

    # Operation 400: insert
    tree[7578] = 'value_688690'
    reference[7578] = 'value_688690'
    assert tree.invariants(), "Invariants failed at step 400"

    # Operation 401: delete
    del tree[7584]
    del reference[7584]
    assert tree.invariants(), "Invariants failed at step 401"

    # Operation 402: get
    assert tree.invariants(), "Invariants failed at step 402"

    # Operation 403: insert
    tree[6851] = 'value_979111'
    reference[6851] = 'value_979111'
    assert tree.invariants(), "Invariants failed at step 403"

    # Operation 404: delete
    del tree[7578]
    del reference[7578]
    assert tree.invariants(), "Invariants failed at step 404"

    # Operation 405: insert
    tree[7172] = 'value_915058'
    reference[7172] = 'value_915058'
    assert tree.invariants(), "Invariants failed at step 405"

    # Operation 406: batch_delete
    keys_to_delete = [5198, 178, 5876, 6775, 1150]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 406"

    # Operation 407: get
    assert tree.invariants(), "Invariants failed at step 407"

    # Operation 408: update
    tree[1409] = 'value_615081'
    reference[1409] = 'value_615081'
    assert tree.invariants(), "Invariants failed at step 408"

    # Operation 409: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 409"

    # Operation 410: insert
    tree[3013] = 'value_522606'
    reference[3013] = 'value_522606'
    assert tree.invariants(), "Invariants failed at step 410"

    # Operation 411: get
    assert tree.invariants(), "Invariants failed at step 411"

    # Operation 412: insert
    tree[2147] = 'value_108563'
    reference[2147] = 'value_108563'
    assert tree.invariants(), "Invariants failed at step 412"

    # Operation 413: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 413"

    # Operation 414: insert
    tree[6185] = 'value_856855'
    reference[6185] = 'value_856855'
    assert tree.invariants(), "Invariants failed at step 414"

    # Operation 415: batch_delete
    keys_to_delete = [2147, 7172, 3013, 6851, 3886, 1271, 4218]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 415"

    # Operation 416: delete
    del tree[4957]
    del reference[4957]
    assert tree.invariants(), "Invariants failed at step 416"

    # Operation 417: update
    tree[8332] = 'value_184247'
    reference[8332] = 'value_184247'
    assert tree.invariants(), "Invariants failed at step 417"

    # Operation 418: delete
    del tree[1409]
    del reference[1409]
    assert tree.invariants(), "Invariants failed at step 418"

    # Operation 419: get
    assert tree.invariants(), "Invariants failed at step 419"

    # Operation 420: insert
    tree[6655] = 'value_281036'
    reference[6655] = 'value_281036'
    assert tree.invariants(), "Invariants failed at step 420"

    # Operation 421: insert
    tree[1934] = 'value_576327'
    reference[1934] = 'value_576327'
    assert tree.invariants(), "Invariants failed at step 421"

    # Operation 422: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 422"

    # Operation 423: update
    tree[6655] = 'value_553208'
    reference[6655] = 'value_553208'
    assert tree.invariants(), "Invariants failed at step 423"

    # Operation 424: insert
    tree[360] = 'value_417426'
    reference[360] = 'value_417426'
    assert tree.invariants(), "Invariants failed at step 424"

    # Operation 425: update
    tree[360] = 'value_476226'
    reference[360] = 'value_476226'
    assert tree.invariants(), "Invariants failed at step 425"

    # Operation 426: insert
    tree[3122] = 'value_28824'
    reference[3122] = 'value_28824'
    assert tree.invariants(), "Invariants failed at step 426"

    # Operation 427: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 427"

    # Operation 428: update
    tree[6655] = 'value_562109'
    reference[6655] = 'value_562109'
    assert tree.invariants(), "Invariants failed at step 428"

    # Operation 429: get
    assert tree.invariants(), "Invariants failed at step 429"

    # Operation 430: delete
    del tree[8851]
    del reference[8851]
    assert tree.invariants(), "Invariants failed at step 430"

    # Operation 431: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 431"

    # Operation 432: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 432"

    # Operation 433: insert
    tree[7053] = 'value_760313'
    reference[7053] = 'value_760313'
    assert tree.invariants(), "Invariants failed at step 433"

    # Operation 434: update
    tree[6185] = 'value_162070'
    reference[6185] = 'value_162070'
    assert tree.invariants(), "Invariants failed at step 434"

    # Operation 435: delete
    del tree[8775]
    del reference[8775]
    assert tree.invariants(), "Invariants failed at step 435"

    # Operation 436: insert
    tree[8517] = 'value_832044'
    reference[8517] = 'value_832044'
    assert tree.invariants(), "Invariants failed at step 436"

    # Operation 437: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 437"

    # Operation 438: insert
    tree[153] = 'value_243957'
    reference[153] = 'value_243957'
    assert tree.invariants(), "Invariants failed at step 438"

    # Operation 439: delete
    del tree[7053]
    del reference[7053]
    assert tree.invariants(), "Invariants failed at step 439"

    # Operation 440: insert
    tree[9323] = 'value_812126'
    reference[9323] = 'value_812126'
    assert tree.invariants(), "Invariants failed at step 440"

    # Operation 441: delete
    del tree[6655]
    del reference[6655]
    assert tree.invariants(), "Invariants failed at step 441"

    # Operation 442: update
    tree[6185] = 'value_598833'
    reference[6185] = 'value_598833'
    assert tree.invariants(), "Invariants failed at step 442"

    # Operation 443: insert
    tree[7869] = 'value_118663'
    reference[7869] = 'value_118663'
    assert tree.invariants(), "Invariants failed at step 443"

    # Operation 444: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 444"

    # Operation 445: insert
    tree[6812] = 'value_58543'
    reference[6812] = 'value_58543'
    assert tree.invariants(), "Invariants failed at step 445"

    # Operation 446: insert
    tree[7065] = 'value_80419'
    reference[7065] = 'value_80419'
    assert tree.invariants(), "Invariants failed at step 446"

    # Operation 447: get
    assert tree.invariants(), "Invariants failed at step 447"

    # Operation 448: get
    assert tree.invariants(), "Invariants failed at step 448"

    # Operation 449: insert
    tree[3565] = 'value_872355'
    reference[3565] = 'value_872355'
    assert tree.invariants(), "Invariants failed at step 449"

    # Operation 450: insert
    tree[2683] = 'value_196348'
    reference[2683] = 'value_196348'
    assert tree.invariants(), "Invariants failed at step 450"

    # Operation 451: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 451"

    # Operation 452: get
    assert tree.invariants(), "Invariants failed at step 452"

    # Operation 453: delete
    del tree[7065]
    del reference[7065]
    assert tree.invariants(), "Invariants failed at step 453"

    # Operation 454: delete
    del tree[7869]
    del reference[7869]
    assert tree.invariants(), "Invariants failed at step 454"

    # Operation 455: delete
    del tree[3565]
    del reference[3565]
    assert tree.invariants(), "Invariants failed at step 455"

    # Operation 456: batch_delete
    keys_to_delete = [8517, 360, 9323, 8332, 5709, 1934, 3052]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 456"

    # Operation 457: insert
    tree[2926] = 'value_637827'
    reference[2926] = 'value_637827'
    assert tree.invariants(), "Invariants failed at step 457"

    # Operation 458: batch_delete
    keys_to_delete = [4674, 6185, 3122, 2683, 1309]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 458"

    # Operation 459: get
    assert tree.invariants(), "Invariants failed at step 459"

    # Operation 460: delete
    del tree[2926]
    del reference[2926]
    assert tree.invariants(), "Invariants failed at step 460"

    # Operation 461: get
    assert tree.invariants(), "Invariants failed at step 461"

    # Operation 462: delete
    del tree[153]
    del reference[153]
    assert tree.invariants(), "Invariants failed at step 462"

    # Operation 463: insert
    tree[4213] = 'value_931191'
    reference[4213] = 'value_931191'
    assert tree.invariants(), "Invariants failed at step 463"

    # Operation 464: delete
    del tree[4213]
    del reference[4213]
    assert tree.invariants(), "Invariants failed at step 464"

    # Operation 465: delete
    del tree[6812]
    del reference[6812]
    assert tree.invariants(), "Invariants failed at step 465"

    # Operation 466: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 466"

    # Operation 467: insert
    tree[7299] = 'value_826845'
    reference[7299] = 'value_826845'
    assert tree.invariants(), "Invariants failed at step 467"

    # Operation 468: insert
    tree[3814] = 'value_846873'
    reference[3814] = 'value_846873'
    assert tree.invariants(), "Invariants failed at step 468"

    # Operation 469: insert
    tree[5900] = 'value_841533'
    reference[5900] = 'value_841533'
    assert tree.invariants(), "Invariants failed at step 469"

    # Operation 470: get
    assert tree.invariants(), "Invariants failed at step 470"

    # Operation 471: insert
    tree[6587] = 'value_379941'
    reference[6587] = 'value_379941'
    assert tree.invariants(), "Invariants failed at step 471"

    # Operation 472: delete
    del tree[6587]
    del reference[6587]
    assert tree.invariants(), "Invariants failed at step 472"

    # Operation 473: delete
    del tree[3814]
    del reference[3814]
    assert tree.invariants(), "Invariants failed at step 473"

    # Operation 474: update
    tree[7299] = 'value_909879'
    reference[7299] = 'value_909879'
    assert tree.invariants(), "Invariants failed at step 474"

    # Operation 475: insert
    tree[1250] = 'value_626871'
    reference[1250] = 'value_626871'
    assert tree.invariants(), "Invariants failed at step 475"

    # Operation 476: get
    assert tree.invariants(), "Invariants failed at step 476"

    # Operation 477: get
    assert tree.invariants(), "Invariants failed at step 477"

    # Operation 478: update
    tree[7299] = 'value_166806'
    reference[7299] = 'value_166806'
    assert tree.invariants(), "Invariants failed at step 478"

    # Operation 479: update
    tree[1250] = 'value_602571'
    reference[1250] = 'value_602571'
    assert tree.invariants(), "Invariants failed at step 479"

    # Operation 480: update
    tree[1250] = 'value_290014'
    reference[1250] = 'value_290014'
    assert tree.invariants(), "Invariants failed at step 480"

    # Operation 481: delete
    del tree[1250]
    del reference[1250]
    assert tree.invariants(), "Invariants failed at step 481"

    # Operation 482: insert
    tree[1148] = 'value_907813'
    reference[1148] = 'value_907813'
    assert tree.invariants(), "Invariants failed at step 482"

    # Operation 483: get
    assert tree.invariants(), "Invariants failed at step 483"

    # Operation 484: update
    tree[7299] = 'value_763660'
    reference[7299] = 'value_763660'
    assert tree.invariants(), "Invariants failed at step 484"

    # Operation 485: delete
    del tree[1148]
    del reference[1148]
    assert tree.invariants(), "Invariants failed at step 485"

    # Operation 486: insert
    tree[5869] = 'value_81549'
    reference[5869] = 'value_81549'
    assert tree.invariants(), "Invariants failed at step 486"

    # Operation 487: delete
    del tree[7299]
    del reference[7299]
    assert tree.invariants(), "Invariants failed at step 487"

    # Operation 488: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 488"

    # Operation 489: insert
    tree[6281] = 'value_956986'
    reference[6281] = 'value_956986'
    assert tree.invariants(), "Invariants failed at step 489"

    # Operation 490: delete
    del tree[5900]
    del reference[5900]
    assert tree.invariants(), "Invariants failed at step 490"

    # Operation 491: insert
    tree[7448] = 'value_508472'
    reference[7448] = 'value_508472'
    assert tree.invariants(), "Invariants failed at step 491"

    # Operation 492: delete
    del tree[6281]
    del reference[6281]
    assert tree.invariants(), "Invariants failed at step 492"

    # Operation 493: insert
    tree[8826] = 'value_456480'
    reference[8826] = 'value_456480'
    assert tree.invariants(), "Invariants failed at step 493"

    # Operation 494: update
    tree[8826] = 'value_578935'
    reference[8826] = 'value_578935'
    assert tree.invariants(), "Invariants failed at step 494"

    # Operation 495: insert
    tree[8631] = 'value_245928'
    reference[8631] = 'value_245928'
    assert tree.invariants(), "Invariants failed at step 495"

    # Operation 496: insert
    tree[9423] = 'value_14305'
    reference[9423] = 'value_14305'
    assert tree.invariants(), "Invariants failed at step 496"

    # Operation 497: insert
    tree[536] = 'value_271599'
    reference[536] = 'value_271599'
    assert tree.invariants(), "Invariants failed at step 497"

    # Operation 498: delete
    del tree[8631]
    del reference[8631]
    assert tree.invariants(), "Invariants failed at step 498"

    # Operation 499: get
    assert tree.invariants(), "Invariants failed at step 499"

    # Operation 500: get
    assert tree.invariants(), "Invariants failed at step 500"

    # Operation 501: insert
    tree[7510] = 'value_430935'
    reference[7510] = 'value_430935'
    assert tree.invariants(), "Invariants failed at step 501"

    # Operation 502: get
    assert tree.invariants(), "Invariants failed at step 502"

    # Operation 503: get
    assert tree.invariants(), "Invariants failed at step 503"

    # Operation 504: update
    tree[7448] = 'value_458443'
    reference[7448] = 'value_458443'
    assert tree.invariants(), "Invariants failed at step 504"

    # Operation 505: get
    assert tree.invariants(), "Invariants failed at step 505"

    # Operation 506: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 506"

    # Operation 507: get
    assert tree.invariants(), "Invariants failed at step 507"

    # Operation 508: delete
    del tree[7448]
    del reference[7448]
    assert tree.invariants(), "Invariants failed at step 508"

    # Operation 509: insert
    tree[6824] = 'value_311826'
    reference[6824] = 'value_311826'
    assert tree.invariants(), "Invariants failed at step 509"

    # Operation 510: get
    assert tree.invariants(), "Invariants failed at step 510"

    # Operation 511: delete
    del tree[536]
    del reference[536]
    assert tree.invariants(), "Invariants failed at step 511"

    # Operation 512: insert
    tree[9364] = 'value_673249'
    reference[9364] = 'value_673249'
    assert tree.invariants(), "Invariants failed at step 512"

    # Operation 513: delete
    del tree[8826]
    del reference[8826]
    assert tree.invariants(), "Invariants failed at step 513"

    # Operation 514: insert
    tree[4150] = 'value_151371'
    reference[4150] = 'value_151371'
    assert tree.invariants(), "Invariants failed at step 514"

    # Operation 515: batch_delete
    keys_to_delete = [6824, 4427, 7510, 4150, 5142]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 515"

    # Operation 516: delete
    del tree[5869]
    del reference[5869]
    assert tree.invariants(), "Invariants failed at step 516"

    # Operation 517: delete
    del tree[9423]
    del reference[9423]
    assert tree.invariants(), "Invariants failed at step 517"

    # Operation 518: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 518"

    # Operation 519: insert
    tree[8525] = 'value_272137'
    reference[8525] = 'value_272137'
    assert tree.invariants(), "Invariants failed at step 519"

    # Operation 520: insert
    tree[256] = 'value_780029'
    reference[256] = 'value_780029'
    assert tree.invariants(), "Invariants failed at step 520"

    # Operation 521: get
    assert tree.invariants(), "Invariants failed at step 521"

    # Operation 522: update
    tree[256] = 'value_733851'
    reference[256] = 'value_733851'
    assert tree.invariants(), "Invariants failed at step 522"

    # Operation 523: insert
    tree[2753] = 'value_39963'
    reference[2753] = 'value_39963'
    assert tree.invariants(), "Invariants failed at step 523"

    # Operation 524: insert
    tree[504] = 'value_539886'
    reference[504] = 'value_539886'
    assert tree.invariants(), "Invariants failed at step 524"

    # Operation 525: update
    tree[256] = 'value_423301'
    reference[256] = 'value_423301'
    assert tree.invariants(), "Invariants failed at step 525"

    # Operation 526: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 526"

    # Operation 527: insert
    tree[8433] = 'value_351571'
    reference[8433] = 'value_351571'
    assert tree.invariants(), "Invariants failed at step 527"

    # Operation 528: insert
    tree[4753] = 'value_16615'
    reference[4753] = 'value_16615'
    assert tree.invariants(), "Invariants failed at step 528"

    # Operation 529: insert
    tree[5515] = 'value_856965'
    reference[5515] = 'value_856965'
    assert tree.invariants(), "Invariants failed at step 529"

    # Operation 530: insert
    tree[4528] = 'value_148872'
    reference[4528] = 'value_148872'
    assert tree.invariants(), "Invariants failed at step 530"

    # Operation 531: update
    tree[4528] = 'value_951342'
    reference[4528] = 'value_951342'
    assert tree.invariants(), "Invariants failed at step 531"

    # Operation 532: delete
    del tree[9364]
    del reference[9364]
    assert tree.invariants(), "Invariants failed at step 532"

    # Operation 533: get
    assert tree.invariants(), "Invariants failed at step 533"

    # Operation 534: batch_delete
    keys_to_delete = [2753, 74, 7116, 8525]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 534"

    # Operation 535: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 535"

    # Operation 536: insert
    tree[3780] = 'value_976406'
    reference[3780] = 'value_976406'
    assert tree.invariants(), "Invariants failed at step 536"

    # Operation 537: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 537"

    # Operation 538: update
    tree[256] = 'value_344258'
    reference[256] = 'value_344258'
    assert tree.invariants(), "Invariants failed at step 538"

    # Operation 539: get
    assert tree.invariants(), "Invariants failed at step 539"

    # Operation 540: insert
    tree[3675] = 'value_45241'
    reference[3675] = 'value_45241'
    assert tree.invariants(), "Invariants failed at step 540"

    # Operation 541: delete
    del tree[4528]
    del reference[4528]
    assert tree.invariants(), "Invariants failed at step 541"

    # Operation 542: get
    assert tree.invariants(), "Invariants failed at step 542"

    # Operation 543: insert
    tree[7580] = 'value_87129'
    reference[7580] = 'value_87129'
    assert tree.invariants(), "Invariants failed at step 543"

    # Operation 544: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 544"

    # Operation 545: get
    assert tree.invariants(), "Invariants failed at step 545"

    # Operation 546: get
    assert tree.invariants(), "Invariants failed at step 546"

    # Operation 547: insert
    tree[9943] = 'value_832350'
    reference[9943] = 'value_832350'
    assert tree.invariants(), "Invariants failed at step 547"

    # Operation 548: update
    tree[3675] = 'value_594064'
    reference[3675] = 'value_594064'
    assert tree.invariants(), "Invariants failed at step 548"

    # Operation 549: get
    assert tree.invariants(), "Invariants failed at step 549"

    # Operation 550: get
    assert tree.invariants(), "Invariants failed at step 550"

    # Operation 551: get
    assert tree.invariants(), "Invariants failed at step 551"

    # Operation 552: delete
    del tree[504]
    del reference[504]
    assert tree.invariants(), "Invariants failed at step 552"

    # Operation 553: update
    tree[4753] = 'value_408730'
    reference[4753] = 'value_408730'
    assert tree.invariants(), "Invariants failed at step 553"

    # Operation 554: update
    tree[9943] = 'value_690104'
    reference[9943] = 'value_690104'
    assert tree.invariants(), "Invariants failed at step 554"

    # Operation 555: get
    assert tree.invariants(), "Invariants failed at step 555"

    # Operation 556: insert
    tree[2157] = 'value_696886'
    reference[2157] = 'value_696886'
    assert tree.invariants(), "Invariants failed at step 556"

    # Operation 557: delete
    del tree[7580]
    del reference[7580]
    assert tree.invariants(), "Invariants failed at step 557"

    # Operation 558: delete
    del tree[256]
    del reference[256]
    assert tree.invariants(), "Invariants failed at step 558"

    # Operation 559: update
    tree[9943] = 'value_279662'
    reference[9943] = 'value_279662'
    assert tree.invariants(), "Invariants failed at step 559"

    # Operation 560: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 560"

    # Operation 561: update
    tree[5515] = 'value_208831'
    reference[5515] = 'value_208831'
    assert tree.invariants(), "Invariants failed at step 561"

    # Operation 562: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 562"

    # Operation 563: insert
    tree[802] = 'value_500344'
    reference[802] = 'value_500344'
    assert tree.invariants(), "Invariants failed at step 563"

    # Operation 564: insert
    tree[3021] = 'value_560961'
    reference[3021] = 'value_560961'
    assert tree.invariants(), "Invariants failed at step 564"

    # Operation 565: delete
    del tree[5515]
    del reference[5515]
    assert tree.invariants(), "Invariants failed at step 565"

    # Operation 566: update
    tree[3675] = 'value_869125'
    reference[3675] = 'value_869125'
    assert tree.invariants(), "Invariants failed at step 566"

    # Operation 567: delete
    del tree[8433]
    del reference[8433]
    assert tree.invariants(), "Invariants failed at step 567"

    # Operation 568: delete
    del tree[3021]
    del reference[3021]
    assert tree.invariants(), "Invariants failed at step 568"

    # Operation 569: get
    assert tree.invariants(), "Invariants failed at step 569"

    # Operation 570: get
    assert tree.invariants(), "Invariants failed at step 570"

    # Operation 571: insert
    tree[2932] = 'value_704661'
    reference[2932] = 'value_704661'
    assert tree.invariants(), "Invariants failed at step 571"

    # Operation 572: get
    assert tree.invariants(), "Invariants failed at step 572"

    # Operation 573: delete
    del tree[802]
    del reference[802]
    assert tree.invariants(), "Invariants failed at step 573"

    # Operation 574: insert
    tree[6975] = 'value_951377'
    reference[6975] = 'value_951377'
    assert tree.invariants(), "Invariants failed at step 574"

    # Operation 575: update
    tree[3675] = 'value_547823'
    reference[3675] = 'value_547823'
    assert tree.invariants(), "Invariants failed at step 575"

    # Operation 576: delete
    del tree[2157]
    del reference[2157]
    assert tree.invariants(), "Invariants failed at step 576"

    # Operation 577: update
    tree[3675] = 'value_541373'
    reference[3675] = 'value_541373'
    assert tree.invariants(), "Invariants failed at step 577"

    # Operation 578: delete
    del tree[3675]
    del reference[3675]
    assert tree.invariants(), "Invariants failed at step 578"

    # Operation 579: insert
    tree[1042] = 'value_314661'
    reference[1042] = 'value_314661'
    assert tree.invariants(), "Invariants failed at step 579"

    # Operation 580: delete
    del tree[3780]
    del reference[3780]
    assert tree.invariants(), "Invariants failed at step 580"

    # Operation 581: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 581"

    # Operation 582: insert
    tree[6041] = 'value_272312'
    reference[6041] = 'value_272312'
    assert tree.invariants(), "Invariants failed at step 582"

    # Operation 583: insert
    tree[3240] = 'value_113640'
    reference[3240] = 'value_113640'
    assert tree.invariants(), "Invariants failed at step 583"

    # Operation 584: batch_delete
    keys_to_delete = [3290, 1042, 8948, 6975]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 584"

    # Operation 585: delete
    del tree[2932]
    del reference[2932]
    assert tree.invariants(), "Invariants failed at step 585"

    # Operation 586: delete
    del tree[3240]
    del reference[3240]
    assert tree.invariants(), "Invariants failed at step 586"

    # Operation 587: insert
    tree[8511] = 'value_700689'
    reference[8511] = 'value_700689'
    assert tree.invariants(), "Invariants failed at step 587"

    # Operation 588: delete
    del tree[6041]
    del reference[6041]
    assert tree.invariants(), "Invariants failed at step 588"

    # Operation 589: delete
    del tree[8511]
    del reference[8511]
    assert tree.invariants(), "Invariants failed at step 589"

    # Operation 590: get
    assert tree.invariants(), "Invariants failed at step 590"

    # Operation 591: insert
    tree[6904] = 'value_711893'
    reference[6904] = 'value_711893'
    assert tree.invariants(), "Invariants failed at step 591"

    # Operation 592: delete
    del tree[9943]
    del reference[9943]
    assert tree.invariants(), "Invariants failed at step 592"

    # Operation 593: insert
    tree[6910] = 'value_420553'
    reference[6910] = 'value_420553'
    assert tree.invariants(), "Invariants failed at step 593"

    # Operation 594: insert
    tree[3363] = 'value_663050'
    reference[3363] = 'value_663050'
    assert tree.invariants(), "Invariants failed at step 594"

    # Operation 595: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 595"

    # Operation 596: get
    assert tree.invariants(), "Invariants failed at step 596"

    # Operation 597: insert
    tree[2884] = 'value_556041'
    reference[2884] = 'value_556041'
    assert tree.invariants(), "Invariants failed at step 597"

    # Operation 598: insert
    tree[9669] = 'value_451267'
    reference[9669] = 'value_451267'
    assert tree.invariants(), "Invariants failed at step 598"

    # Operation 599: update
    tree[2884] = 'value_809696'
    reference[2884] = 'value_809696'
    assert tree.invariants(), "Invariants failed at step 599"

    # Operation 600: update
    tree[4753] = 'value_159441'
    reference[4753] = 'value_159441'
    assert tree.invariants(), "Invariants failed at step 600"

    # Operation 601: delete
    del tree[6910]
    del reference[6910]
    assert tree.invariants(), "Invariants failed at step 601"

    # Operation 602: insert
    tree[6963] = 'value_521102'
    reference[6963] = 'value_521102'
    assert tree.invariants(), "Invariants failed at step 602"

    # Operation 603: insert
    tree[4877] = 'value_687698'
    reference[4877] = 'value_687698'
    assert tree.invariants(), "Invariants failed at step 603"

    # Operation 604: update
    tree[6963] = 'value_106433'
    reference[6963] = 'value_106433'
    assert tree.invariants(), "Invariants failed at step 604"

    # Operation 605: get
    assert tree.invariants(), "Invariants failed at step 605"

    # Operation 606: insert
    tree[5820] = 'value_760707'
    reference[5820] = 'value_760707'
    assert tree.invariants(), "Invariants failed at step 606"

    # Operation 607: delete
    del tree[3363]
    del reference[3363]
    assert tree.invariants(), "Invariants failed at step 607"

    # Operation 608: insert
    tree[125] = 'value_28205'
    reference[125] = 'value_28205'
    assert tree.invariants(), "Invariants failed at step 608"

    # Operation 609: insert
    tree[2966] = 'value_587085'
    reference[2966] = 'value_587085'
    assert tree.invariants(), "Invariants failed at step 609"

    # Operation 610: delete
    del tree[6963]
    del reference[6963]
    assert tree.invariants(), "Invariants failed at step 610"

    # Operation 611: delete
    del tree[6904]
    del reference[6904]
    assert tree.invariants(), "Invariants failed at step 611"

    # Operation 612: batch_delete
    keys_to_delete = [6722, 9669, 4877, 2966, 5113]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 612"

    # Operation 613: insert
    tree[722] = 'value_607630'
    reference[722] = 'value_607630'
    assert tree.invariants(), "Invariants failed at step 613"

    # Operation 614: insert
    tree[5027] = 'value_107073'
    reference[5027] = 'value_107073'
    assert tree.invariants(), "Invariants failed at step 614"

    # Operation 615: get
    assert tree.invariants(), "Invariants failed at step 615"

    # Operation 616: get
    assert tree.invariants(), "Invariants failed at step 616"

    # Operation 617: update
    tree[2884] = 'value_161112'
    reference[2884] = 'value_161112'
    assert tree.invariants(), "Invariants failed at step 617"

    # Operation 618: update
    tree[4753] = 'value_261588'
    reference[4753] = 'value_261588'
    assert tree.invariants(), "Invariants failed at step 618"

    # Operation 619: get
    assert tree.invariants(), "Invariants failed at step 619"

    # Operation 620: insert
    tree[9975] = 'value_831042'
    reference[9975] = 'value_831042'
    assert tree.invariants(), "Invariants failed at step 620"

    # Operation 621: update
    tree[722] = 'value_632714'
    reference[722] = 'value_632714'
    assert tree.invariants(), "Invariants failed at step 621"

    # Operation 622: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 622"

    # Operation 623: update
    tree[2884] = 'value_682118'
    reference[2884] = 'value_682118'
    assert tree.invariants(), "Invariants failed at step 623"

    # Operation 624: batch_delete
    keys_to_delete = [3653, 2376, 4753, 9975, 5820]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 624"

    # Operation 625: get
    assert tree.invariants(), "Invariants failed at step 625"

    # Operation 626: get
    assert tree.invariants(), "Invariants failed at step 626"

    # Operation 627: update
    tree[125] = 'value_474170'
    reference[125] = 'value_474170'
    assert tree.invariants(), "Invariants failed at step 627"

    # Operation 628: insert
    tree[6164] = 'value_357261'
    reference[6164] = 'value_357261'
    assert tree.invariants(), "Invariants failed at step 628"

    # Operation 629: insert
    tree[5102] = 'value_75875'
    reference[5102] = 'value_75875'
    assert tree.invariants(), "Invariants failed at step 629"

    # Operation 630: delete
    del tree[722]
    del reference[722]
    assert tree.invariants(), "Invariants failed at step 630"

    # Operation 631: get
    assert tree.invariants(), "Invariants failed at step 631"

    # Operation 632: get
    assert tree.invariants(), "Invariants failed at step 632"

    # Operation 633: delete
    del tree[6164]
    del reference[6164]
    assert tree.invariants(), "Invariants failed at step 633"

    # Operation 634: insert
    tree[5638] = 'value_678714'
    reference[5638] = 'value_678714'
    assert tree.invariants(), "Invariants failed at step 634"

    # Operation 635: get
    assert tree.invariants(), "Invariants failed at step 635"

    # Operation 636: get
    assert tree.invariants(), "Invariants failed at step 636"

    # Operation 637: insert
    tree[6929] = 'value_603538'
    reference[6929] = 'value_603538'
    assert tree.invariants(), "Invariants failed at step 637"

    # Operation 638: batch_delete
    keys_to_delete = [5027, 2884, 5638, 7433, 5102]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 638"

    # Operation 639: get
    assert tree.invariants(), "Invariants failed at step 639"

    # Operation 640: get
    assert tree.invariants(), "Invariants failed at step 640"

    # Operation 641: delete
    del tree[6929]
    del reference[6929]
    assert tree.invariants(), "Invariants failed at step 641"

    # Operation 642: get
    assert tree.invariants(), "Invariants failed at step 642"

    # Operation 643: get
    assert tree.invariants(), "Invariants failed at step 643"

    # Operation 644: update
    tree[125] = 'value_244426'
    reference[125] = 'value_244426'
    assert tree.invariants(), "Invariants failed at step 644"

    # Operation 645: delete
    del tree[125]
    del reference[125]
    assert tree.invariants(), "Invariants failed at step 645"

    # Operation 646: get
    assert tree.invariants(), "Invariants failed at step 646"

    # Operation 647: insert
    tree[5500] = 'value_254486'
    reference[5500] = 'value_254486'
    assert tree.invariants(), "Invariants failed at step 647"

    # Operation 648: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 648"

    # Operation 649: insert
    tree[2344] = 'value_608286'
    reference[2344] = 'value_608286'
    assert tree.invariants(), "Invariants failed at step 649"

    # Operation 650: delete
    del tree[2344]
    del reference[2344]
    assert tree.invariants(), "Invariants failed at step 650"

    # Operation 651: insert
    tree[7072] = 'value_237214'
    reference[7072] = 'value_237214'
    assert tree.invariants(), "Invariants failed at step 651"

    # Operation 652: delete
    del tree[5500]
    del reference[5500]
    assert tree.invariants(), "Invariants failed at step 652"

    # Operation 653: get
    assert tree.invariants(), "Invariants failed at step 653"

    # Operation 654: update
    tree[7072] = 'value_897869'
    reference[7072] = 'value_897869'
    assert tree.invariants(), "Invariants failed at step 654"

    # Operation 655: get
    assert tree.invariants(), "Invariants failed at step 655"

    # Operation 656: insert
    tree[9149] = 'value_601857'
    reference[9149] = 'value_601857'
    assert tree.invariants(), "Invariants failed at step 656"

    # Operation 657: delete
    del tree[9149]
    del reference[9149]
    assert tree.invariants(), "Invariants failed at step 657"

    # Operation 658: delete
    del tree[7072]
    del reference[7072]
    assert tree.invariants(), "Invariants failed at step 658"

    # Operation 659: get
    assert tree.invariants(), "Invariants failed at step 659"

    # Operation 660: insert
    tree[4675] = 'value_793393'
    reference[4675] = 'value_793393'
    assert tree.invariants(), "Invariants failed at step 660"

    # Operation 661: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 661"

    # Operation 662: get
    assert tree.invariants(), "Invariants failed at step 662"

    # Operation 663: update
    tree[4675] = 'value_400945'
    reference[4675] = 'value_400945'
    assert tree.invariants(), "Invariants failed at step 663"

    # Operation 664: insert
    tree[5355] = 'value_747076'
    reference[5355] = 'value_747076'
    assert tree.invariants(), "Invariants failed at step 664"

    # Operation 665: delete
    del tree[4675]
    del reference[4675]
    assert tree.invariants(), "Invariants failed at step 665"

    # Operation 666: delete
    del tree[5355]
    del reference[5355]
    assert tree.invariants(), "Invariants failed at step 666"

    # Operation 667: get
    assert tree.invariants(), "Invariants failed at step 667"

    # Operation 668: insert
    tree[4424] = 'value_421433'
    reference[4424] = 'value_421433'
    assert tree.invariants(), "Invariants failed at step 668"

    # Operation 669: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 669"

    # Operation 670: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 670"

    # Operation 671: insert
    tree[9172] = 'value_979547'
    reference[9172] = 'value_979547'
    assert tree.invariants(), "Invariants failed at step 671"

    # Operation 672: get
    assert tree.invariants(), "Invariants failed at step 672"

    # Operation 673: update
    tree[9172] = 'value_401861'
    reference[9172] = 'value_401861'
    assert tree.invariants(), "Invariants failed at step 673"

    # Operation 674: get
    assert tree.invariants(), "Invariants failed at step 674"

    # Operation 675: insert
    tree[8273] = 'value_650709'
    reference[8273] = 'value_650709'
    assert tree.invariants(), "Invariants failed at step 675"

    # Operation 676: insert
    tree[2083] = 'value_969106'
    reference[2083] = 'value_969106'
    assert tree.invariants(), "Invariants failed at step 676"

    # Operation 677: insert
    tree[3665] = 'value_819487'
    reference[3665] = 'value_819487'
    assert tree.invariants(), "Invariants failed at step 677"

    # Operation 678: delete
    del tree[3665]
    del reference[3665]
    assert tree.invariants(), "Invariants failed at step 678"

    # Operation 679: get
    assert tree.invariants(), "Invariants failed at step 679"

    # Operation 680: update
    tree[2083] = 'value_321163'
    reference[2083] = 'value_321163'
    assert tree.invariants(), "Invariants failed at step 680"

    # Operation 681: update
    tree[8273] = 'value_183682'
    reference[8273] = 'value_183682'
    assert tree.invariants(), "Invariants failed at step 681"

    # Operation 682: insert
    tree[9704] = 'value_530390'
    reference[9704] = 'value_530390'
    assert tree.invariants(), "Invariants failed at step 682"

    # Operation 683: delete
    del tree[9172]
    del reference[9172]
    assert tree.invariants(), "Invariants failed at step 683"

    # Operation 684: insert
    tree[3672] = 'value_767853'
    reference[3672] = 'value_767853'
    assert tree.invariants(), "Invariants failed at step 684"

    # Operation 685: batch_delete
    keys_to_delete = [8273, 2083, 5604, 2054]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 685"

    # Operation 686: get
    assert tree.invariants(), "Invariants failed at step 686"

    # Operation 687: insert
    tree[2124] = 'value_524600'
    reference[2124] = 'value_524600'
    assert tree.invariants(), "Invariants failed at step 687"

    # Operation 688: delete
    del tree[2124]
    del reference[2124]
    assert tree.invariants(), "Invariants failed at step 688"

    # Operation 689: update
    tree[3672] = 'value_718428'
    reference[3672] = 'value_718428'
    assert tree.invariants(), "Invariants failed at step 689"

    # Operation 690: update
    tree[9704] = 'value_892347'
    reference[9704] = 'value_892347'
    assert tree.invariants(), "Invariants failed at step 690"

    # Operation 691: get
    assert tree.invariants(), "Invariants failed at step 691"

    # Operation 692: update
    tree[3672] = 'value_607859'
    reference[3672] = 'value_607859'
    assert tree.invariants(), "Invariants failed at step 692"

    # Operation 693: insert
    tree[7310] = 'value_496705'
    reference[7310] = 'value_496705'
    assert tree.invariants(), "Invariants failed at step 693"

    # Operation 694: insert
    tree[6886] = 'value_310610'
    reference[6886] = 'value_310610'
    assert tree.invariants(), "Invariants failed at step 694"

    # Operation 695: delete
    del tree[7310]
    del reference[7310]
    assert tree.invariants(), "Invariants failed at step 695"

    # Operation 696: get
    assert tree.invariants(), "Invariants failed at step 696"

    # Operation 697: get
    assert tree.invariants(), "Invariants failed at step 697"

    # Operation 698: delete
    del tree[3672]
    del reference[3672]
    assert tree.invariants(), "Invariants failed at step 698"

    # Operation 699: insert
    tree[4058] = 'value_297520'
    reference[4058] = 'value_297520'
    assert tree.invariants(), "Invariants failed at step 699"

    # Operation 700: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 700"

    # Operation 701: insert
    tree[6115] = 'value_216145'
    reference[6115] = 'value_216145'
    assert tree.invariants(), "Invariants failed at step 701"

    # Operation 702: update
    tree[4058] = 'value_482685'
    reference[4058] = 'value_482685'
    assert tree.invariants(), "Invariants failed at step 702"

    # Operation 703: delete
    del tree[4058]
    del reference[4058]
    assert tree.invariants(), "Invariants failed at step 703"

    # Operation 704: update
    tree[6886] = 'value_431305'
    reference[6886] = 'value_431305'
    assert tree.invariants(), "Invariants failed at step 704"

    # Operation 705: delete
    del tree[6115]
    del reference[6115]
    assert tree.invariants(), "Invariants failed at step 705"

    # Operation 706: update
    tree[9704] = 'value_410496'
    reference[9704] = 'value_410496'
    assert tree.invariants(), "Invariants failed at step 706"

    # Operation 707: delete
    del tree[6886]
    del reference[6886]
    assert tree.invariants(), "Invariants failed at step 707"

    # Operation 708: get
    assert tree.invariants(), "Invariants failed at step 708"

    # Operation 709: insert
    tree[6429] = 'value_344234'
    reference[6429] = 'value_344234'
    assert tree.invariants(), "Invariants failed at step 709"

    # Operation 710: insert
    tree[3682] = 'value_815217'
    reference[3682] = 'value_815217'
    assert tree.invariants(), "Invariants failed at step 710"

    # Operation 711: get
    assert tree.invariants(), "Invariants failed at step 711"

    # Operation 712: delete
    del tree[9704]
    del reference[9704]
    assert tree.invariants(), "Invariants failed at step 712"

    # Operation 713: delete
    del tree[4424]
    del reference[4424]
    assert tree.invariants(), "Invariants failed at step 713"

    # Operation 714: delete
    del tree[3682]
    del reference[3682]
    assert tree.invariants(), "Invariants failed at step 714"

    # Operation 715: update
    tree[6429] = 'value_4985'
    reference[6429] = 'value_4985'
    assert tree.invariants(), "Invariants failed at step 715"

    # Operation 716: insert
    tree[3130] = 'value_51404'
    reference[3130] = 'value_51404'
    assert tree.invariants(), "Invariants failed at step 716"

    # Operation 717: update
    tree[6429] = 'value_983781'
    reference[6429] = 'value_983781'
    assert tree.invariants(), "Invariants failed at step 717"

    # Operation 718: insert
    tree[853] = 'value_166462'
    reference[853] = 'value_166462'
    assert tree.invariants(), "Invariants failed at step 718"

    # Operation 719: insert
    tree[7908] = 'value_405102'
    reference[7908] = 'value_405102'
    assert tree.invariants(), "Invariants failed at step 719"

    # Operation 720: delete
    del tree[6429]
    del reference[6429]
    assert tree.invariants(), "Invariants failed at step 720"

    # Operation 721: get
    assert tree.invariants(), "Invariants failed at step 721"

    # Operation 722: insert
    tree[8356] = 'value_771446'
    reference[8356] = 'value_771446'
    assert tree.invariants(), "Invariants failed at step 722"

    # Operation 723: get
    assert tree.invariants(), "Invariants failed at step 723"

    # Operation 724: insert
    tree[6881] = 'value_538219'
    reference[6881] = 'value_538219'
    assert tree.invariants(), "Invariants failed at step 724"

    # Operation 725: batch_delete
    keys_to_delete = [6457, 3130, 3925, 853]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 725"

    # Operation 726: insert
    tree[4240] = 'value_694552'
    reference[4240] = 'value_694552'
    assert tree.invariants(), "Invariants failed at step 726"

    # Operation 727: insert
    tree[9364] = 'value_819451'
    reference[9364] = 'value_819451'
    assert tree.invariants(), "Invariants failed at step 727"

    # Operation 728: update
    tree[8356] = 'value_25545'
    reference[8356] = 'value_25545'
    assert tree.invariants(), "Invariants failed at step 728"

    # Operation 729: insert
    tree[1642] = 'value_147785'
    reference[1642] = 'value_147785'
    assert tree.invariants(), "Invariants failed at step 729"

    # Operation 730: batch_delete
    keys_to_delete = [5224, 1642, 4240, 9364, 5243]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 730"

    # Operation 731: get
    assert tree.invariants(), "Invariants failed at step 731"

    # Operation 732: insert
    tree[3334] = 'value_440682'
    reference[3334] = 'value_440682'
    assert tree.invariants(), "Invariants failed at step 732"

    # Operation 733: insert
    tree[7704] = 'value_986511'
    reference[7704] = 'value_986511'
    assert tree.invariants(), "Invariants failed at step 733"

    # Operation 734: batch_delete
    keys_to_delete = [6568, 8356, 8277, 3334]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 734"

    # Operation 735: get
    assert tree.invariants(), "Invariants failed at step 735"

    # Operation 736: update
    tree[7908] = 'value_146110'
    reference[7908] = 'value_146110'
    assert tree.invariants(), "Invariants failed at step 736"

    # Operation 737: insert
    tree[9037] = 'value_403123'
    reference[9037] = 'value_403123'
    assert tree.invariants(), "Invariants failed at step 737"

    # Operation 738: get
    assert tree.invariants(), "Invariants failed at step 738"

    # Operation 739: insert
    tree[9187] = 'value_372911'
    reference[9187] = 'value_372911'
    assert tree.invariants(), "Invariants failed at step 739"

    # Operation 740: insert
    tree[566] = 'value_195758'
    reference[566] = 'value_195758'
    assert tree.invariants(), "Invariants failed at step 740"

    # Operation 741: insert
    tree[1637] = 'value_25810'
    reference[1637] = 'value_25810'
    assert tree.invariants(), "Invariants failed at step 741"

    # Operation 742: batch_delete
    keys_to_delete = [6881, 2370, 5507, 7908, 7704]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 742"

    # Operation 743: delete
    del tree[1637]
    del reference[1637]
    assert tree.invariants(), "Invariants failed at step 743"

    # Operation 744: insert
    tree[7312] = 'value_921713'
    reference[7312] = 'value_921713'
    assert tree.invariants(), "Invariants failed at step 744"

    # Operation 745: delete
    del tree[9037]
    del reference[9037]
    assert tree.invariants(), "Invariants failed at step 745"

    # Operation 746: delete
    del tree[7312]
    del reference[7312]
    assert tree.invariants(), "Invariants failed at step 746"

    # Operation 747: delete
    del tree[9187]
    del reference[9187]
    assert tree.invariants(), "Invariants failed at step 747"

    # Operation 748: insert
    tree[6271] = 'value_403411'
    reference[6271] = 'value_403411'
    assert tree.invariants(), "Invariants failed at step 748"

    # Operation 749: get
    assert tree.invariants(), "Invariants failed at step 749"

    # Operation 750: insert
    tree[8668] = 'value_738110'
    reference[8668] = 'value_738110'
    assert tree.invariants(), "Invariants failed at step 750"

    # Operation 751: insert
    tree[8834] = 'value_104058'
    reference[8834] = 'value_104058'
    assert tree.invariants(), "Invariants failed at step 751"

    # Operation 752: get
    assert tree.invariants(), "Invariants failed at step 752"

    # Operation 753: delete
    del tree[566]
    del reference[566]
    assert tree.invariants(), "Invariants failed at step 753"

    # Operation 754: insert
    tree[159] = 'value_229472'
    reference[159] = 'value_229472'
    assert tree.invariants(), "Invariants failed at step 754"

    # Operation 755: delete
    del tree[159]
    del reference[159]
    assert tree.invariants(), "Invariants failed at step 755"

    # Operation 756: insert
    tree[3352] = 'value_294265'
    reference[3352] = 'value_294265'
    assert tree.invariants(), "Invariants failed at step 756"

    # Operation 757: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 757"

    # Operation 758: insert
    tree[4211] = 'value_33657'
    reference[4211] = 'value_33657'
    assert tree.invariants(), "Invariants failed at step 758"

    # Operation 759: update
    tree[3352] = 'value_291611'
    reference[3352] = 'value_291611'
    assert tree.invariants(), "Invariants failed at step 759"

    # Operation 760: insert
    tree[4118] = 'value_514069'
    reference[4118] = 'value_514069'
    assert tree.invariants(), "Invariants failed at step 760"

    # Operation 761: update
    tree[4118] = 'value_49264'
    reference[4118] = 'value_49264'
    assert tree.invariants(), "Invariants failed at step 761"

    # Operation 762: insert
    tree[978] = 'value_665588'
    reference[978] = 'value_665588'
    assert tree.invariants(), "Invariants failed at step 762"

    # Operation 763: update
    tree[3352] = 'value_79596'
    reference[3352] = 'value_79596'
    assert tree.invariants(), "Invariants failed at step 763"

    # Operation 764: insert
    tree[905] = 'value_692969'
    reference[905] = 'value_692969'
    assert tree.invariants(), "Invariants failed at step 764"

    # Operation 765: delete
    del tree[905]
    del reference[905]
    assert tree.invariants(), "Invariants failed at step 765"

    # Operation 766: insert
    tree[2927] = 'value_513165'
    reference[2927] = 'value_513165'
    assert tree.invariants(), "Invariants failed at step 766"

    # Operation 767: insert
    tree[5573] = 'value_386281'
    reference[5573] = 'value_386281'
    assert tree.invariants(), "Invariants failed at step 767"

    # Operation 768: batch_delete
    keys_to_delete = [201, 3147, 8668, 2927]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 768"

    # Operation 769: delete
    del tree[978]
    del reference[978]
    assert tree.invariants(), "Invariants failed at step 769"

    # Operation 770: delete
    del tree[3352]
    del reference[3352]
    assert tree.invariants(), "Invariants failed at step 770"

    # Operation 771: insert
    tree[4981] = 'value_49565'
    reference[4981] = 'value_49565'
    assert tree.invariants(), "Invariants failed at step 771"

    # Operation 772: batch_delete
    keys_to_delete = [1950, 8834, 4211, 4118]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 772"

    # Operation 773: update
    tree[5573] = 'value_404099'
    reference[5573] = 'value_404099'
    assert tree.invariants(), "Invariants failed at step 773"

    # Operation 774: get
    assert tree.invariants(), "Invariants failed at step 774"

    # Operation 775: get
    assert tree.invariants(), "Invariants failed at step 775"

    # Operation 776: get
    assert tree.invariants(), "Invariants failed at step 776"

    # Operation 777: delete
    del tree[6271]
    del reference[6271]
    assert tree.invariants(), "Invariants failed at step 777"

    # Operation 778: delete
    del tree[5573]
    del reference[5573]
    assert tree.invariants(), "Invariants failed at step 778"

    # Operation 779: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 779"

    # Operation 780: delete
    del tree[4981]
    del reference[4981]
    assert tree.invariants(), "Invariants failed at step 780"

    # Operation 781: insert
    tree[7125] = 'value_890574'
    reference[7125] = 'value_890574'
    assert tree.invariants(), "Invariants failed at step 781"

    # Operation 782: update
    tree[7125] = 'value_285490'
    reference[7125] = 'value_285490'
    assert tree.invariants(), "Invariants failed at step 782"

    # Operation 783: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 783"

    # Operation 784: get
    assert tree.invariants(), "Invariants failed at step 784"

    # Operation 785: insert
    tree[9980] = 'value_335925'
    reference[9980] = 'value_335925'
    assert tree.invariants(), "Invariants failed at step 785"

    # Operation 786: get
    assert tree.invariants(), "Invariants failed at step 786"

    # Operation 787: insert
    tree[4641] = 'value_855090'
    reference[4641] = 'value_855090'
    assert tree.invariants(), "Invariants failed at step 787"

    # Operation 788: get
    assert tree.invariants(), "Invariants failed at step 788"

    # Operation 789: insert
    tree[166] = 'value_728130'
    reference[166] = 'value_728130'
    assert tree.invariants(), "Invariants failed at step 789"

    # Operation 790: insert
    tree[8305] = 'value_925020'
    reference[8305] = 'value_925020'
    assert tree.invariants(), "Invariants failed at step 790"

    # Operation 791: update
    tree[7125] = 'value_652997'
    reference[7125] = 'value_652997'
    assert tree.invariants(), "Invariants failed at step 791"

    # Operation 792: get
    assert tree.invariants(), "Invariants failed at step 792"

    # Operation 793: get
    assert tree.invariants(), "Invariants failed at step 793"

    # Operation 794: delete
    del tree[7125]
    del reference[7125]
    assert tree.invariants(), "Invariants failed at step 794"

    # Operation 795: update
    tree[8305] = 'value_859926'
    reference[8305] = 'value_859926'
    assert tree.invariants(), "Invariants failed at step 795"

    # Operation 796: delete
    del tree[166]
    del reference[166]
    assert tree.invariants(), "Invariants failed at step 796"

    # Operation 797: insert
    tree[6973] = 'value_674394'
    reference[6973] = 'value_674394'
    assert tree.invariants(), "Invariants failed at step 797"

    # Operation 798: insert
    tree[1053] = 'value_667493'
    reference[1053] = 'value_667493'
    assert tree.invariants(), "Invariants failed at step 798"

    # Operation 799: update
    tree[8305] = 'value_747613'
    reference[8305] = 'value_747613'
    assert tree.invariants(), "Invariants failed at step 799"

    # Operation 800: insert
    tree[9582] = 'value_904985'
    reference[9582] = 'value_904985'
    assert tree.invariants(), "Invariants failed at step 800"

    # Operation 801: get
    assert tree.invariants(), "Invariants failed at step 801"

    # Operation 802: delete
    del tree[4641]
    del reference[4641]
    assert tree.invariants(), "Invariants failed at step 802"

    # Operation 803: get
    assert tree.invariants(), "Invariants failed at step 803"

    # Operation 804: batch_delete
    keys_to_delete = [4629, 8067, 1053, 9582]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 804"

    # Operation 805: insert
    tree[6177] = 'value_910471'
    reference[6177] = 'value_910471'
    assert tree.invariants(), "Invariants failed at step 805"

    # Operation 806: delete
    del tree[9980]
    del reference[9980]
    assert tree.invariants(), "Invariants failed at step 806"

    # Operation 807: delete
    del tree[6973]
    del reference[6973]
    assert tree.invariants(), "Invariants failed at step 807"

    # Operation 808: insert
    tree[8904] = 'value_732848'
    reference[8904] = 'value_732848'
    assert tree.invariants(), "Invariants failed at step 808"

    # Operation 809: update
    tree[8904] = 'value_388828'
    reference[8904] = 'value_388828'
    assert tree.invariants(), "Invariants failed at step 809"

    # Operation 810: insert
    tree[4258] = 'value_517480'
    reference[4258] = 'value_517480'
    assert tree.invariants(), "Invariants failed at step 810"

    # Operation 811: get
    assert tree.invariants(), "Invariants failed at step 811"

    # Operation 812: delete
    del tree[8305]
    del reference[8305]
    assert tree.invariants(), "Invariants failed at step 812"

    # Operation 813: update
    tree[6177] = 'value_532220'
    reference[6177] = 'value_532220'
    assert tree.invariants(), "Invariants failed at step 813"

    # Operation 814: delete
    del tree[6177]
    del reference[6177]
    assert tree.invariants(), "Invariants failed at step 814"

    # Operation 815: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 815"

    # Operation 816: delete
    del tree[4258]
    del reference[4258]
    assert tree.invariants(), "Invariants failed at step 816"

    # Operation 817: insert
    tree[6091] = 'value_278911'
    reference[6091] = 'value_278911'
    assert tree.invariants(), "Invariants failed at step 817"

    # Operation 818: insert
    tree[6794] = 'value_17295'
    reference[6794] = 'value_17295'
    assert tree.invariants(), "Invariants failed at step 818"

    # Operation 819: delete
    del tree[8904]
    del reference[8904]
    assert tree.invariants(), "Invariants failed at step 819"

    # Operation 820: insert
    tree[7981] = 'value_588288'
    reference[7981] = 'value_588288'
    assert tree.invariants(), "Invariants failed at step 820"

    # Operation 821: get
    assert tree.invariants(), "Invariants failed at step 821"

    # Operation 822: delete
    del tree[6091]
    del reference[6091]
    assert tree.invariants(), "Invariants failed at step 822"

    # Operation 823: delete
    del tree[7981]
    del reference[7981]
    assert tree.invariants(), "Invariants failed at step 823"

    # Operation 824: get
    assert tree.invariants(), "Invariants failed at step 824"

    # Operation 825: insert
    tree[9240] = 'value_584510'
    reference[9240] = 'value_584510'
    assert tree.invariants(), "Invariants failed at step 825"

    # Operation 826: delete
    del tree[6794]
    del reference[6794]
    assert tree.invariants(), "Invariants failed at step 826"

    # Operation 827: insert
    tree[7723] = 'value_510090'
    reference[7723] = 'value_510090'
    assert tree.invariants(), "Invariants failed at step 827"

    # Operation 828: get
    assert tree.invariants(), "Invariants failed at step 828"

    # Operation 829: insert
    tree[2863] = 'value_509751'
    reference[2863] = 'value_509751'
    assert tree.invariants(), "Invariants failed at step 829"

    # Operation 830: insert
    tree[5245] = 'value_902228'
    reference[5245] = 'value_902228'
    assert tree.invariants(), "Invariants failed at step 830"

    # Operation 831: delete
    del tree[2863]
    del reference[2863]
    assert tree.invariants(), "Invariants failed at step 831"

    # Operation 832: get
    assert tree.invariants(), "Invariants failed at step 832"

    # Operation 833: get
    assert tree.invariants(), "Invariants failed at step 833"

    # Operation 834: insert
    tree[9524] = 'value_187670'
    reference[9524] = 'value_187670'
    assert tree.invariants(), "Invariants failed at step 834"

    # Operation 835: insert
    tree[6282] = 'value_620894'
    reference[6282] = 'value_620894'
    assert tree.invariants(), "Invariants failed at step 835"

    # Operation 836: delete
    del tree[9240]
    del reference[9240]
    assert tree.invariants(), "Invariants failed at step 836"

    # Operation 837: insert
    tree[8085] = 'value_684058'
    reference[8085] = 'value_684058'
    assert tree.invariants(), "Invariants failed at step 837"

    # Operation 838: batch_delete
    keys_to_delete = [5017, 6282, 9524, 5002]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 838"

    # Operation 839: delete
    del tree[7723]
    del reference[7723]
    assert tree.invariants(), "Invariants failed at step 839"

    # Operation 840: insert
    tree[3765] = 'value_860739'
    reference[3765] = 'value_860739'
    assert tree.invariants(), "Invariants failed at step 840"

    # Operation 841: insert
    tree[3969] = 'value_2688'
    reference[3969] = 'value_2688'
    assert tree.invariants(), "Invariants failed at step 841"

    # Operation 842: delete
    del tree[8085]
    del reference[8085]
    assert tree.invariants(), "Invariants failed at step 842"

    # Operation 843: delete
    del tree[3969]
    del reference[3969]
    assert tree.invariants(), "Invariants failed at step 843"

    # Operation 844: insert
    tree[5220] = 'value_480937'
    reference[5220] = 'value_480937'
    assert tree.invariants(), "Invariants failed at step 844"

    # Operation 845: insert
    tree[3245] = 'value_608205'
    reference[3245] = 'value_608205'
    assert tree.invariants(), "Invariants failed at step 845"

    # Operation 846: insert
    tree[9668] = 'value_997747'
    reference[9668] = 'value_997747'
    assert tree.invariants(), "Invariants failed at step 846"

    # Operation 847: insert
    tree[9053] = 'value_134225'
    reference[9053] = 'value_134225'
    assert tree.invariants(), "Invariants failed at step 847"

    # Operation 848: delete
    del tree[3765]
    del reference[3765]
    assert tree.invariants(), "Invariants failed at step 848"

    # Operation 849: delete
    del tree[5245]
    del reference[5245]
    assert tree.invariants(), "Invariants failed at step 849"

    # Operation 850: delete
    del tree[9053]
    del reference[9053]
    assert tree.invariants(), "Invariants failed at step 850"

    # Operation 851: delete
    del tree[3245]
    del reference[3245]
    assert tree.invariants(), "Invariants failed at step 851"

    # Operation 852: get
    assert tree.invariants(), "Invariants failed at step 852"

    # Operation 853: delete
    del tree[5220]
    del reference[5220]
    assert tree.invariants(), "Invariants failed at step 853"

    # Operation 854: get
    assert tree.invariants(), "Invariants failed at step 854"

    # Operation 855: insert
    tree[3929] = 'value_99838'
    reference[3929] = 'value_99838'
    assert tree.invariants(), "Invariants failed at step 855"

    # Operation 856: delete
    del tree[9668]
    del reference[9668]
    assert tree.invariants(), "Invariants failed at step 856"

    # Operation 857: delete
    del tree[3929]
    del reference[3929]
    assert tree.invariants(), "Invariants failed at step 857"

    # Operation 858: insert
    tree[4956] = 'value_458467'
    reference[4956] = 'value_458467'
    assert tree.invariants(), "Invariants failed at step 858"

    # Operation 859: delete
    del tree[4956]
    del reference[4956]
    assert tree.invariants(), "Invariants failed at step 859"

    # Operation 860: get
    assert tree.invariants(), "Invariants failed at step 860"

    # Operation 861: insert
    tree[1254] = 'value_114379'
    reference[1254] = 'value_114379'
    assert tree.invariants(), "Invariants failed at step 861"

    # Operation 862: delete
    del tree[1254]
    del reference[1254]
    assert tree.invariants(), "Invariants failed at step 862"

    # Operation 863: get
    assert tree.invariants(), "Invariants failed at step 863"

    # Operation 864: insert
    tree[6546] = 'value_761379'
    reference[6546] = 'value_761379'
    assert tree.invariants(), "Invariants failed at step 864"

    # Operation 865: update
    tree[6546] = 'value_623512'
    reference[6546] = 'value_623512'
    assert tree.invariants(), "Invariants failed at step 865"

    # Operation 866: get
    assert tree.invariants(), "Invariants failed at step 866"

    # Operation 867: delete
    del tree[6546]
    del reference[6546]
    assert tree.invariants(), "Invariants failed at step 867"

    # Operation 868: insert
    tree[3412] = 'value_724415'
    reference[3412] = 'value_724415'
    assert tree.invariants(), "Invariants failed at step 868"

    # Operation 869: get
    assert tree.invariants(), "Invariants failed at step 869"

    # Operation 870: insert
    tree[4614] = 'value_786493'
    reference[4614] = 'value_786493'
    assert tree.invariants(), "Invariants failed at step 870"

    # Operation 871: get
    assert tree.invariants(), "Invariants failed at step 871"

    # Operation 872: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 872"

    # Operation 873: insert
    tree[1799] = 'value_773203'
    reference[1799] = 'value_773203'
    assert tree.invariants(), "Invariants failed at step 873"

    # Operation 874: insert
    tree[9974] = 'value_921899'
    reference[9974] = 'value_921899'
    assert tree.invariants(), "Invariants failed at step 874"

    # Operation 875: insert
    tree[9159] = 'value_101826'
    reference[9159] = 'value_101826'
    assert tree.invariants(), "Invariants failed at step 875"

    # Operation 876: insert
    tree[6934] = 'value_823603'
    reference[6934] = 'value_823603'
    assert tree.invariants(), "Invariants failed at step 876"

    # Operation 877: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 877"

    # Operation 878: insert
    tree[9854] = 'value_916549'
    reference[9854] = 'value_916549'
    assert tree.invariants(), "Invariants failed at step 878"

    # Operation 879: insert
    tree[2509] = 'value_368007'
    reference[2509] = 'value_368007'
    assert tree.invariants(), "Invariants failed at step 879"

    # Operation 880: insert
    tree[1240] = 'value_998742'
    reference[1240] = 'value_998742'
    assert tree.invariants(), "Invariants failed at step 880"

    # Operation 881: get
    assert tree.invariants(), "Invariants failed at step 881"

    # Operation 882: delete
    del tree[1799]
    del reference[1799]
    assert tree.invariants(), "Invariants failed at step 882"

    # Operation 883: delete
    del tree[9974]
    del reference[9974]
    assert tree.invariants(), "Invariants failed at step 883"

    # Operation 884: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 884"

    # Operation 885: delete
    del tree[4614]
    del reference[4614]
    assert tree.invariants(), "Invariants failed at step 885"

    # Operation 886: insert
    tree[7285] = 'value_323960'
    reference[7285] = 'value_323960'
    assert tree.invariants(), "Invariants failed at step 886"

    # Operation 887: delete
    del tree[3412]
    del reference[3412]
    assert tree.invariants(), "Invariants failed at step 887"

    # Operation 888: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 888"

    # Operation 889: insert
    tree[7372] = 'value_65418'
    reference[7372] = 'value_65418'
    assert tree.invariants(), "Invariants failed at step 889"

    # Operation 890: update
    tree[2509] = 'value_406052'
    reference[2509] = 'value_406052'
    assert tree.invariants(), "Invariants failed at step 890"

    # Operation 891: delete
    del tree[7285]
    del reference[7285]
    assert tree.invariants(), "Invariants failed at step 891"

    # Operation 892: delete
    del tree[9159]
    del reference[9159]
    assert tree.invariants(), "Invariants failed at step 892"

    # Operation 893: get
    assert tree.invariants(), "Invariants failed at step 893"

    # Operation 894: delete
    del tree[2509]
    del reference[2509]
    assert tree.invariants(), "Invariants failed at step 894"

    # Operation 895: insert
    tree[2816] = 'value_191009'
    reference[2816] = 'value_191009'
    assert tree.invariants(), "Invariants failed at step 895"

    # Operation 896: update
    tree[2816] = 'value_921658'
    reference[2816] = 'value_921658'
    assert tree.invariants(), "Invariants failed at step 896"

    # Operation 897: insert
    tree[9322] = 'value_974437'
    reference[9322] = 'value_974437'
    assert tree.invariants(), "Invariants failed at step 897"

    # Operation 898: update
    tree[2816] = 'value_632771'
    reference[2816] = 'value_632771'
    assert tree.invariants(), "Invariants failed at step 898"

    # Operation 899: delete
    del tree[9322]
    del reference[9322]
    assert tree.invariants(), "Invariants failed at step 899"

    # Operation 900: get
    assert tree.invariants(), "Invariants failed at step 900"

    # Operation 901: update
    tree[2816] = 'value_839661'
    reference[2816] = 'value_839661'
    assert tree.invariants(), "Invariants failed at step 901"

    # Operation 902: update
    tree[2816] = 'value_353422'
    reference[2816] = 'value_353422'
    assert tree.invariants(), "Invariants failed at step 902"

    # Operation 903: insert
    tree[8656] = 'value_425903'
    reference[8656] = 'value_425903'
    assert tree.invariants(), "Invariants failed at step 903"

    # Operation 904: insert
    tree[7265] = 'value_552939'
    reference[7265] = 'value_552939'
    assert tree.invariants(), "Invariants failed at step 904"

    # Operation 905: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 905"

    # Operation 906: delete
    del tree[7265]
    del reference[7265]
    assert tree.invariants(), "Invariants failed at step 906"

    # Operation 907: get
    assert tree.invariants(), "Invariants failed at step 907"

    # Operation 908: insert
    tree[7724] = 'value_396597'
    reference[7724] = 'value_396597'
    assert tree.invariants(), "Invariants failed at step 908"

    # Operation 909: batch_delete
    keys_to_delete = [1240, 1450, 7372, 6934]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 909"

    # Operation 910: delete
    del tree[8656]
    del reference[8656]
    assert tree.invariants(), "Invariants failed at step 910"

    # Operation 911: delete
    del tree[7724]
    del reference[7724]
    assert tree.invariants(), "Invariants failed at step 911"

    # Operation 912: get
    assert tree.invariants(), "Invariants failed at step 912"

    # Operation 913: get
    assert tree.invariants(), "Invariants failed at step 913"

    # Operation 914: get
    assert tree.invariants(), "Invariants failed at step 914"

    # Operation 915: delete
    del tree[2816]
    del reference[2816]
    assert tree.invariants(), "Invariants failed at step 915"

    # Operation 916: get
    assert tree.invariants(), "Invariants failed at step 916"

    # Operation 917: delete
    del tree[9854]
    del reference[9854]
    assert tree.invariants(), "Invariants failed at step 917"

    # Operation 918: get
    assert tree.invariants(), "Invariants failed at step 918"

    # Operation 919: insert
    tree[2998] = 'value_61300'
    reference[2998] = 'value_61300'
    assert tree.invariants(), "Invariants failed at step 919"

    # Operation 920: insert
    tree[2728] = 'value_490714'
    reference[2728] = 'value_490714'
    assert tree.invariants(), "Invariants failed at step 920"

    # Operation 921: delete
    del tree[2728]
    del reference[2728]
    assert tree.invariants(), "Invariants failed at step 921"

    # Operation 922: get
    assert tree.invariants(), "Invariants failed at step 922"

    # Operation 923: get
    assert tree.invariants(), "Invariants failed at step 923"

    # Operation 924: insert
    tree[1525] = 'value_707298'
    reference[1525] = 'value_707298'
    assert tree.invariants(), "Invariants failed at step 924"

    # Operation 925: get
    assert tree.invariants(), "Invariants failed at step 925"

    # Operation 926: insert
    tree[8023] = 'value_642982'
    reference[8023] = 'value_642982'
    assert tree.invariants(), "Invariants failed at step 926"

    # Operation 927: delete
    del tree[8023]
    del reference[8023]
    assert tree.invariants(), "Invariants failed at step 927"

    # Operation 928: delete
    del tree[1525]
    del reference[1525]
    assert tree.invariants(), "Invariants failed at step 928"

    # Operation 929: update
    tree[2998] = 'value_713749'
    reference[2998] = 'value_713749'
    assert tree.invariants(), "Invariants failed at step 929"

    # Operation 930: delete
    del tree[2998]
    del reference[2998]
    assert tree.invariants(), "Invariants failed at step 930"

    # Operation 931: insert
    tree[5215] = 'value_269790'
    reference[5215] = 'value_269790'
    assert tree.invariants(), "Invariants failed at step 931"

    # Operation 932: delete
    del tree[5215]
    del reference[5215]
    assert tree.invariants(), "Invariants failed at step 932"

    # Operation 933: insert
    tree[345] = 'value_772420'
    reference[345] = 'value_772420'
    assert tree.invariants(), "Invariants failed at step 933"

    # Operation 934: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 934"

    # Operation 935: insert
    tree[9378] = 'value_903162'
    reference[9378] = 'value_903162'
    assert tree.invariants(), "Invariants failed at step 935"

    # Operation 936: insert
    tree[2577] = 'value_294596'
    reference[2577] = 'value_294596'
    assert tree.invariants(), "Invariants failed at step 936"

    # Operation 937: insert
    tree[2317] = 'value_368264'
    reference[2317] = 'value_368264'
    assert tree.invariants(), "Invariants failed at step 937"

    # Operation 938: insert
    tree[9033] = 'value_311527'
    reference[9033] = 'value_311527'
    assert tree.invariants(), "Invariants failed at step 938"

    # Operation 939: insert
    tree[4635] = 'value_591066'
    reference[4635] = 'value_591066'
    assert tree.invariants(), "Invariants failed at step 939"

    # Operation 940: delete
    del tree[2577]
    del reference[2577]
    assert tree.invariants(), "Invariants failed at step 940"

    # Operation 941: get
    assert tree.invariants(), "Invariants failed at step 941"

    # Operation 942: update
    tree[9378] = 'value_922326'
    reference[9378] = 'value_922326'
    assert tree.invariants(), "Invariants failed at step 942"

    # Operation 943: insert
    tree[9064] = 'value_81311'
    reference[9064] = 'value_81311'
    assert tree.invariants(), "Invariants failed at step 943"

    # Operation 944: insert
    tree[3955] = 'value_772396'
    reference[3955] = 'value_772396'
    assert tree.invariants(), "Invariants failed at step 944"

    # Operation 945: get
    assert tree.invariants(), "Invariants failed at step 945"

    # Operation 946: get
    assert tree.invariants(), "Invariants failed at step 946"

    # Operation 947: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 947"

    # Operation 948: insert
    tree[1149] = 'value_715236'
    reference[1149] = 'value_715236'
    assert tree.invariants(), "Invariants failed at step 948"

    # Operation 949: delete
    del tree[3955]
    del reference[3955]
    assert tree.invariants(), "Invariants failed at step 949"

    # Operation 950: insert
    tree[5920] = 'value_655651'
    reference[5920] = 'value_655651'
    assert tree.invariants(), "Invariants failed at step 950"

    # Operation 951: get
    assert tree.invariants(), "Invariants failed at step 951"

    # Operation 952: insert
    tree[7089] = 'value_634343'
    reference[7089] = 'value_634343'
    assert tree.invariants(), "Invariants failed at step 952"

    # Operation 953: batch_delete
    keys_to_delete = [5920, 9378, 9033, 9513, 7377, 4635]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 953"

    # Operation 954: delete
    del tree[2317]
    del reference[2317]
    assert tree.invariants(), "Invariants failed at step 954"

    # Operation 955: insert
    tree[9471] = 'value_923524'
    reference[9471] = 'value_923524'
    assert tree.invariants(), "Invariants failed at step 955"

    # Operation 956: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 956"

    # Operation 957: delete
    del tree[9471]
    del reference[9471]
    assert tree.invariants(), "Invariants failed at step 957"

    # Operation 958: delete
    del tree[1149]
    del reference[1149]
    assert tree.invariants(), "Invariants failed at step 958"

    # Operation 959: insert
    tree[1978] = 'value_143021'
    reference[1978] = 'value_143021'
    assert tree.invariants(), "Invariants failed at step 959"

    # Operation 960: get
    assert tree.invariants(), "Invariants failed at step 960"

    # Operation 961: update
    tree[1978] = 'value_929969'
    reference[1978] = 'value_929969'
    assert tree.invariants(), "Invariants failed at step 961"

    # Operation 962: get
    assert tree.invariants(), "Invariants failed at step 962"

    # Operation 963: insert
    tree[4903] = 'value_636637'
    reference[4903] = 'value_636637'
    assert tree.invariants(), "Invariants failed at step 963"

    # Operation 964: delete
    del tree[1978]
    del reference[1978]
    assert tree.invariants(), "Invariants failed at step 964"

    # Operation 965: insert
    tree[650] = 'value_123654'
    reference[650] = 'value_123654'
    assert tree.invariants(), "Invariants failed at step 965"

    # Operation 966: delete
    del tree[650]
    del reference[650]
    assert tree.invariants(), "Invariants failed at step 966"

    # Operation 967: insert
    tree[3076] = 'value_493694'
    reference[3076] = 'value_493694'
    assert tree.invariants(), "Invariants failed at step 967"

    # Operation 968: batch_delete
    keys_to_delete = [9064, 2452, 3076, 8576]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 968"

    # Operation 969: get
    assert tree.invariants(), "Invariants failed at step 969"

    # Operation 970: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 970"

    # Operation 971: insert
    tree[2344] = 'value_769257'
    reference[2344] = 'value_769257'
    assert tree.invariants(), "Invariants failed at step 971"

    # Operation 972: get
    assert tree.invariants(), "Invariants failed at step 972"

    # Operation 973: get
    assert tree.invariants(), "Invariants failed at step 973"

    # Operation 974: get
    assert tree.invariants(), "Invariants failed at step 974"

    # Operation 975: update
    tree[7089] = 'value_767784'
    reference[7089] = 'value_767784'
    assert tree.invariants(), "Invariants failed at step 975"

    # Operation 976: insert
    tree[7612] = 'value_437220'
    reference[7612] = 'value_437220'
    assert tree.invariants(), "Invariants failed at step 976"

    # Operation 977: insert
    tree[4033] = 'value_604231'
    reference[4033] = 'value_604231'
    assert tree.invariants(), "Invariants failed at step 977"

    # Operation 978: insert
    tree[5490] = 'value_1310'
    reference[5490] = 'value_1310'
    assert tree.invariants(), "Invariants failed at step 978"

    # Operation 979: update
    tree[2344] = 'value_474095'
    reference[2344] = 'value_474095'
    assert tree.invariants(), "Invariants failed at step 979"

    # Operation 980: delete
    del tree[7089]
    del reference[7089]
    assert tree.invariants(), "Invariants failed at step 980"

    # Operation 981: insert
    tree[4630] = 'value_655104'
    reference[4630] = 'value_655104'
    assert tree.invariants(), "Invariants failed at step 981"

    # Operation 982: batch_delete
    keys_to_delete = [4033, 5490, 4630, 6075, 8188]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 982"

    # Operation 983: get
    assert tree.invariants(), "Invariants failed at step 983"

    # Operation 984: get
    assert tree.invariants(), "Invariants failed at step 984"

    # Operation 985: delete
    del tree[7612]
    del reference[7612]
    assert tree.invariants(), "Invariants failed at step 985"

    # Operation 986: delete
    del tree[345]
    del reference[345]
    assert tree.invariants(), "Invariants failed at step 986"

    # Operation 987: delete
    del tree[4903]
    del reference[4903]
    assert tree.invariants(), "Invariants failed at step 987"

    # Operation 988: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 988"

    # Operation 989: get
    assert tree.invariants(), "Invariants failed at step 989"

    # Operation 990: insert
    tree[4272] = 'value_704563'
    reference[4272] = 'value_704563'
    assert tree.invariants(), "Invariants failed at step 990"

    # Operation 991: insert
    tree[6579] = 'value_499599'
    reference[6579] = 'value_499599'
    assert tree.invariants(), "Invariants failed at step 991"

    # Operation 992: insert
    tree[5694] = 'value_471105'
    reference[5694] = 'value_471105'
    assert tree.invariants(), "Invariants failed at step 992"

    # Operation 993: insert
    tree[15] = 'value_842106'
    reference[15] = 'value_842106'
    assert tree.invariants(), "Invariants failed at step 993"

    # Operation 994: update
    tree[15] = 'value_788430'
    reference[15] = 'value_788430'
    assert tree.invariants(), "Invariants failed at step 994"

    # Operation 995: update
    tree[6579] = 'value_664882'
    reference[6579] = 'value_664882'
    assert tree.invariants(), "Invariants failed at step 995"

    # Operation 996: update
    tree[2344] = 'value_765529'
    reference[2344] = 'value_765529'
    assert tree.invariants(), "Invariants failed at step 996"

    # Operation 997: get
    assert tree.invariants(), "Invariants failed at step 997"

    # Operation 998: update
    tree[2344] = 'value_388306'
    reference[2344] = 'value_388306'
    assert tree.invariants(), "Invariants failed at step 998"

    # Operation 999: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 999"

    # Operation 1000: get
    assert tree.invariants(), "Invariants failed at step 1000"

    # Operation 1001: delete
    del tree[4272]
    del reference[4272]
    assert tree.invariants(), "Invariants failed at step 1001"

    # Operation 1002: update
    tree[6579] = 'value_510714'
    reference[6579] = 'value_510714'
    assert tree.invariants(), "Invariants failed at step 1002"

    # Operation 1003: get
    assert tree.invariants(), "Invariants failed at step 1003"

    # Operation 1004: update
    tree[2344] = 'value_219965'
    reference[2344] = 'value_219965'
    assert tree.invariants(), "Invariants failed at step 1004"

    # Operation 1005: delete
    del tree[5694]
    del reference[5694]
    assert tree.invariants(), "Invariants failed at step 1005"

    # Operation 1006: update
    tree[15] = 'value_623702'
    reference[15] = 'value_623702'
    assert tree.invariants(), "Invariants failed at step 1006"

    # Operation 1007: insert
    tree[9460] = 'value_80887'
    reference[9460] = 'value_80887'
    assert tree.invariants(), "Invariants failed at step 1007"

    # Operation 1008: insert
    tree[1460] = 'value_136793'
    reference[1460] = 'value_136793'
    assert tree.invariants(), "Invariants failed at step 1008"

    # Operation 1009: get
    assert tree.invariants(), "Invariants failed at step 1009"

    # Operation 1010: insert
    tree[2261] = 'value_110660'
    reference[2261] = 'value_110660'
    assert tree.invariants(), "Invariants failed at step 1010"

    # Operation 1011: delete
    del tree[2261]
    del reference[2261]
    assert tree.invariants(), "Invariants failed at step 1011"

    # Operation 1012: update
    tree[6579] = 'value_366986'
    reference[6579] = 'value_366986'
    assert tree.invariants(), "Invariants failed at step 1012"

    # Operation 1013: update
    tree[2344] = 'value_142218'
    reference[2344] = 'value_142218'
    assert tree.invariants(), "Invariants failed at step 1013"

    # Operation 1014: insert
    tree[5068] = 'value_808376'
    reference[5068] = 'value_808376'
    assert tree.invariants(), "Invariants failed at step 1014"

    # Operation 1015: get
    assert tree.invariants(), "Invariants failed at step 1015"

    # Operation 1016: batch_delete
    keys_to_delete = [2344, 5068, 3116, 1460, 375]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1016"

    # Operation 1017: get
    assert tree.invariants(), "Invariants failed at step 1017"

    # Operation 1018: get
    assert tree.invariants(), "Invariants failed at step 1018"

    # Operation 1019: insert
    tree[7983] = 'value_803051'
    reference[7983] = 'value_803051'
    assert tree.invariants(), "Invariants failed at step 1019"

    # Operation 1020: get
    assert tree.invariants(), "Invariants failed at step 1020"

    # Operation 1021: update
    tree[9460] = 'value_247814'
    reference[9460] = 'value_247814'
    assert tree.invariants(), "Invariants failed at step 1021"

    # Operation 1022: get
    assert tree.invariants(), "Invariants failed at step 1022"

    # Operation 1023: update
    tree[15] = 'value_276638'
    reference[15] = 'value_276638'
    assert tree.invariants(), "Invariants failed at step 1023"

    # Operation 1024: insert
    tree[335] = 'value_97475'
    reference[335] = 'value_97475'
    assert tree.invariants(), "Invariants failed at step 1024"

    # Operation 1025: insert
    tree[83] = 'value_116544'
    reference[83] = 'value_116544'
    assert tree.invariants(), "Invariants failed at step 1025"

    # Operation 1026: update
    tree[9460] = 'value_532534'
    reference[9460] = 'value_532534'
    assert tree.invariants(), "Invariants failed at step 1026"

    # Operation 1027: insert
    tree[8816] = 'value_81518'
    reference[8816] = 'value_81518'
    assert tree.invariants(), "Invariants failed at step 1027"

    # Operation 1028: update
    tree[83] = 'value_153063'
    reference[83] = 'value_153063'
    assert tree.invariants(), "Invariants failed at step 1028"

    # Operation 1029: get
    assert tree.invariants(), "Invariants failed at step 1029"

    # Operation 1030: delete
    del tree[335]
    del reference[335]
    assert tree.invariants(), "Invariants failed at step 1030"

    # Operation 1031: get
    assert tree.invariants(), "Invariants failed at step 1031"

    # Operation 1032: update
    tree[9460] = 'value_93052'
    reference[9460] = 'value_93052'
    assert tree.invariants(), "Invariants failed at step 1032"

    # Operation 1033: batch_delete
    keys_to_delete = [7815, 1707, 83, 6579, 9460]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1033"

    # Operation 1034: delete
    del tree[7983]
    del reference[7983]
    assert tree.invariants(), "Invariants failed at step 1034"

    # Operation 1035: get
    assert tree.invariants(), "Invariants failed at step 1035"

    # Operation 1036: update
    tree[8816] = 'value_979309'
    reference[8816] = 'value_979309'
    assert tree.invariants(), "Invariants failed at step 1036"

    # Operation 1037: delete
    del tree[15]
    del reference[15]
    assert tree.invariants(), "Invariants failed at step 1037"

    # Operation 1038: insert
    tree[1663] = 'value_405067'
    reference[1663] = 'value_405067'
    assert tree.invariants(), "Invariants failed at step 1038"

    # Operation 1039: delete
    del tree[8816]
    del reference[8816]
    assert tree.invariants(), "Invariants failed at step 1039"

    # Operation 1040: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1040"

    # Operation 1041: insert
    tree[4781] = 'value_783936'
    reference[4781] = 'value_783936'
    assert tree.invariants(), "Invariants failed at step 1041"

    # Operation 1042: get
    assert tree.invariants(), "Invariants failed at step 1042"

    # Operation 1043: delete
    del tree[4781]
    del reference[4781]
    assert tree.invariants(), "Invariants failed at step 1043"

    # Operation 1044: delete
    del tree[1663]
    del reference[1663]
    assert tree.invariants(), "Invariants failed at step 1044"

    # Operation 1045: insert
    tree[6647] = 'value_145060'
    reference[6647] = 'value_145060'
    assert tree.invariants(), "Invariants failed at step 1045"

    # Operation 1046: delete
    del tree[6647]
    del reference[6647]
    assert tree.invariants(), "Invariants failed at step 1046"

    # Operation 1047: get
    assert tree.invariants(), "Invariants failed at step 1047"

    # Operation 1048: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1048"

    # Operation 1049: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1049"

    # Operation 1050: insert
    tree[2947] = 'value_796778'
    reference[2947] = 'value_796778'
    assert tree.invariants(), "Invariants failed at step 1050"

    # Operation 1051: get
    assert tree.invariants(), "Invariants failed at step 1051"

    # Operation 1052: insert
    tree[3268] = 'value_932553'
    reference[3268] = 'value_932553'
    assert tree.invariants(), "Invariants failed at step 1052"

    # Operation 1053: delete
    del tree[2947]
    del reference[2947]
    assert tree.invariants(), "Invariants failed at step 1053"

    # Operation 1054: delete
    del tree[3268]
    del reference[3268]
    assert tree.invariants(), "Invariants failed at step 1054"

    # Operation 1055: get
    assert tree.invariants(), "Invariants failed at step 1055"

    # Operation 1056: get
    assert tree.invariants(), "Invariants failed at step 1056"

    # Operation 1057: get
    assert tree.invariants(), "Invariants failed at step 1057"

    # Operation 1058: insert
    tree[7877] = 'value_658096'
    reference[7877] = 'value_658096'
    assert tree.invariants(), "Invariants failed at step 1058"

    # Operation 1059: update
    tree[7877] = 'value_198668'
    reference[7877] = 'value_198668'
    assert tree.invariants(), "Invariants failed at step 1059"

    # Operation 1060: insert
    tree[2019] = 'value_869019'
    reference[2019] = 'value_869019'
    assert tree.invariants(), "Invariants failed at step 1060"

    # Operation 1061: insert
    tree[1350] = 'value_663898'
    reference[1350] = 'value_663898'
    assert tree.invariants(), "Invariants failed at step 1061"

    # Operation 1062: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1062"

    # Operation 1063: delete
    del tree[2019]
    del reference[2019]
    assert tree.invariants(), "Invariants failed at step 1063"

    # Operation 1064: insert
    tree[1710] = 'value_261528'
    reference[1710] = 'value_261528'
    assert tree.invariants(), "Invariants failed at step 1064"

    # Operation 1065: delete
    del tree[1710]
    del reference[1710]
    assert tree.invariants(), "Invariants failed at step 1065"

    # Operation 1066: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1066"

    # Operation 1067: get
    assert tree.invariants(), "Invariants failed at step 1067"

    # Operation 1068: delete
    del tree[1350]
    del reference[1350]
    assert tree.invariants(), "Invariants failed at step 1068"

    # Operation 1069: update
    tree[7877] = 'value_106584'
    reference[7877] = 'value_106584'
    assert tree.invariants(), "Invariants failed at step 1069"

    # Operation 1070: get
    assert tree.invariants(), "Invariants failed at step 1070"

    # Operation 1071: get
    assert tree.invariants(), "Invariants failed at step 1071"

    # Operation 1072: delete
    del tree[7877]
    del reference[7877]
    assert tree.invariants(), "Invariants failed at step 1072"

    # Operation 1073: insert
    tree[7823] = 'value_508191'
    reference[7823] = 'value_508191'
    assert tree.invariants(), "Invariants failed at step 1073"

    # Operation 1074: insert
    tree[5915] = 'value_401996'
    reference[5915] = 'value_401996'
    assert tree.invariants(), "Invariants failed at step 1074"

    # Operation 1075: get
    assert tree.invariants(), "Invariants failed at step 1075"

    # Operation 1076: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1076"

    # Operation 1077: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1077"

    # Operation 1078: insert
    tree[7783] = 'value_312386'
    reference[7783] = 'value_312386'
    assert tree.invariants(), "Invariants failed at step 1078"

    # Operation 1079: insert
    tree[3314] = 'value_180339'
    reference[3314] = 'value_180339'
    assert tree.invariants(), "Invariants failed at step 1079"

    # Operation 1080: update
    tree[7823] = 'value_778856'
    reference[7823] = 'value_778856'
    assert tree.invariants(), "Invariants failed at step 1080"

    # Operation 1081: update
    tree[7783] = 'value_438937'
    reference[7783] = 'value_438937'
    assert tree.invariants(), "Invariants failed at step 1081"

    # Operation 1082: update
    tree[5915] = 'value_273900'
    reference[5915] = 'value_273900'
    assert tree.invariants(), "Invariants failed at step 1082"

    # Operation 1083: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1083"

    # Operation 1084: delete
    del tree[3314]
    del reference[3314]
    assert tree.invariants(), "Invariants failed at step 1084"

    # Operation 1085: get
    assert tree.invariants(), "Invariants failed at step 1085"

    # Operation 1086: update
    tree[7823] = 'value_258256'
    reference[7823] = 'value_258256'
    assert tree.invariants(), "Invariants failed at step 1086"

    # Operation 1087: insert
    tree[8354] = 'value_358440'
    reference[8354] = 'value_358440'
    assert tree.invariants(), "Invariants failed at step 1087"

    # Operation 1088: update
    tree[5915] = 'value_342277'
    reference[5915] = 'value_342277'
    assert tree.invariants(), "Invariants failed at step 1088"

    # Operation 1089: delete
    del tree[7783]
    del reference[7783]
    assert tree.invariants(), "Invariants failed at step 1089"

    # Operation 1090: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1090"

    # Operation 1091: delete
    del tree[7823]
    del reference[7823]
    assert tree.invariants(), "Invariants failed at step 1091"

    # Operation 1092: delete
    del tree[8354]
    del reference[8354]
    assert tree.invariants(), "Invariants failed at step 1092"

    # Operation 1093: insert
    tree[7423] = 'value_476022'
    reference[7423] = 'value_476022'
    assert tree.invariants(), "Invariants failed at step 1093"

    # Operation 1094: insert
    tree[6244] = 'value_788741'
    reference[6244] = 'value_788741'
    assert tree.invariants(), "Invariants failed at step 1094"

    # Operation 1095: insert
    tree[2086] = 'value_615313'
    reference[2086] = 'value_615313'
    assert tree.invariants(), "Invariants failed at step 1095"

    # Operation 1096: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1096"

    # Operation 1097: insert
    tree[5308] = 'value_166327'
    reference[5308] = 'value_166327'
    assert tree.invariants(), "Invariants failed at step 1097"

    # Operation 1098: delete
    del tree[7423]
    del reference[7423]
    assert tree.invariants(), "Invariants failed at step 1098"

    # Operation 1099: get
    assert tree.invariants(), "Invariants failed at step 1099"

    # Operation 1100: delete
    del tree[2086]
    del reference[2086]
    assert tree.invariants(), "Invariants failed at step 1100"

    # Operation 1101: update
    tree[5308] = 'value_369720'
    reference[5308] = 'value_369720'
    assert tree.invariants(), "Invariants failed at step 1101"

    # Operation 1102: delete
    del tree[5308]
    del reference[5308]
    assert tree.invariants(), "Invariants failed at step 1102"

    # Operation 1103: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1103"

    # Operation 1104: get
    assert tree.invariants(), "Invariants failed at step 1104"

    # Operation 1105: delete
    del tree[6244]
    del reference[6244]
    assert tree.invariants(), "Invariants failed at step 1105"

    # Operation 1106: delete
    del tree[5915]
    del reference[5915]
    assert tree.invariants(), "Invariants failed at step 1106"

    # Operation 1107: insert
    tree[4179] = 'value_335998'
    reference[4179] = 'value_335998'
    assert tree.invariants(), "Invariants failed at step 1107"

    # Operation 1108: delete
    del tree[4179]
    del reference[4179]
    assert tree.invariants(), "Invariants failed at step 1108"

    # Operation 1109: get
    assert tree.invariants(), "Invariants failed at step 1109"

    # Operation 1110: get
    assert tree.invariants(), "Invariants failed at step 1110"

    # Operation 1111: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1111"

    # Operation 1112: get
    assert tree.invariants(), "Invariants failed at step 1112"

    # Operation 1113: get
    assert tree.invariants(), "Invariants failed at step 1113"

    # Operation 1114: get
    assert tree.invariants(), "Invariants failed at step 1114"

    # Operation 1115: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1115"

    # Operation 1116: insert
    tree[9288] = 'value_498027'
    reference[9288] = 'value_498027'
    assert tree.invariants(), "Invariants failed at step 1116"

    # Operation 1117: insert
    tree[8238] = 'value_385984'
    reference[8238] = 'value_385984'
    assert tree.invariants(), "Invariants failed at step 1117"

    # Operation 1118: delete
    del tree[9288]
    del reference[9288]
    assert tree.invariants(), "Invariants failed at step 1118"

    # Operation 1119: get
    assert tree.invariants(), "Invariants failed at step 1119"

    # Operation 1120: get
    assert tree.invariants(), "Invariants failed at step 1120"

    # Operation 1121: delete
    del tree[8238]
    del reference[8238]
    assert tree.invariants(), "Invariants failed at step 1121"

    # Operation 1122: get
    assert tree.invariants(), "Invariants failed at step 1122"

    # Operation 1123: insert
    tree[8346] = 'value_445833'
    reference[8346] = 'value_445833'
    assert tree.invariants(), "Invariants failed at step 1123"

    # Operation 1124: delete
    del tree[8346]
    del reference[8346]
    assert tree.invariants(), "Invariants failed at step 1124"

    # Operation 1125: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1125"

    # Operation 1126: insert
    tree[219] = 'value_371550'
    reference[219] = 'value_371550'
    assert tree.invariants(), "Invariants failed at step 1126"

    # Operation 1127: get
    assert tree.invariants(), "Invariants failed at step 1127"

    # Operation 1128: insert
    tree[1634] = 'value_107286'
    reference[1634] = 'value_107286'
    assert tree.invariants(), "Invariants failed at step 1128"

    # Operation 1129: delete
    del tree[1634]
    del reference[1634]
    assert tree.invariants(), "Invariants failed at step 1129"

    # Operation 1130: get
    assert tree.invariants(), "Invariants failed at step 1130"

    # Operation 1131: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1131"

    # Operation 1132: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1132"

    # Operation 1133: delete
    del tree[219]
    del reference[219]
    assert tree.invariants(), "Invariants failed at step 1133"

    # Operation 1134: get
    assert tree.invariants(), "Invariants failed at step 1134"

    # Operation 1135: insert
    tree[8712] = 'value_532261'
    reference[8712] = 'value_532261'
    assert tree.invariants(), "Invariants failed at step 1135"

    # Operation 1136: delete
    del tree[8712]
    del reference[8712]
    assert tree.invariants(), "Invariants failed at step 1136"

    # Operation 1137: insert
    tree[3676] = 'value_105951'
    reference[3676] = 'value_105951'
    assert tree.invariants(), "Invariants failed at step 1137"

    # Operation 1138: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1138"

    # Operation 1139: get
    assert tree.invariants(), "Invariants failed at step 1139"

    # Operation 1140: delete
    del tree[3676]
    del reference[3676]
    assert tree.invariants(), "Invariants failed at step 1140"

    # Operation 1141: insert
    tree[4528] = 'value_941722'
    reference[4528] = 'value_941722'
    assert tree.invariants(), "Invariants failed at step 1141"

    # Operation 1142: insert
    tree[3375] = 'value_412907'
    reference[3375] = 'value_412907'
    assert tree.invariants(), "Invariants failed at step 1142"

    # Operation 1143: insert
    tree[5781] = 'value_436485'
    reference[5781] = 'value_436485'
    assert tree.invariants(), "Invariants failed at step 1143"

    # Operation 1144: delete
    del tree[4528]
    del reference[4528]
    assert tree.invariants(), "Invariants failed at step 1144"

    # Operation 1145: update
    tree[5781] = 'value_338444'
    reference[5781] = 'value_338444'
    assert tree.invariants(), "Invariants failed at step 1145"

    # Operation 1146: get
    assert tree.invariants(), "Invariants failed at step 1146"

    # Operation 1147: insert
    tree[3278] = 'value_150393'
    reference[3278] = 'value_150393'
    assert tree.invariants(), "Invariants failed at step 1147"

    # Operation 1148: get
    assert tree.invariants(), "Invariants failed at step 1148"

    # Operation 1149: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1149"

    # Operation 1150: get
    assert tree.invariants(), "Invariants failed at step 1150"

    # Operation 1151: get
    assert tree.invariants(), "Invariants failed at step 1151"

    # Operation 1152: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1152"

    # Operation 1153: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1153"

    # Operation 1154: get
    assert tree.invariants(), "Invariants failed at step 1154"

    # Operation 1155: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1155"

    # Operation 1156: insert
    tree[348] = 'value_351780'
    reference[348] = 'value_351780'
    assert tree.invariants(), "Invariants failed at step 1156"

    # Operation 1157: get
    assert tree.invariants(), "Invariants failed at step 1157"

    # Operation 1158: insert
    tree[1501] = 'value_483339'
    reference[1501] = 'value_483339'
    assert tree.invariants(), "Invariants failed at step 1158"

    # Operation 1159: insert
    tree[1236] = 'value_982437'
    reference[1236] = 'value_982437'
    assert tree.invariants(), "Invariants failed at step 1159"

    # Operation 1160: batch_delete
    keys_to_delete = [1221, 7143, 3278, 3375, 1236]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1160"

    # Operation 1161: insert
    tree[9666] = 'value_64147'
    reference[9666] = 'value_64147'
    assert tree.invariants(), "Invariants failed at step 1161"

    # Operation 1162: get
    assert tree.invariants(), "Invariants failed at step 1162"

    # Operation 1163: delete
    del tree[348]
    del reference[348]
    assert tree.invariants(), "Invariants failed at step 1163"

    # Operation 1164: update
    tree[1501] = 'value_230046'
    reference[1501] = 'value_230046'
    assert tree.invariants(), "Invariants failed at step 1164"

    # Operation 1165: get
    assert tree.invariants(), "Invariants failed at step 1165"

    # Operation 1166: update
    tree[9666] = 'value_728911'
    reference[9666] = 'value_728911'
    assert tree.invariants(), "Invariants failed at step 1166"

    # Operation 1167: delete
    del tree[5781]
    del reference[5781]
    assert tree.invariants(), "Invariants failed at step 1167"

    # Operation 1168: insert
    tree[6518] = 'value_973182'
    reference[6518] = 'value_973182'
    assert tree.invariants(), "Invariants failed at step 1168"

    # Operation 1169: insert
    tree[8977] = 'value_796626'
    reference[8977] = 'value_796626'
    assert tree.invariants(), "Invariants failed at step 1169"

    # Operation 1170: insert
    tree[981] = 'value_345777'
    reference[981] = 'value_345777'
    assert tree.invariants(), "Invariants failed at step 1170"

    # Operation 1171: get
    assert tree.invariants(), "Invariants failed at step 1171"

    # Operation 1172: insert
    tree[1714] = 'value_499987'
    reference[1714] = 'value_499987'
    assert tree.invariants(), "Invariants failed at step 1172"

    # Operation 1173: update
    tree[1714] = 'value_605577'
    reference[1714] = 'value_605577'
    assert tree.invariants(), "Invariants failed at step 1173"

    # Operation 1174: insert
    tree[3396] = 'value_269496'
    reference[3396] = 'value_269496'
    assert tree.invariants(), "Invariants failed at step 1174"

    # Operation 1175: delete
    del tree[981]
    del reference[981]
    assert tree.invariants(), "Invariants failed at step 1175"

    # Operation 1176: get
    assert tree.invariants(), "Invariants failed at step 1176"

    # Operation 1177: batch_delete
    keys_to_delete = [3042, 7249, 8977, 6518, 1501]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1177"

    # Operation 1178: delete
    del tree[1714]
    del reference[1714]
    assert tree.invariants(), "Invariants failed at step 1178"

    # Operation 1179: update
    tree[9666] = 'value_974355'
    reference[9666] = 'value_974355'
    assert tree.invariants(), "Invariants failed at step 1179"

    # Operation 1180: get
    assert tree.invariants(), "Invariants failed at step 1180"

    # Operation 1181: delete
    del tree[9666]
    del reference[9666]
    assert tree.invariants(), "Invariants failed at step 1181"

    # Operation 1182: get
    assert tree.invariants(), "Invariants failed at step 1182"

    # Operation 1183: delete
    del tree[3396]
    del reference[3396]
    assert tree.invariants(), "Invariants failed at step 1183"

    # Operation 1184: get
    assert tree.invariants(), "Invariants failed at step 1184"

    # Operation 1185: insert
    tree[5300] = 'value_710546'
    reference[5300] = 'value_710546'
    assert tree.invariants(), "Invariants failed at step 1185"

    # Operation 1186: update
    tree[5300] = 'value_545831'
    reference[5300] = 'value_545831'
    assert tree.invariants(), "Invariants failed at step 1186"

    # Operation 1187: insert
    tree[9962] = 'value_129612'
    reference[9962] = 'value_129612'
    assert tree.invariants(), "Invariants failed at step 1187"

    # Operation 1188: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1188"

    # Operation 1189: get
    assert tree.invariants(), "Invariants failed at step 1189"

    # Operation 1190: delete
    del tree[5300]
    del reference[5300]
    assert tree.invariants(), "Invariants failed at step 1190"

    # Operation 1191: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1191"

    # Operation 1192: update
    tree[9962] = 'value_329001'
    reference[9962] = 'value_329001'
    assert tree.invariants(), "Invariants failed at step 1192"

    # Operation 1193: delete
    del tree[9962]
    del reference[9962]
    assert tree.invariants(), "Invariants failed at step 1193"

    # Operation 1194: get
    assert tree.invariants(), "Invariants failed at step 1194"

    # Operation 1195: get
    assert tree.invariants(), "Invariants failed at step 1195"

    # Operation 1196: insert
    tree[4381] = 'value_784093'
    reference[4381] = 'value_784093'
    assert tree.invariants(), "Invariants failed at step 1196"

    # Operation 1197: update
    tree[4381] = 'value_75814'
    reference[4381] = 'value_75814'
    assert tree.invariants(), "Invariants failed at step 1197"

    # Operation 1198: delete
    del tree[4381]
    del reference[4381]
    assert tree.invariants(), "Invariants failed at step 1198"

    # Operation 1199: insert
    tree[4588] = 'value_969119'
    reference[4588] = 'value_969119'
    assert tree.invariants(), "Invariants failed at step 1199"

    # Operation 1200: delete
    del tree[4588]
    del reference[4588]
    assert tree.invariants(), "Invariants failed at step 1200"

    # Operation 1201: get
    assert tree.invariants(), "Invariants failed at step 1201"

    # Operation 1202: insert
    tree[1986] = 'value_301355'
    reference[1986] = 'value_301355'
    assert tree.invariants(), "Invariants failed at step 1202"

    # Operation 1203: insert
    tree[9348] = 'value_380049'
    reference[9348] = 'value_380049'
    assert tree.invariants(), "Invariants failed at step 1203"

    # Operation 1204: update
    tree[1986] = 'value_828103'
    reference[1986] = 'value_828103'
    assert tree.invariants(), "Invariants failed at step 1204"

    # Operation 1205: insert
    tree[8399] = 'value_649104'
    reference[8399] = 'value_649104'
    assert tree.invariants(), "Invariants failed at step 1205"

    # Operation 1206: update
    tree[8399] = 'value_282639'
    reference[8399] = 'value_282639'
    assert tree.invariants(), "Invariants failed at step 1206"

    # Operation 1207: insert
    tree[9579] = 'value_843562'
    reference[9579] = 'value_843562'
    assert tree.invariants(), "Invariants failed at step 1207"

    # Operation 1208: insert
    tree[7111] = 'value_867774'
    reference[7111] = 'value_867774'
    assert tree.invariants(), "Invariants failed at step 1208"

    # Operation 1209: update
    tree[9579] = 'value_352966'
    reference[9579] = 'value_352966'
    assert tree.invariants(), "Invariants failed at step 1209"

    # Operation 1210: batch_delete
    keys_to_delete = [10, 9579, 4244, 8399]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1210"

    # Operation 1211: insert
    tree[2147] = 'value_706043'
    reference[2147] = 'value_706043'
    assert tree.invariants(), "Invariants failed at step 1211"

    # Operation 1212: get
    assert tree.invariants(), "Invariants failed at step 1212"

    # Operation 1213: insert
    tree[9350] = 'value_663680'
    reference[9350] = 'value_663680'
    assert tree.invariants(), "Invariants failed at step 1213"

    # Operation 1214: insert
    tree[2365] = 'value_107905'
    reference[2365] = 'value_107905'
    assert tree.invariants(), "Invariants failed at step 1214"

    # Operation 1215: insert
    tree[6517] = 'value_520562'
    reference[6517] = 'value_520562'
    assert tree.invariants(), "Invariants failed at step 1215"

    # Operation 1216: delete
    del tree[7111]
    del reference[7111]
    assert tree.invariants(), "Invariants failed at step 1216"

    # Operation 1217: insert
    tree[4720] = 'value_451480'
    reference[4720] = 'value_451480'
    assert tree.invariants(), "Invariants failed at step 1217"

    # Operation 1218: update
    tree[1986] = 'value_306327'
    reference[1986] = 'value_306327'
    assert tree.invariants(), "Invariants failed at step 1218"

    # Operation 1219: get
    assert tree.invariants(), "Invariants failed at step 1219"

    # Operation 1220: insert
    tree[1749] = 'value_955376'
    reference[1749] = 'value_955376'
    assert tree.invariants(), "Invariants failed at step 1220"

    # Operation 1221: get
    assert tree.invariants(), "Invariants failed at step 1221"

    # Operation 1222: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1222"

    # Operation 1223: insert
    tree[5015] = 'value_195375'
    reference[5015] = 'value_195375'
    assert tree.invariants(), "Invariants failed at step 1223"

    # Operation 1224: insert
    tree[5433] = 'value_946539'
    reference[5433] = 'value_946539'
    assert tree.invariants(), "Invariants failed at step 1224"

    # Operation 1225: delete
    del tree[2147]
    del reference[2147]
    assert tree.invariants(), "Invariants failed at step 1225"

    # Operation 1226: insert
    tree[8671] = 'value_152343'
    reference[8671] = 'value_152343'
    assert tree.invariants(), "Invariants failed at step 1226"

    # Operation 1227: update
    tree[4720] = 'value_943339'
    reference[4720] = 'value_943339'
    assert tree.invariants(), "Invariants failed at step 1227"

    # Operation 1228: update
    tree[6517] = 'value_366472'
    reference[6517] = 'value_366472'
    assert tree.invariants(), "Invariants failed at step 1228"

    # Operation 1229: insert
    tree[6834] = 'value_261078'
    reference[6834] = 'value_261078'
    assert tree.invariants(), "Invariants failed at step 1229"

    # Operation 1230: batch_delete
    keys_to_delete = [8268, 1749, 6517, 5015, 1656, 5433, 2365]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1230"

    # Operation 1231: batch_delete
    keys_to_delete = [1986, 9090, 9350, 110, 8671]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1231"

    # Operation 1232: get
    assert tree.invariants(), "Invariants failed at step 1232"

    # Operation 1233: insert
    tree[5740] = 'value_674362'
    reference[5740] = 'value_674362'
    assert tree.invariants(), "Invariants failed at step 1233"

    # Operation 1234: insert
    tree[9829] = 'value_19908'
    reference[9829] = 'value_19908'
    assert tree.invariants(), "Invariants failed at step 1234"

    # Operation 1235: update
    tree[5740] = 'value_785718'
    reference[5740] = 'value_785718'
    assert tree.invariants(), "Invariants failed at step 1235"

    # Operation 1236: insert
    tree[8082] = 'value_115619'
    reference[8082] = 'value_115619'
    assert tree.invariants(), "Invariants failed at step 1236"

    # Operation 1237: insert
    tree[6392] = 'value_728983'
    reference[6392] = 'value_728983'
    assert tree.invariants(), "Invariants failed at step 1237"

    # Operation 1238: get
    assert tree.invariants(), "Invariants failed at step 1238"

    # Operation 1239: batch_delete
    keys_to_delete = [9829, 7563, 5740, 4720, 5438]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1239"

    # Operation 1240: insert
    tree[9020] = 'value_391993'
    reference[9020] = 'value_391993'
    assert tree.invariants(), "Invariants failed at step 1240"

    # Operation 1241: delete
    del tree[9020]
    del reference[9020]
    assert tree.invariants(), "Invariants failed at step 1241"

    # Operation 1242: insert
    tree[1079] = 'value_5105'
    reference[1079] = 'value_5105'
    assert tree.invariants(), "Invariants failed at step 1242"

    # Operation 1243: get
    assert tree.invariants(), "Invariants failed at step 1243"

    # Operation 1244: insert
    tree[8661] = 'value_902498'
    reference[8661] = 'value_902498'
    assert tree.invariants(), "Invariants failed at step 1244"

    # Operation 1245: batch_delete
    keys_to_delete = [6392, 8082, 8661]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1245"

    # Operation 1246: delete
    del tree[1079]
    del reference[1079]
    assert tree.invariants(), "Invariants failed at step 1246"

    # Operation 1247: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1247"

    # Operation 1248: get
    assert tree.invariants(), "Invariants failed at step 1248"

    # Operation 1249: get
    assert tree.invariants(), "Invariants failed at step 1249"

    # Operation 1250: insert
    tree[3176] = 'value_99505'
    reference[3176] = 'value_99505'
    assert tree.invariants(), "Invariants failed at step 1250"

    # Operation 1251: delete
    del tree[6834]
    del reference[6834]
    assert tree.invariants(), "Invariants failed at step 1251"

    # Operation 1252: update
    tree[9348] = 'value_226881'
    reference[9348] = 'value_226881'
    assert tree.invariants(), "Invariants failed at step 1252"

    # Operation 1253: insert
    tree[883] = 'value_632822'
    reference[883] = 'value_632822'
    assert tree.invariants(), "Invariants failed at step 1253"

    # Operation 1254: insert
    tree[8521] = 'value_616949'
    reference[8521] = 'value_616949'
    assert tree.invariants(), "Invariants failed at step 1254"

    # Operation 1255: delete
    del tree[3176]
    del reference[3176]
    assert tree.invariants(), "Invariants failed at step 1255"

    # Operation 1256: delete
    del tree[883]
    del reference[883]
    assert tree.invariants(), "Invariants failed at step 1256"

    # Operation 1257: insert
    tree[9137] = 'value_165034'
    reference[9137] = 'value_165034'
    assert tree.invariants(), "Invariants failed at step 1257"

    # Operation 1258: delete
    del tree[9348]
    del reference[9348]
    assert tree.invariants(), "Invariants failed at step 1258"

    # Operation 1259: update
    tree[8521] = 'value_26908'
    reference[8521] = 'value_26908'
    assert tree.invariants(), "Invariants failed at step 1259"

    # Operation 1260: get
    assert tree.invariants(), "Invariants failed at step 1260"

    # Operation 1261: delete
    del tree[8521]
    del reference[8521]
    assert tree.invariants(), "Invariants failed at step 1261"

    # Operation 1262: insert
    tree[9526] = 'value_673235'
    reference[9526] = 'value_673235'
    assert tree.invariants(), "Invariants failed at step 1262"

    # Operation 1263: get
    assert tree.invariants(), "Invariants failed at step 1263"

    # Operation 1264: delete
    del tree[9137]
    del reference[9137]
    assert tree.invariants(), "Invariants failed at step 1264"

    # Operation 1265: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1265"

    # Operation 1266: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1266"

    # Operation 1267: insert
    tree[8591] = 'value_26800'
    reference[8591] = 'value_26800'
    assert tree.invariants(), "Invariants failed at step 1267"

    # Operation 1268: get
    assert tree.invariants(), "Invariants failed at step 1268"

    # Operation 1269: delete
    del tree[9526]
    del reference[9526]
    assert tree.invariants(), "Invariants failed at step 1269"

    # Operation 1270: update
    tree[8591] = 'value_670352'
    reference[8591] = 'value_670352'
    assert tree.invariants(), "Invariants failed at step 1270"

    # Operation 1271: get
    assert tree.invariants(), "Invariants failed at step 1271"

    # Operation 1272: insert
    tree[692] = 'value_186568'
    reference[692] = 'value_186568'
    assert tree.invariants(), "Invariants failed at step 1272"

    # Operation 1273: update
    tree[692] = 'value_612329'
    reference[692] = 'value_612329'
    assert tree.invariants(), "Invariants failed at step 1273"

    # Operation 1274: insert
    tree[5950] = 'value_140675'
    reference[5950] = 'value_140675'
    assert tree.invariants(), "Invariants failed at step 1274"

    # Operation 1275: delete
    del tree[5950]
    del reference[5950]
    assert tree.invariants(), "Invariants failed at step 1275"

    # Operation 1276: update
    tree[692] = 'value_279656'
    reference[692] = 'value_279656'
    assert tree.invariants(), "Invariants failed at step 1276"

    # Operation 1277: delete
    del tree[692]
    del reference[692]
    assert tree.invariants(), "Invariants failed at step 1277"

    # Operation 1278: insert
    tree[8677] = 'value_485345'
    reference[8677] = 'value_485345'
    assert tree.invariants(), "Invariants failed at step 1278"

    # Operation 1279: delete
    del tree[8591]
    del reference[8591]
    assert tree.invariants(), "Invariants failed at step 1279"

    # Operation 1280: insert
    tree[727] = 'value_27190'
    reference[727] = 'value_27190'
    assert tree.invariants(), "Invariants failed at step 1280"

    # Operation 1281: insert
    tree[4182] = 'value_522517'
    reference[4182] = 'value_522517'
    assert tree.invariants(), "Invariants failed at step 1281"

    # Operation 1282: delete
    del tree[727]
    del reference[727]
    assert tree.invariants(), "Invariants failed at step 1282"

    # Operation 1283: delete
    del tree[4182]
    del reference[4182]
    assert tree.invariants(), "Invariants failed at step 1283"

    # Operation 1284: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1284"

    # Operation 1285: update
    tree[8677] = 'value_413251'
    reference[8677] = 'value_413251'
    assert tree.invariants(), "Invariants failed at step 1285"

    # Operation 1286: delete
    del tree[8677]
    del reference[8677]
    assert tree.invariants(), "Invariants failed at step 1286"

    # Operation 1287: get
    assert tree.invariants(), "Invariants failed at step 1287"

    # Operation 1288: get
    assert tree.invariants(), "Invariants failed at step 1288"

    # Operation 1289: insert
    tree[5913] = 'value_945219'
    reference[5913] = 'value_945219'
    assert tree.invariants(), "Invariants failed at step 1289"

    # Operation 1290: insert
    tree[6874] = 'value_998734'
    reference[6874] = 'value_998734'
    assert tree.invariants(), "Invariants failed at step 1290"

    # Operation 1291: insert
    tree[943] = 'value_146158'
    reference[943] = 'value_146158'
    assert tree.invariants(), "Invariants failed at step 1291"

    # Operation 1292: delete
    del tree[943]
    del reference[943]
    assert tree.invariants(), "Invariants failed at step 1292"

    # Operation 1293: insert
    tree[367] = 'value_936919'
    reference[367] = 'value_936919'
    assert tree.invariants(), "Invariants failed at step 1293"

    # Operation 1294: update
    tree[367] = 'value_677304'
    reference[367] = 'value_677304'
    assert tree.invariants(), "Invariants failed at step 1294"

    # Operation 1295: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1295"

    # Operation 1296: delete
    del tree[367]
    del reference[367]
    assert tree.invariants(), "Invariants failed at step 1296"

    # Operation 1297: get
    assert tree.invariants(), "Invariants failed at step 1297"

    # Operation 1298: get
    assert tree.invariants(), "Invariants failed at step 1298"

    # Operation 1299: delete
    del tree[5913]
    del reference[5913]
    assert tree.invariants(), "Invariants failed at step 1299"

    # Operation 1300: insert
    tree[222] = 'value_766052'
    reference[222] = 'value_766052'
    assert tree.invariants(), "Invariants failed at step 1300"

    # Operation 1301: delete
    del tree[6874]
    del reference[6874]
    assert tree.invariants(), "Invariants failed at step 1301"

    # Operation 1302: delete
    del tree[222]
    del reference[222]
    assert tree.invariants(), "Invariants failed at step 1302"

    # Operation 1303: insert
    tree[2653] = 'value_817319'
    reference[2653] = 'value_817319'
    assert tree.invariants(), "Invariants failed at step 1303"

    # Operation 1304: insert
    tree[8292] = 'value_996722'
    reference[8292] = 'value_996722'
    assert tree.invariants(), "Invariants failed at step 1304"

    # Operation 1305: insert
    tree[624] = 'value_945429'
    reference[624] = 'value_945429'
    assert tree.invariants(), "Invariants failed at step 1305"

    # Operation 1306: get
    assert tree.invariants(), "Invariants failed at step 1306"

    # Operation 1307: update
    tree[624] = 'value_847529'
    reference[624] = 'value_847529'
    assert tree.invariants(), "Invariants failed at step 1307"

    # Operation 1308: get
    assert tree.invariants(), "Invariants failed at step 1308"

    # Operation 1309: insert
    tree[2412] = 'value_677918'
    reference[2412] = 'value_677918'
    assert tree.invariants(), "Invariants failed at step 1309"

    # Operation 1310: update
    tree[624] = 'value_873161'
    reference[624] = 'value_873161'
    assert tree.invariants(), "Invariants failed at step 1310"

    # Operation 1311: update
    tree[2653] = 'value_604568'
    reference[2653] = 'value_604568'
    assert tree.invariants(), "Invariants failed at step 1311"

    # Operation 1312: insert
    tree[5775] = 'value_826270'
    reference[5775] = 'value_826270'
    assert tree.invariants(), "Invariants failed at step 1312"

    # Operation 1313: batch_delete
    keys_to_delete = [8292, 2653, 2454, 7301]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1313"

    # Operation 1314: get
    assert tree.invariants(), "Invariants failed at step 1314"

    # Operation 1315: get
    assert tree.invariants(), "Invariants failed at step 1315"

    # Operation 1316: insert
    tree[2559] = 'value_580984'
    reference[2559] = 'value_580984'
    assert tree.invariants(), "Invariants failed at step 1316"

    # Operation 1317: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1317"

    # Operation 1318: insert
    tree[1155] = 'value_524290'
    reference[1155] = 'value_524290'
    assert tree.invariants(), "Invariants failed at step 1318"

    # Operation 1319: get
    assert tree.invariants(), "Invariants failed at step 1319"

    # Operation 1320: insert
    tree[3394] = 'value_574466'
    reference[3394] = 'value_574466'
    assert tree.invariants(), "Invariants failed at step 1320"

    # Operation 1321: update
    tree[2559] = 'value_114500'
    reference[2559] = 'value_114500'
    assert tree.invariants(), "Invariants failed at step 1321"

    # Operation 1322: insert
    tree[4259] = 'value_226740'
    reference[4259] = 'value_226740'
    assert tree.invariants(), "Invariants failed at step 1322"

    # Operation 1323: batch_delete
    keys_to_delete = [3394, 4260, 2412, 624, 8082]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1323"

    # Operation 1324: get
    assert tree.invariants(), "Invariants failed at step 1324"

    # Operation 1325: delete
    del tree[4259]
    del reference[4259]
    assert tree.invariants(), "Invariants failed at step 1325"

    # Operation 1326: delete
    del tree[2559]
    del reference[2559]
    assert tree.invariants(), "Invariants failed at step 1326"

    # Operation 1327: update
    tree[5775] = 'value_762730'
    reference[5775] = 'value_762730'
    assert tree.invariants(), "Invariants failed at step 1327"

    # Operation 1328: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1328"

    # Operation 1329: delete
    del tree[5775]
    del reference[5775]
    assert tree.invariants(), "Invariants failed at step 1329"

    # Operation 1330: update
    tree[1155] = 'value_647902'
    reference[1155] = 'value_647902'
    assert tree.invariants(), "Invariants failed at step 1330"

    # Operation 1331: delete
    del tree[1155]
    del reference[1155]
    assert tree.invariants(), "Invariants failed at step 1331"

    # Operation 1332: get
    assert tree.invariants(), "Invariants failed at step 1332"

    # Operation 1333: insert
    tree[4210] = 'value_556672'
    reference[4210] = 'value_556672'
    assert tree.invariants(), "Invariants failed at step 1333"

    # Operation 1334: delete
    del tree[4210]
    del reference[4210]
    assert tree.invariants(), "Invariants failed at step 1334"

    # Operation 1335: get
    assert tree.invariants(), "Invariants failed at step 1335"

    # Operation 1336: insert
    tree[7159] = 'value_878105'
    reference[7159] = 'value_878105'
    assert tree.invariants(), "Invariants failed at step 1336"

    # Operation 1337: delete
    del tree[7159]
    del reference[7159]
    assert tree.invariants(), "Invariants failed at step 1337"

    # Operation 1338: insert
    tree[9443] = 'value_700023'
    reference[9443] = 'value_700023'
    assert tree.invariants(), "Invariants failed at step 1338"

    # Operation 1339: insert
    tree[7275] = 'value_684826'
    reference[7275] = 'value_684826'
    assert tree.invariants(), "Invariants failed at step 1339"

    # Operation 1340: insert
    tree[1319] = 'value_629075'
    reference[1319] = 'value_629075'
    assert tree.invariants(), "Invariants failed at step 1340"

    # Operation 1341: get
    assert tree.invariants(), "Invariants failed at step 1341"

    # Operation 1342: delete
    del tree[9443]
    del reference[9443]
    assert tree.invariants(), "Invariants failed at step 1342"

    # Operation 1343: insert
    tree[4117] = 'value_353765'
    reference[4117] = 'value_353765'
    assert tree.invariants(), "Invariants failed at step 1343"

    # Operation 1344: delete
    del tree[1319]
    del reference[1319]
    assert tree.invariants(), "Invariants failed at step 1344"

    # Operation 1345: insert
    tree[315] = 'value_176596'
    reference[315] = 'value_176596'
    assert tree.invariants(), "Invariants failed at step 1345"

    # Operation 1346: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1346"

    # Operation 1347: delete
    del tree[315]
    del reference[315]
    assert tree.invariants(), "Invariants failed at step 1347"

    # Operation 1348: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1348"

    # Operation 1349: delete
    del tree[4117]
    del reference[4117]
    assert tree.invariants(), "Invariants failed at step 1349"

    # Operation 1350: delete
    del tree[7275]
    del reference[7275]
    assert tree.invariants(), "Invariants failed at step 1350"

    # Operation 1351: get
    assert tree.invariants(), "Invariants failed at step 1351"

    # Operation 1352: insert
    tree[4170] = 'value_931136'
    reference[4170] = 'value_931136'
    assert tree.invariants(), "Invariants failed at step 1352"

    # Operation 1353: get
    assert tree.invariants(), "Invariants failed at step 1353"

    # Operation 1354: get
    assert tree.invariants(), "Invariants failed at step 1354"

    # Operation 1355: insert
    tree[9442] = 'value_885891'
    reference[9442] = 'value_885891'
    assert tree.invariants(), "Invariants failed at step 1355"

    # Operation 1356: get
    assert tree.invariants(), "Invariants failed at step 1356"

    # Operation 1357: get
    assert tree.invariants(), "Invariants failed at step 1357"

    # Operation 1358: update
    tree[4170] = 'value_443558'
    reference[4170] = 'value_443558'
    assert tree.invariants(), "Invariants failed at step 1358"

    # Operation 1359: delete
    del tree[4170]
    del reference[4170]
    assert tree.invariants(), "Invariants failed at step 1359"

    # Operation 1360: delete
    del tree[9442]
    del reference[9442]
    assert tree.invariants(), "Invariants failed at step 1360"

    # Operation 1361: insert
    tree[648] = 'value_661389'
    reference[648] = 'value_661389'
    assert tree.invariants(), "Invariants failed at step 1361"

    # Operation 1362: insert
    tree[2871] = 'value_302125'
    reference[2871] = 'value_302125'
    assert tree.invariants(), "Invariants failed at step 1362"

    # Operation 1363: insert
    tree[3775] = 'value_771457'
    reference[3775] = 'value_771457'
    assert tree.invariants(), "Invariants failed at step 1363"

    # Operation 1364: delete
    del tree[2871]
    del reference[2871]
    assert tree.invariants(), "Invariants failed at step 1364"

    # Operation 1365: update
    tree[648] = 'value_703084'
    reference[648] = 'value_703084'
    assert tree.invariants(), "Invariants failed at step 1365"

    # Operation 1366: insert
    tree[8314] = 'value_613748'
    reference[8314] = 'value_613748'
    assert tree.invariants(), "Invariants failed at step 1366"

    # Operation 1367: insert
    tree[3048] = 'value_858888'
    reference[3048] = 'value_858888'
    assert tree.invariants(), "Invariants failed at step 1367"

    # Operation 1368: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1368"

    # Operation 1369: insert
    tree[3885] = 'value_258019'
    reference[3885] = 'value_258019'
    assert tree.invariants(), "Invariants failed at step 1369"

    # Operation 1370: get
    assert tree.invariants(), "Invariants failed at step 1370"

    # Operation 1371: insert
    tree[9757] = 'value_898713'
    reference[9757] = 'value_898713'
    assert tree.invariants(), "Invariants failed at step 1371"

    # Operation 1372: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1372"

    # Operation 1373: update
    tree[3048] = 'value_453398'
    reference[3048] = 'value_453398'
    assert tree.invariants(), "Invariants failed at step 1373"

    # Operation 1374: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1374"

    # Operation 1375: delete
    del tree[8314]
    del reference[8314]
    assert tree.invariants(), "Invariants failed at step 1375"

    # Operation 1376: get
    assert tree.invariants(), "Invariants failed at step 1376"

    # Operation 1377: update
    tree[648] = 'value_306729'
    reference[648] = 'value_306729'
    assert tree.invariants(), "Invariants failed at step 1377"

    # Operation 1378: batch_delete
    keys_to_delete = [3048, 9937, 275, 9757]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1378"

    # Operation 1379: get
    assert tree.invariants(), "Invariants failed at step 1379"

    # Operation 1380: insert
    tree[4374] = 'value_631390'
    reference[4374] = 'value_631390'
    assert tree.invariants(), "Invariants failed at step 1380"

    # Operation 1381: delete
    del tree[4374]
    del reference[4374]
    assert tree.invariants(), "Invariants failed at step 1381"

    # Operation 1382: delete
    del tree[3775]
    del reference[3775]
    assert tree.invariants(), "Invariants failed at step 1382"

    # Operation 1383: update
    tree[648] = 'value_760553'
    reference[648] = 'value_760553'
    assert tree.invariants(), "Invariants failed at step 1383"

    # Operation 1384: get
    assert tree.invariants(), "Invariants failed at step 1384"

    # Operation 1385: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1385"

    # Operation 1386: update
    tree[3885] = 'value_470034'
    reference[3885] = 'value_470034'
    assert tree.invariants(), "Invariants failed at step 1386"

    # Operation 1387: get
    assert tree.invariants(), "Invariants failed at step 1387"

    # Operation 1388: delete
    del tree[648]
    del reference[648]
    assert tree.invariants(), "Invariants failed at step 1388"

    # Operation 1389: insert
    tree[2426] = 'value_547466'
    reference[2426] = 'value_547466'
    assert tree.invariants(), "Invariants failed at step 1389"

    # Operation 1390: insert
    tree[8933] = 'value_730112'
    reference[8933] = 'value_730112'
    assert tree.invariants(), "Invariants failed at step 1390"

    # Operation 1391: insert
    tree[7676] = 'value_876766'
    reference[7676] = 'value_876766'
    assert tree.invariants(), "Invariants failed at step 1391"

    # Operation 1392: insert
    tree[7863] = 'value_219381'
    reference[7863] = 'value_219381'
    assert tree.invariants(), "Invariants failed at step 1392"

    # Operation 1393: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1393"

    # Operation 1394: get
    assert tree.invariants(), "Invariants failed at step 1394"

    # Operation 1395: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1395"

    # Operation 1396: delete
    del tree[3885]
    del reference[3885]
    assert tree.invariants(), "Invariants failed at step 1396"

    # Operation 1397: update
    tree[8933] = 'value_454910'
    reference[8933] = 'value_454910'
    assert tree.invariants(), "Invariants failed at step 1397"

    # Operation 1398: delete
    del tree[7863]
    del reference[7863]
    assert tree.invariants(), "Invariants failed at step 1398"

    # Operation 1399: insert
    tree[7359] = 'value_841353'
    reference[7359] = 'value_841353'
    assert tree.invariants(), "Invariants failed at step 1399"

    # Operation 1400: delete
    del tree[8933]
    del reference[8933]
    assert tree.invariants(), "Invariants failed at step 1400"

    # Operation 1401: insert
    tree[7153] = 'value_922421'
    reference[7153] = 'value_922421'
    assert tree.invariants(), "Invariants failed at step 1401"

    # Operation 1402: delete
    del tree[7676]
    del reference[7676]
    assert tree.invariants(), "Invariants failed at step 1402"

    # Operation 1403: insert
    tree[8322] = 'value_528134'
    reference[8322] = 'value_528134'
    assert tree.invariants(), "Invariants failed at step 1403"

    # Operation 1404: get
    assert tree.invariants(), "Invariants failed at step 1404"

    # Operation 1405: get
    assert tree.invariants(), "Invariants failed at step 1405"

    # Operation 1406: insert
    tree[8036] = 'value_855332'
    reference[8036] = 'value_855332'
    assert tree.invariants(), "Invariants failed at step 1406"

    # Operation 1407: update
    tree[8322] = 'value_87815'
    reference[8322] = 'value_87815'
    assert tree.invariants(), "Invariants failed at step 1407"

    # Operation 1408: update
    tree[7153] = 'value_178705'
    reference[7153] = 'value_178705'
    assert tree.invariants(), "Invariants failed at step 1408"

    # Operation 1409: insert
    tree[6367] = 'value_655007'
    reference[6367] = 'value_655007'
    assert tree.invariants(), "Invariants failed at step 1409"

    # Operation 1410: insert
    tree[3765] = 'value_981233'
    reference[3765] = 'value_981233'
    assert tree.invariants(), "Invariants failed at step 1410"

    # Operation 1411: delete
    del tree[7153]
    del reference[7153]
    assert tree.invariants(), "Invariants failed at step 1411"

    # Operation 1412: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1412"

    # Operation 1413: insert
    tree[6111] = 'value_61425'
    reference[6111] = 'value_61425'
    assert tree.invariants(), "Invariants failed at step 1413"

    # Operation 1414: delete
    del tree[6367]
    del reference[6367]
    assert tree.invariants(), "Invariants failed at step 1414"

    # Operation 1415: delete
    del tree[8322]
    del reference[8322]
    assert tree.invariants(), "Invariants failed at step 1415"

    # Operation 1416: delete
    del tree[2426]
    del reference[2426]
    assert tree.invariants(), "Invariants failed at step 1416"

    # Operation 1417: delete
    del tree[7359]
    del reference[7359]
    assert tree.invariants(), "Invariants failed at step 1417"

    # Operation 1418: get
    assert tree.invariants(), "Invariants failed at step 1418"

    # Operation 1419: delete
    del tree[3765]
    del reference[3765]
    assert tree.invariants(), "Invariants failed at step 1419"

    # Operation 1420: insert
    tree[7704] = 'value_430896'
    reference[7704] = 'value_430896'
    assert tree.invariants(), "Invariants failed at step 1420"

    # Operation 1421: delete
    del tree[6111]
    del reference[6111]
    assert tree.invariants(), "Invariants failed at step 1421"

    # Operation 1422: insert
    tree[6569] = 'value_214444'
    reference[6569] = 'value_214444'
    assert tree.invariants(), "Invariants failed at step 1422"

    # Operation 1423: get
    assert tree.invariants(), "Invariants failed at step 1423"

    # Operation 1424: delete
    del tree[6569]
    del reference[6569]
    assert tree.invariants(), "Invariants failed at step 1424"

    # Operation 1425: get
    assert tree.invariants(), "Invariants failed at step 1425"

    # Operation 1426: delete
    del tree[7704]
    del reference[7704]
    assert tree.invariants(), "Invariants failed at step 1426"

    # Operation 1427: get
    assert tree.invariants(), "Invariants failed at step 1427"

    # Operation 1428: delete
    del tree[8036]
    del reference[8036]
    assert tree.invariants(), "Invariants failed at step 1428"

    # Operation 1429: insert
    tree[2668] = 'value_674340'
    reference[2668] = 'value_674340'
    assert tree.invariants(), "Invariants failed at step 1429"

    # Operation 1430: insert
    tree[570] = 'value_328501'
    reference[570] = 'value_328501'
    assert tree.invariants(), "Invariants failed at step 1430"

    # Operation 1431: update
    tree[570] = 'value_468551'
    reference[570] = 'value_468551'
    assert tree.invariants(), "Invariants failed at step 1431"

    # Operation 1432: insert
    tree[6223] = 'value_958726'
    reference[6223] = 'value_958726'
    assert tree.invariants(), "Invariants failed at step 1432"

    # Operation 1433: delete
    del tree[570]
    del reference[570]
    assert tree.invariants(), "Invariants failed at step 1433"

    # Operation 1434: insert
    tree[7573] = 'value_668246'
    reference[7573] = 'value_668246'
    assert tree.invariants(), "Invariants failed at step 1434"

    # Operation 1435: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1435"

    # Operation 1436: insert
    tree[3486] = 'value_970486'
    reference[3486] = 'value_970486'
    assert tree.invariants(), "Invariants failed at step 1436"

    # Operation 1437: insert
    tree[1037] = 'value_838964'
    reference[1037] = 'value_838964'
    assert tree.invariants(), "Invariants failed at step 1437"

    # Operation 1438: delete
    del tree[6223]
    del reference[6223]
    assert tree.invariants(), "Invariants failed at step 1438"

    # Operation 1439: insert
    tree[5662] = 'value_824098'
    reference[5662] = 'value_824098'
    assert tree.invariants(), "Invariants failed at step 1439"

    # Operation 1440: insert
    tree[4861] = 'value_301257'
    reference[4861] = 'value_301257'
    assert tree.invariants(), "Invariants failed at step 1440"

    # Operation 1441: insert
    tree[9577] = 'value_626549'
    reference[9577] = 'value_626549'
    assert tree.invariants(), "Invariants failed at step 1441"

    # Operation 1442: insert
    tree[3367] = 'value_953715'
    reference[3367] = 'value_953715'
    assert tree.invariants(), "Invariants failed at step 1442"

    # Operation 1443: get
    assert tree.invariants(), "Invariants failed at step 1443"

    # Operation 1444: insert
    tree[2292] = 'value_572835'
    reference[2292] = 'value_572835'
    assert tree.invariants(), "Invariants failed at step 1444"

    # Operation 1445: insert
    tree[4260] = 'value_103149'
    reference[4260] = 'value_103149'
    assert tree.invariants(), "Invariants failed at step 1445"

    # Operation 1446: insert
    tree[674] = 'value_230270'
    reference[674] = 'value_230270'
    assert tree.invariants(), "Invariants failed at step 1446"

    # Operation 1447: delete
    del tree[4861]
    del reference[4861]
    assert tree.invariants(), "Invariants failed at step 1447"

    # Operation 1448: batch_delete
    keys_to_delete = [674, 5662, 9577, 1037, 8946, 7196, 3486]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1448"

    # Operation 1449: update
    tree[2668] = 'value_223618'
    reference[2668] = 'value_223618'
    assert tree.invariants(), "Invariants failed at step 1449"

    # Operation 1450: insert
    tree[2228] = 'value_472751'
    reference[2228] = 'value_472751'
    assert tree.invariants(), "Invariants failed at step 1450"

    # Operation 1451: delete
    del tree[7573]
    del reference[7573]
    assert tree.invariants(), "Invariants failed at step 1451"

    # Operation 1452: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1452"

    # Operation 1453: get
    assert tree.invariants(), "Invariants failed at step 1453"

    # Operation 1454: insert
    tree[9877] = 'value_271609'
    reference[9877] = 'value_271609'
    assert tree.invariants(), "Invariants failed at step 1454"

    # Operation 1455: get
    assert tree.invariants(), "Invariants failed at step 1455"

    # Operation 1456: batch_delete
    keys_to_delete = [5348, 4260, 6150, 2228, 9877]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1456"

    # Operation 1457: insert
    tree[6067] = 'value_55601'
    reference[6067] = 'value_55601'
    assert tree.invariants(), "Invariants failed at step 1457"

    # Operation 1458: delete
    del tree[6067]
    del reference[6067]
    assert tree.invariants(), "Invariants failed at step 1458"

    # Operation 1459: get
    assert tree.invariants(), "Invariants failed at step 1459"

    # Operation 1460: insert
    tree[6914] = 'value_425227'
    reference[6914] = 'value_425227'
    assert tree.invariants(), "Invariants failed at step 1460"

    # Operation 1461: insert
    tree[2102] = 'value_527350'
    reference[2102] = 'value_527350'
    assert tree.invariants(), "Invariants failed at step 1461"

    # Operation 1462: batch_delete
    keys_to_delete = [2668, 7604, 2292, 9215]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1462"

    # Operation 1463: get
    assert tree.invariants(), "Invariants failed at step 1463"

    # Operation 1464: delete
    del tree[2102]
    del reference[2102]
    assert tree.invariants(), "Invariants failed at step 1464"

    # Operation 1465: insert
    tree[11] = 'value_449441'
    reference[11] = 'value_449441'
    assert tree.invariants(), "Invariants failed at step 1465"

    # Operation 1466: insert
    tree[1649] = 'value_3614'
    reference[1649] = 'value_3614'
    assert tree.invariants(), "Invariants failed at step 1466"

    # Operation 1467: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1467"

    # Operation 1468: insert
    tree[4572] = 'value_894302'
    reference[4572] = 'value_894302'
    assert tree.invariants(), "Invariants failed at step 1468"

    # Operation 1469: delete
    del tree[3367]
    del reference[3367]
    assert tree.invariants(), "Invariants failed at step 1469"

    # Operation 1470: insert
    tree[5569] = 'value_38987'
    reference[5569] = 'value_38987'
    assert tree.invariants(), "Invariants failed at step 1470"

    # Operation 1471: insert
    tree[5807] = 'value_819293'
    reference[5807] = 'value_819293'
    assert tree.invariants(), "Invariants failed at step 1471"

    # Operation 1472: insert
    tree[4648] = 'value_20355'
    reference[4648] = 'value_20355'
    assert tree.invariants(), "Invariants failed at step 1472"

    # Operation 1473: get
    assert tree.invariants(), "Invariants failed at step 1473"

    # Operation 1474: insert
    tree[5453] = 'value_852240'
    reference[5453] = 'value_852240'
    assert tree.invariants(), "Invariants failed at step 1474"

    # Operation 1475: batch_delete
    keys_to_delete = [5569, 6914, 4648, 11, 5453, 8912]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1475"

    # Operation 1476: get
    assert tree.invariants(), "Invariants failed at step 1476"

    # Operation 1477: update
    tree[5807] = 'value_508575'
    reference[5807] = 'value_508575'
    assert tree.invariants(), "Invariants failed at step 1477"

    # Operation 1478: insert
    tree[1400] = 'value_134454'
    reference[1400] = 'value_134454'
    assert tree.invariants(), "Invariants failed at step 1478"

    # Operation 1479: insert
    tree[2615] = 'value_403798'
    reference[2615] = 'value_403798'
    assert tree.invariants(), "Invariants failed at step 1479"

    # Operation 1480: delete
    del tree[1400]
    del reference[1400]
    assert tree.invariants(), "Invariants failed at step 1480"

    # Operation 1481: update
    tree[4572] = 'value_321390'
    reference[4572] = 'value_321390'
    assert tree.invariants(), "Invariants failed at step 1481"

    # Operation 1482: get
    assert tree.invariants(), "Invariants failed at step 1482"

    # Operation 1483: insert
    tree[5883] = 'value_884570'
    reference[5883] = 'value_884570'
    assert tree.invariants(), "Invariants failed at step 1483"

    # Operation 1484: update
    tree[5807] = 'value_253799'
    reference[5807] = 'value_253799'
    assert tree.invariants(), "Invariants failed at step 1484"

    # Operation 1485: insert
    tree[2007] = 'value_214758'
    reference[2007] = 'value_214758'
    assert tree.invariants(), "Invariants failed at step 1485"

    # Operation 1486: batch_delete
    keys_to_delete = [3397, 5807, 1649, 2615, 4543]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1486"

    # Operation 1487: delete
    del tree[5883]
    del reference[5883]
    assert tree.invariants(), "Invariants failed at step 1487"

    # Operation 1488: insert
    tree[4345] = 'value_579453'
    reference[4345] = 'value_579453'
    assert tree.invariants(), "Invariants failed at step 1488"

    # Operation 1489: get
    assert tree.invariants(), "Invariants failed at step 1489"

    # Operation 1490: insert
    tree[1531] = 'value_751637'
    reference[1531] = 'value_751637'
    assert tree.invariants(), "Invariants failed at step 1490"

    # Operation 1491: delete
    del tree[1531]
    del reference[1531]
    assert tree.invariants(), "Invariants failed at step 1491"

    # Operation 1492: update
    tree[4572] = 'value_503657'
    reference[4572] = 'value_503657'
    assert tree.invariants(), "Invariants failed at step 1492"

    # Operation 1493: delete
    del tree[4345]
    del reference[4345]
    assert tree.invariants(), "Invariants failed at step 1493"

    # Operation 1494: insert
    tree[7469] = 'value_550035'
    reference[7469] = 'value_550035'
    assert tree.invariants(), "Invariants failed at step 1494"

    # Operation 1495: get
    assert tree.invariants(), "Invariants failed at step 1495"

    # Operation 1496: get
    assert tree.invariants(), "Invariants failed at step 1496"

    # Operation 1497: delete
    del tree[4572]
    del reference[4572]
    assert tree.invariants(), "Invariants failed at step 1497"

    # Operation 1498: insert
    tree[1221] = 'value_83058'
    reference[1221] = 'value_83058'
    assert tree.invariants(), "Invariants failed at step 1498"

    # Operation 1499: get
    assert tree.invariants(), "Invariants failed at step 1499"

    # Operation 1500: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1500"

    # Operation 1501: delete
    del tree[1221]
    del reference[1221]
    assert tree.invariants(), "Invariants failed at step 1501"

    # Operation 1502: delete
    del tree[7469]
    del reference[7469]
    assert tree.invariants(), "Invariants failed at step 1502"

    # Operation 1503: insert
    tree[4329] = 'value_86925'
    reference[4329] = 'value_86925'
    assert tree.invariants(), "Invariants failed at step 1503"

    # Operation 1504: delete
    del tree[4329]
    del reference[4329]
    assert tree.invariants(), "Invariants failed at step 1504"

    # Operation 1505: delete
    del tree[2007]
    del reference[2007]
    assert tree.invariants(), "Invariants failed at step 1505"

    # Operation 1506: insert
    tree[4185] = 'value_899450'
    reference[4185] = 'value_899450'
    assert tree.invariants(), "Invariants failed at step 1506"

    # Operation 1507: get
    assert tree.invariants(), "Invariants failed at step 1507"

    # Operation 1508: update
    tree[4185] = 'value_23453'
    reference[4185] = 'value_23453'
    assert tree.invariants(), "Invariants failed at step 1508"

    # Operation 1509: insert
    tree[9497] = 'value_323814'
    reference[9497] = 'value_323814'
    assert tree.invariants(), "Invariants failed at step 1509"

    # Operation 1510: delete
    del tree[9497]
    del reference[9497]
    assert tree.invariants(), "Invariants failed at step 1510"

    # Operation 1511: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1511"

    # Operation 1512: get
    assert tree.invariants(), "Invariants failed at step 1512"

    # Operation 1513: get
    assert tree.invariants(), "Invariants failed at step 1513"

    # Operation 1514: get
    assert tree.invariants(), "Invariants failed at step 1514"

    # Operation 1515: update
    tree[4185] = 'value_143691'
    reference[4185] = 'value_143691'
    assert tree.invariants(), "Invariants failed at step 1515"

    # Operation 1516: get
    assert tree.invariants(), "Invariants failed at step 1516"

    # Operation 1517: get
    assert tree.invariants(), "Invariants failed at step 1517"

    # Operation 1518: update
    tree[4185] = 'value_628890'
    reference[4185] = 'value_628890'
    assert tree.invariants(), "Invariants failed at step 1518"

    # Operation 1519: delete
    del tree[4185]
    del reference[4185]
    assert tree.invariants(), "Invariants failed at step 1519"

    # Operation 1520: get
    assert tree.invariants(), "Invariants failed at step 1520"

    # Operation 1521: insert
    tree[6678] = 'value_792786'
    reference[6678] = 'value_792786'
    assert tree.invariants(), "Invariants failed at step 1521"

    # Operation 1522: update
    tree[6678] = 'value_55455'
    reference[6678] = 'value_55455'
    assert tree.invariants(), "Invariants failed at step 1522"

    # Operation 1523: delete
    del tree[6678]
    del reference[6678]
    assert tree.invariants(), "Invariants failed at step 1523"

    # Operation 1524: get
    assert tree.invariants(), "Invariants failed at step 1524"

    # Operation 1525: insert
    tree[8870] = 'value_669810'
    reference[8870] = 'value_669810'
    assert tree.invariants(), "Invariants failed at step 1525"

    # Operation 1526: insert
    tree[9922] = 'value_247795'
    reference[9922] = 'value_247795'
    assert tree.invariants(), "Invariants failed at step 1526"

    # Operation 1527: get
    assert tree.invariants(), "Invariants failed at step 1527"

    # Operation 1528: insert
    tree[4280] = 'value_651095'
    reference[4280] = 'value_651095'
    assert tree.invariants(), "Invariants failed at step 1528"

    # Operation 1529: get
    assert tree.invariants(), "Invariants failed at step 1529"

    # Operation 1530: delete
    del tree[4280]
    del reference[4280]
    assert tree.invariants(), "Invariants failed at step 1530"

    # Operation 1531: delete
    del tree[8870]
    del reference[8870]
    assert tree.invariants(), "Invariants failed at step 1531"

    # Operation 1532: get
    assert tree.invariants(), "Invariants failed at step 1532"

    # Operation 1533: get
    assert tree.invariants(), "Invariants failed at step 1533"

    # Operation 1534: insert
    tree[8822] = 'value_790198'
    reference[8822] = 'value_790198'
    assert tree.invariants(), "Invariants failed at step 1534"

    # Operation 1535: insert
    tree[6288] = 'value_553786'
    reference[6288] = 'value_553786'
    assert tree.invariants(), "Invariants failed at step 1535"

    # Operation 1536: delete
    del tree[8822]
    del reference[8822]
    assert tree.invariants(), "Invariants failed at step 1536"

    # Operation 1537: insert
    tree[9522] = 'value_506451'
    reference[9522] = 'value_506451'
    assert tree.invariants(), "Invariants failed at step 1537"

    # Operation 1538: get
    assert tree.invariants(), "Invariants failed at step 1538"

    # Operation 1539: insert
    tree[1738] = 'value_448395'
    reference[1738] = 'value_448395'
    assert tree.invariants(), "Invariants failed at step 1539"

    # Operation 1540: insert
    tree[9938] = 'value_639472'
    reference[9938] = 'value_639472'
    assert tree.invariants(), "Invariants failed at step 1540"

    # Operation 1541: delete
    del tree[1738]
    del reference[1738]
    assert tree.invariants(), "Invariants failed at step 1541"

    # Operation 1542: get
    assert tree.invariants(), "Invariants failed at step 1542"

    # Operation 1543: insert
    tree[7382] = 'value_39057'
    reference[7382] = 'value_39057'
    assert tree.invariants(), "Invariants failed at step 1543"

    # Operation 1544: delete
    del tree[9522]
    del reference[9522]
    assert tree.invariants(), "Invariants failed at step 1544"

    # Operation 1545: insert
    tree[9622] = 'value_818327'
    reference[9622] = 'value_818327'
    assert tree.invariants(), "Invariants failed at step 1545"

    # Operation 1546: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1546"

    # Operation 1547: batch_delete
    keys_to_delete = [184, 9938, 7387, 7382]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1547"

    # Operation 1548: get
    assert tree.invariants(), "Invariants failed at step 1548"

    # Operation 1549: update
    tree[6288] = 'value_949615'
    reference[6288] = 'value_949615'
    assert tree.invariants(), "Invariants failed at step 1549"

    # Operation 1550: delete
    del tree[6288]
    del reference[6288]
    assert tree.invariants(), "Invariants failed at step 1550"

    # Operation 1551: insert
    tree[6189] = 'value_564051'
    reference[6189] = 'value_564051'
    assert tree.invariants(), "Invariants failed at step 1551"

    # Operation 1552: get
    assert tree.invariants(), "Invariants failed at step 1552"

    # Operation 1553: delete
    del tree[9922]
    del reference[9922]
    assert tree.invariants(), "Invariants failed at step 1553"

    # Operation 1554: insert
    tree[7989] = 'value_901098'
    reference[7989] = 'value_901098'
    assert tree.invariants(), "Invariants failed at step 1554"

    # Operation 1555: delete
    del tree[6189]
    del reference[6189]
    assert tree.invariants(), "Invariants failed at step 1555"

    # Operation 1556: insert
    tree[2613] = 'value_70406'
    reference[2613] = 'value_70406'
    assert tree.invariants(), "Invariants failed at step 1556"

    # Operation 1557: insert
    tree[5097] = 'value_318444'
    reference[5097] = 'value_318444'
    assert tree.invariants(), "Invariants failed at step 1557"

    # Operation 1558: get
    assert tree.invariants(), "Invariants failed at step 1558"

    # Operation 1559: update
    tree[5097] = 'value_661864'
    reference[5097] = 'value_661864'
    assert tree.invariants(), "Invariants failed at step 1559"

    # Operation 1560: get
    assert tree.invariants(), "Invariants failed at step 1560"

    # Operation 1561: get
    assert tree.invariants(), "Invariants failed at step 1561"

    # Operation 1562: update
    tree[7989] = 'value_394462'
    reference[7989] = 'value_394462'
    assert tree.invariants(), "Invariants failed at step 1562"

    # Operation 1563: update
    tree[9622] = 'value_942788'
    reference[9622] = 'value_942788'
    assert tree.invariants(), "Invariants failed at step 1563"

    # Operation 1564: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1564"

    # Operation 1565: delete
    del tree[5097]
    del reference[5097]
    assert tree.invariants(), "Invariants failed at step 1565"

    # Operation 1566: get
    assert tree.invariants(), "Invariants failed at step 1566"

    # Operation 1567: update
    tree[2613] = 'value_568914'
    reference[2613] = 'value_568914'
    assert tree.invariants(), "Invariants failed at step 1567"

    # Operation 1568: update
    tree[7989] = 'value_868323'
    reference[7989] = 'value_868323'
    assert tree.invariants(), "Invariants failed at step 1568"

    # Operation 1569: delete
    del tree[9622]
    del reference[9622]
    assert tree.invariants(), "Invariants failed at step 1569"

    # Operation 1570: get
    assert tree.invariants(), "Invariants failed at step 1570"

    # Operation 1571: insert
    tree[3345] = 'value_495712'
    reference[3345] = 'value_495712'
    assert tree.invariants(), "Invariants failed at step 1571"

    # Operation 1572: delete
    del tree[2613]
    del reference[2613]
    assert tree.invariants(), "Invariants failed at step 1572"

    # Operation 1573: delete
    del tree[7989]
    del reference[7989]
    assert tree.invariants(), "Invariants failed at step 1573"

    # Operation 1574: get
    assert tree.invariants(), "Invariants failed at step 1574"

    # Operation 1575: delete
    del tree[3345]
    del reference[3345]
    assert tree.invariants(), "Invariants failed at step 1575"

    # Operation 1576: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1576"

    # Operation 1577: insert
    tree[291] = 'value_348159'
    reference[291] = 'value_348159'
    assert tree.invariants(), "Invariants failed at step 1577"

    # Operation 1578: insert
    tree[6551] = 'value_816074'
    reference[6551] = 'value_816074'
    assert tree.invariants(), "Invariants failed at step 1578"

    # Operation 1579: insert
    tree[7976] = 'value_434339'
    reference[7976] = 'value_434339'
    assert tree.invariants(), "Invariants failed at step 1579"

    # Operation 1580: delete
    del tree[291]
    del reference[291]
    assert tree.invariants(), "Invariants failed at step 1580"

    # Operation 1581: get
    assert tree.invariants(), "Invariants failed at step 1581"

    # Operation 1582: get
    assert tree.invariants(), "Invariants failed at step 1582"

    # Operation 1583: update
    tree[6551] = 'value_505010'
    reference[6551] = 'value_505010'
    assert tree.invariants(), "Invariants failed at step 1583"

    # Operation 1584: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1584"

    # Operation 1585: insert
    tree[1221] = 'value_415895'
    reference[1221] = 'value_415895'
    assert tree.invariants(), "Invariants failed at step 1585"

    # Operation 1586: get
    assert tree.invariants(), "Invariants failed at step 1586"

    # Operation 1587: update
    tree[1221] = 'value_515357'
    reference[1221] = 'value_515357'
    assert tree.invariants(), "Invariants failed at step 1587"

    # Operation 1588: insert
    tree[7898] = 'value_421067'
    reference[7898] = 'value_421067'
    assert tree.invariants(), "Invariants failed at step 1588"

    # Operation 1589: insert
    tree[9746] = 'value_457445'
    reference[9746] = 'value_457445'
    assert tree.invariants(), "Invariants failed at step 1589"

    # Operation 1590: update
    tree[1221] = 'value_573987'
    reference[1221] = 'value_573987'
    assert tree.invariants(), "Invariants failed at step 1590"

    # Operation 1591: delete
    del tree[1221]
    del reference[1221]
    assert tree.invariants(), "Invariants failed at step 1591"

    # Operation 1592: insert
    tree[4811] = 'value_699478'
    reference[4811] = 'value_699478'
    assert tree.invariants(), "Invariants failed at step 1592"

    # Operation 1593: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1593"

    # Operation 1594: insert
    tree[1938] = 'value_795379'
    reference[1938] = 'value_795379'
    assert tree.invariants(), "Invariants failed at step 1594"

    # Operation 1595: update
    tree[6551] = 'value_870333'
    reference[6551] = 'value_870333'
    assert tree.invariants(), "Invariants failed at step 1595"

    # Operation 1596: insert
    tree[2994] = 'value_359355'
    reference[2994] = 'value_359355'
    assert tree.invariants(), "Invariants failed at step 1596"

    # Operation 1597: update
    tree[7898] = 'value_663124'
    reference[7898] = 'value_663124'
    assert tree.invariants(), "Invariants failed at step 1597"

    # Operation 1598: insert
    tree[7383] = 'value_382873'
    reference[7383] = 'value_382873'
    assert tree.invariants(), "Invariants failed at step 1598"

    # Operation 1599: batch_delete
    keys_to_delete = [1348, 4811, 9746, 2994, 1938, 9438]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1599"

    # Operation 1600: get
    assert tree.invariants(), "Invariants failed at step 1600"

    # Operation 1601: insert
    tree[669] = 'value_511453'
    reference[669] = 'value_511453'
    assert tree.invariants(), "Invariants failed at step 1601"

    # Operation 1602: insert
    tree[7637] = 'value_769251'
    reference[7637] = 'value_769251'
    assert tree.invariants(), "Invariants failed at step 1602"

    # Operation 1603: delete
    del tree[669]
    del reference[669]
    assert tree.invariants(), "Invariants failed at step 1603"

    # Operation 1604: insert
    tree[462] = 'value_499686'
    reference[462] = 'value_499686'
    assert tree.invariants(), "Invariants failed at step 1604"

    # Operation 1605: batch_delete
    keys_to_delete = [7976, 3701, 7637, 462]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1605"

    # Operation 1606: get
    assert tree.invariants(), "Invariants failed at step 1606"

    # Operation 1607: delete
    del tree[6551]
    del reference[6551]
    assert tree.invariants(), "Invariants failed at step 1607"

    # Operation 1608: insert
    tree[5290] = 'value_622112'
    reference[5290] = 'value_622112'
    assert tree.invariants(), "Invariants failed at step 1608"

    # Operation 1609: insert
    tree[7777] = 'value_760850'
    reference[7777] = 'value_760850'
    assert tree.invariants(), "Invariants failed at step 1609"

    # Operation 1610: update
    tree[7383] = 'value_58020'
    reference[7383] = 'value_58020'
    assert tree.invariants(), "Invariants failed at step 1610"

    # Operation 1611: get
    assert tree.invariants(), "Invariants failed at step 1611"

    # Operation 1612: insert
    tree[8991] = 'value_771860'
    reference[8991] = 'value_771860'
    assert tree.invariants(), "Invariants failed at step 1612"

    # Operation 1613: insert
    tree[3205] = 'value_536201'
    reference[3205] = 'value_536201'
    assert tree.invariants(), "Invariants failed at step 1613"

    # Operation 1614: get
    assert tree.invariants(), "Invariants failed at step 1614"

    # Operation 1615: get
    assert tree.invariants(), "Invariants failed at step 1615"

    # Operation 1616: insert
    tree[2995] = 'value_559594'
    reference[2995] = 'value_559594'
    assert tree.invariants(), "Invariants failed at step 1616"

    # Operation 1617: get
    assert tree.invariants(), "Invariants failed at step 1617"

    # Operation 1618: update
    tree[8991] = 'value_796190'
    reference[8991] = 'value_796190'
    assert tree.invariants(), "Invariants failed at step 1618"

    # Operation 1619: insert
    tree[3742] = 'value_710725'
    reference[3742] = 'value_710725'
    assert tree.invariants(), "Invariants failed at step 1619"

    # Operation 1620: insert
    tree[5476] = 'value_240653'
    reference[5476] = 'value_240653'
    assert tree.invariants(), "Invariants failed at step 1620"

    # Operation 1621: delete
    del tree[5290]
    del reference[5290]
    assert tree.invariants(), "Invariants failed at step 1621"

    # Operation 1622: insert
    tree[1461] = 'value_369105'
    reference[1461] = 'value_369105'
    assert tree.invariants(), "Invariants failed at step 1622"

    # Operation 1623: get
    assert tree.invariants(), "Invariants failed at step 1623"

    # Operation 1624: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1624"

    # Operation 1625: delete
    del tree[1461]
    del reference[1461]
    assert tree.invariants(), "Invariants failed at step 1625"

    # Operation 1626: delete
    del tree[7777]
    del reference[7777]
    assert tree.invariants(), "Invariants failed at step 1626"

    # Operation 1627: insert
    tree[6567] = 'value_222192'
    reference[6567] = 'value_222192'
    assert tree.invariants(), "Invariants failed at step 1627"

    # Operation 1628: delete
    del tree[3742]
    del reference[3742]
    assert tree.invariants(), "Invariants failed at step 1628"

    # Operation 1629: insert
    tree[2459] = 'value_119055'
    reference[2459] = 'value_119055'
    assert tree.invariants(), "Invariants failed at step 1629"

    # Operation 1630: delete
    del tree[7383]
    del reference[7383]
    assert tree.invariants(), "Invariants failed at step 1630"

    # Operation 1631: insert
    tree[8070] = 'value_205750'
    reference[8070] = 'value_205750'
    assert tree.invariants(), "Invariants failed at step 1631"

    # Operation 1632: get
    assert tree.invariants(), "Invariants failed at step 1632"

    # Operation 1633: delete
    del tree[8070]
    del reference[8070]
    assert tree.invariants(), "Invariants failed at step 1633"

    # Operation 1634: insert
    tree[8510] = 'value_935376'
    reference[8510] = 'value_935376'
    assert tree.invariants(), "Invariants failed at step 1634"

    # Operation 1635: delete
    del tree[3205]
    del reference[3205]
    assert tree.invariants(), "Invariants failed at step 1635"

    # Operation 1636: batch_delete
    keys_to_delete = [6567, 9387, 2932, 2459, 8991]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1636"

    # Operation 1637: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1637"

    # Operation 1638: delete
    del tree[5476]
    del reference[5476]
    assert tree.invariants(), "Invariants failed at step 1638"

    # Operation 1639: insert
    tree[687] = 'value_21532'
    reference[687] = 'value_21532'
    assert tree.invariants(), "Invariants failed at step 1639"

    # Operation 1640: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1640"

    # Operation 1641: delete
    del tree[7898]
    del reference[7898]
    assert tree.invariants(), "Invariants failed at step 1641"

    # Operation 1642: insert
    tree[6514] = 'value_302085'
    reference[6514] = 'value_302085'
    assert tree.invariants(), "Invariants failed at step 1642"

    # Operation 1643: update
    tree[8510] = 'value_580465'
    reference[8510] = 'value_580465'
    assert tree.invariants(), "Invariants failed at step 1643"

    # Operation 1644: insert
    tree[6771] = 'value_120612'
    reference[6771] = 'value_120612'
    assert tree.invariants(), "Invariants failed at step 1644"

    # Operation 1645: insert
    tree[4914] = 'value_945625'
    reference[4914] = 'value_945625'
    assert tree.invariants(), "Invariants failed at step 1645"

    # Operation 1646: get
    assert tree.invariants(), "Invariants failed at step 1646"

    # Operation 1647: delete
    del tree[6514]
    del reference[6514]
    assert tree.invariants(), "Invariants failed at step 1647"

    # Operation 1648: update
    tree[6771] = 'value_898640'
    reference[6771] = 'value_898640'
    assert tree.invariants(), "Invariants failed at step 1648"

    # Operation 1649: delete
    del tree[6771]
    del reference[6771]
    assert tree.invariants(), "Invariants failed at step 1649"

    # Operation 1650: get
    assert tree.invariants(), "Invariants failed at step 1650"

    # Operation 1651: insert
    tree[488] = 'value_963365'
    reference[488] = 'value_963365'
    assert tree.invariants(), "Invariants failed at step 1651"

    # Operation 1652: delete
    del tree[2995]
    del reference[2995]
    assert tree.invariants(), "Invariants failed at step 1652"

    # Operation 1653: get
    assert tree.invariants(), "Invariants failed at step 1653"

    # Operation 1654: insert
    tree[2874] = 'value_267116'
    reference[2874] = 'value_267116'
    assert tree.invariants(), "Invariants failed at step 1654"

    # Operation 1655: insert
    tree[3342] = 'value_493169'
    reference[3342] = 'value_493169'
    assert tree.invariants(), "Invariants failed at step 1655"

    # Operation 1656: get
    assert tree.invariants(), "Invariants failed at step 1656"

    # Operation 1657: get
    assert tree.invariants(), "Invariants failed at step 1657"

    # Operation 1658: update
    tree[3342] = 'value_788949'
    reference[3342] = 'value_788949'
    assert tree.invariants(), "Invariants failed at step 1658"

    # Operation 1659: get
    assert tree.invariants(), "Invariants failed at step 1659"

    # Operation 1660: get
    assert tree.invariants(), "Invariants failed at step 1660"

    # Operation 1661: insert
    tree[2079] = 'value_615467'
    reference[2079] = 'value_615467'
    assert tree.invariants(), "Invariants failed at step 1661"

    # Operation 1662: update
    tree[8510] = 'value_808964'
    reference[8510] = 'value_808964'
    assert tree.invariants(), "Invariants failed at step 1662"

    # Operation 1663: get
    assert tree.invariants(), "Invariants failed at step 1663"

    # Operation 1664: insert
    tree[8104] = 'value_196951'
    reference[8104] = 'value_196951'
    assert tree.invariants(), "Invariants failed at step 1664"

    # Operation 1665: get
    assert tree.invariants(), "Invariants failed at step 1665"

    # Operation 1666: update
    tree[2874] = 'value_730125'
    reference[2874] = 'value_730125'
    assert tree.invariants(), "Invariants failed at step 1666"

    # Operation 1667: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1667"

    # Operation 1668: insert
    tree[1114] = 'value_811716'
    reference[1114] = 'value_811716'
    assert tree.invariants(), "Invariants failed at step 1668"

    # Operation 1669: insert
    tree[1158] = 'value_241025'
    reference[1158] = 'value_241025'
    assert tree.invariants(), "Invariants failed at step 1669"

    # Operation 1670: insert
    tree[7009] = 'value_277778'
    reference[7009] = 'value_277778'
    assert tree.invariants(), "Invariants failed at step 1670"

    # Operation 1671: delete
    del tree[687]
    del reference[687]
    assert tree.invariants(), "Invariants failed at step 1671"

    # Operation 1672: delete
    del tree[8510]
    del reference[8510]
    assert tree.invariants(), "Invariants failed at step 1672"

    # Operation 1673: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1673"

    # Operation 1674: batch_delete
    keys_to_delete = [1114, 719, 4914, 2874, 8254, 2079]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1674"

    # Operation 1675: update
    tree[8104] = 'value_313178'
    reference[8104] = 'value_313178'
    assert tree.invariants(), "Invariants failed at step 1675"

    # Operation 1676: batch_delete
    keys_to_delete = [488, 1102, 233, 1158]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1676"

    # Operation 1677: delete
    del tree[3342]
    del reference[3342]
    assert tree.invariants(), "Invariants failed at step 1677"

    # Operation 1678: delete
    del tree[7009]
    del reference[7009]
    assert tree.invariants(), "Invariants failed at step 1678"

    # Operation 1679: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1679"

    # Operation 1680: insert
    tree[3429] = 'value_444411'
    reference[3429] = 'value_444411'
    assert tree.invariants(), "Invariants failed at step 1680"

    # Operation 1681: insert
    tree[2736] = 'value_980410'
    reference[2736] = 'value_980410'
    assert tree.invariants(), "Invariants failed at step 1681"

    # Operation 1682: insert
    tree[8747] = 'value_400554'
    reference[8747] = 'value_400554'
    assert tree.invariants(), "Invariants failed at step 1682"

    # Operation 1683: insert
    tree[1620] = 'value_175309'
    reference[1620] = 'value_175309'
    assert tree.invariants(), "Invariants failed at step 1683"

    # Operation 1684: update
    tree[8747] = 'value_711615'
    reference[8747] = 'value_711615'
    assert tree.invariants(), "Invariants failed at step 1684"

    # Operation 1685: insert
    tree[6474] = 'value_941684'
    reference[6474] = 'value_941684'
    assert tree.invariants(), "Invariants failed at step 1685"

    # Operation 1686: delete
    del tree[3429]
    del reference[3429]
    assert tree.invariants(), "Invariants failed at step 1686"

    # Operation 1687: update
    tree[2736] = 'value_965886'
    reference[2736] = 'value_965886'
    assert tree.invariants(), "Invariants failed at step 1687"

    # Operation 1688: delete
    del tree[8747]
    del reference[8747]
    assert tree.invariants(), "Invariants failed at step 1688"

    # Operation 1689: insert
    tree[3731] = 'value_803018'
    reference[3731] = 'value_803018'
    assert tree.invariants(), "Invariants failed at step 1689"

    # Operation 1690: delete
    del tree[1620]
    del reference[1620]
    assert tree.invariants(), "Invariants failed at step 1690"

    # Operation 1691: insert
    tree[7373] = 'value_863281'
    reference[7373] = 'value_863281'
    assert tree.invariants(), "Invariants failed at step 1691"

    # Operation 1692: delete
    del tree[6474]
    del reference[6474]
    assert tree.invariants(), "Invariants failed at step 1692"

    # Operation 1693: delete
    del tree[3731]
    del reference[3731]
    assert tree.invariants(), "Invariants failed at step 1693"

    # Operation 1694: delete
    del tree[7373]
    del reference[7373]
    assert tree.invariants(), "Invariants failed at step 1694"

    # Operation 1695: delete
    del tree[8104]
    del reference[8104]
    assert tree.invariants(), "Invariants failed at step 1695"

    # Operation 1696: insert
    tree[184] = 'value_951629'
    reference[184] = 'value_951629'
    assert tree.invariants(), "Invariants failed at step 1696"

    # Operation 1697: insert
    tree[2824] = 'value_182920'
    reference[2824] = 'value_182920'
    assert tree.invariants(), "Invariants failed at step 1697"

    # Operation 1698: delete
    del tree[2824]
    del reference[2824]
    assert tree.invariants(), "Invariants failed at step 1698"

    # Operation 1699: delete
    del tree[2736]
    del reference[2736]
    assert tree.invariants(), "Invariants failed at step 1699"

    # Operation 1700: update
    tree[184] = 'value_970960'
    reference[184] = 'value_970960'
    assert tree.invariants(), "Invariants failed at step 1700"

    # Operation 1701: insert
    tree[7420] = 'value_681707'
    reference[7420] = 'value_681707'
    assert tree.invariants(), "Invariants failed at step 1701"

    # Operation 1702: delete
    del tree[184]
    del reference[184]
    assert tree.invariants(), "Invariants failed at step 1702"

    # Operation 1703: insert
    tree[9184] = 'value_798585'
    reference[9184] = 'value_798585'
    assert tree.invariants(), "Invariants failed at step 1703"

    # Operation 1704: insert
    tree[3687] = 'value_954363'
    reference[3687] = 'value_954363'
    assert tree.invariants(), "Invariants failed at step 1704"

    # Operation 1705: insert
    tree[9264] = 'value_736537'
    reference[9264] = 'value_736537'
    assert tree.invariants(), "Invariants failed at step 1705"

    # Operation 1706: delete
    del tree[7420]
    del reference[7420]
    assert tree.invariants(), "Invariants failed at step 1706"

    # Operation 1707: get
    assert tree.invariants(), "Invariants failed at step 1707"

    # Operation 1708: insert
    tree[4470] = 'value_378960'
    reference[4470] = 'value_378960'
    assert tree.invariants(), "Invariants failed at step 1708"

    # Operation 1709: update
    tree[9264] = 'value_156457'
    reference[9264] = 'value_156457'
    assert tree.invariants(), "Invariants failed at step 1709"

    # Operation 1710: get
    assert tree.invariants(), "Invariants failed at step 1710"

    # Operation 1711: update
    tree[4470] = 'value_742139'
    reference[4470] = 'value_742139'
    assert tree.invariants(), "Invariants failed at step 1711"

    # Operation 1712: get
    assert tree.invariants(), "Invariants failed at step 1712"

    # Operation 1713: get
    assert tree.invariants(), "Invariants failed at step 1713"

    # Operation 1714: delete
    del tree[9184]
    del reference[9184]
    assert tree.invariants(), "Invariants failed at step 1714"

    # Operation 1715: update
    tree[3687] = 'value_363312'
    reference[3687] = 'value_363312'
    assert tree.invariants(), "Invariants failed at step 1715"

    # Operation 1716: delete
    del tree[9264]
    del reference[9264]
    assert tree.invariants(), "Invariants failed at step 1716"

    # Operation 1717: delete
    del tree[3687]
    del reference[3687]
    assert tree.invariants(), "Invariants failed at step 1717"

    # Operation 1718: update
    tree[4470] = 'value_816247'
    reference[4470] = 'value_816247'
    assert tree.invariants(), "Invariants failed at step 1718"

    # Operation 1719: insert
    tree[9218] = 'value_773474'
    reference[9218] = 'value_773474'
    assert tree.invariants(), "Invariants failed at step 1719"

    # Operation 1720: update
    tree[4470] = 'value_408707'
    reference[4470] = 'value_408707'
    assert tree.invariants(), "Invariants failed at step 1720"

    # Operation 1721: get
    assert tree.invariants(), "Invariants failed at step 1721"

    # Operation 1722: delete
    del tree[4470]
    del reference[4470]
    assert tree.invariants(), "Invariants failed at step 1722"

    # Operation 1723: get
    assert tree.invariants(), "Invariants failed at step 1723"

    # Operation 1724: delete
    del tree[9218]
    del reference[9218]
    assert tree.invariants(), "Invariants failed at step 1724"

    # Operation 1725: insert
    tree[1979] = 'value_566602'
    reference[1979] = 'value_566602'
    assert tree.invariants(), "Invariants failed at step 1725"

    # Operation 1726: delete
    del tree[1979]
    del reference[1979]
    assert tree.invariants(), "Invariants failed at step 1726"

    # Operation 1727: get
    assert tree.invariants(), "Invariants failed at step 1727"

    # Operation 1728: insert
    tree[1003] = 'value_582820'
    reference[1003] = 'value_582820'
    assert tree.invariants(), "Invariants failed at step 1728"

    # Operation 1729: insert
    tree[7129] = 'value_295332'
    reference[7129] = 'value_295332'
    assert tree.invariants(), "Invariants failed at step 1729"

    # Operation 1730: get
    assert tree.invariants(), "Invariants failed at step 1730"

    # Operation 1731: insert
    tree[4196] = 'value_893037'
    reference[4196] = 'value_893037'
    assert tree.invariants(), "Invariants failed at step 1731"

    # Operation 1732: delete
    del tree[4196]
    del reference[4196]
    assert tree.invariants(), "Invariants failed at step 1732"

    # Operation 1733: update
    tree[1003] = 'value_302303'
    reference[1003] = 'value_302303'
    assert tree.invariants(), "Invariants failed at step 1733"

    # Operation 1734: delete
    del tree[7129]
    del reference[7129]
    assert tree.invariants(), "Invariants failed at step 1734"

    # Operation 1735: update
    tree[1003] = 'value_874380'
    reference[1003] = 'value_874380'
    assert tree.invariants(), "Invariants failed at step 1735"

    # Operation 1736: get
    assert tree.invariants(), "Invariants failed at step 1736"

    # Operation 1737: delete
    del tree[1003]
    del reference[1003]
    assert tree.invariants(), "Invariants failed at step 1737"

    # Operation 1738: insert
    tree[975] = 'value_842769'
    reference[975] = 'value_842769'
    assert tree.invariants(), "Invariants failed at step 1738"

    # Operation 1739: insert
    tree[1009] = 'value_986583'
    reference[1009] = 'value_986583'
    assert tree.invariants(), "Invariants failed at step 1739"

    # Operation 1740: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1740"

    # Operation 1741: insert
    tree[1613] = 'value_382111'
    reference[1613] = 'value_382111'
    assert tree.invariants(), "Invariants failed at step 1741"

    # Operation 1742: insert
    tree[4090] = 'value_404814'
    reference[4090] = 'value_404814'
    assert tree.invariants(), "Invariants failed at step 1742"

    # Operation 1743: delete
    del tree[1613]
    del reference[1613]
    assert tree.invariants(), "Invariants failed at step 1743"

    # Operation 1744: insert
    tree[4208] = 'value_606547'
    reference[4208] = 'value_606547'
    assert tree.invariants(), "Invariants failed at step 1744"

    # Operation 1745: get
    assert tree.invariants(), "Invariants failed at step 1745"

    # Operation 1746: delete
    del tree[975]
    del reference[975]
    assert tree.invariants(), "Invariants failed at step 1746"

    # Operation 1747: get
    assert tree.invariants(), "Invariants failed at step 1747"

    # Operation 1748: insert
    tree[4069] = 'value_994191'
    reference[4069] = 'value_994191'
    assert tree.invariants(), "Invariants failed at step 1748"

    # Operation 1749: get
    assert tree.invariants(), "Invariants failed at step 1749"

    # Operation 1750: delete
    del tree[1009]
    del reference[1009]
    assert tree.invariants(), "Invariants failed at step 1750"

    # Operation 1751: get
    assert tree.invariants(), "Invariants failed at step 1751"

    # Operation 1752: update
    tree[4208] = 'value_859873'
    reference[4208] = 'value_859873'
    assert tree.invariants(), "Invariants failed at step 1752"

    # Operation 1753: update
    tree[4069] = 'value_990096'
    reference[4069] = 'value_990096'
    assert tree.invariants(), "Invariants failed at step 1753"

    # Operation 1754: get
    assert tree.invariants(), "Invariants failed at step 1754"

    # Operation 1755: insert
    tree[2698] = 'value_482664'
    reference[2698] = 'value_482664'
    assert tree.invariants(), "Invariants failed at step 1755"

    # Operation 1756: get
    assert tree.invariants(), "Invariants failed at step 1756"

    # Operation 1757: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1757"

    # Operation 1758: delete
    del tree[2698]
    del reference[2698]
    assert tree.invariants(), "Invariants failed at step 1758"

    # Operation 1759: update
    tree[4069] = 'value_400228'
    reference[4069] = 'value_400228'
    assert tree.invariants(), "Invariants failed at step 1759"

    # Operation 1760: insert
    tree[7808] = 'value_160411'
    reference[7808] = 'value_160411'
    assert tree.invariants(), "Invariants failed at step 1760"

    # Operation 1761: update
    tree[4090] = 'value_242641'
    reference[4090] = 'value_242641'
    assert tree.invariants(), "Invariants failed at step 1761"

    # Operation 1762: delete
    del tree[7808]
    del reference[7808]
    assert tree.invariants(), "Invariants failed at step 1762"

    # Operation 1763: update
    tree[4090] = 'value_784329'
    reference[4090] = 'value_784329'
    assert tree.invariants(), "Invariants failed at step 1763"

    # Operation 1764: get
    assert tree.invariants(), "Invariants failed at step 1764"

    # Operation 1765: delete
    del tree[4208]
    del reference[4208]
    assert tree.invariants(), "Invariants failed at step 1765"

    # Operation 1766: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1766"

    # Operation 1767: get
    assert tree.invariants(), "Invariants failed at step 1767"

    # Operation 1768: get
    assert tree.invariants(), "Invariants failed at step 1768"

    # Operation 1769: insert
    tree[7667] = 'value_941281'
    reference[7667] = 'value_941281'
    assert tree.invariants(), "Invariants failed at step 1769"

    # Operation 1770: insert
    tree[1231] = 'value_760253'
    reference[1231] = 'value_760253'
    assert tree.invariants(), "Invariants failed at step 1770"

    # Operation 1771: update
    tree[7667] = 'value_715301'
    reference[7667] = 'value_715301'
    assert tree.invariants(), "Invariants failed at step 1771"

    # Operation 1772: get
    assert tree.invariants(), "Invariants failed at step 1772"

    # Operation 1773: insert
    tree[6157] = 'value_474360'
    reference[6157] = 'value_474360'
    assert tree.invariants(), "Invariants failed at step 1773"

    # Operation 1774: batch_delete
    keys_to_delete = [9792, 4090, 4038, 1231]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1774"

    # Operation 1775: get
    assert tree.invariants(), "Invariants failed at step 1775"

    # Operation 1776: delete
    del tree[6157]
    del reference[6157]
    assert tree.invariants(), "Invariants failed at step 1776"

    # Operation 1777: insert
    tree[6050] = 'value_476537'
    reference[6050] = 'value_476537'
    assert tree.invariants(), "Invariants failed at step 1777"

    # Operation 1778: get
    assert tree.invariants(), "Invariants failed at step 1778"

    # Operation 1779: get
    assert tree.invariants(), "Invariants failed at step 1779"

    # Operation 1780: insert
    tree[9629] = 'value_372513'
    reference[9629] = 'value_372513'
    assert tree.invariants(), "Invariants failed at step 1780"

    # Operation 1781: get
    assert tree.invariants(), "Invariants failed at step 1781"

    # Operation 1782: get
    assert tree.invariants(), "Invariants failed at step 1782"

    # Operation 1783: delete
    del tree[6050]
    del reference[6050]
    assert tree.invariants(), "Invariants failed at step 1783"

    # Operation 1784: insert
    tree[8160] = 'value_29986'
    reference[8160] = 'value_29986'
    assert tree.invariants(), "Invariants failed at step 1784"

    # Operation 1785: update
    tree[7667] = 'value_17675'
    reference[7667] = 'value_17675'
    assert tree.invariants(), "Invariants failed at step 1785"

    # Operation 1786: get
    assert tree.invariants(), "Invariants failed at step 1786"

    # Operation 1787: update
    tree[7667] = 'value_516808'
    reference[7667] = 'value_516808'
    assert tree.invariants(), "Invariants failed at step 1787"

    # Operation 1788: insert
    tree[3530] = 'value_450484'
    reference[3530] = 'value_450484'
    assert tree.invariants(), "Invariants failed at step 1788"

    # Operation 1789: get
    assert tree.invariants(), "Invariants failed at step 1789"

    # Operation 1790: get
    assert tree.invariants(), "Invariants failed at step 1790"

    # Operation 1791: insert
    tree[4124] = 'value_490788'
    reference[4124] = 'value_490788'
    assert tree.invariants(), "Invariants failed at step 1791"

    # Operation 1792: get
    assert tree.invariants(), "Invariants failed at step 1792"

    # Operation 1793: insert
    tree[4216] = 'value_273755'
    reference[4216] = 'value_273755'
    assert tree.invariants(), "Invariants failed at step 1793"

    # Operation 1794: get
    assert tree.invariants(), "Invariants failed at step 1794"

    # Operation 1795: update
    tree[4069] = 'value_400029'
    reference[4069] = 'value_400029'
    assert tree.invariants(), "Invariants failed at step 1795"

    # Operation 1796: delete
    del tree[3530]
    del reference[3530]
    assert tree.invariants(), "Invariants failed at step 1796"

    # Operation 1797: get
    assert tree.invariants(), "Invariants failed at step 1797"

    # Operation 1798: insert
    tree[9220] = 'value_706310'
    reference[9220] = 'value_706310'
    assert tree.invariants(), "Invariants failed at step 1798"

    # Operation 1799: insert
    tree[7048] = 'value_788932'
    reference[7048] = 'value_788932'
    assert tree.invariants(), "Invariants failed at step 1799"

    # Operation 1800: insert
    tree[6001] = 'value_178852'
    reference[6001] = 'value_178852'
    assert tree.invariants(), "Invariants failed at step 1800"

    # Operation 1801: get
    assert tree.invariants(), "Invariants failed at step 1801"

    # Operation 1802: insert
    tree[9003] = 'value_98307'
    reference[9003] = 'value_98307'
    assert tree.invariants(), "Invariants failed at step 1802"

    # Operation 1803: get
    assert tree.invariants(), "Invariants failed at step 1803"

    # Operation 1804: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1804"

    # Operation 1805: update
    tree[9003] = 'value_495378'
    reference[9003] = 'value_495378'
    assert tree.invariants(), "Invariants failed at step 1805"

    # Operation 1806: batch_delete
    keys_to_delete = [8160, 1859, 4069, 9003, 9099, 6001, 4124]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1806"

    # Operation 1807: insert
    tree[5722] = 'value_843047'
    reference[5722] = 'value_843047'
    assert tree.invariants(), "Invariants failed at step 1807"

    # Operation 1808: delete
    del tree[9629]
    del reference[9629]
    assert tree.invariants(), "Invariants failed at step 1808"

    # Operation 1809: batch_delete
    keys_to_delete = [7048, 7667, 6140, 4077]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1809"

    # Operation 1810: delete
    del tree[4216]
    del reference[4216]
    assert tree.invariants(), "Invariants failed at step 1810"

    # Operation 1811: delete
    del tree[5722]
    del reference[5722]
    assert tree.invariants(), "Invariants failed at step 1811"

    # Operation 1812: delete
    del tree[9220]
    del reference[9220]
    assert tree.invariants(), "Invariants failed at step 1812"

    # Operation 1813: get
    assert tree.invariants(), "Invariants failed at step 1813"

    # Operation 1814: insert
    tree[8231] = 'value_287930'
    reference[8231] = 'value_287930'
    assert tree.invariants(), "Invariants failed at step 1814"

    # Operation 1815: insert
    tree[4998] = 'value_806799'
    reference[4998] = 'value_806799'
    assert tree.invariants(), "Invariants failed at step 1815"

    # Operation 1816: delete
    del tree[4998]
    del reference[4998]
    assert tree.invariants(), "Invariants failed at step 1816"

    # Operation 1817: delete
    del tree[8231]
    del reference[8231]
    assert tree.invariants(), "Invariants failed at step 1817"

    # Operation 1818: get
    assert tree.invariants(), "Invariants failed at step 1818"

    # Operation 1819: get
    assert tree.invariants(), "Invariants failed at step 1819"

    # Operation 1820: insert
    tree[5261] = 'value_660965'
    reference[5261] = 'value_660965'
    assert tree.invariants(), "Invariants failed at step 1820"

    # Operation 1821: get
    assert tree.invariants(), "Invariants failed at step 1821"

    # Operation 1822: update
    tree[5261] = 'value_936747'
    reference[5261] = 'value_936747'
    assert tree.invariants(), "Invariants failed at step 1822"

    # Operation 1823: insert
    tree[117] = 'value_41885'
    reference[117] = 'value_41885'
    assert tree.invariants(), "Invariants failed at step 1823"

    # Operation 1824: insert
    tree[258] = 'value_809553'
    reference[258] = 'value_809553'
    assert tree.invariants(), "Invariants failed at step 1824"

    # Operation 1825: delete
    del tree[117]
    del reference[117]
    assert tree.invariants(), "Invariants failed at step 1825"

    # Operation 1826: insert
    tree[927] = 'value_51435'
    reference[927] = 'value_51435'
    assert tree.invariants(), "Invariants failed at step 1826"

    # Operation 1827: insert
    tree[9575] = 'value_560969'
    reference[9575] = 'value_560969'
    assert tree.invariants(), "Invariants failed at step 1827"

    # Operation 1828: delete
    del tree[927]
    del reference[927]
    assert tree.invariants(), "Invariants failed at step 1828"

    # Operation 1829: insert
    tree[5144] = 'value_868399'
    reference[5144] = 'value_868399'
    assert tree.invariants(), "Invariants failed at step 1829"

    # Operation 1830: update
    tree[258] = 'value_11640'
    reference[258] = 'value_11640'
    assert tree.invariants(), "Invariants failed at step 1830"

    # Operation 1831: delete
    del tree[5261]
    del reference[5261]
    assert tree.invariants(), "Invariants failed at step 1831"

    # Operation 1832: get
    assert tree.invariants(), "Invariants failed at step 1832"

    # Operation 1833: delete
    del tree[258]
    del reference[258]
    assert tree.invariants(), "Invariants failed at step 1833"

    # Operation 1834: insert
    tree[1457] = 'value_702164'
    reference[1457] = 'value_702164'
    assert tree.invariants(), "Invariants failed at step 1834"

    # Operation 1835: get
    assert tree.invariants(), "Invariants failed at step 1835"

    # Operation 1836: insert
    tree[4172] = 'value_234895'
    reference[4172] = 'value_234895'
    assert tree.invariants(), "Invariants failed at step 1836"

    # Operation 1837: get
    assert tree.invariants(), "Invariants failed at step 1837"

    # Operation 1838: delete
    del tree[1457]
    del reference[1457]
    assert tree.invariants(), "Invariants failed at step 1838"

    # Operation 1839: get
    assert tree.invariants(), "Invariants failed at step 1839"

    # Operation 1840: delete
    del tree[4172]
    del reference[4172]
    assert tree.invariants(), "Invariants failed at step 1840"

    # Operation 1841: delete
    del tree[9575]
    del reference[9575]
    assert tree.invariants(), "Invariants failed at step 1841"

    # Operation 1842: insert
    tree[9636] = 'value_777281'
    reference[9636] = 'value_777281'
    assert tree.invariants(), "Invariants failed at step 1842"

    # Operation 1843: update
    tree[5144] = 'value_262169'
    reference[5144] = 'value_262169'
    assert tree.invariants(), "Invariants failed at step 1843"

    # Operation 1844: get
    assert tree.invariants(), "Invariants failed at step 1844"

    # Operation 1845: update
    tree[5144] = 'value_69628'
    reference[5144] = 'value_69628'
    assert tree.invariants(), "Invariants failed at step 1845"

    # Operation 1846: insert
    tree[7536] = 'value_993560'
    reference[7536] = 'value_993560'
    assert tree.invariants(), "Invariants failed at step 1846"

    # Operation 1847: update
    tree[7536] = 'value_410618'
    reference[7536] = 'value_410618'
    assert tree.invariants(), "Invariants failed at step 1847"

    # Operation 1848: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1848"

    # Operation 1849: update
    tree[7536] = 'value_857922'
    reference[7536] = 'value_857922'
    assert tree.invariants(), "Invariants failed at step 1849"

    # Operation 1850: get
    assert tree.invariants(), "Invariants failed at step 1850"

    # Operation 1851: insert
    tree[2740] = 'value_410352'
    reference[2740] = 'value_410352'
    assert tree.invariants(), "Invariants failed at step 1851"

    # Operation 1852: insert
    tree[3797] = 'value_475626'
    reference[3797] = 'value_475626'
    assert tree.invariants(), "Invariants failed at step 1852"

    # Operation 1853: delete
    del tree[7536]
    del reference[7536]
    assert tree.invariants(), "Invariants failed at step 1853"

    # Operation 1854: insert
    tree[1853] = 'value_767298'
    reference[1853] = 'value_767298'
    assert tree.invariants(), "Invariants failed at step 1854"

    # Operation 1855: update
    tree[9636] = 'value_458931'
    reference[9636] = 'value_458931'
    assert tree.invariants(), "Invariants failed at step 1855"

    # Operation 1856: get
    assert tree.invariants(), "Invariants failed at step 1856"

    # Operation 1857: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1857"

    # Operation 1858: get
    assert tree.invariants(), "Invariants failed at step 1858"

    # Operation 1859: get
    assert tree.invariants(), "Invariants failed at step 1859"

    # Operation 1860: delete
    del tree[9636]
    del reference[9636]
    assert tree.invariants(), "Invariants failed at step 1860"

    # Operation 1861: get
    assert tree.invariants(), "Invariants failed at step 1861"

    # Operation 1862: update
    tree[3797] = 'value_780584'
    reference[3797] = 'value_780584'
    assert tree.invariants(), "Invariants failed at step 1862"

    # Operation 1863: get
    assert tree.invariants(), "Invariants failed at step 1863"

    # Operation 1864: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1864"

    # Operation 1865: insert
    tree[415] = 'value_299327'
    reference[415] = 'value_299327'
    assert tree.invariants(), "Invariants failed at step 1865"

    # Operation 1866: insert
    tree[5446] = 'value_681748'
    reference[5446] = 'value_681748'
    assert tree.invariants(), "Invariants failed at step 1866"

    # Operation 1867: get
    assert tree.invariants(), "Invariants failed at step 1867"

    # Operation 1868: insert
    tree[988] = 'value_694291'
    reference[988] = 'value_694291'
    assert tree.invariants(), "Invariants failed at step 1868"

    # Operation 1869: get
    assert tree.invariants(), "Invariants failed at step 1869"

    # Operation 1870: update
    tree[415] = 'value_122997'
    reference[415] = 'value_122997'
    assert tree.invariants(), "Invariants failed at step 1870"

    # Operation 1871: insert
    tree[7859] = 'value_85687'
    reference[7859] = 'value_85687'
    assert tree.invariants(), "Invariants failed at step 1871"

    # Operation 1872: insert
    tree[6551] = 'value_605511'
    reference[6551] = 'value_605511'
    assert tree.invariants(), "Invariants failed at step 1872"

    # Operation 1873: get
    assert tree.invariants(), "Invariants failed at step 1873"

    # Operation 1874: batch_delete
    keys_to_delete = [3797, 1757, 1853, 6503]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1874"

    # Operation 1875: get
    assert tree.invariants(), "Invariants failed at step 1875"

    # Operation 1876: get
    assert tree.invariants(), "Invariants failed at step 1876"

    # Operation 1877: delete
    del tree[5144]
    del reference[5144]
    assert tree.invariants(), "Invariants failed at step 1877"

    # Operation 1878: get
    assert tree.invariants(), "Invariants failed at step 1878"

    # Operation 1879: insert
    tree[2362] = 'value_908027'
    reference[2362] = 'value_908027'
    assert tree.invariants(), "Invariants failed at step 1879"

    # Operation 1880: insert
    tree[1335] = 'value_404060'
    reference[1335] = 'value_404060'
    assert tree.invariants(), "Invariants failed at step 1880"

    # Operation 1881: batch_delete
    keys_to_delete = [961, 5446, 4556, 7859, 988, 415]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1881"

    # Operation 1882: delete
    del tree[6551]
    del reference[6551]
    assert tree.invariants(), "Invariants failed at step 1882"

    # Operation 1883: delete
    del tree[2740]
    del reference[2740]
    assert tree.invariants(), "Invariants failed at step 1883"

    # Operation 1884: update
    tree[2362] = 'value_11905'
    reference[2362] = 'value_11905'
    assert tree.invariants(), "Invariants failed at step 1884"

    # Operation 1885: insert
    tree[8486] = 'value_747345'
    reference[8486] = 'value_747345'
    assert tree.invariants(), "Invariants failed at step 1885"

    # Operation 1886: delete
    del tree[1335]
    del reference[1335]
    assert tree.invariants(), "Invariants failed at step 1886"

    # Operation 1887: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1887"

    # Operation 1888: insert
    tree[1241] = 'value_438737'
    reference[1241] = 'value_438737'
    assert tree.invariants(), "Invariants failed at step 1888"

    # Operation 1889: update
    tree[1241] = 'value_870514'
    reference[1241] = 'value_870514'
    assert tree.invariants(), "Invariants failed at step 1889"

    # Operation 1890: delete
    del tree[1241]
    del reference[1241]
    assert tree.invariants(), "Invariants failed at step 1890"

    # Operation 1891: insert
    tree[3540] = 'value_735621'
    reference[3540] = 'value_735621'
    assert tree.invariants(), "Invariants failed at step 1891"

    # Operation 1892: get
    assert tree.invariants(), "Invariants failed at step 1892"

    # Operation 1893: get
    assert tree.invariants(), "Invariants failed at step 1893"

    # Operation 1894: get
    assert tree.invariants(), "Invariants failed at step 1894"

    # Operation 1895: delete
    del tree[2362]
    del reference[2362]
    assert tree.invariants(), "Invariants failed at step 1895"

    # Operation 1896: insert
    tree[5168] = 'value_629845'
    reference[5168] = 'value_629845'
    assert tree.invariants(), "Invariants failed at step 1896"

    # Operation 1897: update
    tree[5168] = 'value_376510'
    reference[5168] = 'value_376510'
    assert tree.invariants(), "Invariants failed at step 1897"

    # Operation 1898: update
    tree[8486] = 'value_8210'
    reference[8486] = 'value_8210'
    assert tree.invariants(), "Invariants failed at step 1898"

    # Operation 1899: get
    assert tree.invariants(), "Invariants failed at step 1899"

    # Operation 1900: delete
    del tree[3540]
    del reference[3540]
    assert tree.invariants(), "Invariants failed at step 1900"

    # Operation 1901: get
    assert tree.invariants(), "Invariants failed at step 1901"

    # Operation 1902: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1902"

    # Operation 1903: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1903"

    # Operation 1904: delete
    del tree[5168]
    del reference[5168]
    assert tree.invariants(), "Invariants failed at step 1904"

    # Operation 1905: get
    assert tree.invariants(), "Invariants failed at step 1905"

    # Operation 1906: delete
    del tree[8486]
    del reference[8486]
    assert tree.invariants(), "Invariants failed at step 1906"

    # Operation 1907: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1907"

    # Operation 1908: get
    assert tree.invariants(), "Invariants failed at step 1908"

    # Operation 1909: insert
    tree[4944] = 'value_800510'
    reference[4944] = 'value_800510'
    assert tree.invariants(), "Invariants failed at step 1909"

    # Operation 1910: update
    tree[4944] = 'value_232008'
    reference[4944] = 'value_232008'
    assert tree.invariants(), "Invariants failed at step 1910"

    # Operation 1911: get
    assert tree.invariants(), "Invariants failed at step 1911"

    # Operation 1912: update
    tree[4944] = 'value_775489'
    reference[4944] = 'value_775489'
    assert tree.invariants(), "Invariants failed at step 1912"

    # Operation 1913: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1913"

    # Operation 1914: update
    tree[4944] = 'value_905185'
    reference[4944] = 'value_905185'
    assert tree.invariants(), "Invariants failed at step 1914"

    # Operation 1915: insert
    tree[3952] = 'value_565419'
    reference[3952] = 'value_565419'
    assert tree.invariants(), "Invariants failed at step 1915"

    # Operation 1916: insert
    tree[5492] = 'value_923207'
    reference[5492] = 'value_923207'
    assert tree.invariants(), "Invariants failed at step 1916"

    # Operation 1917: insert
    tree[4544] = 'value_450469'
    reference[4544] = 'value_450469'
    assert tree.invariants(), "Invariants failed at step 1917"

    # Operation 1918: insert
    tree[2232] = 'value_401818'
    reference[2232] = 'value_401818'
    assert tree.invariants(), "Invariants failed at step 1918"

    # Operation 1919: delete
    del tree[3952]
    del reference[3952]
    assert tree.invariants(), "Invariants failed at step 1919"

    # Operation 1920: get
    assert tree.invariants(), "Invariants failed at step 1920"

    # Operation 1921: insert
    tree[7645] = 'value_483634'
    reference[7645] = 'value_483634'
    assert tree.invariants(), "Invariants failed at step 1921"

    # Operation 1922: delete
    del tree[5492]
    del reference[5492]
    assert tree.invariants(), "Invariants failed at step 1922"

    # Operation 1923: get
    assert tree.invariants(), "Invariants failed at step 1923"

    # Operation 1924: delete
    del tree[2232]
    del reference[2232]
    assert tree.invariants(), "Invariants failed at step 1924"

    # Operation 1925: update
    tree[4544] = 'value_34907'
    reference[4544] = 'value_34907'
    assert tree.invariants(), "Invariants failed at step 1925"

    # Operation 1926: update
    tree[4544] = 'value_11634'
    reference[4544] = 'value_11634'
    assert tree.invariants(), "Invariants failed at step 1926"

    # Operation 1927: delete
    del tree[4944]
    del reference[4944]
    assert tree.invariants(), "Invariants failed at step 1927"

    # Operation 1928: insert
    tree[5270] = 'value_566616'
    reference[5270] = 'value_566616'
    assert tree.invariants(), "Invariants failed at step 1928"

    # Operation 1929: update
    tree[7645] = 'value_606645'
    reference[7645] = 'value_606645'
    assert tree.invariants(), "Invariants failed at step 1929"

    # Operation 1930: update
    tree[7645] = 'value_147717'
    reference[7645] = 'value_147717'
    assert tree.invariants(), "Invariants failed at step 1930"

    # Operation 1931: insert
    tree[8680] = 'value_997466'
    reference[8680] = 'value_997466'
    assert tree.invariants(), "Invariants failed at step 1931"

    # Operation 1932: update
    tree[8680] = 'value_168492'
    reference[8680] = 'value_168492'
    assert tree.invariants(), "Invariants failed at step 1932"

    # Operation 1933: update
    tree[4544] = 'value_52326'
    reference[4544] = 'value_52326'
    assert tree.invariants(), "Invariants failed at step 1933"

    # Operation 1934: get
    assert tree.invariants(), "Invariants failed at step 1934"

    # Operation 1935: get
    assert tree.invariants(), "Invariants failed at step 1935"

    # Operation 1936: insert
    tree[4643] = 'value_241078'
    reference[4643] = 'value_241078'
    assert tree.invariants(), "Invariants failed at step 1936"

    # Operation 1937: batch_delete
    keys_to_delete = [8680, 2440, 7645, 5837]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1937"

    # Operation 1938: get
    assert tree.invariants(), "Invariants failed at step 1938"

    # Operation 1939: update
    tree[5270] = 'value_170080'
    reference[5270] = 'value_170080'
    assert tree.invariants(), "Invariants failed at step 1939"

    # Operation 1940: update
    tree[4643] = 'value_456403'
    reference[4643] = 'value_456403'
    assert tree.invariants(), "Invariants failed at step 1940"

    # Operation 1941: delete
    del tree[4643]
    del reference[4643]
    assert tree.invariants(), "Invariants failed at step 1941"

    # Operation 1942: update
    tree[5270] = 'value_185937'
    reference[5270] = 'value_185937'
    assert tree.invariants(), "Invariants failed at step 1942"

    # Operation 1943: delete
    del tree[4544]
    del reference[4544]
    assert tree.invariants(), "Invariants failed at step 1943"

    # Operation 1944: insert
    tree[2280] = 'value_798976'
    reference[2280] = 'value_798976'
    assert tree.invariants(), "Invariants failed at step 1944"

    # Operation 1945: update
    tree[5270] = 'value_441865'
    reference[5270] = 'value_441865'
    assert tree.invariants(), "Invariants failed at step 1945"

    # Operation 1946: get
    assert tree.invariants(), "Invariants failed at step 1946"

    # Operation 1947: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1947"

    # Operation 1948: delete
    del tree[2280]
    del reference[2280]
    assert tree.invariants(), "Invariants failed at step 1948"

    # Operation 1949: delete
    del tree[5270]
    del reference[5270]
    assert tree.invariants(), "Invariants failed at step 1949"

    # Operation 1950: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1950"

    # Operation 1951: insert
    tree[8417] = 'value_495800'
    reference[8417] = 'value_495800'
    assert tree.invariants(), "Invariants failed at step 1951"

    # Operation 1952: insert
    tree[2320] = 'value_259019'
    reference[2320] = 'value_259019'
    assert tree.invariants(), "Invariants failed at step 1952"

    # Operation 1953: insert
    tree[4475] = 'value_166573'
    reference[4475] = 'value_166573'
    assert tree.invariants(), "Invariants failed at step 1953"

    # Operation 1954: update
    tree[8417] = 'value_755296'
    reference[8417] = 'value_755296'
    assert tree.invariants(), "Invariants failed at step 1954"

    # Operation 1955: get
    assert tree.invariants(), "Invariants failed at step 1955"

    # Operation 1956: update
    tree[8417] = 'value_50324'
    reference[8417] = 'value_50324'
    assert tree.invariants(), "Invariants failed at step 1956"

    # Operation 1957: insert
    tree[2918] = 'value_997436'
    reference[2918] = 'value_997436'
    assert tree.invariants(), "Invariants failed at step 1957"

    # Operation 1958: get
    assert tree.invariants(), "Invariants failed at step 1958"

    # Operation 1959: insert
    tree[9468] = 'value_460328'
    reference[9468] = 'value_460328'
    assert tree.invariants(), "Invariants failed at step 1959"

    # Operation 1960: get
    assert tree.invariants(), "Invariants failed at step 1960"

    # Operation 1961: get
    assert tree.invariants(), "Invariants failed at step 1961"

    # Operation 1962: insert
    tree[696] = 'value_283751'
    reference[696] = 'value_283751'
    assert tree.invariants(), "Invariants failed at step 1962"

    # Operation 1963: get
    assert tree.invariants(), "Invariants failed at step 1963"

    # Operation 1964: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 1964"

    # Operation 1965: update
    tree[2918] = 'value_815155'
    reference[2918] = 'value_815155'
    assert tree.invariants(), "Invariants failed at step 1965"

    # Operation 1966: delete
    del tree[8417]
    del reference[8417]
    assert tree.invariants(), "Invariants failed at step 1966"

    # Operation 1967: batch_delete
    keys_to_delete = [1940, 9468, 2918, 6975]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 1967"

    # Operation 1968: update
    tree[4475] = 'value_652036'
    reference[4475] = 'value_652036'
    assert tree.invariants(), "Invariants failed at step 1968"

    # Operation 1969: update
    tree[2320] = 'value_712666'
    reference[2320] = 'value_712666'
    assert tree.invariants(), "Invariants failed at step 1969"

    # Operation 1970: insert
    tree[8476] = 'value_538894'
    reference[8476] = 'value_538894'
    assert tree.invariants(), "Invariants failed at step 1970"

    # Operation 1971: update
    tree[696] = 'value_604149'
    reference[696] = 'value_604149'
    assert tree.invariants(), "Invariants failed at step 1971"

    # Operation 1972: get
    assert tree.invariants(), "Invariants failed at step 1972"

    # Operation 1973: insert
    tree[9522] = 'value_454277'
    reference[9522] = 'value_454277'
    assert tree.invariants(), "Invariants failed at step 1973"

    # Operation 1974: insert
    tree[2732] = 'value_70427'
    reference[2732] = 'value_70427'
    assert tree.invariants(), "Invariants failed at step 1974"

    # Operation 1975: insert
    tree[8795] = 'value_754886'
    reference[8795] = 'value_754886'
    assert tree.invariants(), "Invariants failed at step 1975"

    # Operation 1976: delete
    del tree[9522]
    del reference[9522]
    assert tree.invariants(), "Invariants failed at step 1976"

    # Operation 1977: delete
    del tree[2732]
    del reference[2732]
    assert tree.invariants(), "Invariants failed at step 1977"

    # Operation 1978: get
    assert tree.invariants(), "Invariants failed at step 1978"

    # Operation 1979: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1979"

    # Operation 1980: insert
    tree[1684] = 'value_714685'
    reference[1684] = 'value_714685'
    assert tree.invariants(), "Invariants failed at step 1980"

    # Operation 1981: get
    assert tree.invariants(), "Invariants failed at step 1981"

    # Operation 1982: get
    assert tree.invariants(), "Invariants failed at step 1982"

    # Operation 1983: delete
    del tree[4475]
    del reference[4475]
    assert tree.invariants(), "Invariants failed at step 1983"

    # Operation 1984: insert
    tree[6724] = 'value_84302'
    reference[6724] = 'value_84302'
    assert tree.invariants(), "Invariants failed at step 1984"

    # Operation 1985: insert
    tree[6847] = 'value_960047'
    reference[6847] = 'value_960047'
    assert tree.invariants(), "Invariants failed at step 1985"

    # Operation 1986: delete
    del tree[6847]
    del reference[6847]
    assert tree.invariants(), "Invariants failed at step 1986"

    # Operation 1987: insert
    tree[2171] = 'value_445595'
    reference[2171] = 'value_445595'
    assert tree.invariants(), "Invariants failed at step 1987"

    # Operation 1988: delete
    del tree[2171]
    del reference[2171]
    assert tree.invariants(), "Invariants failed at step 1988"

    # Operation 1989: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1989"

    # Operation 1990: delete
    del tree[6724]
    del reference[6724]
    assert tree.invariants(), "Invariants failed at step 1990"

    # Operation 1991: delete
    del tree[8795]
    del reference[8795]
    assert tree.invariants(), "Invariants failed at step 1991"

    # Operation 1992: insert
    tree[5739] = 'value_626082'
    reference[5739] = 'value_626082'
    assert tree.invariants(), "Invariants failed at step 1992"

    # Operation 1993: get
    assert tree.invariants(), "Invariants failed at step 1993"

    # Operation 1994: get
    assert tree.invariants(), "Invariants failed at step 1994"

    # Operation 1995: delete
    del tree[5739]
    del reference[5739]
    assert tree.invariants(), "Invariants failed at step 1995"

    # Operation 1996: delete
    del tree[8476]
    del reference[8476]
    assert tree.invariants(), "Invariants failed at step 1996"

    # Operation 1997: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 1997"

    # Operation 1998: delete
    del tree[2320]
    del reference[2320]
    assert tree.invariants(), "Invariants failed at step 1998"

    # Operation 1999: insert
    tree[7399] = 'value_186363'
    reference[7399] = 'value_186363'
    assert tree.invariants(), "Invariants failed at step 1999"

    # Operation 2000: get
    assert tree.invariants(), "Invariants failed at step 2000"

    # Operation 2001: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2001"

    # Operation 2002: insert
    tree[7067] = 'value_144215'
    reference[7067] = 'value_144215'
    assert tree.invariants(), "Invariants failed at step 2002"

    # Operation 2003: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2003"

    # Operation 2004: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2004"

    # Operation 2005: insert
    tree[6163] = 'value_615642'
    reference[6163] = 'value_615642'
    assert tree.invariants(), "Invariants failed at step 2005"

    # Operation 2006: delete
    del tree[6163]
    del reference[6163]
    assert tree.invariants(), "Invariants failed at step 2006"

    # Operation 2007: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2007"

    # Operation 2008: insert
    tree[7010] = 'value_203023'
    reference[7010] = 'value_203023'
    assert tree.invariants(), "Invariants failed at step 2008"

    # Operation 2009: get
    assert tree.invariants(), "Invariants failed at step 2009"

    # Operation 2010: delete
    del tree[7010]
    del reference[7010]
    assert tree.invariants(), "Invariants failed at step 2010"

    # Operation 2011: insert
    tree[7153] = 'value_448893'
    reference[7153] = 'value_448893'
    assert tree.invariants(), "Invariants failed at step 2011"

    # Operation 2012: update
    tree[7153] = 'value_511403'
    reference[7153] = 'value_511403'
    assert tree.invariants(), "Invariants failed at step 2012"

    # Operation 2013: delete
    del tree[696]
    del reference[696]
    assert tree.invariants(), "Invariants failed at step 2013"

    # Operation 2014: get
    assert tree.invariants(), "Invariants failed at step 2014"

    # Operation 2015: insert
    tree[160] = 'value_700745'
    reference[160] = 'value_700745'
    assert tree.invariants(), "Invariants failed at step 2015"

    # Operation 2016: get
    assert tree.invariants(), "Invariants failed at step 2016"

    # Operation 2017: insert
    tree[8708] = 'value_90052'
    reference[8708] = 'value_90052'
    assert tree.invariants(), "Invariants failed at step 2017"

    # Operation 2018: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2018"

    # Operation 2019: get
    assert tree.invariants(), "Invariants failed at step 2019"

    # Operation 2020: get
    assert tree.invariants(), "Invariants failed at step 2020"

    # Operation 2021: batch_delete
    keys_to_delete = [160, 7399, 2454, 3768, 7067]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2021"

    # Operation 2022: get
    assert tree.invariants(), "Invariants failed at step 2022"

    # Operation 2023: delete
    del tree[7153]
    del reference[7153]
    assert tree.invariants(), "Invariants failed at step 2023"

    # Operation 2024: get
    assert tree.invariants(), "Invariants failed at step 2024"

    # Operation 2025: delete
    del tree[8708]
    del reference[8708]
    assert tree.invariants(), "Invariants failed at step 2025"

    # Operation 2026: delete
    del tree[1684]
    del reference[1684]
    assert tree.invariants(), "Invariants failed at step 2026"

    # Operation 2027: insert
    tree[1355] = 'value_889227'
    reference[1355] = 'value_889227'
    assert tree.invariants(), "Invariants failed at step 2027"

    # Operation 2028: insert
    tree[22] = 'value_225616'
    reference[22] = 'value_225616'
    assert tree.invariants(), "Invariants failed at step 2028"

    # Operation 2029: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2029"

    # Operation 2030: delete
    del tree[22]
    del reference[22]
    assert tree.invariants(), "Invariants failed at step 2030"

    # Operation 2031: insert
    tree[651] = 'value_878232'
    reference[651] = 'value_878232'
    assert tree.invariants(), "Invariants failed at step 2031"

    # Operation 2032: get
    assert tree.invariants(), "Invariants failed at step 2032"

    # Operation 2033: insert
    tree[5801] = 'value_949017'
    reference[5801] = 'value_949017'
    assert tree.invariants(), "Invariants failed at step 2033"

    # Operation 2034: update
    tree[5801] = 'value_387854'
    reference[5801] = 'value_387854'
    assert tree.invariants(), "Invariants failed at step 2034"

    # Operation 2035: insert
    tree[5004] = 'value_795772'
    reference[5004] = 'value_795772'
    assert tree.invariants(), "Invariants failed at step 2035"

    # Operation 2036: delete
    del tree[5801]
    del reference[5801]
    assert tree.invariants(), "Invariants failed at step 2036"

    # Operation 2037: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2037"

    # Operation 2038: delete
    del tree[651]
    del reference[651]
    assert tree.invariants(), "Invariants failed at step 2038"

    # Operation 2039: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2039"

    # Operation 2040: get
    assert tree.invariants(), "Invariants failed at step 2040"

    # Operation 2041: insert
    tree[4732] = 'value_86915'
    reference[4732] = 'value_86915'
    assert tree.invariants(), "Invariants failed at step 2041"

    # Operation 2042: insert
    tree[6752] = 'value_991491'
    reference[6752] = 'value_991491'
    assert tree.invariants(), "Invariants failed at step 2042"

    # Operation 2043: get
    assert tree.invariants(), "Invariants failed at step 2043"

    # Operation 2044: update
    tree[6752] = 'value_559094'
    reference[6752] = 'value_559094'
    assert tree.invariants(), "Invariants failed at step 2044"

    # Operation 2045: delete
    del tree[1355]
    del reference[1355]
    assert tree.invariants(), "Invariants failed at step 2045"

    # Operation 2046: update
    tree[5004] = 'value_533775'
    reference[5004] = 'value_533775'
    assert tree.invariants(), "Invariants failed at step 2046"

    # Operation 2047: update
    tree[5004] = 'value_874254'
    reference[5004] = 'value_874254'
    assert tree.invariants(), "Invariants failed at step 2047"

    # Operation 2048: update
    tree[5004] = 'value_490516'
    reference[5004] = 'value_490516'
    assert tree.invariants(), "Invariants failed at step 2048"

    # Operation 2049: insert
    tree[5719] = 'value_80186'
    reference[5719] = 'value_80186'
    assert tree.invariants(), "Invariants failed at step 2049"

    # Operation 2050: update
    tree[4732] = 'value_453904'
    reference[4732] = 'value_453904'
    assert tree.invariants(), "Invariants failed at step 2050"

    # Operation 2051: insert
    tree[730] = 'value_956091'
    reference[730] = 'value_956091'
    assert tree.invariants(), "Invariants failed at step 2051"

    # Operation 2052: insert
    tree[8208] = 'value_54955'
    reference[8208] = 'value_54955'
    assert tree.invariants(), "Invariants failed at step 2052"

    # Operation 2053: get
    assert tree.invariants(), "Invariants failed at step 2053"

    # Operation 2054: delete
    del tree[8208]
    del reference[8208]
    assert tree.invariants(), "Invariants failed at step 2054"

    # Operation 2055: insert
    tree[2006] = 'value_347789'
    reference[2006] = 'value_347789'
    assert tree.invariants(), "Invariants failed at step 2055"

    # Operation 2056: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2056"

    # Operation 2057: insert
    tree[3394] = 'value_163741'
    reference[3394] = 'value_163741'
    assert tree.invariants(), "Invariants failed at step 2057"

    # Operation 2058: insert
    tree[2183] = 'value_239641'
    reference[2183] = 'value_239641'
    assert tree.invariants(), "Invariants failed at step 2058"

    # Operation 2059: insert
    tree[6610] = 'value_216656'
    reference[6610] = 'value_216656'
    assert tree.invariants(), "Invariants failed at step 2059"

    # Operation 2060: batch_delete
    keys_to_delete = [3394, 2183, 6610, 2006, 4732, 7230]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2060"

    # Operation 2061: update
    tree[5004] = 'value_385594'
    reference[5004] = 'value_385594'
    assert tree.invariants(), "Invariants failed at step 2061"

    # Operation 2062: delete
    del tree[6752]
    del reference[6752]
    assert tree.invariants(), "Invariants failed at step 2062"

    # Operation 2063: insert
    tree[8342] = 'value_847546'
    reference[8342] = 'value_847546'
    assert tree.invariants(), "Invariants failed at step 2063"

    # Operation 2064: get
    assert tree.invariants(), "Invariants failed at step 2064"

    # Operation 2065: get
    assert tree.invariants(), "Invariants failed at step 2065"

    # Operation 2066: insert
    tree[4111] = 'value_935605'
    reference[4111] = 'value_935605'
    assert tree.invariants(), "Invariants failed at step 2066"

    # Operation 2067: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2067"

    # Operation 2068: insert
    tree[4018] = 'value_89703'
    reference[4018] = 'value_89703'
    assert tree.invariants(), "Invariants failed at step 2068"

    # Operation 2069: get
    assert tree.invariants(), "Invariants failed at step 2069"

    # Operation 2070: delete
    del tree[4018]
    del reference[4018]
    assert tree.invariants(), "Invariants failed at step 2070"

    # Operation 2071: update
    tree[5004] = 'value_413935'
    reference[5004] = 'value_413935'
    assert tree.invariants(), "Invariants failed at step 2071"

    # Operation 2072: insert
    tree[9255] = 'value_320276'
    reference[9255] = 'value_320276'
    assert tree.invariants(), "Invariants failed at step 2072"

    # Operation 2073: update
    tree[4111] = 'value_739253'
    reference[4111] = 'value_739253'
    assert tree.invariants(), "Invariants failed at step 2073"

    # Operation 2074: update
    tree[9255] = 'value_922317'
    reference[9255] = 'value_922317'
    assert tree.invariants(), "Invariants failed at step 2074"

    # Operation 2075: batch_delete
    keys_to_delete = [2500, 9255, 5004, 8342, 730]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2075"

    # Operation 2076: insert
    tree[3989] = 'value_172002'
    reference[3989] = 'value_172002'
    assert tree.invariants(), "Invariants failed at step 2076"

    # Operation 2077: delete
    del tree[3989]
    del reference[3989]
    assert tree.invariants(), "Invariants failed at step 2077"

    # Operation 2078: insert
    tree[4653] = 'value_959881'
    reference[4653] = 'value_959881'
    assert tree.invariants(), "Invariants failed at step 2078"

    # Operation 2079: delete
    del tree[4111]
    del reference[4111]
    assert tree.invariants(), "Invariants failed at step 2079"

    # Operation 2080: get
    assert tree.invariants(), "Invariants failed at step 2080"

    # Operation 2081: get
    assert tree.invariants(), "Invariants failed at step 2081"

    # Operation 2082: insert
    tree[5481] = 'value_288734'
    reference[5481] = 'value_288734'
    assert tree.invariants(), "Invariants failed at step 2082"

    # Operation 2083: get
    assert tree.invariants(), "Invariants failed at step 2083"

    # Operation 2084: delete
    del tree[5719]
    del reference[5719]
    assert tree.invariants(), "Invariants failed at step 2084"

    # Operation 2085: delete
    del tree[4653]
    del reference[4653]
    assert tree.invariants(), "Invariants failed at step 2085"

    # Operation 2086: insert
    tree[7025] = 'value_796131'
    reference[7025] = 'value_796131'
    assert tree.invariants(), "Invariants failed at step 2086"

    # Operation 2087: update
    tree[5481] = 'value_670235'
    reference[5481] = 'value_670235'
    assert tree.invariants(), "Invariants failed at step 2087"

    # Operation 2088: update
    tree[7025] = 'value_617499'
    reference[7025] = 'value_617499'
    assert tree.invariants(), "Invariants failed at step 2088"

    # Operation 2089: delete
    del tree[7025]
    del reference[7025]
    assert tree.invariants(), "Invariants failed at step 2089"

    # Operation 2090: get
    assert tree.invariants(), "Invariants failed at step 2090"

    # Operation 2091: delete
    del tree[5481]
    del reference[5481]
    assert tree.invariants(), "Invariants failed at step 2091"

    # Operation 2092: insert
    tree[6451] = 'value_920492'
    reference[6451] = 'value_920492'
    assert tree.invariants(), "Invariants failed at step 2092"

    # Operation 2093: insert
    tree[8705] = 'value_550303'
    reference[8705] = 'value_550303'
    assert tree.invariants(), "Invariants failed at step 2093"

    # Operation 2094: get
    assert tree.invariants(), "Invariants failed at step 2094"

    # Operation 2095: delete
    del tree[8705]
    del reference[8705]
    assert tree.invariants(), "Invariants failed at step 2095"

    # Operation 2096: update
    tree[6451] = 'value_597779'
    reference[6451] = 'value_597779'
    assert tree.invariants(), "Invariants failed at step 2096"

    # Operation 2097: insert
    tree[4294] = 'value_694210'
    reference[4294] = 'value_694210'
    assert tree.invariants(), "Invariants failed at step 2097"

    # Operation 2098: insert
    tree[6413] = 'value_720663'
    reference[6413] = 'value_720663'
    assert tree.invariants(), "Invariants failed at step 2098"

    # Operation 2099: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2099"

    # Operation 2100: get
    assert tree.invariants(), "Invariants failed at step 2100"

    # Operation 2101: delete
    del tree[4294]
    del reference[4294]
    assert tree.invariants(), "Invariants failed at step 2101"

    # Operation 2102: get
    assert tree.invariants(), "Invariants failed at step 2102"

    # Operation 2103: get
    assert tree.invariants(), "Invariants failed at step 2103"

    # Operation 2104: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2104"

    # Operation 2105: insert
    tree[4316] = 'value_759367'
    reference[4316] = 'value_759367'
    assert tree.invariants(), "Invariants failed at step 2105"

    # Operation 2106: delete
    del tree[6451]
    del reference[6451]
    assert tree.invariants(), "Invariants failed at step 2106"

    # Operation 2107: get
    assert tree.invariants(), "Invariants failed at step 2107"

    # Operation 2108: insert
    tree[7773] = 'value_415326'
    reference[7773] = 'value_415326'
    assert tree.invariants(), "Invariants failed at step 2108"

    # Operation 2109: insert
    tree[8488] = 'value_270009'
    reference[8488] = 'value_270009'
    assert tree.invariants(), "Invariants failed at step 2109"

    # Operation 2110: delete
    del tree[7773]
    del reference[7773]
    assert tree.invariants(), "Invariants failed at step 2110"

    # Operation 2111: get
    assert tree.invariants(), "Invariants failed at step 2111"

    # Operation 2112: delete
    del tree[4316]
    del reference[4316]
    assert tree.invariants(), "Invariants failed at step 2112"

    # Operation 2113: delete
    del tree[8488]
    del reference[8488]
    assert tree.invariants(), "Invariants failed at step 2113"

    # Operation 2114: get
    assert tree.invariants(), "Invariants failed at step 2114"

    # Operation 2115: update
    tree[6413] = 'value_246235'
    reference[6413] = 'value_246235'
    assert tree.invariants(), "Invariants failed at step 2115"

    # Operation 2116: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2116"

    # Operation 2117: delete
    del tree[6413]
    del reference[6413]
    assert tree.invariants(), "Invariants failed at step 2117"

    # Operation 2118: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2118"

    # Operation 2119: insert
    tree[5120] = 'value_497967'
    reference[5120] = 'value_497967'
    assert tree.invariants(), "Invariants failed at step 2119"

    # Operation 2120: insert
    tree[802] = 'value_68938'
    reference[802] = 'value_68938'
    assert tree.invariants(), "Invariants failed at step 2120"

    # Operation 2121: insert
    tree[3094] = 'value_212188'
    reference[3094] = 'value_212188'
    assert tree.invariants(), "Invariants failed at step 2121"

    # Operation 2122: update
    tree[3094] = 'value_400124'
    reference[3094] = 'value_400124'
    assert tree.invariants(), "Invariants failed at step 2122"

    # Operation 2123: delete
    del tree[3094]
    del reference[3094]
    assert tree.invariants(), "Invariants failed at step 2123"

    # Operation 2124: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2124"

    # Operation 2125: delete
    del tree[5120]
    del reference[5120]
    assert tree.invariants(), "Invariants failed at step 2125"

    # Operation 2126: get
    assert tree.invariants(), "Invariants failed at step 2126"

    # Operation 2127: delete
    del tree[802]
    del reference[802]
    assert tree.invariants(), "Invariants failed at step 2127"

    # Operation 2128: insert
    tree[6022] = 'value_845604'
    reference[6022] = 'value_845604'
    assert tree.invariants(), "Invariants failed at step 2128"

    # Operation 2129: update
    tree[6022] = 'value_418966'
    reference[6022] = 'value_418966'
    assert tree.invariants(), "Invariants failed at step 2129"

    # Operation 2130: insert
    tree[1464] = 'value_139928'
    reference[1464] = 'value_139928'
    assert tree.invariants(), "Invariants failed at step 2130"

    # Operation 2131: insert
    tree[2356] = 'value_918778'
    reference[2356] = 'value_918778'
    assert tree.invariants(), "Invariants failed at step 2131"

    # Operation 2132: insert
    tree[8744] = 'value_288291'
    reference[8744] = 'value_288291'
    assert tree.invariants(), "Invariants failed at step 2132"

    # Operation 2133: insert
    tree[3960] = 'value_889435'
    reference[3960] = 'value_889435'
    assert tree.invariants(), "Invariants failed at step 2133"

    # Operation 2134: delete
    del tree[1464]
    del reference[1464]
    assert tree.invariants(), "Invariants failed at step 2134"

    # Operation 2135: delete
    del tree[2356]
    del reference[2356]
    assert tree.invariants(), "Invariants failed at step 2135"

    # Operation 2136: get
    assert tree.invariants(), "Invariants failed at step 2136"

    # Operation 2137: delete
    del tree[3960]
    del reference[3960]
    assert tree.invariants(), "Invariants failed at step 2137"

    # Operation 2138: delete
    del tree[6022]
    del reference[6022]
    assert tree.invariants(), "Invariants failed at step 2138"

    # Operation 2139: get
    assert tree.invariants(), "Invariants failed at step 2139"

    # Operation 2140: update
    tree[8744] = 'value_598957'
    reference[8744] = 'value_598957'
    assert tree.invariants(), "Invariants failed at step 2140"

    # Operation 2141: insert
    tree[3479] = 'value_11285'
    reference[3479] = 'value_11285'
    assert tree.invariants(), "Invariants failed at step 2141"

    # Operation 2142: insert
    tree[1032] = 'value_515723'
    reference[1032] = 'value_515723'
    assert tree.invariants(), "Invariants failed at step 2142"

    # Operation 2143: insert
    tree[6875] = 'value_989177'
    reference[6875] = 'value_989177'
    assert tree.invariants(), "Invariants failed at step 2143"

    # Operation 2144: insert
    tree[7001] = 'value_372281'
    reference[7001] = 'value_372281'
    assert tree.invariants(), "Invariants failed at step 2144"

    # Operation 2145: get
    assert tree.invariants(), "Invariants failed at step 2145"

    # Operation 2146: update
    tree[7001] = 'value_479578'
    reference[7001] = 'value_479578'
    assert tree.invariants(), "Invariants failed at step 2146"

    # Operation 2147: delete
    del tree[1032]
    del reference[1032]
    assert tree.invariants(), "Invariants failed at step 2147"

    # Operation 2148: update
    tree[8744] = 'value_532990'
    reference[8744] = 'value_532990'
    assert tree.invariants(), "Invariants failed at step 2148"

    # Operation 2149: get
    assert tree.invariants(), "Invariants failed at step 2149"

    # Operation 2150: insert
    tree[7661] = 'value_94594'
    reference[7661] = 'value_94594'
    assert tree.invariants(), "Invariants failed at step 2150"

    # Operation 2151: insert
    tree[478] = 'value_588960'
    reference[478] = 'value_588960'
    assert tree.invariants(), "Invariants failed at step 2151"

    # Operation 2152: delete
    del tree[478]
    del reference[478]
    assert tree.invariants(), "Invariants failed at step 2152"

    # Operation 2153: update
    tree[7661] = 'value_178470'
    reference[7661] = 'value_178470'
    assert tree.invariants(), "Invariants failed at step 2153"

    # Operation 2154: delete
    del tree[3479]
    del reference[3479]
    assert tree.invariants(), "Invariants failed at step 2154"

    # Operation 2155: delete
    del tree[8744]
    del reference[8744]
    assert tree.invariants(), "Invariants failed at step 2155"

    # Operation 2156: get
    assert tree.invariants(), "Invariants failed at step 2156"

    # Operation 2157: insert
    tree[7925] = 'value_329812'
    reference[7925] = 'value_329812'
    assert tree.invariants(), "Invariants failed at step 2157"

    # Operation 2158: update
    tree[7661] = 'value_387122'
    reference[7661] = 'value_387122'
    assert tree.invariants(), "Invariants failed at step 2158"

    # Operation 2159: insert
    tree[6487] = 'value_162721'
    reference[6487] = 'value_162721'
    assert tree.invariants(), "Invariants failed at step 2159"

    # Operation 2160: insert
    tree[5644] = 'value_229426'
    reference[5644] = 'value_229426'
    assert tree.invariants(), "Invariants failed at step 2160"

    # Operation 2161: insert
    tree[1053] = 'value_897598'
    reference[1053] = 'value_897598'
    assert tree.invariants(), "Invariants failed at step 2161"

    # Operation 2162: insert
    tree[5635] = 'value_672846'
    reference[5635] = 'value_672846'
    assert tree.invariants(), "Invariants failed at step 2162"

    # Operation 2163: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2163"

    # Operation 2164: delete
    del tree[5644]
    del reference[5644]
    assert tree.invariants(), "Invariants failed at step 2164"

    # Operation 2165: delete
    del tree[7661]
    del reference[7661]
    assert tree.invariants(), "Invariants failed at step 2165"

    # Operation 2166: update
    tree[6875] = 'value_138660'
    reference[6875] = 'value_138660'
    assert tree.invariants(), "Invariants failed at step 2166"

    # Operation 2167: get
    assert tree.invariants(), "Invariants failed at step 2167"

    # Operation 2168: update
    tree[5635] = 'value_817403'
    reference[5635] = 'value_817403'
    assert tree.invariants(), "Invariants failed at step 2168"

    # Operation 2169: batch_delete
    keys_to_delete = [9429, 6487, 7001, 6875, 1053]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2169"

    # Operation 2170: update
    tree[7925] = 'value_150931'
    reference[7925] = 'value_150931'
    assert tree.invariants(), "Invariants failed at step 2170"

    # Operation 2171: get
    assert tree.invariants(), "Invariants failed at step 2171"

    # Operation 2172: insert
    tree[300] = 'value_225558'
    reference[300] = 'value_225558'
    assert tree.invariants(), "Invariants failed at step 2172"

    # Operation 2173: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2173"

    # Operation 2174: get
    assert tree.invariants(), "Invariants failed at step 2174"

    # Operation 2175: insert
    tree[3947] = 'value_595627'
    reference[3947] = 'value_595627'
    assert tree.invariants(), "Invariants failed at step 2175"

    # Operation 2176: delete
    del tree[7925]
    del reference[7925]
    assert tree.invariants(), "Invariants failed at step 2176"

    # Operation 2177: insert
    tree[1150] = 'value_358632'
    reference[1150] = 'value_358632'
    assert tree.invariants(), "Invariants failed at step 2177"

    # Operation 2178: insert
    tree[979] = 'value_831989'
    reference[979] = 'value_831989'
    assert tree.invariants(), "Invariants failed at step 2178"

    # Operation 2179: delete
    del tree[3947]
    del reference[3947]
    assert tree.invariants(), "Invariants failed at step 2179"

    # Operation 2180: update
    tree[1150] = 'value_833198'
    reference[1150] = 'value_833198'
    assert tree.invariants(), "Invariants failed at step 2180"

    # Operation 2181: insert
    tree[857] = 'value_949901'
    reference[857] = 'value_949901'
    assert tree.invariants(), "Invariants failed at step 2181"

    # Operation 2182: delete
    del tree[5635]
    del reference[5635]
    assert tree.invariants(), "Invariants failed at step 2182"

    # Operation 2183: get
    assert tree.invariants(), "Invariants failed at step 2183"

    # Operation 2184: delete
    del tree[857]
    del reference[857]
    assert tree.invariants(), "Invariants failed at step 2184"

    # Operation 2185: get
    assert tree.invariants(), "Invariants failed at step 2185"

    # Operation 2186: delete
    del tree[1150]
    del reference[1150]
    assert tree.invariants(), "Invariants failed at step 2186"

    # Operation 2187: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2187"

    # Operation 2188: insert
    tree[7318] = 'value_112164'
    reference[7318] = 'value_112164'
    assert tree.invariants(), "Invariants failed at step 2188"

    # Operation 2189: delete
    del tree[300]
    del reference[300]
    assert tree.invariants(), "Invariants failed at step 2189"

    # Operation 2190: update
    tree[979] = 'value_281589'
    reference[979] = 'value_281589'
    assert tree.invariants(), "Invariants failed at step 2190"

    # Operation 2191: get
    assert tree.invariants(), "Invariants failed at step 2191"

    # Operation 2192: insert
    tree[6854] = 'value_267066'
    reference[6854] = 'value_267066'
    assert tree.invariants(), "Invariants failed at step 2192"

    # Operation 2193: insert
    tree[2718] = 'value_871998'
    reference[2718] = 'value_871998'
    assert tree.invariants(), "Invariants failed at step 2193"

    # Operation 2194: insert
    tree[8639] = 'value_834014'
    reference[8639] = 'value_834014'
    assert tree.invariants(), "Invariants failed at step 2194"

    # Operation 2195: insert
    tree[2682] = 'value_240076'
    reference[2682] = 'value_240076'
    assert tree.invariants(), "Invariants failed at step 2195"

    # Operation 2196: get
    assert tree.invariants(), "Invariants failed at step 2196"

    # Operation 2197: delete
    del tree[6854]
    del reference[6854]
    assert tree.invariants(), "Invariants failed at step 2197"

    # Operation 2198: get
    assert tree.invariants(), "Invariants failed at step 2198"

    # Operation 2199: insert
    tree[7540] = 'value_739561'
    reference[7540] = 'value_739561'
    assert tree.invariants(), "Invariants failed at step 2199"

    # Operation 2200: update
    tree[2682] = 'value_571725'
    reference[2682] = 'value_571725'
    assert tree.invariants(), "Invariants failed at step 2200"

    # Operation 2201: delete
    del tree[7540]
    del reference[7540]
    assert tree.invariants(), "Invariants failed at step 2201"

    # Operation 2202: delete
    del tree[8639]
    del reference[8639]
    assert tree.invariants(), "Invariants failed at step 2202"

    # Operation 2203: delete
    del tree[2682]
    del reference[2682]
    assert tree.invariants(), "Invariants failed at step 2203"

    # Operation 2204: insert
    tree[6618] = 'value_112609'
    reference[6618] = 'value_112609'
    assert tree.invariants(), "Invariants failed at step 2204"

    # Operation 2205: insert
    tree[7295] = 'value_779955'
    reference[7295] = 'value_779955'
    assert tree.invariants(), "Invariants failed at step 2205"

    # Operation 2206: get
    assert tree.invariants(), "Invariants failed at step 2206"

    # Operation 2207: insert
    tree[8268] = 'value_597978'
    reference[8268] = 'value_597978'
    assert tree.invariants(), "Invariants failed at step 2207"

    # Operation 2208: insert
    tree[5764] = 'value_315506'
    reference[5764] = 'value_315506'
    assert tree.invariants(), "Invariants failed at step 2208"

    # Operation 2209: delete
    del tree[6618]
    del reference[6618]
    assert tree.invariants(), "Invariants failed at step 2209"

    # Operation 2210: get
    assert tree.invariants(), "Invariants failed at step 2210"

    # Operation 2211: get
    assert tree.invariants(), "Invariants failed at step 2211"

    # Operation 2212: insert
    tree[5371] = 'value_462201'
    reference[5371] = 'value_462201'
    assert tree.invariants(), "Invariants failed at step 2212"

    # Operation 2213: delete
    del tree[5764]
    del reference[5764]
    assert tree.invariants(), "Invariants failed at step 2213"

    # Operation 2214: delete
    del tree[979]
    del reference[979]
    assert tree.invariants(), "Invariants failed at step 2214"

    # Operation 2215: update
    tree[7295] = 'value_914174'
    reference[7295] = 'value_914174'
    assert tree.invariants(), "Invariants failed at step 2215"

    # Operation 2216: batch_delete
    keys_to_delete = [6768, 5371, 7318]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2216"

    # Operation 2217: delete
    del tree[7295]
    del reference[7295]
    assert tree.invariants(), "Invariants failed at step 2217"

    # Operation 2218: insert
    tree[5743] = 'value_449471'
    reference[5743] = 'value_449471'
    assert tree.invariants(), "Invariants failed at step 2218"

    # Operation 2219: insert
    tree[231] = 'value_709682'
    reference[231] = 'value_709682'
    assert tree.invariants(), "Invariants failed at step 2219"

    # Operation 2220: update
    tree[5743] = 'value_460282'
    reference[5743] = 'value_460282'
    assert tree.invariants(), "Invariants failed at step 2220"

    # Operation 2221: delete
    del tree[2718]
    del reference[2718]
    assert tree.invariants(), "Invariants failed at step 2221"

    # Operation 2222: delete
    del tree[5743]
    del reference[5743]
    assert tree.invariants(), "Invariants failed at step 2222"

    # Operation 2223: insert
    tree[2285] = 'value_640764'
    reference[2285] = 'value_640764'
    assert tree.invariants(), "Invariants failed at step 2223"

    # Operation 2224: get
    assert tree.invariants(), "Invariants failed at step 2224"

    # Operation 2225: get
    assert tree.invariants(), "Invariants failed at step 2225"

    # Operation 2226: delete
    del tree[231]
    del reference[231]
    assert tree.invariants(), "Invariants failed at step 2226"

    # Operation 2227: get
    assert tree.invariants(), "Invariants failed at step 2227"

    # Operation 2228: get
    assert tree.invariants(), "Invariants failed at step 2228"

    # Operation 2229: get
    assert tree.invariants(), "Invariants failed at step 2229"

    # Operation 2230: update
    tree[2285] = 'value_950572'
    reference[2285] = 'value_950572'
    assert tree.invariants(), "Invariants failed at step 2230"

    # Operation 2231: get
    assert tree.invariants(), "Invariants failed at step 2231"

    # Operation 2232: insert
    tree[4951] = 'value_799892'
    reference[4951] = 'value_799892'
    assert tree.invariants(), "Invariants failed at step 2232"

    # Operation 2233: delete
    del tree[4951]
    del reference[4951]
    assert tree.invariants(), "Invariants failed at step 2233"

    # Operation 2234: delete
    del tree[2285]
    del reference[2285]
    assert tree.invariants(), "Invariants failed at step 2234"

    # Operation 2235: get
    assert tree.invariants(), "Invariants failed at step 2235"

    # Operation 2236: delete
    del tree[8268]
    del reference[8268]
    assert tree.invariants(), "Invariants failed at step 2236"

    # Operation 2237: get
    assert tree.invariants(), "Invariants failed at step 2237"

    # Operation 2238: insert
    tree[7773] = 'value_922203'
    reference[7773] = 'value_922203'
    assert tree.invariants(), "Invariants failed at step 2238"

    # Operation 2239: delete
    del tree[7773]
    del reference[7773]
    assert tree.invariants(), "Invariants failed at step 2239"

    # Operation 2240: insert
    tree[7491] = 'value_872803'
    reference[7491] = 'value_872803'
    assert tree.invariants(), "Invariants failed at step 2240"

    # Operation 2241: delete
    del tree[7491]
    del reference[7491]
    assert tree.invariants(), "Invariants failed at step 2241"

    # Operation 2242: insert
    tree[1597] = 'value_55782'
    reference[1597] = 'value_55782'
    assert tree.invariants(), "Invariants failed at step 2242"

    # Operation 2243: insert
    tree[940] = 'value_26799'
    reference[940] = 'value_26799'
    assert tree.invariants(), "Invariants failed at step 2243"

    # Operation 2244: insert
    tree[4210] = 'value_680228'
    reference[4210] = 'value_680228'
    assert tree.invariants(), "Invariants failed at step 2244"

    # Operation 2245: delete
    del tree[4210]
    del reference[4210]
    assert tree.invariants(), "Invariants failed at step 2245"

    # Operation 2246: delete
    del tree[940]
    del reference[940]
    assert tree.invariants(), "Invariants failed at step 2246"

    # Operation 2247: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2247"

    # Operation 2248: update
    tree[1597] = 'value_762947'
    reference[1597] = 'value_762947'
    assert tree.invariants(), "Invariants failed at step 2248"

    # Operation 2249: insert
    tree[4223] = 'value_277444'
    reference[4223] = 'value_277444'
    assert tree.invariants(), "Invariants failed at step 2249"

    # Operation 2250: insert
    tree[5712] = 'value_204177'
    reference[5712] = 'value_204177'
    assert tree.invariants(), "Invariants failed at step 2250"

    # Operation 2251: insert
    tree[635] = 'value_582186'
    reference[635] = 'value_582186'
    assert tree.invariants(), "Invariants failed at step 2251"

    # Operation 2252: delete
    del tree[635]
    del reference[635]
    assert tree.invariants(), "Invariants failed at step 2252"

    # Operation 2253: insert
    tree[1179] = 'value_724426'
    reference[1179] = 'value_724426'
    assert tree.invariants(), "Invariants failed at step 2253"

    # Operation 2254: delete
    del tree[1179]
    del reference[1179]
    assert tree.invariants(), "Invariants failed at step 2254"

    # Operation 2255: insert
    tree[2821] = 'value_217385'
    reference[2821] = 'value_217385'
    assert tree.invariants(), "Invariants failed at step 2255"

    # Operation 2256: insert
    tree[1709] = 'value_393216'
    reference[1709] = 'value_393216'
    assert tree.invariants(), "Invariants failed at step 2256"

    # Operation 2257: batch_delete
    keys_to_delete = [5712, 9500, 2821, 4223]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2257"

    # Operation 2258: delete
    del tree[1597]
    del reference[1597]
    assert tree.invariants(), "Invariants failed at step 2258"

    # Operation 2259: insert
    tree[4665] = 'value_298765'
    reference[4665] = 'value_298765'
    assert tree.invariants(), "Invariants failed at step 2259"

    # Operation 2260: update
    tree[4665] = 'value_118375'
    reference[4665] = 'value_118375'
    assert tree.invariants(), "Invariants failed at step 2260"

    # Operation 2261: insert
    tree[7250] = 'value_164543'
    reference[7250] = 'value_164543'
    assert tree.invariants(), "Invariants failed at step 2261"

    # Operation 2262: update
    tree[1709] = 'value_253820'
    reference[1709] = 'value_253820'
    assert tree.invariants(), "Invariants failed at step 2262"

    # Operation 2263: insert
    tree[597] = 'value_603929'
    reference[597] = 'value_603929'
    assert tree.invariants(), "Invariants failed at step 2263"

    # Operation 2264: insert
    tree[1771] = 'value_701692'
    reference[1771] = 'value_701692'
    assert tree.invariants(), "Invariants failed at step 2264"

    # Operation 2265: get
    assert tree.invariants(), "Invariants failed at step 2265"

    # Operation 2266: delete
    del tree[1709]
    del reference[1709]
    assert tree.invariants(), "Invariants failed at step 2266"

    # Operation 2267: update
    tree[1771] = 'value_663072'
    reference[1771] = 'value_663072'
    assert tree.invariants(), "Invariants failed at step 2267"

    # Operation 2268: get
    assert tree.invariants(), "Invariants failed at step 2268"

    # Operation 2269: insert
    tree[1392] = 'value_514148'
    reference[1392] = 'value_514148'
    assert tree.invariants(), "Invariants failed at step 2269"

    # Operation 2270: delete
    del tree[1392]
    del reference[1392]
    assert tree.invariants(), "Invariants failed at step 2270"

    # Operation 2271: delete
    del tree[4665]
    del reference[4665]
    assert tree.invariants(), "Invariants failed at step 2271"

    # Operation 2272: update
    tree[7250] = 'value_254214'
    reference[7250] = 'value_254214'
    assert tree.invariants(), "Invariants failed at step 2272"

    # Operation 2273: insert
    tree[8087] = 'value_158439'
    reference[8087] = 'value_158439'
    assert tree.invariants(), "Invariants failed at step 2273"

    # Operation 2274: delete
    del tree[7250]
    del reference[7250]
    assert tree.invariants(), "Invariants failed at step 2274"

    # Operation 2275: delete
    del tree[597]
    del reference[597]
    assert tree.invariants(), "Invariants failed at step 2275"

    # Operation 2276: delete
    del tree[1771]
    del reference[1771]
    assert tree.invariants(), "Invariants failed at step 2276"

    # Operation 2277: delete
    del tree[8087]
    del reference[8087]
    assert tree.invariants(), "Invariants failed at step 2277"

    # Operation 2278: get
    assert tree.invariants(), "Invariants failed at step 2278"

    # Operation 2279: insert
    tree[4373] = 'value_553241'
    reference[4373] = 'value_553241'
    assert tree.invariants(), "Invariants failed at step 2279"

    # Operation 2280: update
    tree[4373] = 'value_307441'
    reference[4373] = 'value_307441'
    assert tree.invariants(), "Invariants failed at step 2280"

    # Operation 2281: get
    assert tree.invariants(), "Invariants failed at step 2281"

    # Operation 2282: insert
    tree[4005] = 'value_716481'
    reference[4005] = 'value_716481'
    assert tree.invariants(), "Invariants failed at step 2282"

    # Operation 2283: delete
    del tree[4373]
    del reference[4373]
    assert tree.invariants(), "Invariants failed at step 2283"

    # Operation 2284: insert
    tree[7996] = 'value_27149'
    reference[7996] = 'value_27149'
    assert tree.invariants(), "Invariants failed at step 2284"

    # Operation 2285: delete
    del tree[4005]
    del reference[4005]
    assert tree.invariants(), "Invariants failed at step 2285"

    # Operation 2286: insert
    tree[7328] = 'value_936379'
    reference[7328] = 'value_936379'
    assert tree.invariants(), "Invariants failed at step 2286"

    # Operation 2287: delete
    del tree[7996]
    del reference[7996]
    assert tree.invariants(), "Invariants failed at step 2287"

    # Operation 2288: get
    assert tree.invariants(), "Invariants failed at step 2288"

    # Operation 2289: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2289"

    # Operation 2290: delete
    del tree[7328]
    del reference[7328]
    assert tree.invariants(), "Invariants failed at step 2290"

    # Operation 2291: insert
    tree[1673] = 'value_292477'
    reference[1673] = 'value_292477'
    assert tree.invariants(), "Invariants failed at step 2291"

    # Operation 2292: insert
    tree[4662] = 'value_911232'
    reference[4662] = 'value_911232'
    assert tree.invariants(), "Invariants failed at step 2292"

    # Operation 2293: insert
    tree[1740] = 'value_64262'
    reference[1740] = 'value_64262'
    assert tree.invariants(), "Invariants failed at step 2293"

    # Operation 2294: get
    assert tree.invariants(), "Invariants failed at step 2294"

    # Operation 2295: insert
    tree[606] = 'value_760289'
    reference[606] = 'value_760289'
    assert tree.invariants(), "Invariants failed at step 2295"

    # Operation 2296: delete
    del tree[1673]
    del reference[1673]
    assert tree.invariants(), "Invariants failed at step 2296"

    # Operation 2297: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2297"

    # Operation 2298: get
    assert tree.invariants(), "Invariants failed at step 2298"

    # Operation 2299: update
    tree[606] = 'value_276694'
    reference[606] = 'value_276694'
    assert tree.invariants(), "Invariants failed at step 2299"

    # Operation 2300: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2300"

    # Operation 2301: get
    assert tree.invariants(), "Invariants failed at step 2301"

    # Operation 2302: insert
    tree[6789] = 'value_812109'
    reference[6789] = 'value_812109'
    assert tree.invariants(), "Invariants failed at step 2302"

    # Operation 2303: get
    assert tree.invariants(), "Invariants failed at step 2303"

    # Operation 2304: insert
    tree[70] = 'value_448393'
    reference[70] = 'value_448393'
    assert tree.invariants(), "Invariants failed at step 2304"

    # Operation 2305: insert
    tree[3851] = 'value_867632'
    reference[3851] = 'value_867632'
    assert tree.invariants(), "Invariants failed at step 2305"

    # Operation 2306: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2306"

    # Operation 2307: insert
    tree[8537] = 'value_815871'
    reference[8537] = 'value_815871'
    assert tree.invariants(), "Invariants failed at step 2307"

    # Operation 2308: delete
    del tree[3851]
    del reference[3851]
    assert tree.invariants(), "Invariants failed at step 2308"

    # Operation 2309: delete
    del tree[6789]
    del reference[6789]
    assert tree.invariants(), "Invariants failed at step 2309"

    # Operation 2310: delete
    del tree[8537]
    del reference[8537]
    assert tree.invariants(), "Invariants failed at step 2310"

    # Operation 2311: insert
    tree[8191] = 'value_8179'
    reference[8191] = 'value_8179'
    assert tree.invariants(), "Invariants failed at step 2311"

    # Operation 2312: delete
    del tree[606]
    del reference[606]
    assert tree.invariants(), "Invariants failed at step 2312"

    # Operation 2313: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2313"

    # Operation 2314: update
    tree[8191] = 'value_434662'
    reference[8191] = 'value_434662'
    assert tree.invariants(), "Invariants failed at step 2314"

    # Operation 2315: delete
    del tree[4662]
    del reference[4662]
    assert tree.invariants(), "Invariants failed at step 2315"

    # Operation 2316: get
    assert tree.invariants(), "Invariants failed at step 2316"

    # Operation 2317: delete
    del tree[1740]
    del reference[1740]
    assert tree.invariants(), "Invariants failed at step 2317"

    # Operation 2318: insert
    tree[9984] = 'value_176827'
    reference[9984] = 'value_176827'
    assert tree.invariants(), "Invariants failed at step 2318"

    # Operation 2319: get
    assert tree.invariants(), "Invariants failed at step 2319"

    # Operation 2320: delete
    del tree[9984]
    del reference[9984]
    assert tree.invariants(), "Invariants failed at step 2320"

    # Operation 2321: get
    assert tree.invariants(), "Invariants failed at step 2321"

    # Operation 2322: insert
    tree[3033] = 'value_888124'
    reference[3033] = 'value_888124'
    assert tree.invariants(), "Invariants failed at step 2322"

    # Operation 2323: delete
    del tree[3033]
    del reference[3033]
    assert tree.invariants(), "Invariants failed at step 2323"

    # Operation 2324: delete
    del tree[70]
    del reference[70]
    assert tree.invariants(), "Invariants failed at step 2324"

    # Operation 2325: insert
    tree[4835] = 'value_505342'
    reference[4835] = 'value_505342'
    assert tree.invariants(), "Invariants failed at step 2325"

    # Operation 2326: get
    assert tree.invariants(), "Invariants failed at step 2326"

    # Operation 2327: update
    tree[8191] = 'value_79214'
    reference[8191] = 'value_79214'
    assert tree.invariants(), "Invariants failed at step 2327"

    # Operation 2328: insert
    tree[5128] = 'value_623064'
    reference[5128] = 'value_623064'
    assert tree.invariants(), "Invariants failed at step 2328"

    # Operation 2329: insert
    tree[7508] = 'value_828270'
    reference[7508] = 'value_828270'
    assert tree.invariants(), "Invariants failed at step 2329"

    # Operation 2330: insert
    tree[7965] = 'value_769148'
    reference[7965] = 'value_769148'
    assert tree.invariants(), "Invariants failed at step 2330"

    # Operation 2331: delete
    del tree[4835]
    del reference[4835]
    assert tree.invariants(), "Invariants failed at step 2331"

    # Operation 2332: get
    assert tree.invariants(), "Invariants failed at step 2332"

    # Operation 2333: update
    tree[5128] = 'value_624222'
    reference[5128] = 'value_624222'
    assert tree.invariants(), "Invariants failed at step 2333"

    # Operation 2334: insert
    tree[1106] = 'value_631710'
    reference[1106] = 'value_631710'
    assert tree.invariants(), "Invariants failed at step 2334"

    # Operation 2335: delete
    del tree[7508]
    del reference[7508]
    assert tree.invariants(), "Invariants failed at step 2335"

    # Operation 2336: get
    assert tree.invariants(), "Invariants failed at step 2336"

    # Operation 2337: insert
    tree[1038] = 'value_815024'
    reference[1038] = 'value_815024'
    assert tree.invariants(), "Invariants failed at step 2337"

    # Operation 2338: update
    tree[7965] = 'value_111080'
    reference[7965] = 'value_111080'
    assert tree.invariants(), "Invariants failed at step 2338"

    # Operation 2339: insert
    tree[7152] = 'value_745492'
    reference[7152] = 'value_745492'
    assert tree.invariants(), "Invariants failed at step 2339"

    # Operation 2340: insert
    tree[1668] = 'value_903962'
    reference[1668] = 'value_903962'
    assert tree.invariants(), "Invariants failed at step 2340"

    # Operation 2341: insert
    tree[4624] = 'value_916198'
    reference[4624] = 'value_916198'
    assert tree.invariants(), "Invariants failed at step 2341"

    # Operation 2342: delete
    del tree[5128]
    del reference[5128]
    assert tree.invariants(), "Invariants failed at step 2342"

    # Operation 2343: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2343"

    # Operation 2344: delete
    del tree[1106]
    del reference[1106]
    assert tree.invariants(), "Invariants failed at step 2344"

    # Operation 2345: insert
    tree[9792] = 'value_104373'
    reference[9792] = 'value_104373'
    assert tree.invariants(), "Invariants failed at step 2345"

    # Operation 2346: get
    assert tree.invariants(), "Invariants failed at step 2346"

    # Operation 2347: get
    assert tree.invariants(), "Invariants failed at step 2347"

    # Operation 2348: get
    assert tree.invariants(), "Invariants failed at step 2348"

    # Operation 2349: delete
    del tree[4624]
    del reference[4624]
    assert tree.invariants(), "Invariants failed at step 2349"

    # Operation 2350: batch_delete
    keys_to_delete = [1668, 825, 7354, 7965, 8191]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2350"

    # Operation 2351: insert
    tree[4039] = 'value_726418'
    reference[4039] = 'value_726418'
    assert tree.invariants(), "Invariants failed at step 2351"

    # Operation 2352: update
    tree[7152] = 'value_960383'
    reference[7152] = 'value_960383'
    assert tree.invariants(), "Invariants failed at step 2352"

    # Operation 2353: update
    tree[9792] = 'value_118592'
    reference[9792] = 'value_118592'
    assert tree.invariants(), "Invariants failed at step 2353"

    # Operation 2354: insert
    tree[6782] = 'value_389938'
    reference[6782] = 'value_389938'
    assert tree.invariants(), "Invariants failed at step 2354"

    # Operation 2355: batch_delete
    keys_to_delete = [7152, 5169, 9792, 1038]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2355"

    # Operation 2356: get
    assert tree.invariants(), "Invariants failed at step 2356"

    # Operation 2357: get
    assert tree.invariants(), "Invariants failed at step 2357"

    # Operation 2358: delete
    del tree[4039]
    del reference[4039]
    assert tree.invariants(), "Invariants failed at step 2358"

    # Operation 2359: insert
    tree[6824] = 'value_931534'
    reference[6824] = 'value_931534'
    assert tree.invariants(), "Invariants failed at step 2359"

    # Operation 2360: insert
    tree[9542] = 'value_467008'
    reference[9542] = 'value_467008'
    assert tree.invariants(), "Invariants failed at step 2360"

    # Operation 2361: delete
    del tree[6824]
    del reference[6824]
    assert tree.invariants(), "Invariants failed at step 2361"

    # Operation 2362: get
    assert tree.invariants(), "Invariants failed at step 2362"

    # Operation 2363: insert
    tree[2472] = 'value_179529'
    reference[2472] = 'value_179529'
    assert tree.invariants(), "Invariants failed at step 2363"

    # Operation 2364: delete
    del tree[9542]
    del reference[9542]
    assert tree.invariants(), "Invariants failed at step 2364"

    # Operation 2365: get
    assert tree.invariants(), "Invariants failed at step 2365"

    # Operation 2366: insert
    tree[2582] = 'value_203684'
    reference[2582] = 'value_203684'
    assert tree.invariants(), "Invariants failed at step 2366"

    # Operation 2367: delete
    del tree[6782]
    del reference[6782]
    assert tree.invariants(), "Invariants failed at step 2367"

    # Operation 2368: delete
    del tree[2582]
    del reference[2582]
    assert tree.invariants(), "Invariants failed at step 2368"

    # Operation 2369: insert
    tree[4111] = 'value_895992'
    reference[4111] = 'value_895992'
    assert tree.invariants(), "Invariants failed at step 2369"

    # Operation 2370: delete
    del tree[4111]
    del reference[4111]
    assert tree.invariants(), "Invariants failed at step 2370"

    # Operation 2371: delete
    del tree[2472]
    del reference[2472]
    assert tree.invariants(), "Invariants failed at step 2371"

    # Operation 2372: insert
    tree[7122] = 'value_43012'
    reference[7122] = 'value_43012'
    assert tree.invariants(), "Invariants failed at step 2372"

    # Operation 2373: delete
    del tree[7122]
    del reference[7122]
    assert tree.invariants(), "Invariants failed at step 2373"

    # Operation 2374: insert
    tree[917] = 'value_6161'
    reference[917] = 'value_6161'
    assert tree.invariants(), "Invariants failed at step 2374"

    # Operation 2375: delete
    del tree[917]
    del reference[917]
    assert tree.invariants(), "Invariants failed at step 2375"

    # Operation 2376: insert
    tree[4306] = 'value_339401'
    reference[4306] = 'value_339401'
    assert tree.invariants(), "Invariants failed at step 2376"

    # Operation 2377: get
    assert tree.invariants(), "Invariants failed at step 2377"

    # Operation 2378: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2378"

    # Operation 2379: delete
    del tree[4306]
    del reference[4306]
    assert tree.invariants(), "Invariants failed at step 2379"

    # Operation 2380: get
    assert tree.invariants(), "Invariants failed at step 2380"

    # Operation 2381: insert
    tree[9839] = 'value_309251'
    reference[9839] = 'value_309251'
    assert tree.invariants(), "Invariants failed at step 2381"

    # Operation 2382: insert
    tree[1399] = 'value_333418'
    reference[1399] = 'value_333418'
    assert tree.invariants(), "Invariants failed at step 2382"

    # Operation 2383: delete
    del tree[9839]
    del reference[9839]
    assert tree.invariants(), "Invariants failed at step 2383"

    # Operation 2384: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2384"

    # Operation 2385: delete
    del tree[1399]
    del reference[1399]
    assert tree.invariants(), "Invariants failed at step 2385"

    # Operation 2386: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2386"

    # Operation 2387: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2387"

    # Operation 2388: insert
    tree[7024] = 'value_372674'
    reference[7024] = 'value_372674'
    assert tree.invariants(), "Invariants failed at step 2388"

    # Operation 2389: delete
    del tree[7024]
    del reference[7024]
    assert tree.invariants(), "Invariants failed at step 2389"

    # Operation 2390: insert
    tree[9386] = 'value_939485'
    reference[9386] = 'value_939485'
    assert tree.invariants(), "Invariants failed at step 2390"

    # Operation 2391: get
    assert tree.invariants(), "Invariants failed at step 2391"

    # Operation 2392: delete
    del tree[9386]
    del reference[9386]
    assert tree.invariants(), "Invariants failed at step 2392"

    # Operation 2393: insert
    tree[8843] = 'value_329664'
    reference[8843] = 'value_329664'
    assert tree.invariants(), "Invariants failed at step 2393"

    # Operation 2394: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2394"

    # Operation 2395: update
    tree[8843] = 'value_573022'
    reference[8843] = 'value_573022'
    assert tree.invariants(), "Invariants failed at step 2395"

    # Operation 2396: delete
    del tree[8843]
    del reference[8843]
    assert tree.invariants(), "Invariants failed at step 2396"

    # Operation 2397: insert
    tree[5356] = 'value_849647'
    reference[5356] = 'value_849647'
    assert tree.invariants(), "Invariants failed at step 2397"

    # Operation 2398: get
    assert tree.invariants(), "Invariants failed at step 2398"

    # Operation 2399: insert
    tree[6789] = 'value_748253'
    reference[6789] = 'value_748253'
    assert tree.invariants(), "Invariants failed at step 2399"

    # Operation 2400: update
    tree[6789] = 'value_62659'
    reference[6789] = 'value_62659'
    assert tree.invariants(), "Invariants failed at step 2400"

    # Operation 2401: update
    tree[6789] = 'value_391858'
    reference[6789] = 'value_391858'
    assert tree.invariants(), "Invariants failed at step 2401"

    # Operation 2402: get
    assert tree.invariants(), "Invariants failed at step 2402"

    # Operation 2403: update
    tree[5356] = 'value_360811'
    reference[5356] = 'value_360811'
    assert tree.invariants(), "Invariants failed at step 2403"

    # Operation 2404: delete
    del tree[6789]
    del reference[6789]
    assert tree.invariants(), "Invariants failed at step 2404"

    # Operation 2405: insert
    tree[481] = 'value_81337'
    reference[481] = 'value_81337'
    assert tree.invariants(), "Invariants failed at step 2405"

    # Operation 2406: get
    assert tree.invariants(), "Invariants failed at step 2406"

    # Operation 2407: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2407"

    # Operation 2408: delete
    del tree[481]
    del reference[481]
    assert tree.invariants(), "Invariants failed at step 2408"

    # Operation 2409: delete
    del tree[5356]
    del reference[5356]
    assert tree.invariants(), "Invariants failed at step 2409"

    # Operation 2410: insert
    tree[4746] = 'value_833235'
    reference[4746] = 'value_833235'
    assert tree.invariants(), "Invariants failed at step 2410"

    # Operation 2411: get
    assert tree.invariants(), "Invariants failed at step 2411"

    # Operation 2412: get
    assert tree.invariants(), "Invariants failed at step 2412"

    # Operation 2413: delete
    del tree[4746]
    del reference[4746]
    assert tree.invariants(), "Invariants failed at step 2413"

    # Operation 2414: insert
    tree[8772] = 'value_187804'
    reference[8772] = 'value_187804'
    assert tree.invariants(), "Invariants failed at step 2414"

    # Operation 2415: get
    assert tree.invariants(), "Invariants failed at step 2415"

    # Operation 2416: get
    assert tree.invariants(), "Invariants failed at step 2416"

    # Operation 2417: insert
    tree[2139] = 'value_983567'
    reference[2139] = 'value_983567'
    assert tree.invariants(), "Invariants failed at step 2417"

    # Operation 2418: update
    tree[2139] = 'value_829506'
    reference[2139] = 'value_829506'
    assert tree.invariants(), "Invariants failed at step 2418"

    # Operation 2419: insert
    tree[9699] = 'value_254835'
    reference[9699] = 'value_254835'
    assert tree.invariants(), "Invariants failed at step 2419"

    # Operation 2420: insert
    tree[9184] = 'value_502744'
    reference[9184] = 'value_502744'
    assert tree.invariants(), "Invariants failed at step 2420"

    # Operation 2421: delete
    del tree[9699]
    del reference[9699]
    assert tree.invariants(), "Invariants failed at step 2421"

    # Operation 2422: get
    assert tree.invariants(), "Invariants failed at step 2422"

    # Operation 2423: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2423"

    # Operation 2424: update
    tree[9184] = 'value_709988'
    reference[9184] = 'value_709988'
    assert tree.invariants(), "Invariants failed at step 2424"

    # Operation 2425: get
    assert tree.invariants(), "Invariants failed at step 2425"

    # Operation 2426: get
    assert tree.invariants(), "Invariants failed at step 2426"

    # Operation 2427: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2427"

    # Operation 2428: insert
    tree[734] = 'value_5757'
    reference[734] = 'value_5757'
    assert tree.invariants(), "Invariants failed at step 2428"

    # Operation 2429: delete
    del tree[9184]
    del reference[9184]
    assert tree.invariants(), "Invariants failed at step 2429"

    # Operation 2430: delete
    del tree[8772]
    del reference[8772]
    assert tree.invariants(), "Invariants failed at step 2430"

    # Operation 2431: update
    tree[2139] = 'value_334961'
    reference[2139] = 'value_334961'
    assert tree.invariants(), "Invariants failed at step 2431"

    # Operation 2432: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2432"

    # Operation 2433: insert
    tree[3195] = 'value_951781'
    reference[3195] = 'value_951781'
    assert tree.invariants(), "Invariants failed at step 2433"

    # Operation 2434: update
    tree[734] = 'value_696500'
    reference[734] = 'value_696500'
    assert tree.invariants(), "Invariants failed at step 2434"

    # Operation 2435: update
    tree[3195] = 'value_361361'
    reference[3195] = 'value_361361'
    assert tree.invariants(), "Invariants failed at step 2435"

    # Operation 2436: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2436"

    # Operation 2437: delete
    del tree[3195]
    del reference[3195]
    assert tree.invariants(), "Invariants failed at step 2437"

    # Operation 2438: get
    assert tree.invariants(), "Invariants failed at step 2438"

    # Operation 2439: insert
    tree[3762] = 'value_337105'
    reference[3762] = 'value_337105'
    assert tree.invariants(), "Invariants failed at step 2439"

    # Operation 2440: insert
    tree[9133] = 'value_617107'
    reference[9133] = 'value_617107'
    assert tree.invariants(), "Invariants failed at step 2440"

    # Operation 2441: insert
    tree[6123] = 'value_238841'
    reference[6123] = 'value_238841'
    assert tree.invariants(), "Invariants failed at step 2441"

    # Operation 2442: insert
    tree[4083] = 'value_699094'
    reference[4083] = 'value_699094'
    assert tree.invariants(), "Invariants failed at step 2442"

    # Operation 2443: insert
    tree[1920] = 'value_494470'
    reference[1920] = 'value_494470'
    assert tree.invariants(), "Invariants failed at step 2443"

    # Operation 2444: insert
    tree[4143] = 'value_318830'
    reference[4143] = 'value_318830'
    assert tree.invariants(), "Invariants failed at step 2444"

    # Operation 2445: get
    assert tree.invariants(), "Invariants failed at step 2445"

    # Operation 2446: delete
    del tree[4083]
    del reference[4083]
    assert tree.invariants(), "Invariants failed at step 2446"

    # Operation 2447: insert
    tree[3095] = 'value_6179'
    reference[3095] = 'value_6179'
    assert tree.invariants(), "Invariants failed at step 2447"

    # Operation 2448: batch_delete
    keys_to_delete = [1920, 6343, 6123, 9436, 734]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2448"

    # Operation 2449: insert
    tree[3818] = 'value_899399'
    reference[3818] = 'value_899399'
    assert tree.invariants(), "Invariants failed at step 2449"

    # Operation 2450: insert
    tree[1290] = 'value_967258'
    reference[1290] = 'value_967258'
    assert tree.invariants(), "Invariants failed at step 2450"

    # Operation 2451: insert
    tree[4497] = 'value_211056'
    reference[4497] = 'value_211056'
    assert tree.invariants(), "Invariants failed at step 2451"

    # Operation 2452: insert
    tree[4069] = 'value_910154'
    reference[4069] = 'value_910154'
    assert tree.invariants(), "Invariants failed at step 2452"

    # Operation 2453: batch_delete
    keys_to_delete = [4069, 9933, 4143, 3095, 2139, 6909]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2453"

    # Operation 2454: get
    assert tree.invariants(), "Invariants failed at step 2454"

    # Operation 2455: insert
    tree[2771] = 'value_652470'
    reference[2771] = 'value_652470'
    assert tree.invariants(), "Invariants failed at step 2455"

    # Operation 2456: insert
    tree[5272] = 'value_20027'
    reference[5272] = 'value_20027'
    assert tree.invariants(), "Invariants failed at step 2456"

    # Operation 2457: delete
    del tree[3762]
    del reference[3762]
    assert tree.invariants(), "Invariants failed at step 2457"

    # Operation 2458: get
    assert tree.invariants(), "Invariants failed at step 2458"

    # Operation 2459: delete
    del tree[2771]
    del reference[2771]
    assert tree.invariants(), "Invariants failed at step 2459"

    # Operation 2460: update
    tree[4497] = 'value_139519'
    reference[4497] = 'value_139519'
    assert tree.invariants(), "Invariants failed at step 2460"

    # Operation 2461: delete
    del tree[4497]
    del reference[4497]
    assert tree.invariants(), "Invariants failed at step 2461"

    # Operation 2462: insert
    tree[1327] = 'value_26191'
    reference[1327] = 'value_26191'
    assert tree.invariants(), "Invariants failed at step 2462"

    # Operation 2463: batch_delete
    keys_to_delete = [5272, 1290, 5179, 9133]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2463"

    # Operation 2464: delete
    del tree[1327]
    del reference[1327]
    assert tree.invariants(), "Invariants failed at step 2464"

    # Operation 2465: insert
    tree[9204] = 'value_317746'
    reference[9204] = 'value_317746'
    assert tree.invariants(), "Invariants failed at step 2465"

    # Operation 2466: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2466"

    # Operation 2467: get
    assert tree.invariants(), "Invariants failed at step 2467"

    # Operation 2468: insert
    tree[1131] = 'value_235652'
    reference[1131] = 'value_235652'
    assert tree.invariants(), "Invariants failed at step 2468"

    # Operation 2469: delete
    del tree[1131]
    del reference[1131]
    assert tree.invariants(), "Invariants failed at step 2469"

    # Operation 2470: get
    assert tree.invariants(), "Invariants failed at step 2470"

    # Operation 2471: delete
    del tree[9204]
    del reference[9204]
    assert tree.invariants(), "Invariants failed at step 2471"

    # Operation 2472: insert
    tree[2132] = 'value_366016'
    reference[2132] = 'value_366016'
    assert tree.invariants(), "Invariants failed at step 2472"

    # Operation 2473: insert
    tree[3120] = 'value_243219'
    reference[3120] = 'value_243219'
    assert tree.invariants(), "Invariants failed at step 2473"

    # Operation 2474: insert
    tree[6012] = 'value_545871'
    reference[6012] = 'value_545871'
    assert tree.invariants(), "Invariants failed at step 2474"

    # Operation 2475: delete
    del tree[6012]
    del reference[6012]
    assert tree.invariants(), "Invariants failed at step 2475"

    # Operation 2476: insert
    tree[8862] = 'value_338226'
    reference[8862] = 'value_338226'
    assert tree.invariants(), "Invariants failed at step 2476"

    # Operation 2477: insert
    tree[5044] = 'value_6492'
    reference[5044] = 'value_6492'
    assert tree.invariants(), "Invariants failed at step 2477"

    # Operation 2478: insert
    tree[6266] = 'value_750661'
    reference[6266] = 'value_750661'
    assert tree.invariants(), "Invariants failed at step 2478"

    # Operation 2479: get
    assert tree.invariants(), "Invariants failed at step 2479"

    # Operation 2480: insert
    tree[6377] = 'value_69524'
    reference[6377] = 'value_69524'
    assert tree.invariants(), "Invariants failed at step 2480"

    # Operation 2481: update
    tree[2132] = 'value_583155'
    reference[2132] = 'value_583155'
    assert tree.invariants(), "Invariants failed at step 2481"

    # Operation 2482: get
    assert tree.invariants(), "Invariants failed at step 2482"

    # Operation 2483: batch_delete
    keys_to_delete = [3120, 2132, 759, 1436, 8862]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2483"

    # Operation 2484: get
    assert tree.invariants(), "Invariants failed at step 2484"

    # Operation 2485: update
    tree[3818] = 'value_300751'
    reference[3818] = 'value_300751'
    assert tree.invariants(), "Invariants failed at step 2485"

    # Operation 2486: get
    assert tree.invariants(), "Invariants failed at step 2486"

    # Operation 2487: get
    assert tree.invariants(), "Invariants failed at step 2487"

    # Operation 2488: delete
    del tree[6377]
    del reference[6377]
    assert tree.invariants(), "Invariants failed at step 2488"

    # Operation 2489: get
    assert tree.invariants(), "Invariants failed at step 2489"

    # Operation 2490: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2490"

    # Operation 2491: get
    assert tree.invariants(), "Invariants failed at step 2491"

    # Operation 2492: insert
    tree[5691] = 'value_494950'
    reference[5691] = 'value_494950'
    assert tree.invariants(), "Invariants failed at step 2492"

    # Operation 2493: get
    assert tree.invariants(), "Invariants failed at step 2493"

    # Operation 2494: insert
    tree[6391] = 'value_543350'
    reference[6391] = 'value_543350'
    assert tree.invariants(), "Invariants failed at step 2494"

    # Operation 2495: get
    assert tree.invariants(), "Invariants failed at step 2495"

    # Operation 2496: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2496"

    # Operation 2497: delete
    del tree[6266]
    del reference[6266]
    assert tree.invariants(), "Invariants failed at step 2497"

    # Operation 2498: delete
    del tree[6391]
    del reference[6391]
    assert tree.invariants(), "Invariants failed at step 2498"

    # Operation 2499: insert
    tree[8701] = 'value_260098'
    reference[8701] = 'value_260098'
    assert tree.invariants(), "Invariants failed at step 2499"

    # Operation 2500: update
    tree[5691] = 'value_501482'
    reference[5691] = 'value_501482'
    assert tree.invariants(), "Invariants failed at step 2500"

    # Operation 2501: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2501"

    # Operation 2502: update
    tree[5691] = 'value_438721'
    reference[5691] = 'value_438721'
    assert tree.invariants(), "Invariants failed at step 2502"

    # Operation 2503: get
    assert tree.invariants(), "Invariants failed at step 2503"

    # Operation 2504: insert
    tree[482] = 'value_456688'
    reference[482] = 'value_456688'
    assert tree.invariants(), "Invariants failed at step 2504"

    # Operation 2505: insert
    tree[9334] = 'value_125927'
    reference[9334] = 'value_125927'
    assert tree.invariants(), "Invariants failed at step 2505"

    # Operation 2506: insert
    tree[2018] = 'value_66098'
    reference[2018] = 'value_66098'
    assert tree.invariants(), "Invariants failed at step 2506"

    # Operation 2507: delete
    del tree[482]
    del reference[482]
    assert tree.invariants(), "Invariants failed at step 2507"

    # Operation 2508: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2508"

    # Operation 2509: delete
    del tree[5691]
    del reference[5691]
    assert tree.invariants(), "Invariants failed at step 2509"

    # Operation 2510: update
    tree[2018] = 'value_34427'
    reference[2018] = 'value_34427'
    assert tree.invariants(), "Invariants failed at step 2510"

    # Operation 2511: insert
    tree[6621] = 'value_52986'
    reference[6621] = 'value_52986'
    assert tree.invariants(), "Invariants failed at step 2511"

    # Operation 2512: get
    assert tree.invariants(), "Invariants failed at step 2512"

    # Operation 2513: delete
    del tree[6621]
    del reference[6621]
    assert tree.invariants(), "Invariants failed at step 2513"

    # Operation 2514: delete
    del tree[5044]
    del reference[5044]
    assert tree.invariants(), "Invariants failed at step 2514"

    # Operation 2515: delete
    del tree[2018]
    del reference[2018]
    assert tree.invariants(), "Invariants failed at step 2515"

    # Operation 2516: delete
    del tree[3818]
    del reference[3818]
    assert tree.invariants(), "Invariants failed at step 2516"

    # Operation 2517: insert
    tree[6298] = 'value_374968'
    reference[6298] = 'value_374968'
    assert tree.invariants(), "Invariants failed at step 2517"

    # Operation 2518: delete
    del tree[8701]
    del reference[8701]
    assert tree.invariants(), "Invariants failed at step 2518"

    # Operation 2519: get
    assert tree.invariants(), "Invariants failed at step 2519"

    # Operation 2520: update
    tree[6298] = 'value_862383'
    reference[6298] = 'value_862383'
    assert tree.invariants(), "Invariants failed at step 2520"

    # Operation 2521: insert
    tree[6715] = 'value_46665'
    reference[6715] = 'value_46665'
    assert tree.invariants(), "Invariants failed at step 2521"

    # Operation 2522: insert
    tree[7668] = 'value_766851'
    reference[7668] = 'value_766851'
    assert tree.invariants(), "Invariants failed at step 2522"

    # Operation 2523: insert
    tree[5027] = 'value_144272'
    reference[5027] = 'value_144272'
    assert tree.invariants(), "Invariants failed at step 2523"

    # Operation 2524: insert
    tree[2449] = 'value_766449'
    reference[2449] = 'value_766449'
    assert tree.invariants(), "Invariants failed at step 2524"

    # Operation 2525: delete
    del tree[2449]
    del reference[2449]
    assert tree.invariants(), "Invariants failed at step 2525"

    # Operation 2526: batch_delete
    keys_to_delete = [6715, 7668, 9334, 747]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2526"

    # Operation 2527: delete
    del tree[5027]
    del reference[5027]
    assert tree.invariants(), "Invariants failed at step 2527"

    # Operation 2528: get
    assert tree.invariants(), "Invariants failed at step 2528"

    # Operation 2529: get
    assert tree.invariants(), "Invariants failed at step 2529"

    # Operation 2530: get
    assert tree.invariants(), "Invariants failed at step 2530"

    # Operation 2531: get
    assert tree.invariants(), "Invariants failed at step 2531"

    # Operation 2532: get
    assert tree.invariants(), "Invariants failed at step 2532"

    # Operation 2533: get
    assert tree.invariants(), "Invariants failed at step 2533"

    # Operation 2534: delete
    del tree[6298]
    del reference[6298]
    assert tree.invariants(), "Invariants failed at step 2534"

    # Operation 2535: insert
    tree[8815] = 'value_511471'
    reference[8815] = 'value_511471'
    assert tree.invariants(), "Invariants failed at step 2535"

    # Operation 2536: get
    assert tree.invariants(), "Invariants failed at step 2536"

    # Operation 2537: delete
    del tree[8815]
    del reference[8815]
    assert tree.invariants(), "Invariants failed at step 2537"

    # Operation 2538: insert
    tree[1563] = 'value_81459'
    reference[1563] = 'value_81459'
    assert tree.invariants(), "Invariants failed at step 2538"

    # Operation 2539: get
    assert tree.invariants(), "Invariants failed at step 2539"

    # Operation 2540: get
    assert tree.invariants(), "Invariants failed at step 2540"

    # Operation 2541: get
    assert tree.invariants(), "Invariants failed at step 2541"

    # Operation 2542: get
    assert tree.invariants(), "Invariants failed at step 2542"

    # Operation 2543: get
    assert tree.invariants(), "Invariants failed at step 2543"

    # Operation 2544: get
    assert tree.invariants(), "Invariants failed at step 2544"

    # Operation 2545: insert
    tree[7047] = 'value_121915'
    reference[7047] = 'value_121915'
    assert tree.invariants(), "Invariants failed at step 2545"

    # Operation 2546: delete
    del tree[7047]
    del reference[7047]
    assert tree.invariants(), "Invariants failed at step 2546"

    # Operation 2547: get
    assert tree.invariants(), "Invariants failed at step 2547"

    # Operation 2548: delete
    del tree[1563]
    del reference[1563]
    assert tree.invariants(), "Invariants failed at step 2548"

    # Operation 2549: get
    assert tree.invariants(), "Invariants failed at step 2549"

    # Operation 2550: insert
    tree[2223] = 'value_271059'
    reference[2223] = 'value_271059'
    assert tree.invariants(), "Invariants failed at step 2550"

    # Operation 2551: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2551"

    # Operation 2552: get
    assert tree.invariants(), "Invariants failed at step 2552"

    # Operation 2553: insert
    tree[3177] = 'value_765858'
    reference[3177] = 'value_765858'
    assert tree.invariants(), "Invariants failed at step 2553"

    # Operation 2554: delete
    del tree[3177]
    del reference[3177]
    assert tree.invariants(), "Invariants failed at step 2554"

    # Operation 2555: delete
    del tree[2223]
    del reference[2223]
    assert tree.invariants(), "Invariants failed at step 2555"

    # Operation 2556: get
    assert tree.invariants(), "Invariants failed at step 2556"

    # Operation 2557: get
    assert tree.invariants(), "Invariants failed at step 2557"

    # Operation 2558: insert
    tree[1520] = 'value_563889'
    reference[1520] = 'value_563889'
    assert tree.invariants(), "Invariants failed at step 2558"

    # Operation 2559: get
    assert tree.invariants(), "Invariants failed at step 2559"

    # Operation 2560: get
    assert tree.invariants(), "Invariants failed at step 2560"

    # Operation 2561: delete
    del tree[1520]
    del reference[1520]
    assert tree.invariants(), "Invariants failed at step 2561"

    # Operation 2562: insert
    tree[3938] = 'value_338235'
    reference[3938] = 'value_338235'
    assert tree.invariants(), "Invariants failed at step 2562"

    # Operation 2563: insert
    tree[9426] = 'value_49543'
    reference[9426] = 'value_49543'
    assert tree.invariants(), "Invariants failed at step 2563"

    # Operation 2564: delete
    del tree[3938]
    del reference[3938]
    assert tree.invariants(), "Invariants failed at step 2564"

    # Operation 2565: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2565"

    # Operation 2566: delete
    del tree[9426]
    del reference[9426]
    assert tree.invariants(), "Invariants failed at step 2566"

    # Operation 2567: get
    assert tree.invariants(), "Invariants failed at step 2567"

    # Operation 2568: get
    assert tree.invariants(), "Invariants failed at step 2568"

    # Operation 2569: insert
    tree[8375] = 'value_945661'
    reference[8375] = 'value_945661'
    assert tree.invariants(), "Invariants failed at step 2569"

    # Operation 2570: insert
    tree[770] = 'value_470681'
    reference[770] = 'value_470681'
    assert tree.invariants(), "Invariants failed at step 2570"

    # Operation 2571: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2571"

    # Operation 2572: delete
    del tree[8375]
    del reference[8375]
    assert tree.invariants(), "Invariants failed at step 2572"

    # Operation 2573: insert
    tree[2363] = 'value_868431'
    reference[2363] = 'value_868431'
    assert tree.invariants(), "Invariants failed at step 2573"

    # Operation 2574: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2574"

    # Operation 2575: delete
    del tree[770]
    del reference[770]
    assert tree.invariants(), "Invariants failed at step 2575"

    # Operation 2576: delete
    del tree[2363]
    del reference[2363]
    assert tree.invariants(), "Invariants failed at step 2576"

    # Operation 2577: insert
    tree[4383] = 'value_420280'
    reference[4383] = 'value_420280'
    assert tree.invariants(), "Invariants failed at step 2577"

    # Operation 2578: get
    assert tree.invariants(), "Invariants failed at step 2578"

    # Operation 2579: insert
    tree[4687] = 'value_755912'
    reference[4687] = 'value_755912'
    assert tree.invariants(), "Invariants failed at step 2579"

    # Operation 2580: get
    assert tree.invariants(), "Invariants failed at step 2580"

    # Operation 2581: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2581"

    # Operation 2582: insert
    tree[253] = 'value_520578'
    reference[253] = 'value_520578'
    assert tree.invariants(), "Invariants failed at step 2582"

    # Operation 2583: get
    assert tree.invariants(), "Invariants failed at step 2583"

    # Operation 2584: update
    tree[253] = 'value_224149'
    reference[253] = 'value_224149'
    assert tree.invariants(), "Invariants failed at step 2584"

    # Operation 2585: delete
    del tree[4383]
    del reference[4383]
    assert tree.invariants(), "Invariants failed at step 2585"

    # Operation 2586: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2586"

    # Operation 2587: delete
    del tree[4687]
    del reference[4687]
    assert tree.invariants(), "Invariants failed at step 2587"

    # Operation 2588: delete
    del tree[253]
    del reference[253]
    assert tree.invariants(), "Invariants failed at step 2588"

    # Operation 2589: get
    assert tree.invariants(), "Invariants failed at step 2589"

    # Operation 2590: insert
    tree[2301] = 'value_742124'
    reference[2301] = 'value_742124'
    assert tree.invariants(), "Invariants failed at step 2590"

    # Operation 2591: update
    tree[2301] = 'value_960134'
    reference[2301] = 'value_960134'
    assert tree.invariants(), "Invariants failed at step 2591"

    # Operation 2592: insert
    tree[5364] = 'value_347582'
    reference[5364] = 'value_347582'
    assert tree.invariants(), "Invariants failed at step 2592"

    # Operation 2593: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2593"

    # Operation 2594: insert
    tree[8265] = 'value_668584'
    reference[8265] = 'value_668584'
    assert tree.invariants(), "Invariants failed at step 2594"

    # Operation 2595: delete
    del tree[5364]
    del reference[5364]
    assert tree.invariants(), "Invariants failed at step 2595"

    # Operation 2596: get
    assert tree.invariants(), "Invariants failed at step 2596"

    # Operation 2597: insert
    tree[8679] = 'value_307709'
    reference[8679] = 'value_307709'
    assert tree.invariants(), "Invariants failed at step 2597"

    # Operation 2598: insert
    tree[518] = 'value_834205'
    reference[518] = 'value_834205'
    assert tree.invariants(), "Invariants failed at step 2598"

    # Operation 2599: insert
    tree[7149] = 'value_427069'
    reference[7149] = 'value_427069'
    assert tree.invariants(), "Invariants failed at step 2599"

    # Operation 2600: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2600"

    # Operation 2601: update
    tree[8265] = 'value_489529'
    reference[8265] = 'value_489529'
    assert tree.invariants(), "Invariants failed at step 2601"

    # Operation 2602: insert
    tree[9169] = 'value_693512'
    reference[9169] = 'value_693512'
    assert tree.invariants(), "Invariants failed at step 2602"

    # Operation 2603: get
    assert tree.invariants(), "Invariants failed at step 2603"

    # Operation 2604: get
    assert tree.invariants(), "Invariants failed at step 2604"

    # Operation 2605: insert
    tree[1086] = 'value_292927'
    reference[1086] = 'value_292927'
    assert tree.invariants(), "Invariants failed at step 2605"

    # Operation 2606: update
    tree[8679] = 'value_239038'
    reference[8679] = 'value_239038'
    assert tree.invariants(), "Invariants failed at step 2606"

    # Operation 2607: batch_delete
    keys_to_delete = [5831, 8679, 9169, 6971, 1086]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2607"

    # Operation 2608: insert
    tree[5275] = 'value_143042'
    reference[5275] = 'value_143042'
    assert tree.invariants(), "Invariants failed at step 2608"

    # Operation 2609: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2609"

    # Operation 2610: update
    tree[2301] = 'value_564036'
    reference[2301] = 'value_564036'
    assert tree.invariants(), "Invariants failed at step 2610"

    # Operation 2611: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2611"

    # Operation 2612: get
    assert tree.invariants(), "Invariants failed at step 2612"

    # Operation 2613: delete
    del tree[5275]
    del reference[5275]
    assert tree.invariants(), "Invariants failed at step 2613"

    # Operation 2614: update
    tree[7149] = 'value_786676'
    reference[7149] = 'value_786676'
    assert tree.invariants(), "Invariants failed at step 2614"

    # Operation 2615: update
    tree[518] = 'value_144764'
    reference[518] = 'value_144764'
    assert tree.invariants(), "Invariants failed at step 2615"

    # Operation 2616: update
    tree[8265] = 'value_913443'
    reference[8265] = 'value_913443'
    assert tree.invariants(), "Invariants failed at step 2616"

    # Operation 2617: delete
    del tree[8265]
    del reference[8265]
    assert tree.invariants(), "Invariants failed at step 2617"

    # Operation 2618: get
    assert tree.invariants(), "Invariants failed at step 2618"

    # Operation 2619: update
    tree[2301] = 'value_878431'
    reference[2301] = 'value_878431'
    assert tree.invariants(), "Invariants failed at step 2619"

    # Operation 2620: delete
    del tree[7149]
    del reference[7149]
    assert tree.invariants(), "Invariants failed at step 2620"

    # Operation 2621: insert
    tree[2024] = 'value_313217'
    reference[2024] = 'value_313217'
    assert tree.invariants(), "Invariants failed at step 2621"

    # Operation 2622: delete
    del tree[2301]
    del reference[2301]
    assert tree.invariants(), "Invariants failed at step 2622"

    # Operation 2623: insert
    tree[9794] = 'value_920831'
    reference[9794] = 'value_920831'
    assert tree.invariants(), "Invariants failed at step 2623"

    # Operation 2624: insert
    tree[7938] = 'value_765546'
    reference[7938] = 'value_765546'
    assert tree.invariants(), "Invariants failed at step 2624"

    # Operation 2625: insert
    tree[3814] = 'value_278884'
    reference[3814] = 'value_278884'
    assert tree.invariants(), "Invariants failed at step 2625"

    # Operation 2626: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2626"

    # Operation 2627: batch_delete
    keys_to_delete = [2024, 8216, 9794, 8452]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2627"

    # Operation 2628: insert
    tree[1300] = 'value_696474'
    reference[1300] = 'value_696474'
    assert tree.invariants(), "Invariants failed at step 2628"

    # Operation 2629: get
    assert tree.invariants(), "Invariants failed at step 2629"

    # Operation 2630: delete
    del tree[518]
    del reference[518]
    assert tree.invariants(), "Invariants failed at step 2630"

    # Operation 2631: get
    assert tree.invariants(), "Invariants failed at step 2631"

    # Operation 2632: get
    assert tree.invariants(), "Invariants failed at step 2632"

    # Operation 2633: delete
    del tree[7938]
    del reference[7938]
    assert tree.invariants(), "Invariants failed at step 2633"

    # Operation 2634: get
    assert tree.invariants(), "Invariants failed at step 2634"

    # Operation 2635: get
    assert tree.invariants(), "Invariants failed at step 2635"

    # Operation 2636: insert
    tree[4587] = 'value_144068'
    reference[4587] = 'value_144068'
    assert tree.invariants(), "Invariants failed at step 2636"

    # Operation 2637: delete
    del tree[3814]
    del reference[3814]
    assert tree.invariants(), "Invariants failed at step 2637"

    # Operation 2638: delete
    del tree[1300]
    del reference[1300]
    assert tree.invariants(), "Invariants failed at step 2638"

    # Operation 2639: get
    assert tree.invariants(), "Invariants failed at step 2639"

    # Operation 2640: delete
    del tree[4587]
    del reference[4587]
    assert tree.invariants(), "Invariants failed at step 2640"

    # Operation 2641: insert
    tree[2699] = 'value_931818'
    reference[2699] = 'value_931818'
    assert tree.invariants(), "Invariants failed at step 2641"

    # Operation 2642: update
    tree[2699] = 'value_980819'
    reference[2699] = 'value_980819'
    assert tree.invariants(), "Invariants failed at step 2642"

    # Operation 2643: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2643"

    # Operation 2644: get
    assert tree.invariants(), "Invariants failed at step 2644"

    # Operation 2645: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2645"

    # Operation 2646: get
    assert tree.invariants(), "Invariants failed at step 2646"

    # Operation 2647: insert
    tree[5371] = 'value_4634'
    reference[5371] = 'value_4634'
    assert tree.invariants(), "Invariants failed at step 2647"

    # Operation 2648: insert
    tree[6854] = 'value_54361'
    reference[6854] = 'value_54361'
    assert tree.invariants(), "Invariants failed at step 2648"

    # Operation 2649: update
    tree[5371] = 'value_317569'
    reference[5371] = 'value_317569'
    assert tree.invariants(), "Invariants failed at step 2649"

    # Operation 2650: delete
    del tree[2699]
    del reference[2699]
    assert tree.invariants(), "Invariants failed at step 2650"

    # Operation 2651: get
    assert tree.invariants(), "Invariants failed at step 2651"

    # Operation 2652: delete
    del tree[5371]
    del reference[5371]
    assert tree.invariants(), "Invariants failed at step 2652"

    # Operation 2653: delete
    del tree[6854]
    del reference[6854]
    assert tree.invariants(), "Invariants failed at step 2653"

    # Operation 2654: get
    assert tree.invariants(), "Invariants failed at step 2654"

    # Operation 2655: insert
    tree[4341] = 'value_310272'
    reference[4341] = 'value_310272'
    assert tree.invariants(), "Invariants failed at step 2655"

    # Operation 2656: update
    tree[4341] = 'value_564130'
    reference[4341] = 'value_564130'
    assert tree.invariants(), "Invariants failed at step 2656"

    # Operation 2657: insert
    tree[168] = 'value_976649'
    reference[168] = 'value_976649'
    assert tree.invariants(), "Invariants failed at step 2657"

    # Operation 2658: delete
    del tree[168]
    del reference[168]
    assert tree.invariants(), "Invariants failed at step 2658"

    # Operation 2659: update
    tree[4341] = 'value_899958'
    reference[4341] = 'value_899958'
    assert tree.invariants(), "Invariants failed at step 2659"

    # Operation 2660: insert
    tree[9916] = 'value_981943'
    reference[9916] = 'value_981943'
    assert tree.invariants(), "Invariants failed at step 2660"

    # Operation 2661: get
    assert tree.invariants(), "Invariants failed at step 2661"

    # Operation 2662: delete
    del tree[9916]
    del reference[9916]
    assert tree.invariants(), "Invariants failed at step 2662"

    # Operation 2663: update
    tree[4341] = 'value_312422'
    reference[4341] = 'value_312422'
    assert tree.invariants(), "Invariants failed at step 2663"

    # Operation 2664: delete
    del tree[4341]
    del reference[4341]
    assert tree.invariants(), "Invariants failed at step 2664"

    # Operation 2665: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2665"

    # Operation 2666: get
    assert tree.invariants(), "Invariants failed at step 2666"

    # Operation 2667: get
    assert tree.invariants(), "Invariants failed at step 2667"

    # Operation 2668: get
    assert tree.invariants(), "Invariants failed at step 2668"

    # Operation 2669: get
    assert tree.invariants(), "Invariants failed at step 2669"

    # Operation 2670: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2670"

    # Operation 2671: insert
    tree[4338] = 'value_868581'
    reference[4338] = 'value_868581'
    assert tree.invariants(), "Invariants failed at step 2671"

    # Operation 2672: insert
    tree[3265] = 'value_314494'
    reference[3265] = 'value_314494'
    assert tree.invariants(), "Invariants failed at step 2672"

    # Operation 2673: delete
    del tree[4338]
    del reference[4338]
    assert tree.invariants(), "Invariants failed at step 2673"

    # Operation 2674: insert
    tree[3139] = 'value_719474'
    reference[3139] = 'value_719474'
    assert tree.invariants(), "Invariants failed at step 2674"

    # Operation 2675: delete
    del tree[3139]
    del reference[3139]
    assert tree.invariants(), "Invariants failed at step 2675"

    # Operation 2676: get
    assert tree.invariants(), "Invariants failed at step 2676"

    # Operation 2677: delete
    del tree[3265]
    del reference[3265]
    assert tree.invariants(), "Invariants failed at step 2677"

    # Operation 2678: get
    assert tree.invariants(), "Invariants failed at step 2678"

    # Operation 2679: get
    assert tree.invariants(), "Invariants failed at step 2679"

    # Operation 2680: get
    assert tree.invariants(), "Invariants failed at step 2680"

    # Operation 2681: insert
    tree[6632] = 'value_875456'
    reference[6632] = 'value_875456'
    assert tree.invariants(), "Invariants failed at step 2681"

    # Operation 2682: delete
    del tree[6632]
    del reference[6632]
    assert tree.invariants(), "Invariants failed at step 2682"

    # Operation 2683: insert
    tree[4596] = 'value_754731'
    reference[4596] = 'value_754731'
    assert tree.invariants(), "Invariants failed at step 2683"

    # Operation 2684: update
    tree[4596] = 'value_315863'
    reference[4596] = 'value_315863'
    assert tree.invariants(), "Invariants failed at step 2684"

    # Operation 2685: insert
    tree[8241] = 'value_688658'
    reference[8241] = 'value_688658'
    assert tree.invariants(), "Invariants failed at step 2685"

    # Operation 2686: update
    tree[4596] = 'value_845697'
    reference[4596] = 'value_845697'
    assert tree.invariants(), "Invariants failed at step 2686"

    # Operation 2687: delete
    del tree[4596]
    del reference[4596]
    assert tree.invariants(), "Invariants failed at step 2687"

    # Operation 2688: get
    assert tree.invariants(), "Invariants failed at step 2688"

    # Operation 2689: get
    assert tree.invariants(), "Invariants failed at step 2689"

    # Operation 2690: update
    tree[8241] = 'value_310215'
    reference[8241] = 'value_310215'
    assert tree.invariants(), "Invariants failed at step 2690"

    # Operation 2691: delete
    del tree[8241]
    del reference[8241]
    assert tree.invariants(), "Invariants failed at step 2691"

    # Operation 2692: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2692"

    # Operation 2693: get
    assert tree.invariants(), "Invariants failed at step 2693"

    # Operation 2694: insert
    tree[8098] = 'value_215644'
    reference[8098] = 'value_215644'
    assert tree.invariants(), "Invariants failed at step 2694"

    # Operation 2695: insert
    tree[2012] = 'value_985386'
    reference[2012] = 'value_985386'
    assert tree.invariants(), "Invariants failed at step 2695"

    # Operation 2696: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2696"

    # Operation 2697: get
    assert tree.invariants(), "Invariants failed at step 2697"

    # Operation 2698: insert
    tree[6169] = 'value_148219'
    reference[6169] = 'value_148219'
    assert tree.invariants(), "Invariants failed at step 2698"

    # Operation 2699: update
    tree[6169] = 'value_428280'
    reference[6169] = 'value_428280'
    assert tree.invariants(), "Invariants failed at step 2699"

    # Operation 2700: insert
    tree[2798] = 'value_341881'
    reference[2798] = 'value_341881'
    assert tree.invariants(), "Invariants failed at step 2700"

    # Operation 2701: insert
    tree[946] = 'value_119007'
    reference[946] = 'value_119007'
    assert tree.invariants(), "Invariants failed at step 2701"

    # Operation 2702: batch_delete
    keys_to_delete = [946, 2225, 8098, 9357]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2702"

    # Operation 2703: insert
    tree[6627] = 'value_424721'
    reference[6627] = 'value_424721'
    assert tree.invariants(), "Invariants failed at step 2703"

    # Operation 2704: get
    assert tree.invariants(), "Invariants failed at step 2704"

    # Operation 2705: get
    assert tree.invariants(), "Invariants failed at step 2705"

    # Operation 2706: insert
    tree[153] = 'value_706895'
    reference[153] = 'value_706895'
    assert tree.invariants(), "Invariants failed at step 2706"

    # Operation 2707: update
    tree[6169] = 'value_406036'
    reference[6169] = 'value_406036'
    assert tree.invariants(), "Invariants failed at step 2707"

    # Operation 2708: get
    assert tree.invariants(), "Invariants failed at step 2708"

    # Operation 2709: delete
    del tree[6627]
    del reference[6627]
    assert tree.invariants(), "Invariants failed at step 2709"

    # Operation 2710: delete
    del tree[153]
    del reference[153]
    assert tree.invariants(), "Invariants failed at step 2710"

    # Operation 2711: insert
    tree[3610] = 'value_23570'
    reference[3610] = 'value_23570'
    assert tree.invariants(), "Invariants failed at step 2711"

    # Operation 2712: get
    assert tree.invariants(), "Invariants failed at step 2712"

    # Operation 2713: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2713"

    # Operation 2714: insert
    tree[6822] = 'value_330665'
    reference[6822] = 'value_330665'
    assert tree.invariants(), "Invariants failed at step 2714"

    # Operation 2715: delete
    del tree[6822]
    del reference[6822]
    assert tree.invariants(), "Invariants failed at step 2715"

    # Operation 2716: insert
    tree[8348] = 'value_331627'
    reference[8348] = 'value_331627'
    assert tree.invariants(), "Invariants failed at step 2716"

    # Operation 2717: get
    assert tree.invariants(), "Invariants failed at step 2717"

    # Operation 2718: insert
    tree[8085] = 'value_786334'
    reference[8085] = 'value_786334'
    assert tree.invariants(), "Invariants failed at step 2718"

    # Operation 2719: get
    assert tree.invariants(), "Invariants failed at step 2719"

    # Operation 2720: get
    assert tree.invariants(), "Invariants failed at step 2720"

    # Operation 2721: delete
    del tree[8348]
    del reference[8348]
    assert tree.invariants(), "Invariants failed at step 2721"

    # Operation 2722: batch_delete
    keys_to_delete = [9069, 4051, 2012, 8085]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2722"

    # Operation 2723: get
    assert tree.invariants(), "Invariants failed at step 2723"

    # Operation 2724: get
    assert tree.invariants(), "Invariants failed at step 2724"

    # Operation 2725: insert
    tree[9829] = 'value_341314'
    reference[9829] = 'value_341314'
    assert tree.invariants(), "Invariants failed at step 2725"

    # Operation 2726: update
    tree[6169] = 'value_765657'
    reference[6169] = 'value_765657'
    assert tree.invariants(), "Invariants failed at step 2726"

    # Operation 2727: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2727"

    # Operation 2728: delete
    del tree[9829]
    del reference[9829]
    assert tree.invariants(), "Invariants failed at step 2728"

    # Operation 2729: get
    assert tree.invariants(), "Invariants failed at step 2729"

    # Operation 2730: get
    assert tree.invariants(), "Invariants failed at step 2730"

    # Operation 2731: delete
    del tree[3610]
    del reference[3610]
    assert tree.invariants(), "Invariants failed at step 2731"

    # Operation 2732: insert
    tree[5912] = 'value_486691'
    reference[5912] = 'value_486691'
    assert tree.invariants(), "Invariants failed at step 2732"

    # Operation 2733: delete
    del tree[6169]
    del reference[6169]
    assert tree.invariants(), "Invariants failed at step 2733"

    # Operation 2734: delete
    del tree[5912]
    del reference[5912]
    assert tree.invariants(), "Invariants failed at step 2734"

    # Operation 2735: delete
    del tree[2798]
    del reference[2798]
    assert tree.invariants(), "Invariants failed at step 2735"

    # Operation 2736: insert
    tree[6112] = 'value_259201'
    reference[6112] = 'value_259201'
    assert tree.invariants(), "Invariants failed at step 2736"

    # Operation 2737: delete
    del tree[6112]
    del reference[6112]
    assert tree.invariants(), "Invariants failed at step 2737"

    # Operation 2738: insert
    tree[7720] = 'value_593460'
    reference[7720] = 'value_593460'
    assert tree.invariants(), "Invariants failed at step 2738"

    # Operation 2739: insert
    tree[8777] = 'value_824206'
    reference[8777] = 'value_824206'
    assert tree.invariants(), "Invariants failed at step 2739"

    # Operation 2740: get
    assert tree.invariants(), "Invariants failed at step 2740"

    # Operation 2741: insert
    tree[6747] = 'value_413352'
    reference[6747] = 'value_413352'
    assert tree.invariants(), "Invariants failed at step 2741"

    # Operation 2742: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2742"

    # Operation 2743: get
    assert tree.invariants(), "Invariants failed at step 2743"

    # Operation 2744: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2744"

    # Operation 2745: delete
    del tree[7720]
    del reference[7720]
    assert tree.invariants(), "Invariants failed at step 2745"

    # Operation 2746: get
    assert tree.invariants(), "Invariants failed at step 2746"

    # Operation 2747: insert
    tree[6776] = 'value_329140'
    reference[6776] = 'value_329140'
    assert tree.invariants(), "Invariants failed at step 2747"

    # Operation 2748: insert
    tree[828] = 'value_744953'
    reference[828] = 'value_744953'
    assert tree.invariants(), "Invariants failed at step 2748"

    # Operation 2749: get
    assert tree.invariants(), "Invariants failed at step 2749"

    # Operation 2750: insert
    tree[3914] = 'value_923646'
    reference[3914] = 'value_923646'
    assert tree.invariants(), "Invariants failed at step 2750"

    # Operation 2751: insert
    tree[4704] = 'value_511479'
    reference[4704] = 'value_511479'
    assert tree.invariants(), "Invariants failed at step 2751"

    # Operation 2752: delete
    del tree[6776]
    del reference[6776]
    assert tree.invariants(), "Invariants failed at step 2752"

    # Operation 2753: delete
    del tree[3914]
    del reference[3914]
    assert tree.invariants(), "Invariants failed at step 2753"

    # Operation 2754: get
    assert tree.invariants(), "Invariants failed at step 2754"

    # Operation 2755: insert
    tree[3770] = 'value_177701'
    reference[3770] = 'value_177701'
    assert tree.invariants(), "Invariants failed at step 2755"

    # Operation 2756: get
    assert tree.invariants(), "Invariants failed at step 2756"

    # Operation 2757: get
    assert tree.invariants(), "Invariants failed at step 2757"

    # Operation 2758: insert
    tree[6164] = 'value_955319'
    reference[6164] = 'value_955319'
    assert tree.invariants(), "Invariants failed at step 2758"

    # Operation 2759: batch_delete
    keys_to_delete = [4704, 5700, 3770, 2010, 828]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2759"

    # Operation 2760: delete
    del tree[6747]
    del reference[6747]
    assert tree.invariants(), "Invariants failed at step 2760"

    # Operation 2761: get
    assert tree.invariants(), "Invariants failed at step 2761"

    # Operation 2762: insert
    tree[2594] = 'value_228524'
    reference[2594] = 'value_228524'
    assert tree.invariants(), "Invariants failed at step 2762"

    # Operation 2763: update
    tree[8777] = 'value_596211'
    reference[8777] = 'value_596211'
    assert tree.invariants(), "Invariants failed at step 2763"

    # Operation 2764: update
    tree[8777] = 'value_534899'
    reference[8777] = 'value_534899'
    assert tree.invariants(), "Invariants failed at step 2764"

    # Operation 2765: update
    tree[2594] = 'value_722223'
    reference[2594] = 'value_722223'
    assert tree.invariants(), "Invariants failed at step 2765"

    # Operation 2766: insert
    tree[7677] = 'value_24789'
    reference[7677] = 'value_24789'
    assert tree.invariants(), "Invariants failed at step 2766"

    # Operation 2767: insert
    tree[8118] = 'value_940271'
    reference[8118] = 'value_940271'
    assert tree.invariants(), "Invariants failed at step 2767"

    # Operation 2768: insert
    tree[6095] = 'value_406006'
    reference[6095] = 'value_406006'
    assert tree.invariants(), "Invariants failed at step 2768"

    # Operation 2769: insert
    tree[5145] = 'value_851607'
    reference[5145] = 'value_851607'
    assert tree.invariants(), "Invariants failed at step 2769"

    # Operation 2770: batch_delete
    keys_to_delete = [4744, 2594, 6164, 6095]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2770"

    # Operation 2771: delete
    del tree[8777]
    del reference[8777]
    assert tree.invariants(), "Invariants failed at step 2771"

    # Operation 2772: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2772"

    # Operation 2773: delete
    del tree[5145]
    del reference[5145]
    assert tree.invariants(), "Invariants failed at step 2773"

    # Operation 2774: insert
    tree[242] = 'value_675104'
    reference[242] = 'value_675104'
    assert tree.invariants(), "Invariants failed at step 2774"

    # Operation 2775: delete
    del tree[7677]
    del reference[7677]
    assert tree.invariants(), "Invariants failed at step 2775"

    # Operation 2776: insert
    tree[4349] = 'value_337073'
    reference[4349] = 'value_337073'
    assert tree.invariants(), "Invariants failed at step 2776"

    # Operation 2777: insert
    tree[5761] = 'value_808541'
    reference[5761] = 'value_808541'
    assert tree.invariants(), "Invariants failed at step 2777"

    # Operation 2778: insert
    tree[2654] = 'value_716126'
    reference[2654] = 'value_716126'
    assert tree.invariants(), "Invariants failed at step 2778"

    # Operation 2779: get
    assert tree.invariants(), "Invariants failed at step 2779"

    # Operation 2780: insert
    tree[4613] = 'value_679597'
    reference[4613] = 'value_679597'
    assert tree.invariants(), "Invariants failed at step 2780"

    # Operation 2781: insert
    tree[4573] = 'value_368581'
    reference[4573] = 'value_368581'
    assert tree.invariants(), "Invariants failed at step 2781"

    # Operation 2782: insert
    tree[1335] = 'value_499649'
    reference[1335] = 'value_499649'
    assert tree.invariants(), "Invariants failed at step 2782"

    # Operation 2783: insert
    tree[3590] = 'value_602883'
    reference[3590] = 'value_602883'
    assert tree.invariants(), "Invariants failed at step 2783"

    # Operation 2784: insert
    tree[6213] = 'value_250246'
    reference[6213] = 'value_250246'
    assert tree.invariants(), "Invariants failed at step 2784"

    # Operation 2785: update
    tree[6213] = 'value_443976'
    reference[6213] = 'value_443976'
    assert tree.invariants(), "Invariants failed at step 2785"

    # Operation 2786: get
    assert tree.invariants(), "Invariants failed at step 2786"

    # Operation 2787: update
    tree[2654] = 'value_648048'
    reference[2654] = 'value_648048'
    assert tree.invariants(), "Invariants failed at step 2787"

    # Operation 2788: get
    assert tree.invariants(), "Invariants failed at step 2788"

    # Operation 2789: batch_delete
    keys_to_delete = [8705, 4613, 242, 4154, 4349]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2789"

    # Operation 2790: insert
    tree[7353] = 'value_187438'
    reference[7353] = 'value_187438'
    assert tree.invariants(), "Invariants failed at step 2790"

    # Operation 2791: insert
    tree[1118] = 'value_82561'
    reference[1118] = 'value_82561'
    assert tree.invariants(), "Invariants failed at step 2791"

    # Operation 2792: get
    assert tree.invariants(), "Invariants failed at step 2792"

    # Operation 2793: delete
    del tree[7353]
    del reference[7353]
    assert tree.invariants(), "Invariants failed at step 2793"

    # Operation 2794: delete
    del tree[1118]
    del reference[1118]
    assert tree.invariants(), "Invariants failed at step 2794"

    # Operation 2795: batch_delete
    keys_to_delete = [940, 4957, 3590, 1335]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2795"

    # Operation 2796: batch_delete
    keys_to_delete = [5761, 5553, 6213]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2796"

    # Operation 2797: insert
    tree[6064] = 'value_827662'
    reference[6064] = 'value_827662'
    assert tree.invariants(), "Invariants failed at step 2797"

    # Operation 2798: get
    assert tree.invariants(), "Invariants failed at step 2798"

    # Operation 2799: insert
    tree[1029] = 'value_266872'
    reference[1029] = 'value_266872'
    assert tree.invariants(), "Invariants failed at step 2799"

    # Operation 2800: get
    assert tree.invariants(), "Invariants failed at step 2800"

    # Operation 2801: get
    assert tree.invariants(), "Invariants failed at step 2801"

    # Operation 2802: insert
    tree[4732] = 'value_942973'
    reference[4732] = 'value_942973'
    assert tree.invariants(), "Invariants failed at step 2802"

    # Operation 2803: delete
    del tree[4732]
    del reference[4732]
    assert tree.invariants(), "Invariants failed at step 2803"

    # Operation 2804: get
    assert tree.invariants(), "Invariants failed at step 2804"

    # Operation 2805: update
    tree[6064] = 'value_50627'
    reference[6064] = 'value_50627'
    assert tree.invariants(), "Invariants failed at step 2805"

    # Operation 2806: insert
    tree[7410] = 'value_42807'
    reference[7410] = 'value_42807'
    assert tree.invariants(), "Invariants failed at step 2806"

    # Operation 2807: insert
    tree[1213] = 'value_427779'
    reference[1213] = 'value_427779'
    assert tree.invariants(), "Invariants failed at step 2807"

    # Operation 2808: get
    assert tree.invariants(), "Invariants failed at step 2808"

    # Operation 2809: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2809"

    # Operation 2810: update
    tree[6064] = 'value_437932'
    reference[6064] = 'value_437932'
    assert tree.invariants(), "Invariants failed at step 2810"

    # Operation 2811: update
    tree[7410] = 'value_888689'
    reference[7410] = 'value_888689'
    assert tree.invariants(), "Invariants failed at step 2811"

    # Operation 2812: insert
    tree[2094] = 'value_709849'
    reference[2094] = 'value_709849'
    assert tree.invariants(), "Invariants failed at step 2812"

    # Operation 2813: get
    assert tree.invariants(), "Invariants failed at step 2813"

    # Operation 2814: batch_delete
    keys_to_delete = [5090, 1029, 6064, 4144, 8118, 1213]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2814"

    # Operation 2815: delete
    del tree[2654]
    del reference[2654]
    assert tree.invariants(), "Invariants failed at step 2815"

    # Operation 2816: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2816"

    # Operation 2817: insert
    tree[8299] = 'value_714529'
    reference[8299] = 'value_714529'
    assert tree.invariants(), "Invariants failed at step 2817"

    # Operation 2818: delete
    del tree[4573]
    del reference[4573]
    assert tree.invariants(), "Invariants failed at step 2818"

    # Operation 2819: get
    assert tree.invariants(), "Invariants failed at step 2819"

    # Operation 2820: delete
    del tree[8299]
    del reference[8299]
    assert tree.invariants(), "Invariants failed at step 2820"

    # Operation 2821: delete
    del tree[2094]
    del reference[2094]
    assert tree.invariants(), "Invariants failed at step 2821"

    # Operation 2822: get
    assert tree.invariants(), "Invariants failed at step 2822"

    # Operation 2823: insert
    tree[8344] = 'value_632285'
    reference[8344] = 'value_632285'
    assert tree.invariants(), "Invariants failed at step 2823"

    # Operation 2824: insert
    tree[3177] = 'value_426655'
    reference[3177] = 'value_426655'
    assert tree.invariants(), "Invariants failed at step 2824"

    # Operation 2825: insert
    tree[3631] = 'value_725808'
    reference[3631] = 'value_725808'
    assert tree.invariants(), "Invariants failed at step 2825"

    # Operation 2826: get
    assert tree.invariants(), "Invariants failed at step 2826"

    # Operation 2827: update
    tree[3177] = 'value_661394'
    reference[3177] = 'value_661394'
    assert tree.invariants(), "Invariants failed at step 2827"

    # Operation 2828: get
    assert tree.invariants(), "Invariants failed at step 2828"

    # Operation 2829: insert
    tree[2001] = 'value_822523'
    reference[2001] = 'value_822523'
    assert tree.invariants(), "Invariants failed at step 2829"

    # Operation 2830: insert
    tree[9311] = 'value_228716'
    reference[9311] = 'value_228716'
    assert tree.invariants(), "Invariants failed at step 2830"

    # Operation 2831: update
    tree[2001] = 'value_44035'
    reference[2001] = 'value_44035'
    assert tree.invariants(), "Invariants failed at step 2831"

    # Operation 2832: delete
    del tree[7410]
    del reference[7410]
    assert tree.invariants(), "Invariants failed at step 2832"

    # Operation 2833: delete
    del tree[8344]
    del reference[8344]
    assert tree.invariants(), "Invariants failed at step 2833"

    # Operation 2834: delete
    del tree[3177]
    del reference[3177]
    assert tree.invariants(), "Invariants failed at step 2834"

    # Operation 2835: insert
    tree[737] = 'value_444730'
    reference[737] = 'value_444730'
    assert tree.invariants(), "Invariants failed at step 2835"

    # Operation 2836: get
    assert tree.invariants(), "Invariants failed at step 2836"

    # Operation 2837: insert
    tree[5131] = 'value_114559'
    reference[5131] = 'value_114559'
    assert tree.invariants(), "Invariants failed at step 2837"

    # Operation 2838: insert
    tree[925] = 'value_254723'
    reference[925] = 'value_254723'
    assert tree.invariants(), "Invariants failed at step 2838"

    # Operation 2839: delete
    del tree[925]
    del reference[925]
    assert tree.invariants(), "Invariants failed at step 2839"

    # Operation 2840: insert
    tree[3775] = 'value_802827'
    reference[3775] = 'value_802827'
    assert tree.invariants(), "Invariants failed at step 2840"

    # Operation 2841: update
    tree[737] = 'value_21085'
    reference[737] = 'value_21085'
    assert tree.invariants(), "Invariants failed at step 2841"

    # Operation 2842: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2842"

    # Operation 2843: delete
    del tree[9311]
    del reference[9311]
    assert tree.invariants(), "Invariants failed at step 2843"

    # Operation 2844: get
    assert tree.invariants(), "Invariants failed at step 2844"

    # Operation 2845: get
    assert tree.invariants(), "Invariants failed at step 2845"

    # Operation 2846: update
    tree[3775] = 'value_193924'
    reference[3775] = 'value_193924'
    assert tree.invariants(), "Invariants failed at step 2846"

    # Operation 2847: delete
    del tree[2001]
    del reference[2001]
    assert tree.invariants(), "Invariants failed at step 2847"

    # Operation 2848: get
    assert tree.invariants(), "Invariants failed at step 2848"

    # Operation 2849: delete
    del tree[5131]
    del reference[5131]
    assert tree.invariants(), "Invariants failed at step 2849"

    # Operation 2850: delete
    del tree[3775]
    del reference[3775]
    assert tree.invariants(), "Invariants failed at step 2850"

    # Operation 2851: get
    assert tree.invariants(), "Invariants failed at step 2851"

    # Operation 2852: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2852"

    # Operation 2853: insert
    tree[5720] = 'value_294616'
    reference[5720] = 'value_294616'
    assert tree.invariants(), "Invariants failed at step 2853"

    # Operation 2854: insert
    tree[9866] = 'value_573809'
    reference[9866] = 'value_573809'
    assert tree.invariants(), "Invariants failed at step 2854"

    # Operation 2855: insert
    tree[8436] = 'value_874317'
    reference[8436] = 'value_874317'
    assert tree.invariants(), "Invariants failed at step 2855"

    # Operation 2856: batch_delete
    keys_to_delete = [737, 3371, 8436, 4135]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2856"

    # Operation 2857: delete
    del tree[9866]
    del reference[9866]
    assert tree.invariants(), "Invariants failed at step 2857"

    # Operation 2858: get
    assert tree.invariants(), "Invariants failed at step 2858"

    # Operation 2859: insert
    tree[4156] = 'value_790991'
    reference[4156] = 'value_790991'
    assert tree.invariants(), "Invariants failed at step 2859"

    # Operation 2860: insert
    tree[7700] = 'value_684455'
    reference[7700] = 'value_684455'
    assert tree.invariants(), "Invariants failed at step 2860"

    # Operation 2861: insert
    tree[2256] = 'value_764956'
    reference[2256] = 'value_764956'
    assert tree.invariants(), "Invariants failed at step 2861"

    # Operation 2862: batch_delete
    keys_to_delete = [5720, 9755, 4156, 2256]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2862"

    # Operation 2863: get
    assert tree.invariants(), "Invariants failed at step 2863"

    # Operation 2864: delete
    del tree[3631]
    del reference[3631]
    assert tree.invariants(), "Invariants failed at step 2864"

    # Operation 2865: insert
    tree[2053] = 'value_599132'
    reference[2053] = 'value_599132'
    assert tree.invariants(), "Invariants failed at step 2865"

    # Operation 2866: get
    assert tree.invariants(), "Invariants failed at step 2866"

    # Operation 2867: insert
    tree[4080] = 'value_30564'
    reference[4080] = 'value_30564'
    assert tree.invariants(), "Invariants failed at step 2867"

    # Operation 2868: delete
    del tree[2053]
    del reference[2053]
    assert tree.invariants(), "Invariants failed at step 2868"

    # Operation 2869: update
    tree[4080] = 'value_30188'
    reference[4080] = 'value_30188'
    assert tree.invariants(), "Invariants failed at step 2869"

    # Operation 2870: get
    assert tree.invariants(), "Invariants failed at step 2870"

    # Operation 2871: insert
    tree[5485] = 'value_386569'
    reference[5485] = 'value_386569'
    assert tree.invariants(), "Invariants failed at step 2871"

    # Operation 2872: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2872"

    # Operation 2873: insert
    tree[4480] = 'value_758081'
    reference[4480] = 'value_758081'
    assert tree.invariants(), "Invariants failed at step 2873"

    # Operation 2874: insert
    tree[1845] = 'value_856549'
    reference[1845] = 'value_856549'
    assert tree.invariants(), "Invariants failed at step 2874"

    # Operation 2875: delete
    del tree[4080]
    del reference[4080]
    assert tree.invariants(), "Invariants failed at step 2875"

    # Operation 2876: update
    tree[1845] = 'value_305404'
    reference[1845] = 'value_305404'
    assert tree.invariants(), "Invariants failed at step 2876"

    # Operation 2877: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2877"

    # Operation 2878: insert
    tree[2248] = 'value_680727'
    reference[2248] = 'value_680727'
    assert tree.invariants(), "Invariants failed at step 2878"

    # Operation 2879: insert
    tree[8002] = 'value_26067'
    reference[8002] = 'value_26067'
    assert tree.invariants(), "Invariants failed at step 2879"

    # Operation 2880: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2880"

    # Operation 2881: get
    assert tree.invariants(), "Invariants failed at step 2881"

    # Operation 2882: delete
    del tree[4480]
    del reference[4480]
    assert tree.invariants(), "Invariants failed at step 2882"

    # Operation 2883: batch_delete
    keys_to_delete = [8002, 2795, 5485, 8613]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2883"

    # Operation 2884: get
    assert tree.invariants(), "Invariants failed at step 2884"

    # Operation 2885: delete
    del tree[1845]
    del reference[1845]
    assert tree.invariants(), "Invariants failed at step 2885"

    # Operation 2886: get
    assert tree.invariants(), "Invariants failed at step 2886"

    # Operation 2887: insert
    tree[5718] = 'value_395368'
    reference[5718] = 'value_395368'
    assert tree.invariants(), "Invariants failed at step 2887"

    # Operation 2888: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2888"

    # Operation 2889: insert
    tree[1596] = 'value_519818'
    reference[1596] = 'value_519818'
    assert tree.invariants(), "Invariants failed at step 2889"

    # Operation 2890: delete
    del tree[7700]
    del reference[7700]
    assert tree.invariants(), "Invariants failed at step 2890"

    # Operation 2891: get
    assert tree.invariants(), "Invariants failed at step 2891"

    # Operation 2892: update
    tree[2248] = 'value_848042'
    reference[2248] = 'value_848042'
    assert tree.invariants(), "Invariants failed at step 2892"

    # Operation 2893: insert
    tree[468] = 'value_356803'
    reference[468] = 'value_356803'
    assert tree.invariants(), "Invariants failed at step 2893"

    # Operation 2894: update
    tree[5718] = 'value_408258'
    reference[5718] = 'value_408258'
    assert tree.invariants(), "Invariants failed at step 2894"

    # Operation 2895: insert
    tree[6677] = 'value_197712'
    reference[6677] = 'value_197712'
    assert tree.invariants(), "Invariants failed at step 2895"

    # Operation 2896: batch_delete
    keys_to_delete = [7816, 7604, 468, 1596]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2896"

    # Operation 2897: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2897"

    # Operation 2898: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2898"

    # Operation 2899: update
    tree[6677] = 'value_425446'
    reference[6677] = 'value_425446'
    assert tree.invariants(), "Invariants failed at step 2899"

    # Operation 2900: delete
    del tree[6677]
    del reference[6677]
    assert tree.invariants(), "Invariants failed at step 2900"

    # Operation 2901: insert
    tree[318] = 'value_508887'
    reference[318] = 'value_508887'
    assert tree.invariants(), "Invariants failed at step 2901"

    # Operation 2902: insert
    tree[1740] = 'value_255540'
    reference[1740] = 'value_255540'
    assert tree.invariants(), "Invariants failed at step 2902"

    # Operation 2903: insert
    tree[9533] = 'value_184730'
    reference[9533] = 'value_184730'
    assert tree.invariants(), "Invariants failed at step 2903"

    # Operation 2904: get
    assert tree.invariants(), "Invariants failed at step 2904"

    # Operation 2905: get
    assert tree.invariants(), "Invariants failed at step 2905"

    # Operation 2906: insert
    tree[9840] = 'value_417617'
    reference[9840] = 'value_417617'
    assert tree.invariants(), "Invariants failed at step 2906"

    # Operation 2907: get
    assert tree.invariants(), "Invariants failed at step 2907"

    # Operation 2908: get
    assert tree.invariants(), "Invariants failed at step 2908"

    # Operation 2909: update
    tree[9840] = 'value_196527'
    reference[9840] = 'value_196527'
    assert tree.invariants(), "Invariants failed at step 2909"

    # Operation 2910: insert
    tree[6591] = 'value_752207'
    reference[6591] = 'value_752207'
    assert tree.invariants(), "Invariants failed at step 2910"

    # Operation 2911: get
    assert tree.invariants(), "Invariants failed at step 2911"

    # Operation 2912: update
    tree[6591] = 'value_710693'
    reference[6591] = 'value_710693'
    assert tree.invariants(), "Invariants failed at step 2912"

    # Operation 2913: batch_delete
    keys_to_delete = [7075, 2248, 9840, 318, 6591]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2913"

    # Operation 2914: insert
    tree[4600] = 'value_7163'
    reference[4600] = 'value_7163'
    assert tree.invariants(), "Invariants failed at step 2914"

    # Operation 2915: get
    assert tree.invariants(), "Invariants failed at step 2915"

    # Operation 2916: update
    tree[1740] = 'value_7350'
    reference[1740] = 'value_7350'
    assert tree.invariants(), "Invariants failed at step 2916"

    # Operation 2917: insert
    tree[3666] = 'value_525905'
    reference[3666] = 'value_525905'
    assert tree.invariants(), "Invariants failed at step 2917"

    # Operation 2918: batch_delete
    keys_to_delete = [513, 3666, 9533, 5718]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2918"

    # Operation 2919: get
    assert tree.invariants(), "Invariants failed at step 2919"

    # Operation 2920: update
    tree[4600] = 'value_144814'
    reference[4600] = 'value_144814'
    assert tree.invariants(), "Invariants failed at step 2920"

    # Operation 2921: update
    tree[4600] = 'value_85795'
    reference[4600] = 'value_85795'
    assert tree.invariants(), "Invariants failed at step 2921"

    # Operation 2922: get
    assert tree.invariants(), "Invariants failed at step 2922"

    # Operation 2923: insert
    tree[5746] = 'value_846982'
    reference[5746] = 'value_846982'
    assert tree.invariants(), "Invariants failed at step 2923"

    # Operation 2924: delete
    del tree[4600]
    del reference[4600]
    assert tree.invariants(), "Invariants failed at step 2924"

    # Operation 2925: delete
    del tree[5746]
    del reference[5746]
    assert tree.invariants(), "Invariants failed at step 2925"

    # Operation 2926: insert
    tree[3279] = 'value_702638'
    reference[3279] = 'value_702638'
    assert tree.invariants(), "Invariants failed at step 2926"

    # Operation 2927: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2927"

    # Operation 2928: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2928"

    # Operation 2929: insert
    tree[5840] = 'value_263782'
    reference[5840] = 'value_263782'
    assert tree.invariants(), "Invariants failed at step 2929"

    # Operation 2930: insert
    tree[6767] = 'value_285935'
    reference[6767] = 'value_285935'
    assert tree.invariants(), "Invariants failed at step 2930"

    # Operation 2931: get
    assert tree.invariants(), "Invariants failed at step 2931"

    # Operation 2932: insert
    tree[9004] = 'value_514404'
    reference[9004] = 'value_514404'
    assert tree.invariants(), "Invariants failed at step 2932"

    # Operation 2933: delete
    del tree[9004]
    del reference[9004]
    assert tree.invariants(), "Invariants failed at step 2933"

    # Operation 2934: delete
    del tree[5840]
    del reference[5840]
    assert tree.invariants(), "Invariants failed at step 2934"

    # Operation 2935: get
    assert tree.invariants(), "Invariants failed at step 2935"

    # Operation 2936: insert
    tree[6808] = 'value_400369'
    reference[6808] = 'value_400369'
    assert tree.invariants(), "Invariants failed at step 2936"

    # Operation 2937: insert
    tree[469] = 'value_711464'
    reference[469] = 'value_711464'
    assert tree.invariants(), "Invariants failed at step 2937"

    # Operation 2938: delete
    del tree[6808]
    del reference[6808]
    assert tree.invariants(), "Invariants failed at step 2938"

    # Operation 2939: insert
    tree[9659] = 'value_162742'
    reference[9659] = 'value_162742'
    assert tree.invariants(), "Invariants failed at step 2939"

    # Operation 2940: get
    assert tree.invariants(), "Invariants failed at step 2940"

    # Operation 2941: insert
    tree[5187] = 'value_281109'
    reference[5187] = 'value_281109'
    assert tree.invariants(), "Invariants failed at step 2941"

    # Operation 2942: insert
    tree[3830] = 'value_476428'
    reference[3830] = 'value_476428'
    assert tree.invariants(), "Invariants failed at step 2942"

    # Operation 2943: delete
    del tree[469]
    del reference[469]
    assert tree.invariants(), "Invariants failed at step 2943"

    # Operation 2944: update
    tree[3830] = 'value_583260'
    reference[3830] = 'value_583260'
    assert tree.invariants(), "Invariants failed at step 2944"

    # Operation 2945: delete
    del tree[3830]
    del reference[3830]
    assert tree.invariants(), "Invariants failed at step 2945"

    # Operation 2946: get
    assert tree.invariants(), "Invariants failed at step 2946"

    # Operation 2947: delete
    del tree[9659]
    del reference[9659]
    assert tree.invariants(), "Invariants failed at step 2947"

    # Operation 2948: delete
    del tree[3279]
    del reference[3279]
    assert tree.invariants(), "Invariants failed at step 2948"

    # Operation 2949: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2949"

    # Operation 2950: insert
    tree[4338] = 'value_328127'
    reference[4338] = 'value_328127'
    assert tree.invariants(), "Invariants failed at step 2950"

    # Operation 2951: update
    tree[1740] = 'value_855676'
    reference[1740] = 'value_855676'
    assert tree.invariants(), "Invariants failed at step 2951"

    # Operation 2952: delete
    del tree[4338]
    del reference[4338]
    assert tree.invariants(), "Invariants failed at step 2952"

    # Operation 2953: insert
    tree[7778] = 'value_625066'
    reference[7778] = 'value_625066'
    assert tree.invariants(), "Invariants failed at step 2953"

    # Operation 2954: update
    tree[5187] = 'value_952387'
    reference[5187] = 'value_952387'
    assert tree.invariants(), "Invariants failed at step 2954"

    # Operation 2955: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2955"

    # Operation 2956: insert
    tree[6128] = 'value_450985'
    reference[6128] = 'value_450985'
    assert tree.invariants(), "Invariants failed at step 2956"

    # Operation 2957: insert
    tree[7947] = 'value_852301'
    reference[7947] = 'value_852301'
    assert tree.invariants(), "Invariants failed at step 2957"

    # Operation 2958: get
    assert tree.invariants(), "Invariants failed at step 2958"

    # Operation 2959: insert
    tree[5704] = 'value_918053'
    reference[5704] = 'value_918053'
    assert tree.invariants(), "Invariants failed at step 2959"

    # Operation 2960: insert
    tree[9484] = 'value_5768'
    reference[9484] = 'value_5768'
    assert tree.invariants(), "Invariants failed at step 2960"

    # Operation 2961: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2961"

    # Operation 2962: get
    assert tree.invariants(), "Invariants failed at step 2962"

    # Operation 2963: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 2963"

    # Operation 2964: update
    tree[7947] = 'value_375997'
    reference[7947] = 'value_375997'
    assert tree.invariants(), "Invariants failed at step 2964"

    # Operation 2965: delete
    del tree[6128]
    del reference[6128]
    assert tree.invariants(), "Invariants failed at step 2965"

    # Operation 2966: update
    tree[9484] = 'value_368822'
    reference[9484] = 'value_368822'
    assert tree.invariants(), "Invariants failed at step 2966"

    # Operation 2967: delete
    del tree[7947]
    del reference[7947]
    assert tree.invariants(), "Invariants failed at step 2967"

    # Operation 2968: get
    assert tree.invariants(), "Invariants failed at step 2968"

    # Operation 2969: insert
    tree[8053] = 'value_411709'
    reference[8053] = 'value_411709'
    assert tree.invariants(), "Invariants failed at step 2969"

    # Operation 2970: insert
    tree[9350] = 'value_668530'
    reference[9350] = 'value_668530'
    assert tree.invariants(), "Invariants failed at step 2970"

    # Operation 2971: update
    tree[1740] = 'value_121004'
    reference[1740] = 'value_121004'
    assert tree.invariants(), "Invariants failed at step 2971"

    # Operation 2972: get
    assert tree.invariants(), "Invariants failed at step 2972"

    # Operation 2973: delete
    del tree[9484]
    del reference[9484]
    assert tree.invariants(), "Invariants failed at step 2973"

    # Operation 2974: delete
    del tree[7778]
    del reference[7778]
    assert tree.invariants(), "Invariants failed at step 2974"

    # Operation 2975: batch_delete
    keys_to_delete = [5187, 9350, 6767, 8053, 3479]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 2975"

    # Operation 2976: delete
    del tree[1740]
    del reference[1740]
    assert tree.invariants(), "Invariants failed at step 2976"

    # Operation 2977: update
    tree[5704] = 'value_951985'
    reference[5704] = 'value_951985'
    assert tree.invariants(), "Invariants failed at step 2977"

    # Operation 2978: update
    tree[5704] = 'value_183823'
    reference[5704] = 'value_183823'
    assert tree.invariants(), "Invariants failed at step 2978"

    # Operation 2979: insert
    tree[5222] = 'value_904633'
    reference[5222] = 'value_904633'
    assert tree.invariants(), "Invariants failed at step 2979"

    # Operation 2980: insert
    tree[5895] = 'value_188510'
    reference[5895] = 'value_188510'
    assert tree.invariants(), "Invariants failed at step 2980"

    # Operation 2981: update
    tree[5222] = 'value_512700'
    reference[5222] = 'value_512700'
    assert tree.invariants(), "Invariants failed at step 2981"

    # Operation 2982: get
    assert tree.invariants(), "Invariants failed at step 2982"

    # Operation 2983: delete
    del tree[5895]
    del reference[5895]
    assert tree.invariants(), "Invariants failed at step 2983"

    # Operation 2984: insert
    tree[4794] = 'value_358512'
    reference[4794] = 'value_358512'
    assert tree.invariants(), "Invariants failed at step 2984"

    # Operation 2985: get
    assert tree.invariants(), "Invariants failed at step 2985"

    # Operation 2986: delete
    del tree[4794]
    del reference[4794]
    assert tree.invariants(), "Invariants failed at step 2986"

    # Operation 2987: insert
    tree[3884] = 'value_130420'
    reference[3884] = 'value_130420'
    assert tree.invariants(), "Invariants failed at step 2987"

    # Operation 2988: delete
    del tree[5222]
    del reference[5222]
    assert tree.invariants(), "Invariants failed at step 2988"

    # Operation 2989: get
    assert tree.invariants(), "Invariants failed at step 2989"

    # Operation 2990: update
    tree[5704] = 'value_445524'
    reference[5704] = 'value_445524'
    assert tree.invariants(), "Invariants failed at step 2990"

    # Operation 2991: insert
    tree[4066] = 'value_634767'
    reference[4066] = 'value_634767'
    assert tree.invariants(), "Invariants failed at step 2991"

    # Operation 2992: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 2992"

    # Operation 2993: update
    tree[4066] = 'value_480888'
    reference[4066] = 'value_480888'
    assert tree.invariants(), "Invariants failed at step 2993"

    # Operation 2994: update
    tree[5704] = 'value_95019'
    reference[5704] = 'value_95019'
    assert tree.invariants(), "Invariants failed at step 2994"

    # Operation 2995: update
    tree[4066] = 'value_390272'
    reference[4066] = 'value_390272'
    assert tree.invariants(), "Invariants failed at step 2995"

    # Operation 2996: get
    assert tree.invariants(), "Invariants failed at step 2996"

    # Operation 2997: get
    assert tree.invariants(), "Invariants failed at step 2997"

    # Operation 2998: get
    assert tree.invariants(), "Invariants failed at step 2998"

    # Operation 2999: insert
    tree[5394] = 'value_681913'
    reference[5394] = 'value_681913'
    assert tree.invariants(), "Invariants failed at step 2999"

    # Operation 3000: insert
    tree[2521] = 'value_105640'
    reference[2521] = 'value_105640'
    assert tree.invariants(), "Invariants failed at step 3000"

    # Operation 3001: get
    assert tree.invariants(), "Invariants failed at step 3001"

    # Operation 3002: insert
    tree[3664] = 'value_728180'
    reference[3664] = 'value_728180'
    assert tree.invariants(), "Invariants failed at step 3002"

    # Operation 3003: delete
    del tree[3884]
    del reference[3884]
    assert tree.invariants(), "Invariants failed at step 3003"

    # Operation 3004: update
    tree[4066] = 'value_439561'
    reference[4066] = 'value_439561'
    assert tree.invariants(), "Invariants failed at step 3004"

    # Operation 3005: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3005"

    # Operation 3006: insert
    tree[3583] = 'value_505940'
    reference[3583] = 'value_505940'
    assert tree.invariants(), "Invariants failed at step 3006"

    # Operation 3007: insert
    tree[1061] = 'value_635041'
    reference[1061] = 'value_635041'
    assert tree.invariants(), "Invariants failed at step 3007"

    # Operation 3008: insert
    tree[7193] = 'value_700690'
    reference[7193] = 'value_700690'
    assert tree.invariants(), "Invariants failed at step 3008"

    # Operation 3009: insert
    tree[8035] = 'value_147679'
    reference[8035] = 'value_147679'
    assert tree.invariants(), "Invariants failed at step 3009"

    # Operation 3010: delete
    del tree[2521]
    del reference[2521]
    assert tree.invariants(), "Invariants failed at step 3010"

    # Operation 3011: get
    assert tree.invariants(), "Invariants failed at step 3011"

    # Operation 3012: insert
    tree[1912] = 'value_462272'
    reference[1912] = 'value_462272'
    assert tree.invariants(), "Invariants failed at step 3012"

    # Operation 3013: insert
    tree[4413] = 'value_84854'
    reference[4413] = 'value_84854'
    assert tree.invariants(), "Invariants failed at step 3013"

    # Operation 3014: get
    assert tree.invariants(), "Invariants failed at step 3014"

    # Operation 3015: insert
    tree[6113] = 'value_754193'
    reference[6113] = 'value_754193'
    assert tree.invariants(), "Invariants failed at step 3015"

    # Operation 3016: insert
    tree[6589] = 'value_443665'
    reference[6589] = 'value_443665'
    assert tree.invariants(), "Invariants failed at step 3016"

    # Operation 3017: insert
    tree[7466] = 'value_456817'
    reference[7466] = 'value_456817'
    assert tree.invariants(), "Invariants failed at step 3017"

    # Operation 3018: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3018"

    # Operation 3019: insert
    tree[3095] = 'value_927526'
    reference[3095] = 'value_927526'
    assert tree.invariants(), "Invariants failed at step 3019"

    # Operation 3020: delete
    del tree[3095]
    del reference[3095]
    assert tree.invariants(), "Invariants failed at step 3020"

    # Operation 3021: delete
    del tree[5704]
    del reference[5704]
    assert tree.invariants(), "Invariants failed at step 3021"

    # Operation 3022: batch_delete
    keys_to_delete = [4066, 8035, 7466, 3664, 5941, 4349, 6589, 3583]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3022"

    # Operation 3023: insert
    tree[5224] = 'value_285028'
    reference[5224] = 'value_285028'
    assert tree.invariants(), "Invariants failed at step 3023"

    # Operation 3024: delete
    del tree[4413]
    del reference[4413]
    assert tree.invariants(), "Invariants failed at step 3024"

    # Operation 3025: update
    tree[1912] = 'value_600496'
    reference[1912] = 'value_600496'
    assert tree.invariants(), "Invariants failed at step 3025"

    # Operation 3026: update
    tree[5394] = 'value_354764'
    reference[5394] = 'value_354764'
    assert tree.invariants(), "Invariants failed at step 3026"

    # Operation 3027: insert
    tree[2177] = 'value_763191'
    reference[2177] = 'value_763191'
    assert tree.invariants(), "Invariants failed at step 3027"

    # Operation 3028: update
    tree[5394] = 'value_773618'
    reference[5394] = 'value_773618'
    assert tree.invariants(), "Invariants failed at step 3028"

    # Operation 3029: get
    assert tree.invariants(), "Invariants failed at step 3029"

    # Operation 3030: delete
    del tree[6113]
    del reference[6113]
    assert tree.invariants(), "Invariants failed at step 3030"

    # Operation 3031: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3031"

    # Operation 3032: batch_delete
    keys_to_delete = [2177, 5224, 1912, 8797, 5374]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3032"

    # Operation 3033: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3033"

    # Operation 3034: insert
    tree[1071] = 'value_148634'
    reference[1071] = 'value_148634'
    assert tree.invariants(), "Invariants failed at step 3034"

    # Operation 3035: insert
    tree[2038] = 'value_222708'
    reference[2038] = 'value_222708'
    assert tree.invariants(), "Invariants failed at step 3035"

    # Operation 3036: delete
    del tree[7193]
    del reference[7193]
    assert tree.invariants(), "Invariants failed at step 3036"

    # Operation 3037: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3037"

    # Operation 3038: update
    tree[5394] = 'value_198490'
    reference[5394] = 'value_198490'
    assert tree.invariants(), "Invariants failed at step 3038"

    # Operation 3039: insert
    tree[6379] = 'value_628956'
    reference[6379] = 'value_628956'
    assert tree.invariants(), "Invariants failed at step 3039"

    # Operation 3040: insert
    tree[8055] = 'value_430570'
    reference[8055] = 'value_430570'
    assert tree.invariants(), "Invariants failed at step 3040"

    # Operation 3041: insert
    tree[8224] = 'value_718757'
    reference[8224] = 'value_718757'
    assert tree.invariants(), "Invariants failed at step 3041"

    # Operation 3042: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3042"

    # Operation 3043: batch_delete
    keys_to_delete = [6433, 1061, 2439, 6379, 1071]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3043"

    # Operation 3044: update
    tree[2038] = 'value_623493'
    reference[2038] = 'value_623493'
    assert tree.invariants(), "Invariants failed at step 3044"

    # Operation 3045: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3045"

    # Operation 3046: get
    assert tree.invariants(), "Invariants failed at step 3046"

    # Operation 3047: delete
    del tree[2038]
    del reference[2038]
    assert tree.invariants(), "Invariants failed at step 3047"

    # Operation 3048: delete
    del tree[5394]
    del reference[5394]
    assert tree.invariants(), "Invariants failed at step 3048"

    # Operation 3049: insert
    tree[4354] = 'value_768660'
    reference[4354] = 'value_768660'
    assert tree.invariants(), "Invariants failed at step 3049"

    # Operation 3050: insert
    tree[9966] = 'value_4943'
    reference[9966] = 'value_4943'
    assert tree.invariants(), "Invariants failed at step 3050"

    # Operation 3051: get
    assert tree.invariants(), "Invariants failed at step 3051"

    # Operation 3052: get
    assert tree.invariants(), "Invariants failed at step 3052"

    # Operation 3053: update
    tree[8224] = 'value_845537'
    reference[8224] = 'value_845537'
    assert tree.invariants(), "Invariants failed at step 3053"

    # Operation 3054: insert
    tree[3553] = 'value_202849'
    reference[3553] = 'value_202849'
    assert tree.invariants(), "Invariants failed at step 3054"

    # Operation 3055: get
    assert tree.invariants(), "Invariants failed at step 3055"

    # Operation 3056: get
    assert tree.invariants(), "Invariants failed at step 3056"

    # Operation 3057: insert
    tree[2285] = 'value_47895'
    reference[2285] = 'value_47895'
    assert tree.invariants(), "Invariants failed at step 3057"

    # Operation 3058: update
    tree[3553] = 'value_966160'
    reference[3553] = 'value_966160'
    assert tree.invariants(), "Invariants failed at step 3058"

    # Operation 3059: update
    tree[8224] = 'value_465043'
    reference[8224] = 'value_465043'
    assert tree.invariants(), "Invariants failed at step 3059"

    # Operation 3060: insert
    tree[3426] = 'value_410896'
    reference[3426] = 'value_410896'
    assert tree.invariants(), "Invariants failed at step 3060"

    # Operation 3061: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3061"

    # Operation 3062: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3062"

    # Operation 3063: insert
    tree[1276] = 'value_559679'
    reference[1276] = 'value_559679'
    assert tree.invariants(), "Invariants failed at step 3063"

    # Operation 3064: update
    tree[4354] = 'value_202030'
    reference[4354] = 'value_202030'
    assert tree.invariants(), "Invariants failed at step 3064"

    # Operation 3065: insert
    tree[6109] = 'value_21707'
    reference[6109] = 'value_21707'
    assert tree.invariants(), "Invariants failed at step 3065"

    # Operation 3066: insert
    tree[4792] = 'value_884565'
    reference[4792] = 'value_884565'
    assert tree.invariants(), "Invariants failed at step 3066"

    # Operation 3067: delete
    del tree[4792]
    del reference[4792]
    assert tree.invariants(), "Invariants failed at step 3067"

    # Operation 3068: delete
    del tree[3553]
    del reference[3553]
    assert tree.invariants(), "Invariants failed at step 3068"

    # Operation 3069: batch_delete
    keys_to_delete = [3426, 3797, 8055, 5528, 6109]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3069"

    # Operation 3070: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3070"

    # Operation 3071: insert
    tree[4568] = 'value_311437'
    reference[4568] = 'value_311437'
    assert tree.invariants(), "Invariants failed at step 3071"

    # Operation 3072: delete
    del tree[9966]
    del reference[9966]
    assert tree.invariants(), "Invariants failed at step 3072"

    # Operation 3073: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3073"

    # Operation 3074: get
    assert tree.invariants(), "Invariants failed at step 3074"

    # Operation 3075: delete
    del tree[8224]
    del reference[8224]
    assert tree.invariants(), "Invariants failed at step 3075"

    # Operation 3076: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3076"

    # Operation 3077: delete
    del tree[2285]
    del reference[2285]
    assert tree.invariants(), "Invariants failed at step 3077"

    # Operation 3078: insert
    tree[8239] = 'value_972457'
    reference[8239] = 'value_972457'
    assert tree.invariants(), "Invariants failed at step 3078"

    # Operation 3079: delete
    del tree[1276]
    del reference[1276]
    assert tree.invariants(), "Invariants failed at step 3079"

    # Operation 3080: delete
    del tree[8239]
    del reference[8239]
    assert tree.invariants(), "Invariants failed at step 3080"

    # Operation 3081: delete
    del tree[4568]
    del reference[4568]
    assert tree.invariants(), "Invariants failed at step 3081"

    # Operation 3082: delete
    del tree[4354]
    del reference[4354]
    assert tree.invariants(), "Invariants failed at step 3082"

    # Operation 3083: get
    assert tree.invariants(), "Invariants failed at step 3083"

    # Operation 3084: insert
    tree[1861] = 'value_989505'
    reference[1861] = 'value_989505'
    assert tree.invariants(), "Invariants failed at step 3084"

    # Operation 3085: delete
    del tree[1861]
    del reference[1861]
    assert tree.invariants(), "Invariants failed at step 3085"

    # Operation 3086: insert
    tree[9572] = 'value_244010'
    reference[9572] = 'value_244010'
    assert tree.invariants(), "Invariants failed at step 3086"

    # Operation 3087: delete
    del tree[9572]
    del reference[9572]
    assert tree.invariants(), "Invariants failed at step 3087"

    # Operation 3088: insert
    tree[1521] = 'value_610296'
    reference[1521] = 'value_610296'
    assert tree.invariants(), "Invariants failed at step 3088"

    # Operation 3089: insert
    tree[7928] = 'value_73436'
    reference[7928] = 'value_73436'
    assert tree.invariants(), "Invariants failed at step 3089"

    # Operation 3090: update
    tree[1521] = 'value_516387'
    reference[1521] = 'value_516387'
    assert tree.invariants(), "Invariants failed at step 3090"

    # Operation 3091: insert
    tree[9750] = 'value_523633'
    reference[9750] = 'value_523633'
    assert tree.invariants(), "Invariants failed at step 3091"

    # Operation 3092: update
    tree[9750] = 'value_53147'
    reference[9750] = 'value_53147'
    assert tree.invariants(), "Invariants failed at step 3092"

    # Operation 3093: get
    assert tree.invariants(), "Invariants failed at step 3093"

    # Operation 3094: update
    tree[1521] = 'value_2856'
    reference[1521] = 'value_2856'
    assert tree.invariants(), "Invariants failed at step 3094"

    # Operation 3095: get
    assert tree.invariants(), "Invariants failed at step 3095"

    # Operation 3096: delete
    del tree[7928]
    del reference[7928]
    assert tree.invariants(), "Invariants failed at step 3096"

    # Operation 3097: insert
    tree[4370] = 'value_667722'
    reference[4370] = 'value_667722'
    assert tree.invariants(), "Invariants failed at step 3097"

    # Operation 3098: delete
    del tree[9750]
    del reference[9750]
    assert tree.invariants(), "Invariants failed at step 3098"

    # Operation 3099: delete
    del tree[4370]
    del reference[4370]
    assert tree.invariants(), "Invariants failed at step 3099"

    # Operation 3100: update
    tree[1521] = 'value_713166'
    reference[1521] = 'value_713166'
    assert tree.invariants(), "Invariants failed at step 3100"

    # Operation 3101: get
    assert tree.invariants(), "Invariants failed at step 3101"

    # Operation 3102: update
    tree[1521] = 'value_991543'
    reference[1521] = 'value_991543'
    assert tree.invariants(), "Invariants failed at step 3102"

    # Operation 3103: insert
    tree[8004] = 'value_716304'
    reference[8004] = 'value_716304'
    assert tree.invariants(), "Invariants failed at step 3103"

    # Operation 3104: delete
    del tree[8004]
    del reference[8004]
    assert tree.invariants(), "Invariants failed at step 3104"

    # Operation 3105: insert
    tree[1719] = 'value_940545'
    reference[1719] = 'value_940545'
    assert tree.invariants(), "Invariants failed at step 3105"

    # Operation 3106: insert
    tree[2161] = 'value_274691'
    reference[2161] = 'value_274691'
    assert tree.invariants(), "Invariants failed at step 3106"

    # Operation 3107: update
    tree[1521] = 'value_985693'
    reference[1521] = 'value_985693'
    assert tree.invariants(), "Invariants failed at step 3107"

    # Operation 3108: delete
    del tree[1719]
    del reference[1719]
    assert tree.invariants(), "Invariants failed at step 3108"

    # Operation 3109: insert
    tree[5566] = 'value_12957'
    reference[5566] = 'value_12957'
    assert tree.invariants(), "Invariants failed at step 3109"

    # Operation 3110: get
    assert tree.invariants(), "Invariants failed at step 3110"

    # Operation 3111: insert
    tree[9755] = 'value_244422'
    reference[9755] = 'value_244422'
    assert tree.invariants(), "Invariants failed at step 3111"

    # Operation 3112: delete
    del tree[1521]
    del reference[1521]
    assert tree.invariants(), "Invariants failed at step 3112"

    # Operation 3113: delete
    del tree[9755]
    del reference[9755]
    assert tree.invariants(), "Invariants failed at step 3113"

    # Operation 3114: update
    tree[2161] = 'value_205464'
    reference[2161] = 'value_205464'
    assert tree.invariants(), "Invariants failed at step 3114"

    # Operation 3115: get
    assert tree.invariants(), "Invariants failed at step 3115"

    # Operation 3116: get
    assert tree.invariants(), "Invariants failed at step 3116"

    # Operation 3117: delete
    del tree[5566]
    del reference[5566]
    assert tree.invariants(), "Invariants failed at step 3117"

    # Operation 3118: get
    assert tree.invariants(), "Invariants failed at step 3118"

    # Operation 3119: delete
    del tree[2161]
    del reference[2161]
    assert tree.invariants(), "Invariants failed at step 3119"

    # Operation 3120: insert
    tree[9319] = 'value_801334'
    reference[9319] = 'value_801334'
    assert tree.invariants(), "Invariants failed at step 3120"

    # Operation 3121: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3121"

    # Operation 3122: update
    tree[9319] = 'value_946644'
    reference[9319] = 'value_946644'
    assert tree.invariants(), "Invariants failed at step 3122"

    # Operation 3123: update
    tree[9319] = 'value_990668'
    reference[9319] = 'value_990668'
    assert tree.invariants(), "Invariants failed at step 3123"

    # Operation 3124: insert
    tree[8059] = 'value_563112'
    reference[8059] = 'value_563112'
    assert tree.invariants(), "Invariants failed at step 3124"

    # Operation 3125: insert
    tree[6643] = 'value_837350'
    reference[6643] = 'value_837350'
    assert tree.invariants(), "Invariants failed at step 3125"

    # Operation 3126: update
    tree[8059] = 'value_22315'
    reference[8059] = 'value_22315'
    assert tree.invariants(), "Invariants failed at step 3126"

    # Operation 3127: insert
    tree[8783] = 'value_720119'
    reference[8783] = 'value_720119'
    assert tree.invariants(), "Invariants failed at step 3127"

    # Operation 3128: get
    assert tree.invariants(), "Invariants failed at step 3128"

    # Operation 3129: delete
    del tree[9319]
    del reference[9319]
    assert tree.invariants(), "Invariants failed at step 3129"

    # Operation 3130: insert
    tree[1698] = 'value_961306'
    reference[1698] = 'value_961306'
    assert tree.invariants(), "Invariants failed at step 3130"

    # Operation 3131: delete
    del tree[6643]
    del reference[6643]
    assert tree.invariants(), "Invariants failed at step 3131"

    # Operation 3132: get
    assert tree.invariants(), "Invariants failed at step 3132"

    # Operation 3133: delete
    del tree[1698]
    del reference[1698]
    assert tree.invariants(), "Invariants failed at step 3133"

    # Operation 3134: delete
    del tree[8059]
    del reference[8059]
    assert tree.invariants(), "Invariants failed at step 3134"

    # Operation 3135: get
    assert tree.invariants(), "Invariants failed at step 3135"

    # Operation 3136: get
    assert tree.invariants(), "Invariants failed at step 3136"

    # Operation 3137: delete
    del tree[8783]
    del reference[8783]
    assert tree.invariants(), "Invariants failed at step 3137"

    # Operation 3138: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3138"

    # Operation 3139: insert
    tree[5851] = 'value_542142'
    reference[5851] = 'value_542142'
    assert tree.invariants(), "Invariants failed at step 3139"

    # Operation 3140: update
    tree[5851] = 'value_618493'
    reference[5851] = 'value_618493'
    assert tree.invariants(), "Invariants failed at step 3140"

    # Operation 3141: get
    assert tree.invariants(), "Invariants failed at step 3141"

    # Operation 3142: update
    tree[5851] = 'value_964045'
    reference[5851] = 'value_964045'
    assert tree.invariants(), "Invariants failed at step 3142"

    # Operation 3143: delete
    del tree[5851]
    del reference[5851]
    assert tree.invariants(), "Invariants failed at step 3143"

    # Operation 3144: insert
    tree[8500] = 'value_811779'
    reference[8500] = 'value_811779'
    assert tree.invariants(), "Invariants failed at step 3144"

    # Operation 3145: update
    tree[8500] = 'value_776580'
    reference[8500] = 'value_776580'
    assert tree.invariants(), "Invariants failed at step 3145"

    # Operation 3146: get
    assert tree.invariants(), "Invariants failed at step 3146"

    # Operation 3147: delete
    del tree[8500]
    del reference[8500]
    assert tree.invariants(), "Invariants failed at step 3147"

    # Operation 3148: get
    assert tree.invariants(), "Invariants failed at step 3148"

    # Operation 3149: get
    assert tree.invariants(), "Invariants failed at step 3149"

    # Operation 3150: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3150"

    # Operation 3151: insert
    tree[6215] = 'value_748766'
    reference[6215] = 'value_748766'
    assert tree.invariants(), "Invariants failed at step 3151"

    # Operation 3152: insert
    tree[877] = 'value_54900'
    reference[877] = 'value_54900'
    assert tree.invariants(), "Invariants failed at step 3152"

    # Operation 3153: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3153"

    # Operation 3154: get
    assert tree.invariants(), "Invariants failed at step 3154"

    # Operation 3155: delete
    del tree[6215]
    del reference[6215]
    assert tree.invariants(), "Invariants failed at step 3155"

    # Operation 3156: insert
    tree[4966] = 'value_908161'
    reference[4966] = 'value_908161'
    assert tree.invariants(), "Invariants failed at step 3156"

    # Operation 3157: get
    assert tree.invariants(), "Invariants failed at step 3157"

    # Operation 3158: delete
    del tree[877]
    del reference[877]
    assert tree.invariants(), "Invariants failed at step 3158"

    # Operation 3159: insert
    tree[8076] = 'value_926108'
    reference[8076] = 'value_926108'
    assert tree.invariants(), "Invariants failed at step 3159"

    # Operation 3160: get
    assert tree.invariants(), "Invariants failed at step 3160"

    # Operation 3161: update
    tree[8076] = 'value_607818'
    reference[8076] = 'value_607818'
    assert tree.invariants(), "Invariants failed at step 3161"

    # Operation 3162: get
    assert tree.invariants(), "Invariants failed at step 3162"

    # Operation 3163: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3163"

    # Operation 3164: insert
    tree[5362] = 'value_201810'
    reference[5362] = 'value_201810'
    assert tree.invariants(), "Invariants failed at step 3164"

    # Operation 3165: delete
    del tree[8076]
    del reference[8076]
    assert tree.invariants(), "Invariants failed at step 3165"

    # Operation 3166: get
    assert tree.invariants(), "Invariants failed at step 3166"

    # Operation 3167: insert
    tree[8243] = 'value_810803'
    reference[8243] = 'value_810803'
    assert tree.invariants(), "Invariants failed at step 3167"

    # Operation 3168: insert
    tree[5361] = 'value_111651'
    reference[5361] = 'value_111651'
    assert tree.invariants(), "Invariants failed at step 3168"

    # Operation 3169: delete
    del tree[8243]
    del reference[8243]
    assert tree.invariants(), "Invariants failed at step 3169"

    # Operation 3170: delete
    del tree[5361]
    del reference[5361]
    assert tree.invariants(), "Invariants failed at step 3170"

    # Operation 3171: insert
    tree[9247] = 'value_174717'
    reference[9247] = 'value_174717'
    assert tree.invariants(), "Invariants failed at step 3171"

    # Operation 3172: update
    tree[4966] = 'value_219130'
    reference[4966] = 'value_219130'
    assert tree.invariants(), "Invariants failed at step 3172"

    # Operation 3173: get
    assert tree.invariants(), "Invariants failed at step 3173"

    # Operation 3174: get
    assert tree.invariants(), "Invariants failed at step 3174"

    # Operation 3175: delete
    del tree[5362]
    del reference[5362]
    assert tree.invariants(), "Invariants failed at step 3175"

    # Operation 3176: update
    tree[9247] = 'value_351860'
    reference[9247] = 'value_351860'
    assert tree.invariants(), "Invariants failed at step 3176"

    # Operation 3177: delete
    del tree[4966]
    del reference[4966]
    assert tree.invariants(), "Invariants failed at step 3177"

    # Operation 3178: update
    tree[9247] = 'value_454997'
    reference[9247] = 'value_454997'
    assert tree.invariants(), "Invariants failed at step 3178"

    # Operation 3179: delete
    del tree[9247]
    del reference[9247]
    assert tree.invariants(), "Invariants failed at step 3179"

    # Operation 3180: insert
    tree[896] = 'value_66251'
    reference[896] = 'value_66251'
    assert tree.invariants(), "Invariants failed at step 3180"

    # Operation 3181: get
    assert tree.invariants(), "Invariants failed at step 3181"

    # Operation 3182: insert
    tree[2030] = 'value_146111'
    reference[2030] = 'value_146111'
    assert tree.invariants(), "Invariants failed at step 3182"

    # Operation 3183: insert
    tree[208] = 'value_784583'
    reference[208] = 'value_784583'
    assert tree.invariants(), "Invariants failed at step 3183"

    # Operation 3184: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3184"

    # Operation 3185: delete
    del tree[2030]
    del reference[2030]
    assert tree.invariants(), "Invariants failed at step 3185"

    # Operation 3186: get
    assert tree.invariants(), "Invariants failed at step 3186"

    # Operation 3187: delete
    del tree[208]
    del reference[208]
    assert tree.invariants(), "Invariants failed at step 3187"

    # Operation 3188: delete
    del tree[896]
    del reference[896]
    assert tree.invariants(), "Invariants failed at step 3188"

    # Operation 3189: get
    assert tree.invariants(), "Invariants failed at step 3189"

    # Operation 3190: insert
    tree[316] = 'value_412692'
    reference[316] = 'value_412692'
    assert tree.invariants(), "Invariants failed at step 3190"

    # Operation 3191: get
    assert tree.invariants(), "Invariants failed at step 3191"

    # Operation 3192: insert
    tree[8332] = 'value_196318'
    reference[8332] = 'value_196318'
    assert tree.invariants(), "Invariants failed at step 3192"

    # Operation 3193: delete
    del tree[8332]
    del reference[8332]
    assert tree.invariants(), "Invariants failed at step 3193"

    # Operation 3194: insert
    tree[9931] = 'value_626280'
    reference[9931] = 'value_626280'
    assert tree.invariants(), "Invariants failed at step 3194"

    # Operation 3195: get
    assert tree.invariants(), "Invariants failed at step 3195"

    # Operation 3196: delete
    del tree[9931]
    del reference[9931]
    assert tree.invariants(), "Invariants failed at step 3196"

    # Operation 3197: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3197"

    # Operation 3198: delete
    del tree[316]
    del reference[316]
    assert tree.invariants(), "Invariants failed at step 3198"

    # Operation 3199: insert
    tree[2889] = 'value_503388'
    reference[2889] = 'value_503388'
    assert tree.invariants(), "Invariants failed at step 3199"

    # Operation 3200: insert
    tree[1827] = 'value_97298'
    reference[1827] = 'value_97298'
    assert tree.invariants(), "Invariants failed at step 3200"

    # Operation 3201: delete
    del tree[2889]
    del reference[2889]
    assert tree.invariants(), "Invariants failed at step 3201"

    # Operation 3202: update
    tree[1827] = 'value_309500'
    reference[1827] = 'value_309500'
    assert tree.invariants(), "Invariants failed at step 3202"

    # Operation 3203: delete
    del tree[1827]
    del reference[1827]
    assert tree.invariants(), "Invariants failed at step 3203"

    # Operation 3204: insert
    tree[7140] = 'value_810623'
    reference[7140] = 'value_810623'
    assert tree.invariants(), "Invariants failed at step 3204"

    # Operation 3205: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3205"

    # Operation 3206: insert
    tree[4480] = 'value_295513'
    reference[4480] = 'value_295513'
    assert tree.invariants(), "Invariants failed at step 3206"

    # Operation 3207: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3207"

    # Operation 3208: delete
    del tree[4480]
    del reference[4480]
    assert tree.invariants(), "Invariants failed at step 3208"

    # Operation 3209: insert
    tree[8460] = 'value_313759'
    reference[8460] = 'value_313759'
    assert tree.invariants(), "Invariants failed at step 3209"

    # Operation 3210: update
    tree[8460] = 'value_640349'
    reference[8460] = 'value_640349'
    assert tree.invariants(), "Invariants failed at step 3210"

    # Operation 3211: delete
    del tree[7140]
    del reference[7140]
    assert tree.invariants(), "Invariants failed at step 3211"

    # Operation 3212: delete
    del tree[8460]
    del reference[8460]
    assert tree.invariants(), "Invariants failed at step 3212"

    # Operation 3213: insert
    tree[2275] = 'value_20498'
    reference[2275] = 'value_20498'
    assert tree.invariants(), "Invariants failed at step 3213"

    # Operation 3214: delete
    del tree[2275]
    del reference[2275]
    assert tree.invariants(), "Invariants failed at step 3214"

    # Operation 3215: insert
    tree[6705] = 'value_630029'
    reference[6705] = 'value_630029'
    assert tree.invariants(), "Invariants failed at step 3215"

    # Operation 3216: get
    assert tree.invariants(), "Invariants failed at step 3216"

    # Operation 3217: delete
    del tree[6705]
    del reference[6705]
    assert tree.invariants(), "Invariants failed at step 3217"

    # Operation 3218: get
    assert tree.invariants(), "Invariants failed at step 3218"

    # Operation 3219: insert
    tree[6483] = 'value_398923'
    reference[6483] = 'value_398923'
    assert tree.invariants(), "Invariants failed at step 3219"

    # Operation 3220: insert
    tree[1877] = 'value_483975'
    reference[1877] = 'value_483975'
    assert tree.invariants(), "Invariants failed at step 3220"

    # Operation 3221: delete
    del tree[6483]
    del reference[6483]
    assert tree.invariants(), "Invariants failed at step 3221"

    # Operation 3222: insert
    tree[7009] = 'value_602956'
    reference[7009] = 'value_602956'
    assert tree.invariants(), "Invariants failed at step 3222"

    # Operation 3223: insert
    tree[3754] = 'value_787663'
    reference[3754] = 'value_787663'
    assert tree.invariants(), "Invariants failed at step 3223"

    # Operation 3224: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3224"

    # Operation 3225: update
    tree[3754] = 'value_663678'
    reference[3754] = 'value_663678'
    assert tree.invariants(), "Invariants failed at step 3225"

    # Operation 3226: insert
    tree[4621] = 'value_130285'
    reference[4621] = 'value_130285'
    assert tree.invariants(), "Invariants failed at step 3226"

    # Operation 3227: insert
    tree[2713] = 'value_862769'
    reference[2713] = 'value_862769'
    assert tree.invariants(), "Invariants failed at step 3227"

    # Operation 3228: get
    assert tree.invariants(), "Invariants failed at step 3228"

    # Operation 3229: insert
    tree[613] = 'value_221350'
    reference[613] = 'value_221350'
    assert tree.invariants(), "Invariants failed at step 3229"

    # Operation 3230: batch_delete
    keys_to_delete = [3754, 7455, 1877, 4621]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3230"

    # Operation 3231: delete
    del tree[7009]
    del reference[7009]
    assert tree.invariants(), "Invariants failed at step 3231"

    # Operation 3232: insert
    tree[1877] = 'value_879418'
    reference[1877] = 'value_879418'
    assert tree.invariants(), "Invariants failed at step 3232"

    # Operation 3233: get
    assert tree.invariants(), "Invariants failed at step 3233"

    # Operation 3234: insert
    tree[1513] = 'value_706049'
    reference[1513] = 'value_706049'
    assert tree.invariants(), "Invariants failed at step 3234"

    # Operation 3235: insert
    tree[1232] = 'value_501548'
    reference[1232] = 'value_501548'
    assert tree.invariants(), "Invariants failed at step 3235"

    # Operation 3236: get
    assert tree.invariants(), "Invariants failed at step 3236"

    # Operation 3237: delete
    del tree[1877]
    del reference[1877]
    assert tree.invariants(), "Invariants failed at step 3237"

    # Operation 3238: delete
    del tree[1232]
    del reference[1232]
    assert tree.invariants(), "Invariants failed at step 3238"

    # Operation 3239: delete
    del tree[613]
    del reference[613]
    assert tree.invariants(), "Invariants failed at step 3239"

    # Operation 3240: delete
    del tree[2713]
    del reference[2713]
    assert tree.invariants(), "Invariants failed at step 3240"

    # Operation 3241: get
    assert tree.invariants(), "Invariants failed at step 3241"

    # Operation 3242: get
    assert tree.invariants(), "Invariants failed at step 3242"

    # Operation 3243: delete
    del tree[1513]
    del reference[1513]
    assert tree.invariants(), "Invariants failed at step 3243"

    # Operation 3244: insert
    tree[2774] = 'value_55935'
    reference[2774] = 'value_55935'
    assert tree.invariants(), "Invariants failed at step 3244"

    # Operation 3245: update
    tree[2774] = 'value_912346'
    reference[2774] = 'value_912346'
    assert tree.invariants(), "Invariants failed at step 3245"

    # Operation 3246: insert
    tree[3448] = 'value_933007'
    reference[3448] = 'value_933007'
    assert tree.invariants(), "Invariants failed at step 3246"

    # Operation 3247: get
    assert tree.invariants(), "Invariants failed at step 3247"

    # Operation 3248: delete
    del tree[3448]
    del reference[3448]
    assert tree.invariants(), "Invariants failed at step 3248"

    # Operation 3249: insert
    tree[8259] = 'value_961988'
    reference[8259] = 'value_961988'
    assert tree.invariants(), "Invariants failed at step 3249"

    # Operation 3250: delete
    del tree[8259]
    del reference[8259]
    assert tree.invariants(), "Invariants failed at step 3250"

    # Operation 3251: get
    assert tree.invariants(), "Invariants failed at step 3251"

    # Operation 3252: insert
    tree[2277] = 'value_364305'
    reference[2277] = 'value_364305'
    assert tree.invariants(), "Invariants failed at step 3252"

    # Operation 3253: delete
    del tree[2774]
    del reference[2774]
    assert tree.invariants(), "Invariants failed at step 3253"

    # Operation 3254: update
    tree[2277] = 'value_143944'
    reference[2277] = 'value_143944'
    assert tree.invariants(), "Invariants failed at step 3254"

    # Operation 3255: delete
    del tree[2277]
    del reference[2277]
    assert tree.invariants(), "Invariants failed at step 3255"

    # Operation 3256: insert
    tree[2780] = 'value_943454'
    reference[2780] = 'value_943454'
    assert tree.invariants(), "Invariants failed at step 3256"

    # Operation 3257: delete
    del tree[2780]
    del reference[2780]
    assert tree.invariants(), "Invariants failed at step 3257"

    # Operation 3258: insert
    tree[8275] = 'value_713582'
    reference[8275] = 'value_713582'
    assert tree.invariants(), "Invariants failed at step 3258"

    # Operation 3259: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3259"

    # Operation 3260: insert
    tree[7475] = 'value_701999'
    reference[7475] = 'value_701999'
    assert tree.invariants(), "Invariants failed at step 3260"

    # Operation 3261: insert
    tree[4557] = 'value_346800'
    reference[4557] = 'value_346800'
    assert tree.invariants(), "Invariants failed at step 3261"

    # Operation 3262: insert
    tree[5303] = 'value_570733'
    reference[5303] = 'value_570733'
    assert tree.invariants(), "Invariants failed at step 3262"

    # Operation 3263: insert
    tree[4550] = 'value_608832'
    reference[4550] = 'value_608832'
    assert tree.invariants(), "Invariants failed at step 3263"

    # Operation 3264: get
    assert tree.invariants(), "Invariants failed at step 3264"

    # Operation 3265: update
    tree[5303] = 'value_588456'
    reference[5303] = 'value_588456'
    assert tree.invariants(), "Invariants failed at step 3265"

    # Operation 3266: batch_delete
    keys_to_delete = [2633, 548, 4557, 5303]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3266"

    # Operation 3267: insert
    tree[6729] = 'value_758824'
    reference[6729] = 'value_758824'
    assert tree.invariants(), "Invariants failed at step 3267"

    # Operation 3268: insert
    tree[1867] = 'value_430993'
    reference[1867] = 'value_430993'
    assert tree.invariants(), "Invariants failed at step 3268"

    # Operation 3269: batch_delete
    keys_to_delete = [8409, 1867, 7475, 4550]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3269"

    # Operation 3270: get
    assert tree.invariants(), "Invariants failed at step 3270"

    # Operation 3271: delete
    del tree[8275]
    del reference[8275]
    assert tree.invariants(), "Invariants failed at step 3271"

    # Operation 3272: update
    tree[6729] = 'value_367612'
    reference[6729] = 'value_367612'
    assert tree.invariants(), "Invariants failed at step 3272"

    # Operation 3273: insert
    tree[5137] = 'value_245978'
    reference[5137] = 'value_245978'
    assert tree.invariants(), "Invariants failed at step 3273"

    # Operation 3274: update
    tree[6729] = 'value_29558'
    reference[6729] = 'value_29558'
    assert tree.invariants(), "Invariants failed at step 3274"

    # Operation 3275: delete
    del tree[6729]
    del reference[6729]
    assert tree.invariants(), "Invariants failed at step 3275"

    # Operation 3276: delete
    del tree[5137]
    del reference[5137]
    assert tree.invariants(), "Invariants failed at step 3276"

    # Operation 3277: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3277"

    # Operation 3278: insert
    tree[2558] = 'value_516616'
    reference[2558] = 'value_516616'
    assert tree.invariants(), "Invariants failed at step 3278"

    # Operation 3279: get
    assert tree.invariants(), "Invariants failed at step 3279"

    # Operation 3280: update
    tree[2558] = 'value_271690'
    reference[2558] = 'value_271690'
    assert tree.invariants(), "Invariants failed at step 3280"

    # Operation 3281: get
    assert tree.invariants(), "Invariants failed at step 3281"

    # Operation 3282: update
    tree[2558] = 'value_181050'
    reference[2558] = 'value_181050'
    assert tree.invariants(), "Invariants failed at step 3282"

    # Operation 3283: delete
    del tree[2558]
    del reference[2558]
    assert tree.invariants(), "Invariants failed at step 3283"

    # Operation 3284: insert
    tree[8048] = 'value_237653'
    reference[8048] = 'value_237653'
    assert tree.invariants(), "Invariants failed at step 3284"

    # Operation 3285: insert
    tree[3955] = 'value_26786'
    reference[3955] = 'value_26786'
    assert tree.invariants(), "Invariants failed at step 3285"

    # Operation 3286: delete
    del tree[3955]
    del reference[3955]
    assert tree.invariants(), "Invariants failed at step 3286"

    # Operation 3287: get
    assert tree.invariants(), "Invariants failed at step 3287"

    # Operation 3288: insert
    tree[715] = 'value_82291'
    reference[715] = 'value_82291'
    assert tree.invariants(), "Invariants failed at step 3288"

    # Operation 3289: delete
    del tree[715]
    del reference[715]
    assert tree.invariants(), "Invariants failed at step 3289"

    # Operation 3290: update
    tree[8048] = 'value_794735'
    reference[8048] = 'value_794735'
    assert tree.invariants(), "Invariants failed at step 3290"

    # Operation 3291: insert
    tree[3003] = 'value_490718'
    reference[3003] = 'value_490718'
    assert tree.invariants(), "Invariants failed at step 3291"

    # Operation 3292: get
    assert tree.invariants(), "Invariants failed at step 3292"

    # Operation 3293: update
    tree[8048] = 'value_890036'
    reference[8048] = 'value_890036'
    assert tree.invariants(), "Invariants failed at step 3293"

    # Operation 3294: insert
    tree[5242] = 'value_729246'
    reference[5242] = 'value_729246'
    assert tree.invariants(), "Invariants failed at step 3294"

    # Operation 3295: get
    assert tree.invariants(), "Invariants failed at step 3295"

    # Operation 3296: insert
    tree[8964] = 'value_834577'
    reference[8964] = 'value_834577'
    assert tree.invariants(), "Invariants failed at step 3296"

    # Operation 3297: get
    assert tree.invariants(), "Invariants failed at step 3297"

    # Operation 3298: delete
    del tree[8964]
    del reference[8964]
    assert tree.invariants(), "Invariants failed at step 3298"

    # Operation 3299: update
    tree[8048] = 'value_754992'
    reference[8048] = 'value_754992'
    assert tree.invariants(), "Invariants failed at step 3299"

    # Operation 3300: get
    assert tree.invariants(), "Invariants failed at step 3300"

    # Operation 3301: get
    assert tree.invariants(), "Invariants failed at step 3301"

    # Operation 3302: delete
    del tree[8048]
    del reference[8048]
    assert tree.invariants(), "Invariants failed at step 3302"

    # Operation 3303: insert
    tree[8097] = 'value_593189'
    reference[8097] = 'value_593189'
    assert tree.invariants(), "Invariants failed at step 3303"

    # Operation 3304: delete
    del tree[8097]
    del reference[8097]
    assert tree.invariants(), "Invariants failed at step 3304"

    # Operation 3305: insert
    tree[9469] = 'value_171541'
    reference[9469] = 'value_171541'
    assert tree.invariants(), "Invariants failed at step 3305"

    # Operation 3306: delete
    del tree[5242]
    del reference[5242]
    assert tree.invariants(), "Invariants failed at step 3306"

    # Operation 3307: get
    assert tree.invariants(), "Invariants failed at step 3307"

    # Operation 3308: insert
    tree[6803] = 'value_579595'
    reference[6803] = 'value_579595'
    assert tree.invariants(), "Invariants failed at step 3308"

    # Operation 3309: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3309"

    # Operation 3310: insert
    tree[2034] = 'value_253686'
    reference[2034] = 'value_253686'
    assert tree.invariants(), "Invariants failed at step 3310"

    # Operation 3311: update
    tree[9469] = 'value_811792'
    reference[9469] = 'value_811792'
    assert tree.invariants(), "Invariants failed at step 3311"

    # Operation 3312: delete
    del tree[6803]
    del reference[6803]
    assert tree.invariants(), "Invariants failed at step 3312"

    # Operation 3313: insert
    tree[2701] = 'value_256312'
    reference[2701] = 'value_256312'
    assert tree.invariants(), "Invariants failed at step 3313"

    # Operation 3314: get
    assert tree.invariants(), "Invariants failed at step 3314"

    # Operation 3315: delete
    del tree[3003]
    del reference[3003]
    assert tree.invariants(), "Invariants failed at step 3315"

    # Operation 3316: get
    assert tree.invariants(), "Invariants failed at step 3316"

    # Operation 3317: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3317"

    # Operation 3318: delete
    del tree[2034]
    del reference[2034]
    assert tree.invariants(), "Invariants failed at step 3318"

    # Operation 3319: delete
    del tree[2701]
    del reference[2701]
    assert tree.invariants(), "Invariants failed at step 3319"

    # Operation 3320: delete
    del tree[9469]
    del reference[9469]
    assert tree.invariants(), "Invariants failed at step 3320"

    # Operation 3321: insert
    tree[3470] = 'value_357521'
    reference[3470] = 'value_357521'
    assert tree.invariants(), "Invariants failed at step 3321"

    # Operation 3322: insert
    tree[2001] = 'value_26942'
    reference[2001] = 'value_26942'
    assert tree.invariants(), "Invariants failed at step 3322"

    # Operation 3323: insert
    tree[6850] = 'value_946039'
    reference[6850] = 'value_946039'
    assert tree.invariants(), "Invariants failed at step 3323"

    # Operation 3324: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3324"

    # Operation 3325: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3325"

    # Operation 3326: insert
    tree[1278] = 'value_861639'
    reference[1278] = 'value_861639'
    assert tree.invariants(), "Invariants failed at step 3326"

    # Operation 3327: get
    assert tree.invariants(), "Invariants failed at step 3327"

    # Operation 3328: delete
    del tree[1278]
    del reference[1278]
    assert tree.invariants(), "Invariants failed at step 3328"

    # Operation 3329: insert
    tree[6994] = 'value_912672'
    reference[6994] = 'value_912672'
    assert tree.invariants(), "Invariants failed at step 3329"

    # Operation 3330: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3330"

    # Operation 3331: insert
    tree[5603] = 'value_518129'
    reference[5603] = 'value_518129'
    assert tree.invariants(), "Invariants failed at step 3331"

    # Operation 3332: get
    assert tree.invariants(), "Invariants failed at step 3332"

    # Operation 3333: update
    tree[6850] = 'value_544191'
    reference[6850] = 'value_544191'
    assert tree.invariants(), "Invariants failed at step 3333"

    # Operation 3334: get
    assert tree.invariants(), "Invariants failed at step 3334"

    # Operation 3335: insert
    tree[346] = 'value_365658'
    reference[346] = 'value_365658'
    assert tree.invariants(), "Invariants failed at step 3335"

    # Operation 3336: get
    assert tree.invariants(), "Invariants failed at step 3336"

    # Operation 3337: insert
    tree[6323] = 'value_974117'
    reference[6323] = 'value_974117'
    assert tree.invariants(), "Invariants failed at step 3337"

    # Operation 3338: delete
    del tree[2001]
    del reference[2001]
    assert tree.invariants(), "Invariants failed at step 3338"

    # Operation 3339: update
    tree[3470] = 'value_290978'
    reference[3470] = 'value_290978'
    assert tree.invariants(), "Invariants failed at step 3339"

    # Operation 3340: insert
    tree[4810] = 'value_764692'
    reference[4810] = 'value_764692'
    assert tree.invariants(), "Invariants failed at step 3340"

    # Operation 3341: batch_delete
    keys_to_delete = [3470, 3793, 6994, 6323, 2462]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3341"

    # Operation 3342: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3342"

    # Operation 3343: insert
    tree[3344] = 'value_318631'
    reference[3344] = 'value_318631'
    assert tree.invariants(), "Invariants failed at step 3343"

    # Operation 3344: insert
    tree[290] = 'value_798543'
    reference[290] = 'value_798543'
    assert tree.invariants(), "Invariants failed at step 3344"

    # Operation 3345: update
    tree[3344] = 'value_870110'
    reference[3344] = 'value_870110'
    assert tree.invariants(), "Invariants failed at step 3345"

    # Operation 3346: insert
    tree[3099] = 'value_332782'
    reference[3099] = 'value_332782'
    assert tree.invariants(), "Invariants failed at step 3346"

    # Operation 3347: update
    tree[5603] = 'value_406068'
    reference[5603] = 'value_406068'
    assert tree.invariants(), "Invariants failed at step 3347"

    # Operation 3348: insert
    tree[7360] = 'value_150563'
    reference[7360] = 'value_150563'
    assert tree.invariants(), "Invariants failed at step 3348"

    # Operation 3349: get
    assert tree.invariants(), "Invariants failed at step 3349"

    # Operation 3350: insert
    tree[975] = 'value_934053'
    reference[975] = 'value_934053'
    assert tree.invariants(), "Invariants failed at step 3350"

    # Operation 3351: insert
    tree[5162] = 'value_14228'
    reference[5162] = 'value_14228'
    assert tree.invariants(), "Invariants failed at step 3351"

    # Operation 3352: insert
    tree[2626] = 'value_79756'
    reference[2626] = 'value_79756'
    assert tree.invariants(), "Invariants failed at step 3352"

    # Operation 3353: get
    assert tree.invariants(), "Invariants failed at step 3353"

    # Operation 3354: update
    tree[6850] = 'value_790427'
    reference[6850] = 'value_790427'
    assert tree.invariants(), "Invariants failed at step 3354"

    # Operation 3355: update
    tree[4810] = 'value_580930'
    reference[4810] = 'value_580930'
    assert tree.invariants(), "Invariants failed at step 3355"

    # Operation 3356: get
    assert tree.invariants(), "Invariants failed at step 3356"

    # Operation 3357: get
    assert tree.invariants(), "Invariants failed at step 3357"

    # Operation 3358: delete
    del tree[2626]
    del reference[2626]
    assert tree.invariants(), "Invariants failed at step 3358"

    # Operation 3359: delete
    del tree[5603]
    del reference[5603]
    assert tree.invariants(), "Invariants failed at step 3359"

    # Operation 3360: insert
    tree[8842] = 'value_390053'
    reference[8842] = 'value_390053'
    assert tree.invariants(), "Invariants failed at step 3360"

    # Operation 3361: insert
    tree[6557] = 'value_346429'
    reference[6557] = 'value_346429'
    assert tree.invariants(), "Invariants failed at step 3361"

    # Operation 3362: get
    assert tree.invariants(), "Invariants failed at step 3362"

    # Operation 3363: delete
    del tree[5162]
    del reference[5162]
    assert tree.invariants(), "Invariants failed at step 3363"

    # Operation 3364: delete
    del tree[6850]
    del reference[6850]
    assert tree.invariants(), "Invariants failed at step 3364"

    # Operation 3365: delete
    del tree[3344]
    del reference[3344]
    assert tree.invariants(), "Invariants failed at step 3365"

    # Operation 3366: get
    assert tree.invariants(), "Invariants failed at step 3366"

    # Operation 3367: insert
    tree[7299] = 'value_840909'
    reference[7299] = 'value_840909'
    assert tree.invariants(), "Invariants failed at step 3367"

    # Operation 3368: get
    assert tree.invariants(), "Invariants failed at step 3368"

    # Operation 3369: batch_delete
    keys_to_delete = [7360, 3099, 1420, 9910]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3369"

    # Operation 3370: insert
    tree[1380] = 'value_923516'
    reference[1380] = 'value_923516'
    assert tree.invariants(), "Invariants failed at step 3370"

    # Operation 3371: insert
    tree[3412] = 'value_564084'
    reference[3412] = 'value_564084'
    assert tree.invariants(), "Invariants failed at step 3371"

    # Operation 3372: batch_delete
    keys_to_delete = [2497, 290, 1235, 3412]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3372"

    # Operation 3373: get
    assert tree.invariants(), "Invariants failed at step 3373"

    # Operation 3374: update
    tree[8842] = 'value_706940'
    reference[8842] = 'value_706940'
    assert tree.invariants(), "Invariants failed at step 3374"

    # Operation 3375: insert
    tree[1635] = 'value_215725'
    reference[1635] = 'value_215725'
    assert tree.invariants(), "Invariants failed at step 3375"

    # Operation 3376: insert
    tree[8886] = 'value_757680'
    reference[8886] = 'value_757680'
    assert tree.invariants(), "Invariants failed at step 3376"

    # Operation 3377: delete
    del tree[8842]
    del reference[8842]
    assert tree.invariants(), "Invariants failed at step 3377"

    # Operation 3378: get
    assert tree.invariants(), "Invariants failed at step 3378"

    # Operation 3379: delete
    del tree[6557]
    del reference[6557]
    assert tree.invariants(), "Invariants failed at step 3379"

    # Operation 3380: get
    assert tree.invariants(), "Invariants failed at step 3380"

    # Operation 3381: insert
    tree[8637] = 'value_804252'
    reference[8637] = 'value_804252'
    assert tree.invariants(), "Invariants failed at step 3381"

    # Operation 3382: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3382"

    # Operation 3383: delete
    del tree[346]
    del reference[346]
    assert tree.invariants(), "Invariants failed at step 3383"

    # Operation 3384: update
    tree[4810] = 'value_316321'
    reference[4810] = 'value_316321'
    assert tree.invariants(), "Invariants failed at step 3384"

    # Operation 3385: get
    assert tree.invariants(), "Invariants failed at step 3385"

    # Operation 3386: insert
    tree[5629] = 'value_556239'
    reference[5629] = 'value_556239'
    assert tree.invariants(), "Invariants failed at step 3386"

    # Operation 3387: delete
    del tree[8637]
    del reference[8637]
    assert tree.invariants(), "Invariants failed at step 3387"

    # Operation 3388: delete
    del tree[4810]
    del reference[4810]
    assert tree.invariants(), "Invariants failed at step 3388"

    # Operation 3389: batch_delete
    keys_to_delete = [1635, 7299, 1380, 5352, 1213]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3389"

    # Operation 3390: delete
    del tree[975]
    del reference[975]
    assert tree.invariants(), "Invariants failed at step 3390"

    # Operation 3391: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3391"

    # Operation 3392: insert
    tree[6237] = 'value_249266'
    reference[6237] = 'value_249266'
    assert tree.invariants(), "Invariants failed at step 3392"

    # Operation 3393: insert
    tree[5474] = 'value_116226'
    reference[5474] = 'value_116226'
    assert tree.invariants(), "Invariants failed at step 3393"

    # Operation 3394: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3394"

    # Operation 3395: delete
    del tree[8886]
    del reference[8886]
    assert tree.invariants(), "Invariants failed at step 3395"

    # Operation 3396: insert
    tree[3656] = 'value_759970'
    reference[3656] = 'value_759970'
    assert tree.invariants(), "Invariants failed at step 3396"

    # Operation 3397: insert
    tree[3019] = 'value_479249'
    reference[3019] = 'value_479249'
    assert tree.invariants(), "Invariants failed at step 3397"

    # Operation 3398: delete
    del tree[3656]
    del reference[3656]
    assert tree.invariants(), "Invariants failed at step 3398"

    # Operation 3399: insert
    tree[3361] = 'value_758180'
    reference[3361] = 'value_758180'
    assert tree.invariants(), "Invariants failed at step 3399"

    # Operation 3400: update
    tree[5474] = 'value_624342'
    reference[5474] = 'value_624342'
    assert tree.invariants(), "Invariants failed at step 3400"

    # Operation 3401: insert
    tree[7176] = 'value_364249'
    reference[7176] = 'value_364249'
    assert tree.invariants(), "Invariants failed at step 3401"

    # Operation 3402: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3402"

    # Operation 3403: delete
    del tree[6237]
    del reference[6237]
    assert tree.invariants(), "Invariants failed at step 3403"

    # Operation 3404: insert
    tree[1518] = 'value_528536'
    reference[1518] = 'value_528536'
    assert tree.invariants(), "Invariants failed at step 3404"

    # Operation 3405: get
    assert tree.invariants(), "Invariants failed at step 3405"

    # Operation 3406: insert
    tree[9578] = 'value_102003'
    reference[9578] = 'value_102003'
    assert tree.invariants(), "Invariants failed at step 3406"

    # Operation 3407: delete
    del tree[5629]
    del reference[5629]
    assert tree.invariants(), "Invariants failed at step 3407"

    # Operation 3408: delete
    del tree[3019]
    del reference[3019]
    assert tree.invariants(), "Invariants failed at step 3408"

    # Operation 3409: delete
    del tree[7176]
    del reference[7176]
    assert tree.invariants(), "Invariants failed at step 3409"

    # Operation 3410: update
    tree[9578] = 'value_318245'
    reference[9578] = 'value_318245'
    assert tree.invariants(), "Invariants failed at step 3410"

    # Operation 3411: insert
    tree[7088] = 'value_265862'
    reference[7088] = 'value_265862'
    assert tree.invariants(), "Invariants failed at step 3411"

    # Operation 3412: insert
    tree[7220] = 'value_888048'
    reference[7220] = 'value_888048'
    assert tree.invariants(), "Invariants failed at step 3412"

    # Operation 3413: delete
    del tree[3361]
    del reference[3361]
    assert tree.invariants(), "Invariants failed at step 3413"

    # Operation 3414: update
    tree[5474] = 'value_409070'
    reference[5474] = 'value_409070'
    assert tree.invariants(), "Invariants failed at step 3414"

    # Operation 3415: insert
    tree[2657] = 'value_361803'
    reference[2657] = 'value_361803'
    assert tree.invariants(), "Invariants failed at step 3415"

    # Operation 3416: insert
    tree[9096] = 'value_270835'
    reference[9096] = 'value_270835'
    assert tree.invariants(), "Invariants failed at step 3416"

    # Operation 3417: update
    tree[7088] = 'value_595849'
    reference[7088] = 'value_595849'
    assert tree.invariants(), "Invariants failed at step 3417"

    # Operation 3418: delete
    del tree[9578]
    del reference[9578]
    assert tree.invariants(), "Invariants failed at step 3418"

    # Operation 3419: delete
    del tree[7220]
    del reference[7220]
    assert tree.invariants(), "Invariants failed at step 3419"

    # Operation 3420: get
    assert tree.invariants(), "Invariants failed at step 3420"

    # Operation 3421: delete
    del tree[7088]
    del reference[7088]
    assert tree.invariants(), "Invariants failed at step 3421"

    # Operation 3422: update
    tree[1518] = 'value_145716'
    reference[1518] = 'value_145716'
    assert tree.invariants(), "Invariants failed at step 3422"

    # Operation 3423: get
    assert tree.invariants(), "Invariants failed at step 3423"

    # Operation 3424: insert
    tree[4898] = 'value_452978'
    reference[4898] = 'value_452978'
    assert tree.invariants(), "Invariants failed at step 3424"

    # Operation 3425: get
    assert tree.invariants(), "Invariants failed at step 3425"

    # Operation 3426: delete
    del tree[4898]
    del reference[4898]
    assert tree.invariants(), "Invariants failed at step 3426"

    # Operation 3427: get
    assert tree.invariants(), "Invariants failed at step 3427"

    # Operation 3428: delete
    del tree[5474]
    del reference[5474]
    assert tree.invariants(), "Invariants failed at step 3428"

    # Operation 3429: delete
    del tree[2657]
    del reference[2657]
    assert tree.invariants(), "Invariants failed at step 3429"

    # Operation 3430: update
    tree[9096] = 'value_434768'
    reference[9096] = 'value_434768'
    assert tree.invariants(), "Invariants failed at step 3430"

    # Operation 3431: get
    assert tree.invariants(), "Invariants failed at step 3431"

    # Operation 3432: insert
    tree[8409] = 'value_695574'
    reference[8409] = 'value_695574'
    assert tree.invariants(), "Invariants failed at step 3432"

    # Operation 3433: delete
    del tree[1518]
    del reference[1518]
    assert tree.invariants(), "Invariants failed at step 3433"

    # Operation 3434: insert
    tree[4844] = 'value_423737'
    reference[4844] = 'value_423737'
    assert tree.invariants(), "Invariants failed at step 3434"

    # Operation 3435: insert
    tree[820] = 'value_793806'
    reference[820] = 'value_793806'
    assert tree.invariants(), "Invariants failed at step 3435"

    # Operation 3436: delete
    del tree[8409]
    del reference[8409]
    assert tree.invariants(), "Invariants failed at step 3436"

    # Operation 3437: delete
    del tree[9096]
    del reference[9096]
    assert tree.invariants(), "Invariants failed at step 3437"

    # Operation 3438: delete
    del tree[820]
    del reference[820]
    assert tree.invariants(), "Invariants failed at step 3438"

    # Operation 3439: delete
    del tree[4844]
    del reference[4844]
    assert tree.invariants(), "Invariants failed at step 3439"

    # Operation 3440: insert
    tree[4748] = 'value_78379'
    reference[4748] = 'value_78379'
    assert tree.invariants(), "Invariants failed at step 3440"

    # Operation 3441: insert
    tree[4519] = 'value_802196'
    reference[4519] = 'value_802196'
    assert tree.invariants(), "Invariants failed at step 3441"

    # Operation 3442: update
    tree[4519] = 'value_236355'
    reference[4519] = 'value_236355'
    assert tree.invariants(), "Invariants failed at step 3442"

    # Operation 3443: update
    tree[4748] = 'value_464186'
    reference[4748] = 'value_464186'
    assert tree.invariants(), "Invariants failed at step 3443"

    # Operation 3444: get
    assert tree.invariants(), "Invariants failed at step 3444"

    # Operation 3445: delete
    del tree[4519]
    del reference[4519]
    assert tree.invariants(), "Invariants failed at step 3445"

    # Operation 3446: update
    tree[4748] = 'value_254957'
    reference[4748] = 'value_254957'
    assert tree.invariants(), "Invariants failed at step 3446"

    # Operation 3447: insert
    tree[5553] = 'value_676553'
    reference[5553] = 'value_676553'
    assert tree.invariants(), "Invariants failed at step 3447"

    # Operation 3448: insert
    tree[6568] = 'value_694432'
    reference[6568] = 'value_694432'
    assert tree.invariants(), "Invariants failed at step 3448"

    # Operation 3449: insert
    tree[7293] = 'value_37621'
    reference[7293] = 'value_37621'
    assert tree.invariants(), "Invariants failed at step 3449"

    # Operation 3450: get
    assert tree.invariants(), "Invariants failed at step 3450"

    # Operation 3451: insert
    tree[1926] = 'value_175415'
    reference[1926] = 'value_175415'
    assert tree.invariants(), "Invariants failed at step 3451"

    # Operation 3452: batch_delete
    keys_to_delete = [1306, 1691, 4748, 7293]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3452"

    # Operation 3453: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3453"

    # Operation 3454: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3454"

    # Operation 3455: update
    tree[1926] = 'value_333975'
    reference[1926] = 'value_333975'
    assert tree.invariants(), "Invariants failed at step 3455"

    # Operation 3456: insert
    tree[1558] = 'value_301997'
    reference[1558] = 'value_301997'
    assert tree.invariants(), "Invariants failed at step 3456"

    # Operation 3457: delete
    del tree[1926]
    del reference[1926]
    assert tree.invariants(), "Invariants failed at step 3457"

    # Operation 3458: update
    tree[6568] = 'value_473656'
    reference[6568] = 'value_473656'
    assert tree.invariants(), "Invariants failed at step 3458"

    # Operation 3459: insert
    tree[3306] = 'value_626325'
    reference[3306] = 'value_626325'
    assert tree.invariants(), "Invariants failed at step 3459"

    # Operation 3460: insert
    tree[2540] = 'value_729189'
    reference[2540] = 'value_729189'
    assert tree.invariants(), "Invariants failed at step 3460"

    # Operation 3461: delete
    del tree[6568]
    del reference[6568]
    assert tree.invariants(), "Invariants failed at step 3461"

    # Operation 3462: delete
    del tree[1558]
    del reference[1558]
    assert tree.invariants(), "Invariants failed at step 3462"

    # Operation 3463: insert
    tree[8483] = 'value_173121'
    reference[8483] = 'value_173121'
    assert tree.invariants(), "Invariants failed at step 3463"

    # Operation 3464: delete
    del tree[8483]
    del reference[8483]
    assert tree.invariants(), "Invariants failed at step 3464"

    # Operation 3465: insert
    tree[1369] = 'value_624914'
    reference[1369] = 'value_624914'
    assert tree.invariants(), "Invariants failed at step 3465"

    # Operation 3466: update
    tree[1369] = 'value_131676'
    reference[1369] = 'value_131676'
    assert tree.invariants(), "Invariants failed at step 3466"

    # Operation 3467: insert
    tree[7698] = 'value_844960'
    reference[7698] = 'value_844960'
    assert tree.invariants(), "Invariants failed at step 3467"

    # Operation 3468: update
    tree[1369] = 'value_846701'
    reference[1369] = 'value_846701'
    assert tree.invariants(), "Invariants failed at step 3468"

    # Operation 3469: update
    tree[3306] = 'value_643668'
    reference[3306] = 'value_643668'
    assert tree.invariants(), "Invariants failed at step 3469"

    # Operation 3470: insert
    tree[437] = 'value_499145'
    reference[437] = 'value_499145'
    assert tree.invariants(), "Invariants failed at step 3470"

    # Operation 3471: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3471"

    # Operation 3472: batch_delete
    keys_to_delete = [7566, 2540, 437, 7526]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3472"

    # Operation 3473: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3473"

    # Operation 3474: get
    assert tree.invariants(), "Invariants failed at step 3474"

    # Operation 3475: update
    tree[3306] = 'value_220497'
    reference[3306] = 'value_220497'
    assert tree.invariants(), "Invariants failed at step 3475"

    # Operation 3476: get
    assert tree.invariants(), "Invariants failed at step 3476"

    # Operation 3477: insert
    tree[1503] = 'value_265863'
    reference[1503] = 'value_265863'
    assert tree.invariants(), "Invariants failed at step 3477"

    # Operation 3478: delete
    del tree[1369]
    del reference[1369]
    assert tree.invariants(), "Invariants failed at step 3478"

    # Operation 3479: delete
    del tree[7698]
    del reference[7698]
    assert tree.invariants(), "Invariants failed at step 3479"

    # Operation 3480: delete
    del tree[3306]
    del reference[3306]
    assert tree.invariants(), "Invariants failed at step 3480"

    # Operation 3481: update
    tree[1503] = 'value_766769'
    reference[1503] = 'value_766769'
    assert tree.invariants(), "Invariants failed at step 3481"

    # Operation 3482: delete
    del tree[5553]
    del reference[5553]
    assert tree.invariants(), "Invariants failed at step 3482"

    # Operation 3483: get
    assert tree.invariants(), "Invariants failed at step 3483"

    # Operation 3484: get
    assert tree.invariants(), "Invariants failed at step 3484"

    # Operation 3485: update
    tree[1503] = 'value_276781'
    reference[1503] = 'value_276781'
    assert tree.invariants(), "Invariants failed at step 3485"

    # Operation 3486: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3486"

    # Operation 3487: insert
    tree[1945] = 'value_31106'
    reference[1945] = 'value_31106'
    assert tree.invariants(), "Invariants failed at step 3487"

    # Operation 3488: insert
    tree[5713] = 'value_934301'
    reference[5713] = 'value_934301'
    assert tree.invariants(), "Invariants failed at step 3488"

    # Operation 3489: get
    assert tree.invariants(), "Invariants failed at step 3489"

    # Operation 3490: insert
    tree[595] = 'value_497335'
    reference[595] = 'value_497335'
    assert tree.invariants(), "Invariants failed at step 3490"

    # Operation 3491: delete
    del tree[5713]
    del reference[5713]
    assert tree.invariants(), "Invariants failed at step 3491"

    # Operation 3492: insert
    tree[8139] = 'value_779759'
    reference[8139] = 'value_779759'
    assert tree.invariants(), "Invariants failed at step 3492"

    # Operation 3493: get
    assert tree.invariants(), "Invariants failed at step 3493"

    # Operation 3494: delete
    del tree[1503]
    del reference[1503]
    assert tree.invariants(), "Invariants failed at step 3494"

    # Operation 3495: insert
    tree[4744] = 'value_485898'
    reference[4744] = 'value_485898'
    assert tree.invariants(), "Invariants failed at step 3495"

    # Operation 3496: update
    tree[4744] = 'value_921065'
    reference[4744] = 'value_921065'
    assert tree.invariants(), "Invariants failed at step 3496"

    # Operation 3497: insert
    tree[4834] = 'value_746330'
    reference[4834] = 'value_746330'
    assert tree.invariants(), "Invariants failed at step 3497"

    # Operation 3498: delete
    del tree[1945]
    del reference[1945]
    assert tree.invariants(), "Invariants failed at step 3498"

    # Operation 3499: get
    assert tree.invariants(), "Invariants failed at step 3499"

    # Operation 3500: get
    assert tree.invariants(), "Invariants failed at step 3500"

    # Operation 3501: insert
    tree[849] = 'value_441759'
    reference[849] = 'value_441759'
    assert tree.invariants(), "Invariants failed at step 3501"

    # Operation 3502: get
    assert tree.invariants(), "Invariants failed at step 3502"

    # Operation 3503: update
    tree[8139] = 'value_466136'
    reference[8139] = 'value_466136'
    assert tree.invariants(), "Invariants failed at step 3503"

    # Operation 3504: get
    assert tree.invariants(), "Invariants failed at step 3504"

    # Operation 3505: insert
    tree[4373] = 'value_313931'
    reference[4373] = 'value_313931'
    assert tree.invariants(), "Invariants failed at step 3505"

    # Operation 3506: delete
    del tree[849]
    del reference[849]
    assert tree.invariants(), "Invariants failed at step 3506"

    # Operation 3507: get
    assert tree.invariants(), "Invariants failed at step 3507"

    # Operation 3508: get
    assert tree.invariants(), "Invariants failed at step 3508"

    # Operation 3509: delete
    del tree[595]
    del reference[595]
    assert tree.invariants(), "Invariants failed at step 3509"

    # Operation 3510: delete
    del tree[8139]
    del reference[8139]
    assert tree.invariants(), "Invariants failed at step 3510"

    # Operation 3511: get
    assert tree.invariants(), "Invariants failed at step 3511"

    # Operation 3512: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3512"

    # Operation 3513: get
    assert tree.invariants(), "Invariants failed at step 3513"

    # Operation 3514: get
    assert tree.invariants(), "Invariants failed at step 3514"

    # Operation 3515: delete
    del tree[4834]
    del reference[4834]
    assert tree.invariants(), "Invariants failed at step 3515"

    # Operation 3516: delete
    del tree[4373]
    del reference[4373]
    assert tree.invariants(), "Invariants failed at step 3516"

    # Operation 3517: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3517"

    # Operation 3518: delete
    del tree[4744]
    del reference[4744]
    assert tree.invariants(), "Invariants failed at step 3518"

    # Operation 3519: insert
    tree[7666] = 'value_816234'
    reference[7666] = 'value_816234'
    assert tree.invariants(), "Invariants failed at step 3519"

    # Operation 3520: delete
    del tree[7666]
    del reference[7666]
    assert tree.invariants(), "Invariants failed at step 3520"

    # Operation 3521: insert
    tree[757] = 'value_800617'
    reference[757] = 'value_800617'
    assert tree.invariants(), "Invariants failed at step 3521"

    # Operation 3522: insert
    tree[2559] = 'value_432403'
    reference[2559] = 'value_432403'
    assert tree.invariants(), "Invariants failed at step 3522"

    # Operation 3523: delete
    del tree[2559]
    del reference[2559]
    assert tree.invariants(), "Invariants failed at step 3523"

    # Operation 3524: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3524"

    # Operation 3525: update
    tree[757] = 'value_183671'
    reference[757] = 'value_183671'
    assert tree.invariants(), "Invariants failed at step 3525"

    # Operation 3526: update
    tree[757] = 'value_616176'
    reference[757] = 'value_616176'
    assert tree.invariants(), "Invariants failed at step 3526"

    # Operation 3527: insert
    tree[7078] = 'value_386595'
    reference[7078] = 'value_386595'
    assert tree.invariants(), "Invariants failed at step 3527"

    # Operation 3528: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3528"

    # Operation 3529: delete
    del tree[7078]
    del reference[7078]
    assert tree.invariants(), "Invariants failed at step 3529"

    # Operation 3530: delete
    del tree[757]
    del reference[757]
    assert tree.invariants(), "Invariants failed at step 3530"

    # Operation 3531: get
    assert tree.invariants(), "Invariants failed at step 3531"

    # Operation 3532: get
    assert tree.invariants(), "Invariants failed at step 3532"

    # Operation 3533: insert
    tree[3953] = 'value_683834'
    reference[3953] = 'value_683834'
    assert tree.invariants(), "Invariants failed at step 3533"

    # Operation 3534: update
    tree[3953] = 'value_860069'
    reference[3953] = 'value_860069'
    assert tree.invariants(), "Invariants failed at step 3534"

    # Operation 3535: delete
    del tree[3953]
    del reference[3953]
    assert tree.invariants(), "Invariants failed at step 3535"

    # Operation 3536: insert
    tree[5046] = 'value_605917'
    reference[5046] = 'value_605917'
    assert tree.invariants(), "Invariants failed at step 3536"

    # Operation 3537: delete
    del tree[5046]
    del reference[5046]
    assert tree.invariants(), "Invariants failed at step 3537"

    # Operation 3538: insert
    tree[6177] = 'value_541836'
    reference[6177] = 'value_541836'
    assert tree.invariants(), "Invariants failed at step 3538"

    # Operation 3539: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3539"

    # Operation 3540: insert
    tree[3013] = 'value_222530'
    reference[3013] = 'value_222530'
    assert tree.invariants(), "Invariants failed at step 3540"

    # Operation 3541: get
    assert tree.invariants(), "Invariants failed at step 3541"

    # Operation 3542: insert
    tree[4963] = 'value_400825'
    reference[4963] = 'value_400825'
    assert tree.invariants(), "Invariants failed at step 3542"

    # Operation 3543: insert
    tree[1877] = 'value_303340'
    reference[1877] = 'value_303340'
    assert tree.invariants(), "Invariants failed at step 3543"

    # Operation 3544: insert
    tree[5650] = 'value_818265'
    reference[5650] = 'value_818265'
    assert tree.invariants(), "Invariants failed at step 3544"

    # Operation 3545: delete
    del tree[5650]
    del reference[5650]
    assert tree.invariants(), "Invariants failed at step 3545"

    # Operation 3546: delete
    del tree[4963]
    del reference[4963]
    assert tree.invariants(), "Invariants failed at step 3546"

    # Operation 3547: get
    assert tree.invariants(), "Invariants failed at step 3547"

    # Operation 3548: insert
    tree[2313] = 'value_807954'
    reference[2313] = 'value_807954'
    assert tree.invariants(), "Invariants failed at step 3548"

    # Operation 3549: insert
    tree[6117] = 'value_418551'
    reference[6117] = 'value_418551'
    assert tree.invariants(), "Invariants failed at step 3549"

    # Operation 3550: insert
    tree[406] = 'value_134617'
    reference[406] = 'value_134617'
    assert tree.invariants(), "Invariants failed at step 3550"

    # Operation 3551: get
    assert tree.invariants(), "Invariants failed at step 3551"

    # Operation 3552: update
    tree[3013] = 'value_459211'
    reference[3013] = 'value_459211'
    assert tree.invariants(), "Invariants failed at step 3552"

    # Operation 3553: delete
    del tree[1877]
    del reference[1877]
    assert tree.invariants(), "Invariants failed at step 3553"

    # Operation 3554: insert
    tree[2484] = 'value_366177'
    reference[2484] = 'value_366177'
    assert tree.invariants(), "Invariants failed at step 3554"

    # Operation 3555: get
    assert tree.invariants(), "Invariants failed at step 3555"

    # Operation 3556: insert
    tree[6800] = 'value_790792'
    reference[6800] = 'value_790792'
    assert tree.invariants(), "Invariants failed at step 3556"

    # Operation 3557: get
    assert tree.invariants(), "Invariants failed at step 3557"

    # Operation 3558: insert
    tree[6709] = 'value_496828'
    reference[6709] = 'value_496828'
    assert tree.invariants(), "Invariants failed at step 3558"

    # Operation 3559: insert
    tree[2481] = 'value_61231'
    reference[2481] = 'value_61231'
    assert tree.invariants(), "Invariants failed at step 3559"

    # Operation 3560: insert
    tree[563] = 'value_799896'
    reference[563] = 'value_799896'
    assert tree.invariants(), "Invariants failed at step 3560"

    # Operation 3561: delete
    del tree[2484]
    del reference[2484]
    assert tree.invariants(), "Invariants failed at step 3561"

    # Operation 3562: update
    tree[6709] = 'value_275181'
    reference[6709] = 'value_275181'
    assert tree.invariants(), "Invariants failed at step 3562"

    # Operation 3563: update
    tree[6117] = 'value_619170'
    reference[6117] = 'value_619170'
    assert tree.invariants(), "Invariants failed at step 3563"

    # Operation 3564: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3564"

    # Operation 3565: get
    assert tree.invariants(), "Invariants failed at step 3565"

    # Operation 3566: get
    assert tree.invariants(), "Invariants failed at step 3566"

    # Operation 3567: get
    assert tree.invariants(), "Invariants failed at step 3567"

    # Operation 3568: delete
    del tree[563]
    del reference[563]
    assert tree.invariants(), "Invariants failed at step 3568"

    # Operation 3569: get
    assert tree.invariants(), "Invariants failed at step 3569"

    # Operation 3570: batch_delete
    keys_to_delete = [2313, 6828, 9685, 6709, 406]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3570"

    # Operation 3571: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3571"

    # Operation 3572: delete
    del tree[6800]
    del reference[6800]
    assert tree.invariants(), "Invariants failed at step 3572"

    # Operation 3573: insert
    tree[2427] = 'value_761398'
    reference[2427] = 'value_761398'
    assert tree.invariants(), "Invariants failed at step 3573"

    # Operation 3574: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3574"

    # Operation 3575: insert
    tree[6105] = 'value_882159'
    reference[6105] = 'value_882159'
    assert tree.invariants(), "Invariants failed at step 3575"

    # Operation 3576: delete
    del tree[6177]
    del reference[6177]
    assert tree.invariants(), "Invariants failed at step 3576"

    # Operation 3577: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3577"

    # Operation 3578: delete
    del tree[6117]
    del reference[6117]
    assert tree.invariants(), "Invariants failed at step 3578"

    # Operation 3579: insert
    tree[7792] = 'value_666'
    reference[7792] = 'value_666'
    assert tree.invariants(), "Invariants failed at step 3579"

    # Operation 3580: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3580"

    # Operation 3581: batch_delete
    keys_to_delete = [7792, 2481, 90, 6413]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3581"

    # Operation 3582: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3582"

    # Operation 3583: delete
    del tree[3013]
    del reference[3013]
    assert tree.invariants(), "Invariants failed at step 3583"

    # Operation 3584: insert
    tree[5267] = 'value_321416'
    reference[5267] = 'value_321416'
    assert tree.invariants(), "Invariants failed at step 3584"

    # Operation 3585: insert
    tree[9901] = 'value_36982'
    reference[9901] = 'value_36982'
    assert tree.invariants(), "Invariants failed at step 3585"

    # Operation 3586: insert
    tree[3830] = 'value_429528'
    reference[3830] = 'value_429528'
    assert tree.invariants(), "Invariants failed at step 3586"

    # Operation 3587: insert
    tree[2892] = 'value_618438'
    reference[2892] = 'value_618438'
    assert tree.invariants(), "Invariants failed at step 3587"

    # Operation 3588: update
    tree[3830] = 'value_744315'
    reference[3830] = 'value_744315'
    assert tree.invariants(), "Invariants failed at step 3588"

    # Operation 3589: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3589"

    # Operation 3590: delete
    del tree[9901]
    del reference[9901]
    assert tree.invariants(), "Invariants failed at step 3590"

    # Operation 3591: insert
    tree[8863] = 'value_79046'
    reference[8863] = 'value_79046'
    assert tree.invariants(), "Invariants failed at step 3591"

    # Operation 3592: get
    assert tree.invariants(), "Invariants failed at step 3592"

    # Operation 3593: batch_delete
    keys_to_delete = [6506, 5872, 5267, 3830, 2427]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3593"

    # Operation 3594: update
    tree[2892] = 'value_357212'
    reference[2892] = 'value_357212'
    assert tree.invariants(), "Invariants failed at step 3594"

    # Operation 3595: delete
    del tree[6105]
    del reference[6105]
    assert tree.invariants(), "Invariants failed at step 3595"

    # Operation 3596: insert
    tree[1003] = 'value_838794'
    reference[1003] = 'value_838794'
    assert tree.invariants(), "Invariants failed at step 3596"

    # Operation 3597: delete
    del tree[2892]
    del reference[2892]
    assert tree.invariants(), "Invariants failed at step 3597"

    # Operation 3598: delete
    del tree[1003]
    del reference[1003]
    assert tree.invariants(), "Invariants failed at step 3598"

    # Operation 3599: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3599"

    # Operation 3600: get
    assert tree.invariants(), "Invariants failed at step 3600"

    # Operation 3601: insert
    tree[9381] = 'value_11124'
    reference[9381] = 'value_11124'
    assert tree.invariants(), "Invariants failed at step 3601"

    # Operation 3602: delete
    del tree[9381]
    del reference[9381]
    assert tree.invariants(), "Invariants failed at step 3602"

    # Operation 3603: insert
    tree[3371] = 'value_353828'
    reference[3371] = 'value_353828'
    assert tree.invariants(), "Invariants failed at step 3603"

    # Operation 3604: delete
    del tree[3371]
    del reference[3371]
    assert tree.invariants(), "Invariants failed at step 3604"

    # Operation 3605: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3605"

    # Operation 3606: insert
    tree[9174] = 'value_923072'
    reference[9174] = 'value_923072'
    assert tree.invariants(), "Invariants failed at step 3606"

    # Operation 3607: delete
    del tree[8863]
    del reference[8863]
    assert tree.invariants(), "Invariants failed at step 3607"

    # Operation 3608: insert
    tree[978] = 'value_982159'
    reference[978] = 'value_982159'
    assert tree.invariants(), "Invariants failed at step 3608"

    # Operation 3609: insert
    tree[5242] = 'value_106406'
    reference[5242] = 'value_106406'
    assert tree.invariants(), "Invariants failed at step 3609"

    # Operation 3610: get
    assert tree.invariants(), "Invariants failed at step 3610"

    # Operation 3611: insert
    tree[3711] = 'value_943532'
    reference[3711] = 'value_943532'
    assert tree.invariants(), "Invariants failed at step 3611"

    # Operation 3612: delete
    del tree[978]
    del reference[978]
    assert tree.invariants(), "Invariants failed at step 3612"

    # Operation 3613: update
    tree[9174] = 'value_390150'
    reference[9174] = 'value_390150'
    assert tree.invariants(), "Invariants failed at step 3613"

    # Operation 3614: insert
    tree[1214] = 'value_834698'
    reference[1214] = 'value_834698'
    assert tree.invariants(), "Invariants failed at step 3614"

    # Operation 3615: insert
    tree[4008] = 'value_62916'
    reference[4008] = 'value_62916'
    assert tree.invariants(), "Invariants failed at step 3615"

    # Operation 3616: insert
    tree[1469] = 'value_950858'
    reference[1469] = 'value_950858'
    assert tree.invariants(), "Invariants failed at step 3616"

    # Operation 3617: delete
    del tree[3711]
    del reference[3711]
    assert tree.invariants(), "Invariants failed at step 3617"

    # Operation 3618: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3618"

    # Operation 3619: get
    assert tree.invariants(), "Invariants failed at step 3619"

    # Operation 3620: get
    assert tree.invariants(), "Invariants failed at step 3620"

    # Operation 3621: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3621"

    # Operation 3622: delete
    del tree[1214]
    del reference[1214]
    assert tree.invariants(), "Invariants failed at step 3622"

    # Operation 3623: insert
    tree[6468] = 'value_962345'
    reference[6468] = 'value_962345'
    assert tree.invariants(), "Invariants failed at step 3623"

    # Operation 3624: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3624"

    # Operation 3625: insert
    tree[6897] = 'value_750179'
    reference[6897] = 'value_750179'
    assert tree.invariants(), "Invariants failed at step 3625"

    # Operation 3626: update
    tree[5242] = 'value_350228'
    reference[5242] = 'value_350228'
    assert tree.invariants(), "Invariants failed at step 3626"

    # Operation 3627: insert
    tree[7438] = 'value_368943'
    reference[7438] = 'value_368943'
    assert tree.invariants(), "Invariants failed at step 3627"

    # Operation 3628: insert
    tree[1544] = 'value_525918'
    reference[1544] = 'value_525918'
    assert tree.invariants(), "Invariants failed at step 3628"

    # Operation 3629: delete
    del tree[4008]
    del reference[4008]
    assert tree.invariants(), "Invariants failed at step 3629"

    # Operation 3630: get
    assert tree.invariants(), "Invariants failed at step 3630"

    # Operation 3631: delete
    del tree[1469]
    del reference[1469]
    assert tree.invariants(), "Invariants failed at step 3631"

    # Operation 3632: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3632"

    # Operation 3633: insert
    tree[5407] = 'value_506118'
    reference[5407] = 'value_506118'
    assert tree.invariants(), "Invariants failed at step 3633"

    # Operation 3634: get
    assert tree.invariants(), "Invariants failed at step 3634"

    # Operation 3635: get
    assert tree.invariants(), "Invariants failed at step 3635"

    # Operation 3636: batch_delete
    keys_to_delete = [6468, 6984, 1419, 9174, 5242]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3636"

    # Operation 3637: insert
    tree[4321] = 'value_856931'
    reference[4321] = 'value_856931'
    assert tree.invariants(), "Invariants failed at step 3637"

    # Operation 3638: get
    assert tree.invariants(), "Invariants failed at step 3638"

    # Operation 3639: batch_delete
    keys_to_delete = [4321, 5914, 7750, 5407]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3639"

    # Operation 3640: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3640"

    # Operation 3641: insert
    tree[6067] = 'value_894911'
    reference[6067] = 'value_894911'
    assert tree.invariants(), "Invariants failed at step 3641"

    # Operation 3642: delete
    del tree[1544]
    del reference[1544]
    assert tree.invariants(), "Invariants failed at step 3642"

    # Operation 3643: get
    assert tree.invariants(), "Invariants failed at step 3643"

    # Operation 3644: insert
    tree[6281] = 'value_387307'
    reference[6281] = 'value_387307'
    assert tree.invariants(), "Invariants failed at step 3644"

    # Operation 3645: update
    tree[6067] = 'value_906462'
    reference[6067] = 'value_906462'
    assert tree.invariants(), "Invariants failed at step 3645"

    # Operation 3646: update
    tree[6281] = 'value_824393'
    reference[6281] = 'value_824393'
    assert tree.invariants(), "Invariants failed at step 3646"

    # Operation 3647: insert
    tree[8637] = 'value_971562'
    reference[8637] = 'value_971562'
    assert tree.invariants(), "Invariants failed at step 3647"

    # Operation 3648: batch_delete
    keys_to_delete = [6897, 3237, 7438]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3648"

    # Operation 3649: get
    assert tree.invariants(), "Invariants failed at step 3649"

    # Operation 3650: insert
    tree[4930] = 'value_587696'
    reference[4930] = 'value_587696'
    assert tree.invariants(), "Invariants failed at step 3650"

    # Operation 3651: get
    assert tree.invariants(), "Invariants failed at step 3651"

    # Operation 3652: insert
    tree[8151] = 'value_928865'
    reference[8151] = 'value_928865'
    assert tree.invariants(), "Invariants failed at step 3652"

    # Operation 3653: delete
    del tree[4930]
    del reference[4930]
    assert tree.invariants(), "Invariants failed at step 3653"

    # Operation 3654: insert
    tree[4425] = 'value_774652'
    reference[4425] = 'value_774652'
    assert tree.invariants(), "Invariants failed at step 3654"

    # Operation 3655: insert
    tree[4194] = 'value_514414'
    reference[4194] = 'value_514414'
    assert tree.invariants(), "Invariants failed at step 3655"

    # Operation 3656: insert
    tree[5382] = 'value_692642'
    reference[5382] = 'value_692642'
    assert tree.invariants(), "Invariants failed at step 3656"

    # Operation 3657: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3657"

    # Operation 3658: get
    assert tree.invariants(), "Invariants failed at step 3658"

    # Operation 3659: insert
    tree[2865] = 'value_707132'
    reference[2865] = 'value_707132'
    assert tree.invariants(), "Invariants failed at step 3659"

    # Operation 3660: get
    assert tree.invariants(), "Invariants failed at step 3660"

    # Operation 3661: update
    tree[4425] = 'value_457854'
    reference[4425] = 'value_457854'
    assert tree.invariants(), "Invariants failed at step 3661"

    # Operation 3662: update
    tree[6067] = 'value_429651'
    reference[6067] = 'value_429651'
    assert tree.invariants(), "Invariants failed at step 3662"

    # Operation 3663: delete
    del tree[6067]
    del reference[6067]
    assert tree.invariants(), "Invariants failed at step 3663"

    # Operation 3664: get
    assert tree.invariants(), "Invariants failed at step 3664"

    # Operation 3665: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3665"

    # Operation 3666: insert
    tree[300] = 'value_900301'
    reference[300] = 'value_900301'
    assert tree.invariants(), "Invariants failed at step 3666"

    # Operation 3667: get
    assert tree.invariants(), "Invariants failed at step 3667"

    # Operation 3668: insert
    tree[3689] = 'value_342635'
    reference[3689] = 'value_342635'
    assert tree.invariants(), "Invariants failed at step 3668"

    # Operation 3669: delete
    del tree[300]
    del reference[300]
    assert tree.invariants(), "Invariants failed at step 3669"

    # Operation 3670: insert
    tree[9623] = 'value_62617'
    reference[9623] = 'value_62617'
    assert tree.invariants(), "Invariants failed at step 3670"

    # Operation 3671: insert
    tree[7029] = 'value_323794'
    reference[7029] = 'value_323794'
    assert tree.invariants(), "Invariants failed at step 3671"

    # Operation 3672: batch_delete
    keys_to_delete = [5382, 4425, 3689, 6281, 7625, 7029, 5630]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3672"

    # Operation 3673: delete
    del tree[2865]
    del reference[2865]
    assert tree.invariants(), "Invariants failed at step 3673"

    # Operation 3674: update
    tree[8637] = 'value_940365'
    reference[8637] = 'value_940365'
    assert tree.invariants(), "Invariants failed at step 3674"

    # Operation 3675: get
    assert tree.invariants(), "Invariants failed at step 3675"

    # Operation 3676: insert
    tree[6915] = 'value_463449'
    reference[6915] = 'value_463449'
    assert tree.invariants(), "Invariants failed at step 3676"

    # Operation 3677: get
    assert tree.invariants(), "Invariants failed at step 3677"

    # Operation 3678: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3678"

    # Operation 3679: batch_delete
    keys_to_delete = [6915, 8637, 7213]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3679"

    # Operation 3680: update
    tree[8151] = 'value_973341'
    reference[8151] = 'value_973341'
    assert tree.invariants(), "Invariants failed at step 3680"

    # Operation 3681: get
    assert tree.invariants(), "Invariants failed at step 3681"

    # Operation 3682: update
    tree[9623] = 'value_50973'
    reference[9623] = 'value_50973'
    assert tree.invariants(), "Invariants failed at step 3682"

    # Operation 3683: delete
    del tree[4194]
    del reference[4194]
    assert tree.invariants(), "Invariants failed at step 3683"

    # Operation 3684: get
    assert tree.invariants(), "Invariants failed at step 3684"

    # Operation 3685: insert
    tree[109] = 'value_532291'
    reference[109] = 'value_532291'
    assert tree.invariants(), "Invariants failed at step 3685"

    # Operation 3686: insert
    tree[8964] = 'value_442062'
    reference[8964] = 'value_442062'
    assert tree.invariants(), "Invariants failed at step 3686"

    # Operation 3687: get
    assert tree.invariants(), "Invariants failed at step 3687"

    # Operation 3688: delete
    del tree[8964]
    del reference[8964]
    assert tree.invariants(), "Invariants failed at step 3688"

    # Operation 3689: update
    tree[9623] = 'value_46130'
    reference[9623] = 'value_46130'
    assert tree.invariants(), "Invariants failed at step 3689"

    # Operation 3690: insert
    tree[618] = 'value_536984'
    reference[618] = 'value_536984'
    assert tree.invariants(), "Invariants failed at step 3690"

    # Operation 3691: get
    assert tree.invariants(), "Invariants failed at step 3691"

    # Operation 3692: insert
    tree[2452] = 'value_77050'
    reference[2452] = 'value_77050'
    assert tree.invariants(), "Invariants failed at step 3692"

    # Operation 3693: insert
    tree[7014] = 'value_35712'
    reference[7014] = 'value_35712'
    assert tree.invariants(), "Invariants failed at step 3693"

    # Operation 3694: delete
    del tree[618]
    del reference[618]
    assert tree.invariants(), "Invariants failed at step 3694"

    # Operation 3695: get
    assert tree.invariants(), "Invariants failed at step 3695"

    # Operation 3696: update
    tree[9623] = 'value_874036'
    reference[9623] = 'value_874036'
    assert tree.invariants(), "Invariants failed at step 3696"

    # Operation 3697: delete
    del tree[8151]
    del reference[8151]
    assert tree.invariants(), "Invariants failed at step 3697"

    # Operation 3698: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3698"

    # Operation 3699: delete
    del tree[109]
    del reference[109]
    assert tree.invariants(), "Invariants failed at step 3699"

    # Operation 3700: insert
    tree[2692] = 'value_716914'
    reference[2692] = 'value_716914'
    assert tree.invariants(), "Invariants failed at step 3700"

    # Operation 3701: insert
    tree[4952] = 'value_39911'
    reference[4952] = 'value_39911'
    assert tree.invariants(), "Invariants failed at step 3701"

    # Operation 3702: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3702"

    # Operation 3703: insert
    tree[9559] = 'value_720720'
    reference[9559] = 'value_720720'
    assert tree.invariants(), "Invariants failed at step 3703"

    # Operation 3704: get
    assert tree.invariants(), "Invariants failed at step 3704"

    # Operation 3705: delete
    del tree[9623]
    del reference[9623]
    assert tree.invariants(), "Invariants failed at step 3705"

    # Operation 3706: insert
    tree[740] = 'value_647070'
    reference[740] = 'value_647070'
    assert tree.invariants(), "Invariants failed at step 3706"

    # Operation 3707: batch_delete
    keys_to_delete = [740, 7014, 73, 2452, 3869]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3707"

    # Operation 3708: insert
    tree[4845] = 'value_625631'
    reference[4845] = 'value_625631'
    assert tree.invariants(), "Invariants failed at step 3708"

    # Operation 3709: delete
    del tree[2692]
    del reference[2692]
    assert tree.invariants(), "Invariants failed at step 3709"

    # Operation 3710: insert
    tree[8514] = 'value_379210'
    reference[8514] = 'value_379210'
    assert tree.invariants(), "Invariants failed at step 3710"

    # Operation 3711: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3711"

    # Operation 3712: delete
    del tree[4845]
    del reference[4845]
    assert tree.invariants(), "Invariants failed at step 3712"

    # Operation 3713: delete
    del tree[4952]
    del reference[4952]
    assert tree.invariants(), "Invariants failed at step 3713"

    # Operation 3714: get
    assert tree.invariants(), "Invariants failed at step 3714"

    # Operation 3715: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3715"

    # Operation 3716: insert
    tree[8227] = 'value_718728'
    reference[8227] = 'value_718728'
    assert tree.invariants(), "Invariants failed at step 3716"

    # Operation 3717: insert
    tree[1169] = 'value_290330'
    reference[1169] = 'value_290330'
    assert tree.invariants(), "Invariants failed at step 3717"

    # Operation 3718: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3718"

    # Operation 3719: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3719"

    # Operation 3720: delete
    del tree[8514]
    del reference[8514]
    assert tree.invariants(), "Invariants failed at step 3720"

    # Operation 3721: get
    assert tree.invariants(), "Invariants failed at step 3721"

    # Operation 3722: update
    tree[1169] = 'value_393479'
    reference[1169] = 'value_393479'
    assert tree.invariants(), "Invariants failed at step 3722"

    # Operation 3723: insert
    tree[60] = 'value_807233'
    reference[60] = 'value_807233'
    assert tree.invariants(), "Invariants failed at step 3723"

    # Operation 3724: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3724"

    # Operation 3725: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3725"

    # Operation 3726: update
    tree[8227] = 'value_58466'
    reference[8227] = 'value_58466'
    assert tree.invariants(), "Invariants failed at step 3726"

    # Operation 3727: update
    tree[60] = 'value_893024'
    reference[60] = 'value_893024'
    assert tree.invariants(), "Invariants failed at step 3727"

    # Operation 3728: get
    assert tree.invariants(), "Invariants failed at step 3728"

    # Operation 3729: update
    tree[60] = 'value_938832'
    reference[60] = 'value_938832'
    assert tree.invariants(), "Invariants failed at step 3729"

    # Operation 3730: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3730"

    # Operation 3731: delete
    del tree[60]
    del reference[60]
    assert tree.invariants(), "Invariants failed at step 3731"

    # Operation 3732: insert
    tree[1371] = 'value_613867'
    reference[1371] = 'value_613867'
    assert tree.invariants(), "Invariants failed at step 3732"

    # Operation 3733: get
    assert tree.invariants(), "Invariants failed at step 3733"

    # Operation 3734: delete
    del tree[1371]
    del reference[1371]
    assert tree.invariants(), "Invariants failed at step 3734"

    # Operation 3735: insert
    tree[8290] = 'value_692745'
    reference[8290] = 'value_692745'
    assert tree.invariants(), "Invariants failed at step 3735"

    # Operation 3736: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3736"

    # Operation 3737: delete
    del tree[8227]
    del reference[8227]
    assert tree.invariants(), "Invariants failed at step 3737"

    # Operation 3738: delete
    del tree[8290]
    del reference[8290]
    assert tree.invariants(), "Invariants failed at step 3738"

    # Operation 3739: insert
    tree[8932] = 'value_819538'
    reference[8932] = 'value_819538'
    assert tree.invariants(), "Invariants failed at step 3739"

    # Operation 3740: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3740"

    # Operation 3741: delete
    del tree[1169]
    del reference[1169]
    assert tree.invariants(), "Invariants failed at step 3741"

    # Operation 3742: delete
    del tree[8932]
    del reference[8932]
    assert tree.invariants(), "Invariants failed at step 3742"

    # Operation 3743: insert
    tree[9985] = 'value_789288'
    reference[9985] = 'value_789288'
    assert tree.invariants(), "Invariants failed at step 3743"

    # Operation 3744: insert
    tree[9082] = 'value_380073'
    reference[9082] = 'value_380073'
    assert tree.invariants(), "Invariants failed at step 3744"

    # Operation 3745: get
    assert tree.invariants(), "Invariants failed at step 3745"

    # Operation 3746: insert
    tree[4140] = 'value_657442'
    reference[4140] = 'value_657442'
    assert tree.invariants(), "Invariants failed at step 3746"

    # Operation 3747: get
    assert tree.invariants(), "Invariants failed at step 3747"

    # Operation 3748: get
    assert tree.invariants(), "Invariants failed at step 3748"

    # Operation 3749: get
    assert tree.invariants(), "Invariants failed at step 3749"

    # Operation 3750: insert
    tree[3443] = 'value_377321'
    reference[3443] = 'value_377321'
    assert tree.invariants(), "Invariants failed at step 3750"

    # Operation 3751: get
    assert tree.invariants(), "Invariants failed at step 3751"

    # Operation 3752: insert
    tree[7248] = 'value_121151'
    reference[7248] = 'value_121151'
    assert tree.invariants(), "Invariants failed at step 3752"

    # Operation 3753: insert
    tree[6138] = 'value_171315'
    reference[6138] = 'value_171315'
    assert tree.invariants(), "Invariants failed at step 3753"

    # Operation 3754: delete
    del tree[7248]
    del reference[7248]
    assert tree.invariants(), "Invariants failed at step 3754"

    # Operation 3755: delete
    del tree[9559]
    del reference[9559]
    assert tree.invariants(), "Invariants failed at step 3755"

    # Operation 3756: get
    assert tree.invariants(), "Invariants failed at step 3756"

    # Operation 3757: delete
    del tree[4140]
    del reference[4140]
    assert tree.invariants(), "Invariants failed at step 3757"

    # Operation 3758: get
    assert tree.invariants(), "Invariants failed at step 3758"

    # Operation 3759: delete
    del tree[9082]
    del reference[9082]
    assert tree.invariants(), "Invariants failed at step 3759"

    # Operation 3760: get
    assert tree.invariants(), "Invariants failed at step 3760"

    # Operation 3761: insert
    tree[1616] = 'value_853958'
    reference[1616] = 'value_853958'
    assert tree.invariants(), "Invariants failed at step 3761"

    # Operation 3762: insert
    tree[5018] = 'value_225850'
    reference[5018] = 'value_225850'
    assert tree.invariants(), "Invariants failed at step 3762"

    # Operation 3763: get
    assert tree.invariants(), "Invariants failed at step 3763"

    # Operation 3764: insert
    tree[187] = 'value_508870'
    reference[187] = 'value_508870'
    assert tree.invariants(), "Invariants failed at step 3764"

    # Operation 3765: update
    tree[1616] = 'value_461708'
    reference[1616] = 'value_461708'
    assert tree.invariants(), "Invariants failed at step 3765"

    # Operation 3766: insert
    tree[5668] = 'value_121188'
    reference[5668] = 'value_121188'
    assert tree.invariants(), "Invariants failed at step 3766"

    # Operation 3767: update
    tree[5668] = 'value_243472'
    reference[5668] = 'value_243472'
    assert tree.invariants(), "Invariants failed at step 3767"

    # Operation 3768: delete
    del tree[6138]
    del reference[6138]
    assert tree.invariants(), "Invariants failed at step 3768"

    # Operation 3769: update
    tree[1616] = 'value_168125'
    reference[1616] = 'value_168125'
    assert tree.invariants(), "Invariants failed at step 3769"

    # Operation 3770: delete
    del tree[3443]
    del reference[3443]
    assert tree.invariants(), "Invariants failed at step 3770"

    # Operation 3771: update
    tree[5668] = 'value_389385'
    reference[5668] = 'value_389385'
    assert tree.invariants(), "Invariants failed at step 3771"

    # Operation 3772: update
    tree[5668] = 'value_149478'
    reference[5668] = 'value_149478'
    assert tree.invariants(), "Invariants failed at step 3772"

    # Operation 3773: update
    tree[5018] = 'value_708903'
    reference[5018] = 'value_708903'
    assert tree.invariants(), "Invariants failed at step 3773"

    # Operation 3774: delete
    del tree[9985]
    del reference[9985]
    assert tree.invariants(), "Invariants failed at step 3774"

    # Operation 3775: insert
    tree[9309] = 'value_563803'
    reference[9309] = 'value_563803'
    assert tree.invariants(), "Invariants failed at step 3775"

    # Operation 3776: get
    assert tree.invariants(), "Invariants failed at step 3776"

    # Operation 3777: delete
    del tree[1616]
    del reference[1616]
    assert tree.invariants(), "Invariants failed at step 3777"

    # Operation 3778: insert
    tree[3662] = 'value_467573'
    reference[3662] = 'value_467573'
    assert tree.invariants(), "Invariants failed at step 3778"

    # Operation 3779: get
    assert tree.invariants(), "Invariants failed at step 3779"

    # Operation 3780: delete
    del tree[5668]
    del reference[5668]
    assert tree.invariants(), "Invariants failed at step 3780"

    # Operation 3781: get
    assert tree.invariants(), "Invariants failed at step 3781"

    # Operation 3782: delete
    del tree[187]
    del reference[187]
    assert tree.invariants(), "Invariants failed at step 3782"

    # Operation 3783: insert
    tree[4997] = 'value_615464'
    reference[4997] = 'value_615464'
    assert tree.invariants(), "Invariants failed at step 3783"

    # Operation 3784: insert
    tree[1507] = 'value_839581'
    reference[1507] = 'value_839581'
    assert tree.invariants(), "Invariants failed at step 3784"

    # Operation 3785: update
    tree[4997] = 'value_677191'
    reference[4997] = 'value_677191'
    assert tree.invariants(), "Invariants failed at step 3785"

    # Operation 3786: insert
    tree[6740] = 'value_113120'
    reference[6740] = 'value_113120'
    assert tree.invariants(), "Invariants failed at step 3786"

    # Operation 3787: delete
    del tree[5018]
    del reference[5018]
    assert tree.invariants(), "Invariants failed at step 3787"

    # Operation 3788: get
    assert tree.invariants(), "Invariants failed at step 3788"

    # Operation 3789: insert
    tree[2305] = 'value_571259'
    reference[2305] = 'value_571259'
    assert tree.invariants(), "Invariants failed at step 3789"

    # Operation 3790: update
    tree[9309] = 'value_747724'
    reference[9309] = 'value_747724'
    assert tree.invariants(), "Invariants failed at step 3790"

    # Operation 3791: insert
    tree[9889] = 'value_317159'
    reference[9889] = 'value_317159'
    assert tree.invariants(), "Invariants failed at step 3791"

    # Operation 3792: get
    assert tree.invariants(), "Invariants failed at step 3792"

    # Operation 3793: insert
    tree[4552] = 'value_221769'
    reference[4552] = 'value_221769'
    assert tree.invariants(), "Invariants failed at step 3793"

    # Operation 3794: update
    tree[2305] = 'value_368088'
    reference[2305] = 'value_368088'
    assert tree.invariants(), "Invariants failed at step 3794"

    # Operation 3795: delete
    del tree[6740]
    del reference[6740]
    assert tree.invariants(), "Invariants failed at step 3795"

    # Operation 3796: get
    assert tree.invariants(), "Invariants failed at step 3796"

    # Operation 3797: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3797"

    # Operation 3798: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3798"

    # Operation 3799: insert
    tree[4643] = 'value_152696'
    reference[4643] = 'value_152696'
    assert tree.invariants(), "Invariants failed at step 3799"

    # Operation 3800: delete
    del tree[3662]
    del reference[3662]
    assert tree.invariants(), "Invariants failed at step 3800"

    # Operation 3801: get
    assert tree.invariants(), "Invariants failed at step 3801"

    # Operation 3802: get
    assert tree.invariants(), "Invariants failed at step 3802"

    # Operation 3803: insert
    tree[2290] = 'value_297313'
    reference[2290] = 'value_297313'
    assert tree.invariants(), "Invariants failed at step 3803"

    # Operation 3804: get
    assert tree.invariants(), "Invariants failed at step 3804"

    # Operation 3805: insert
    tree[4184] = 'value_354903'
    reference[4184] = 'value_354903'
    assert tree.invariants(), "Invariants failed at step 3805"

    # Operation 3806: batch_delete
    keys_to_delete = [9889, 4643, 4552, 2290, 9106, 2844]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3806"

    # Operation 3807: delete
    del tree[4997]
    del reference[4997]
    assert tree.invariants(), "Invariants failed at step 3807"

    # Operation 3808: delete
    del tree[1507]
    del reference[1507]
    assert tree.invariants(), "Invariants failed at step 3808"

    # Operation 3809: insert
    tree[7056] = 'value_977832'
    reference[7056] = 'value_977832'
    assert tree.invariants(), "Invariants failed at step 3809"

    # Operation 3810: update
    tree[4184] = 'value_436844'
    reference[4184] = 'value_436844'
    assert tree.invariants(), "Invariants failed at step 3810"

    # Operation 3811: insert
    tree[610] = 'value_363147'
    reference[610] = 'value_363147'
    assert tree.invariants(), "Invariants failed at step 3811"

    # Operation 3812: get
    assert tree.invariants(), "Invariants failed at step 3812"

    # Operation 3813: delete
    del tree[9309]
    del reference[9309]
    assert tree.invariants(), "Invariants failed at step 3813"

    # Operation 3814: get
    assert tree.invariants(), "Invariants failed at step 3814"

    # Operation 3815: insert
    tree[3481] = 'value_795245'
    reference[3481] = 'value_795245'
    assert tree.invariants(), "Invariants failed at step 3815"

    # Operation 3816: delete
    del tree[3481]
    del reference[3481]
    assert tree.invariants(), "Invariants failed at step 3816"

    # Operation 3817: delete
    del tree[4184]
    del reference[4184]
    assert tree.invariants(), "Invariants failed at step 3817"

    # Operation 3818: get
    assert tree.invariants(), "Invariants failed at step 3818"

    # Operation 3819: insert
    tree[296] = 'value_700493'
    reference[296] = 'value_700493'
    assert tree.invariants(), "Invariants failed at step 3819"

    # Operation 3820: delete
    del tree[2305]
    del reference[2305]
    assert tree.invariants(), "Invariants failed at step 3820"

    # Operation 3821: insert
    tree[2418] = 'value_31525'
    reference[2418] = 'value_31525'
    assert tree.invariants(), "Invariants failed at step 3821"

    # Operation 3822: delete
    del tree[296]
    del reference[296]
    assert tree.invariants(), "Invariants failed at step 3822"

    # Operation 3823: update
    tree[7056] = 'value_111556'
    reference[7056] = 'value_111556'
    assert tree.invariants(), "Invariants failed at step 3823"

    # Operation 3824: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3824"

    # Operation 3825: get
    assert tree.invariants(), "Invariants failed at step 3825"

    # Operation 3826: get
    assert tree.invariants(), "Invariants failed at step 3826"

    # Operation 3827: insert
    tree[1014] = 'value_447848'
    reference[1014] = 'value_447848'
    assert tree.invariants(), "Invariants failed at step 3827"

    # Operation 3828: insert
    tree[3731] = 'value_155861'
    reference[3731] = 'value_155861'
    assert tree.invariants(), "Invariants failed at step 3828"

    # Operation 3829: get
    assert tree.invariants(), "Invariants failed at step 3829"

    # Operation 3830: batch_delete
    keys_to_delete = [7056, 610, 136, 1014]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3830"

    # Operation 3831: get
    assert tree.invariants(), "Invariants failed at step 3831"

    # Operation 3832: get
    assert tree.invariants(), "Invariants failed at step 3832"

    # Operation 3833: insert
    tree[408] = 'value_169254'
    reference[408] = 'value_169254'
    assert tree.invariants(), "Invariants failed at step 3833"

    # Operation 3834: insert
    tree[2198] = 'value_418922'
    reference[2198] = 'value_418922'
    assert tree.invariants(), "Invariants failed at step 3834"

    # Operation 3835: get
    assert tree.invariants(), "Invariants failed at step 3835"

    # Operation 3836: get
    assert tree.invariants(), "Invariants failed at step 3836"

    # Operation 3837: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3837"

    # Operation 3838: get
    assert tree.invariants(), "Invariants failed at step 3838"

    # Operation 3839: delete
    del tree[408]
    del reference[408]
    assert tree.invariants(), "Invariants failed at step 3839"

    # Operation 3840: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3840"

    # Operation 3841: get
    assert tree.invariants(), "Invariants failed at step 3841"

    # Operation 3842: delete
    del tree[2418]
    del reference[2418]
    assert tree.invariants(), "Invariants failed at step 3842"

    # Operation 3843: delete
    del tree[2198]
    del reference[2198]
    assert tree.invariants(), "Invariants failed at step 3843"

    # Operation 3844: delete
    del tree[3731]
    del reference[3731]
    assert tree.invariants(), "Invariants failed at step 3844"

    # Operation 3845: get
    assert tree.invariants(), "Invariants failed at step 3845"

    # Operation 3846: insert
    tree[3043] = 'value_643212'
    reference[3043] = 'value_643212'
    assert tree.invariants(), "Invariants failed at step 3846"

    # Operation 3847: get
    assert tree.invariants(), "Invariants failed at step 3847"

    # Operation 3848: insert
    tree[2804] = 'value_231724'
    reference[2804] = 'value_231724'
    assert tree.invariants(), "Invariants failed at step 3848"

    # Operation 3849: get
    assert tree.invariants(), "Invariants failed at step 3849"

    # Operation 3850: get
    assert tree.invariants(), "Invariants failed at step 3850"

    # Operation 3851: get
    assert tree.invariants(), "Invariants failed at step 3851"

    # Operation 3852: delete
    del tree[2804]
    del reference[2804]
    assert tree.invariants(), "Invariants failed at step 3852"

    # Operation 3853: get
    assert tree.invariants(), "Invariants failed at step 3853"

    # Operation 3854: update
    tree[3043] = 'value_628890'
    reference[3043] = 'value_628890'
    assert tree.invariants(), "Invariants failed at step 3854"

    # Operation 3855: update
    tree[3043] = 'value_139876'
    reference[3043] = 'value_139876'
    assert tree.invariants(), "Invariants failed at step 3855"

    # Operation 3856: insert
    tree[7364] = 'value_953804'
    reference[7364] = 'value_953804'
    assert tree.invariants(), "Invariants failed at step 3856"

    # Operation 3857: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3857"

    # Operation 3858: delete
    del tree[3043]
    del reference[3043]
    assert tree.invariants(), "Invariants failed at step 3858"

    # Operation 3859: insert
    tree[5458] = 'value_165017'
    reference[5458] = 'value_165017'
    assert tree.invariants(), "Invariants failed at step 3859"

    # Operation 3860: delete
    del tree[5458]
    del reference[5458]
    assert tree.invariants(), "Invariants failed at step 3860"

    # Operation 3861: delete
    del tree[7364]
    del reference[7364]
    assert tree.invariants(), "Invariants failed at step 3861"

    # Operation 3862: insert
    tree[3544] = 'value_194572'
    reference[3544] = 'value_194572'
    assert tree.invariants(), "Invariants failed at step 3862"

    # Operation 3863: delete
    del tree[3544]
    del reference[3544]
    assert tree.invariants(), "Invariants failed at step 3863"

    # Operation 3864: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3864"

    # Operation 3865: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3865"

    # Operation 3866: insert
    tree[3898] = 'value_249178'
    reference[3898] = 'value_249178'
    assert tree.invariants(), "Invariants failed at step 3866"

    # Operation 3867: get
    assert tree.invariants(), "Invariants failed at step 3867"

    # Operation 3868: get
    assert tree.invariants(), "Invariants failed at step 3868"

    # Operation 3869: insert
    tree[669] = 'value_347352'
    reference[669] = 'value_347352'
    assert tree.invariants(), "Invariants failed at step 3869"

    # Operation 3870: delete
    del tree[669]
    del reference[669]
    assert tree.invariants(), "Invariants failed at step 3870"

    # Operation 3871: insert
    tree[9652] = 'value_542250'
    reference[9652] = 'value_542250'
    assert tree.invariants(), "Invariants failed at step 3871"

    # Operation 3872: update
    tree[9652] = 'value_227294'
    reference[9652] = 'value_227294'
    assert tree.invariants(), "Invariants failed at step 3872"

    # Operation 3873: insert
    tree[2280] = 'value_850827'
    reference[2280] = 'value_850827'
    assert tree.invariants(), "Invariants failed at step 3873"

    # Operation 3874: insert
    tree[711] = 'value_637287'
    reference[711] = 'value_637287'
    assert tree.invariants(), "Invariants failed at step 3874"

    # Operation 3875: get
    assert tree.invariants(), "Invariants failed at step 3875"

    # Operation 3876: update
    tree[2280] = 'value_101074'
    reference[2280] = 'value_101074'
    assert tree.invariants(), "Invariants failed at step 3876"

    # Operation 3877: insert
    tree[2478] = 'value_841524'
    reference[2478] = 'value_841524'
    assert tree.invariants(), "Invariants failed at step 3877"

    # Operation 3878: insert
    tree[4835] = 'value_782843'
    reference[4835] = 'value_782843'
    assert tree.invariants(), "Invariants failed at step 3878"

    # Operation 3879: delete
    del tree[2280]
    del reference[2280]
    assert tree.invariants(), "Invariants failed at step 3879"

    # Operation 3880: get
    assert tree.invariants(), "Invariants failed at step 3880"

    # Operation 3881: batch_delete
    keys_to_delete = [3898, 4835, 932, 2450]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3881"

    # Operation 3882: insert
    tree[1668] = 'value_62907'
    reference[1668] = 'value_62907'
    assert tree.invariants(), "Invariants failed at step 3882"

    # Operation 3883: get
    assert tree.invariants(), "Invariants failed at step 3883"

    # Operation 3884: delete
    del tree[2478]
    del reference[2478]
    assert tree.invariants(), "Invariants failed at step 3884"

    # Operation 3885: insert
    tree[1078] = 'value_337170'
    reference[1078] = 'value_337170'
    assert tree.invariants(), "Invariants failed at step 3885"

    # Operation 3886: delete
    del tree[711]
    del reference[711]
    assert tree.invariants(), "Invariants failed at step 3886"

    # Operation 3887: update
    tree[1078] = 'value_155628'
    reference[1078] = 'value_155628'
    assert tree.invariants(), "Invariants failed at step 3887"

    # Operation 3888: get
    assert tree.invariants(), "Invariants failed at step 3888"

    # Operation 3889: update
    tree[9652] = 'value_243365'
    reference[9652] = 'value_243365'
    assert tree.invariants(), "Invariants failed at step 3889"

    # Operation 3890: update
    tree[1078] = 'value_796835'
    reference[1078] = 'value_796835'
    assert tree.invariants(), "Invariants failed at step 3890"

    # Operation 3891: get
    assert tree.invariants(), "Invariants failed at step 3891"

    # Operation 3892: delete
    del tree[1668]
    del reference[1668]
    assert tree.invariants(), "Invariants failed at step 3892"

    # Operation 3893: delete
    del tree[1078]
    del reference[1078]
    assert tree.invariants(), "Invariants failed at step 3893"

    # Operation 3894: get
    assert tree.invariants(), "Invariants failed at step 3894"

    # Operation 3895: get
    assert tree.invariants(), "Invariants failed at step 3895"

    # Operation 3896: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3896"

    # Operation 3897: get
    assert tree.invariants(), "Invariants failed at step 3897"

    # Operation 3898: update
    tree[9652] = 'value_44714'
    reference[9652] = 'value_44714'
    assert tree.invariants(), "Invariants failed at step 3898"

    # Operation 3899: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3899"

    # Operation 3900: get
    assert tree.invariants(), "Invariants failed at step 3900"

    # Operation 3901: insert
    tree[7429] = 'value_652939'
    reference[7429] = 'value_652939'
    assert tree.invariants(), "Invariants failed at step 3901"

    # Operation 3902: get
    assert tree.invariants(), "Invariants failed at step 3902"

    # Operation 3903: delete
    del tree[7429]
    del reference[7429]
    assert tree.invariants(), "Invariants failed at step 3903"

    # Operation 3904: delete
    del tree[9652]
    del reference[9652]
    assert tree.invariants(), "Invariants failed at step 3904"

    # Operation 3905: get
    assert tree.invariants(), "Invariants failed at step 3905"

    # Operation 3906: insert
    tree[718] = 'value_696258'
    reference[718] = 'value_696258'
    assert tree.invariants(), "Invariants failed at step 3906"

    # Operation 3907: get
    assert tree.invariants(), "Invariants failed at step 3907"

    # Operation 3908: get
    assert tree.invariants(), "Invariants failed at step 3908"

    # Operation 3909: delete
    del tree[718]
    del reference[718]
    assert tree.invariants(), "Invariants failed at step 3909"

    # Operation 3910: insert
    tree[9209] = 'value_240673'
    reference[9209] = 'value_240673'
    assert tree.invariants(), "Invariants failed at step 3910"

    # Operation 3911: get
    assert tree.invariants(), "Invariants failed at step 3911"

    # Operation 3912: update
    tree[9209] = 'value_645170'
    reference[9209] = 'value_645170'
    assert tree.invariants(), "Invariants failed at step 3912"

    # Operation 3913: delete
    del tree[9209]
    del reference[9209]
    assert tree.invariants(), "Invariants failed at step 3913"

    # Operation 3914: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3914"

    # Operation 3915: insert
    tree[5426] = 'value_744438'
    reference[5426] = 'value_744438'
    assert tree.invariants(), "Invariants failed at step 3915"

    # Operation 3916: delete
    del tree[5426]
    del reference[5426]
    assert tree.invariants(), "Invariants failed at step 3916"

    # Operation 3917: insert
    tree[4922] = 'value_259571'
    reference[4922] = 'value_259571'
    assert tree.invariants(), "Invariants failed at step 3917"

    # Operation 3918: delete
    del tree[4922]
    del reference[4922]
    assert tree.invariants(), "Invariants failed at step 3918"

    # Operation 3919: insert
    tree[9647] = 'value_568452'
    reference[9647] = 'value_568452'
    assert tree.invariants(), "Invariants failed at step 3919"

    # Operation 3920: insert
    tree[3457] = 'value_619590'
    reference[3457] = 'value_619590'
    assert tree.invariants(), "Invariants failed at step 3920"

    # Operation 3921: update
    tree[3457] = 'value_640783'
    reference[3457] = 'value_640783'
    assert tree.invariants(), "Invariants failed at step 3921"

    # Operation 3922: insert
    tree[5976] = 'value_281148'
    reference[5976] = 'value_281148'
    assert tree.invariants(), "Invariants failed at step 3922"

    # Operation 3923: update
    tree[5976] = 'value_114557'
    reference[5976] = 'value_114557'
    assert tree.invariants(), "Invariants failed at step 3923"

    # Operation 3924: update
    tree[9647] = 'value_82261'
    reference[9647] = 'value_82261'
    assert tree.invariants(), "Invariants failed at step 3924"

    # Operation 3925: delete
    del tree[9647]
    del reference[9647]
    assert tree.invariants(), "Invariants failed at step 3925"

    # Operation 3926: get
    assert tree.invariants(), "Invariants failed at step 3926"

    # Operation 3927: insert
    tree[2964] = 'value_792016'
    reference[2964] = 'value_792016'
    assert tree.invariants(), "Invariants failed at step 3927"

    # Operation 3928: insert
    tree[7236] = 'value_903745'
    reference[7236] = 'value_903745'
    assert tree.invariants(), "Invariants failed at step 3928"

    # Operation 3929: get
    assert tree.invariants(), "Invariants failed at step 3929"

    # Operation 3930: insert
    tree[59] = 'value_984425'
    reference[59] = 'value_984425'
    assert tree.invariants(), "Invariants failed at step 3930"

    # Operation 3931: insert
    tree[7954] = 'value_683049'
    reference[7954] = 'value_683049'
    assert tree.invariants(), "Invariants failed at step 3931"

    # Operation 3932: delete
    del tree[2964]
    del reference[2964]
    assert tree.invariants(), "Invariants failed at step 3932"

    # Operation 3933: delete
    del tree[7954]
    del reference[7954]
    assert tree.invariants(), "Invariants failed at step 3933"

    # Operation 3934: insert
    tree[9093] = 'value_594419'
    reference[9093] = 'value_594419'
    assert tree.invariants(), "Invariants failed at step 3934"

    # Operation 3935: insert
    tree[183] = 'value_8206'
    reference[183] = 'value_8206'
    assert tree.invariants(), "Invariants failed at step 3935"

    # Operation 3936: delete
    del tree[7236]
    del reference[7236]
    assert tree.invariants(), "Invariants failed at step 3936"

    # Operation 3937: update
    tree[5976] = 'value_797819'
    reference[5976] = 'value_797819'
    assert tree.invariants(), "Invariants failed at step 3937"

    # Operation 3938: update
    tree[183] = 'value_187406'
    reference[183] = 'value_187406'
    assert tree.invariants(), "Invariants failed at step 3938"

    # Operation 3939: update
    tree[183] = 'value_120330'
    reference[183] = 'value_120330'
    assert tree.invariants(), "Invariants failed at step 3939"

    # Operation 3940: insert
    tree[7722] = 'value_607367'
    reference[7722] = 'value_607367'
    assert tree.invariants(), "Invariants failed at step 3940"

    # Operation 3941: get
    assert tree.invariants(), "Invariants failed at step 3941"

    # Operation 3942: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3942"

    # Operation 3943: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3943"

    # Operation 3944: delete
    del tree[5976]
    del reference[5976]
    assert tree.invariants(), "Invariants failed at step 3944"

    # Operation 3945: get
    assert tree.invariants(), "Invariants failed at step 3945"

    # Operation 3946: insert
    tree[2003] = 'value_409776'
    reference[2003] = 'value_409776'
    assert tree.invariants(), "Invariants failed at step 3946"

    # Operation 3947: insert
    tree[6351] = 'value_392959'
    reference[6351] = 'value_392959'
    assert tree.invariants(), "Invariants failed at step 3947"

    # Operation 3948: insert
    tree[3068] = 'value_666310'
    reference[3068] = 'value_666310'
    assert tree.invariants(), "Invariants failed at step 3948"

    # Operation 3949: batch_delete
    keys_to_delete = [3457, 2003, 3772, 5454]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3949"

    # Operation 3950: get
    assert tree.invariants(), "Invariants failed at step 3950"

    # Operation 3951: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3951"

    # Operation 3952: insert
    tree[3074] = 'value_818800'
    reference[3074] = 'value_818800'
    assert tree.invariants(), "Invariants failed at step 3952"

    # Operation 3953: insert
    tree[686] = 'value_191582'
    reference[686] = 'value_191582'
    assert tree.invariants(), "Invariants failed at step 3953"

    # Operation 3954: get
    assert tree.invariants(), "Invariants failed at step 3954"

    # Operation 3955: get
    assert tree.invariants(), "Invariants failed at step 3955"

    # Operation 3956: get
    assert tree.invariants(), "Invariants failed at step 3956"

    # Operation 3957: delete
    del tree[3074]
    del reference[3074]
    assert tree.invariants(), "Invariants failed at step 3957"

    # Operation 3958: insert
    tree[4782] = 'value_917305'
    reference[4782] = 'value_917305'
    assert tree.invariants(), "Invariants failed at step 3958"

    # Operation 3959: update
    tree[59] = 'value_595849'
    reference[59] = 'value_595849'
    assert tree.invariants(), "Invariants failed at step 3959"

    # Operation 3960: insert
    tree[829] = 'value_725693'
    reference[829] = 'value_725693'
    assert tree.invariants(), "Invariants failed at step 3960"

    # Operation 3961: batch_delete
    keys_to_delete = [3558, 7722, 4782, 183, 59, 6524]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3961"

    # Operation 3962: delete
    del tree[9093]
    del reference[9093]
    assert tree.invariants(), "Invariants failed at step 3962"

    # Operation 3963: insert
    tree[976] = 'value_909846'
    reference[976] = 'value_909846'
    assert tree.invariants(), "Invariants failed at step 3963"

    # Operation 3964: insert
    tree[2297] = 'value_787560'
    reference[2297] = 'value_787560'
    assert tree.invariants(), "Invariants failed at step 3964"

    # Operation 3965: delete
    del tree[2297]
    del reference[2297]
    assert tree.invariants(), "Invariants failed at step 3965"

    # Operation 3966: batch_delete
    keys_to_delete = [5002, 7678, 686, 6351]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 3966"

    # Operation 3967: insert
    tree[5142] = 'value_341120'
    reference[5142] = 'value_341120'
    assert tree.invariants(), "Invariants failed at step 3967"

    # Operation 3968: insert
    tree[1913] = 'value_615050'
    reference[1913] = 'value_615050'
    assert tree.invariants(), "Invariants failed at step 3968"

    # Operation 3969: get
    assert tree.invariants(), "Invariants failed at step 3969"

    # Operation 3970: delete
    del tree[829]
    del reference[829]
    assert tree.invariants(), "Invariants failed at step 3970"

    # Operation 3971: delete
    del tree[1913]
    del reference[1913]
    assert tree.invariants(), "Invariants failed at step 3971"

    # Operation 3972: insert
    tree[4935] = 'value_108403'
    reference[4935] = 'value_108403'
    assert tree.invariants(), "Invariants failed at step 3972"

    # Operation 3973: delete
    del tree[5142]
    del reference[5142]
    assert tree.invariants(), "Invariants failed at step 3973"

    # Operation 3974: update
    tree[976] = 'value_77105'
    reference[976] = 'value_77105'
    assert tree.invariants(), "Invariants failed at step 3974"

    # Operation 3975: delete
    del tree[4935]
    del reference[4935]
    assert tree.invariants(), "Invariants failed at step 3975"

    # Operation 3976: get
    assert tree.invariants(), "Invariants failed at step 3976"

    # Operation 3977: insert
    tree[2821] = 'value_571102'
    reference[2821] = 'value_571102'
    assert tree.invariants(), "Invariants failed at step 3977"

    # Operation 3978: insert
    tree[1150] = 'value_762589'
    reference[1150] = 'value_762589'
    assert tree.invariants(), "Invariants failed at step 3978"

    # Operation 3979: get
    assert tree.invariants(), "Invariants failed at step 3979"

    # Operation 3980: insert
    tree[4891] = 'value_554052'
    reference[4891] = 'value_554052'
    assert tree.invariants(), "Invariants failed at step 3980"

    # Operation 3981: insert
    tree[9788] = 'value_888755'
    reference[9788] = 'value_888755'
    assert tree.invariants(), "Invariants failed at step 3981"

    # Operation 3982: get
    assert tree.invariants(), "Invariants failed at step 3982"

    # Operation 3983: delete
    del tree[976]
    del reference[976]
    assert tree.invariants(), "Invariants failed at step 3983"

    # Operation 3984: insert
    tree[3783] = 'value_8253'
    reference[3783] = 'value_8253'
    assert tree.invariants(), "Invariants failed at step 3984"

    # Operation 3985: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 3985"

    # Operation 3986: delete
    del tree[9788]
    del reference[9788]
    assert tree.invariants(), "Invariants failed at step 3986"

    # Operation 3987: update
    tree[2821] = 'value_553297'
    reference[2821] = 'value_553297'
    assert tree.invariants(), "Invariants failed at step 3987"

    # Operation 3988: delete
    del tree[4891]
    del reference[4891]
    assert tree.invariants(), "Invariants failed at step 3988"

    # Operation 3989: get
    assert tree.invariants(), "Invariants failed at step 3989"

    # Operation 3990: insert
    tree[34] = 'value_891031'
    reference[34] = 'value_891031'
    assert tree.invariants(), "Invariants failed at step 3990"

    # Operation 3991: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 3991"

    # Operation 3992: get
    assert tree.invariants(), "Invariants failed at step 3992"

    # Operation 3993: delete
    del tree[2821]
    del reference[2821]
    assert tree.invariants(), "Invariants failed at step 3993"

    # Operation 3994: get
    assert tree.invariants(), "Invariants failed at step 3994"

    # Operation 3995: insert
    tree[6205] = 'value_776719'
    reference[6205] = 'value_776719'
    assert tree.invariants(), "Invariants failed at step 3995"

    # Operation 3996: get
    assert tree.invariants(), "Invariants failed at step 3996"

    # Operation 3997: delete
    del tree[6205]
    del reference[6205]
    assert tree.invariants(), "Invariants failed at step 3997"

    # Operation 3998: get
    assert tree.invariants(), "Invariants failed at step 3998"

    # Operation 3999: delete
    del tree[3068]
    del reference[3068]
    assert tree.invariants(), "Invariants failed at step 3999"

    # Operation 4000: get
    assert tree.invariants(), "Invariants failed at step 4000"

    # Operation 4001: insert
    tree[4055] = 'value_458757'
    reference[4055] = 'value_458757'
    assert tree.invariants(), "Invariants failed at step 4001"

    # Operation 4002: update
    tree[3783] = 'value_672750'
    reference[3783] = 'value_672750'
    assert tree.invariants(), "Invariants failed at step 4002"

    # Operation 4003: delete
    del tree[1150]
    del reference[1150]
    assert tree.invariants(), "Invariants failed at step 4003"

    # Operation 4004: get
    assert tree.invariants(), "Invariants failed at step 4004"

    # Operation 4005: insert
    tree[417] = 'value_743066'
    reference[417] = 'value_743066'
    assert tree.invariants(), "Invariants failed at step 4005"

    # Operation 4006: get
    assert tree.invariants(), "Invariants failed at step 4006"

    # Operation 4007: delete
    del tree[34]
    del reference[34]
    assert tree.invariants(), "Invariants failed at step 4007"

    # Operation 4008: insert
    tree[7260] = 'value_253062'
    reference[7260] = 'value_253062'
    assert tree.invariants(), "Invariants failed at step 4008"

    # Operation 4009: insert
    tree[8207] = 'value_679433'
    reference[8207] = 'value_679433'
    assert tree.invariants(), "Invariants failed at step 4009"

    # Operation 4010: delete
    del tree[3783]
    del reference[3783]
    assert tree.invariants(), "Invariants failed at step 4010"

    # Operation 4011: delete
    del tree[7260]
    del reference[7260]
    assert tree.invariants(), "Invariants failed at step 4011"

    # Operation 4012: delete
    del tree[4055]
    del reference[4055]
    assert tree.invariants(), "Invariants failed at step 4012"

    # Operation 4013: delete
    del tree[417]
    del reference[417]
    assert tree.invariants(), "Invariants failed at step 4013"

    # Operation 4014: delete
    del tree[8207]
    del reference[8207]
    assert tree.invariants(), "Invariants failed at step 4014"

    # Operation 4015: insert
    tree[1160] = 'value_237835'
    reference[1160] = 'value_237835'
    assert tree.invariants(), "Invariants failed at step 4015"

    # Operation 4016: delete
    del tree[1160]
    del reference[1160]
    assert tree.invariants(), "Invariants failed at step 4016"

    # Operation 4017: get
    assert tree.invariants(), "Invariants failed at step 4017"

    # Operation 4018: insert
    tree[9759] = 'value_954406'
    reference[9759] = 'value_954406'
    assert tree.invariants(), "Invariants failed at step 4018"

    # Operation 4019: update
    tree[9759] = 'value_856326'
    reference[9759] = 'value_856326'
    assert tree.invariants(), "Invariants failed at step 4019"

    # Operation 4020: insert
    tree[3791] = 'value_209410'
    reference[3791] = 'value_209410'
    assert tree.invariants(), "Invariants failed at step 4020"

    # Operation 4021: update
    tree[3791] = 'value_792656'
    reference[3791] = 'value_792656'
    assert tree.invariants(), "Invariants failed at step 4021"

    # Operation 4022: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4022"

    # Operation 4023: get
    assert tree.invariants(), "Invariants failed at step 4023"

    # Operation 4024: insert
    tree[1491] = 'value_17332'
    reference[1491] = 'value_17332'
    assert tree.invariants(), "Invariants failed at step 4024"

    # Operation 4025: delete
    del tree[3791]
    del reference[3791]
    assert tree.invariants(), "Invariants failed at step 4025"

    # Operation 4026: delete
    del tree[9759]
    del reference[9759]
    assert tree.invariants(), "Invariants failed at step 4026"

    # Operation 4027: get
    assert tree.invariants(), "Invariants failed at step 4027"

    # Operation 4028: get
    assert tree.invariants(), "Invariants failed at step 4028"

    # Operation 4029: insert
    tree[5716] = 'value_370666'
    reference[5716] = 'value_370666'
    assert tree.invariants(), "Invariants failed at step 4029"

    # Operation 4030: update
    tree[1491] = 'value_988145'
    reference[1491] = 'value_988145'
    assert tree.invariants(), "Invariants failed at step 4030"

    # Operation 4031: get
    assert tree.invariants(), "Invariants failed at step 4031"

    # Operation 4032: delete
    del tree[1491]
    del reference[1491]
    assert tree.invariants(), "Invariants failed at step 4032"

    # Operation 4033: insert
    tree[8398] = 'value_786425'
    reference[8398] = 'value_786425'
    assert tree.invariants(), "Invariants failed at step 4033"

    # Operation 4034: get
    assert tree.invariants(), "Invariants failed at step 4034"

    # Operation 4035: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4035"

    # Operation 4036: insert
    tree[4258] = 'value_880181'
    reference[4258] = 'value_880181'
    assert tree.invariants(), "Invariants failed at step 4036"

    # Operation 4037: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4037"

    # Operation 4038: get
    assert tree.invariants(), "Invariants failed at step 4038"

    # Operation 4039: get
    assert tree.invariants(), "Invariants failed at step 4039"

    # Operation 4040: update
    tree[5716] = 'value_203372'
    reference[5716] = 'value_203372'
    assert tree.invariants(), "Invariants failed at step 4040"

    # Operation 4041: delete
    del tree[8398]
    del reference[8398]
    assert tree.invariants(), "Invariants failed at step 4041"

    # Operation 4042: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4042"

    # Operation 4043: delete
    del tree[5716]
    del reference[5716]
    assert tree.invariants(), "Invariants failed at step 4043"

    # Operation 4044: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4044"

    # Operation 4045: delete
    del tree[4258]
    del reference[4258]
    assert tree.invariants(), "Invariants failed at step 4045"

    # Operation 4046: get
    assert tree.invariants(), "Invariants failed at step 4046"

    # Operation 4047: get
    assert tree.invariants(), "Invariants failed at step 4047"

    # Operation 4048: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4048"

    # Operation 4049: insert
    tree[8235] = 'value_422265'
    reference[8235] = 'value_422265'
    assert tree.invariants(), "Invariants failed at step 4049"

    # Operation 4050: insert
    tree[7790] = 'value_154982'
    reference[7790] = 'value_154982'
    assert tree.invariants(), "Invariants failed at step 4050"

    # Operation 4051: delete
    del tree[7790]
    del reference[7790]
    assert tree.invariants(), "Invariants failed at step 4051"

    # Operation 4052: delete
    del tree[8235]
    del reference[8235]
    assert tree.invariants(), "Invariants failed at step 4052"

    # Operation 4053: insert
    tree[4974] = 'value_603553'
    reference[4974] = 'value_603553'
    assert tree.invariants(), "Invariants failed at step 4053"

    # Operation 4054: update
    tree[4974] = 'value_630027'
    reference[4974] = 'value_630027'
    assert tree.invariants(), "Invariants failed at step 4054"

    # Operation 4055: delete
    del tree[4974]
    del reference[4974]
    assert tree.invariants(), "Invariants failed at step 4055"

    # Operation 4056: insert
    tree[5113] = 'value_462149'
    reference[5113] = 'value_462149'
    assert tree.invariants(), "Invariants failed at step 4056"

    # Operation 4057: delete
    del tree[5113]
    del reference[5113]
    assert tree.invariants(), "Invariants failed at step 4057"

    # Operation 4058: insert
    tree[7632] = 'value_740762'
    reference[7632] = 'value_740762'
    assert tree.invariants(), "Invariants failed at step 4058"

    # Operation 4059: update
    tree[7632] = 'value_261030'
    reference[7632] = 'value_261030'
    assert tree.invariants(), "Invariants failed at step 4059"

    # Operation 4060: get
    assert tree.invariants(), "Invariants failed at step 4060"

    # Operation 4061: get
    assert tree.invariants(), "Invariants failed at step 4061"

    # Operation 4062: delete
    del tree[7632]
    del reference[7632]
    assert tree.invariants(), "Invariants failed at step 4062"

    # Operation 4063: insert
    tree[19] = 'value_681329'
    reference[19] = 'value_681329'
    assert tree.invariants(), "Invariants failed at step 4063"

    # Operation 4064: insert
    tree[8088] = 'value_858803'
    reference[8088] = 'value_858803'
    assert tree.invariants(), "Invariants failed at step 4064"

    # Operation 4065: get
    assert tree.invariants(), "Invariants failed at step 4065"

    # Operation 4066: get
    assert tree.invariants(), "Invariants failed at step 4066"

    # Operation 4067: insert
    tree[8882] = 'value_651461'
    reference[8882] = 'value_651461'
    assert tree.invariants(), "Invariants failed at step 4067"

    # Operation 4068: insert
    tree[426] = 'value_519945'
    reference[426] = 'value_519945'
    assert tree.invariants(), "Invariants failed at step 4068"

    # Operation 4069: insert
    tree[9110] = 'value_536037'
    reference[9110] = 'value_536037'
    assert tree.invariants(), "Invariants failed at step 4069"

    # Operation 4070: insert
    tree[5059] = 'value_547170'
    reference[5059] = 'value_547170'
    assert tree.invariants(), "Invariants failed at step 4070"

    # Operation 4071: insert
    tree[6604] = 'value_989837'
    reference[6604] = 'value_989837'
    assert tree.invariants(), "Invariants failed at step 4071"

    # Operation 4072: update
    tree[8882] = 'value_304706'
    reference[8882] = 'value_304706'
    assert tree.invariants(), "Invariants failed at step 4072"

    # Operation 4073: delete
    del tree[8882]
    del reference[8882]
    assert tree.invariants(), "Invariants failed at step 4073"

    # Operation 4074: insert
    tree[1452] = 'value_382753'
    reference[1452] = 'value_382753'
    assert tree.invariants(), "Invariants failed at step 4074"

    # Operation 4075: insert
    tree[1656] = 'value_18845'
    reference[1656] = 'value_18845'
    assert tree.invariants(), "Invariants failed at step 4075"

    # Operation 4076: insert
    tree[2400] = 'value_470226'
    reference[2400] = 'value_470226'
    assert tree.invariants(), "Invariants failed at step 4076"

    # Operation 4077: update
    tree[426] = 'value_857481'
    reference[426] = 'value_857481'
    assert tree.invariants(), "Invariants failed at step 4077"

    # Operation 4078: get
    assert tree.invariants(), "Invariants failed at step 4078"

    # Operation 4079: batch_delete
    keys_to_delete = [2400, 5059, 426, 6604, 9110, 1656]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4079"

    # Operation 4080: delete
    del tree[19]
    del reference[19]
    assert tree.invariants(), "Invariants failed at step 4080"

    # Operation 4081: delete
    del tree[1452]
    del reference[1452]
    assert tree.invariants(), "Invariants failed at step 4081"

    # Operation 4082: update
    tree[8088] = 'value_689197'
    reference[8088] = 'value_689197'
    assert tree.invariants(), "Invariants failed at step 4082"

    # Operation 4083: insert
    tree[40] = 'value_877504'
    reference[40] = 'value_877504'
    assert tree.invariants(), "Invariants failed at step 4083"

    # Operation 4084: get
    assert tree.invariants(), "Invariants failed at step 4084"

    # Operation 4085: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4085"

    # Operation 4086: update
    tree[40] = 'value_326550'
    reference[40] = 'value_326550'
    assert tree.invariants(), "Invariants failed at step 4086"

    # Operation 4087: delete
    del tree[40]
    del reference[40]
    assert tree.invariants(), "Invariants failed at step 4087"

    # Operation 4088: get
    assert tree.invariants(), "Invariants failed at step 4088"

    # Operation 4089: delete
    del tree[8088]
    del reference[8088]
    assert tree.invariants(), "Invariants failed at step 4089"

    # Operation 4090: insert
    tree[7735] = 'value_484360'
    reference[7735] = 'value_484360'
    assert tree.invariants(), "Invariants failed at step 4090"

    # Operation 4091: update
    tree[7735] = 'value_176110'
    reference[7735] = 'value_176110'
    assert tree.invariants(), "Invariants failed at step 4091"

    # Operation 4092: delete
    del tree[7735]
    del reference[7735]
    assert tree.invariants(), "Invariants failed at step 4092"

    # Operation 4093: insert
    tree[8281] = 'value_564022'
    reference[8281] = 'value_564022'
    assert tree.invariants(), "Invariants failed at step 4093"

    # Operation 4094: get
    assert tree.invariants(), "Invariants failed at step 4094"

    # Operation 4095: delete
    del tree[8281]
    del reference[8281]
    assert tree.invariants(), "Invariants failed at step 4095"

    # Operation 4096: insert
    tree[589] = 'value_687615'
    reference[589] = 'value_687615'
    assert tree.invariants(), "Invariants failed at step 4096"

    # Operation 4097: insert
    tree[1437] = 'value_636653'
    reference[1437] = 'value_636653'
    assert tree.invariants(), "Invariants failed at step 4097"

    # Operation 4098: get
    assert tree.invariants(), "Invariants failed at step 4098"

    # Operation 4099: get
    assert tree.invariants(), "Invariants failed at step 4099"

    # Operation 4100: get
    assert tree.invariants(), "Invariants failed at step 4100"

    # Operation 4101: get
    assert tree.invariants(), "Invariants failed at step 4101"

    # Operation 4102: update
    tree[1437] = 'value_592204'
    reference[1437] = 'value_592204'
    assert tree.invariants(), "Invariants failed at step 4102"

    # Operation 4103: insert
    tree[1401] = 'value_726902'
    reference[1401] = 'value_726902'
    assert tree.invariants(), "Invariants failed at step 4103"

    # Operation 4104: get
    assert tree.invariants(), "Invariants failed at step 4104"

    # Operation 4105: delete
    del tree[1437]
    del reference[1437]
    assert tree.invariants(), "Invariants failed at step 4105"

    # Operation 4106: get
    assert tree.invariants(), "Invariants failed at step 4106"

    # Operation 4107: delete
    del tree[1401]
    del reference[1401]
    assert tree.invariants(), "Invariants failed at step 4107"

    # Operation 4108: get
    assert tree.invariants(), "Invariants failed at step 4108"

    # Operation 4109: delete
    del tree[589]
    del reference[589]
    assert tree.invariants(), "Invariants failed at step 4109"

    # Operation 4110: get
    assert tree.invariants(), "Invariants failed at step 4110"

    # Operation 4111: insert
    tree[3234] = 'value_841924'
    reference[3234] = 'value_841924'
    assert tree.invariants(), "Invariants failed at step 4111"

    # Operation 4112: delete
    del tree[3234]
    del reference[3234]
    assert tree.invariants(), "Invariants failed at step 4112"

    # Operation 4113: insert
    tree[7303] = 'value_725650'
    reference[7303] = 'value_725650'
    assert tree.invariants(), "Invariants failed at step 4113"

    # Operation 4114: insert
    tree[2486] = 'value_36980'
    reference[2486] = 'value_36980'
    assert tree.invariants(), "Invariants failed at step 4114"

    # Operation 4115: update
    tree[7303] = 'value_143591'
    reference[7303] = 'value_143591'
    assert tree.invariants(), "Invariants failed at step 4115"

    # Operation 4116: insert
    tree[6249] = 'value_80677'
    reference[6249] = 'value_80677'
    assert tree.invariants(), "Invariants failed at step 4116"

    # Operation 4117: get
    assert tree.invariants(), "Invariants failed at step 4117"

    # Operation 4118: delete
    del tree[7303]
    del reference[7303]
    assert tree.invariants(), "Invariants failed at step 4118"

    # Operation 4119: delete
    del tree[6249]
    del reference[6249]
    assert tree.invariants(), "Invariants failed at step 4119"

    # Operation 4120: delete
    del tree[2486]
    del reference[2486]
    assert tree.invariants(), "Invariants failed at step 4120"

    # Operation 4121: insert
    tree[5526] = 'value_522309'
    reference[5526] = 'value_522309'
    assert tree.invariants(), "Invariants failed at step 4121"

    # Operation 4122: delete
    del tree[5526]
    del reference[5526]
    assert tree.invariants(), "Invariants failed at step 4122"

    # Operation 4123: insert
    tree[7652] = 'value_281554'
    reference[7652] = 'value_281554'
    assert tree.invariants(), "Invariants failed at step 4123"

    # Operation 4124: delete
    del tree[7652]
    del reference[7652]
    assert tree.invariants(), "Invariants failed at step 4124"

    # Operation 4125: insert
    tree[8459] = 'value_641711'
    reference[8459] = 'value_641711'
    assert tree.invariants(), "Invariants failed at step 4125"

    # Operation 4126: insert
    tree[560] = 'value_12509'
    reference[560] = 'value_12509'
    assert tree.invariants(), "Invariants failed at step 4126"

    # Operation 4127: get
    assert tree.invariants(), "Invariants failed at step 4127"

    # Operation 4128: insert
    tree[4347] = 'value_527684'
    reference[4347] = 'value_527684'
    assert tree.invariants(), "Invariants failed at step 4128"

    # Operation 4129: insert
    tree[1869] = 'value_570283'
    reference[1869] = 'value_570283'
    assert tree.invariants(), "Invariants failed at step 4129"

    # Operation 4130: insert
    tree[6101] = 'value_833243'
    reference[6101] = 'value_833243'
    assert tree.invariants(), "Invariants failed at step 4130"

    # Operation 4131: delete
    del tree[560]
    del reference[560]
    assert tree.invariants(), "Invariants failed at step 4131"

    # Operation 4132: delete
    del tree[6101]
    del reference[6101]
    assert tree.invariants(), "Invariants failed at step 4132"

    # Operation 4133: insert
    tree[787] = 'value_338266'
    reference[787] = 'value_338266'
    assert tree.invariants(), "Invariants failed at step 4133"

    # Operation 4134: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4134"

    # Operation 4135: get
    assert tree.invariants(), "Invariants failed at step 4135"

    # Operation 4136: get
    assert tree.invariants(), "Invariants failed at step 4136"

    # Operation 4137: insert
    tree[736] = 'value_728235'
    reference[736] = 'value_728235'
    assert tree.invariants(), "Invariants failed at step 4137"

    # Operation 4138: insert
    tree[395] = 'value_736085'
    reference[395] = 'value_736085'
    assert tree.invariants(), "Invariants failed at step 4138"

    # Operation 4139: insert
    tree[5668] = 'value_181956'
    reference[5668] = 'value_181956'
    assert tree.invariants(), "Invariants failed at step 4139"

    # Operation 4140: delete
    del tree[1869]
    del reference[1869]
    assert tree.invariants(), "Invariants failed at step 4140"

    # Operation 4141: get
    assert tree.invariants(), "Invariants failed at step 4141"

    # Operation 4142: insert
    tree[7855] = 'value_150768'
    reference[7855] = 'value_150768'
    assert tree.invariants(), "Invariants failed at step 4142"

    # Operation 4143: delete
    del tree[8459]
    del reference[8459]
    assert tree.invariants(), "Invariants failed at step 4143"

    # Operation 4144: insert
    tree[2564] = 'value_806781'
    reference[2564] = 'value_806781'
    assert tree.invariants(), "Invariants failed at step 4144"

    # Operation 4145: get
    assert tree.invariants(), "Invariants failed at step 4145"

    # Operation 4146: delete
    del tree[395]
    del reference[395]
    assert tree.invariants(), "Invariants failed at step 4146"

    # Operation 4147: delete
    del tree[5668]
    del reference[5668]
    assert tree.invariants(), "Invariants failed at step 4147"

    # Operation 4148: get
    assert tree.invariants(), "Invariants failed at step 4148"

    # Operation 4149: delete
    del tree[2564]
    del reference[2564]
    assert tree.invariants(), "Invariants failed at step 4149"

    # Operation 4150: delete
    del tree[736]
    del reference[736]
    assert tree.invariants(), "Invariants failed at step 4150"

    # Operation 4151: get
    assert tree.invariants(), "Invariants failed at step 4151"

    # Operation 4152: insert
    tree[6196] = 'value_39402'
    reference[6196] = 'value_39402'
    assert tree.invariants(), "Invariants failed at step 4152"

    # Operation 4153: insert
    tree[8830] = 'value_376156'
    reference[8830] = 'value_376156'
    assert tree.invariants(), "Invariants failed at step 4153"

    # Operation 4154: delete
    del tree[6196]
    del reference[6196]
    assert tree.invariants(), "Invariants failed at step 4154"

    # Operation 4155: insert
    tree[8165] = 'value_344937'
    reference[8165] = 'value_344937'
    assert tree.invariants(), "Invariants failed at step 4155"

    # Operation 4156: insert
    tree[7877] = 'value_891439'
    reference[7877] = 'value_891439'
    assert tree.invariants(), "Invariants failed at step 4156"

    # Operation 4157: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4157"

    # Operation 4158: insert
    tree[1204] = 'value_198313'
    reference[1204] = 'value_198313'
    assert tree.invariants(), "Invariants failed at step 4158"

    # Operation 4159: batch_delete
    keys_to_delete = [5037, 2931, 787, 1204, 8830]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4159"

    # Operation 4160: insert
    tree[6187] = 'value_487429'
    reference[6187] = 'value_487429'
    assert tree.invariants(), "Invariants failed at step 4160"

    # Operation 4161: insert
    tree[9298] = 'value_736018'
    reference[9298] = 'value_736018'
    assert tree.invariants(), "Invariants failed at step 4161"

    # Operation 4162: batch_delete
    keys_to_delete = [6565, 6187, 7855, 9298, 7251]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4162"

    # Operation 4163: delete
    del tree[7877]
    del reference[7877]
    assert tree.invariants(), "Invariants failed at step 4163"

    # Operation 4164: insert
    tree[9775] = 'value_419310'
    reference[9775] = 'value_419310'
    assert tree.invariants(), "Invariants failed at step 4164"

    # Operation 4165: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4165"

    # Operation 4166: delete
    del tree[4347]
    del reference[4347]
    assert tree.invariants(), "Invariants failed at step 4166"

    # Operation 4167: delete
    del tree[8165]
    del reference[8165]
    assert tree.invariants(), "Invariants failed at step 4167"

    # Operation 4168: insert
    tree[7187] = 'value_75619'
    reference[7187] = 'value_75619'
    assert tree.invariants(), "Invariants failed at step 4168"

    # Operation 4169: insert
    tree[7656] = 'value_457559'
    reference[7656] = 'value_457559'
    assert tree.invariants(), "Invariants failed at step 4169"

    # Operation 4170: insert
    tree[9180] = 'value_598770'
    reference[9180] = 'value_598770'
    assert tree.invariants(), "Invariants failed at step 4170"

    # Operation 4171: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4171"

    # Operation 4172: get
    assert tree.invariants(), "Invariants failed at step 4172"

    # Operation 4173: delete
    del tree[9775]
    del reference[9775]
    assert tree.invariants(), "Invariants failed at step 4173"

    # Operation 4174: insert
    tree[8451] = 'value_525940'
    reference[8451] = 'value_525940'
    assert tree.invariants(), "Invariants failed at step 4174"

    # Operation 4175: insert
    tree[4154] = 'value_604034'
    reference[4154] = 'value_604034'
    assert tree.invariants(), "Invariants failed at step 4175"

    # Operation 4176: delete
    del tree[9180]
    del reference[9180]
    assert tree.invariants(), "Invariants failed at step 4176"

    # Operation 4177: get
    assert tree.invariants(), "Invariants failed at step 4177"

    # Operation 4178: insert
    tree[6703] = 'value_694691'
    reference[6703] = 'value_694691'
    assert tree.invariants(), "Invariants failed at step 4178"

    # Operation 4179: insert
    tree[6962] = 'value_365357'
    reference[6962] = 'value_365357'
    assert tree.invariants(), "Invariants failed at step 4179"

    # Operation 4180: insert
    tree[6513] = 'value_126614'
    reference[6513] = 'value_126614'
    assert tree.invariants(), "Invariants failed at step 4180"

    # Operation 4181: insert
    tree[1173] = 'value_353214'
    reference[1173] = 'value_353214'
    assert tree.invariants(), "Invariants failed at step 4181"

    # Operation 4182: delete
    del tree[6703]
    del reference[6703]
    assert tree.invariants(), "Invariants failed at step 4182"

    # Operation 4183: delete
    del tree[7656]
    del reference[7656]
    assert tree.invariants(), "Invariants failed at step 4183"

    # Operation 4184: insert
    tree[1038] = 'value_199154'
    reference[1038] = 'value_199154'
    assert tree.invariants(), "Invariants failed at step 4184"

    # Operation 4185: get
    assert tree.invariants(), "Invariants failed at step 4185"

    # Operation 4186: insert
    tree[170] = 'value_902123'
    reference[170] = 'value_902123'
    assert tree.invariants(), "Invariants failed at step 4186"

    # Operation 4187: insert
    tree[7805] = 'value_610175'
    reference[7805] = 'value_610175'
    assert tree.invariants(), "Invariants failed at step 4187"

    # Operation 4188: insert
    tree[6159] = 'value_228049'
    reference[6159] = 'value_228049'
    assert tree.invariants(), "Invariants failed at step 4188"

    # Operation 4189: delete
    del tree[6962]
    del reference[6962]
    assert tree.invariants(), "Invariants failed at step 4189"

    # Operation 4190: insert
    tree[7524] = 'value_620121'
    reference[7524] = 'value_620121'
    assert tree.invariants(), "Invariants failed at step 4190"

    # Operation 4191: get
    assert tree.invariants(), "Invariants failed at step 4191"

    # Operation 4192: batch_delete
    keys_to_delete = [170, 4154, 4468, 102]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4192"

    # Operation 4193: update
    tree[7524] = 'value_457595'
    reference[7524] = 'value_457595'
    assert tree.invariants(), "Invariants failed at step 4193"

    # Operation 4194: update
    tree[6513] = 'value_109134'
    reference[6513] = 'value_109134'
    assert tree.invariants(), "Invariants failed at step 4194"

    # Operation 4195: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4195"

    # Operation 4196: batch_delete
    keys_to_delete = [8451, 3018, 1038, 6159, 7630, 6513]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4196"

    # Operation 4197: delete
    del tree[1173]
    del reference[1173]
    assert tree.invariants(), "Invariants failed at step 4197"

    # Operation 4198: delete
    del tree[7187]
    del reference[7187]
    assert tree.invariants(), "Invariants failed at step 4198"

    # Operation 4199: insert
    tree[4759] = 'value_564088'
    reference[4759] = 'value_564088'
    assert tree.invariants(), "Invariants failed at step 4199"

    # Operation 4200: update
    tree[7524] = 'value_445494'
    reference[7524] = 'value_445494'
    assert tree.invariants(), "Invariants failed at step 4200"

    # Operation 4201: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4201"

    # Operation 4202: delete
    del tree[7805]
    del reference[7805]
    assert tree.invariants(), "Invariants failed at step 4202"

    # Operation 4203: insert
    tree[621] = 'value_296018'
    reference[621] = 'value_296018'
    assert tree.invariants(), "Invariants failed at step 4203"

    # Operation 4204: delete
    del tree[7524]
    del reference[7524]
    assert tree.invariants(), "Invariants failed at step 4204"

    # Operation 4205: delete
    del tree[621]
    del reference[621]
    assert tree.invariants(), "Invariants failed at step 4205"

    # Operation 4206: insert
    tree[3238] = 'value_848360'
    reference[3238] = 'value_848360'
    assert tree.invariants(), "Invariants failed at step 4206"

    # Operation 4207: get
    assert tree.invariants(), "Invariants failed at step 4207"

    # Operation 4208: get
    assert tree.invariants(), "Invariants failed at step 4208"

    # Operation 4209: update
    tree[3238] = 'value_852553'
    reference[3238] = 'value_852553'
    assert tree.invariants(), "Invariants failed at step 4209"

    # Operation 4210: insert
    tree[7442] = 'value_298944'
    reference[7442] = 'value_298944'
    assert tree.invariants(), "Invariants failed at step 4210"

    # Operation 4211: insert
    tree[4687] = 'value_852602'
    reference[4687] = 'value_852602'
    assert tree.invariants(), "Invariants failed at step 4211"

    # Operation 4212: update
    tree[3238] = 'value_145555'
    reference[3238] = 'value_145555'
    assert tree.invariants(), "Invariants failed at step 4212"

    # Operation 4213: delete
    del tree[4687]
    del reference[4687]
    assert tree.invariants(), "Invariants failed at step 4213"

    # Operation 4214: insert
    tree[2680] = 'value_184893'
    reference[2680] = 'value_184893'
    assert tree.invariants(), "Invariants failed at step 4214"

    # Operation 4215: insert
    tree[260] = 'value_121530'
    reference[260] = 'value_121530'
    assert tree.invariants(), "Invariants failed at step 4215"

    # Operation 4216: insert
    tree[3921] = 'value_656008'
    reference[3921] = 'value_656008'
    assert tree.invariants(), "Invariants failed at step 4216"

    # Operation 4217: delete
    del tree[3238]
    del reference[3238]
    assert tree.invariants(), "Invariants failed at step 4217"

    # Operation 4218: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4218"

    # Operation 4219: get
    assert tree.invariants(), "Invariants failed at step 4219"

    # Operation 4220: get
    assert tree.invariants(), "Invariants failed at step 4220"

    # Operation 4221: insert
    tree[3986] = 'value_402283'
    reference[3986] = 'value_402283'
    assert tree.invariants(), "Invariants failed at step 4221"

    # Operation 4222: batch_delete
    keys_to_delete = [2680, 4351, 9332, 4759]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4222"

    # Operation 4223: insert
    tree[5556] = 'value_787721'
    reference[5556] = 'value_787721'
    assert tree.invariants(), "Invariants failed at step 4223"

    # Operation 4224: update
    tree[7442] = 'value_190578'
    reference[7442] = 'value_190578'
    assert tree.invariants(), "Invariants failed at step 4224"

    # Operation 4225: delete
    del tree[5556]
    del reference[5556]
    assert tree.invariants(), "Invariants failed at step 4225"

    # Operation 4226: insert
    tree[8508] = 'value_466672'
    reference[8508] = 'value_466672'
    assert tree.invariants(), "Invariants failed at step 4226"

    # Operation 4227: insert
    tree[877] = 'value_145567'
    reference[877] = 'value_145567'
    assert tree.invariants(), "Invariants failed at step 4227"

    # Operation 4228: batch_delete
    keys_to_delete = [1731, 260, 3986, 8508, 7292]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4228"

    # Operation 4229: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4229"

    # Operation 4230: insert
    tree[9126] = 'value_623232'
    reference[9126] = 'value_623232'
    assert tree.invariants(), "Invariants failed at step 4230"

    # Operation 4231: insert
    tree[2544] = 'value_587120'
    reference[2544] = 'value_587120'
    assert tree.invariants(), "Invariants failed at step 4231"

    # Operation 4232: get
    assert tree.invariants(), "Invariants failed at step 4232"

    # Operation 4233: update
    tree[877] = 'value_664601'
    reference[877] = 'value_664601'
    assert tree.invariants(), "Invariants failed at step 4233"

    # Operation 4234: insert
    tree[9001] = 'value_873283'
    reference[9001] = 'value_873283'
    assert tree.invariants(), "Invariants failed at step 4234"

    # Operation 4235: delete
    del tree[9001]
    del reference[9001]
    assert tree.invariants(), "Invariants failed at step 4235"

    # Operation 4236: delete
    del tree[877]
    del reference[877]
    assert tree.invariants(), "Invariants failed at step 4236"

    # Operation 4237: get
    assert tree.invariants(), "Invariants failed at step 4237"

    # Operation 4238: insert
    tree[5109] = 'value_53637'
    reference[5109] = 'value_53637'
    assert tree.invariants(), "Invariants failed at step 4238"

    # Operation 4239: insert
    tree[9127] = 'value_358930'
    reference[9127] = 'value_358930'
    assert tree.invariants(), "Invariants failed at step 4239"

    # Operation 4240: update
    tree[9127] = 'value_912496'
    reference[9127] = 'value_912496'
    assert tree.invariants(), "Invariants failed at step 4240"

    # Operation 4241: batch_delete
    keys_to_delete = [9127, 7442, 5109, 1212, 2430]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4241"

    # Operation 4242: delete
    del tree[3921]
    del reference[3921]
    assert tree.invariants(), "Invariants failed at step 4242"

    # Operation 4243: insert
    tree[7048] = 'value_324977'
    reference[7048] = 'value_324977'
    assert tree.invariants(), "Invariants failed at step 4243"

    # Operation 4244: update
    tree[7048] = 'value_859765'
    reference[7048] = 'value_859765'
    assert tree.invariants(), "Invariants failed at step 4244"

    # Operation 4245: update
    tree[9126] = 'value_263252'
    reference[9126] = 'value_263252'
    assert tree.invariants(), "Invariants failed at step 4245"

    # Operation 4246: insert
    tree[6337] = 'value_60001'
    reference[6337] = 'value_60001'
    assert tree.invariants(), "Invariants failed at step 4246"

    # Operation 4247: delete
    del tree[6337]
    del reference[6337]
    assert tree.invariants(), "Invariants failed at step 4247"

    # Operation 4248: insert
    tree[6059] = 'value_287038'
    reference[6059] = 'value_287038'
    assert tree.invariants(), "Invariants failed at step 4248"

    # Operation 4249: get
    assert tree.invariants(), "Invariants failed at step 4249"

    # Operation 4250: update
    tree[9126] = 'value_676469'
    reference[9126] = 'value_676469'
    assert tree.invariants(), "Invariants failed at step 4250"

    # Operation 4251: get
    assert tree.invariants(), "Invariants failed at step 4251"

    # Operation 4252: delete
    del tree[9126]
    del reference[9126]
    assert tree.invariants(), "Invariants failed at step 4252"

    # Operation 4253: delete
    del tree[2544]
    del reference[2544]
    assert tree.invariants(), "Invariants failed at step 4253"

    # Operation 4254: delete
    del tree[7048]
    del reference[7048]
    assert tree.invariants(), "Invariants failed at step 4254"

    # Operation 4255: insert
    tree[3920] = 'value_176452'
    reference[3920] = 'value_176452'
    assert tree.invariants(), "Invariants failed at step 4255"

    # Operation 4256: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4256"

    # Operation 4257: insert
    tree[9428] = 'value_245676'
    reference[9428] = 'value_245676'
    assert tree.invariants(), "Invariants failed at step 4257"

    # Operation 4258: delete
    del tree[9428]
    del reference[9428]
    assert tree.invariants(), "Invariants failed at step 4258"

    # Operation 4259: insert
    tree[8049] = 'value_104152'
    reference[8049] = 'value_104152'
    assert tree.invariants(), "Invariants failed at step 4259"

    # Operation 4260: update
    tree[3920] = 'value_816958'
    reference[3920] = 'value_816958'
    assert tree.invariants(), "Invariants failed at step 4260"

    # Operation 4261: insert
    tree[4508] = 'value_150711'
    reference[4508] = 'value_150711'
    assert tree.invariants(), "Invariants failed at step 4261"

    # Operation 4262: delete
    del tree[6059]
    del reference[6059]
    assert tree.invariants(), "Invariants failed at step 4262"

    # Operation 4263: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4263"

    # Operation 4264: insert
    tree[2821] = 'value_425277'
    reference[2821] = 'value_425277'
    assert tree.invariants(), "Invariants failed at step 4264"

    # Operation 4265: delete
    del tree[2821]
    del reference[2821]
    assert tree.invariants(), "Invariants failed at step 4265"

    # Operation 4266: delete
    del tree[4508]
    del reference[4508]
    assert tree.invariants(), "Invariants failed at step 4266"

    # Operation 4267: insert
    tree[4534] = 'value_235139'
    reference[4534] = 'value_235139'
    assert tree.invariants(), "Invariants failed at step 4267"

    # Operation 4268: delete
    del tree[3920]
    del reference[3920]
    assert tree.invariants(), "Invariants failed at step 4268"

    # Operation 4269: insert
    tree[6040] = 'value_948915'
    reference[6040] = 'value_948915'
    assert tree.invariants(), "Invariants failed at step 4269"

    # Operation 4270: update
    tree[8049] = 'value_350245'
    reference[8049] = 'value_350245'
    assert tree.invariants(), "Invariants failed at step 4270"

    # Operation 4271: update
    tree[6040] = 'value_979639'
    reference[6040] = 'value_979639'
    assert tree.invariants(), "Invariants failed at step 4271"

    # Operation 4272: insert
    tree[1742] = 'value_445434'
    reference[1742] = 'value_445434'
    assert tree.invariants(), "Invariants failed at step 4272"

    # Operation 4273: update
    tree[1742] = 'value_209345'
    reference[1742] = 'value_209345'
    assert tree.invariants(), "Invariants failed at step 4273"

    # Operation 4274: insert
    tree[7839] = 'value_233629'
    reference[7839] = 'value_233629'
    assert tree.invariants(), "Invariants failed at step 4274"

    # Operation 4275: insert
    tree[6628] = 'value_391406'
    reference[6628] = 'value_391406'
    assert tree.invariants(), "Invariants failed at step 4275"

    # Operation 4276: delete
    del tree[4534]
    del reference[4534]
    assert tree.invariants(), "Invariants failed at step 4276"

    # Operation 4277: get
    assert tree.invariants(), "Invariants failed at step 4277"

    # Operation 4278: insert
    tree[1327] = 'value_802123'
    reference[1327] = 'value_802123'
    assert tree.invariants(), "Invariants failed at step 4278"

    # Operation 4279: delete
    del tree[6040]
    del reference[6040]
    assert tree.invariants(), "Invariants failed at step 4279"

    # Operation 4280: batch_delete
    keys_to_delete = [8049, 3931, 6628, 7212]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4280"

    # Operation 4281: delete
    del tree[7839]
    del reference[7839]
    assert tree.invariants(), "Invariants failed at step 4281"

    # Operation 4282: insert
    tree[4624] = 'value_478087'
    reference[4624] = 'value_478087'
    assert tree.invariants(), "Invariants failed at step 4282"

    # Operation 4283: delete
    del tree[1742]
    del reference[1742]
    assert tree.invariants(), "Invariants failed at step 4283"

    # Operation 4284: insert
    tree[3779] = 'value_937938'
    reference[3779] = 'value_937938'
    assert tree.invariants(), "Invariants failed at step 4284"

    # Operation 4285: get
    assert tree.invariants(), "Invariants failed at step 4285"

    # Operation 4286: delete
    del tree[3779]
    del reference[3779]
    assert tree.invariants(), "Invariants failed at step 4286"

    # Operation 4287: delete
    del tree[4624]
    del reference[4624]
    assert tree.invariants(), "Invariants failed at step 4287"

    # Operation 4288: insert
    tree[2581] = 'value_554080'
    reference[2581] = 'value_554080'
    assert tree.invariants(), "Invariants failed at step 4288"

    # Operation 4289: delete
    del tree[1327]
    del reference[1327]
    assert tree.invariants(), "Invariants failed at step 4289"

    # Operation 4290: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4290"

    # Operation 4291: delete
    del tree[2581]
    del reference[2581]
    assert tree.invariants(), "Invariants failed at step 4291"

    # Operation 4292: get
    assert tree.invariants(), "Invariants failed at step 4292"

    # Operation 4293: get
    assert tree.invariants(), "Invariants failed at step 4293"

    # Operation 4294: insert
    tree[8164] = 'value_126698'
    reference[8164] = 'value_126698'
    assert tree.invariants(), "Invariants failed at step 4294"

    # Operation 4295: insert
    tree[5927] = 'value_361255'
    reference[5927] = 'value_361255'
    assert tree.invariants(), "Invariants failed at step 4295"

    # Operation 4296: insert
    tree[1230] = 'value_243496'
    reference[1230] = 'value_243496'
    assert tree.invariants(), "Invariants failed at step 4296"

    # Operation 4297: get
    assert tree.invariants(), "Invariants failed at step 4297"

    # Operation 4298: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4298"

    # Operation 4299: get
    assert tree.invariants(), "Invariants failed at step 4299"

    # Operation 4300: get
    assert tree.invariants(), "Invariants failed at step 4300"

    # Operation 4301: insert
    tree[5206] = 'value_365677'
    reference[5206] = 'value_365677'
    assert tree.invariants(), "Invariants failed at step 4301"

    # Operation 4302: get
    assert tree.invariants(), "Invariants failed at step 4302"

    # Operation 4303: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4303"

    # Operation 4304: insert
    tree[33] = 'value_391406'
    reference[33] = 'value_391406'
    assert tree.invariants(), "Invariants failed at step 4304"

    # Operation 4305: delete
    del tree[5206]
    del reference[5206]
    assert tree.invariants(), "Invariants failed at step 4305"

    # Operation 4306: delete
    del tree[8164]
    del reference[8164]
    assert tree.invariants(), "Invariants failed at step 4306"

    # Operation 4307: get
    assert tree.invariants(), "Invariants failed at step 4307"

    # Operation 4308: insert
    tree[2614] = 'value_643305'
    reference[2614] = 'value_643305'
    assert tree.invariants(), "Invariants failed at step 4308"

    # Operation 4309: insert
    tree[899] = 'value_784953'
    reference[899] = 'value_784953'
    assert tree.invariants(), "Invariants failed at step 4309"

    # Operation 4310: insert
    tree[2014] = 'value_751167'
    reference[2014] = 'value_751167'
    assert tree.invariants(), "Invariants failed at step 4310"

    # Operation 4311: insert
    tree[1456] = 'value_236822'
    reference[1456] = 'value_236822'
    assert tree.invariants(), "Invariants failed at step 4311"

    # Operation 4312: delete
    del tree[1230]
    del reference[1230]
    assert tree.invariants(), "Invariants failed at step 4312"

    # Operation 4313: get
    assert tree.invariants(), "Invariants failed at step 4313"

    # Operation 4314: insert
    tree[9928] = 'value_910305'
    reference[9928] = 'value_910305'
    assert tree.invariants(), "Invariants failed at step 4314"

    # Operation 4315: delete
    del tree[1456]
    del reference[1456]
    assert tree.invariants(), "Invariants failed at step 4315"

    # Operation 4316: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4316"

    # Operation 4317: delete
    del tree[33]
    del reference[33]
    assert tree.invariants(), "Invariants failed at step 4317"

    # Operation 4318: batch_delete
    keys_to_delete = [9928, 9891, 9422, 2014]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4318"

    # Operation 4319: get
    assert tree.invariants(), "Invariants failed at step 4319"

    # Operation 4320: get
    assert tree.invariants(), "Invariants failed at step 4320"

    # Operation 4321: get
    assert tree.invariants(), "Invariants failed at step 4321"

    # Operation 4322: insert
    tree[9612] = 'value_879508'
    reference[9612] = 'value_879508'
    assert tree.invariants(), "Invariants failed at step 4322"

    # Operation 4323: update
    tree[9612] = 'value_104629'
    reference[9612] = 'value_104629'
    assert tree.invariants(), "Invariants failed at step 4323"

    # Operation 4324: update
    tree[5927] = 'value_361118'
    reference[5927] = 'value_361118'
    assert tree.invariants(), "Invariants failed at step 4324"

    # Operation 4325: delete
    del tree[899]
    del reference[899]
    assert tree.invariants(), "Invariants failed at step 4325"

    # Operation 4326: update
    tree[5927] = 'value_355284'
    reference[5927] = 'value_355284'
    assert tree.invariants(), "Invariants failed at step 4326"

    # Operation 4327: get
    assert tree.invariants(), "Invariants failed at step 4327"

    # Operation 4328: get
    assert tree.invariants(), "Invariants failed at step 4328"

    # Operation 4329: insert
    tree[6937] = 'value_744003'
    reference[6937] = 'value_744003'
    assert tree.invariants(), "Invariants failed at step 4329"

    # Operation 4330: update
    tree[5927] = 'value_50321'
    reference[5927] = 'value_50321'
    assert tree.invariants(), "Invariants failed at step 4330"

    # Operation 4331: get
    assert tree.invariants(), "Invariants failed at step 4331"

    # Operation 4332: insert
    tree[4930] = 'value_947650'
    reference[4930] = 'value_947650'
    assert tree.invariants(), "Invariants failed at step 4332"

    # Operation 4333: get
    assert tree.invariants(), "Invariants failed at step 4333"

    # Operation 4334: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4334"

    # Operation 4335: delete
    del tree[9612]
    del reference[9612]
    assert tree.invariants(), "Invariants failed at step 4335"

    # Operation 4336: get
    assert tree.invariants(), "Invariants failed at step 4336"

    # Operation 4337: insert
    tree[5715] = 'value_904276'
    reference[5715] = 'value_904276'
    assert tree.invariants(), "Invariants failed at step 4337"

    # Operation 4338: insert
    tree[8907] = 'value_768924'
    reference[8907] = 'value_768924'
    assert tree.invariants(), "Invariants failed at step 4338"

    # Operation 4339: update
    tree[5715] = 'value_439054'
    reference[5715] = 'value_439054'
    assert tree.invariants(), "Invariants failed at step 4339"

    # Operation 4340: get
    assert tree.invariants(), "Invariants failed at step 4340"

    # Operation 4341: update
    tree[8907] = 'value_957090'
    reference[8907] = 'value_957090'
    assert tree.invariants(), "Invariants failed at step 4341"

    # Operation 4342: get
    assert tree.invariants(), "Invariants failed at step 4342"

    # Operation 4343: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4343"

    # Operation 4344: get
    assert tree.invariants(), "Invariants failed at step 4344"

    # Operation 4345: delete
    del tree[4930]
    del reference[4930]
    assert tree.invariants(), "Invariants failed at step 4345"

    # Operation 4346: get
    assert tree.invariants(), "Invariants failed at step 4346"

    # Operation 4347: get
    assert tree.invariants(), "Invariants failed at step 4347"

    # Operation 4348: get
    assert tree.invariants(), "Invariants failed at step 4348"

    # Operation 4349: insert
    tree[9854] = 'value_10642'
    reference[9854] = 'value_10642'
    assert tree.invariants(), "Invariants failed at step 4349"

    # Operation 4350: get
    assert tree.invariants(), "Invariants failed at step 4350"

    # Operation 4351: get
    assert tree.invariants(), "Invariants failed at step 4351"

    # Operation 4352: delete
    del tree[5927]
    del reference[5927]
    assert tree.invariants(), "Invariants failed at step 4352"

    # Operation 4353: insert
    tree[2421] = 'value_876062'
    reference[2421] = 'value_876062'
    assert tree.invariants(), "Invariants failed at step 4353"

    # Operation 4354: delete
    del tree[2614]
    del reference[2614]
    assert tree.invariants(), "Invariants failed at step 4354"

    # Operation 4355: insert
    tree[1003] = 'value_505046'
    reference[1003] = 'value_505046'
    assert tree.invariants(), "Invariants failed at step 4355"

    # Operation 4356: update
    tree[5715] = 'value_771752'
    reference[5715] = 'value_771752'
    assert tree.invariants(), "Invariants failed at step 4356"

    # Operation 4357: batch_delete
    keys_to_delete = [1003, 1900, 8907, 2421, 3802]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4357"

    # Operation 4358: get
    assert tree.invariants(), "Invariants failed at step 4358"

    # Operation 4359: insert
    tree[2068] = 'value_606609'
    reference[2068] = 'value_606609'
    assert tree.invariants(), "Invariants failed at step 4359"

    # Operation 4360: insert
    tree[5160] = 'value_226860'
    reference[5160] = 'value_226860'
    assert tree.invariants(), "Invariants failed at step 4360"

    # Operation 4361: update
    tree[9854] = 'value_148655'
    reference[9854] = 'value_148655'
    assert tree.invariants(), "Invariants failed at step 4361"

    # Operation 4362: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4362"

    # Operation 4363: get
    assert tree.invariants(), "Invariants failed at step 4363"

    # Operation 4364: delete
    del tree[5715]
    del reference[5715]
    assert tree.invariants(), "Invariants failed at step 4364"

    # Operation 4365: get
    assert tree.invariants(), "Invariants failed at step 4365"

    # Operation 4366: delete
    del tree[2068]
    del reference[2068]
    assert tree.invariants(), "Invariants failed at step 4366"

    # Operation 4367: get
    assert tree.invariants(), "Invariants failed at step 4367"

    # Operation 4368: delete
    del tree[9854]
    del reference[9854]
    assert tree.invariants(), "Invariants failed at step 4368"

    # Operation 4369: insert
    tree[7763] = 'value_259689'
    reference[7763] = 'value_259689'
    assert tree.invariants(), "Invariants failed at step 4369"

    # Operation 4370: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4370"

    # Operation 4371: get
    assert tree.invariants(), "Invariants failed at step 4371"

    # Operation 4372: delete
    del tree[5160]
    del reference[5160]
    assert tree.invariants(), "Invariants failed at step 4372"

    # Operation 4373: get
    assert tree.invariants(), "Invariants failed at step 4373"

    # Operation 4374: delete
    del tree[7763]
    del reference[7763]
    assert tree.invariants(), "Invariants failed at step 4374"

    # Operation 4375: delete
    del tree[6937]
    del reference[6937]
    assert tree.invariants(), "Invariants failed at step 4375"

    # Operation 4376: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4376"

    # Operation 4377: insert
    tree[8462] = 'value_123102'
    reference[8462] = 'value_123102'
    assert tree.invariants(), "Invariants failed at step 4377"

    # Operation 4378: insert
    tree[3447] = 'value_664719'
    reference[3447] = 'value_664719'
    assert tree.invariants(), "Invariants failed at step 4378"

    # Operation 4379: delete
    del tree[3447]
    del reference[3447]
    assert tree.invariants(), "Invariants failed at step 4379"

    # Operation 4380: delete
    del tree[8462]
    del reference[8462]
    assert tree.invariants(), "Invariants failed at step 4380"

    # Operation 4381: get
    assert tree.invariants(), "Invariants failed at step 4381"

    # Operation 4382: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4382"

    # Operation 4383: insert
    tree[3984] = 'value_193192'
    reference[3984] = 'value_193192'
    assert tree.invariants(), "Invariants failed at step 4383"

    # Operation 4384: get
    assert tree.invariants(), "Invariants failed at step 4384"

    # Operation 4385: insert
    tree[4609] = 'value_936851'
    reference[4609] = 'value_936851'
    assert tree.invariants(), "Invariants failed at step 4385"

    # Operation 4386: update
    tree[4609] = 'value_759674'
    reference[4609] = 'value_759674'
    assert tree.invariants(), "Invariants failed at step 4386"

    # Operation 4387: insert
    tree[236] = 'value_261671'
    reference[236] = 'value_261671'
    assert tree.invariants(), "Invariants failed at step 4387"

    # Operation 4388: insert
    tree[7231] = 'value_596441'
    reference[7231] = 'value_596441'
    assert tree.invariants(), "Invariants failed at step 4388"

    # Operation 4389: insert
    tree[8618] = 'value_2890'
    reference[8618] = 'value_2890'
    assert tree.invariants(), "Invariants failed at step 4389"

    # Operation 4390: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4390"

    # Operation 4391: delete
    del tree[8618]
    del reference[8618]
    assert tree.invariants(), "Invariants failed at step 4391"

    # Operation 4392: get
    assert tree.invariants(), "Invariants failed at step 4392"

    # Operation 4393: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4393"

    # Operation 4394: insert
    tree[604] = 'value_163290'
    reference[604] = 'value_163290'
    assert tree.invariants(), "Invariants failed at step 4394"

    # Operation 4395: update
    tree[4609] = 'value_751690'
    reference[4609] = 'value_751690'
    assert tree.invariants(), "Invariants failed at step 4395"

    # Operation 4396: delete
    del tree[3984]
    del reference[3984]
    assert tree.invariants(), "Invariants failed at step 4396"

    # Operation 4397: update
    tree[4609] = 'value_919294'
    reference[4609] = 'value_919294'
    assert tree.invariants(), "Invariants failed at step 4397"

    # Operation 4398: insert
    tree[4136] = 'value_955590'
    reference[4136] = 'value_955590'
    assert tree.invariants(), "Invariants failed at step 4398"

    # Operation 4399: delete
    del tree[4136]
    del reference[4136]
    assert tree.invariants(), "Invariants failed at step 4399"

    # Operation 4400: insert
    tree[4589] = 'value_590301'
    reference[4589] = 'value_590301'
    assert tree.invariants(), "Invariants failed at step 4400"

    # Operation 4401: update
    tree[4589] = 'value_422317'
    reference[4589] = 'value_422317'
    assert tree.invariants(), "Invariants failed at step 4401"

    # Operation 4402: get
    assert tree.invariants(), "Invariants failed at step 4402"

    # Operation 4403: insert
    tree[7583] = 'value_271022'
    reference[7583] = 'value_271022'
    assert tree.invariants(), "Invariants failed at step 4403"

    # Operation 4404: get
    assert tree.invariants(), "Invariants failed at step 4404"

    # Operation 4405: delete
    del tree[4589]
    del reference[4589]
    assert tree.invariants(), "Invariants failed at step 4405"

    # Operation 4406: get
    assert tree.invariants(), "Invariants failed at step 4406"

    # Operation 4407: insert
    tree[9139] = 'value_103895'
    reference[9139] = 'value_103895'
    assert tree.invariants(), "Invariants failed at step 4407"

    # Operation 4408: batch_delete
    keys_to_delete = [4609, 236, 9139, 5557, 6965]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4408"

    # Operation 4409: get
    assert tree.invariants(), "Invariants failed at step 4409"

    # Operation 4410: insert
    tree[6260] = 'value_19795'
    reference[6260] = 'value_19795'
    assert tree.invariants(), "Invariants failed at step 4410"

    # Operation 4411: update
    tree[604] = 'value_679288'
    reference[604] = 'value_679288'
    assert tree.invariants(), "Invariants failed at step 4411"

    # Operation 4412: get
    assert tree.invariants(), "Invariants failed at step 4412"

    # Operation 4413: insert
    tree[4759] = 'value_598326'
    reference[4759] = 'value_598326'
    assert tree.invariants(), "Invariants failed at step 4413"

    # Operation 4414: update
    tree[7583] = 'value_150876'
    reference[7583] = 'value_150876'
    assert tree.invariants(), "Invariants failed at step 4414"

    # Operation 4415: get
    assert tree.invariants(), "Invariants failed at step 4415"

    # Operation 4416: update
    tree[7583] = 'value_976462'
    reference[7583] = 'value_976462'
    assert tree.invariants(), "Invariants failed at step 4416"

    # Operation 4417: get
    assert tree.invariants(), "Invariants failed at step 4417"

    # Operation 4418: insert
    tree[9147] = 'value_402705'
    reference[9147] = 'value_402705'
    assert tree.invariants(), "Invariants failed at step 4418"

    # Operation 4419: delete
    del tree[7583]
    del reference[7583]
    assert tree.invariants(), "Invariants failed at step 4419"

    # Operation 4420: get
    assert tree.invariants(), "Invariants failed at step 4420"

    # Operation 4421: batch_delete
    keys_to_delete = [9147, 7231, 6670, 4759]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4421"

    # Operation 4422: get
    assert tree.invariants(), "Invariants failed at step 4422"

    # Operation 4423: delete
    del tree[604]
    del reference[604]
    assert tree.invariants(), "Invariants failed at step 4423"

    # Operation 4424: get
    assert tree.invariants(), "Invariants failed at step 4424"

    # Operation 4425: update
    tree[6260] = 'value_627242'
    reference[6260] = 'value_627242'
    assert tree.invariants(), "Invariants failed at step 4425"

    # Operation 4426: get
    assert tree.invariants(), "Invariants failed at step 4426"

    # Operation 4427: get
    assert tree.invariants(), "Invariants failed at step 4427"

    # Operation 4428: delete
    del tree[6260]
    del reference[6260]
    assert tree.invariants(), "Invariants failed at step 4428"

    # Operation 4429: get
    assert tree.invariants(), "Invariants failed at step 4429"

    # Operation 4430: get
    assert tree.invariants(), "Invariants failed at step 4430"

    # Operation 4431: insert
    tree[3047] = 'value_702321'
    reference[3047] = 'value_702321'
    assert tree.invariants(), "Invariants failed at step 4431"

    # Operation 4432: insert
    tree[4640] = 'value_633572'
    reference[4640] = 'value_633572'
    assert tree.invariants(), "Invariants failed at step 4432"

    # Operation 4433: update
    tree[4640] = 'value_130633'
    reference[4640] = 'value_130633'
    assert tree.invariants(), "Invariants failed at step 4433"

    # Operation 4434: delete
    del tree[3047]
    del reference[3047]
    assert tree.invariants(), "Invariants failed at step 4434"

    # Operation 4435: delete
    del tree[4640]
    del reference[4640]
    assert tree.invariants(), "Invariants failed at step 4435"

    # Operation 4436: insert
    tree[9363] = 'value_149332'
    reference[9363] = 'value_149332'
    assert tree.invariants(), "Invariants failed at step 4436"

    # Operation 4437: delete
    del tree[9363]
    del reference[9363]
    assert tree.invariants(), "Invariants failed at step 4437"

    # Operation 4438: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4438"

    # Operation 4439: insert
    tree[7662] = 'value_63657'
    reference[7662] = 'value_63657'
    assert tree.invariants(), "Invariants failed at step 4439"

    # Operation 4440: delete
    del tree[7662]
    del reference[7662]
    assert tree.invariants(), "Invariants failed at step 4440"

    # Operation 4441: insert
    tree[4865] = 'value_148521'
    reference[4865] = 'value_148521'
    assert tree.invariants(), "Invariants failed at step 4441"

    # Operation 4442: get
    assert tree.invariants(), "Invariants failed at step 4442"

    # Operation 4443: update
    tree[4865] = 'value_758258'
    reference[4865] = 'value_758258'
    assert tree.invariants(), "Invariants failed at step 4443"

    # Operation 4444: insert
    tree[8357] = 'value_91060'
    reference[8357] = 'value_91060'
    assert tree.invariants(), "Invariants failed at step 4444"

    # Operation 4445: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4445"

    # Operation 4446: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4446"

    # Operation 4447: insert
    tree[5709] = 'value_116055'
    reference[5709] = 'value_116055'
    assert tree.invariants(), "Invariants failed at step 4447"

    # Operation 4448: delete
    del tree[8357]
    del reference[8357]
    assert tree.invariants(), "Invariants failed at step 4448"

    # Operation 4449: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4449"

    # Operation 4450: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4450"

    # Operation 4451: get
    assert tree.invariants(), "Invariants failed at step 4451"

    # Operation 4452: insert
    tree[2249] = 'value_262879'
    reference[2249] = 'value_262879'
    assert tree.invariants(), "Invariants failed at step 4452"

    # Operation 4453: get
    assert tree.invariants(), "Invariants failed at step 4453"

    # Operation 4454: insert
    tree[8659] = 'value_674242'
    reference[8659] = 'value_674242'
    assert tree.invariants(), "Invariants failed at step 4454"

    # Operation 4455: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4455"

    # Operation 4456: update
    tree[8659] = 'value_173547'
    reference[8659] = 'value_173547'
    assert tree.invariants(), "Invariants failed at step 4456"

    # Operation 4457: insert
    tree[7588] = 'value_222681'
    reference[7588] = 'value_222681'
    assert tree.invariants(), "Invariants failed at step 4457"

    # Operation 4458: insert
    tree[2909] = 'value_63681'
    reference[2909] = 'value_63681'
    assert tree.invariants(), "Invariants failed at step 4458"

    # Operation 4459: insert
    tree[4548] = 'value_89390'
    reference[4548] = 'value_89390'
    assert tree.invariants(), "Invariants failed at step 4459"

    # Operation 4460: insert
    tree[5303] = 'value_876749'
    reference[5303] = 'value_876749'
    assert tree.invariants(), "Invariants failed at step 4460"

    # Operation 4461: batch_delete
    keys_to_delete = [4865, 7588, 2249, 6062, 8659, 5303]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4461"

    # Operation 4462: insert
    tree[5352] = 'value_472740'
    reference[5352] = 'value_472740'
    assert tree.invariants(), "Invariants failed at step 4462"

    # Operation 4463: insert
    tree[1749] = 'value_499119'
    reference[1749] = 'value_499119'
    assert tree.invariants(), "Invariants failed at step 4463"

    # Operation 4464: delete
    del tree[2909]
    del reference[2909]
    assert tree.invariants(), "Invariants failed at step 4464"

    # Operation 4465: update
    tree[4548] = 'value_989993'
    reference[4548] = 'value_989993'
    assert tree.invariants(), "Invariants failed at step 4465"

    # Operation 4466: update
    tree[4548] = 'value_766487'
    reference[4548] = 'value_766487'
    assert tree.invariants(), "Invariants failed at step 4466"

    # Operation 4467: insert
    tree[7225] = 'value_563690'
    reference[7225] = 'value_563690'
    assert tree.invariants(), "Invariants failed at step 4467"

    # Operation 4468: delete
    del tree[5709]
    del reference[5709]
    assert tree.invariants(), "Invariants failed at step 4468"

    # Operation 4469: delete
    del tree[5352]
    del reference[5352]
    assert tree.invariants(), "Invariants failed at step 4469"

    # Operation 4470: delete
    del tree[4548]
    del reference[4548]
    assert tree.invariants(), "Invariants failed at step 4470"

    # Operation 4471: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4471"

    # Operation 4472: update
    tree[7225] = 'value_917034'
    reference[7225] = 'value_917034'
    assert tree.invariants(), "Invariants failed at step 4472"

    # Operation 4473: update
    tree[1749] = 'value_604913'
    reference[1749] = 'value_604913'
    assert tree.invariants(), "Invariants failed at step 4473"

    # Operation 4474: get
    assert tree.invariants(), "Invariants failed at step 4474"

    # Operation 4475: get
    assert tree.invariants(), "Invariants failed at step 4475"

    # Operation 4476: insert
    tree[4819] = 'value_490433'
    reference[4819] = 'value_490433'
    assert tree.invariants(), "Invariants failed at step 4476"

    # Operation 4477: delete
    del tree[4819]
    del reference[4819]
    assert tree.invariants(), "Invariants failed at step 4477"

    # Operation 4478: get
    assert tree.invariants(), "Invariants failed at step 4478"

    # Operation 4479: insert
    tree[1053] = 'value_217721'
    reference[1053] = 'value_217721'
    assert tree.invariants(), "Invariants failed at step 4479"

    # Operation 4480: delete
    del tree[1053]
    del reference[1053]
    assert tree.invariants(), "Invariants failed at step 4480"

    # Operation 4481: delete
    del tree[7225]
    del reference[7225]
    assert tree.invariants(), "Invariants failed at step 4481"

    # Operation 4482: get
    assert tree.invariants(), "Invariants failed at step 4482"

    # Operation 4483: insert
    tree[8966] = 'value_719773'
    reference[8966] = 'value_719773'
    assert tree.invariants(), "Invariants failed at step 4483"

    # Operation 4484: insert
    tree[6163] = 'value_773485'
    reference[6163] = 'value_773485'
    assert tree.invariants(), "Invariants failed at step 4484"

    # Operation 4485: update
    tree[8966] = 'value_399699'
    reference[8966] = 'value_399699'
    assert tree.invariants(), "Invariants failed at step 4485"

    # Operation 4486: delete
    del tree[8966]
    del reference[8966]
    assert tree.invariants(), "Invariants failed at step 4486"

    # Operation 4487: insert
    tree[2808] = 'value_868137'
    reference[2808] = 'value_868137'
    assert tree.invariants(), "Invariants failed at step 4487"

    # Operation 4488: delete
    del tree[6163]
    del reference[6163]
    assert tree.invariants(), "Invariants failed at step 4488"

    # Operation 4489: get
    assert tree.invariants(), "Invariants failed at step 4489"

    # Operation 4490: delete
    del tree[1749]
    del reference[1749]
    assert tree.invariants(), "Invariants failed at step 4490"

    # Operation 4491: get
    assert tree.invariants(), "Invariants failed at step 4491"

    # Operation 4492: insert
    tree[6702] = 'value_160184'
    reference[6702] = 'value_160184'
    assert tree.invariants(), "Invariants failed at step 4492"

    # Operation 4493: get
    assert tree.invariants(), "Invariants failed at step 4493"

    # Operation 4494: insert
    tree[3734] = 'value_494238'
    reference[3734] = 'value_494238'
    assert tree.invariants(), "Invariants failed at step 4494"

    # Operation 4495: insert
    tree[7254] = 'value_248391'
    reference[7254] = 'value_248391'
    assert tree.invariants(), "Invariants failed at step 4495"

    # Operation 4496: delete
    del tree[6702]
    del reference[6702]
    assert tree.invariants(), "Invariants failed at step 4496"

    # Operation 4497: delete
    del tree[3734]
    del reference[3734]
    assert tree.invariants(), "Invariants failed at step 4497"

    # Operation 4498: delete
    del tree[7254]
    del reference[7254]
    assert tree.invariants(), "Invariants failed at step 4498"

    # Operation 4499: get
    assert tree.invariants(), "Invariants failed at step 4499"

    # Operation 4500: update
    tree[2808] = 'value_153182'
    reference[2808] = 'value_153182'
    assert tree.invariants(), "Invariants failed at step 4500"

    # Operation 4501: delete
    del tree[2808]
    del reference[2808]
    assert tree.invariants(), "Invariants failed at step 4501"

    # Operation 4502: insert
    tree[6984] = 'value_443606'
    reference[6984] = 'value_443606'
    assert tree.invariants(), "Invariants failed at step 4502"

    # Operation 4503: delete
    del tree[6984]
    del reference[6984]
    assert tree.invariants(), "Invariants failed at step 4503"

    # Operation 4504: insert
    tree[7258] = 'value_650944'
    reference[7258] = 'value_650944'
    assert tree.invariants(), "Invariants failed at step 4504"

    # Operation 4505: insert
    tree[3419] = 'value_755216'
    reference[3419] = 'value_755216'
    assert tree.invariants(), "Invariants failed at step 4505"

    # Operation 4506: insert
    tree[5608] = 'value_171938'
    reference[5608] = 'value_171938'
    assert tree.invariants(), "Invariants failed at step 4506"

    # Operation 4507: insert
    tree[1929] = 'value_287045'
    reference[1929] = 'value_287045'
    assert tree.invariants(), "Invariants failed at step 4507"

    # Operation 4508: insert
    tree[9988] = 'value_253471'
    reference[9988] = 'value_253471'
    assert tree.invariants(), "Invariants failed at step 4508"

    # Operation 4509: update
    tree[3419] = 'value_274456'
    reference[3419] = 'value_274456'
    assert tree.invariants(), "Invariants failed at step 4509"

    # Operation 4510: insert
    tree[4799] = 'value_635503'
    reference[4799] = 'value_635503'
    assert tree.invariants(), "Invariants failed at step 4510"

    # Operation 4511: insert
    tree[4195] = 'value_383848'
    reference[4195] = 'value_383848'
    assert tree.invariants(), "Invariants failed at step 4511"

    # Operation 4512: get
    assert tree.invariants(), "Invariants failed at step 4512"

    # Operation 4513: get
    assert tree.invariants(), "Invariants failed at step 4513"

    # Operation 4514: delete
    del tree[4195]
    del reference[4195]
    assert tree.invariants(), "Invariants failed at step 4514"

    # Operation 4515: delete
    del tree[1929]
    del reference[1929]
    assert tree.invariants(), "Invariants failed at step 4515"

    # Operation 4516: insert
    tree[5211] = 'value_822102'
    reference[5211] = 'value_822102'
    assert tree.invariants(), "Invariants failed at step 4516"

    # Operation 4517: insert
    tree[8616] = 'value_185895'
    reference[8616] = 'value_185895'
    assert tree.invariants(), "Invariants failed at step 4517"

    # Operation 4518: insert
    tree[9480] = 'value_631506'
    reference[9480] = 'value_631506'
    assert tree.invariants(), "Invariants failed at step 4518"

    # Operation 4519: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4519"

    # Operation 4520: get
    assert tree.invariants(), "Invariants failed at step 4520"

    # Operation 4521: get
    assert tree.invariants(), "Invariants failed at step 4521"

    # Operation 4522: insert
    tree[6827] = 'value_721886'
    reference[6827] = 'value_721886'
    assert tree.invariants(), "Invariants failed at step 4522"

    # Operation 4523: insert
    tree[4174] = 'value_598629'
    reference[4174] = 'value_598629'
    assert tree.invariants(), "Invariants failed at step 4523"

    # Operation 4524: insert
    tree[7736] = 'value_133414'
    reference[7736] = 'value_133414'
    assert tree.invariants(), "Invariants failed at step 4524"

    # Operation 4525: delete
    del tree[5608]
    del reference[5608]
    assert tree.invariants(), "Invariants failed at step 4525"

    # Operation 4526: batch_delete
    keys_to_delete = [2849, 9988, 9480, 938, 7258, 3419, 4799]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4526"

    # Operation 4527: delete
    del tree[6827]
    del reference[6827]
    assert tree.invariants(), "Invariants failed at step 4527"

    # Operation 4528: insert
    tree[3593] = 'value_46421'
    reference[3593] = 'value_46421'
    assert tree.invariants(), "Invariants failed at step 4528"

    # Operation 4529: get
    assert tree.invariants(), "Invariants failed at step 4529"

    # Operation 4530: get
    assert tree.invariants(), "Invariants failed at step 4530"

    # Operation 4531: insert
    tree[2428] = 'value_795068'
    reference[2428] = 'value_795068'
    assert tree.invariants(), "Invariants failed at step 4531"

    # Operation 4532: insert
    tree[3480] = 'value_950149'
    reference[3480] = 'value_950149'
    assert tree.invariants(), "Invariants failed at step 4532"

    # Operation 4533: insert
    tree[7871] = 'value_630313'
    reference[7871] = 'value_630313'
    assert tree.invariants(), "Invariants failed at step 4533"

    # Operation 4534: get
    assert tree.invariants(), "Invariants failed at step 4534"

    # Operation 4535: delete
    del tree[3480]
    del reference[3480]
    assert tree.invariants(), "Invariants failed at step 4535"

    # Operation 4536: batch_delete
    keys_to_delete = [7736, 3593, 4859, 7871]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4536"

    # Operation 4537: insert
    tree[464] = 'value_481899'
    reference[464] = 'value_481899'
    assert tree.invariants(), "Invariants failed at step 4537"

    # Operation 4538: batch_delete
    keys_to_delete = [307, 5211, 2428]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4538"

    # Operation 4539: get
    assert tree.invariants(), "Invariants failed at step 4539"

    # Operation 4540: get
    assert tree.invariants(), "Invariants failed at step 4540"

    # Operation 4541: insert
    tree[9632] = 'value_510821'
    reference[9632] = 'value_510821'
    assert tree.invariants(), "Invariants failed at step 4541"

    # Operation 4542: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4542"

    # Operation 4543: update
    tree[4174] = 'value_533757'
    reference[4174] = 'value_533757'
    assert tree.invariants(), "Invariants failed at step 4543"

    # Operation 4544: delete
    del tree[464]
    del reference[464]
    assert tree.invariants(), "Invariants failed at step 4544"

    # Operation 4545: update
    tree[4174] = 'value_815940'
    reference[4174] = 'value_815940'
    assert tree.invariants(), "Invariants failed at step 4545"

    # Operation 4546: get
    assert tree.invariants(), "Invariants failed at step 4546"

    # Operation 4547: insert
    tree[8956] = 'value_401079'
    reference[8956] = 'value_401079'
    assert tree.invariants(), "Invariants failed at step 4547"

    # Operation 4548: delete
    del tree[8956]
    del reference[8956]
    assert tree.invariants(), "Invariants failed at step 4548"

    # Operation 4549: insert
    tree[5455] = 'value_421120'
    reference[5455] = 'value_421120'
    assert tree.invariants(), "Invariants failed at step 4549"

    # Operation 4550: delete
    del tree[8616]
    del reference[8616]
    assert tree.invariants(), "Invariants failed at step 4550"

    # Operation 4551: get
    assert tree.invariants(), "Invariants failed at step 4551"

    # Operation 4552: insert
    tree[319] = 'value_586619'
    reference[319] = 'value_586619'
    assert tree.invariants(), "Invariants failed at step 4552"

    # Operation 4553: delete
    del tree[319]
    del reference[319]
    assert tree.invariants(), "Invariants failed at step 4553"

    # Operation 4554: delete
    del tree[5455]
    del reference[5455]
    assert tree.invariants(), "Invariants failed at step 4554"

    # Operation 4555: delete
    del tree[9632]
    del reference[9632]
    assert tree.invariants(), "Invariants failed at step 4555"

    # Operation 4556: delete
    del tree[4174]
    del reference[4174]
    assert tree.invariants(), "Invariants failed at step 4556"

    # Operation 4557: get
    assert tree.invariants(), "Invariants failed at step 4557"

    # Operation 4558: get
    assert tree.invariants(), "Invariants failed at step 4558"

    # Operation 4559: get
    assert tree.invariants(), "Invariants failed at step 4559"

    # Operation 4560: insert
    tree[4920] = 'value_861152'
    reference[4920] = 'value_861152'
    assert tree.invariants(), "Invariants failed at step 4560"

    # Operation 4561: insert
    tree[453] = 'value_692851'
    reference[453] = 'value_692851'
    assert tree.invariants(), "Invariants failed at step 4561"

    # Operation 4562: insert
    tree[4892] = 'value_926364'
    reference[4892] = 'value_926364'
    assert tree.invariants(), "Invariants failed at step 4562"

    # Operation 4563: insert
    tree[1415] = 'value_794442'
    reference[1415] = 'value_794442'
    assert tree.invariants(), "Invariants failed at step 4563"

    # Operation 4564: get
    assert tree.invariants(), "Invariants failed at step 4564"

    # Operation 4565: insert
    tree[8069] = 'value_214724'
    reference[8069] = 'value_214724'
    assert tree.invariants(), "Invariants failed at step 4565"

    # Operation 4566: insert
    tree[4370] = 'value_112488'
    reference[4370] = 'value_112488'
    assert tree.invariants(), "Invariants failed at step 4566"

    # Operation 4567: update
    tree[1415] = 'value_211322'
    reference[1415] = 'value_211322'
    assert tree.invariants(), "Invariants failed at step 4567"

    # Operation 4568: get
    assert tree.invariants(), "Invariants failed at step 4568"

    # Operation 4569: delete
    del tree[4920]
    del reference[4920]
    assert tree.invariants(), "Invariants failed at step 4569"

    # Operation 4570: batch_delete
    keys_to_delete = [3421, 3732, 453, 1415]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4570"

    # Operation 4571: get
    assert tree.invariants(), "Invariants failed at step 4571"

    # Operation 4572: insert
    tree[4312] = 'value_135521'
    reference[4312] = 'value_135521'
    assert tree.invariants(), "Invariants failed at step 4572"

    # Operation 4573: get
    assert tree.invariants(), "Invariants failed at step 4573"

    # Operation 4574: get
    assert tree.invariants(), "Invariants failed at step 4574"

    # Operation 4575: delete
    del tree[4312]
    del reference[4312]
    assert tree.invariants(), "Invariants failed at step 4575"

    # Operation 4576: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4576"

    # Operation 4577: insert
    tree[7575] = 'value_925137'
    reference[7575] = 'value_925137'
    assert tree.invariants(), "Invariants failed at step 4577"

    # Operation 4578: delete
    del tree[8069]
    del reference[8069]
    assert tree.invariants(), "Invariants failed at step 4578"

    # Operation 4579: insert
    tree[6617] = 'value_528266'
    reference[6617] = 'value_528266'
    assert tree.invariants(), "Invariants failed at step 4579"

    # Operation 4580: delete
    del tree[6617]
    del reference[6617]
    assert tree.invariants(), "Invariants failed at step 4580"

    # Operation 4581: get
    assert tree.invariants(), "Invariants failed at step 4581"

    # Operation 4582: insert
    tree[2306] = 'value_492565'
    reference[2306] = 'value_492565'
    assert tree.invariants(), "Invariants failed at step 4582"

    # Operation 4583: delete
    del tree[2306]
    del reference[2306]
    assert tree.invariants(), "Invariants failed at step 4583"

    # Operation 4584: insert
    tree[9553] = 'value_816098'
    reference[9553] = 'value_816098'
    assert tree.invariants(), "Invariants failed at step 4584"

    # Operation 4585: delete
    del tree[7575]
    del reference[7575]
    assert tree.invariants(), "Invariants failed at step 4585"

    # Operation 4586: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4586"

    # Operation 4587: delete
    del tree[4370]
    del reference[4370]
    assert tree.invariants(), "Invariants failed at step 4587"

    # Operation 4588: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4588"

    # Operation 4589: get
    assert tree.invariants(), "Invariants failed at step 4589"

    # Operation 4590: delete
    del tree[4892]
    del reference[4892]
    assert tree.invariants(), "Invariants failed at step 4590"

    # Operation 4591: delete
    del tree[9553]
    del reference[9553]
    assert tree.invariants(), "Invariants failed at step 4591"

    # Operation 4592: insert
    tree[7808] = 'value_168424'
    reference[7808] = 'value_168424'
    assert tree.invariants(), "Invariants failed at step 4592"

    # Operation 4593: delete
    del tree[7808]
    del reference[7808]
    assert tree.invariants(), "Invariants failed at step 4593"

    # Operation 4594: insert
    tree[6156] = 'value_292332'
    reference[6156] = 'value_292332'
    assert tree.invariants(), "Invariants failed at step 4594"

    # Operation 4595: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4595"

    # Operation 4596: delete
    del tree[6156]
    del reference[6156]
    assert tree.invariants(), "Invariants failed at step 4596"

    # Operation 4597: insert
    tree[7228] = 'value_265350'
    reference[7228] = 'value_265350'
    assert tree.invariants(), "Invariants failed at step 4597"

    # Operation 4598: delete
    del tree[7228]
    del reference[7228]
    assert tree.invariants(), "Invariants failed at step 4598"

    # Operation 4599: insert
    tree[8015] = 'value_100783'
    reference[8015] = 'value_100783'
    assert tree.invariants(), "Invariants failed at step 4599"

    # Operation 4600: delete
    del tree[8015]
    del reference[8015]
    assert tree.invariants(), "Invariants failed at step 4600"

    # Operation 4601: get
    assert tree.invariants(), "Invariants failed at step 4601"

    # Operation 4602: insert
    tree[2516] = 'value_571360'
    reference[2516] = 'value_571360'
    assert tree.invariants(), "Invariants failed at step 4602"

    # Operation 4603: insert
    tree[5198] = 'value_446968'
    reference[5198] = 'value_446968'
    assert tree.invariants(), "Invariants failed at step 4603"

    # Operation 4604: get
    assert tree.invariants(), "Invariants failed at step 4604"

    # Operation 4605: insert
    tree[2903] = 'value_108561'
    reference[2903] = 'value_108561'
    assert tree.invariants(), "Invariants failed at step 4605"

    # Operation 4606: insert
    tree[4605] = 'value_3834'
    reference[4605] = 'value_3834'
    assert tree.invariants(), "Invariants failed at step 4606"

    # Operation 4607: delete
    del tree[2516]
    del reference[2516]
    assert tree.invariants(), "Invariants failed at step 4607"

    # Operation 4608: delete
    del tree[5198]
    del reference[5198]
    assert tree.invariants(), "Invariants failed at step 4608"

    # Operation 4609: insert
    tree[6716] = 'value_540753'
    reference[6716] = 'value_540753'
    assert tree.invariants(), "Invariants failed at step 4609"

    # Operation 4610: get
    assert tree.invariants(), "Invariants failed at step 4610"

    # Operation 4611: delete
    del tree[4605]
    del reference[4605]
    assert tree.invariants(), "Invariants failed at step 4611"

    # Operation 4612: update
    tree[2903] = 'value_796218'
    reference[2903] = 'value_796218'
    assert tree.invariants(), "Invariants failed at step 4612"

    # Operation 4613: delete
    del tree[6716]
    del reference[6716]
    assert tree.invariants(), "Invariants failed at step 4613"

    # Operation 4614: get
    assert tree.invariants(), "Invariants failed at step 4614"

    # Operation 4615: get
    assert tree.invariants(), "Invariants failed at step 4615"

    # Operation 4616: get
    assert tree.invariants(), "Invariants failed at step 4616"

    # Operation 4617: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4617"

    # Operation 4618: insert
    tree[1873] = 'value_564011'
    reference[1873] = 'value_564011'
    assert tree.invariants(), "Invariants failed at step 4618"

    # Operation 4619: delete
    del tree[1873]
    del reference[1873]
    assert tree.invariants(), "Invariants failed at step 4619"

    # Operation 4620: get
    assert tree.invariants(), "Invariants failed at step 4620"

    # Operation 4621: delete
    del tree[2903]
    del reference[2903]
    assert tree.invariants(), "Invariants failed at step 4621"

    # Operation 4622: insert
    tree[7959] = 'value_115314'
    reference[7959] = 'value_115314'
    assert tree.invariants(), "Invariants failed at step 4622"

    # Operation 4623: insert
    tree[5407] = 'value_765733'
    reference[5407] = 'value_765733'
    assert tree.invariants(), "Invariants failed at step 4623"

    # Operation 4624: insert
    tree[346] = 'value_600064'
    reference[346] = 'value_600064'
    assert tree.invariants(), "Invariants failed at step 4624"

    # Operation 4625: delete
    del tree[5407]
    del reference[5407]
    assert tree.invariants(), "Invariants failed at step 4625"

    # Operation 4626: insert
    tree[618] = 'value_378673'
    reference[618] = 'value_378673'
    assert tree.invariants(), "Invariants failed at step 4626"

    # Operation 4627: delete
    del tree[618]
    del reference[618]
    assert tree.invariants(), "Invariants failed at step 4627"

    # Operation 4628: insert
    tree[8189] = 'value_496645'
    reference[8189] = 'value_496645'
    assert tree.invariants(), "Invariants failed at step 4628"

    # Operation 4629: insert
    tree[4217] = 'value_686022'
    reference[4217] = 'value_686022'
    assert tree.invariants(), "Invariants failed at step 4629"

    # Operation 4630: delete
    del tree[346]
    del reference[346]
    assert tree.invariants(), "Invariants failed at step 4630"

    # Operation 4631: delete
    del tree[7959]
    del reference[7959]
    assert tree.invariants(), "Invariants failed at step 4631"

    # Operation 4632: delete
    del tree[4217]
    del reference[4217]
    assert tree.invariants(), "Invariants failed at step 4632"

    # Operation 4633: insert
    tree[7390] = 'value_91416'
    reference[7390] = 'value_91416'
    assert tree.invariants(), "Invariants failed at step 4633"

    # Operation 4634: update
    tree[8189] = 'value_652349'
    reference[8189] = 'value_652349'
    assert tree.invariants(), "Invariants failed at step 4634"

    # Operation 4635: get
    assert tree.invariants(), "Invariants failed at step 4635"

    # Operation 4636: insert
    tree[1127] = 'value_489760'
    reference[1127] = 'value_489760'
    assert tree.invariants(), "Invariants failed at step 4636"

    # Operation 4637: update
    tree[8189] = 'value_68234'
    reference[8189] = 'value_68234'
    assert tree.invariants(), "Invariants failed at step 4637"

    # Operation 4638: insert
    tree[3822] = 'value_43551'
    reference[3822] = 'value_43551'
    assert tree.invariants(), "Invariants failed at step 4638"

    # Operation 4639: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4639"

    # Operation 4640: insert
    tree[4368] = 'value_90435'
    reference[4368] = 'value_90435'
    assert tree.invariants(), "Invariants failed at step 4640"

    # Operation 4641: delete
    del tree[3822]
    del reference[3822]
    assert tree.invariants(), "Invariants failed at step 4641"

    # Operation 4642: get
    assert tree.invariants(), "Invariants failed at step 4642"

    # Operation 4643: insert
    tree[9704] = 'value_220846'
    reference[9704] = 'value_220846'
    assert tree.invariants(), "Invariants failed at step 4643"

    # Operation 4644: insert
    tree[485] = 'value_223285'
    reference[485] = 'value_223285'
    assert tree.invariants(), "Invariants failed at step 4644"

    # Operation 4645: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4645"

    # Operation 4646: get
    assert tree.invariants(), "Invariants failed at step 4646"

    # Operation 4647: insert
    tree[6595] = 'value_890152'
    reference[6595] = 'value_890152'
    assert tree.invariants(), "Invariants failed at step 4647"

    # Operation 4648: update
    tree[485] = 'value_204134'
    reference[485] = 'value_204134'
    assert tree.invariants(), "Invariants failed at step 4648"

    # Operation 4649: update
    tree[6595] = 'value_204093'
    reference[6595] = 'value_204093'
    assert tree.invariants(), "Invariants failed at step 4649"

    # Operation 4650: get
    assert tree.invariants(), "Invariants failed at step 4650"

    # Operation 4651: delete
    del tree[7390]
    del reference[7390]
    assert tree.invariants(), "Invariants failed at step 4651"

    # Operation 4652: insert
    tree[3543] = 'value_563654'
    reference[3543] = 'value_563654'
    assert tree.invariants(), "Invariants failed at step 4652"

    # Operation 4653: update
    tree[1127] = 'value_66269'
    reference[1127] = 'value_66269'
    assert tree.invariants(), "Invariants failed at step 4653"

    # Operation 4654: insert
    tree[5966] = 'value_610234'
    reference[5966] = 'value_610234'
    assert tree.invariants(), "Invariants failed at step 4654"

    # Operation 4655: batch_delete
    keys_to_delete = [6595, 1127, 1703, 4368, 3732, 8189]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4655"

    # Operation 4656: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4656"

    # Operation 4657: get
    assert tree.invariants(), "Invariants failed at step 4657"

    # Operation 4658: delete
    del tree[9704]
    del reference[9704]
    assert tree.invariants(), "Invariants failed at step 4658"

    # Operation 4659: insert
    tree[2259] = 'value_571374'
    reference[2259] = 'value_571374'
    assert tree.invariants(), "Invariants failed at step 4659"

    # Operation 4660: insert
    tree[2199] = 'value_686895'
    reference[2199] = 'value_686895'
    assert tree.invariants(), "Invariants failed at step 4660"

    # Operation 4661: insert
    tree[9737] = 'value_684271'
    reference[9737] = 'value_684271'
    assert tree.invariants(), "Invariants failed at step 4661"

    # Operation 4662: batch_delete
    keys_to_delete = [4770, 9737, 5966, 6163, 2259]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4662"

    # Operation 4663: insert
    tree[6302] = 'value_604250'
    reference[6302] = 'value_604250'
    assert tree.invariants(), "Invariants failed at step 4663"

    # Operation 4664: delete
    del tree[485]
    del reference[485]
    assert tree.invariants(), "Invariants failed at step 4664"

    # Operation 4665: update
    tree[6302] = 'value_760322'
    reference[6302] = 'value_760322'
    assert tree.invariants(), "Invariants failed at step 4665"

    # Operation 4666: get
    assert tree.invariants(), "Invariants failed at step 4666"

    # Operation 4667: update
    tree[3543] = 'value_555490'
    reference[3543] = 'value_555490'
    assert tree.invariants(), "Invariants failed at step 4667"

    # Operation 4668: insert
    tree[8092] = 'value_31008'
    reference[8092] = 'value_31008'
    assert tree.invariants(), "Invariants failed at step 4668"

    # Operation 4669: insert
    tree[2444] = 'value_340438'
    reference[2444] = 'value_340438'
    assert tree.invariants(), "Invariants failed at step 4669"

    # Operation 4670: insert
    tree[9570] = 'value_953805'
    reference[9570] = 'value_953805'
    assert tree.invariants(), "Invariants failed at step 4670"

    # Operation 4671: insert
    tree[447] = 'value_649959'
    reference[447] = 'value_649959'
    assert tree.invariants(), "Invariants failed at step 4671"

    # Operation 4672: batch_delete
    keys_to_delete = [2444, 5486, 2199, 2395, 447]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4672"

    # Operation 4673: update
    tree[3543] = 'value_334785'
    reference[3543] = 'value_334785'
    assert tree.invariants(), "Invariants failed at step 4673"

    # Operation 4674: get
    assert tree.invariants(), "Invariants failed at step 4674"

    # Operation 4675: get
    assert tree.invariants(), "Invariants failed at step 4675"

    # Operation 4676: delete
    del tree[3543]
    del reference[3543]
    assert tree.invariants(), "Invariants failed at step 4676"

    # Operation 4677: delete
    del tree[6302]
    del reference[6302]
    assert tree.invariants(), "Invariants failed at step 4677"

    # Operation 4678: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4678"

    # Operation 4679: delete
    del tree[9570]
    del reference[9570]
    assert tree.invariants(), "Invariants failed at step 4679"

    # Operation 4680: get
    assert tree.invariants(), "Invariants failed at step 4680"

    # Operation 4681: delete
    del tree[8092]
    del reference[8092]
    assert tree.invariants(), "Invariants failed at step 4681"

    # Operation 4682: get
    assert tree.invariants(), "Invariants failed at step 4682"

    # Operation 4683: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4683"

    # Operation 4684: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4684"

    # Operation 4685: get
    assert tree.invariants(), "Invariants failed at step 4685"

    # Operation 4686: insert
    tree[6783] = 'value_259289'
    reference[6783] = 'value_259289'
    assert tree.invariants(), "Invariants failed at step 4686"

    # Operation 4687: get
    assert tree.invariants(), "Invariants failed at step 4687"

    # Operation 4688: get
    assert tree.invariants(), "Invariants failed at step 4688"

    # Operation 4689: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4689"

    # Operation 4690: delete
    del tree[6783]
    del reference[6783]
    assert tree.invariants(), "Invariants failed at step 4690"

    # Operation 4691: insert
    tree[7912] = 'value_609937'
    reference[7912] = 'value_609937'
    assert tree.invariants(), "Invariants failed at step 4691"

    # Operation 4692: update
    tree[7912] = 'value_593815'
    reference[7912] = 'value_593815'
    assert tree.invariants(), "Invariants failed at step 4692"

    # Operation 4693: get
    assert tree.invariants(), "Invariants failed at step 4693"

    # Operation 4694: delete
    del tree[7912]
    del reference[7912]
    assert tree.invariants(), "Invariants failed at step 4694"

    # Operation 4695: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4695"

    # Operation 4696: get
    assert tree.invariants(), "Invariants failed at step 4696"

    # Operation 4697: get
    assert tree.invariants(), "Invariants failed at step 4697"

    # Operation 4698: insert
    tree[4302] = 'value_307184'
    reference[4302] = 'value_307184'
    assert tree.invariants(), "Invariants failed at step 4698"

    # Operation 4699: delete
    del tree[4302]
    del reference[4302]
    assert tree.invariants(), "Invariants failed at step 4699"

    # Operation 4700: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4700"

    # Operation 4701: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4701"

    # Operation 4702: insert
    tree[2297] = 'value_449569'
    reference[2297] = 'value_449569'
    assert tree.invariants(), "Invariants failed at step 4702"

    # Operation 4703: delete
    del tree[2297]
    del reference[2297]
    assert tree.invariants(), "Invariants failed at step 4703"

    # Operation 4704: insert
    tree[5005] = 'value_848613'
    reference[5005] = 'value_848613'
    assert tree.invariants(), "Invariants failed at step 4704"

    # Operation 4705: insert
    tree[9303] = 'value_249759'
    reference[9303] = 'value_249759'
    assert tree.invariants(), "Invariants failed at step 4705"

    # Operation 4706: delete
    del tree[9303]
    del reference[9303]
    assert tree.invariants(), "Invariants failed at step 4706"

    # Operation 4707: update
    tree[5005] = 'value_270281'
    reference[5005] = 'value_270281'
    assert tree.invariants(), "Invariants failed at step 4707"

    # Operation 4708: insert
    tree[2133] = 'value_655926'
    reference[2133] = 'value_655926'
    assert tree.invariants(), "Invariants failed at step 4708"

    # Operation 4709: insert
    tree[296] = 'value_303651'
    reference[296] = 'value_303651'
    assert tree.invariants(), "Invariants failed at step 4709"

    # Operation 4710: update
    tree[2133] = 'value_69698'
    reference[2133] = 'value_69698'
    assert tree.invariants(), "Invariants failed at step 4710"

    # Operation 4711: insert
    tree[7610] = 'value_213773'
    reference[7610] = 'value_213773'
    assert tree.invariants(), "Invariants failed at step 4711"

    # Operation 4712: delete
    del tree[2133]
    del reference[2133]
    assert tree.invariants(), "Invariants failed at step 4712"

    # Operation 4713: get
    assert tree.invariants(), "Invariants failed at step 4713"

    # Operation 4714: update
    tree[7610] = 'value_358893'
    reference[7610] = 'value_358893'
    assert tree.invariants(), "Invariants failed at step 4714"

    # Operation 4715: get
    assert tree.invariants(), "Invariants failed at step 4715"

    # Operation 4716: insert
    tree[959] = 'value_773172'
    reference[959] = 'value_773172'
    assert tree.invariants(), "Invariants failed at step 4716"

    # Operation 4717: get
    assert tree.invariants(), "Invariants failed at step 4717"

    # Operation 4718: insert
    tree[8275] = 'value_775184'
    reference[8275] = 'value_775184'
    assert tree.invariants(), "Invariants failed at step 4718"

    # Operation 4719: update
    tree[7610] = 'value_452714'
    reference[7610] = 'value_452714'
    assert tree.invariants(), "Invariants failed at step 4719"

    # Operation 4720: get
    assert tree.invariants(), "Invariants failed at step 4720"

    # Operation 4721: insert
    tree[5391] = 'value_60976'
    reference[5391] = 'value_60976'
    assert tree.invariants(), "Invariants failed at step 4721"

    # Operation 4722: get
    assert tree.invariants(), "Invariants failed at step 4722"

    # Operation 4723: delete
    del tree[296]
    del reference[296]
    assert tree.invariants(), "Invariants failed at step 4723"

    # Operation 4724: get
    assert tree.invariants(), "Invariants failed at step 4724"

    # Operation 4725: get
    assert tree.invariants(), "Invariants failed at step 4725"

    # Operation 4726: insert
    tree[5952] = 'value_683621'
    reference[5952] = 'value_683621'
    assert tree.invariants(), "Invariants failed at step 4726"

    # Operation 4727: insert
    tree[4935] = 'value_617692'
    reference[4935] = 'value_617692'
    assert tree.invariants(), "Invariants failed at step 4727"

    # Operation 4728: get
    assert tree.invariants(), "Invariants failed at step 4728"

    # Operation 4729: delete
    del tree[5005]
    del reference[5005]
    assert tree.invariants(), "Invariants failed at step 4729"

    # Operation 4730: batch_delete
    keys_to_delete = [5952, 4935, 5391, 8275, 959]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4730"

    # Operation 4731: get
    assert tree.invariants(), "Invariants failed at step 4731"

    # Operation 4732: insert
    tree[7352] = 'value_433839'
    reference[7352] = 'value_433839'
    assert tree.invariants(), "Invariants failed at step 4732"

    # Operation 4733: insert
    tree[9392] = 'value_139059'
    reference[9392] = 'value_139059'
    assert tree.invariants(), "Invariants failed at step 4733"

    # Operation 4734: get
    assert tree.invariants(), "Invariants failed at step 4734"

    # Operation 4735: delete
    del tree[7610]
    del reference[7610]
    assert tree.invariants(), "Invariants failed at step 4735"

    # Operation 4736: insert
    tree[8894] = 'value_771682'
    reference[8894] = 'value_771682'
    assert tree.invariants(), "Invariants failed at step 4736"

    # Operation 4737: delete
    del tree[8894]
    del reference[8894]
    assert tree.invariants(), "Invariants failed at step 4737"

    # Operation 4738: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4738"

    # Operation 4739: delete
    del tree[7352]
    del reference[7352]
    assert tree.invariants(), "Invariants failed at step 4739"

    # Operation 4740: get
    assert tree.invariants(), "Invariants failed at step 4740"

    # Operation 4741: update
    tree[9392] = 'value_426102'
    reference[9392] = 'value_426102'
    assert tree.invariants(), "Invariants failed at step 4741"

    # Operation 4742: get
    assert tree.invariants(), "Invariants failed at step 4742"

    # Operation 4743: get
    assert tree.invariants(), "Invariants failed at step 4743"

    # Operation 4744: get
    assert tree.invariants(), "Invariants failed at step 4744"

    # Operation 4745: insert
    tree[1092] = 'value_640327'
    reference[1092] = 'value_640327'
    assert tree.invariants(), "Invariants failed at step 4745"

    # Operation 4746: insert
    tree[9931] = 'value_104036'
    reference[9931] = 'value_104036'
    assert tree.invariants(), "Invariants failed at step 4746"

    # Operation 4747: update
    tree[9931] = 'value_651417'
    reference[9931] = 'value_651417'
    assert tree.invariants(), "Invariants failed at step 4747"

    # Operation 4748: delete
    del tree[9392]
    del reference[9392]
    assert tree.invariants(), "Invariants failed at step 4748"

    # Operation 4749: update
    tree[9931] = 'value_904023'
    reference[9931] = 'value_904023'
    assert tree.invariants(), "Invariants failed at step 4749"

    # Operation 4750: insert
    tree[3820] = 'value_725101'
    reference[3820] = 'value_725101'
    assert tree.invariants(), "Invariants failed at step 4750"

    # Operation 4751: insert
    tree[3960] = 'value_161635'
    reference[3960] = 'value_161635'
    assert tree.invariants(), "Invariants failed at step 4751"

    # Operation 4752: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4752"

    # Operation 4753: delete
    del tree[3960]
    del reference[3960]
    assert tree.invariants(), "Invariants failed at step 4753"

    # Operation 4754: insert
    tree[1506] = 'value_482513'
    reference[1506] = 'value_482513'
    assert tree.invariants(), "Invariants failed at step 4754"

    # Operation 4755: delete
    del tree[1506]
    del reference[1506]
    assert tree.invariants(), "Invariants failed at step 4755"

    # Operation 4756: insert
    tree[2948] = 'value_961836'
    reference[2948] = 'value_961836'
    assert tree.invariants(), "Invariants failed at step 4756"

    # Operation 4757: delete
    del tree[2948]
    del reference[2948]
    assert tree.invariants(), "Invariants failed at step 4757"

    # Operation 4758: get
    assert tree.invariants(), "Invariants failed at step 4758"

    # Operation 4759: delete
    del tree[9931]
    del reference[9931]
    assert tree.invariants(), "Invariants failed at step 4759"

    # Operation 4760: insert
    tree[2171] = 'value_998066'
    reference[2171] = 'value_998066'
    assert tree.invariants(), "Invariants failed at step 4760"

    # Operation 4761: update
    tree[1092] = 'value_158537'
    reference[1092] = 'value_158537'
    assert tree.invariants(), "Invariants failed at step 4761"

    # Operation 4762: get
    assert tree.invariants(), "Invariants failed at step 4762"

    # Operation 4763: insert
    tree[7121] = 'value_774865'
    reference[7121] = 'value_774865'
    assert tree.invariants(), "Invariants failed at step 4763"

    # Operation 4764: insert
    tree[5502] = 'value_714215'
    reference[5502] = 'value_714215'
    assert tree.invariants(), "Invariants failed at step 4764"

    # Operation 4765: batch_delete
    keys_to_delete = [7209, 3820, 5502, 8745]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4765"

    # Operation 4766: delete
    del tree[7121]
    del reference[7121]
    assert tree.invariants(), "Invariants failed at step 4766"

    # Operation 4767: insert
    tree[4248] = 'value_875387'
    reference[4248] = 'value_875387'
    assert tree.invariants(), "Invariants failed at step 4767"

    # Operation 4768: update
    tree[1092] = 'value_54024'
    reference[1092] = 'value_54024'
    assert tree.invariants(), "Invariants failed at step 4768"

    # Operation 4769: update
    tree[4248] = 'value_196867'
    reference[4248] = 'value_196867'
    assert tree.invariants(), "Invariants failed at step 4769"

    # Operation 4770: insert
    tree[8065] = 'value_960605'
    reference[8065] = 'value_960605'
    assert tree.invariants(), "Invariants failed at step 4770"

    # Operation 4771: delete
    del tree[2171]
    del reference[2171]
    assert tree.invariants(), "Invariants failed at step 4771"

    # Operation 4772: insert
    tree[4272] = 'value_587334'
    reference[4272] = 'value_587334'
    assert tree.invariants(), "Invariants failed at step 4772"

    # Operation 4773: delete
    del tree[8065]
    del reference[8065]
    assert tree.invariants(), "Invariants failed at step 4773"

    # Operation 4774: get
    assert tree.invariants(), "Invariants failed at step 4774"

    # Operation 4775: insert
    tree[4047] = 'value_588732'
    reference[4047] = 'value_588732'
    assert tree.invariants(), "Invariants failed at step 4775"

    # Operation 4776: delete
    del tree[4272]
    del reference[4272]
    assert tree.invariants(), "Invariants failed at step 4776"

    # Operation 4777: update
    tree[1092] = 'value_31689'
    reference[1092] = 'value_31689'
    assert tree.invariants(), "Invariants failed at step 4777"

    # Operation 4778: get
    assert tree.invariants(), "Invariants failed at step 4778"

    # Operation 4779: get
    assert tree.invariants(), "Invariants failed at step 4779"

    # Operation 4780: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4780"

    # Operation 4781: insert
    tree[5783] = 'value_464097'
    reference[5783] = 'value_464097'
    assert tree.invariants(), "Invariants failed at step 4781"

    # Operation 4782: insert
    tree[2820] = 'value_797491'
    reference[2820] = 'value_797491'
    assert tree.invariants(), "Invariants failed at step 4782"

    # Operation 4783: batch_delete
    keys_to_delete = [4047, 7796, 2182, 5783]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4783"

    # Operation 4784: delete
    del tree[4248]
    del reference[4248]
    assert tree.invariants(), "Invariants failed at step 4784"

    # Operation 4785: delete
    del tree[1092]
    del reference[1092]
    assert tree.invariants(), "Invariants failed at step 4785"

    # Operation 4786: insert
    tree[5473] = 'value_830761'
    reference[5473] = 'value_830761'
    assert tree.invariants(), "Invariants failed at step 4786"

    # Operation 4787: get
    assert tree.invariants(), "Invariants failed at step 4787"

    # Operation 4788: delete
    del tree[5473]
    del reference[5473]
    assert tree.invariants(), "Invariants failed at step 4788"

    # Operation 4789: insert
    tree[3090] = 'value_435491'
    reference[3090] = 'value_435491'
    assert tree.invariants(), "Invariants failed at step 4789"

    # Operation 4790: insert
    tree[8625] = 'value_738315'
    reference[8625] = 'value_738315'
    assert tree.invariants(), "Invariants failed at step 4790"

    # Operation 4791: insert
    tree[2252] = 'value_881529'
    reference[2252] = 'value_881529'
    assert tree.invariants(), "Invariants failed at step 4791"

    # Operation 4792: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4792"

    # Operation 4793: insert
    tree[7631] = 'value_522535'
    reference[7631] = 'value_522535'
    assert tree.invariants(), "Invariants failed at step 4793"

    # Operation 4794: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4794"

    # Operation 4795: insert
    tree[6544] = 'value_527595'
    reference[6544] = 'value_527595'
    assert tree.invariants(), "Invariants failed at step 4795"

    # Operation 4796: update
    tree[7631] = 'value_555618'
    reference[7631] = 'value_555618'
    assert tree.invariants(), "Invariants failed at step 4796"

    # Operation 4797: insert
    tree[5871] = 'value_532022'
    reference[5871] = 'value_532022'
    assert tree.invariants(), "Invariants failed at step 4797"

    # Operation 4798: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4798"

    # Operation 4799: delete
    del tree[7631]
    del reference[7631]
    assert tree.invariants(), "Invariants failed at step 4799"

    # Operation 4800: insert
    tree[7307] = 'value_378676'
    reference[7307] = 'value_378676'
    assert tree.invariants(), "Invariants failed at step 4800"

    # Operation 4801: delete
    del tree[3090]
    del reference[3090]
    assert tree.invariants(), "Invariants failed at step 4801"

    # Operation 4802: insert
    tree[3865] = 'value_638167'
    reference[3865] = 'value_638167'
    assert tree.invariants(), "Invariants failed at step 4802"

    # Operation 4803: insert
    tree[3957] = 'value_326839'
    reference[3957] = 'value_326839'
    assert tree.invariants(), "Invariants failed at step 4803"

    # Operation 4804: delete
    del tree[6544]
    del reference[6544]
    assert tree.invariants(), "Invariants failed at step 4804"

    # Operation 4805: get
    assert tree.invariants(), "Invariants failed at step 4805"

    # Operation 4806: get
    assert tree.invariants(), "Invariants failed at step 4806"

    # Operation 4807: insert
    tree[1451] = 'value_245496'
    reference[1451] = 'value_245496'
    assert tree.invariants(), "Invariants failed at step 4807"

    # Operation 4808: get
    assert tree.invariants(), "Invariants failed at step 4808"

    # Operation 4809: batch_delete
    keys_to_delete = [2820, 1451, 2252, 9721, 977, 3865]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4809"

    # Operation 4810: insert
    tree[1599] = 'value_591349'
    reference[1599] = 'value_591349'
    assert tree.invariants(), "Invariants failed at step 4810"

    # Operation 4811: get
    assert tree.invariants(), "Invariants failed at step 4811"

    # Operation 4812: insert
    tree[8256] = 'value_411194'
    reference[8256] = 'value_411194'
    assert tree.invariants(), "Invariants failed at step 4812"

    # Operation 4813: insert
    tree[2693] = 'value_730441'
    reference[2693] = 'value_730441'
    assert tree.invariants(), "Invariants failed at step 4813"

    # Operation 4814: update
    tree[5871] = 'value_53117'
    reference[5871] = 'value_53117'
    assert tree.invariants(), "Invariants failed at step 4814"

    # Operation 4815: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4815"

    # Operation 4816: insert
    tree[5324] = 'value_789190'
    reference[5324] = 'value_789190'
    assert tree.invariants(), "Invariants failed at step 4816"

    # Operation 4817: insert
    tree[8245] = 'value_271222'
    reference[8245] = 'value_271222'
    assert tree.invariants(), "Invariants failed at step 4817"

    # Operation 4818: insert
    tree[4278] = 'value_42156'
    reference[4278] = 'value_42156'
    assert tree.invariants(), "Invariants failed at step 4818"

    # Operation 4819: insert
    tree[6902] = 'value_484122'
    reference[6902] = 'value_484122'
    assert tree.invariants(), "Invariants failed at step 4819"

    # Operation 4820: get
    assert tree.invariants(), "Invariants failed at step 4820"

    # Operation 4821: get
    assert tree.invariants(), "Invariants failed at step 4821"

    # Operation 4822: insert
    tree[7822] = 'value_313267'
    reference[7822] = 'value_313267'
    assert tree.invariants(), "Invariants failed at step 4822"

    # Operation 4823: delete
    del tree[1599]
    del reference[1599]
    assert tree.invariants(), "Invariants failed at step 4823"

    # Operation 4824: insert
    tree[6028] = 'value_171992'
    reference[6028] = 'value_171992'
    assert tree.invariants(), "Invariants failed at step 4824"

    # Operation 4825: delete
    del tree[4278]
    del reference[4278]
    assert tree.invariants(), "Invariants failed at step 4825"

    # Operation 4826: insert
    tree[5386] = 'value_147549'
    reference[5386] = 'value_147549'
    assert tree.invariants(), "Invariants failed at step 4826"

    # Operation 4827: get
    assert tree.invariants(), "Invariants failed at step 4827"

    # Operation 4828: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4828"

    # Operation 4829: delete
    del tree[7307]
    del reference[7307]
    assert tree.invariants(), "Invariants failed at step 4829"

    # Operation 4830: insert
    tree[1636] = 'value_379911'
    reference[1636] = 'value_379911'
    assert tree.invariants(), "Invariants failed at step 4830"

    # Operation 4831: delete
    del tree[7822]
    del reference[7822]
    assert tree.invariants(), "Invariants failed at step 4831"

    # Operation 4832: get
    assert tree.invariants(), "Invariants failed at step 4832"

    # Operation 4833: get
    assert tree.invariants(), "Invariants failed at step 4833"

    # Operation 4834: delete
    del tree[8245]
    del reference[8245]
    assert tree.invariants(), "Invariants failed at step 4834"

    # Operation 4835: update
    tree[2693] = 'value_73260'
    reference[2693] = 'value_73260'
    assert tree.invariants(), "Invariants failed at step 4835"

    # Operation 4836: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4836"

    # Operation 4837: get
    assert tree.invariants(), "Invariants failed at step 4837"

    # Operation 4838: insert
    tree[4874] = 'value_584047'
    reference[4874] = 'value_584047'
    assert tree.invariants(), "Invariants failed at step 4838"

    # Operation 4839: get
    assert tree.invariants(), "Invariants failed at step 4839"

    # Operation 4840: insert
    tree[8487] = 'value_99789'
    reference[8487] = 'value_99789'
    assert tree.invariants(), "Invariants failed at step 4840"

    # Operation 4841: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4841"

    # Operation 4842: insert
    tree[3101] = 'value_464330'
    reference[3101] = 'value_464330'
    assert tree.invariants(), "Invariants failed at step 4842"

    # Operation 4843: get
    assert tree.invariants(), "Invariants failed at step 4843"

    # Operation 4844: get
    assert tree.invariants(), "Invariants failed at step 4844"

    # Operation 4845: get
    assert tree.invariants(), "Invariants failed at step 4845"

    # Operation 4846: insert
    tree[7264] = 'value_181769'
    reference[7264] = 'value_181769'
    assert tree.invariants(), "Invariants failed at step 4846"

    # Operation 4847: get
    assert tree.invariants(), "Invariants failed at step 4847"

    # Operation 4848: insert
    tree[8597] = 'value_239470'
    reference[8597] = 'value_239470'
    assert tree.invariants(), "Invariants failed at step 4848"

    # Operation 4849: get
    assert tree.invariants(), "Invariants failed at step 4849"

    # Operation 4850: get
    assert tree.invariants(), "Invariants failed at step 4850"

    # Operation 4851: insert
    tree[1108] = 'value_763233'
    reference[1108] = 'value_763233'
    assert tree.invariants(), "Invariants failed at step 4851"

    # Operation 4852: insert
    tree[2669] = 'value_292811'
    reference[2669] = 'value_292811'
    assert tree.invariants(), "Invariants failed at step 4852"

    # Operation 4853: delete
    del tree[2693]
    del reference[2693]
    assert tree.invariants(), "Invariants failed at step 4853"

    # Operation 4854: delete
    del tree[6028]
    del reference[6028]
    assert tree.invariants(), "Invariants failed at step 4854"

    # Operation 4855: batch_delete
    keys_to_delete = [7264, 8487, 5386, 2669, 4989, 8625, 6902, 1724, 3101]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4855"

    # Operation 4856: delete
    del tree[3957]
    del reference[3957]
    assert tree.invariants(), "Invariants failed at step 4856"

    # Operation 4857: insert
    tree[1207] = 'value_604234'
    reference[1207] = 'value_604234'
    assert tree.invariants(), "Invariants failed at step 4857"

    # Operation 4858: get
    assert tree.invariants(), "Invariants failed at step 4858"

    # Operation 4859: insert
    tree[7037] = 'value_300283'
    reference[7037] = 'value_300283'
    assert tree.invariants(), "Invariants failed at step 4859"

    # Operation 4860: batch_delete
    keys_to_delete = [4874, 148, 1108, 8597, 1207, 7131]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4860"

    # Operation 4861: get
    assert tree.invariants(), "Invariants failed at step 4861"

    # Operation 4862: delete
    del tree[5871]
    del reference[5871]
    assert tree.invariants(), "Invariants failed at step 4862"

    # Operation 4863: insert
    tree[8119] = 'value_369961'
    reference[8119] = 'value_369961'
    assert tree.invariants(), "Invariants failed at step 4863"

    # Operation 4864: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4864"

    # Operation 4865: get
    assert tree.invariants(), "Invariants failed at step 4865"

    # Operation 4866: get
    assert tree.invariants(), "Invariants failed at step 4866"

    # Operation 4867: update
    tree[8256] = 'value_212022'
    reference[8256] = 'value_212022'
    assert tree.invariants(), "Invariants failed at step 4867"

    # Operation 4868: get
    assert tree.invariants(), "Invariants failed at step 4868"

    # Operation 4869: insert
    tree[1152] = 'value_148818'
    reference[1152] = 'value_148818'
    assert tree.invariants(), "Invariants failed at step 4869"

    # Operation 4870: update
    tree[1152] = 'value_597513'
    reference[1152] = 'value_597513'
    assert tree.invariants(), "Invariants failed at step 4870"

    # Operation 4871: batch_delete
    keys_to_delete = [4937, 5324, 4722, 8119, 7037]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4871"

    # Operation 4872: get
    assert tree.invariants(), "Invariants failed at step 4872"

    # Operation 4873: delete
    del tree[1636]
    del reference[1636]
    assert tree.invariants(), "Invariants failed at step 4873"

    # Operation 4874: update
    tree[1152] = 'value_393160'
    reference[1152] = 'value_393160'
    assert tree.invariants(), "Invariants failed at step 4874"

    # Operation 4875: get
    assert tree.invariants(), "Invariants failed at step 4875"

    # Operation 4876: insert
    tree[477] = 'value_526371'
    reference[477] = 'value_526371'
    assert tree.invariants(), "Invariants failed at step 4876"

    # Operation 4877: get
    assert tree.invariants(), "Invariants failed at step 4877"

    # Operation 4878: insert
    tree[6518] = 'value_540907'
    reference[6518] = 'value_540907'
    assert tree.invariants(), "Invariants failed at step 4878"

    # Operation 4879: delete
    del tree[8256]
    del reference[8256]
    assert tree.invariants(), "Invariants failed at step 4879"

    # Operation 4880: delete
    del tree[1152]
    del reference[1152]
    assert tree.invariants(), "Invariants failed at step 4880"

    # Operation 4881: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4881"

    # Operation 4882: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4882"

    # Operation 4883: get
    assert tree.invariants(), "Invariants failed at step 4883"

    # Operation 4884: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4884"

    # Operation 4885: insert
    tree[7293] = 'value_390805'
    reference[7293] = 'value_390805'
    assert tree.invariants(), "Invariants failed at step 4885"

    # Operation 4886: update
    tree[477] = 'value_178112'
    reference[477] = 'value_178112'
    assert tree.invariants(), "Invariants failed at step 4886"

    # Operation 4887: delete
    del tree[7293]
    del reference[7293]
    assert tree.invariants(), "Invariants failed at step 4887"

    # Operation 4888: insert
    tree[5526] = 'value_834852'
    reference[5526] = 'value_834852'
    assert tree.invariants(), "Invariants failed at step 4888"

    # Operation 4889: delete
    del tree[6518]
    del reference[6518]
    assert tree.invariants(), "Invariants failed at step 4889"

    # Operation 4890: delete
    del tree[5526]
    del reference[5526]
    assert tree.invariants(), "Invariants failed at step 4890"

    # Operation 4891: delete
    del tree[477]
    del reference[477]
    assert tree.invariants(), "Invariants failed at step 4891"

    # Operation 4892: insert
    tree[9110] = 'value_172691'
    reference[9110] = 'value_172691'
    assert tree.invariants(), "Invariants failed at step 4892"

    # Operation 4893: get
    assert tree.invariants(), "Invariants failed at step 4893"

    # Operation 4894: update
    tree[9110] = 'value_859337'
    reference[9110] = 'value_859337'
    assert tree.invariants(), "Invariants failed at step 4894"

    # Operation 4895: delete
    del tree[9110]
    del reference[9110]
    assert tree.invariants(), "Invariants failed at step 4895"

    # Operation 4896: get
    assert tree.invariants(), "Invariants failed at step 4896"

    # Operation 4897: insert
    tree[4274] = 'value_649397'
    reference[4274] = 'value_649397'
    assert tree.invariants(), "Invariants failed at step 4897"

    # Operation 4898: insert
    tree[5358] = 'value_892759'
    reference[5358] = 'value_892759'
    assert tree.invariants(), "Invariants failed at step 4898"

    # Operation 4899: insert
    tree[9355] = 'value_231914'
    reference[9355] = 'value_231914'
    assert tree.invariants(), "Invariants failed at step 4899"

    # Operation 4900: get
    assert tree.invariants(), "Invariants failed at step 4900"

    # Operation 4901: get
    assert tree.invariants(), "Invariants failed at step 4901"

    # Operation 4902: insert
    tree[7025] = 'value_81058'
    reference[7025] = 'value_81058'
    assert tree.invariants(), "Invariants failed at step 4902"

    # Operation 4903: delete
    del tree[4274]
    del reference[4274]
    assert tree.invariants(), "Invariants failed at step 4903"

    # Operation 4904: delete
    del tree[7025]
    del reference[7025]
    assert tree.invariants(), "Invariants failed at step 4904"

    # Operation 4905: insert
    tree[180] = 'value_633828'
    reference[180] = 'value_633828'
    assert tree.invariants(), "Invariants failed at step 4905"

    # Operation 4906: delete
    del tree[5358]
    del reference[5358]
    assert tree.invariants(), "Invariants failed at step 4906"

    # Operation 4907: get
    assert tree.invariants(), "Invariants failed at step 4907"

    # Operation 4908: delete
    del tree[180]
    del reference[180]
    assert tree.invariants(), "Invariants failed at step 4908"

    # Operation 4909: get
    assert tree.invariants(), "Invariants failed at step 4909"

    # Operation 4910: insert
    tree[4236] = 'value_235382'
    reference[4236] = 'value_235382'
    assert tree.invariants(), "Invariants failed at step 4910"

    # Operation 4911: get
    assert tree.invariants(), "Invariants failed at step 4911"

    # Operation 4912: get
    assert tree.invariants(), "Invariants failed at step 4912"

    # Operation 4913: get
    assert tree.invariants(), "Invariants failed at step 4913"

    # Operation 4914: delete
    del tree[4236]
    del reference[4236]
    assert tree.invariants(), "Invariants failed at step 4914"

    # Operation 4915: update
    tree[9355] = 'value_985762'
    reference[9355] = 'value_985762'
    assert tree.invariants(), "Invariants failed at step 4915"

    # Operation 4916: delete
    del tree[9355]
    del reference[9355]
    assert tree.invariants(), "Invariants failed at step 4916"

    # Operation 4917: get
    assert tree.invariants(), "Invariants failed at step 4917"

    # Operation 4918: get
    assert tree.invariants(), "Invariants failed at step 4918"

    # Operation 4919: insert
    tree[7274] = 'value_43829'
    reference[7274] = 'value_43829'
    assert tree.invariants(), "Invariants failed at step 4919"

    # Operation 4920: delete
    del tree[7274]
    del reference[7274]
    assert tree.invariants(), "Invariants failed at step 4920"

    # Operation 4921: insert
    tree[4945] = 'value_399026'
    reference[4945] = 'value_399026'
    assert tree.invariants(), "Invariants failed at step 4921"

    # Operation 4922: insert
    tree[3656] = 'value_115207'
    reference[3656] = 'value_115207'
    assert tree.invariants(), "Invariants failed at step 4922"

    # Operation 4923: insert
    tree[5247] = 'value_915778'
    reference[5247] = 'value_915778'
    assert tree.invariants(), "Invariants failed at step 4923"

    # Operation 4924: update
    tree[4945] = 'value_151172'
    reference[4945] = 'value_151172'
    assert tree.invariants(), "Invariants failed at step 4924"

    # Operation 4925: insert
    tree[8489] = 'value_570112'
    reference[8489] = 'value_570112'
    assert tree.invariants(), "Invariants failed at step 4925"

    # Operation 4926: get
    assert tree.invariants(), "Invariants failed at step 4926"

    # Operation 4927: delete
    del tree[4945]
    del reference[4945]
    assert tree.invariants(), "Invariants failed at step 4927"

    # Operation 4928: update
    tree[8489] = 'value_751007'
    reference[8489] = 'value_751007'
    assert tree.invariants(), "Invariants failed at step 4928"

    # Operation 4929: get
    assert tree.invariants(), "Invariants failed at step 4929"

    # Operation 4930: update
    tree[5247] = 'value_598448'
    reference[5247] = 'value_598448'
    assert tree.invariants(), "Invariants failed at step 4930"

    # Operation 4931: insert
    tree[3349] = 'value_446414'
    reference[3349] = 'value_446414'
    assert tree.invariants(), "Invariants failed at step 4931"

    # Operation 4932: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4932"

    # Operation 4933: delete
    del tree[5247]
    del reference[5247]
    assert tree.invariants(), "Invariants failed at step 4933"

    # Operation 4934: delete
    del tree[8489]
    del reference[8489]
    assert tree.invariants(), "Invariants failed at step 4934"

    # Operation 4935: get
    assert tree.invariants(), "Invariants failed at step 4935"

    # Operation 4936: delete
    del tree[3349]
    del reference[3349]
    assert tree.invariants(), "Invariants failed at step 4936"

    # Operation 4937: insert
    tree[8932] = 'value_547659'
    reference[8932] = 'value_547659'
    assert tree.invariants(), "Invariants failed at step 4937"

    # Operation 4938: get
    assert tree.invariants(), "Invariants failed at step 4938"

    # Operation 4939: get
    assert tree.invariants(), "Invariants failed at step 4939"

    # Operation 4940: insert
    tree[9970] = 'value_368572'
    reference[9970] = 'value_368572'
    assert tree.invariants(), "Invariants failed at step 4940"

    # Operation 4941: delete
    del tree[8932]
    del reference[8932]
    assert tree.invariants(), "Invariants failed at step 4941"

    # Operation 4942: insert
    tree[7917] = 'value_240241'
    reference[7917] = 'value_240241'
    assert tree.invariants(), "Invariants failed at step 4942"

    # Operation 4943: delete
    del tree[3656]
    del reference[3656]
    assert tree.invariants(), "Invariants failed at step 4943"

    # Operation 4944: insert
    tree[1946] = 'value_561296'
    reference[1946] = 'value_561296'
    assert tree.invariants(), "Invariants failed at step 4944"

    # Operation 4945: get
    assert tree.invariants(), "Invariants failed at step 4945"

    # Operation 4946: insert
    tree[9727] = 'value_239430'
    reference[9727] = 'value_239430'
    assert tree.invariants(), "Invariants failed at step 4946"

    # Operation 4947: delete
    del tree[1946]
    del reference[1946]
    assert tree.invariants(), "Invariants failed at step 4947"

    # Operation 4948: insert
    tree[5588] = 'value_893696'
    reference[5588] = 'value_893696'
    assert tree.invariants(), "Invariants failed at step 4948"

    # Operation 4949: get
    assert tree.invariants(), "Invariants failed at step 4949"

    # Operation 4950: get
    assert tree.invariants(), "Invariants failed at step 4950"

    # Operation 4951: insert
    tree[3350] = 'value_817210'
    reference[3350] = 'value_817210'
    assert tree.invariants(), "Invariants failed at step 4951"

    # Operation 4952: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4952"

    # Operation 4953: insert
    tree[1283] = 'value_391894'
    reference[1283] = 'value_391894'
    assert tree.invariants(), "Invariants failed at step 4953"

    # Operation 4954: get
    assert tree.invariants(), "Invariants failed at step 4954"

    # Operation 4955: get
    assert tree.invariants(), "Invariants failed at step 4955"

    # Operation 4956: delete
    del tree[9727]
    del reference[9727]
    assert tree.invariants(), "Invariants failed at step 4956"

    # Operation 4957: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4957"

    # Operation 4958: delete
    del tree[7917]
    del reference[7917]
    assert tree.invariants(), "Invariants failed at step 4958"

    # Operation 4959: update
    tree[9970] = 'value_516160'
    reference[9970] = 'value_516160'
    assert tree.invariants(), "Invariants failed at step 4959"

    # Operation 4960: get
    assert tree.invariants(), "Invariants failed at step 4960"

    # Operation 4961: insert
    tree[4205] = 'value_693730'
    reference[4205] = 'value_693730'
    assert tree.invariants(), "Invariants failed at step 4961"

    # Operation 4962: delete
    del tree[3350]
    del reference[3350]
    assert tree.invariants(), "Invariants failed at step 4962"

    # Operation 4963: update
    tree[1283] = 'value_903485'
    reference[1283] = 'value_903485'
    assert tree.invariants(), "Invariants failed at step 4963"

    # Operation 4964: insert
    tree[786] = 'value_259829'
    reference[786] = 'value_259829'
    assert tree.invariants(), "Invariants failed at step 4964"

    # Operation 4965: get
    assert tree.invariants(), "Invariants failed at step 4965"

    # Operation 4966: insert
    tree[163] = 'value_918578'
    reference[163] = 'value_918578'
    assert tree.invariants(), "Invariants failed at step 4966"

    # Operation 4967: update
    tree[4205] = 'value_314146'
    reference[4205] = 'value_314146'
    assert tree.invariants(), "Invariants failed at step 4967"

    # Operation 4968: insert
    tree[7227] = 'value_523166'
    reference[7227] = 'value_523166'
    assert tree.invariants(), "Invariants failed at step 4968"

    # Operation 4969: get
    assert tree.invariants(), "Invariants failed at step 4969"

    # Operation 4970: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4970"

    # Operation 4971: get
    assert tree.invariants(), "Invariants failed at step 4971"

    # Operation 4972: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 4972"

    # Operation 4973: insert
    tree[6337] = 'value_203955'
    reference[6337] = 'value_203955'
    assert tree.invariants(), "Invariants failed at step 4973"

    # Operation 4974: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4974"

    # Operation 4975: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4975"

    # Operation 4976: delete
    del tree[163]
    del reference[163]
    assert tree.invariants(), "Invariants failed at step 4976"

    # Operation 4977: insert
    tree[5006] = 'value_949855'
    reference[5006] = 'value_949855'
    assert tree.invariants(), "Invariants failed at step 4977"

    # Operation 4978: insert
    tree[9004] = 'value_401050'
    reference[9004] = 'value_401050'
    assert tree.invariants(), "Invariants failed at step 4978"

    # Operation 4979: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 4979"

    # Operation 4980: get
    assert tree.invariants(), "Invariants failed at step 4980"

    # Operation 4981: delete
    del tree[1283]
    del reference[1283]
    assert tree.invariants(), "Invariants failed at step 4981"

    # Operation 4982: update
    tree[9004] = 'value_317038'
    reference[9004] = 'value_317038'
    assert tree.invariants(), "Invariants failed at step 4982"

    # Operation 4983: insert
    tree[7050] = 'value_787830'
    reference[7050] = 'value_787830'
    assert tree.invariants(), "Invariants failed at step 4983"

    # Operation 4984: update
    tree[7227] = 'value_323814'
    reference[7227] = 'value_323814'
    assert tree.invariants(), "Invariants failed at step 4984"

    # Operation 4985: get
    assert tree.invariants(), "Invariants failed at step 4985"

    # Operation 4986: delete
    del tree[9004]
    del reference[9004]
    assert tree.invariants(), "Invariants failed at step 4986"

    # Operation 4987: delete
    del tree[5006]
    del reference[5006]
    assert tree.invariants(), "Invariants failed at step 4987"

    # Operation 4988: get
    assert tree.invariants(), "Invariants failed at step 4988"

    # Operation 4989: insert
    tree[8388] = 'value_624300'
    reference[8388] = 'value_624300'
    assert tree.invariants(), "Invariants failed at step 4989"

    # Operation 4990: delete
    del tree[5588]
    del reference[5588]
    assert tree.invariants(), "Invariants failed at step 4990"

    # Operation 4991: batch_delete
    keys_to_delete = [5409, 8388, 786, 6393, 7227]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4991"

    # Operation 4992: insert
    tree[3512] = 'value_682284'
    reference[3512] = 'value_682284'
    assert tree.invariants(), "Invariants failed at step 4992"

    # Operation 4993: insert
    tree[5688] = 'value_546966'
    reference[5688] = 'value_546966'
    assert tree.invariants(), "Invariants failed at step 4993"

    # Operation 4994: update
    tree[7050] = 'value_642391'
    reference[7050] = 'value_642391'
    assert tree.invariants(), "Invariants failed at step 4994"

    # Operation 4995: update
    tree[7050] = 'value_536345'
    reference[7050] = 'value_536345'
    assert tree.invariants(), "Invariants failed at step 4995"

    # Operation 4996: insert
    tree[4701] = 'value_689094'
    reference[4701] = 'value_689094'
    assert tree.invariants(), "Invariants failed at step 4996"

    # Operation 4997: batch_delete
    keys_to_delete = [3521, 7050, 5688, 4315, 4701]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 4997"

    # Operation 4998: insert
    tree[443] = 'value_278501'
    reference[443] = 'value_278501'
    assert tree.invariants(), "Invariants failed at step 4998"

    # Operation 4999: get
    assert tree.invariants(), "Invariants failed at step 4999"

    # Operation 5000: delete
    del tree[6337]
    del reference[6337]
    assert tree.invariants(), "Invariants failed at step 5000"

    # Operation 5001: update
    tree[4205] = 'value_188915'
    reference[4205] = 'value_188915'
    assert tree.invariants(), "Invariants failed at step 5001"

    # Operation 5002: get
    assert tree.invariants(), "Invariants failed at step 5002"

    # Operation 5003: insert
    tree[9834] = 'value_483017'
    reference[9834] = 'value_483017'
    assert tree.invariants(), "Invariants failed at step 5003"

    # Operation 5004: delete
    del tree[9970]
    del reference[9970]
    assert tree.invariants(), "Invariants failed at step 5004"

    # Operation 5005: delete
    del tree[3512]
    del reference[3512]
    assert tree.invariants(), "Invariants failed at step 5005"

    # Operation 5006: update
    tree[443] = 'value_750346'
    reference[443] = 'value_750346'
    assert tree.invariants(), "Invariants failed at step 5006"

    # Operation 5007: delete
    del tree[9834]
    del reference[9834]
    assert tree.invariants(), "Invariants failed at step 5007"

    # Operation 5008: insert
    tree[9406] = 'value_155422'
    reference[9406] = 'value_155422'
    assert tree.invariants(), "Invariants failed at step 5008"

    # Operation 5009: delete
    del tree[4205]
    del reference[4205]
    assert tree.invariants(), "Invariants failed at step 5009"

    # Operation 5010: insert
    tree[3984] = 'value_440451'
    reference[3984] = 'value_440451'
    assert tree.invariants(), "Invariants failed at step 5010"

    # Operation 5011: insert
    tree[685] = 'value_229131'
    reference[685] = 'value_229131'
    assert tree.invariants(), "Invariants failed at step 5011"

    # Operation 5012: update
    tree[9406] = 'value_139788'
    reference[9406] = 'value_139788'
    assert tree.invariants(), "Invariants failed at step 5012"

    # Operation 5013: delete
    del tree[3984]
    del reference[3984]
    assert tree.invariants(), "Invariants failed at step 5013"

    # Operation 5014: get
    assert tree.invariants(), "Invariants failed at step 5014"

    # Operation 5015: get
    assert tree.invariants(), "Invariants failed at step 5015"

    # Operation 5016: get
    assert tree.invariants(), "Invariants failed at step 5016"

    # Operation 5017: update
    tree[443] = 'value_619955'
    reference[443] = 'value_619955'
    assert tree.invariants(), "Invariants failed at step 5017"

    # Operation 5018: update
    tree[9406] = 'value_285544'
    reference[9406] = 'value_285544'
    assert tree.invariants(), "Invariants failed at step 5018"

    # Operation 5019: get
    assert tree.invariants(), "Invariants failed at step 5019"

    # Operation 5020: delete
    del tree[9406]
    del reference[9406]
    assert tree.invariants(), "Invariants failed at step 5020"

    # Operation 5021: get
    assert tree.invariants(), "Invariants failed at step 5021"

    # Operation 5022: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5022"

    # Operation 5023: delete
    del tree[685]
    del reference[685]
    assert tree.invariants(), "Invariants failed at step 5023"

    # Operation 5024: insert
    tree[5645] = 'value_578806'
    reference[5645] = 'value_578806'
    assert tree.invariants(), "Invariants failed at step 5024"

    # Operation 5025: insert
    tree[8497] = 'value_48173'
    reference[8497] = 'value_48173'
    assert tree.invariants(), "Invariants failed at step 5025"

    # Operation 5026: delete
    del tree[443]
    del reference[443]
    assert tree.invariants(), "Invariants failed at step 5026"

    # Operation 5027: update
    tree[8497] = 'value_800652'
    reference[8497] = 'value_800652'
    assert tree.invariants(), "Invariants failed at step 5027"

    # Operation 5028: get
    assert tree.invariants(), "Invariants failed at step 5028"

    # Operation 5029: insert
    tree[1063] = 'value_93148'
    reference[1063] = 'value_93148'
    assert tree.invariants(), "Invariants failed at step 5029"

    # Operation 5030: update
    tree[5645] = 'value_802260'
    reference[5645] = 'value_802260'
    assert tree.invariants(), "Invariants failed at step 5030"

    # Operation 5031: get
    assert tree.invariants(), "Invariants failed at step 5031"

    # Operation 5032: update
    tree[5645] = 'value_787749'
    reference[5645] = 'value_787749'
    assert tree.invariants(), "Invariants failed at step 5032"

    # Operation 5033: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5033"

    # Operation 5034: delete
    del tree[8497]
    del reference[8497]
    assert tree.invariants(), "Invariants failed at step 5034"

    # Operation 5035: delete
    del tree[1063]
    del reference[1063]
    assert tree.invariants(), "Invariants failed at step 5035"

    # Operation 5036: get
    assert tree.invariants(), "Invariants failed at step 5036"

    # Operation 5037: insert
    tree[3712] = 'value_593868'
    reference[3712] = 'value_593868'
    assert tree.invariants(), "Invariants failed at step 5037"

    # Operation 5038: update
    tree[5645] = 'value_688807'
    reference[5645] = 'value_688807'
    assert tree.invariants(), "Invariants failed at step 5038"

    # Operation 5039: delete
    del tree[5645]
    del reference[5645]
    assert tree.invariants(), "Invariants failed at step 5039"

    # Operation 5040: insert
    tree[4929] = 'value_356625'
    reference[4929] = 'value_356625'
    assert tree.invariants(), "Invariants failed at step 5040"

    # Operation 5041: delete
    del tree[3712]
    del reference[3712]
    assert tree.invariants(), "Invariants failed at step 5041"

    # Operation 5042: insert
    tree[2293] = 'value_278525'
    reference[2293] = 'value_278525'
    assert tree.invariants(), "Invariants failed at step 5042"

    # Operation 5043: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5043"

    # Operation 5044: update
    tree[4929] = 'value_485841'
    reference[4929] = 'value_485841'
    assert tree.invariants(), "Invariants failed at step 5044"

    # Operation 5045: insert
    tree[1342] = 'value_646252'
    reference[1342] = 'value_646252'
    assert tree.invariants(), "Invariants failed at step 5045"

    # Operation 5046: get
    assert tree.invariants(), "Invariants failed at step 5046"

    # Operation 5047: get
    assert tree.invariants(), "Invariants failed at step 5047"

    # Operation 5048: get
    assert tree.invariants(), "Invariants failed at step 5048"

    # Operation 5049: delete
    del tree[4929]
    del reference[4929]
    assert tree.invariants(), "Invariants failed at step 5049"

    # Operation 5050: insert
    tree[1736] = 'value_466428'
    reference[1736] = 'value_466428'
    assert tree.invariants(), "Invariants failed at step 5050"

    # Operation 5051: get
    assert tree.invariants(), "Invariants failed at step 5051"

    # Operation 5052: delete
    del tree[1342]
    del reference[1342]
    assert tree.invariants(), "Invariants failed at step 5052"

    # Operation 5053: insert
    tree[3492] = 'value_772194'
    reference[3492] = 'value_772194'
    assert tree.invariants(), "Invariants failed at step 5053"

    # Operation 5054: update
    tree[1736] = 'value_227671'
    reference[1736] = 'value_227671'
    assert tree.invariants(), "Invariants failed at step 5054"

    # Operation 5055: update
    tree[2293] = 'value_190783'
    reference[2293] = 'value_190783'
    assert tree.invariants(), "Invariants failed at step 5055"

    # Operation 5056: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5056"

    # Operation 5057: insert
    tree[3414] = 'value_182882'
    reference[3414] = 'value_182882'
    assert tree.invariants(), "Invariants failed at step 5057"

    # Operation 5058: insert
    tree[8591] = 'value_494758'
    reference[8591] = 'value_494758'
    assert tree.invariants(), "Invariants failed at step 5058"

    # Operation 5059: get
    assert tree.invariants(), "Invariants failed at step 5059"

    # Operation 5060: delete
    del tree[2293]
    del reference[2293]
    assert tree.invariants(), "Invariants failed at step 5060"

    # Operation 5061: delete
    del tree[3414]
    del reference[3414]
    assert tree.invariants(), "Invariants failed at step 5061"

    # Operation 5062: insert
    tree[1385] = 'value_833979'
    reference[1385] = 'value_833979'
    assert tree.invariants(), "Invariants failed at step 5062"

    # Operation 5063: delete
    del tree[8591]
    del reference[8591]
    assert tree.invariants(), "Invariants failed at step 5063"

    # Operation 5064: insert
    tree[5972] = 'value_37353'
    reference[5972] = 'value_37353'
    assert tree.invariants(), "Invariants failed at step 5064"

    # Operation 5065: insert
    tree[9742] = 'value_393199'
    reference[9742] = 'value_393199'
    assert tree.invariants(), "Invariants failed at step 5065"

    # Operation 5066: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5066"

    # Operation 5067: delete
    del tree[1736]
    del reference[1736]
    assert tree.invariants(), "Invariants failed at step 5067"

    # Operation 5068: delete
    del tree[9742]
    del reference[9742]
    assert tree.invariants(), "Invariants failed at step 5068"

    # Operation 5069: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5069"

    # Operation 5070: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5070"

    # Operation 5071: insert
    tree[1794] = 'value_424430'
    reference[1794] = 'value_424430'
    assert tree.invariants(), "Invariants failed at step 5071"

    # Operation 5072: insert
    tree[847] = 'value_549132'
    reference[847] = 'value_549132'
    assert tree.invariants(), "Invariants failed at step 5072"

    # Operation 5073: insert
    tree[4488] = 'value_346234'
    reference[4488] = 'value_346234'
    assert tree.invariants(), "Invariants failed at step 5073"

    # Operation 5074: insert
    tree[6039] = 'value_570768'
    reference[6039] = 'value_570768'
    assert tree.invariants(), "Invariants failed at step 5074"

    # Operation 5075: update
    tree[5972] = 'value_377798'
    reference[5972] = 'value_377798'
    assert tree.invariants(), "Invariants failed at step 5075"

    # Operation 5076: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5076"

    # Operation 5077: delete
    del tree[3492]
    del reference[3492]
    assert tree.invariants(), "Invariants failed at step 5077"

    # Operation 5078: delete
    del tree[1385]
    del reference[1385]
    assert tree.invariants(), "Invariants failed at step 5078"

    # Operation 5079: get
    assert tree.invariants(), "Invariants failed at step 5079"

    # Operation 5080: insert
    tree[9122] = 'value_648208'
    reference[9122] = 'value_648208'
    assert tree.invariants(), "Invariants failed at step 5080"

    # Operation 5081: batch_delete
    keys_to_delete = [1794, 4488, 847, 6039, 3227]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5081"

    # Operation 5082: insert
    tree[3960] = 'value_712020'
    reference[3960] = 'value_712020'
    assert tree.invariants(), "Invariants failed at step 5082"

    # Operation 5083: update
    tree[3960] = 'value_28738'
    reference[3960] = 'value_28738'
    assert tree.invariants(), "Invariants failed at step 5083"

    # Operation 5084: get
    assert tree.invariants(), "Invariants failed at step 5084"

    # Operation 5085: get
    assert tree.invariants(), "Invariants failed at step 5085"

    # Operation 5086: get
    assert tree.invariants(), "Invariants failed at step 5086"

    # Operation 5087: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5087"

    # Operation 5088: insert
    tree[4275] = 'value_782943'
    reference[4275] = 'value_782943'
    assert tree.invariants(), "Invariants failed at step 5088"

    # Operation 5089: insert
    tree[5781] = 'value_467048'
    reference[5781] = 'value_467048'
    assert tree.invariants(), "Invariants failed at step 5089"

    # Operation 5090: get
    assert tree.invariants(), "Invariants failed at step 5090"

    # Operation 5091: get
    assert tree.invariants(), "Invariants failed at step 5091"

    # Operation 5092: insert
    tree[6852] = 'value_394863'
    reference[6852] = 'value_394863'
    assert tree.invariants(), "Invariants failed at step 5092"

    # Operation 5093: insert
    tree[4503] = 'value_224306'
    reference[4503] = 'value_224306'
    assert tree.invariants(), "Invariants failed at step 5093"

    # Operation 5094: batch_delete
    keys_to_delete = [6852, 3338, 5781, 9495, 3960]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5094"

    # Operation 5095: delete
    del tree[9122]
    del reference[9122]
    assert tree.invariants(), "Invariants failed at step 5095"

    # Operation 5096: insert
    tree[7042] = 'value_214931'
    reference[7042] = 'value_214931'
    assert tree.invariants(), "Invariants failed at step 5096"

    # Operation 5097: insert
    tree[5039] = 'value_534496'
    reference[5039] = 'value_534496'
    assert tree.invariants(), "Invariants failed at step 5097"

    # Operation 5098: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5098"

    # Operation 5099: update
    tree[5972] = 'value_955768'
    reference[5972] = 'value_955768'
    assert tree.invariants(), "Invariants failed at step 5099"

    # Operation 5100: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5100"

    # Operation 5101: batch_delete
    keys_to_delete = [4560, 7042, 4275, 4934]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5101"

    # Operation 5102: insert
    tree[6692] = 'value_34969'
    reference[6692] = 'value_34969'
    assert tree.invariants(), "Invariants failed at step 5102"

    # Operation 5103: get
    assert tree.invariants(), "Invariants failed at step 5103"

    # Operation 5104: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5104"

    # Operation 5105: delete
    del tree[5039]
    del reference[5039]
    assert tree.invariants(), "Invariants failed at step 5105"

    # Operation 5106: insert
    tree[7857] = 'value_35854'
    reference[7857] = 'value_35854'
    assert tree.invariants(), "Invariants failed at step 5106"

    # Operation 5107: update
    tree[7857] = 'value_186625'
    reference[7857] = 'value_186625'
    assert tree.invariants(), "Invariants failed at step 5107"

    # Operation 5108: insert
    tree[4165] = 'value_856336'
    reference[4165] = 'value_856336'
    assert tree.invariants(), "Invariants failed at step 5108"

    # Operation 5109: delete
    del tree[5972]
    del reference[5972]
    assert tree.invariants(), "Invariants failed at step 5109"

    # Operation 5110: insert
    tree[5075] = 'value_608786'
    reference[5075] = 'value_608786'
    assert tree.invariants(), "Invariants failed at step 5110"

    # Operation 5111: batch_delete
    keys_to_delete = [593, 7857, 8384, 4503]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5111"

    # Operation 5112: delete
    del tree[5075]
    del reference[5075]
    assert tree.invariants(), "Invariants failed at step 5112"

    # Operation 5113: insert
    tree[4023] = 'value_397870'
    reference[4023] = 'value_397870'
    assert tree.invariants(), "Invariants failed at step 5113"

    # Operation 5114: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5114"

    # Operation 5115: get
    assert tree.invariants(), "Invariants failed at step 5115"

    # Operation 5116: delete
    del tree[4165]
    del reference[4165]
    assert tree.invariants(), "Invariants failed at step 5116"

    # Operation 5117: delete
    del tree[6692]
    del reference[6692]
    assert tree.invariants(), "Invariants failed at step 5117"

    # Operation 5118: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5118"

    # Operation 5119: insert
    tree[2396] = 'value_923080'
    reference[2396] = 'value_923080'
    assert tree.invariants(), "Invariants failed at step 5119"

    # Operation 5120: delete
    del tree[4023]
    del reference[4023]
    assert tree.invariants(), "Invariants failed at step 5120"

    # Operation 5121: insert
    tree[8669] = 'value_728470'
    reference[8669] = 'value_728470'
    assert tree.invariants(), "Invariants failed at step 5121"

    # Operation 5122: get
    assert tree.invariants(), "Invariants failed at step 5122"

    # Operation 5123: get
    assert tree.invariants(), "Invariants failed at step 5123"

    # Operation 5124: delete
    del tree[2396]
    del reference[2396]
    assert tree.invariants(), "Invariants failed at step 5124"

    # Operation 5125: insert
    tree[7844] = 'value_557985'
    reference[7844] = 'value_557985'
    assert tree.invariants(), "Invariants failed at step 5125"

    # Operation 5126: insert
    tree[4049] = 'value_96065'
    reference[4049] = 'value_96065'
    assert tree.invariants(), "Invariants failed at step 5126"

    # Operation 5127: insert
    tree[5685] = 'value_389914'
    reference[5685] = 'value_389914'
    assert tree.invariants(), "Invariants failed at step 5127"

    # Operation 5128: get
    assert tree.invariants(), "Invariants failed at step 5128"

    # Operation 5129: delete
    del tree[4049]
    del reference[4049]
    assert tree.invariants(), "Invariants failed at step 5129"

    # Operation 5130: get
    assert tree.invariants(), "Invariants failed at step 5130"

    # Operation 5131: get
    assert tree.invariants(), "Invariants failed at step 5131"

    # Operation 5132: delete
    del tree[5685]
    del reference[5685]
    assert tree.invariants(), "Invariants failed at step 5132"

    # Operation 5133: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5133"

    # Operation 5134: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5134"

    # Operation 5135: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5135"

    # Operation 5136: delete
    del tree[8669]
    del reference[8669]
    assert tree.invariants(), "Invariants failed at step 5136"

    # Operation 5137: get
    assert tree.invariants(), "Invariants failed at step 5137"

    # Operation 5138: update
    tree[7844] = 'value_539573'
    reference[7844] = 'value_539573'
    assert tree.invariants(), "Invariants failed at step 5138"

    # Operation 5139: insert
    tree[6248] = 'value_32328'
    reference[6248] = 'value_32328'
    assert tree.invariants(), "Invariants failed at step 5139"

    # Operation 5140: get
    assert tree.invariants(), "Invariants failed at step 5140"

    # Operation 5141: update
    tree[6248] = 'value_511859'
    reference[6248] = 'value_511859'
    assert tree.invariants(), "Invariants failed at step 5141"

    # Operation 5142: insert
    tree[9478] = 'value_490575'
    reference[9478] = 'value_490575'
    assert tree.invariants(), "Invariants failed at step 5142"

    # Operation 5143: insert
    tree[1175] = 'value_273596'
    reference[1175] = 'value_273596'
    assert tree.invariants(), "Invariants failed at step 5143"

    # Operation 5144: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5144"

    # Operation 5145: insert
    tree[7359] = 'value_749233'
    reference[7359] = 'value_749233'
    assert tree.invariants(), "Invariants failed at step 5145"

    # Operation 5146: insert
    tree[5305] = 'value_161274'
    reference[5305] = 'value_161274'
    assert tree.invariants(), "Invariants failed at step 5146"

    # Operation 5147: delete
    del tree[7844]
    del reference[7844]
    assert tree.invariants(), "Invariants failed at step 5147"

    # Operation 5148: get
    assert tree.invariants(), "Invariants failed at step 5148"

    # Operation 5149: delete
    del tree[6248]
    del reference[6248]
    assert tree.invariants(), "Invariants failed at step 5149"

    # Operation 5150: insert
    tree[4263] = 'value_779896'
    reference[4263] = 'value_779896'
    assert tree.invariants(), "Invariants failed at step 5150"

    # Operation 5151: get
    assert tree.invariants(), "Invariants failed at step 5151"

    # Operation 5152: get
    assert tree.invariants(), "Invariants failed at step 5152"

    # Operation 5153: delete
    del tree[1175]
    del reference[1175]
    assert tree.invariants(), "Invariants failed at step 5153"

    # Operation 5154: insert
    tree[7015] = 'value_491451'
    reference[7015] = 'value_491451'
    assert tree.invariants(), "Invariants failed at step 5154"

    # Operation 5155: delete
    del tree[7359]
    del reference[7359]
    assert tree.invariants(), "Invariants failed at step 5155"

    # Operation 5156: update
    tree[5305] = 'value_709240'
    reference[5305] = 'value_709240'
    assert tree.invariants(), "Invariants failed at step 5156"

    # Operation 5157: delete
    del tree[5305]
    del reference[5305]
    assert tree.invariants(), "Invariants failed at step 5157"

    # Operation 5158: insert
    tree[7819] = 'value_874875'
    reference[7819] = 'value_874875'
    assert tree.invariants(), "Invariants failed at step 5158"

    # Operation 5159: update
    tree[7819] = 'value_578774'
    reference[7819] = 'value_578774'
    assert tree.invariants(), "Invariants failed at step 5159"

    # Operation 5160: insert
    tree[9330] = 'value_578236'
    reference[9330] = 'value_578236'
    assert tree.invariants(), "Invariants failed at step 5160"

    # Operation 5161: insert
    tree[1567] = 'value_23396'
    reference[1567] = 'value_23396'
    assert tree.invariants(), "Invariants failed at step 5161"

    # Operation 5162: insert
    tree[7180] = 'value_964104'
    reference[7180] = 'value_964104'
    assert tree.invariants(), "Invariants failed at step 5162"

    # Operation 5163: delete
    del tree[7180]
    del reference[7180]
    assert tree.invariants(), "Invariants failed at step 5163"

    # Operation 5164: insert
    tree[9733] = 'value_169033'
    reference[9733] = 'value_169033'
    assert tree.invariants(), "Invariants failed at step 5164"

    # Operation 5165: insert
    tree[1270] = 'value_266037'
    reference[1270] = 'value_266037'
    assert tree.invariants(), "Invariants failed at step 5165"

    # Operation 5166: insert
    tree[3688] = 'value_889591'
    reference[3688] = 'value_889591'
    assert tree.invariants(), "Invariants failed at step 5166"

    # Operation 5167: insert
    tree[51] = 'value_539979'
    reference[51] = 'value_539979'
    assert tree.invariants(), "Invariants failed at step 5167"

    # Operation 5168: update
    tree[3688] = 'value_317851'
    reference[3688] = 'value_317851'
    assert tree.invariants(), "Invariants failed at step 5168"

    # Operation 5169: get
    assert tree.invariants(), "Invariants failed at step 5169"

    # Operation 5170: update
    tree[7015] = 'value_307813'
    reference[7015] = 'value_307813'
    assert tree.invariants(), "Invariants failed at step 5170"

    # Operation 5171: delete
    del tree[7819]
    del reference[7819]
    assert tree.invariants(), "Invariants failed at step 5171"

    # Operation 5172: update
    tree[3688] = 'value_335727'
    reference[3688] = 'value_335727'
    assert tree.invariants(), "Invariants failed at step 5172"

    # Operation 5173: get
    assert tree.invariants(), "Invariants failed at step 5173"

    # Operation 5174: get
    assert tree.invariants(), "Invariants failed at step 5174"

    # Operation 5175: delete
    del tree[7015]
    del reference[7015]
    assert tree.invariants(), "Invariants failed at step 5175"

    # Operation 5176: insert
    tree[9657] = 'value_436113'
    reference[9657] = 'value_436113'
    assert tree.invariants(), "Invariants failed at step 5176"

    # Operation 5177: insert
    tree[1698] = 'value_521535'
    reference[1698] = 'value_521535'
    assert tree.invariants(), "Invariants failed at step 5177"

    # Operation 5178: update
    tree[9478] = 'value_289981'
    reference[9478] = 'value_289981'
    assert tree.invariants(), "Invariants failed at step 5178"

    # Operation 5179: batch_delete
    keys_to_delete = [9121, 9044, 9733, 4263]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5179"

    # Operation 5180: get
    assert tree.invariants(), "Invariants failed at step 5180"

    # Operation 5181: delete
    del tree[3688]
    del reference[3688]
    assert tree.invariants(), "Invariants failed at step 5181"

    # Operation 5182: delete
    del tree[1567]
    del reference[1567]
    assert tree.invariants(), "Invariants failed at step 5182"

    # Operation 5183: insert
    tree[6888] = 'value_702625'
    reference[6888] = 'value_702625'
    assert tree.invariants(), "Invariants failed at step 5183"

    # Operation 5184: insert
    tree[3679] = 'value_915527'
    reference[3679] = 'value_915527'
    assert tree.invariants(), "Invariants failed at step 5184"

    # Operation 5185: insert
    tree[8743] = 'value_236619'
    reference[8743] = 'value_236619'
    assert tree.invariants(), "Invariants failed at step 5185"

    # Operation 5186: update
    tree[1270] = 'value_509877'
    reference[1270] = 'value_509877'
    assert tree.invariants(), "Invariants failed at step 5186"

    # Operation 5187: insert
    tree[4275] = 'value_28190'
    reference[4275] = 'value_28190'
    assert tree.invariants(), "Invariants failed at step 5187"

    # Operation 5188: insert
    tree[8007] = 'value_43325'
    reference[8007] = 'value_43325'
    assert tree.invariants(), "Invariants failed at step 5188"

    # Operation 5189: delete
    del tree[4275]
    del reference[4275]
    assert tree.invariants(), "Invariants failed at step 5189"

    # Operation 5190: delete
    del tree[9478]
    del reference[9478]
    assert tree.invariants(), "Invariants failed at step 5190"

    # Operation 5191: insert
    tree[8198] = 'value_853252'
    reference[8198] = 'value_853252'
    assert tree.invariants(), "Invariants failed at step 5191"

    # Operation 5192: insert
    tree[7163] = 'value_369591'
    reference[7163] = 'value_369591'
    assert tree.invariants(), "Invariants failed at step 5192"

    # Operation 5193: get
    assert tree.invariants(), "Invariants failed at step 5193"

    # Operation 5194: insert
    tree[1042] = 'value_620251'
    reference[1042] = 'value_620251'
    assert tree.invariants(), "Invariants failed at step 5194"

    # Operation 5195: get
    assert tree.invariants(), "Invariants failed at step 5195"

    # Operation 5196: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5196"

    # Operation 5197: delete
    del tree[1698]
    del reference[1698]
    assert tree.invariants(), "Invariants failed at step 5197"

    # Operation 5198: update
    tree[1042] = 'value_19617'
    reference[1042] = 'value_19617'
    assert tree.invariants(), "Invariants failed at step 5198"

    # Operation 5199: insert
    tree[8574] = 'value_386363'
    reference[8574] = 'value_386363'
    assert tree.invariants(), "Invariants failed at step 5199"

    # Operation 5200: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5200"

    # Operation 5201: get
    assert tree.invariants(), "Invariants failed at step 5201"

    # Operation 5202: insert
    tree[5074] = 'value_841583'
    reference[5074] = 'value_841583'
    assert tree.invariants(), "Invariants failed at step 5202"

    # Operation 5203: delete
    del tree[9330]
    del reference[9330]
    assert tree.invariants(), "Invariants failed at step 5203"

    # Operation 5204: delete
    del tree[51]
    del reference[51]
    assert tree.invariants(), "Invariants failed at step 5204"

    # Operation 5205: update
    tree[3679] = 'value_371302'
    reference[3679] = 'value_371302'
    assert tree.invariants(), "Invariants failed at step 5205"

    # Operation 5206: insert
    tree[1242] = 'value_504187'
    reference[1242] = 'value_504187'
    assert tree.invariants(), "Invariants failed at step 5206"

    # Operation 5207: get
    assert tree.invariants(), "Invariants failed at step 5207"

    # Operation 5208: delete
    del tree[6888]
    del reference[6888]
    assert tree.invariants(), "Invariants failed at step 5208"

    # Operation 5209: insert
    tree[1501] = 'value_757145'
    reference[1501] = 'value_757145'
    assert tree.invariants(), "Invariants failed at step 5209"

    # Operation 5210: insert
    tree[2656] = 'value_531885'
    reference[2656] = 'value_531885'
    assert tree.invariants(), "Invariants failed at step 5210"

    # Operation 5211: delete
    del tree[3679]
    del reference[3679]
    assert tree.invariants(), "Invariants failed at step 5211"

    # Operation 5212: get
    assert tree.invariants(), "Invariants failed at step 5212"

    # Operation 5213: delete
    del tree[1242]
    del reference[1242]
    assert tree.invariants(), "Invariants failed at step 5213"

    # Operation 5214: get
    assert tree.invariants(), "Invariants failed at step 5214"

    # Operation 5215: delete
    del tree[8007]
    del reference[8007]
    assert tree.invariants(), "Invariants failed at step 5215"

    # Operation 5216: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5216"

    # Operation 5217: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5217"

    # Operation 5218: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5218"

    # Operation 5219: delete
    del tree[2656]
    del reference[2656]
    assert tree.invariants(), "Invariants failed at step 5219"

    # Operation 5220: batch_delete
    keys_to_delete = [8198, 5159, 3860, 1270, 1501, 8574]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5220"

    # Operation 5221: delete
    del tree[5074]
    del reference[5074]
    assert tree.invariants(), "Invariants failed at step 5221"

    # Operation 5222: delete
    del tree[1042]
    del reference[1042]
    assert tree.invariants(), "Invariants failed at step 5222"

    # Operation 5223: delete
    del tree[7163]
    del reference[7163]
    assert tree.invariants(), "Invariants failed at step 5223"

    # Operation 5224: insert
    tree[9408] = 'value_765598'
    reference[9408] = 'value_765598'
    assert tree.invariants(), "Invariants failed at step 5224"

    # Operation 5225: update
    tree[9657] = 'value_306249'
    reference[9657] = 'value_306249'
    assert tree.invariants(), "Invariants failed at step 5225"

    # Operation 5226: get
    assert tree.invariants(), "Invariants failed at step 5226"

    # Operation 5227: get
    assert tree.invariants(), "Invariants failed at step 5227"

    # Operation 5228: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5228"

    # Operation 5229: delete
    del tree[8743]
    del reference[8743]
    assert tree.invariants(), "Invariants failed at step 5229"

    # Operation 5230: update
    tree[9657] = 'value_742134'
    reference[9657] = 'value_742134'
    assert tree.invariants(), "Invariants failed at step 5230"

    # Operation 5231: delete
    del tree[9657]
    del reference[9657]
    assert tree.invariants(), "Invariants failed at step 5231"

    # Operation 5232: update
    tree[9408] = 'value_93880'
    reference[9408] = 'value_93880'
    assert tree.invariants(), "Invariants failed at step 5232"

    # Operation 5233: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5233"

    # Operation 5234: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5234"

    # Operation 5235: insert
    tree[6580] = 'value_930134'
    reference[6580] = 'value_930134'
    assert tree.invariants(), "Invariants failed at step 5235"

    # Operation 5236: delete
    del tree[9408]
    del reference[9408]
    assert tree.invariants(), "Invariants failed at step 5236"

    # Operation 5237: update
    tree[6580] = 'value_126106'
    reference[6580] = 'value_126106'
    assert tree.invariants(), "Invariants failed at step 5237"

    # Operation 5238: insert
    tree[371] = 'value_148390'
    reference[371] = 'value_148390'
    assert tree.invariants(), "Invariants failed at step 5238"

    # Operation 5239: insert
    tree[1944] = 'value_332142'
    reference[1944] = 'value_332142'
    assert tree.invariants(), "Invariants failed at step 5239"

    # Operation 5240: insert
    tree[289] = 'value_274922'
    reference[289] = 'value_274922'
    assert tree.invariants(), "Invariants failed at step 5240"

    # Operation 5241: insert
    tree[2937] = 'value_256956'
    reference[2937] = 'value_256956'
    assert tree.invariants(), "Invariants failed at step 5241"

    # Operation 5242: insert
    tree[1305] = 'value_475055'
    reference[1305] = 'value_475055'
    assert tree.invariants(), "Invariants failed at step 5242"

    # Operation 5243: update
    tree[2937] = 'value_858486'
    reference[2937] = 'value_858486'
    assert tree.invariants(), "Invariants failed at step 5243"

    # Operation 5244: update
    tree[6580] = 'value_205991'
    reference[6580] = 'value_205991'
    assert tree.invariants(), "Invariants failed at step 5244"

    # Operation 5245: get
    assert tree.invariants(), "Invariants failed at step 5245"

    # Operation 5246: update
    tree[6580] = 'value_851588'
    reference[6580] = 'value_851588'
    assert tree.invariants(), "Invariants failed at step 5246"

    # Operation 5247: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5247"

    # Operation 5248: get
    assert tree.invariants(), "Invariants failed at step 5248"

    # Operation 5249: batch_delete
    keys_to_delete = [9768, 6580, 1944, 1305, 5498]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5249"

    # Operation 5250: insert
    tree[5602] = 'value_715015'
    reference[5602] = 'value_715015'
    assert tree.invariants(), "Invariants failed at step 5250"

    # Operation 5251: insert
    tree[9867] = 'value_500473'
    reference[9867] = 'value_500473'
    assert tree.invariants(), "Invariants failed at step 5251"

    # Operation 5252: insert
    tree[7677] = 'value_287466'
    reference[7677] = 'value_287466'
    assert tree.invariants(), "Invariants failed at step 5252"

    # Operation 5253: delete
    del tree[289]
    del reference[289]
    assert tree.invariants(), "Invariants failed at step 5253"

    # Operation 5254: delete
    del tree[2937]
    del reference[2937]
    assert tree.invariants(), "Invariants failed at step 5254"

    # Operation 5255: delete
    del tree[371]
    del reference[371]
    assert tree.invariants(), "Invariants failed at step 5255"

    # Operation 5256: get
    assert tree.invariants(), "Invariants failed at step 5256"

    # Operation 5257: update
    tree[5602] = 'value_874322'
    reference[5602] = 'value_874322'
    assert tree.invariants(), "Invariants failed at step 5257"

    # Operation 5258: get
    assert tree.invariants(), "Invariants failed at step 5258"

    # Operation 5259: insert
    tree[4403] = 'value_147296'
    reference[4403] = 'value_147296'
    assert tree.invariants(), "Invariants failed at step 5259"

    # Operation 5260: delete
    del tree[4403]
    del reference[4403]
    assert tree.invariants(), "Invariants failed at step 5260"

    # Operation 5261: insert
    tree[8572] = 'value_316874'
    reference[8572] = 'value_316874'
    assert tree.invariants(), "Invariants failed at step 5261"

    # Operation 5262: update
    tree[8572] = 'value_171593'
    reference[8572] = 'value_171593'
    assert tree.invariants(), "Invariants failed at step 5262"

    # Operation 5263: delete
    del tree[7677]
    del reference[7677]
    assert tree.invariants(), "Invariants failed at step 5263"

    # Operation 5264: insert
    tree[6355] = 'value_836673'
    reference[6355] = 'value_836673'
    assert tree.invariants(), "Invariants failed at step 5264"

    # Operation 5265: update
    tree[8572] = 'value_539175'
    reference[8572] = 'value_539175'
    assert tree.invariants(), "Invariants failed at step 5265"

    # Operation 5266: get
    assert tree.invariants(), "Invariants failed at step 5266"

    # Operation 5267: get
    assert tree.invariants(), "Invariants failed at step 5267"

    # Operation 5268: insert
    tree[1753] = 'value_619406'
    reference[1753] = 'value_619406'
    assert tree.invariants(), "Invariants failed at step 5268"

    # Operation 5269: batch_delete
    keys_to_delete = [1753, 6355, 9157, 9787]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5269"

    # Operation 5270: insert
    tree[2878] = 'value_486217'
    reference[2878] = 'value_486217'
    assert tree.invariants(), "Invariants failed at step 5270"

    # Operation 5271: insert
    tree[7561] = 'value_334133'
    reference[7561] = 'value_334133'
    assert tree.invariants(), "Invariants failed at step 5271"

    # Operation 5272: insert
    tree[9005] = 'value_632838'
    reference[9005] = 'value_632838'
    assert tree.invariants(), "Invariants failed at step 5272"

    # Operation 5273: update
    tree[8572] = 'value_313893'
    reference[8572] = 'value_313893'
    assert tree.invariants(), "Invariants failed at step 5273"

    # Operation 5274: batch_delete
    keys_to_delete = [5602, 5161, 8572, 2878, 4095]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5274"

    # Operation 5275: delete
    del tree[9005]
    del reference[9005]
    assert tree.invariants(), "Invariants failed at step 5275"

    # Operation 5276: delete
    del tree[9867]
    del reference[9867]
    assert tree.invariants(), "Invariants failed at step 5276"

    # Operation 5277: insert
    tree[4488] = 'value_617168'
    reference[4488] = 'value_617168'
    assert tree.invariants(), "Invariants failed at step 5277"

    # Operation 5278: get
    assert tree.invariants(), "Invariants failed at step 5278"

    # Operation 5279: delete
    del tree[7561]
    del reference[7561]
    assert tree.invariants(), "Invariants failed at step 5279"

    # Operation 5280: insert
    tree[4551] = 'value_541702'
    reference[4551] = 'value_541702'
    assert tree.invariants(), "Invariants failed at step 5280"

    # Operation 5281: insert
    tree[4085] = 'value_477893'
    reference[4085] = 'value_477893'
    assert tree.invariants(), "Invariants failed at step 5281"

    # Operation 5282: get
    assert tree.invariants(), "Invariants failed at step 5282"

    # Operation 5283: insert
    tree[4434] = 'value_322608'
    reference[4434] = 'value_322608'
    assert tree.invariants(), "Invariants failed at step 5283"

    # Operation 5284: get
    assert tree.invariants(), "Invariants failed at step 5284"

    # Operation 5285: insert
    tree[9563] = 'value_634027'
    reference[9563] = 'value_634027'
    assert tree.invariants(), "Invariants failed at step 5285"

    # Operation 5286: batch_delete
    keys_to_delete = [4488, 3438, 4085, 8518]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5286"

    # Operation 5287: insert
    tree[3045] = 'value_723202'
    reference[3045] = 'value_723202'
    assert tree.invariants(), "Invariants failed at step 5287"

    # Operation 5288: get
    assert tree.invariants(), "Invariants failed at step 5288"

    # Operation 5289: get
    assert tree.invariants(), "Invariants failed at step 5289"

    # Operation 5290: update
    tree[4551] = 'value_21573'
    reference[4551] = 'value_21573'
    assert tree.invariants(), "Invariants failed at step 5290"

    # Operation 5291: delete
    del tree[4434]
    del reference[4434]
    assert tree.invariants(), "Invariants failed at step 5291"

    # Operation 5292: update
    tree[3045] = 'value_187810'
    reference[3045] = 'value_187810'
    assert tree.invariants(), "Invariants failed at step 5292"

    # Operation 5293: update
    tree[9563] = 'value_52633'
    reference[9563] = 'value_52633'
    assert tree.invariants(), "Invariants failed at step 5293"

    # Operation 5294: insert
    tree[9996] = 'value_862831'
    reference[9996] = 'value_862831'
    assert tree.invariants(), "Invariants failed at step 5294"

    # Operation 5295: get
    assert tree.invariants(), "Invariants failed at step 5295"

    # Operation 5296: delete
    del tree[9996]
    del reference[9996]
    assert tree.invariants(), "Invariants failed at step 5296"

    # Operation 5297: insert
    tree[1038] = 'value_121573'
    reference[1038] = 'value_121573'
    assert tree.invariants(), "Invariants failed at step 5297"

    # Operation 5298: delete
    del tree[4551]
    del reference[4551]
    assert tree.invariants(), "Invariants failed at step 5298"

    # Operation 5299: get
    assert tree.invariants(), "Invariants failed at step 5299"

    # Operation 5300: delete
    del tree[1038]
    del reference[1038]
    assert tree.invariants(), "Invariants failed at step 5300"

    # Operation 5301: delete
    del tree[9563]
    del reference[9563]
    assert tree.invariants(), "Invariants failed at step 5301"

    # Operation 5302: insert
    tree[153] = 'value_82852'
    reference[153] = 'value_82852'
    assert tree.invariants(), "Invariants failed at step 5302"

    # Operation 5303: delete
    del tree[153]
    del reference[153]
    assert tree.invariants(), "Invariants failed at step 5303"

    # Operation 5304: insert
    tree[7432] = 'value_979830'
    reference[7432] = 'value_979830'
    assert tree.invariants(), "Invariants failed at step 5304"

    # Operation 5305: get
    assert tree.invariants(), "Invariants failed at step 5305"

    # Operation 5306: delete
    del tree[7432]
    del reference[7432]
    assert tree.invariants(), "Invariants failed at step 5306"

    # Operation 5307: get
    assert tree.invariants(), "Invariants failed at step 5307"

    # Operation 5308: insert
    tree[1199] = 'value_803702'
    reference[1199] = 'value_803702'
    assert tree.invariants(), "Invariants failed at step 5308"

    # Operation 5309: insert
    tree[3371] = 'value_482494'
    reference[3371] = 'value_482494'
    assert tree.invariants(), "Invariants failed at step 5309"

    # Operation 5310: insert
    tree[4909] = 'value_158128'
    reference[4909] = 'value_158128'
    assert tree.invariants(), "Invariants failed at step 5310"

    # Operation 5311: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5311"

    # Operation 5312: get
    assert tree.invariants(), "Invariants failed at step 5312"

    # Operation 5313: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5313"

    # Operation 5314: delete
    del tree[3045]
    del reference[3045]
    assert tree.invariants(), "Invariants failed at step 5314"

    # Operation 5315: get
    assert tree.invariants(), "Invariants failed at step 5315"

    # Operation 5316: delete
    del tree[3371]
    del reference[3371]
    assert tree.invariants(), "Invariants failed at step 5316"

    # Operation 5317: delete
    del tree[4909]
    del reference[4909]
    assert tree.invariants(), "Invariants failed at step 5317"

    # Operation 5318: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5318"

    # Operation 5319: update
    tree[1199] = 'value_316209'
    reference[1199] = 'value_316209'
    assert tree.invariants(), "Invariants failed at step 5319"

    # Operation 5320: get
    assert tree.invariants(), "Invariants failed at step 5320"

    # Operation 5321: insert
    tree[2177] = 'value_406630'
    reference[2177] = 'value_406630'
    assert tree.invariants(), "Invariants failed at step 5321"

    # Operation 5322: insert
    tree[7540] = 'value_467928'
    reference[7540] = 'value_467928'
    assert tree.invariants(), "Invariants failed at step 5322"

    # Operation 5323: get
    assert tree.invariants(), "Invariants failed at step 5323"

    # Operation 5324: delete
    del tree[2177]
    del reference[2177]
    assert tree.invariants(), "Invariants failed at step 5324"

    # Operation 5325: delete
    del tree[1199]
    del reference[1199]
    assert tree.invariants(), "Invariants failed at step 5325"

    # Operation 5326: insert
    tree[1258] = 'value_511695'
    reference[1258] = 'value_511695'
    assert tree.invariants(), "Invariants failed at step 5326"

    # Operation 5327: delete
    del tree[1258]
    del reference[1258]
    assert tree.invariants(), "Invariants failed at step 5327"

    # Operation 5328: get
    assert tree.invariants(), "Invariants failed at step 5328"

    # Operation 5329: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5329"

    # Operation 5330: delete
    del tree[7540]
    del reference[7540]
    assert tree.invariants(), "Invariants failed at step 5330"

    # Operation 5331: insert
    tree[9665] = 'value_607559'
    reference[9665] = 'value_607559'
    assert tree.invariants(), "Invariants failed at step 5331"

    # Operation 5332: update
    tree[9665] = 'value_420792'
    reference[9665] = 'value_420792'
    assert tree.invariants(), "Invariants failed at step 5332"

    # Operation 5333: delete
    del tree[9665]
    del reference[9665]
    assert tree.invariants(), "Invariants failed at step 5333"

    # Operation 5334: get
    assert tree.invariants(), "Invariants failed at step 5334"

    # Operation 5335: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5335"

    # Operation 5336: insert
    tree[8378] = 'value_118456'
    reference[8378] = 'value_118456'
    assert tree.invariants(), "Invariants failed at step 5336"

    # Operation 5337: delete
    del tree[8378]
    del reference[8378]
    assert tree.invariants(), "Invariants failed at step 5337"

    # Operation 5338: insert
    tree[8132] = 'value_156255'
    reference[8132] = 'value_156255'
    assert tree.invariants(), "Invariants failed at step 5338"

    # Operation 5339: update
    tree[8132] = 'value_424391'
    reference[8132] = 'value_424391'
    assert tree.invariants(), "Invariants failed at step 5339"

    # Operation 5340: insert
    tree[8996] = 'value_568144'
    reference[8996] = 'value_568144'
    assert tree.invariants(), "Invariants failed at step 5340"

    # Operation 5341: insert
    tree[9948] = 'value_678956'
    reference[9948] = 'value_678956'
    assert tree.invariants(), "Invariants failed at step 5341"

    # Operation 5342: update
    tree[9948] = 'value_302019'
    reference[9948] = 'value_302019'
    assert tree.invariants(), "Invariants failed at step 5342"

    # Operation 5343: get
    assert tree.invariants(), "Invariants failed at step 5343"

    # Operation 5344: delete
    del tree[9948]
    del reference[9948]
    assert tree.invariants(), "Invariants failed at step 5344"

    # Operation 5345: insert
    tree[6477] = 'value_230801'
    reference[6477] = 'value_230801'
    assert tree.invariants(), "Invariants failed at step 5345"

    # Operation 5346: delete
    del tree[8996]
    del reference[8996]
    assert tree.invariants(), "Invariants failed at step 5346"

    # Operation 5347: delete
    del tree[8132]
    del reference[8132]
    assert tree.invariants(), "Invariants failed at step 5347"

    # Operation 5348: get
    assert tree.invariants(), "Invariants failed at step 5348"

    # Operation 5349: get
    assert tree.invariants(), "Invariants failed at step 5349"

    # Operation 5350: delete
    del tree[6477]
    del reference[6477]
    assert tree.invariants(), "Invariants failed at step 5350"

    # Operation 5351: insert
    tree[2735] = 'value_257047'
    reference[2735] = 'value_257047'
    assert tree.invariants(), "Invariants failed at step 5351"

    # Operation 5352: insert
    tree[6122] = 'value_124140'
    reference[6122] = 'value_124140'
    assert tree.invariants(), "Invariants failed at step 5352"

    # Operation 5353: insert
    tree[8213] = 'value_548380'
    reference[8213] = 'value_548380'
    assert tree.invariants(), "Invariants failed at step 5353"

    # Operation 5354: insert
    tree[4356] = 'value_919313'
    reference[4356] = 'value_919313'
    assert tree.invariants(), "Invariants failed at step 5354"

    # Operation 5355: delete
    del tree[4356]
    del reference[4356]
    assert tree.invariants(), "Invariants failed at step 5355"

    # Operation 5356: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5356"

    # Operation 5357: insert
    tree[4399] = 'value_845654'
    reference[4399] = 'value_845654'
    assert tree.invariants(), "Invariants failed at step 5357"

    # Operation 5358: delete
    del tree[8213]
    del reference[8213]
    assert tree.invariants(), "Invariants failed at step 5358"

    # Operation 5359: insert
    tree[8783] = 'value_306889'
    reference[8783] = 'value_306889'
    assert tree.invariants(), "Invariants failed at step 5359"

    # Operation 5360: update
    tree[6122] = 'value_281930'
    reference[6122] = 'value_281930'
    assert tree.invariants(), "Invariants failed at step 5360"

    # Operation 5361: insert
    tree[361] = 'value_553030'
    reference[361] = 'value_553030'
    assert tree.invariants(), "Invariants failed at step 5361"

    # Operation 5362: update
    tree[361] = 'value_466525'
    reference[361] = 'value_466525'
    assert tree.invariants(), "Invariants failed at step 5362"

    # Operation 5363: delete
    del tree[6122]
    del reference[6122]
    assert tree.invariants(), "Invariants failed at step 5363"

    # Operation 5364: update
    tree[2735] = 'value_172957'
    reference[2735] = 'value_172957'
    assert tree.invariants(), "Invariants failed at step 5364"

    # Operation 5365: get
    assert tree.invariants(), "Invariants failed at step 5365"

    # Operation 5366: update
    tree[4399] = 'value_326778'
    reference[4399] = 'value_326778'
    assert tree.invariants(), "Invariants failed at step 5366"

    # Operation 5367: get
    assert tree.invariants(), "Invariants failed at step 5367"

    # Operation 5368: insert
    tree[2722] = 'value_853524'
    reference[2722] = 'value_853524'
    assert tree.invariants(), "Invariants failed at step 5368"

    # Operation 5369: insert
    tree[6183] = 'value_967471'
    reference[6183] = 'value_967471'
    assert tree.invariants(), "Invariants failed at step 5369"

    # Operation 5370: insert
    tree[5412] = 'value_195768'
    reference[5412] = 'value_195768'
    assert tree.invariants(), "Invariants failed at step 5370"

    # Operation 5371: batch_delete
    keys_to_delete = [2722, 5412, 361, 1171, 5498]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5371"

    # Operation 5372: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5372"

    # Operation 5373: update
    tree[6183] = 'value_299678'
    reference[6183] = 'value_299678'
    assert tree.invariants(), "Invariants failed at step 5373"

    # Operation 5374: delete
    del tree[8783]
    del reference[8783]
    assert tree.invariants(), "Invariants failed at step 5374"

    # Operation 5375: delete
    del tree[2735]
    del reference[2735]
    assert tree.invariants(), "Invariants failed at step 5375"

    # Operation 5376: get
    assert tree.invariants(), "Invariants failed at step 5376"

    # Operation 5377: delete
    del tree[4399]
    del reference[4399]
    assert tree.invariants(), "Invariants failed at step 5377"

    # Operation 5378: get
    assert tree.invariants(), "Invariants failed at step 5378"

    # Operation 5379: get
    assert tree.invariants(), "Invariants failed at step 5379"

    # Operation 5380: insert
    tree[1368] = 'value_672739'
    reference[1368] = 'value_672739'
    assert tree.invariants(), "Invariants failed at step 5380"

    # Operation 5381: insert
    tree[9790] = 'value_875963'
    reference[9790] = 'value_875963'
    assert tree.invariants(), "Invariants failed at step 5381"

    # Operation 5382: insert
    tree[6022] = 'value_968957'
    reference[6022] = 'value_968957'
    assert tree.invariants(), "Invariants failed at step 5382"

    # Operation 5383: insert
    tree[7416] = 'value_27342'
    reference[7416] = 'value_27342'
    assert tree.invariants(), "Invariants failed at step 5383"

    # Operation 5384: insert
    tree[6002] = 'value_570322'
    reference[6002] = 'value_570322'
    assert tree.invariants(), "Invariants failed at step 5384"

    # Operation 5385: insert
    tree[1710] = 'value_316624'
    reference[1710] = 'value_316624'
    assert tree.invariants(), "Invariants failed at step 5385"

    # Operation 5386: insert
    tree[3610] = 'value_500218'
    reference[3610] = 'value_500218'
    assert tree.invariants(), "Invariants failed at step 5386"

    # Operation 5387: insert
    tree[7212] = 'value_547866'
    reference[7212] = 'value_547866'
    assert tree.invariants(), "Invariants failed at step 5387"

    # Operation 5388: insert
    tree[4586] = 'value_386393'
    reference[4586] = 'value_386393'
    assert tree.invariants(), "Invariants failed at step 5388"

    # Operation 5389: insert
    tree[992] = 'value_490062'
    reference[992] = 'value_490062'
    assert tree.invariants(), "Invariants failed at step 5389"

    # Operation 5390: delete
    del tree[6183]
    del reference[6183]
    assert tree.invariants(), "Invariants failed at step 5390"

    # Operation 5391: batch_delete
    keys_to_delete = [3499, 7212, 1368, 3610, 7352]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5391"

    # Operation 5392: get
    assert tree.invariants(), "Invariants failed at step 5392"

    # Operation 5393: insert
    tree[8017] = 'value_564023'
    reference[8017] = 'value_564023'
    assert tree.invariants(), "Invariants failed at step 5393"

    # Operation 5394: update
    tree[992] = 'value_719290'
    reference[992] = 'value_719290'
    assert tree.invariants(), "Invariants failed at step 5394"

    # Operation 5395: get
    assert tree.invariants(), "Invariants failed at step 5395"

    # Operation 5396: delete
    del tree[8017]
    del reference[8017]
    assert tree.invariants(), "Invariants failed at step 5396"

    # Operation 5397: insert
    tree[5551] = 'value_377915'
    reference[5551] = 'value_377915'
    assert tree.invariants(), "Invariants failed at step 5397"

    # Operation 5398: delete
    del tree[1710]
    del reference[1710]
    assert tree.invariants(), "Invariants failed at step 5398"

    # Operation 5399: insert
    tree[7319] = 'value_623554'
    reference[7319] = 'value_623554'
    assert tree.invariants(), "Invariants failed at step 5399"

    # Operation 5400: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5400"

    # Operation 5401: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5401"

    # Operation 5402: get
    assert tree.invariants(), "Invariants failed at step 5402"

    # Operation 5403: update
    tree[7319] = 'value_284910'
    reference[7319] = 'value_284910'
    assert tree.invariants(), "Invariants failed at step 5403"

    # Operation 5404: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5404"

    # Operation 5405: batch_delete
    keys_to_delete = [992, 7937, 4586, 6002, 7416, 9947]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5405"

    # Operation 5406: get
    assert tree.invariants(), "Invariants failed at step 5406"

    # Operation 5407: get
    assert tree.invariants(), "Invariants failed at step 5407"

    # Operation 5408: insert
    tree[2368] = 'value_630815'
    reference[2368] = 'value_630815'
    assert tree.invariants(), "Invariants failed at step 5408"

    # Operation 5409: batch_delete
    keys_to_delete = [7319, 5361, 4291, 5551]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5409"

    # Operation 5410: get
    assert tree.invariants(), "Invariants failed at step 5410"

    # Operation 5411: delete
    del tree[6022]
    del reference[6022]
    assert tree.invariants(), "Invariants failed at step 5411"

    # Operation 5412: insert
    tree[1715] = 'value_365934'
    reference[1715] = 'value_365934'
    assert tree.invariants(), "Invariants failed at step 5412"

    # Operation 5413: update
    tree[9790] = 'value_386593'
    reference[9790] = 'value_386593'
    assert tree.invariants(), "Invariants failed at step 5413"

    # Operation 5414: insert
    tree[1279] = 'value_507805'
    reference[1279] = 'value_507805'
    assert tree.invariants(), "Invariants failed at step 5414"

    # Operation 5415: insert
    tree[3969] = 'value_47273'
    reference[3969] = 'value_47273'
    assert tree.invariants(), "Invariants failed at step 5415"

    # Operation 5416: batch_delete
    keys_to_delete = [2368, 9183, 9530, 1279]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5416"

    # Operation 5417: insert
    tree[4736] = 'value_138084'
    reference[4736] = 'value_138084'
    assert tree.invariants(), "Invariants failed at step 5417"

    # Operation 5418: get
    assert tree.invariants(), "Invariants failed at step 5418"

    # Operation 5419: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5419"

    # Operation 5420: insert
    tree[1602] = 'value_622239'
    reference[1602] = 'value_622239'
    assert tree.invariants(), "Invariants failed at step 5420"

    # Operation 5421: get
    assert tree.invariants(), "Invariants failed at step 5421"

    # Operation 5422: update
    tree[4736] = 'value_967091'
    reference[4736] = 'value_967091'
    assert tree.invariants(), "Invariants failed at step 5422"

    # Operation 5423: batch_delete
    keys_to_delete = [8369, 1715, 7325, 9790]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5423"

    # Operation 5424: delete
    del tree[4736]
    del reference[4736]
    assert tree.invariants(), "Invariants failed at step 5424"

    # Operation 5425: insert
    tree[1886] = 'value_835685'
    reference[1886] = 'value_835685'
    assert tree.invariants(), "Invariants failed at step 5425"

    # Operation 5426: insert
    tree[3253] = 'value_495537'
    reference[3253] = 'value_495537'
    assert tree.invariants(), "Invariants failed at step 5426"

    # Operation 5427: insert
    tree[1237] = 'value_895730'
    reference[1237] = 'value_895730'
    assert tree.invariants(), "Invariants failed at step 5427"

    # Operation 5428: get
    assert tree.invariants(), "Invariants failed at step 5428"

    # Operation 5429: delete
    del tree[3969]
    del reference[3969]
    assert tree.invariants(), "Invariants failed at step 5429"

    # Operation 5430: update
    tree[1237] = 'value_333506'
    reference[1237] = 'value_333506'
    assert tree.invariants(), "Invariants failed at step 5430"

    # Operation 5431: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5431"

    # Operation 5432: get
    assert tree.invariants(), "Invariants failed at step 5432"

    # Operation 5433: update
    tree[1237] = 'value_663531'
    reference[1237] = 'value_663531'
    assert tree.invariants(), "Invariants failed at step 5433"

    # Operation 5434: delete
    del tree[1886]
    del reference[1886]
    assert tree.invariants(), "Invariants failed at step 5434"

    # Operation 5435: insert
    tree[2335] = 'value_170343'
    reference[2335] = 'value_170343'
    assert tree.invariants(), "Invariants failed at step 5435"

    # Operation 5436: update
    tree[3253] = 'value_800200'
    reference[3253] = 'value_800200'
    assert tree.invariants(), "Invariants failed at step 5436"

    # Operation 5437: insert
    tree[5406] = 'value_127997'
    reference[5406] = 'value_127997'
    assert tree.invariants(), "Invariants failed at step 5437"

    # Operation 5438: insert
    tree[4696] = 'value_698742'
    reference[4696] = 'value_698742'
    assert tree.invariants(), "Invariants failed at step 5438"

    # Operation 5439: get
    assert tree.invariants(), "Invariants failed at step 5439"

    # Operation 5440: insert
    tree[9141] = 'value_397650'
    reference[9141] = 'value_397650'
    assert tree.invariants(), "Invariants failed at step 5440"

    # Operation 5441: update
    tree[9141] = 'value_326733'
    reference[9141] = 'value_326733'
    assert tree.invariants(), "Invariants failed at step 5441"

    # Operation 5442: delete
    del tree[1237]
    del reference[1237]
    assert tree.invariants(), "Invariants failed at step 5442"

    # Operation 5443: get
    assert tree.invariants(), "Invariants failed at step 5443"

    # Operation 5444: get
    assert tree.invariants(), "Invariants failed at step 5444"

    # Operation 5445: update
    tree[1602] = 'value_158069'
    reference[1602] = 'value_158069'
    assert tree.invariants(), "Invariants failed at step 5445"

    # Operation 5446: update
    tree[9141] = 'value_546522'
    reference[9141] = 'value_546522'
    assert tree.invariants(), "Invariants failed at step 5446"

    # Operation 5447: delete
    del tree[5406]
    del reference[5406]
    assert tree.invariants(), "Invariants failed at step 5447"

    # Operation 5448: delete
    del tree[2335]
    del reference[2335]
    assert tree.invariants(), "Invariants failed at step 5448"

    # Operation 5449: delete
    del tree[9141]
    del reference[9141]
    assert tree.invariants(), "Invariants failed at step 5449"

    # Operation 5450: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5450"

    # Operation 5451: get
    assert tree.invariants(), "Invariants failed at step 5451"

    # Operation 5452: get
    assert tree.invariants(), "Invariants failed at step 5452"

    # Operation 5453: insert
    tree[8646] = 'value_320985'
    reference[8646] = 'value_320985'
    assert tree.invariants(), "Invariants failed at step 5453"

    # Operation 5454: delete
    del tree[1602]
    del reference[1602]
    assert tree.invariants(), "Invariants failed at step 5454"

    # Operation 5455: update
    tree[3253] = 'value_981059'
    reference[3253] = 'value_981059'
    assert tree.invariants(), "Invariants failed at step 5455"

    # Operation 5456: delete
    del tree[3253]
    del reference[3253]
    assert tree.invariants(), "Invariants failed at step 5456"

    # Operation 5457: insert
    tree[4179] = 'value_938520'
    reference[4179] = 'value_938520'
    assert tree.invariants(), "Invariants failed at step 5457"

    # Operation 5458: update
    tree[4179] = 'value_685919'
    reference[4179] = 'value_685919'
    assert tree.invariants(), "Invariants failed at step 5458"

    # Operation 5459: get
    assert tree.invariants(), "Invariants failed at step 5459"

    # Operation 5460: update
    tree[4696] = 'value_566126'
    reference[4696] = 'value_566126'
    assert tree.invariants(), "Invariants failed at step 5460"

    # Operation 5461: insert
    tree[1835] = 'value_341604'
    reference[1835] = 'value_341604'
    assert tree.invariants(), "Invariants failed at step 5461"

    # Operation 5462: get
    assert tree.invariants(), "Invariants failed at step 5462"

    # Operation 5463: delete
    del tree[4179]
    del reference[4179]
    assert tree.invariants(), "Invariants failed at step 5463"

    # Operation 5464: update
    tree[1835] = 'value_50150'
    reference[1835] = 'value_50150'
    assert tree.invariants(), "Invariants failed at step 5464"

    # Operation 5465: insert
    tree[9539] = 'value_862130'
    reference[9539] = 'value_862130'
    assert tree.invariants(), "Invariants failed at step 5465"

    # Operation 5466: get
    assert tree.invariants(), "Invariants failed at step 5466"

    # Operation 5467: get
    assert tree.invariants(), "Invariants failed at step 5467"

    # Operation 5468: insert
    tree[9113] = 'value_153972'
    reference[9113] = 'value_153972'
    assert tree.invariants(), "Invariants failed at step 5468"

    # Operation 5469: insert
    tree[6343] = 'value_123883'
    reference[6343] = 'value_123883'
    assert tree.invariants(), "Invariants failed at step 5469"

    # Operation 5470: insert
    tree[8887] = 'value_681966'
    reference[8887] = 'value_681966'
    assert tree.invariants(), "Invariants failed at step 5470"

    # Operation 5471: delete
    del tree[6343]
    del reference[6343]
    assert tree.invariants(), "Invariants failed at step 5471"

    # Operation 5472: insert
    tree[1228] = 'value_806326'
    reference[1228] = 'value_806326'
    assert tree.invariants(), "Invariants failed at step 5472"

    # Operation 5473: update
    tree[1835] = 'value_743811'
    reference[1835] = 'value_743811'
    assert tree.invariants(), "Invariants failed at step 5473"

    # Operation 5474: batch_delete
    keys_to_delete = [8646, 521, 1228, 4696, 9113]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5474"

    # Operation 5475: delete
    del tree[9539]
    del reference[9539]
    assert tree.invariants(), "Invariants failed at step 5475"

    # Operation 5476: update
    tree[1835] = 'value_590679'
    reference[1835] = 'value_590679'
    assert tree.invariants(), "Invariants failed at step 5476"

    # Operation 5477: delete
    del tree[1835]
    del reference[1835]
    assert tree.invariants(), "Invariants failed at step 5477"

    # Operation 5478: insert
    tree[3843] = 'value_718685'
    reference[3843] = 'value_718685'
    assert tree.invariants(), "Invariants failed at step 5478"

    # Operation 5479: insert
    tree[7852] = 'value_908942'
    reference[7852] = 'value_908942'
    assert tree.invariants(), "Invariants failed at step 5479"

    # Operation 5480: insert
    tree[7863] = 'value_99018'
    reference[7863] = 'value_99018'
    assert tree.invariants(), "Invariants failed at step 5480"

    # Operation 5481: delete
    del tree[7863]
    del reference[7863]
    assert tree.invariants(), "Invariants failed at step 5481"

    # Operation 5482: delete
    del tree[3843]
    del reference[3843]
    assert tree.invariants(), "Invariants failed at step 5482"

    # Operation 5483: get
    assert tree.invariants(), "Invariants failed at step 5483"

    # Operation 5484: update
    tree[8887] = 'value_851650'
    reference[8887] = 'value_851650'
    assert tree.invariants(), "Invariants failed at step 5484"

    # Operation 5485: get
    assert tree.invariants(), "Invariants failed at step 5485"

    # Operation 5486: delete
    del tree[8887]
    del reference[8887]
    assert tree.invariants(), "Invariants failed at step 5486"

    # Operation 5487: get
    assert tree.invariants(), "Invariants failed at step 5487"

    # Operation 5488: get
    assert tree.invariants(), "Invariants failed at step 5488"

    # Operation 5489: insert
    tree[8234] = 'value_801985'
    reference[8234] = 'value_801985'
    assert tree.invariants(), "Invariants failed at step 5489"

    # Operation 5490: insert
    tree[2977] = 'value_993894'
    reference[2977] = 'value_993894'
    assert tree.invariants(), "Invariants failed at step 5490"

    # Operation 5491: get
    assert tree.invariants(), "Invariants failed at step 5491"

    # Operation 5492: get
    assert tree.invariants(), "Invariants failed at step 5492"

    # Operation 5493: insert
    tree[2165] = 'value_611505'
    reference[2165] = 'value_611505'
    assert tree.invariants(), "Invariants failed at step 5493"

    # Operation 5494: insert
    tree[4088] = 'value_810770'
    reference[4088] = 'value_810770'
    assert tree.invariants(), "Invariants failed at step 5494"

    # Operation 5495: update
    tree[7852] = 'value_568489'
    reference[7852] = 'value_568489'
    assert tree.invariants(), "Invariants failed at step 5495"

    # Operation 5496: update
    tree[2165] = 'value_832876'
    reference[2165] = 'value_832876'
    assert tree.invariants(), "Invariants failed at step 5496"

    # Operation 5497: update
    tree[2977] = 'value_560077'
    reference[2977] = 'value_560077'
    assert tree.invariants(), "Invariants failed at step 5497"

    # Operation 5498: insert
    tree[1953] = 'value_618358'
    reference[1953] = 'value_618358'
    assert tree.invariants(), "Invariants failed at step 5498"

    # Operation 5499: insert
    tree[4818] = 'value_577117'
    reference[4818] = 'value_577117'
    assert tree.invariants(), "Invariants failed at step 5499"

    # Operation 5500: update
    tree[1953] = 'value_565721'
    reference[1953] = 'value_565721'
    assert tree.invariants(), "Invariants failed at step 5500"

    # Operation 5501: insert
    tree[2827] = 'value_630146'
    reference[2827] = 'value_630146'
    assert tree.invariants(), "Invariants failed at step 5501"

    # Operation 5502: update
    tree[4088] = 'value_907542'
    reference[4088] = 'value_907542'
    assert tree.invariants(), "Invariants failed at step 5502"

    # Operation 5503: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5503"

    # Operation 5504: update
    tree[7852] = 'value_996524'
    reference[7852] = 'value_996524'
    assert tree.invariants(), "Invariants failed at step 5504"

    # Operation 5505: delete
    del tree[1953]
    del reference[1953]
    assert tree.invariants(), "Invariants failed at step 5505"

    # Operation 5506: insert
    tree[6539] = 'value_116421'
    reference[6539] = 'value_116421'
    assert tree.invariants(), "Invariants failed at step 5506"

    # Operation 5507: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5507"

    # Operation 5508: delete
    del tree[8234]
    del reference[8234]
    assert tree.invariants(), "Invariants failed at step 5508"

    # Operation 5509: delete
    del tree[2977]
    del reference[2977]
    assert tree.invariants(), "Invariants failed at step 5509"

    # Operation 5510: update
    tree[7852] = 'value_578252'
    reference[7852] = 'value_578252'
    assert tree.invariants(), "Invariants failed at step 5510"

    # Operation 5511: delete
    del tree[2165]
    del reference[2165]
    assert tree.invariants(), "Invariants failed at step 5511"

    # Operation 5512: get
    assert tree.invariants(), "Invariants failed at step 5512"

    # Operation 5513: delete
    del tree[4818]
    del reference[4818]
    assert tree.invariants(), "Invariants failed at step 5513"

    # Operation 5514: update
    tree[6539] = 'value_581737'
    reference[6539] = 'value_581737'
    assert tree.invariants(), "Invariants failed at step 5514"

    # Operation 5515: delete
    del tree[4088]
    del reference[4088]
    assert tree.invariants(), "Invariants failed at step 5515"

    # Operation 5516: get
    assert tree.invariants(), "Invariants failed at step 5516"

    # Operation 5517: get
    assert tree.invariants(), "Invariants failed at step 5517"

    # Operation 5518: insert
    tree[5699] = 'value_901493'
    reference[5699] = 'value_901493'
    assert tree.invariants(), "Invariants failed at step 5518"

    # Operation 5519: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5519"

    # Operation 5520: delete
    del tree[2827]
    del reference[2827]
    assert tree.invariants(), "Invariants failed at step 5520"

    # Operation 5521: insert
    tree[4113] = 'value_722384'
    reference[4113] = 'value_722384'
    assert tree.invariants(), "Invariants failed at step 5521"

    # Operation 5522: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5522"

    # Operation 5523: delete
    del tree[5699]
    del reference[5699]
    assert tree.invariants(), "Invariants failed at step 5523"

    # Operation 5524: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5524"

    # Operation 5525: delete
    del tree[4113]
    del reference[4113]
    assert tree.invariants(), "Invariants failed at step 5525"

    # Operation 5526: insert
    tree[6232] = 'value_655206'
    reference[6232] = 'value_655206'
    assert tree.invariants(), "Invariants failed at step 5526"

    # Operation 5527: delete
    del tree[6539]
    del reference[6539]
    assert tree.invariants(), "Invariants failed at step 5527"

    # Operation 5528: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5528"

    # Operation 5529: insert
    tree[553] = 'value_442115'
    reference[553] = 'value_442115'
    assert tree.invariants(), "Invariants failed at step 5529"

    # Operation 5530: update
    tree[7852] = 'value_627906'
    reference[7852] = 'value_627906'
    assert tree.invariants(), "Invariants failed at step 5530"

    # Operation 5531: delete
    del tree[7852]
    del reference[7852]
    assert tree.invariants(), "Invariants failed at step 5531"

    # Operation 5532: delete
    del tree[553]
    del reference[553]
    assert tree.invariants(), "Invariants failed at step 5532"

    # Operation 5533: insert
    tree[1812] = 'value_338460'
    reference[1812] = 'value_338460'
    assert tree.invariants(), "Invariants failed at step 5533"

    # Operation 5534: update
    tree[1812] = 'value_906711'
    reference[1812] = 'value_906711'
    assert tree.invariants(), "Invariants failed at step 5534"

    # Operation 5535: delete
    del tree[6232]
    del reference[6232]
    assert tree.invariants(), "Invariants failed at step 5535"

    # Operation 5536: get
    assert tree.invariants(), "Invariants failed at step 5536"

    # Operation 5537: insert
    tree[420] = 'value_649179'
    reference[420] = 'value_649179'
    assert tree.invariants(), "Invariants failed at step 5537"

    # Operation 5538: get
    assert tree.invariants(), "Invariants failed at step 5538"

    # Operation 5539: get
    assert tree.invariants(), "Invariants failed at step 5539"

    # Operation 5540: get
    assert tree.invariants(), "Invariants failed at step 5540"

    # Operation 5541: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5541"

    # Operation 5542: insert
    tree[2996] = 'value_733391'
    reference[2996] = 'value_733391'
    assert tree.invariants(), "Invariants failed at step 5542"

    # Operation 5543: update
    tree[2996] = 'value_955867'
    reference[2996] = 'value_955867'
    assert tree.invariants(), "Invariants failed at step 5543"

    # Operation 5544: get
    assert tree.invariants(), "Invariants failed at step 5544"

    # Operation 5545: delete
    del tree[2996]
    del reference[2996]
    assert tree.invariants(), "Invariants failed at step 5545"

    # Operation 5546: insert
    tree[8861] = 'value_34554'
    reference[8861] = 'value_34554'
    assert tree.invariants(), "Invariants failed at step 5546"

    # Operation 5547: insert
    tree[8913] = 'value_346755'
    reference[8913] = 'value_346755'
    assert tree.invariants(), "Invariants failed at step 5547"

    # Operation 5548: insert
    tree[1582] = 'value_222658'
    reference[1582] = 'value_222658'
    assert tree.invariants(), "Invariants failed at step 5548"

    # Operation 5549: update
    tree[1582] = 'value_613203'
    reference[1582] = 'value_613203'
    assert tree.invariants(), "Invariants failed at step 5549"

    # Operation 5550: insert
    tree[7985] = 'value_35853'
    reference[7985] = 'value_35853'
    assert tree.invariants(), "Invariants failed at step 5550"

    # Operation 5551: insert
    tree[6285] = 'value_74045'
    reference[6285] = 'value_74045'
    assert tree.invariants(), "Invariants failed at step 5551"

    # Operation 5552: delete
    del tree[8861]
    del reference[8861]
    assert tree.invariants(), "Invariants failed at step 5552"

    # Operation 5553: insert
    tree[2331] = 'value_663404'
    reference[2331] = 'value_663404'
    assert tree.invariants(), "Invariants failed at step 5553"

    # Operation 5554: get
    assert tree.invariants(), "Invariants failed at step 5554"

    # Operation 5555: insert
    tree[9311] = 'value_803023'
    reference[9311] = 'value_803023'
    assert tree.invariants(), "Invariants failed at step 5555"

    # Operation 5556: batch_delete
    keys_to_delete = [420, 8458, 6285, 8913, 2616, 2331]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5556"

    # Operation 5557: delete
    del tree[7985]
    del reference[7985]
    assert tree.invariants(), "Invariants failed at step 5557"

    # Operation 5558: update
    tree[9311] = 'value_894830'
    reference[9311] = 'value_894830'
    assert tree.invariants(), "Invariants failed at step 5558"

    # Operation 5559: insert
    tree[661] = 'value_782546'
    reference[661] = 'value_782546'
    assert tree.invariants(), "Invariants failed at step 5559"

    # Operation 5560: delete
    del tree[1582]
    del reference[1582]
    assert tree.invariants(), "Invariants failed at step 5560"

    # Operation 5561: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5561"

    # Operation 5562: update
    tree[1812] = 'value_438993'
    reference[1812] = 'value_438993'
    assert tree.invariants(), "Invariants failed at step 5562"

    # Operation 5563: delete
    del tree[9311]
    del reference[9311]
    assert tree.invariants(), "Invariants failed at step 5563"

    # Operation 5564: delete
    del tree[1812]
    del reference[1812]
    assert tree.invariants(), "Invariants failed at step 5564"

    # Operation 5565: insert
    tree[4386] = 'value_221441'
    reference[4386] = 'value_221441'
    assert tree.invariants(), "Invariants failed at step 5565"

    # Operation 5566: get
    assert tree.invariants(), "Invariants failed at step 5566"

    # Operation 5567: insert
    tree[3654] = 'value_18554'
    reference[3654] = 'value_18554'
    assert tree.invariants(), "Invariants failed at step 5567"

    # Operation 5568: delete
    del tree[661]
    del reference[661]
    assert tree.invariants(), "Invariants failed at step 5568"

    # Operation 5569: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5569"

    # Operation 5570: insert
    tree[4719] = 'value_93278'
    reference[4719] = 'value_93278'
    assert tree.invariants(), "Invariants failed at step 5570"

    # Operation 5571: get
    assert tree.invariants(), "Invariants failed at step 5571"

    # Operation 5572: insert
    tree[7941] = 'value_989982'
    reference[7941] = 'value_989982'
    assert tree.invariants(), "Invariants failed at step 5572"

    # Operation 5573: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5573"

    # Operation 5574: get
    assert tree.invariants(), "Invariants failed at step 5574"

    # Operation 5575: get
    assert tree.invariants(), "Invariants failed at step 5575"

    # Operation 5576: insert
    tree[682] = 'value_674109'
    reference[682] = 'value_674109'
    assert tree.invariants(), "Invariants failed at step 5576"

    # Operation 5577: get
    assert tree.invariants(), "Invariants failed at step 5577"

    # Operation 5578: insert
    tree[4947] = 'value_107675'
    reference[4947] = 'value_107675'
    assert tree.invariants(), "Invariants failed at step 5578"

    # Operation 5579: insert
    tree[1774] = 'value_301097'
    reference[1774] = 'value_301097'
    assert tree.invariants(), "Invariants failed at step 5579"

    # Operation 5580: insert
    tree[4433] = 'value_919649'
    reference[4433] = 'value_919649'
    assert tree.invariants(), "Invariants failed at step 5580"

    # Operation 5581: insert
    tree[4826] = 'value_446763'
    reference[4826] = 'value_446763'
    assert tree.invariants(), "Invariants failed at step 5581"

    # Operation 5582: get
    assert tree.invariants(), "Invariants failed at step 5582"

    # Operation 5583: delete
    del tree[3654]
    del reference[3654]
    assert tree.invariants(), "Invariants failed at step 5583"

    # Operation 5584: delete
    del tree[4719]
    del reference[4719]
    assert tree.invariants(), "Invariants failed at step 5584"

    # Operation 5585: insert
    tree[8293] = 'value_360306'
    reference[8293] = 'value_360306'
    assert tree.invariants(), "Invariants failed at step 5585"

    # Operation 5586: batch_delete
    keys_to_delete = [4386, 7941, 682, 5233, 4826, 8511]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5586"

    # Operation 5587: update
    tree[8293] = 'value_720402'
    reference[8293] = 'value_720402'
    assert tree.invariants(), "Invariants failed at step 5587"

    # Operation 5588: insert
    tree[248] = 'value_866857'
    reference[248] = 'value_866857'
    assert tree.invariants(), "Invariants failed at step 5588"

    # Operation 5589: delete
    del tree[4433]
    del reference[4433]
    assert tree.invariants(), "Invariants failed at step 5589"

    # Operation 5590: delete
    del tree[1774]
    del reference[1774]
    assert tree.invariants(), "Invariants failed at step 5590"

    # Operation 5591: get
    assert tree.invariants(), "Invariants failed at step 5591"

    # Operation 5592: delete
    del tree[4947]
    del reference[4947]
    assert tree.invariants(), "Invariants failed at step 5592"

    # Operation 5593: insert
    tree[4621] = 'value_762189'
    reference[4621] = 'value_762189'
    assert tree.invariants(), "Invariants failed at step 5593"

    # Operation 5594: insert
    tree[1528] = 'value_800020'
    reference[1528] = 'value_800020'
    assert tree.invariants(), "Invariants failed at step 5594"

    # Operation 5595: insert
    tree[1740] = 'value_634785'
    reference[1740] = 'value_634785'
    assert tree.invariants(), "Invariants failed at step 5595"

    # Operation 5596: delete
    del tree[1740]
    del reference[1740]
    assert tree.invariants(), "Invariants failed at step 5596"

    # Operation 5597: delete
    del tree[248]
    del reference[248]
    assert tree.invariants(), "Invariants failed at step 5597"

    # Operation 5598: delete
    del tree[8293]
    del reference[8293]
    assert tree.invariants(), "Invariants failed at step 5598"

    # Operation 5599: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5599"

    # Operation 5600: insert
    tree[3135] = 'value_912554'
    reference[3135] = 'value_912554'
    assert tree.invariants(), "Invariants failed at step 5600"

    # Operation 5601: insert
    tree[3620] = 'value_665490'
    reference[3620] = 'value_665490'
    assert tree.invariants(), "Invariants failed at step 5601"

    # Operation 5602: delete
    del tree[3135]
    del reference[3135]
    assert tree.invariants(), "Invariants failed at step 5602"

    # Operation 5603: update
    tree[4621] = 'value_922220'
    reference[4621] = 'value_922220'
    assert tree.invariants(), "Invariants failed at step 5603"

    # Operation 5604: insert
    tree[2932] = 'value_188874'
    reference[2932] = 'value_188874'
    assert tree.invariants(), "Invariants failed at step 5604"

    # Operation 5605: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5605"

    # Operation 5606: insert
    tree[8428] = 'value_706577'
    reference[8428] = 'value_706577'
    assert tree.invariants(), "Invariants failed at step 5606"

    # Operation 5607: insert
    tree[886] = 'value_817020'
    reference[886] = 'value_817020'
    assert tree.invariants(), "Invariants failed at step 5607"

    # Operation 5608: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5608"

    # Operation 5609: get
    assert tree.invariants(), "Invariants failed at step 5609"

    # Operation 5610: get
    assert tree.invariants(), "Invariants failed at step 5610"

    # Operation 5611: batch_delete
    keys_to_delete = [3620, 4651, 3436, 8428, 2932]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5611"

    # Operation 5612: delete
    del tree[1528]
    del reference[1528]
    assert tree.invariants(), "Invariants failed at step 5612"

    # Operation 5613: get
    assert tree.invariants(), "Invariants failed at step 5613"

    # Operation 5614: insert
    tree[2409] = 'value_654981'
    reference[2409] = 'value_654981'
    assert tree.invariants(), "Invariants failed at step 5614"

    # Operation 5615: get
    assert tree.invariants(), "Invariants failed at step 5615"

    # Operation 5616: insert
    tree[286] = 'value_434434'
    reference[286] = 'value_434434'
    assert tree.invariants(), "Invariants failed at step 5616"

    # Operation 5617: insert
    tree[5527] = 'value_962106'
    reference[5527] = 'value_962106'
    assert tree.invariants(), "Invariants failed at step 5617"

    # Operation 5618: get
    assert tree.invariants(), "Invariants failed at step 5618"

    # Operation 5619: delete
    del tree[886]
    del reference[886]
    assert tree.invariants(), "Invariants failed at step 5619"

    # Operation 5620: insert
    tree[7018] = 'value_294733'
    reference[7018] = 'value_294733'
    assert tree.invariants(), "Invariants failed at step 5620"

    # Operation 5621: update
    tree[2409] = 'value_47377'
    reference[2409] = 'value_47377'
    assert tree.invariants(), "Invariants failed at step 5621"

    # Operation 5622: get
    assert tree.invariants(), "Invariants failed at step 5622"

    # Operation 5623: delete
    del tree[7018]
    del reference[7018]
    assert tree.invariants(), "Invariants failed at step 5623"

    # Operation 5624: insert
    tree[2744] = 'value_455841'
    reference[2744] = 'value_455841'
    assert tree.invariants(), "Invariants failed at step 5624"

    # Operation 5625: get
    assert tree.invariants(), "Invariants failed at step 5625"

    # Operation 5626: get
    assert tree.invariants(), "Invariants failed at step 5626"

    # Operation 5627: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5627"

    # Operation 5628: get
    assert tree.invariants(), "Invariants failed at step 5628"

    # Operation 5629: delete
    del tree[2409]
    del reference[2409]
    assert tree.invariants(), "Invariants failed at step 5629"

    # Operation 5630: insert
    tree[5179] = 'value_634388'
    reference[5179] = 'value_634388'
    assert tree.invariants(), "Invariants failed at step 5630"

    # Operation 5631: insert
    tree[9998] = 'value_819920'
    reference[9998] = 'value_819920'
    assert tree.invariants(), "Invariants failed at step 5631"

    # Operation 5632: get
    assert tree.invariants(), "Invariants failed at step 5632"

    # Operation 5633: update
    tree[286] = 'value_934554'
    reference[286] = 'value_934554'
    assert tree.invariants(), "Invariants failed at step 5633"

    # Operation 5634: delete
    del tree[4621]
    del reference[4621]
    assert tree.invariants(), "Invariants failed at step 5634"

    # Operation 5635: delete
    del tree[5527]
    del reference[5527]
    assert tree.invariants(), "Invariants failed at step 5635"

    # Operation 5636: get
    assert tree.invariants(), "Invariants failed at step 5636"

    # Operation 5637: insert
    tree[2715] = 'value_529064'
    reference[2715] = 'value_529064'
    assert tree.invariants(), "Invariants failed at step 5637"

    # Operation 5638: delete
    del tree[2715]
    del reference[2715]
    assert tree.invariants(), "Invariants failed at step 5638"

    # Operation 5639: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5639"

    # Operation 5640: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5640"

    # Operation 5641: get
    assert tree.invariants(), "Invariants failed at step 5641"

    # Operation 5642: insert
    tree[6401] = 'value_903649'
    reference[6401] = 'value_903649'
    assert tree.invariants(), "Invariants failed at step 5642"

    # Operation 5643: batch_delete
    keys_to_delete = [2744, 7105, 1956, 9998]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5643"

    # Operation 5644: insert
    tree[8986] = 'value_328930'
    reference[8986] = 'value_328930'
    assert tree.invariants(), "Invariants failed at step 5644"

    # Operation 5645: delete
    del tree[8986]
    del reference[8986]
    assert tree.invariants(), "Invariants failed at step 5645"

    # Operation 5646: get
    assert tree.invariants(), "Invariants failed at step 5646"

    # Operation 5647: get
    assert tree.invariants(), "Invariants failed at step 5647"

    # Operation 5648: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5648"

    # Operation 5649: get
    assert tree.invariants(), "Invariants failed at step 5649"

    # Operation 5650: insert
    tree[5935] = 'value_590900'
    reference[5935] = 'value_590900'
    assert tree.invariants(), "Invariants failed at step 5650"

    # Operation 5651: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5651"

    # Operation 5652: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5652"

    # Operation 5653: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5653"

    # Operation 5654: get
    assert tree.invariants(), "Invariants failed at step 5654"

    # Operation 5655: get
    assert tree.invariants(), "Invariants failed at step 5655"

    # Operation 5656: insert
    tree[4288] = 'value_987413'
    reference[4288] = 'value_987413'
    assert tree.invariants(), "Invariants failed at step 5656"

    # Operation 5657: batch_delete
    keys_to_delete = [4288, 1641, 5179, 6328]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5657"

    # Operation 5658: insert
    tree[2461] = 'value_439381'
    reference[2461] = 'value_439381'
    assert tree.invariants(), "Invariants failed at step 5658"

    # Operation 5659: delete
    del tree[6401]
    del reference[6401]
    assert tree.invariants(), "Invariants failed at step 5659"

    # Operation 5660: delete
    del tree[2461]
    del reference[2461]
    assert tree.invariants(), "Invariants failed at step 5660"

    # Operation 5661: get
    assert tree.invariants(), "Invariants failed at step 5661"

    # Operation 5662: update
    tree[5935] = 'value_683618'
    reference[5935] = 'value_683618'
    assert tree.invariants(), "Invariants failed at step 5662"

    # Operation 5663: get
    assert tree.invariants(), "Invariants failed at step 5663"

    # Operation 5664: delete
    del tree[286]
    del reference[286]
    assert tree.invariants(), "Invariants failed at step 5664"

    # Operation 5665: insert
    tree[1075] = 'value_746394'
    reference[1075] = 'value_746394'
    assert tree.invariants(), "Invariants failed at step 5665"

    # Operation 5666: insert
    tree[2350] = 'value_969376'
    reference[2350] = 'value_969376'
    assert tree.invariants(), "Invariants failed at step 5666"

    # Operation 5667: get
    assert tree.invariants(), "Invariants failed at step 5667"

    # Operation 5668: insert
    tree[1422] = 'value_194512'
    reference[1422] = 'value_194512'
    assert tree.invariants(), "Invariants failed at step 5668"

    # Operation 5669: delete
    del tree[1422]
    del reference[1422]
    assert tree.invariants(), "Invariants failed at step 5669"

    # Operation 5670: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5670"

    # Operation 5671: insert
    tree[1623] = 'value_444576'
    reference[1623] = 'value_444576'
    assert tree.invariants(), "Invariants failed at step 5671"

    # Operation 5672: insert
    tree[4611] = 'value_972500'
    reference[4611] = 'value_972500'
    assert tree.invariants(), "Invariants failed at step 5672"

    # Operation 5673: get
    assert tree.invariants(), "Invariants failed at step 5673"

    # Operation 5674: batch_delete
    keys_to_delete = [7288, 8962, 1075, 2350]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5674"

    # Operation 5675: delete
    del tree[5935]
    del reference[5935]
    assert tree.invariants(), "Invariants failed at step 5675"

    # Operation 5676: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5676"

    # Operation 5677: insert
    tree[7068] = 'value_776065'
    reference[7068] = 'value_776065'
    assert tree.invariants(), "Invariants failed at step 5677"

    # Operation 5678: delete
    del tree[1623]
    del reference[1623]
    assert tree.invariants(), "Invariants failed at step 5678"

    # Operation 5679: update
    tree[4611] = 'value_987023'
    reference[4611] = 'value_987023'
    assert tree.invariants(), "Invariants failed at step 5679"

    # Operation 5680: update
    tree[4611] = 'value_380121'
    reference[4611] = 'value_380121'
    assert tree.invariants(), "Invariants failed at step 5680"

    # Operation 5681: delete
    del tree[4611]
    del reference[4611]
    assert tree.invariants(), "Invariants failed at step 5681"

    # Operation 5682: insert
    tree[2526] = 'value_53072'
    reference[2526] = 'value_53072'
    assert tree.invariants(), "Invariants failed at step 5682"

    # Operation 5683: delete
    del tree[2526]
    del reference[2526]
    assert tree.invariants(), "Invariants failed at step 5683"

    # Operation 5684: delete
    del tree[7068]
    del reference[7068]
    assert tree.invariants(), "Invariants failed at step 5684"

    # Operation 5685: insert
    tree[5155] = 'value_607194'
    reference[5155] = 'value_607194'
    assert tree.invariants(), "Invariants failed at step 5685"

    # Operation 5686: insert
    tree[6613] = 'value_589820'
    reference[6613] = 'value_589820'
    assert tree.invariants(), "Invariants failed at step 5686"

    # Operation 5687: delete
    del tree[5155]
    del reference[5155]
    assert tree.invariants(), "Invariants failed at step 5687"

    # Operation 5688: insert
    tree[3845] = 'value_733986'
    reference[3845] = 'value_733986'
    assert tree.invariants(), "Invariants failed at step 5688"

    # Operation 5689: get
    assert tree.invariants(), "Invariants failed at step 5689"

    # Operation 5690: get
    assert tree.invariants(), "Invariants failed at step 5690"

    # Operation 5691: insert
    tree[5066] = 'value_67282'
    reference[5066] = 'value_67282'
    assert tree.invariants(), "Invariants failed at step 5691"

    # Operation 5692: insert
    tree[739] = 'value_975420'
    reference[739] = 'value_975420'
    assert tree.invariants(), "Invariants failed at step 5692"

    # Operation 5693: insert
    tree[370] = 'value_553453'
    reference[370] = 'value_553453'
    assert tree.invariants(), "Invariants failed at step 5693"

    # Operation 5694: delete
    del tree[6613]
    del reference[6613]
    assert tree.invariants(), "Invariants failed at step 5694"

    # Operation 5695: update
    tree[370] = 'value_479103'
    reference[370] = 'value_479103'
    assert tree.invariants(), "Invariants failed at step 5695"

    # Operation 5696: update
    tree[3845] = 'value_260125'
    reference[3845] = 'value_260125'
    assert tree.invariants(), "Invariants failed at step 5696"

    # Operation 5697: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5697"

    # Operation 5698: get
    assert tree.invariants(), "Invariants failed at step 5698"

    # Operation 5699: delete
    del tree[5066]
    del reference[5066]
    assert tree.invariants(), "Invariants failed at step 5699"

    # Operation 5700: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5700"

    # Operation 5701: insert
    tree[9037] = 'value_230348'
    reference[9037] = 'value_230348'
    assert tree.invariants(), "Invariants failed at step 5701"

    # Operation 5702: insert
    tree[9274] = 'value_511841'
    reference[9274] = 'value_511841'
    assert tree.invariants(), "Invariants failed at step 5702"

    # Operation 5703: delete
    del tree[3845]
    del reference[3845]
    assert tree.invariants(), "Invariants failed at step 5703"

    # Operation 5704: insert
    tree[8944] = 'value_583017'
    reference[8944] = 'value_583017'
    assert tree.invariants(), "Invariants failed at step 5704"

    # Operation 5705: delete
    del tree[739]
    del reference[739]
    assert tree.invariants(), "Invariants failed at step 5705"

    # Operation 5706: get
    assert tree.invariants(), "Invariants failed at step 5706"

    # Operation 5707: delete
    del tree[370]
    del reference[370]
    assert tree.invariants(), "Invariants failed at step 5707"

    # Operation 5708: get
    assert tree.invariants(), "Invariants failed at step 5708"

    # Operation 5709: insert
    tree[6083] = 'value_518207'
    reference[6083] = 'value_518207'
    assert tree.invariants(), "Invariants failed at step 5709"

    # Operation 5710: delete
    del tree[6083]
    del reference[6083]
    assert tree.invariants(), "Invariants failed at step 5710"

    # Operation 5711: update
    tree[8944] = 'value_408041'
    reference[8944] = 'value_408041'
    assert tree.invariants(), "Invariants failed at step 5711"

    # Operation 5712: insert
    tree[1940] = 'value_728021'
    reference[1940] = 'value_728021'
    assert tree.invariants(), "Invariants failed at step 5712"

    # Operation 5713: insert
    tree[79] = 'value_338108'
    reference[79] = 'value_338108'
    assert tree.invariants(), "Invariants failed at step 5713"

    # Operation 5714: insert
    tree[9784] = 'value_345985'
    reference[9784] = 'value_345985'
    assert tree.invariants(), "Invariants failed at step 5714"

    # Operation 5715: get
    assert tree.invariants(), "Invariants failed at step 5715"

    # Operation 5716: insert
    tree[2020] = 'value_180264'
    reference[2020] = 'value_180264'
    assert tree.invariants(), "Invariants failed at step 5716"

    # Operation 5717: insert
    tree[5253] = 'value_63067'
    reference[5253] = 'value_63067'
    assert tree.invariants(), "Invariants failed at step 5717"

    # Operation 5718: get
    assert tree.invariants(), "Invariants failed at step 5718"

    # Operation 5719: delete
    del tree[5253]
    del reference[5253]
    assert tree.invariants(), "Invariants failed at step 5719"

    # Operation 5720: insert
    tree[198] = 'value_123663'
    reference[198] = 'value_123663'
    assert tree.invariants(), "Invariants failed at step 5720"

    # Operation 5721: insert
    tree[9453] = 'value_431661'
    reference[9453] = 'value_431661'
    assert tree.invariants(), "Invariants failed at step 5721"

    # Operation 5722: get
    assert tree.invariants(), "Invariants failed at step 5722"

    # Operation 5723: get
    assert tree.invariants(), "Invariants failed at step 5723"

    # Operation 5724: get
    assert tree.invariants(), "Invariants failed at step 5724"

    # Operation 5725: get
    assert tree.invariants(), "Invariants failed at step 5725"

    # Operation 5726: insert
    tree[1336] = 'value_489388'
    reference[1336] = 'value_489388'
    assert tree.invariants(), "Invariants failed at step 5726"

    # Operation 5727: batch_delete
    keys_to_delete = [198, 9037, 8944, 1940, 2905, 9274, 5659]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5727"

    # Operation 5728: get
    assert tree.invariants(), "Invariants failed at step 5728"

    # Operation 5729: batch_delete
    keys_to_delete = [9784, 1336, 6784, 6415]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5729"

    # Operation 5730: update
    tree[2020] = 'value_571820'
    reference[2020] = 'value_571820'
    assert tree.invariants(), "Invariants failed at step 5730"

    # Operation 5731: get
    assert tree.invariants(), "Invariants failed at step 5731"

    # Operation 5732: delete
    del tree[2020]
    del reference[2020]
    assert tree.invariants(), "Invariants failed at step 5732"

    # Operation 5733: delete
    del tree[79]
    del reference[79]
    assert tree.invariants(), "Invariants failed at step 5733"

    # Operation 5734: get
    assert tree.invariants(), "Invariants failed at step 5734"

    # Operation 5735: delete
    del tree[9453]
    del reference[9453]
    assert tree.invariants(), "Invariants failed at step 5735"

    # Operation 5736: insert
    tree[1190] = 'value_682390'
    reference[1190] = 'value_682390'
    assert tree.invariants(), "Invariants failed at step 5736"

    # Operation 5737: insert
    tree[9772] = 'value_762997'
    reference[9772] = 'value_762997'
    assert tree.invariants(), "Invariants failed at step 5737"

    # Operation 5738: get
    assert tree.invariants(), "Invariants failed at step 5738"

    # Operation 5739: insert
    tree[8513] = 'value_564008'
    reference[8513] = 'value_564008'
    assert tree.invariants(), "Invariants failed at step 5739"

    # Operation 5740: update
    tree[9772] = 'value_35214'
    reference[9772] = 'value_35214'
    assert tree.invariants(), "Invariants failed at step 5740"

    # Operation 5741: get
    assert tree.invariants(), "Invariants failed at step 5741"

    # Operation 5742: delete
    del tree[1190]
    del reference[1190]
    assert tree.invariants(), "Invariants failed at step 5742"

    # Operation 5743: update
    tree[9772] = 'value_885088'
    reference[9772] = 'value_885088'
    assert tree.invariants(), "Invariants failed at step 5743"

    # Operation 5744: delete
    del tree[9772]
    del reference[9772]
    assert tree.invariants(), "Invariants failed at step 5744"

    # Operation 5745: update
    tree[8513] = 'value_637910'
    reference[8513] = 'value_637910'
    assert tree.invariants(), "Invariants failed at step 5745"

    # Operation 5746: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5746"

    # Operation 5747: delete
    del tree[8513]
    del reference[8513]
    assert tree.invariants(), "Invariants failed at step 5747"

    # Operation 5748: insert
    tree[5365] = 'value_134551'
    reference[5365] = 'value_134551'
    assert tree.invariants(), "Invariants failed at step 5748"

    # Operation 5749: get
    assert tree.invariants(), "Invariants failed at step 5749"

    # Operation 5750: update
    tree[5365] = 'value_213578'
    reference[5365] = 'value_213578'
    assert tree.invariants(), "Invariants failed at step 5750"

    # Operation 5751: get
    assert tree.invariants(), "Invariants failed at step 5751"

    # Operation 5752: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5752"

    # Operation 5753: delete
    del tree[5365]
    del reference[5365]
    assert tree.invariants(), "Invariants failed at step 5753"

    # Operation 5754: get
    assert tree.invariants(), "Invariants failed at step 5754"

    # Operation 5755: get
    assert tree.invariants(), "Invariants failed at step 5755"

    # Operation 5756: insert
    tree[3104] = 'value_472009'
    reference[3104] = 'value_472009'
    assert tree.invariants(), "Invariants failed at step 5756"

    # Operation 5757: get
    assert tree.invariants(), "Invariants failed at step 5757"

    # Operation 5758: insert
    tree[4895] = 'value_242764'
    reference[4895] = 'value_242764'
    assert tree.invariants(), "Invariants failed at step 5758"

    # Operation 5759: delete
    del tree[3104]
    del reference[3104]
    assert tree.invariants(), "Invariants failed at step 5759"

    # Operation 5760: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5760"

    # Operation 5761: delete
    del tree[4895]
    del reference[4895]
    assert tree.invariants(), "Invariants failed at step 5761"

    # Operation 5762: insert
    tree[794] = 'value_730940'
    reference[794] = 'value_730940'
    assert tree.invariants(), "Invariants failed at step 5762"

    # Operation 5763: insert
    tree[3144] = 'value_826188'
    reference[3144] = 'value_826188'
    assert tree.invariants(), "Invariants failed at step 5763"

    # Operation 5764: update
    tree[794] = 'value_502420'
    reference[794] = 'value_502420'
    assert tree.invariants(), "Invariants failed at step 5764"

    # Operation 5765: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5765"

    # Operation 5766: insert
    tree[4813] = 'value_609563'
    reference[4813] = 'value_609563'
    assert tree.invariants(), "Invariants failed at step 5766"

    # Operation 5767: insert
    tree[9701] = 'value_705934'
    reference[9701] = 'value_705934'
    assert tree.invariants(), "Invariants failed at step 5767"

    # Operation 5768: delete
    del tree[3144]
    del reference[3144]
    assert tree.invariants(), "Invariants failed at step 5768"

    # Operation 5769: delete
    del tree[9701]
    del reference[9701]
    assert tree.invariants(), "Invariants failed at step 5769"

    # Operation 5770: insert
    tree[9239] = 'value_487590'
    reference[9239] = 'value_487590'
    assert tree.invariants(), "Invariants failed at step 5770"

    # Operation 5771: delete
    del tree[9239]
    del reference[9239]
    assert tree.invariants(), "Invariants failed at step 5771"

    # Operation 5772: delete
    del tree[794]
    del reference[794]
    assert tree.invariants(), "Invariants failed at step 5772"

    # Operation 5773: delete
    del tree[4813]
    del reference[4813]
    assert tree.invariants(), "Invariants failed at step 5773"

    # Operation 5774: get
    assert tree.invariants(), "Invariants failed at step 5774"

    # Operation 5775: get
    assert tree.invariants(), "Invariants failed at step 5775"

    # Operation 5776: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5776"

    # Operation 5777: insert
    tree[6685] = 'value_467079'
    reference[6685] = 'value_467079'
    assert tree.invariants(), "Invariants failed at step 5777"

    # Operation 5778: get
    assert tree.invariants(), "Invariants failed at step 5778"

    # Operation 5779: delete
    del tree[6685]
    del reference[6685]
    assert tree.invariants(), "Invariants failed at step 5779"

    # Operation 5780: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5780"

    # Operation 5781: insert
    tree[6502] = 'value_341100'
    reference[6502] = 'value_341100'
    assert tree.invariants(), "Invariants failed at step 5781"

    # Operation 5782: get
    assert tree.invariants(), "Invariants failed at step 5782"

    # Operation 5783: get
    assert tree.invariants(), "Invariants failed at step 5783"

    # Operation 5784: get
    assert tree.invariants(), "Invariants failed at step 5784"

    # Operation 5785: get
    assert tree.invariants(), "Invariants failed at step 5785"

    # Operation 5786: insert
    tree[3484] = 'value_18938'
    reference[3484] = 'value_18938'
    assert tree.invariants(), "Invariants failed at step 5786"

    # Operation 5787: delete
    del tree[3484]
    del reference[3484]
    assert tree.invariants(), "Invariants failed at step 5787"

    # Operation 5788: insert
    tree[962] = 'value_54725'
    reference[962] = 'value_54725'
    assert tree.invariants(), "Invariants failed at step 5788"

    # Operation 5789: update
    tree[6502] = 'value_447912'
    reference[6502] = 'value_447912'
    assert tree.invariants(), "Invariants failed at step 5789"

    # Operation 5790: get
    assert tree.invariants(), "Invariants failed at step 5790"

    # Operation 5791: delete
    del tree[6502]
    del reference[6502]
    assert tree.invariants(), "Invariants failed at step 5791"

    # Operation 5792: delete
    del tree[962]
    del reference[962]
    assert tree.invariants(), "Invariants failed at step 5792"

    # Operation 5793: get
    assert tree.invariants(), "Invariants failed at step 5793"

    # Operation 5794: get
    assert tree.invariants(), "Invariants failed at step 5794"

    # Operation 5795: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5795"

    # Operation 5796: insert
    tree[3351] = 'value_177240'
    reference[3351] = 'value_177240'
    assert tree.invariants(), "Invariants failed at step 5796"

    # Operation 5797: delete
    del tree[3351]
    del reference[3351]
    assert tree.invariants(), "Invariants failed at step 5797"

    # Operation 5798: get
    assert tree.invariants(), "Invariants failed at step 5798"

    # Operation 5799: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5799"

    # Operation 5800: insert
    tree[1491] = 'value_151646'
    reference[1491] = 'value_151646'
    assert tree.invariants(), "Invariants failed at step 5800"

    # Operation 5801: insert
    tree[8520] = 'value_592932'
    reference[8520] = 'value_592932'
    assert tree.invariants(), "Invariants failed at step 5801"

    # Operation 5802: update
    tree[8520] = 'value_911297'
    reference[8520] = 'value_911297'
    assert tree.invariants(), "Invariants failed at step 5802"

    # Operation 5803: get
    assert tree.invariants(), "Invariants failed at step 5803"

    # Operation 5804: insert
    tree[6933] = 'value_789536'
    reference[6933] = 'value_789536'
    assert tree.invariants(), "Invariants failed at step 5804"

    # Operation 5805: insert
    tree[1995] = 'value_566349'
    reference[1995] = 'value_566349'
    assert tree.invariants(), "Invariants failed at step 5805"

    # Operation 5806: insert
    tree[7507] = 'value_955649'
    reference[7507] = 'value_955649'
    assert tree.invariants(), "Invariants failed at step 5806"

    # Operation 5807: batch_delete
    keys_to_delete = [6971, 7507, 6933, 1491]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5807"

    # Operation 5808: get
    assert tree.invariants(), "Invariants failed at step 5808"

    # Operation 5809: insert
    tree[2387] = 'value_301685'
    reference[2387] = 'value_301685'
    assert tree.invariants(), "Invariants failed at step 5809"

    # Operation 5810: get
    assert tree.invariants(), "Invariants failed at step 5810"

    # Operation 5811: get
    assert tree.invariants(), "Invariants failed at step 5811"

    # Operation 5812: insert
    tree[6884] = 'value_313274'
    reference[6884] = 'value_313274'
    assert tree.invariants(), "Invariants failed at step 5812"

    # Operation 5813: insert
    tree[2142] = 'value_518573'
    reference[2142] = 'value_518573'
    assert tree.invariants(), "Invariants failed at step 5813"

    # Operation 5814: batch_delete
    keys_to_delete = [8520, 1995, 5220]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5814"

    # Operation 5815: update
    tree[2142] = 'value_491090'
    reference[2142] = 'value_491090'
    assert tree.invariants(), "Invariants failed at step 5815"

    # Operation 5816: insert
    tree[8563] = 'value_126656'
    reference[8563] = 'value_126656'
    assert tree.invariants(), "Invariants failed at step 5816"

    # Operation 5817: insert
    tree[7868] = 'value_204568'
    reference[7868] = 'value_204568'
    assert tree.invariants(), "Invariants failed at step 5817"

    # Operation 5818: get
    assert tree.invariants(), "Invariants failed at step 5818"

    # Operation 5819: insert
    tree[3194] = 'value_709664'
    reference[3194] = 'value_709664'
    assert tree.invariants(), "Invariants failed at step 5819"

    # Operation 5820: get
    assert tree.invariants(), "Invariants failed at step 5820"

    # Operation 5821: insert
    tree[8944] = 'value_925851'
    reference[8944] = 'value_925851'
    assert tree.invariants(), "Invariants failed at step 5821"

    # Operation 5822: delete
    del tree[6884]
    del reference[6884]
    assert tree.invariants(), "Invariants failed at step 5822"

    # Operation 5823: batch_delete
    keys_to_delete = [4585, 2387, 8563, 6739, 7868]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5823"

    # Operation 5824: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5824"

    # Operation 5825: insert
    tree[8626] = 'value_186867'
    reference[8626] = 'value_186867'
    assert tree.invariants(), "Invariants failed at step 5825"

    # Operation 5826: get
    assert tree.invariants(), "Invariants failed at step 5826"

    # Operation 5827: get
    assert tree.invariants(), "Invariants failed at step 5827"

    # Operation 5828: update
    tree[8944] = 'value_317658'
    reference[8944] = 'value_317658'
    assert tree.invariants(), "Invariants failed at step 5828"

    # Operation 5829: update
    tree[8944] = 'value_759463'
    reference[8944] = 'value_759463'
    assert tree.invariants(), "Invariants failed at step 5829"

    # Operation 5830: insert
    tree[7465] = 'value_259130'
    reference[7465] = 'value_259130'
    assert tree.invariants(), "Invariants failed at step 5830"

    # Operation 5831: update
    tree[7465] = 'value_457145'
    reference[7465] = 'value_457145'
    assert tree.invariants(), "Invariants failed at step 5831"

    # Operation 5832: insert
    tree[5404] = 'value_389239'
    reference[5404] = 'value_389239'
    assert tree.invariants(), "Invariants failed at step 5832"

    # Operation 5833: batch_delete
    keys_to_delete = [8944, 8626, 6643, 4341, 5404]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5833"

    # Operation 5834: get
    assert tree.invariants(), "Invariants failed at step 5834"

    # Operation 5835: insert
    tree[415] = 'value_941604'
    reference[415] = 'value_941604'
    assert tree.invariants(), "Invariants failed at step 5835"

    # Operation 5836: delete
    del tree[415]
    del reference[415]
    assert tree.invariants(), "Invariants failed at step 5836"

    # Operation 5837: delete
    del tree[3194]
    del reference[3194]
    assert tree.invariants(), "Invariants failed at step 5837"

    # Operation 5838: get
    assert tree.invariants(), "Invariants failed at step 5838"

    # Operation 5839: get
    assert tree.invariants(), "Invariants failed at step 5839"

    # Operation 5840: insert
    tree[9139] = 'value_401542'
    reference[9139] = 'value_401542'
    assert tree.invariants(), "Invariants failed at step 5840"

    # Operation 5841: get
    assert tree.invariants(), "Invariants failed at step 5841"

    # Operation 5842: get
    assert tree.invariants(), "Invariants failed at step 5842"

    # Operation 5843: insert
    tree[1235] = 'value_125672'
    reference[1235] = 'value_125672'
    assert tree.invariants(), "Invariants failed at step 5843"

    # Operation 5844: get
    assert tree.invariants(), "Invariants failed at step 5844"

    # Operation 5845: update
    tree[1235] = 'value_882653'
    reference[1235] = 'value_882653'
    assert tree.invariants(), "Invariants failed at step 5845"

    # Operation 5846: delete
    del tree[1235]
    del reference[1235]
    assert tree.invariants(), "Invariants failed at step 5846"

    # Operation 5847: get
    assert tree.invariants(), "Invariants failed at step 5847"

    # Operation 5848: delete
    del tree[9139]
    del reference[9139]
    assert tree.invariants(), "Invariants failed at step 5848"

    # Operation 5849: get
    assert tree.invariants(), "Invariants failed at step 5849"

    # Operation 5850: update
    tree[2142] = 'value_743983'
    reference[2142] = 'value_743983'
    assert tree.invariants(), "Invariants failed at step 5850"

    # Operation 5851: delete
    del tree[2142]
    del reference[2142]
    assert tree.invariants(), "Invariants failed at step 5851"

    # Operation 5852: delete
    del tree[7465]
    del reference[7465]
    assert tree.invariants(), "Invariants failed at step 5852"

    # Operation 5853: insert
    tree[7240] = 'value_932854'
    reference[7240] = 'value_932854'
    assert tree.invariants(), "Invariants failed at step 5853"

    # Operation 5854: delete
    del tree[7240]
    del reference[7240]
    assert tree.invariants(), "Invariants failed at step 5854"

    # Operation 5855: insert
    tree[2933] = 'value_36942'
    reference[2933] = 'value_36942'
    assert tree.invariants(), "Invariants failed at step 5855"

    # Operation 5856: insert
    tree[8380] = 'value_41807'
    reference[8380] = 'value_41807'
    assert tree.invariants(), "Invariants failed at step 5856"

    # Operation 5857: insert
    tree[1855] = 'value_925729'
    reference[1855] = 'value_925729'
    assert tree.invariants(), "Invariants failed at step 5857"

    # Operation 5858: get
    assert tree.invariants(), "Invariants failed at step 5858"

    # Operation 5859: get
    assert tree.invariants(), "Invariants failed at step 5859"

    # Operation 5860: get
    assert tree.invariants(), "Invariants failed at step 5860"

    # Operation 5861: insert
    tree[2238] = 'value_591179'
    reference[2238] = 'value_591179'
    assert tree.invariants(), "Invariants failed at step 5861"

    # Operation 5862: get
    assert tree.invariants(), "Invariants failed at step 5862"

    # Operation 5863: insert
    tree[8139] = 'value_605439'
    reference[8139] = 'value_605439'
    assert tree.invariants(), "Invariants failed at step 5863"

    # Operation 5864: delete
    del tree[8139]
    del reference[8139]
    assert tree.invariants(), "Invariants failed at step 5864"

    # Operation 5865: insert
    tree[3153] = 'value_928110'
    reference[3153] = 'value_928110'
    assert tree.invariants(), "Invariants failed at step 5865"

    # Operation 5866: get
    assert tree.invariants(), "Invariants failed at step 5866"

    # Operation 5867: delete
    del tree[8380]
    del reference[8380]
    assert tree.invariants(), "Invariants failed at step 5867"

    # Operation 5868: get
    assert tree.invariants(), "Invariants failed at step 5868"

    # Operation 5869: update
    tree[3153] = 'value_540357'
    reference[3153] = 'value_540357'
    assert tree.invariants(), "Invariants failed at step 5869"

    # Operation 5870: insert
    tree[3796] = 'value_435552'
    reference[3796] = 'value_435552'
    assert tree.invariants(), "Invariants failed at step 5870"

    # Operation 5871: insert
    tree[9568] = 'value_65991'
    reference[9568] = 'value_65991'
    assert tree.invariants(), "Invariants failed at step 5871"

    # Operation 5872: insert
    tree[3133] = 'value_900951'
    reference[3133] = 'value_900951'
    assert tree.invariants(), "Invariants failed at step 5872"

    # Operation 5873: insert
    tree[8452] = 'value_482781'
    reference[8452] = 'value_482781'
    assert tree.invariants(), "Invariants failed at step 5873"

    # Operation 5874: delete
    del tree[2933]
    del reference[2933]
    assert tree.invariants(), "Invariants failed at step 5874"

    # Operation 5875: insert
    tree[4250] = 'value_177355'
    reference[4250] = 'value_177355'
    assert tree.invariants(), "Invariants failed at step 5875"

    # Operation 5876: insert
    tree[9356] = 'value_177422'
    reference[9356] = 'value_177422'
    assert tree.invariants(), "Invariants failed at step 5876"

    # Operation 5877: insert
    tree[370] = 'value_362297'
    reference[370] = 'value_362297'
    assert tree.invariants(), "Invariants failed at step 5877"

    # Operation 5878: delete
    del tree[1855]
    del reference[1855]
    assert tree.invariants(), "Invariants failed at step 5878"

    # Operation 5879: insert
    tree[7659] = 'value_791158'
    reference[7659] = 'value_791158'
    assert tree.invariants(), "Invariants failed at step 5879"

    # Operation 5880: get
    assert tree.invariants(), "Invariants failed at step 5880"

    # Operation 5881: delete
    del tree[9568]
    del reference[9568]
    assert tree.invariants(), "Invariants failed at step 5881"

    # Operation 5882: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5882"

    # Operation 5883: insert
    tree[7910] = 'value_51061'
    reference[7910] = 'value_51061'
    assert tree.invariants(), "Invariants failed at step 5883"

    # Operation 5884: get
    assert tree.invariants(), "Invariants failed at step 5884"

    # Operation 5885: insert
    tree[3140] = 'value_9539'
    reference[3140] = 'value_9539'
    assert tree.invariants(), "Invariants failed at step 5885"

    # Operation 5886: insert
    tree[4641] = 'value_647800'
    reference[4641] = 'value_647800'
    assert tree.invariants(), "Invariants failed at step 5886"

    # Operation 5887: delete
    del tree[2238]
    del reference[2238]
    assert tree.invariants(), "Invariants failed at step 5887"

    # Operation 5888: delete
    del tree[370]
    del reference[370]
    assert tree.invariants(), "Invariants failed at step 5888"

    # Operation 5889: get
    assert tree.invariants(), "Invariants failed at step 5889"

    # Operation 5890: delete
    del tree[7659]
    del reference[7659]
    assert tree.invariants(), "Invariants failed at step 5890"

    # Operation 5891: insert
    tree[6831] = 'value_37000'
    reference[6831] = 'value_37000'
    assert tree.invariants(), "Invariants failed at step 5891"

    # Operation 5892: update
    tree[3140] = 'value_375942'
    reference[3140] = 'value_375942'
    assert tree.invariants(), "Invariants failed at step 5892"

    # Operation 5893: update
    tree[3796] = 'value_814579'
    reference[3796] = 'value_814579'
    assert tree.invariants(), "Invariants failed at step 5893"

    # Operation 5894: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5894"

    # Operation 5895: get
    assert tree.invariants(), "Invariants failed at step 5895"

    # Operation 5896: insert
    tree[1155] = 'value_46983'
    reference[1155] = 'value_46983'
    assert tree.invariants(), "Invariants failed at step 5896"

    # Operation 5897: batch_delete
    keys_to_delete = [1642, 6831, 3153, 3796, 4250, 2620, 3133]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5897"

    # Operation 5898: get
    assert tree.invariants(), "Invariants failed at step 5898"

    # Operation 5899: get
    assert tree.invariants(), "Invariants failed at step 5899"

    # Operation 5900: get
    assert tree.invariants(), "Invariants failed at step 5900"

    # Operation 5901: update
    tree[4641] = 'value_819789'
    reference[4641] = 'value_819789'
    assert tree.invariants(), "Invariants failed at step 5901"

    # Operation 5902: delete
    del tree[3140]
    del reference[3140]
    assert tree.invariants(), "Invariants failed at step 5902"

    # Operation 5903: get
    assert tree.invariants(), "Invariants failed at step 5903"

    # Operation 5904: get
    assert tree.invariants(), "Invariants failed at step 5904"

    # Operation 5905: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5905"

    # Operation 5906: delete
    del tree[4641]
    del reference[4641]
    assert tree.invariants(), "Invariants failed at step 5906"

    # Operation 5907: insert
    tree[4424] = 'value_669038'
    reference[4424] = 'value_669038'
    assert tree.invariants(), "Invariants failed at step 5907"

    # Operation 5908: update
    tree[1155] = 'value_285952'
    reference[1155] = 'value_285952'
    assert tree.invariants(), "Invariants failed at step 5908"

    # Operation 5909: delete
    del tree[7910]
    del reference[7910]
    assert tree.invariants(), "Invariants failed at step 5909"

    # Operation 5910: insert
    tree[5541] = 'value_584391'
    reference[5541] = 'value_584391'
    assert tree.invariants(), "Invariants failed at step 5910"

    # Operation 5911: delete
    del tree[8452]
    del reference[8452]
    assert tree.invariants(), "Invariants failed at step 5911"

    # Operation 5912: delete
    del tree[5541]
    del reference[5541]
    assert tree.invariants(), "Invariants failed at step 5912"

    # Operation 5913: insert
    tree[5347] = 'value_490136'
    reference[5347] = 'value_490136'
    assert tree.invariants(), "Invariants failed at step 5913"

    # Operation 5914: delete
    del tree[4424]
    del reference[4424]
    assert tree.invariants(), "Invariants failed at step 5914"

    # Operation 5915: insert
    tree[2046] = 'value_587524'
    reference[2046] = 'value_587524'
    assert tree.invariants(), "Invariants failed at step 5915"

    # Operation 5916: insert
    tree[9352] = 'value_689331'
    reference[9352] = 'value_689331'
    assert tree.invariants(), "Invariants failed at step 5916"

    # Operation 5917: delete
    del tree[2046]
    del reference[2046]
    assert tree.invariants(), "Invariants failed at step 5917"

    # Operation 5918: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5918"

    # Operation 5919: insert
    tree[4112] = 'value_246229'
    reference[4112] = 'value_246229'
    assert tree.invariants(), "Invariants failed at step 5919"

    # Operation 5920: get
    assert tree.invariants(), "Invariants failed at step 5920"

    # Operation 5921: update
    tree[9356] = 'value_247389'
    reference[9356] = 'value_247389'
    assert tree.invariants(), "Invariants failed at step 5921"

    # Operation 5922: insert
    tree[6529] = 'value_958020'
    reference[6529] = 'value_958020'
    assert tree.invariants(), "Invariants failed at step 5922"

    # Operation 5923: get
    assert tree.invariants(), "Invariants failed at step 5923"

    # Operation 5924: insert
    tree[2920] = 'value_707583'
    reference[2920] = 'value_707583'
    assert tree.invariants(), "Invariants failed at step 5924"

    # Operation 5925: update
    tree[2920] = 'value_833934'
    reference[2920] = 'value_833934'
    assert tree.invariants(), "Invariants failed at step 5925"

    # Operation 5926: update
    tree[4112] = 'value_109860'
    reference[4112] = 'value_109860'
    assert tree.invariants(), "Invariants failed at step 5926"

    # Operation 5927: delete
    del tree[6529]
    del reference[6529]
    assert tree.invariants(), "Invariants failed at step 5927"

    # Operation 5928: get
    assert tree.invariants(), "Invariants failed at step 5928"

    # Operation 5929: get
    assert tree.invariants(), "Invariants failed at step 5929"

    # Operation 5930: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5930"

    # Operation 5931: get
    assert tree.invariants(), "Invariants failed at step 5931"

    # Operation 5932: insert
    tree[3481] = 'value_467867'
    reference[3481] = 'value_467867'
    assert tree.invariants(), "Invariants failed at step 5932"

    # Operation 5933: delete
    del tree[9352]
    del reference[9352]
    assert tree.invariants(), "Invariants failed at step 5933"

    # Operation 5934: batch_delete
    keys_to_delete = [5347, 1155, 2920, 104, 8663]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5934"

    # Operation 5935: delete
    del tree[4112]
    del reference[4112]
    assert tree.invariants(), "Invariants failed at step 5935"

    # Operation 5936: delete
    del tree[9356]
    del reference[9356]
    assert tree.invariants(), "Invariants failed at step 5936"

    # Operation 5937: insert
    tree[7487] = 'value_632676'
    reference[7487] = 'value_632676'
    assert tree.invariants(), "Invariants failed at step 5937"

    # Operation 5938: update
    tree[7487] = 'value_898366'
    reference[7487] = 'value_898366'
    assert tree.invariants(), "Invariants failed at step 5938"

    # Operation 5939: get
    assert tree.invariants(), "Invariants failed at step 5939"

    # Operation 5940: get
    assert tree.invariants(), "Invariants failed at step 5940"

    # Operation 5941: delete
    del tree[7487]
    del reference[7487]
    assert tree.invariants(), "Invariants failed at step 5941"

    # Operation 5942: delete
    del tree[3481]
    del reference[3481]
    assert tree.invariants(), "Invariants failed at step 5942"

    # Operation 5943: insert
    tree[9918] = 'value_675014'
    reference[9918] = 'value_675014'
    assert tree.invariants(), "Invariants failed at step 5943"

    # Operation 5944: delete
    del tree[9918]
    del reference[9918]
    assert tree.invariants(), "Invariants failed at step 5944"

    # Operation 5945: insert
    tree[280] = 'value_343829'
    reference[280] = 'value_343829'
    assert tree.invariants(), "Invariants failed at step 5945"

    # Operation 5946: delete
    del tree[280]
    del reference[280]
    assert tree.invariants(), "Invariants failed at step 5946"

    # Operation 5947: insert
    tree[3246] = 'value_938548'
    reference[3246] = 'value_938548'
    assert tree.invariants(), "Invariants failed at step 5947"

    # Operation 5948: delete
    del tree[3246]
    del reference[3246]
    assert tree.invariants(), "Invariants failed at step 5948"

    # Operation 5949: insert
    tree[2403] = 'value_889296'
    reference[2403] = 'value_889296'
    assert tree.invariants(), "Invariants failed at step 5949"

    # Operation 5950: delete
    del tree[2403]
    del reference[2403]
    assert tree.invariants(), "Invariants failed at step 5950"

    # Operation 5951: insert
    tree[1433] = 'value_541615'
    reference[1433] = 'value_541615'
    assert tree.invariants(), "Invariants failed at step 5951"

    # Operation 5952: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5952"

    # Operation 5953: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5953"

    # Operation 5954: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5954"

    # Operation 5955: update
    tree[1433] = 'value_475740'
    reference[1433] = 'value_475740'
    assert tree.invariants(), "Invariants failed at step 5955"

    # Operation 5956: delete
    del tree[1433]
    del reference[1433]
    assert tree.invariants(), "Invariants failed at step 5956"

    # Operation 5957: insert
    tree[5384] = 'value_386199'
    reference[5384] = 'value_386199'
    assert tree.invariants(), "Invariants failed at step 5957"

    # Operation 5958: insert
    tree[8629] = 'value_553589'
    reference[8629] = 'value_553589'
    assert tree.invariants(), "Invariants failed at step 5958"

    # Operation 5959: delete
    del tree[8629]
    del reference[8629]
    assert tree.invariants(), "Invariants failed at step 5959"

    # Operation 5960: update
    tree[5384] = 'value_72032'
    reference[5384] = 'value_72032'
    assert tree.invariants(), "Invariants failed at step 5960"

    # Operation 5961: insert
    tree[5238] = 'value_338671'
    reference[5238] = 'value_338671'
    assert tree.invariants(), "Invariants failed at step 5961"

    # Operation 5962: delete
    del tree[5384]
    del reference[5384]
    assert tree.invariants(), "Invariants failed at step 5962"

    # Operation 5963: insert
    tree[6124] = 'value_194565'
    reference[6124] = 'value_194565'
    assert tree.invariants(), "Invariants failed at step 5963"

    # Operation 5964: get
    assert tree.invariants(), "Invariants failed at step 5964"

    # Operation 5965: insert
    tree[1019] = 'value_234474'
    reference[1019] = 'value_234474'
    assert tree.invariants(), "Invariants failed at step 5965"

    # Operation 5966: insert
    tree[9751] = 'value_572816'
    reference[9751] = 'value_572816'
    assert tree.invariants(), "Invariants failed at step 5966"

    # Operation 5967: get
    assert tree.invariants(), "Invariants failed at step 5967"

    # Operation 5968: get
    assert tree.invariants(), "Invariants failed at step 5968"

    # Operation 5969: update
    tree[6124] = 'value_922424'
    reference[6124] = 'value_922424'
    assert tree.invariants(), "Invariants failed at step 5969"

    # Operation 5970: get
    assert tree.invariants(), "Invariants failed at step 5970"

    # Operation 5971: insert
    tree[1328] = 'value_252026'
    reference[1328] = 'value_252026'
    assert tree.invariants(), "Invariants failed at step 5971"

    # Operation 5972: update
    tree[5238] = 'value_698895'
    reference[5238] = 'value_698895'
    assert tree.invariants(), "Invariants failed at step 5972"

    # Operation 5973: batch_delete
    keys_to_delete = [1328, 3366, 5238, 3862]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5973"

    # Operation 5974: insert
    tree[323] = 'value_809970'
    reference[323] = 'value_809970'
    assert tree.invariants(), "Invariants failed at step 5974"

    # Operation 5975: update
    tree[6124] = 'value_141240'
    reference[6124] = 'value_141240'
    assert tree.invariants(), "Invariants failed at step 5975"

    # Operation 5976: get
    assert tree.invariants(), "Invariants failed at step 5976"

    # Operation 5977: delete
    del tree[323]
    del reference[323]
    assert tree.invariants(), "Invariants failed at step 5977"

    # Operation 5978: get
    assert tree.invariants(), "Invariants failed at step 5978"

    # Operation 5979: delete
    del tree[1019]
    del reference[1019]
    assert tree.invariants(), "Invariants failed at step 5979"

    # Operation 5980: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5980"

    # Operation 5981: delete
    del tree[9751]
    del reference[9751]
    assert tree.invariants(), "Invariants failed at step 5981"

    # Operation 5982: insert
    tree[17] = 'value_381746'
    reference[17] = 'value_381746'
    assert tree.invariants(), "Invariants failed at step 5982"

    # Operation 5983: insert
    tree[3655] = 'value_170921'
    reference[3655] = 'value_170921'
    assert tree.invariants(), "Invariants failed at step 5983"

    # Operation 5984: insert
    tree[1681] = 'value_845168'
    reference[1681] = 'value_845168'
    assert tree.invariants(), "Invariants failed at step 5984"

    # Operation 5985: insert
    tree[1068] = 'value_972326'
    reference[1068] = 'value_972326'
    assert tree.invariants(), "Invariants failed at step 5985"

    # Operation 5986: insert
    tree[9490] = 'value_841959'
    reference[9490] = 'value_841959'
    assert tree.invariants(), "Invariants failed at step 5986"

    # Operation 5987: get
    assert tree.invariants(), "Invariants failed at step 5987"

    # Operation 5988: update
    tree[3655] = 'value_108578'
    reference[3655] = 'value_108578'
    assert tree.invariants(), "Invariants failed at step 5988"

    # Operation 5989: insert
    tree[3220] = 'value_638457'
    reference[3220] = 'value_638457'
    assert tree.invariants(), "Invariants failed at step 5989"

    # Operation 5990: delete
    del tree[3220]
    del reference[3220]
    assert tree.invariants(), "Invariants failed at step 5990"

    # Operation 5991: get
    assert tree.invariants(), "Invariants failed at step 5991"

    # Operation 5992: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 5992"

    # Operation 5993: update
    tree[1068] = 'value_740074'
    reference[1068] = 'value_740074'
    assert tree.invariants(), "Invariants failed at step 5993"

    # Operation 5994: update
    tree[6124] = 'value_61290'
    reference[6124] = 'value_61290'
    assert tree.invariants(), "Invariants failed at step 5994"

    # Operation 5995: insert
    tree[6698] = 'value_995525'
    reference[6698] = 'value_995525'
    assert tree.invariants(), "Invariants failed at step 5995"

    # Operation 5996: batch_delete
    keys_to_delete = [6124, 1068, 9582, 2799, 1681]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 5996"

    # Operation 5997: get
    assert tree.invariants(), "Invariants failed at step 5997"

    # Operation 5998: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 5998"

    # Operation 5999: insert
    tree[8771] = 'value_516168'
    reference[8771] = 'value_516168'
    assert tree.invariants(), "Invariants failed at step 5999"

    # Operation 6000: insert
    tree[2431] = 'value_678188'
    reference[2431] = 'value_678188'
    assert tree.invariants(), "Invariants failed at step 6000"

    # Operation 6001: delete
    del tree[6698]
    del reference[6698]
    assert tree.invariants(), "Invariants failed at step 6001"

    # Operation 6002: insert
    tree[8344] = 'value_62352'
    reference[8344] = 'value_62352'
    assert tree.invariants(), "Invariants failed at step 6002"

    # Operation 6003: insert
    tree[3856] = 'value_330584'
    reference[3856] = 'value_330584'
    assert tree.invariants(), "Invariants failed at step 6003"

    # Operation 6004: insert
    tree[7447] = 'value_584417'
    reference[7447] = 'value_584417'
    assert tree.invariants(), "Invariants failed at step 6004"

    # Operation 6005: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6005"

    # Operation 6006: batch_delete
    keys_to_delete = [4641, 3655, 17, 7447, 8344, 2431]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6006"

    # Operation 6007: delete
    del tree[8771]
    del reference[8771]
    assert tree.invariants(), "Invariants failed at step 6007"

    # Operation 6008: delete
    del tree[3856]
    del reference[3856]
    assert tree.invariants(), "Invariants failed at step 6008"

    # Operation 6009: get
    assert tree.invariants(), "Invariants failed at step 6009"

    # Operation 6010: update
    tree[9490] = 'value_630200'
    reference[9490] = 'value_630200'
    assert tree.invariants(), "Invariants failed at step 6010"

    # Operation 6011: delete
    del tree[9490]
    del reference[9490]
    assert tree.invariants(), "Invariants failed at step 6011"

    # Operation 6012: insert
    tree[8856] = 'value_489987'
    reference[8856] = 'value_489987'
    assert tree.invariants(), "Invariants failed at step 6012"

    # Operation 6013: get
    assert tree.invariants(), "Invariants failed at step 6013"

    # Operation 6014: get
    assert tree.invariants(), "Invariants failed at step 6014"

    # Operation 6015: insert
    tree[2527] = 'value_325752'
    reference[2527] = 'value_325752'
    assert tree.invariants(), "Invariants failed at step 6015"

    # Operation 6016: get
    assert tree.invariants(), "Invariants failed at step 6016"

    # Operation 6017: insert
    tree[9224] = 'value_642752'
    reference[9224] = 'value_642752'
    assert tree.invariants(), "Invariants failed at step 6017"

    # Operation 6018: update
    tree[9224] = 'value_382475'
    reference[9224] = 'value_382475'
    assert tree.invariants(), "Invariants failed at step 6018"

    # Operation 6019: get
    assert tree.invariants(), "Invariants failed at step 6019"

    # Operation 6020: insert
    tree[8835] = 'value_412495'
    reference[8835] = 'value_412495'
    assert tree.invariants(), "Invariants failed at step 6020"

    # Operation 6021: get
    assert tree.invariants(), "Invariants failed at step 6021"

    # Operation 6022: get
    assert tree.invariants(), "Invariants failed at step 6022"

    # Operation 6023: insert
    tree[8144] = 'value_708947'
    reference[8144] = 'value_708947'
    assert tree.invariants(), "Invariants failed at step 6023"

    # Operation 6024: delete
    del tree[8144]
    del reference[8144]
    assert tree.invariants(), "Invariants failed at step 6024"

    # Operation 6025: get
    assert tree.invariants(), "Invariants failed at step 6025"

    # Operation 6026: insert
    tree[6923] = 'value_792812'
    reference[6923] = 'value_792812'
    assert tree.invariants(), "Invariants failed at step 6026"

    # Operation 6027: update
    tree[8856] = 'value_486100'
    reference[8856] = 'value_486100'
    assert tree.invariants(), "Invariants failed at step 6027"

    # Operation 6028: insert
    tree[8804] = 'value_118144'
    reference[8804] = 'value_118144'
    assert tree.invariants(), "Invariants failed at step 6028"

    # Operation 6029: batch_delete
    keys_to_delete = [8835, 8804, 6923, 2603, 4747]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6029"

    # Operation 6030: get
    assert tree.invariants(), "Invariants failed at step 6030"

    # Operation 6031: delete
    del tree[9224]
    del reference[9224]
    assert tree.invariants(), "Invariants failed at step 6031"

    # Operation 6032: get
    assert tree.invariants(), "Invariants failed at step 6032"

    # Operation 6033: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6033"

    # Operation 6034: insert
    tree[5430] = 'value_617658'
    reference[5430] = 'value_617658'
    assert tree.invariants(), "Invariants failed at step 6034"

    # Operation 6035: insert
    tree[3092] = 'value_441276'
    reference[3092] = 'value_441276'
    assert tree.invariants(), "Invariants failed at step 6035"

    # Operation 6036: update
    tree[2527] = 'value_463861'
    reference[2527] = 'value_463861'
    assert tree.invariants(), "Invariants failed at step 6036"

    # Operation 6037: delete
    del tree[8856]
    del reference[8856]
    assert tree.invariants(), "Invariants failed at step 6037"

    # Operation 6038: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6038"

    # Operation 6039: get
    assert tree.invariants(), "Invariants failed at step 6039"

    # Operation 6040: insert
    tree[6694] = 'value_935866'
    reference[6694] = 'value_935866'
    assert tree.invariants(), "Invariants failed at step 6040"

    # Operation 6041: insert
    tree[4019] = 'value_241615'
    reference[4019] = 'value_241615'
    assert tree.invariants(), "Invariants failed at step 6041"

    # Operation 6042: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6042"

    # Operation 6043: get
    assert tree.invariants(), "Invariants failed at step 6043"

    # Operation 6044: delete
    del tree[2527]
    del reference[2527]
    assert tree.invariants(), "Invariants failed at step 6044"

    # Operation 6045: update
    tree[3092] = 'value_598369'
    reference[3092] = 'value_598369'
    assert tree.invariants(), "Invariants failed at step 6045"

    # Operation 6046: delete
    del tree[3092]
    del reference[3092]
    assert tree.invariants(), "Invariants failed at step 6046"

    # Operation 6047: get
    assert tree.invariants(), "Invariants failed at step 6047"

    # Operation 6048: insert
    tree[2563] = 'value_492355'
    reference[2563] = 'value_492355'
    assert tree.invariants(), "Invariants failed at step 6048"

    # Operation 6049: delete
    del tree[5430]
    del reference[5430]
    assert tree.invariants(), "Invariants failed at step 6049"

    # Operation 6050: update
    tree[2563] = 'value_785941'
    reference[2563] = 'value_785941'
    assert tree.invariants(), "Invariants failed at step 6050"

    # Operation 6051: update
    tree[4019] = 'value_150074'
    reference[4019] = 'value_150074'
    assert tree.invariants(), "Invariants failed at step 6051"

    # Operation 6052: insert
    tree[4118] = 'value_692484'
    reference[4118] = 'value_692484'
    assert tree.invariants(), "Invariants failed at step 6052"

    # Operation 6053: insert
    tree[1499] = 'value_400246'
    reference[1499] = 'value_400246'
    assert tree.invariants(), "Invariants failed at step 6053"

    # Operation 6054: update
    tree[1499] = 'value_268333'
    reference[1499] = 'value_268333'
    assert tree.invariants(), "Invariants failed at step 6054"

    # Operation 6055: get
    assert tree.invariants(), "Invariants failed at step 6055"

    # Operation 6056: delete
    del tree[6694]
    del reference[6694]
    assert tree.invariants(), "Invariants failed at step 6056"

    # Operation 6057: insert
    tree[7520] = 'value_520867'
    reference[7520] = 'value_520867'
    assert tree.invariants(), "Invariants failed at step 6057"

    # Operation 6058: get
    assert tree.invariants(), "Invariants failed at step 6058"

    # Operation 6059: update
    tree[4019] = 'value_10694'
    reference[4019] = 'value_10694'
    assert tree.invariants(), "Invariants failed at step 6059"

    # Operation 6060: delete
    del tree[7520]
    del reference[7520]
    assert tree.invariants(), "Invariants failed at step 6060"

    # Operation 6061: delete
    del tree[4118]
    del reference[4118]
    assert tree.invariants(), "Invariants failed at step 6061"

    # Operation 6062: insert
    tree[3755] = 'value_142880'
    reference[3755] = 'value_142880'
    assert tree.invariants(), "Invariants failed at step 6062"

    # Operation 6063: delete
    del tree[1499]
    del reference[1499]
    assert tree.invariants(), "Invariants failed at step 6063"

    # Operation 6064: delete
    del tree[3755]
    del reference[3755]
    assert tree.invariants(), "Invariants failed at step 6064"

    # Operation 6065: delete
    del tree[2563]
    del reference[2563]
    assert tree.invariants(), "Invariants failed at step 6065"

    # Operation 6066: update
    tree[4019] = 'value_781285'
    reference[4019] = 'value_781285'
    assert tree.invariants(), "Invariants failed at step 6066"

    # Operation 6067: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6067"

    # Operation 6068: insert
    tree[31] = 'value_650365'
    reference[31] = 'value_650365'
    assert tree.invariants(), "Invariants failed at step 6068"

    # Operation 6069: get
    assert tree.invariants(), "Invariants failed at step 6069"

    # Operation 6070: insert
    tree[7275] = 'value_675218'
    reference[7275] = 'value_675218'
    assert tree.invariants(), "Invariants failed at step 6070"

    # Operation 6071: delete
    del tree[31]
    del reference[31]
    assert tree.invariants(), "Invariants failed at step 6071"

    # Operation 6072: get
    assert tree.invariants(), "Invariants failed at step 6072"

    # Operation 6073: update
    tree[7275] = 'value_532074'
    reference[7275] = 'value_532074'
    assert tree.invariants(), "Invariants failed at step 6073"

    # Operation 6074: insert
    tree[6187] = 'value_281167'
    reference[6187] = 'value_281167'
    assert tree.invariants(), "Invariants failed at step 6074"

    # Operation 6075: update
    tree[4019] = 'value_716837'
    reference[4019] = 'value_716837'
    assert tree.invariants(), "Invariants failed at step 6075"

    # Operation 6076: insert
    tree[7400] = 'value_801375'
    reference[7400] = 'value_801375'
    assert tree.invariants(), "Invariants failed at step 6076"

    # Operation 6077: get
    assert tree.invariants(), "Invariants failed at step 6077"

    # Operation 6078: insert
    tree[6254] = 'value_968075'
    reference[6254] = 'value_968075'
    assert tree.invariants(), "Invariants failed at step 6078"

    # Operation 6079: batch_delete
    keys_to_delete = [7400, 6187, 5908, 4019]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6079"

    # Operation 6080: delete
    del tree[7275]
    del reference[7275]
    assert tree.invariants(), "Invariants failed at step 6080"

    # Operation 6081: insert
    tree[9488] = 'value_126753'
    reference[9488] = 'value_126753'
    assert tree.invariants(), "Invariants failed at step 6081"

    # Operation 6082: insert
    tree[4444] = 'value_385643'
    reference[4444] = 'value_385643'
    assert tree.invariants(), "Invariants failed at step 6082"

    # Operation 6083: insert
    tree[7987] = 'value_402694'
    reference[7987] = 'value_402694'
    assert tree.invariants(), "Invariants failed at step 6083"

    # Operation 6084: delete
    del tree[4444]
    del reference[4444]
    assert tree.invariants(), "Invariants failed at step 6084"

    # Operation 6085: delete
    del tree[7987]
    del reference[7987]
    assert tree.invariants(), "Invariants failed at step 6085"

    # Operation 6086: insert
    tree[4350] = 'value_176919'
    reference[4350] = 'value_176919'
    assert tree.invariants(), "Invariants failed at step 6086"

    # Operation 6087: insert
    tree[2650] = 'value_469628'
    reference[2650] = 'value_469628'
    assert tree.invariants(), "Invariants failed at step 6087"

    # Operation 6088: get
    assert tree.invariants(), "Invariants failed at step 6088"

    # Operation 6089: get
    assert tree.invariants(), "Invariants failed at step 6089"

    # Operation 6090: insert
    tree[6079] = 'value_157817'
    reference[6079] = 'value_157817'
    assert tree.invariants(), "Invariants failed at step 6090"

    # Operation 6091: batch_delete
    keys_to_delete = [7824, 6127, 6254, 6079]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6091"

    # Operation 6092: delete
    del tree[4350]
    del reference[4350]
    assert tree.invariants(), "Invariants failed at step 6092"

    # Operation 6093: delete
    del tree[9488]
    del reference[9488]
    assert tree.invariants(), "Invariants failed at step 6093"

    # Operation 6094: get
    assert tree.invariants(), "Invariants failed at step 6094"

    # Operation 6095: delete
    del tree[2650]
    del reference[2650]
    assert tree.invariants(), "Invariants failed at step 6095"

    # Operation 6096: insert
    tree[5194] = 'value_997701'
    reference[5194] = 'value_997701'
    assert tree.invariants(), "Invariants failed at step 6096"

    # Operation 6097: delete
    del tree[5194]
    del reference[5194]
    assert tree.invariants(), "Invariants failed at step 6097"

    # Operation 6098: get
    assert tree.invariants(), "Invariants failed at step 6098"

    # Operation 6099: insert
    tree[9353] = 'value_67140'
    reference[9353] = 'value_67140'
    assert tree.invariants(), "Invariants failed at step 6099"

    # Operation 6100: insert
    tree[9332] = 'value_30287'
    reference[9332] = 'value_30287'
    assert tree.invariants(), "Invariants failed at step 6100"

    # Operation 6101: delete
    del tree[9332]
    del reference[9332]
    assert tree.invariants(), "Invariants failed at step 6101"

    # Operation 6102: delete
    del tree[9353]
    del reference[9353]
    assert tree.invariants(), "Invariants failed at step 6102"

    # Operation 6103: get
    assert tree.invariants(), "Invariants failed at step 6103"

    # Operation 6104: insert
    tree[7996] = 'value_899527'
    reference[7996] = 'value_899527'
    assert tree.invariants(), "Invariants failed at step 6104"

    # Operation 6105: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6105"

    # Operation 6106: delete
    del tree[7996]
    del reference[7996]
    assert tree.invariants(), "Invariants failed at step 6106"

    # Operation 6107: get
    assert tree.invariants(), "Invariants failed at step 6107"

    # Operation 6108: insert
    tree[6336] = 'value_40671'
    reference[6336] = 'value_40671'
    assert tree.invariants(), "Invariants failed at step 6108"

    # Operation 6109: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6109"

    # Operation 6110: get
    assert tree.invariants(), "Invariants failed at step 6110"

    # Operation 6111: delete
    del tree[6336]
    del reference[6336]
    assert tree.invariants(), "Invariants failed at step 6111"

    # Operation 6112: insert
    tree[5256] = 'value_737629'
    reference[5256] = 'value_737629'
    assert tree.invariants(), "Invariants failed at step 6112"

    # Operation 6113: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6113"

    # Operation 6114: get
    assert tree.invariants(), "Invariants failed at step 6114"

    # Operation 6115: get
    assert tree.invariants(), "Invariants failed at step 6115"

    # Operation 6116: insert
    tree[1805] = 'value_498671'
    reference[1805] = 'value_498671'
    assert tree.invariants(), "Invariants failed at step 6116"

    # Operation 6117: update
    tree[5256] = 'value_151418'
    reference[5256] = 'value_151418'
    assert tree.invariants(), "Invariants failed at step 6117"

    # Operation 6118: insert
    tree[9284] = 'value_880420'
    reference[9284] = 'value_880420'
    assert tree.invariants(), "Invariants failed at step 6118"

    # Operation 6119: delete
    del tree[9284]
    del reference[9284]
    assert tree.invariants(), "Invariants failed at step 6119"

    # Operation 6120: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6120"

    # Operation 6121: delete
    del tree[5256]
    del reference[5256]
    assert tree.invariants(), "Invariants failed at step 6121"

    # Operation 6122: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6122"

    # Operation 6123: insert
    tree[6747] = 'value_506144'
    reference[6747] = 'value_506144'
    assert tree.invariants(), "Invariants failed at step 6123"

    # Operation 6124: insert
    tree[185] = 'value_308063'
    reference[185] = 'value_308063'
    assert tree.invariants(), "Invariants failed at step 6124"

    # Operation 6125: get
    assert tree.invariants(), "Invariants failed at step 6125"

    # Operation 6126: get
    assert tree.invariants(), "Invariants failed at step 6126"

    # Operation 6127: insert
    tree[8776] = 'value_56927'
    reference[8776] = 'value_56927'
    assert tree.invariants(), "Invariants failed at step 6127"

    # Operation 6128: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6128"

    # Operation 6129: insert
    tree[7160] = 'value_782541'
    reference[7160] = 'value_782541'
    assert tree.invariants(), "Invariants failed at step 6129"

    # Operation 6130: get
    assert tree.invariants(), "Invariants failed at step 6130"

    # Operation 6131: insert
    tree[8168] = 'value_938636'
    reference[8168] = 'value_938636'
    assert tree.invariants(), "Invariants failed at step 6131"

    # Operation 6132: batch_delete
    keys_to_delete = [8168, 7081, 8396, 1805, 7160]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6132"

    # Operation 6133: delete
    del tree[185]
    del reference[185]
    assert tree.invariants(), "Invariants failed at step 6133"

    # Operation 6134: delete
    del tree[8776]
    del reference[8776]
    assert tree.invariants(), "Invariants failed at step 6134"

    # Operation 6135: insert
    tree[7664] = 'value_537294'
    reference[7664] = 'value_537294'
    assert tree.invariants(), "Invariants failed at step 6135"

    # Operation 6136: delete
    del tree[6747]
    del reference[6747]
    assert tree.invariants(), "Invariants failed at step 6136"

    # Operation 6137: get
    assert tree.invariants(), "Invariants failed at step 6137"

    # Operation 6138: get
    assert tree.invariants(), "Invariants failed at step 6138"

    # Operation 6139: update
    tree[7664] = 'value_572170'
    reference[7664] = 'value_572170'
    assert tree.invariants(), "Invariants failed at step 6139"

    # Operation 6140: insert
    tree[5820] = 'value_911829'
    reference[5820] = 'value_911829'
    assert tree.invariants(), "Invariants failed at step 6140"

    # Operation 6141: get
    assert tree.invariants(), "Invariants failed at step 6141"

    # Operation 6142: get
    assert tree.invariants(), "Invariants failed at step 6142"

    # Operation 6143: delete
    del tree[7664]
    del reference[7664]
    assert tree.invariants(), "Invariants failed at step 6143"

    # Operation 6144: get
    assert tree.invariants(), "Invariants failed at step 6144"

    # Operation 6145: delete
    del tree[5820]
    del reference[5820]
    assert tree.invariants(), "Invariants failed at step 6145"

    # Operation 6146: insert
    tree[4782] = 'value_997208'
    reference[4782] = 'value_997208'
    assert tree.invariants(), "Invariants failed at step 6146"

    # Operation 6147: update
    tree[4782] = 'value_863515'
    reference[4782] = 'value_863515'
    assert tree.invariants(), "Invariants failed at step 6147"

    # Operation 6148: delete
    del tree[4782]
    del reference[4782]
    assert tree.invariants(), "Invariants failed at step 6148"

    # Operation 6149: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6149"

    # Operation 6150: insert
    tree[8802] = 'value_689976'
    reference[8802] = 'value_689976'
    assert tree.invariants(), "Invariants failed at step 6150"

    # Operation 6151: update
    tree[8802] = 'value_358392'
    reference[8802] = 'value_358392'
    assert tree.invariants(), "Invariants failed at step 6151"

    # Operation 6152: get
    assert tree.invariants(), "Invariants failed at step 6152"

    # Operation 6153: delete
    del tree[8802]
    del reference[8802]
    assert tree.invariants(), "Invariants failed at step 6153"

    # Operation 6154: insert
    tree[4120] = 'value_300864'
    reference[4120] = 'value_300864'
    assert tree.invariants(), "Invariants failed at step 6154"

    # Operation 6155: update
    tree[4120] = 'value_172808'
    reference[4120] = 'value_172808'
    assert tree.invariants(), "Invariants failed at step 6155"

    # Operation 6156: delete
    del tree[4120]
    del reference[4120]
    assert tree.invariants(), "Invariants failed at step 6156"

    # Operation 6157: insert
    tree[9910] = 'value_887046'
    reference[9910] = 'value_887046'
    assert tree.invariants(), "Invariants failed at step 6157"

    # Operation 6158: insert
    tree[5121] = 'value_379024'
    reference[5121] = 'value_379024'
    assert tree.invariants(), "Invariants failed at step 6158"

    # Operation 6159: get
    assert tree.invariants(), "Invariants failed at step 6159"

    # Operation 6160: update
    tree[9910] = 'value_866845'
    reference[9910] = 'value_866845'
    assert tree.invariants(), "Invariants failed at step 6160"

    # Operation 6161: get
    assert tree.invariants(), "Invariants failed at step 6161"

    # Operation 6162: get
    assert tree.invariants(), "Invariants failed at step 6162"

    # Operation 6163: delete
    del tree[5121]
    del reference[5121]
    assert tree.invariants(), "Invariants failed at step 6163"

    # Operation 6164: update
    tree[9910] = 'value_261083'
    reference[9910] = 'value_261083'
    assert tree.invariants(), "Invariants failed at step 6164"

    # Operation 6165: delete
    del tree[9910]
    del reference[9910]
    assert tree.invariants(), "Invariants failed at step 6165"

    # Operation 6166: get
    assert tree.invariants(), "Invariants failed at step 6166"

    # Operation 6167: get
    assert tree.invariants(), "Invariants failed at step 6167"

    # Operation 6168: insert
    tree[4141] = 'value_409492'
    reference[4141] = 'value_409492'
    assert tree.invariants(), "Invariants failed at step 6168"

    # Operation 6169: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6169"

    # Operation 6170: get
    assert tree.invariants(), "Invariants failed at step 6170"

    # Operation 6171: get
    assert tree.invariants(), "Invariants failed at step 6171"

    # Operation 6172: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6172"

    # Operation 6173: insert
    tree[1324] = 'value_301358'
    reference[1324] = 'value_301358'
    assert tree.invariants(), "Invariants failed at step 6173"

    # Operation 6174: delete
    del tree[1324]
    del reference[1324]
    assert tree.invariants(), "Invariants failed at step 6174"

    # Operation 6175: get
    assert tree.invariants(), "Invariants failed at step 6175"

    # Operation 6176: update
    tree[4141] = 'value_544965'
    reference[4141] = 'value_544965'
    assert tree.invariants(), "Invariants failed at step 6176"

    # Operation 6177: insert
    tree[3570] = 'value_997372'
    reference[3570] = 'value_997372'
    assert tree.invariants(), "Invariants failed at step 6177"

    # Operation 6178: get
    assert tree.invariants(), "Invariants failed at step 6178"

    # Operation 6179: get
    assert tree.invariants(), "Invariants failed at step 6179"

    # Operation 6180: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6180"

    # Operation 6181: insert
    tree[8696] = 'value_779327'
    reference[8696] = 'value_779327'
    assert tree.invariants(), "Invariants failed at step 6181"

    # Operation 6182: get
    assert tree.invariants(), "Invariants failed at step 6182"

    # Operation 6183: get
    assert tree.invariants(), "Invariants failed at step 6183"

    # Operation 6184: insert
    tree[5145] = 'value_687059'
    reference[5145] = 'value_687059'
    assert tree.invariants(), "Invariants failed at step 6184"

    # Operation 6185: delete
    del tree[5145]
    del reference[5145]
    assert tree.invariants(), "Invariants failed at step 6185"

    # Operation 6186: get
    assert tree.invariants(), "Invariants failed at step 6186"

    # Operation 6187: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6187"

    # Operation 6188: insert
    tree[8856] = 'value_93140'
    reference[8856] = 'value_93140'
    assert tree.invariants(), "Invariants failed at step 6188"

    # Operation 6189: get
    assert tree.invariants(), "Invariants failed at step 6189"

    # Operation 6190: delete
    del tree[3570]
    del reference[3570]
    assert tree.invariants(), "Invariants failed at step 6190"

    # Operation 6191: insert
    tree[6249] = 'value_798983'
    reference[6249] = 'value_798983'
    assert tree.invariants(), "Invariants failed at step 6191"

    # Operation 6192: insert
    tree[4363] = 'value_885839'
    reference[4363] = 'value_885839'
    assert tree.invariants(), "Invariants failed at step 6192"

    # Operation 6193: update
    tree[6249] = 'value_614861'
    reference[6249] = 'value_614861'
    assert tree.invariants(), "Invariants failed at step 6193"

    # Operation 6194: batch_delete
    keys_to_delete = [8856, 2, 8418, 4141]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6194"

    # Operation 6195: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6195"

    # Operation 6196: delete
    del tree[8696]
    del reference[8696]
    assert tree.invariants(), "Invariants failed at step 6196"

    # Operation 6197: insert
    tree[7814] = 'value_79028'
    reference[7814] = 'value_79028'
    assert tree.invariants(), "Invariants failed at step 6197"

    # Operation 6198: get
    assert tree.invariants(), "Invariants failed at step 6198"

    # Operation 6199: get
    assert tree.invariants(), "Invariants failed at step 6199"

    # Operation 6200: insert
    tree[8574] = 'value_984005'
    reference[8574] = 'value_984005'
    assert tree.invariants(), "Invariants failed at step 6200"

    # Operation 6201: delete
    del tree[8574]
    del reference[8574]
    assert tree.invariants(), "Invariants failed at step 6201"

    # Operation 6202: delete
    del tree[7814]
    del reference[7814]
    assert tree.invariants(), "Invariants failed at step 6202"

    # Operation 6203: delete
    del tree[4363]
    del reference[4363]
    assert tree.invariants(), "Invariants failed at step 6203"

    # Operation 6204: delete
    del tree[6249]
    del reference[6249]
    assert tree.invariants(), "Invariants failed at step 6204"

    # Operation 6205: insert
    tree[8049] = 'value_194796'
    reference[8049] = 'value_194796'
    assert tree.invariants(), "Invariants failed at step 6205"

    # Operation 6206: get
    assert tree.invariants(), "Invariants failed at step 6206"

    # Operation 6207: get
    assert tree.invariants(), "Invariants failed at step 6207"

    # Operation 6208: get
    assert tree.invariants(), "Invariants failed at step 6208"

    # Operation 6209: insert
    tree[6791] = 'value_254497'
    reference[6791] = 'value_254497'
    assert tree.invariants(), "Invariants failed at step 6209"

    # Operation 6210: insert
    tree[8808] = 'value_595664'
    reference[8808] = 'value_595664'
    assert tree.invariants(), "Invariants failed at step 6210"

    # Operation 6211: get
    assert tree.invariants(), "Invariants failed at step 6211"

    # Operation 6212: delete
    del tree[8049]
    del reference[8049]
    assert tree.invariants(), "Invariants failed at step 6212"

    # Operation 6213: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6213"

    # Operation 6214: insert
    tree[6942] = 'value_807704'
    reference[6942] = 'value_807704'
    assert tree.invariants(), "Invariants failed at step 6214"

    # Operation 6215: get
    assert tree.invariants(), "Invariants failed at step 6215"

    # Operation 6216: delete
    del tree[6791]
    del reference[6791]
    assert tree.invariants(), "Invariants failed at step 6216"

    # Operation 6217: insert
    tree[9212] = 'value_912206'
    reference[9212] = 'value_912206'
    assert tree.invariants(), "Invariants failed at step 6217"

    # Operation 6218: get
    assert tree.invariants(), "Invariants failed at step 6218"

    # Operation 6219: insert
    tree[4806] = 'value_990232'
    reference[4806] = 'value_990232'
    assert tree.invariants(), "Invariants failed at step 6219"

    # Operation 6220: update
    tree[6942] = 'value_851188'
    reference[6942] = 'value_851188'
    assert tree.invariants(), "Invariants failed at step 6220"

    # Operation 6221: insert
    tree[1272] = 'value_813221'
    reference[1272] = 'value_813221'
    assert tree.invariants(), "Invariants failed at step 6221"

    # Operation 6222: delete
    del tree[4806]
    del reference[4806]
    assert tree.invariants(), "Invariants failed at step 6222"

    # Operation 6223: get
    assert tree.invariants(), "Invariants failed at step 6223"

    # Operation 6224: get
    assert tree.invariants(), "Invariants failed at step 6224"

    # Operation 6225: insert
    tree[7046] = 'value_235563'
    reference[7046] = 'value_235563'
    assert tree.invariants(), "Invariants failed at step 6225"

    # Operation 6226: insert
    tree[5201] = 'value_160483'
    reference[5201] = 'value_160483'
    assert tree.invariants(), "Invariants failed at step 6226"

    # Operation 6227: insert
    tree[4186] = 'value_257913'
    reference[4186] = 'value_257913'
    assert tree.invariants(), "Invariants failed at step 6227"

    # Operation 6228: insert
    tree[8583] = 'value_793907'
    reference[8583] = 'value_793907'
    assert tree.invariants(), "Invariants failed at step 6228"

    # Operation 6229: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6229"

    # Operation 6230: update
    tree[1272] = 'value_905381'
    reference[1272] = 'value_905381'
    assert tree.invariants(), "Invariants failed at step 6230"

    # Operation 6231: insert
    tree[5882] = 'value_350748'
    reference[5882] = 'value_350748'
    assert tree.invariants(), "Invariants failed at step 6231"

    # Operation 6232: insert
    tree[8827] = 'value_365128'
    reference[8827] = 'value_365128'
    assert tree.invariants(), "Invariants failed at step 6232"

    # Operation 6233: get
    assert tree.invariants(), "Invariants failed at step 6233"

    # Operation 6234: update
    tree[7046] = 'value_173265'
    reference[7046] = 'value_173265'
    assert tree.invariants(), "Invariants failed at step 6234"

    # Operation 6235: delete
    del tree[1272]
    del reference[1272]
    assert tree.invariants(), "Invariants failed at step 6235"

    # Operation 6236: delete
    del tree[7046]
    del reference[7046]
    assert tree.invariants(), "Invariants failed at step 6236"

    # Operation 6237: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6237"

    # Operation 6238: batch_delete
    keys_to_delete = [2949, 8583, 8808, 9073, 5882, 8827]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6238"

    # Operation 6239: insert
    tree[3634] = 'value_538610'
    reference[3634] = 'value_538610'
    assert tree.invariants(), "Invariants failed at step 6239"

    # Operation 6240: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6240"

    # Operation 6241: insert
    tree[1464] = 'value_651038'
    reference[1464] = 'value_651038'
    assert tree.invariants(), "Invariants failed at step 6241"

    # Operation 6242: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6242"

    # Operation 6243: get
    assert tree.invariants(), "Invariants failed at step 6243"

    # Operation 6244: get
    assert tree.invariants(), "Invariants failed at step 6244"

    # Operation 6245: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6245"

    # Operation 6246: insert
    tree[4294] = 'value_145032'
    reference[4294] = 'value_145032'
    assert tree.invariants(), "Invariants failed at step 6246"

    # Operation 6247: insert
    tree[9253] = 'value_93483'
    reference[9253] = 'value_93483'
    assert tree.invariants(), "Invariants failed at step 6247"

    # Operation 6248: insert
    tree[6530] = 'value_119823'
    reference[6530] = 'value_119823'
    assert tree.invariants(), "Invariants failed at step 6248"

    # Operation 6249: get
    assert tree.invariants(), "Invariants failed at step 6249"

    # Operation 6250: insert
    tree[994] = 'value_241915'
    reference[994] = 'value_241915'
    assert tree.invariants(), "Invariants failed at step 6250"

    # Operation 6251: insert
    tree[7659] = 'value_90005'
    reference[7659] = 'value_90005'
    assert tree.invariants(), "Invariants failed at step 6251"

    # Operation 6252: get
    assert tree.invariants(), "Invariants failed at step 6252"

    # Operation 6253: delete
    del tree[9212]
    del reference[9212]
    assert tree.invariants(), "Invariants failed at step 6253"

    # Operation 6254: delete
    del tree[7659]
    del reference[7659]
    assert tree.invariants(), "Invariants failed at step 6254"

    # Operation 6255: get
    assert tree.invariants(), "Invariants failed at step 6255"

    # Operation 6256: get
    assert tree.invariants(), "Invariants failed at step 6256"

    # Operation 6257: update
    tree[994] = 'value_829067'
    reference[994] = 'value_829067'
    assert tree.invariants(), "Invariants failed at step 6257"

    # Operation 6258: delete
    del tree[3634]
    del reference[3634]
    assert tree.invariants(), "Invariants failed at step 6258"

    # Operation 6259: insert
    tree[9858] = 'value_831691'
    reference[9858] = 'value_831691'
    assert tree.invariants(), "Invariants failed at step 6259"

    # Operation 6260: update
    tree[5201] = 'value_136691'
    reference[5201] = 'value_136691'
    assert tree.invariants(), "Invariants failed at step 6260"

    # Operation 6261: insert
    tree[2593] = 'value_914295'
    reference[2593] = 'value_914295'
    assert tree.invariants(), "Invariants failed at step 6261"

    # Operation 6262: insert
    tree[2937] = 'value_866080'
    reference[2937] = 'value_866080'
    assert tree.invariants(), "Invariants failed at step 6262"

    # Operation 6263: update
    tree[6530] = 'value_419094'
    reference[6530] = 'value_419094'
    assert tree.invariants(), "Invariants failed at step 6263"

    # Operation 6264: insert
    tree[4371] = 'value_502107'
    reference[4371] = 'value_502107'
    assert tree.invariants(), "Invariants failed at step 6264"

    # Operation 6265: insert
    tree[5798] = 'value_676498'
    reference[5798] = 'value_676498'
    assert tree.invariants(), "Invariants failed at step 6265"

    # Operation 6266: insert
    tree[2113] = 'value_308010'
    reference[2113] = 'value_308010'
    assert tree.invariants(), "Invariants failed at step 6266"

    # Operation 6267: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6267"

    # Operation 6268: get
    assert tree.invariants(), "Invariants failed at step 6268"

    # Operation 6269: delete
    del tree[2593]
    del reference[2593]
    assert tree.invariants(), "Invariants failed at step 6269"

    # Operation 6270: delete
    del tree[2937]
    del reference[2937]
    assert tree.invariants(), "Invariants failed at step 6270"

    # Operation 6271: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6271"

    # Operation 6272: insert
    tree[7520] = 'value_198924'
    reference[7520] = 'value_198924'
    assert tree.invariants(), "Invariants failed at step 6272"

    # Operation 6273: update
    tree[9253] = 'value_777052'
    reference[9253] = 'value_777052'
    assert tree.invariants(), "Invariants failed at step 6273"

    # Operation 6274: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6274"

    # Operation 6275: update
    tree[4294] = 'value_592518'
    reference[4294] = 'value_592518'
    assert tree.invariants(), "Invariants failed at step 6275"

    # Operation 6276: get
    assert tree.invariants(), "Invariants failed at step 6276"

    # Operation 6277: insert
    tree[27] = 'value_848232'
    reference[27] = 'value_848232'
    assert tree.invariants(), "Invariants failed at step 6277"

    # Operation 6278: delete
    del tree[9858]
    del reference[9858]
    assert tree.invariants(), "Invariants failed at step 6278"

    # Operation 6279: batch_delete
    keys_to_delete = [6530, 9253, 5798, 3247, 4371, 27, 6942, 4223]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6279"

    # Operation 6280: delete
    del tree[4294]
    del reference[4294]
    assert tree.invariants(), "Invariants failed at step 6280"

    # Operation 6281: batch_delete
    keys_to_delete = [7520, 994, 4451, 588, 5201]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6281"

    # Operation 6282: delete
    del tree[4186]
    del reference[4186]
    assert tree.invariants(), "Invariants failed at step 6282"

    # Operation 6283: insert
    tree[9045] = 'value_647801'
    reference[9045] = 'value_647801'
    assert tree.invariants(), "Invariants failed at step 6283"

    # Operation 6284: get
    assert tree.invariants(), "Invariants failed at step 6284"

    # Operation 6285: get
    assert tree.invariants(), "Invariants failed at step 6285"

    # Operation 6286: insert
    tree[9840] = 'value_978772'
    reference[9840] = 'value_978772'
    assert tree.invariants(), "Invariants failed at step 6286"

    # Operation 6287: insert
    tree[2473] = 'value_221457'
    reference[2473] = 'value_221457'
    assert tree.invariants(), "Invariants failed at step 6287"

    # Operation 6288: insert
    tree[2866] = 'value_89041'
    reference[2866] = 'value_89041'
    assert tree.invariants(), "Invariants failed at step 6288"

    # Operation 6289: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6289"

    # Operation 6290: get
    assert tree.invariants(), "Invariants failed at step 6290"

    # Operation 6291: update
    tree[2473] = 'value_542098'
    reference[2473] = 'value_542098'
    assert tree.invariants(), "Invariants failed at step 6291"

    # Operation 6292: get
    assert tree.invariants(), "Invariants failed at step 6292"

    # Operation 6293: update
    tree[1464] = 'value_928491'
    reference[1464] = 'value_928491'
    assert tree.invariants(), "Invariants failed at step 6293"

    # Operation 6294: insert
    tree[3383] = 'value_980879'
    reference[3383] = 'value_980879'
    assert tree.invariants(), "Invariants failed at step 6294"

    # Operation 6295: update
    tree[2866] = 'value_120870'
    reference[2866] = 'value_120870'
    assert tree.invariants(), "Invariants failed at step 6295"

    # Operation 6296: update
    tree[2473] = 'value_339132'
    reference[2473] = 'value_339132'
    assert tree.invariants(), "Invariants failed at step 6296"

    # Operation 6297: get
    assert tree.invariants(), "Invariants failed at step 6297"

    # Operation 6298: get
    assert tree.invariants(), "Invariants failed at step 6298"

    # Operation 6299: delete
    del tree[9840]
    del reference[9840]
    assert tree.invariants(), "Invariants failed at step 6299"

    # Operation 6300: delete
    del tree[2113]
    del reference[2113]
    assert tree.invariants(), "Invariants failed at step 6300"

    # Operation 6301: insert
    tree[2617] = 'value_861983'
    reference[2617] = 'value_861983'
    assert tree.invariants(), "Invariants failed at step 6301"

    # Operation 6302: batch_delete
    keys_to_delete = [2439, 9045, 3383, 1464, 6491]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6302"

    # Operation 6303: delete
    del tree[2473]
    del reference[2473]
    assert tree.invariants(), "Invariants failed at step 6303"

    # Operation 6304: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6304"

    # Operation 6305: delete
    del tree[2866]
    del reference[2866]
    assert tree.invariants(), "Invariants failed at step 6305"

    # Operation 6306: insert
    tree[5993] = 'value_707285'
    reference[5993] = 'value_707285'
    assert tree.invariants(), "Invariants failed at step 6306"

    # Operation 6307: insert
    tree[7029] = 'value_237840'
    reference[7029] = 'value_237840'
    assert tree.invariants(), "Invariants failed at step 6307"

    # Operation 6308: insert
    tree[869] = 'value_662560'
    reference[869] = 'value_662560'
    assert tree.invariants(), "Invariants failed at step 6308"

    # Operation 6309: delete
    del tree[2617]
    del reference[2617]
    assert tree.invariants(), "Invariants failed at step 6309"

    # Operation 6310: insert
    tree[1954] = 'value_800684'
    reference[1954] = 'value_800684'
    assert tree.invariants(), "Invariants failed at step 6310"

    # Operation 6311: delete
    del tree[5993]
    del reference[5993]
    assert tree.invariants(), "Invariants failed at step 6311"

    # Operation 6312: insert
    tree[6543] = 'value_74982'
    reference[6543] = 'value_74982'
    assert tree.invariants(), "Invariants failed at step 6312"

    # Operation 6313: insert
    tree[9886] = 'value_466152'
    reference[9886] = 'value_466152'
    assert tree.invariants(), "Invariants failed at step 6313"

    # Operation 6314: insert
    tree[6240] = 'value_589290'
    reference[6240] = 'value_589290'
    assert tree.invariants(), "Invariants failed at step 6314"

    # Operation 6315: delete
    del tree[1954]
    del reference[1954]
    assert tree.invariants(), "Invariants failed at step 6315"

    # Operation 6316: batch_delete
    keys_to_delete = [4912, 8299, 869, 9886]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6316"

    # Operation 6317: update
    tree[6543] = 'value_426054'
    reference[6543] = 'value_426054'
    assert tree.invariants(), "Invariants failed at step 6317"

    # Operation 6318: delete
    del tree[7029]
    del reference[7029]
    assert tree.invariants(), "Invariants failed at step 6318"

    # Operation 6319: insert
    tree[6882] = 'value_582161'
    reference[6882] = 'value_582161'
    assert tree.invariants(), "Invariants failed at step 6319"

    # Operation 6320: update
    tree[6240] = 'value_257674'
    reference[6240] = 'value_257674'
    assert tree.invariants(), "Invariants failed at step 6320"

    # Operation 6321: get
    assert tree.invariants(), "Invariants failed at step 6321"

    # Operation 6322: get
    assert tree.invariants(), "Invariants failed at step 6322"

    # Operation 6323: insert
    tree[3108] = 'value_549179'
    reference[3108] = 'value_549179'
    assert tree.invariants(), "Invariants failed at step 6323"

    # Operation 6324: get
    assert tree.invariants(), "Invariants failed at step 6324"

    # Operation 6325: update
    tree[6882] = 'value_728956'
    reference[6882] = 'value_728956'
    assert tree.invariants(), "Invariants failed at step 6325"

    # Operation 6326: get
    assert tree.invariants(), "Invariants failed at step 6326"

    # Operation 6327: get
    assert tree.invariants(), "Invariants failed at step 6327"

    # Operation 6328: delete
    del tree[3108]
    del reference[3108]
    assert tree.invariants(), "Invariants failed at step 6328"

    # Operation 6329: delete
    del tree[6240]
    del reference[6240]
    assert tree.invariants(), "Invariants failed at step 6329"

    # Operation 6330: delete
    del tree[6882]
    del reference[6882]
    assert tree.invariants(), "Invariants failed at step 6330"

    # Operation 6331: delete
    del tree[6543]
    del reference[6543]
    assert tree.invariants(), "Invariants failed at step 6331"

    # Operation 6332: insert
    tree[1616] = 'value_359729'
    reference[1616] = 'value_359729'
    assert tree.invariants(), "Invariants failed at step 6332"

    # Operation 6333: update
    tree[1616] = 'value_193572'
    reference[1616] = 'value_193572'
    assert tree.invariants(), "Invariants failed at step 6333"

    # Operation 6334: insert
    tree[9055] = 'value_231231'
    reference[9055] = 'value_231231'
    assert tree.invariants(), "Invariants failed at step 6334"

    # Operation 6335: insert
    tree[9234] = 'value_260744'
    reference[9234] = 'value_260744'
    assert tree.invariants(), "Invariants failed at step 6335"

    # Operation 6336: insert
    tree[6513] = 'value_727957'
    reference[6513] = 'value_727957'
    assert tree.invariants(), "Invariants failed at step 6336"

    # Operation 6337: get
    assert tree.invariants(), "Invariants failed at step 6337"

    # Operation 6338: delete
    del tree[9234]
    del reference[9234]
    assert tree.invariants(), "Invariants failed at step 6338"

    # Operation 6339: insert
    tree[1628] = 'value_58982'
    reference[1628] = 'value_58982'
    assert tree.invariants(), "Invariants failed at step 6339"

    # Operation 6340: update
    tree[6513] = 'value_595054'
    reference[6513] = 'value_595054'
    assert tree.invariants(), "Invariants failed at step 6340"

    # Operation 6341: delete
    del tree[6513]
    del reference[6513]
    assert tree.invariants(), "Invariants failed at step 6341"

    # Operation 6342: update
    tree[9055] = 'value_258908'
    reference[9055] = 'value_258908'
    assert tree.invariants(), "Invariants failed at step 6342"

    # Operation 6343: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6343"

    # Operation 6344: delete
    del tree[1628]
    del reference[1628]
    assert tree.invariants(), "Invariants failed at step 6344"

    # Operation 6345: delete
    del tree[9055]
    del reference[9055]
    assert tree.invariants(), "Invariants failed at step 6345"

    # Operation 6346: insert
    tree[9227] = 'value_863084'
    reference[9227] = 'value_863084'
    assert tree.invariants(), "Invariants failed at step 6346"

    # Operation 6347: update
    tree[9227] = 'value_34533'
    reference[9227] = 'value_34533'
    assert tree.invariants(), "Invariants failed at step 6347"

    # Operation 6348: delete
    del tree[9227]
    del reference[9227]
    assert tree.invariants(), "Invariants failed at step 6348"

    # Operation 6349: update
    tree[1616] = 'value_878820'
    reference[1616] = 'value_878820'
    assert tree.invariants(), "Invariants failed at step 6349"

    # Operation 6350: delete
    del tree[1616]
    del reference[1616]
    assert tree.invariants(), "Invariants failed at step 6350"

    # Operation 6351: insert
    tree[5292] = 'value_985650'
    reference[5292] = 'value_985650'
    assert tree.invariants(), "Invariants failed at step 6351"

    # Operation 6352: insert
    tree[5967] = 'value_122282'
    reference[5967] = 'value_122282'
    assert tree.invariants(), "Invariants failed at step 6352"

    # Operation 6353: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6353"

    # Operation 6354: get
    assert tree.invariants(), "Invariants failed at step 6354"

    # Operation 6355: get
    assert tree.invariants(), "Invariants failed at step 6355"

    # Operation 6356: delete
    del tree[5292]
    del reference[5292]
    assert tree.invariants(), "Invariants failed at step 6356"

    # Operation 6357: delete
    del tree[5967]
    del reference[5967]
    assert tree.invariants(), "Invariants failed at step 6357"

    # Operation 6358: insert
    tree[4563] = 'value_654037'
    reference[4563] = 'value_654037'
    assert tree.invariants(), "Invariants failed at step 6358"

    # Operation 6359: insert
    tree[2725] = 'value_946177'
    reference[2725] = 'value_946177'
    assert tree.invariants(), "Invariants failed at step 6359"

    # Operation 6360: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6360"

    # Operation 6361: insert
    tree[9764] = 'value_854762'
    reference[9764] = 'value_854762'
    assert tree.invariants(), "Invariants failed at step 6361"

    # Operation 6362: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6362"

    # Operation 6363: insert
    tree[3227] = 'value_31582'
    reference[3227] = 'value_31582'
    assert tree.invariants(), "Invariants failed at step 6363"

    # Operation 6364: insert
    tree[6741] = 'value_822967'
    reference[6741] = 'value_822967'
    assert tree.invariants(), "Invariants failed at step 6364"

    # Operation 6365: insert
    tree[3523] = 'value_564039'
    reference[3523] = 'value_564039'
    assert tree.invariants(), "Invariants failed at step 6365"

    # Operation 6366: update
    tree[9764] = 'value_269187'
    reference[9764] = 'value_269187'
    assert tree.invariants(), "Invariants failed at step 6366"

    # Operation 6367: insert
    tree[301] = 'value_234576'
    reference[301] = 'value_234576'
    assert tree.invariants(), "Invariants failed at step 6367"

    # Operation 6368: delete
    del tree[301]
    del reference[301]
    assert tree.invariants(), "Invariants failed at step 6368"

    # Operation 6369: delete
    del tree[9764]
    del reference[9764]
    assert tree.invariants(), "Invariants failed at step 6369"

    # Operation 6370: update
    tree[3523] = 'value_837240'
    reference[3523] = 'value_837240'
    assert tree.invariants(), "Invariants failed at step 6370"

    # Operation 6371: get
    assert tree.invariants(), "Invariants failed at step 6371"

    # Operation 6372: update
    tree[4563] = 'value_154303'
    reference[4563] = 'value_154303'
    assert tree.invariants(), "Invariants failed at step 6372"

    # Operation 6373: get
    assert tree.invariants(), "Invariants failed at step 6373"

    # Operation 6374: insert
    tree[2427] = 'value_201268'
    reference[2427] = 'value_201268'
    assert tree.invariants(), "Invariants failed at step 6374"

    # Operation 6375: insert
    tree[5634] = 'value_770415'
    reference[5634] = 'value_770415'
    assert tree.invariants(), "Invariants failed at step 6375"

    # Operation 6376: insert
    tree[5007] = 'value_143649'
    reference[5007] = 'value_143649'
    assert tree.invariants(), "Invariants failed at step 6376"

    # Operation 6377: get
    assert tree.invariants(), "Invariants failed at step 6377"

    # Operation 6378: delete
    del tree[4563]
    del reference[4563]
    assert tree.invariants(), "Invariants failed at step 6378"

    # Operation 6379: update
    tree[2725] = 'value_662602'
    reference[2725] = 'value_662602'
    assert tree.invariants(), "Invariants failed at step 6379"

    # Operation 6380: batch_delete
    keys_to_delete = [2010, 2427, 3523, 2725]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6380"

    # Operation 6381: insert
    tree[4117] = 'value_897496'
    reference[4117] = 'value_897496'
    assert tree.invariants(), "Invariants failed at step 6381"

    # Operation 6382: update
    tree[4117] = 'value_605508'
    reference[4117] = 'value_605508'
    assert tree.invariants(), "Invariants failed at step 6382"

    # Operation 6383: batch_delete
    keys_to_delete = [3227, 6741, 4117, 9503]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6383"

    # Operation 6384: delete
    del tree[5634]
    del reference[5634]
    assert tree.invariants(), "Invariants failed at step 6384"

    # Operation 6385: get
    assert tree.invariants(), "Invariants failed at step 6385"

    # Operation 6386: insert
    tree[7829] = 'value_86490'
    reference[7829] = 'value_86490'
    assert tree.invariants(), "Invariants failed at step 6386"

    # Operation 6387: get
    assert tree.invariants(), "Invariants failed at step 6387"

    # Operation 6388: insert
    tree[3388] = 'value_528246'
    reference[3388] = 'value_528246'
    assert tree.invariants(), "Invariants failed at step 6388"

    # Operation 6389: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6389"

    # Operation 6390: insert
    tree[9129] = 'value_83591'
    reference[9129] = 'value_83591'
    assert tree.invariants(), "Invariants failed at step 6390"

    # Operation 6391: delete
    del tree[3388]
    del reference[3388]
    assert tree.invariants(), "Invariants failed at step 6391"

    # Operation 6392: update
    tree[9129] = 'value_207735'
    reference[9129] = 'value_207735'
    assert tree.invariants(), "Invariants failed at step 6392"

    # Operation 6393: delete
    del tree[7829]
    del reference[7829]
    assert tree.invariants(), "Invariants failed at step 6393"

    # Operation 6394: update
    tree[5007] = 'value_358644'
    reference[5007] = 'value_358644'
    assert tree.invariants(), "Invariants failed at step 6394"

    # Operation 6395: update
    tree[5007] = 'value_496757'
    reference[5007] = 'value_496757'
    assert tree.invariants(), "Invariants failed at step 6395"

    # Operation 6396: insert
    tree[8890] = 'value_360884'
    reference[8890] = 'value_360884'
    assert tree.invariants(), "Invariants failed at step 6396"

    # Operation 6397: insert
    tree[7159] = 'value_843881'
    reference[7159] = 'value_843881'
    assert tree.invariants(), "Invariants failed at step 6397"

    # Operation 6398: get
    assert tree.invariants(), "Invariants failed at step 6398"

    # Operation 6399: update
    tree[7159] = 'value_644569'
    reference[7159] = 'value_644569'
    assert tree.invariants(), "Invariants failed at step 6399"

    # Operation 6400: get
    assert tree.invariants(), "Invariants failed at step 6400"

    # Operation 6401: insert
    tree[1716] = 'value_243324'
    reference[1716] = 'value_243324'
    assert tree.invariants(), "Invariants failed at step 6401"

    # Operation 6402: insert
    tree[3017] = 'value_235212'
    reference[3017] = 'value_235212'
    assert tree.invariants(), "Invariants failed at step 6402"

    # Operation 6403: update
    tree[3017] = 'value_616443'
    reference[3017] = 'value_616443'
    assert tree.invariants(), "Invariants failed at step 6403"

    # Operation 6404: get
    assert tree.invariants(), "Invariants failed at step 6404"

    # Operation 6405: insert
    tree[4555] = 'value_102162'
    reference[4555] = 'value_102162'
    assert tree.invariants(), "Invariants failed at step 6405"

    # Operation 6406: batch_delete
    keys_to_delete = [3017, 4555, 3637, 4471, 8890]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6406"

    # Operation 6407: update
    tree[1716] = 'value_125963'
    reference[1716] = 'value_125963'
    assert tree.invariants(), "Invariants failed at step 6407"

    # Operation 6408: insert
    tree[7462] = 'value_15979'
    reference[7462] = 'value_15979'
    assert tree.invariants(), "Invariants failed at step 6408"

    # Operation 6409: update
    tree[1716] = 'value_509170'
    reference[1716] = 'value_509170'
    assert tree.invariants(), "Invariants failed at step 6409"

    # Operation 6410: insert
    tree[5000] = 'value_670537'
    reference[5000] = 'value_670537'
    assert tree.invariants(), "Invariants failed at step 6410"

    # Operation 6411: get
    assert tree.invariants(), "Invariants failed at step 6411"

    # Operation 6412: delete
    del tree[9129]
    del reference[9129]
    assert tree.invariants(), "Invariants failed at step 6412"

    # Operation 6413: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6413"

    # Operation 6414: delete
    del tree[7462]
    del reference[7462]
    assert tree.invariants(), "Invariants failed at step 6414"

    # Operation 6415: get
    assert tree.invariants(), "Invariants failed at step 6415"

    # Operation 6416: delete
    del tree[1716]
    del reference[1716]
    assert tree.invariants(), "Invariants failed at step 6416"

    # Operation 6417: delete
    del tree[5000]
    del reference[5000]
    assert tree.invariants(), "Invariants failed at step 6417"

    # Operation 6418: insert
    tree[8633] = 'value_236360'
    reference[8633] = 'value_236360'
    assert tree.invariants(), "Invariants failed at step 6418"

    # Operation 6419: delete
    del tree[5007]
    del reference[5007]
    assert tree.invariants(), "Invariants failed at step 6419"

    # Operation 6420: insert
    tree[7874] = 'value_158547'
    reference[7874] = 'value_158547'
    assert tree.invariants(), "Invariants failed at step 6420"

    # Operation 6421: insert
    tree[5767] = 'value_358148'
    reference[5767] = 'value_358148'
    assert tree.invariants(), "Invariants failed at step 6421"

    # Operation 6422: get
    assert tree.invariants(), "Invariants failed at step 6422"

    # Operation 6423: get
    assert tree.invariants(), "Invariants failed at step 6423"

    # Operation 6424: delete
    del tree[7159]
    del reference[7159]
    assert tree.invariants(), "Invariants failed at step 6424"

    # Operation 6425: insert
    tree[8428] = 'value_492927'
    reference[8428] = 'value_492927'
    assert tree.invariants(), "Invariants failed at step 6425"

    # Operation 6426: insert
    tree[8874] = 'value_890003'
    reference[8874] = 'value_890003'
    assert tree.invariants(), "Invariants failed at step 6426"

    # Operation 6427: insert
    tree[8503] = 'value_456637'
    reference[8503] = 'value_456637'
    assert tree.invariants(), "Invariants failed at step 6427"

    # Operation 6428: delete
    del tree[8428]
    del reference[8428]
    assert tree.invariants(), "Invariants failed at step 6428"

    # Operation 6429: delete
    del tree[8503]
    del reference[8503]
    assert tree.invariants(), "Invariants failed at step 6429"

    # Operation 6430: delete
    del tree[7874]
    del reference[7874]
    assert tree.invariants(), "Invariants failed at step 6430"

    # Operation 6431: insert
    tree[598] = 'value_326742'
    reference[598] = 'value_326742'
    assert tree.invariants(), "Invariants failed at step 6431"

    # Operation 6432: insert
    tree[2617] = 'value_454134'
    reference[2617] = 'value_454134'
    assert tree.invariants(), "Invariants failed at step 6432"

    # Operation 6433: get
    assert tree.invariants(), "Invariants failed at step 6433"

    # Operation 6434: update
    tree[598] = 'value_101035'
    reference[598] = 'value_101035'
    assert tree.invariants(), "Invariants failed at step 6434"

    # Operation 6435: insert
    tree[5175] = 'value_140878'
    reference[5175] = 'value_140878'
    assert tree.invariants(), "Invariants failed at step 6435"

    # Operation 6436: delete
    del tree[5175]
    del reference[5175]
    assert tree.invariants(), "Invariants failed at step 6436"

    # Operation 6437: update
    tree[598] = 'value_254285'
    reference[598] = 'value_254285'
    assert tree.invariants(), "Invariants failed at step 6437"

    # Operation 6438: batch_delete
    keys_to_delete = [8874, 9770, 7165, 5767]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6438"

    # Operation 6439: insert
    tree[4754] = 'value_315868'
    reference[4754] = 'value_315868'
    assert tree.invariants(), "Invariants failed at step 6439"

    # Operation 6440: update
    tree[598] = 'value_271037'
    reference[598] = 'value_271037'
    assert tree.invariants(), "Invariants failed at step 6440"

    # Operation 6441: insert
    tree[4388] = 'value_856318'
    reference[4388] = 'value_856318'
    assert tree.invariants(), "Invariants failed at step 6441"

    # Operation 6442: delete
    del tree[2617]
    del reference[2617]
    assert tree.invariants(), "Invariants failed at step 6442"

    # Operation 6443: insert
    tree[6831] = 'value_708287'
    reference[6831] = 'value_708287'
    assert tree.invariants(), "Invariants failed at step 6443"

    # Operation 6444: delete
    del tree[8633]
    del reference[8633]
    assert tree.invariants(), "Invariants failed at step 6444"

    # Operation 6445: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6445"

    # Operation 6446: get
    assert tree.invariants(), "Invariants failed at step 6446"

    # Operation 6447: insert
    tree[2406] = 'value_57777'
    reference[2406] = 'value_57777'
    assert tree.invariants(), "Invariants failed at step 6447"

    # Operation 6448: delete
    del tree[6831]
    del reference[6831]
    assert tree.invariants(), "Invariants failed at step 6448"

    # Operation 6449: insert
    tree[7498] = 'value_602332'
    reference[7498] = 'value_602332'
    assert tree.invariants(), "Invariants failed at step 6449"

    # Operation 6450: delete
    del tree[598]
    del reference[598]
    assert tree.invariants(), "Invariants failed at step 6450"

    # Operation 6451: get
    assert tree.invariants(), "Invariants failed at step 6451"

    # Operation 6452: get
    assert tree.invariants(), "Invariants failed at step 6452"

    # Operation 6453: get
    assert tree.invariants(), "Invariants failed at step 6453"

    # Operation 6454: update
    tree[4754] = 'value_277830'
    reference[4754] = 'value_277830'
    assert tree.invariants(), "Invariants failed at step 6454"

    # Operation 6455: get
    assert tree.invariants(), "Invariants failed at step 6455"

    # Operation 6456: insert
    tree[2502] = 'value_843458'
    reference[2502] = 'value_843458'
    assert tree.invariants(), "Invariants failed at step 6456"

    # Operation 6457: get
    assert tree.invariants(), "Invariants failed at step 6457"

    # Operation 6458: get
    assert tree.invariants(), "Invariants failed at step 6458"

    # Operation 6459: delete
    del tree[4388]
    del reference[4388]
    assert tree.invariants(), "Invariants failed at step 6459"

    # Operation 6460: insert
    tree[8617] = 'value_683563'
    reference[8617] = 'value_683563'
    assert tree.invariants(), "Invariants failed at step 6460"

    # Operation 6461: update
    tree[4754] = 'value_652096'
    reference[4754] = 'value_652096'
    assert tree.invariants(), "Invariants failed at step 6461"

    # Operation 6462: delete
    del tree[8617]
    del reference[8617]
    assert tree.invariants(), "Invariants failed at step 6462"

    # Operation 6463: delete
    del tree[2406]
    del reference[2406]
    assert tree.invariants(), "Invariants failed at step 6463"

    # Operation 6464: get
    assert tree.invariants(), "Invariants failed at step 6464"

    # Operation 6465: get
    assert tree.invariants(), "Invariants failed at step 6465"

    # Operation 6466: insert
    tree[5289] = 'value_695701'
    reference[5289] = 'value_695701'
    assert tree.invariants(), "Invariants failed at step 6466"

    # Operation 6467: delete
    del tree[2502]
    del reference[2502]
    assert tree.invariants(), "Invariants failed at step 6467"

    # Operation 6468: update
    tree[7498] = 'value_759657'
    reference[7498] = 'value_759657'
    assert tree.invariants(), "Invariants failed at step 6468"

    # Operation 6469: insert
    tree[76] = 'value_625984'
    reference[76] = 'value_625984'
    assert tree.invariants(), "Invariants failed at step 6469"

    # Operation 6470: update
    tree[4754] = 'value_134861'
    reference[4754] = 'value_134861'
    assert tree.invariants(), "Invariants failed at step 6470"

    # Operation 6471: update
    tree[76] = 'value_886171'
    reference[76] = 'value_886171'
    assert tree.invariants(), "Invariants failed at step 6471"

    # Operation 6472: update
    tree[76] = 'value_622719'
    reference[76] = 'value_622719'
    assert tree.invariants(), "Invariants failed at step 6472"

    # Operation 6473: get
    assert tree.invariants(), "Invariants failed at step 6473"

    # Operation 6474: insert
    tree[7657] = 'value_881547'
    reference[7657] = 'value_881547'
    assert tree.invariants(), "Invariants failed at step 6474"

    # Operation 6475: insert
    tree[8343] = 'value_532634'
    reference[8343] = 'value_532634'
    assert tree.invariants(), "Invariants failed at step 6475"

    # Operation 6476: insert
    tree[5948] = 'value_936838'
    reference[5948] = 'value_936838'
    assert tree.invariants(), "Invariants failed at step 6476"

    # Operation 6477: batch_delete
    keys_to_delete = [7657, 7498, 9047, 1017, 5948]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6477"

    # Operation 6478: get
    assert tree.invariants(), "Invariants failed at step 6478"

    # Operation 6479: delete
    del tree[76]
    del reference[76]
    assert tree.invariants(), "Invariants failed at step 6479"

    # Operation 6480: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6480"

    # Operation 6481: insert
    tree[5407] = 'value_80803'
    reference[5407] = 'value_80803'
    assert tree.invariants(), "Invariants failed at step 6481"

    # Operation 6482: get
    assert tree.invariants(), "Invariants failed at step 6482"

    # Operation 6483: get
    assert tree.invariants(), "Invariants failed at step 6483"

    # Operation 6484: get
    assert tree.invariants(), "Invariants failed at step 6484"

    # Operation 6485: update
    tree[8343] = 'value_357424'
    reference[8343] = 'value_357424'
    assert tree.invariants(), "Invariants failed at step 6485"

    # Operation 6486: update
    tree[5289] = 'value_555594'
    reference[5289] = 'value_555594'
    assert tree.invariants(), "Invariants failed at step 6486"

    # Operation 6487: delete
    del tree[4754]
    del reference[4754]
    assert tree.invariants(), "Invariants failed at step 6487"

    # Operation 6488: get
    assert tree.invariants(), "Invariants failed at step 6488"

    # Operation 6489: delete
    del tree[8343]
    del reference[8343]
    assert tree.invariants(), "Invariants failed at step 6489"

    # Operation 6490: insert
    tree[4722] = 'value_940095'
    reference[4722] = 'value_940095'
    assert tree.invariants(), "Invariants failed at step 6490"

    # Operation 6491: insert
    tree[4218] = 'value_557329'
    reference[4218] = 'value_557329'
    assert tree.invariants(), "Invariants failed at step 6491"

    # Operation 6492: insert
    tree[2242] = 'value_905141'
    reference[2242] = 'value_905141'
    assert tree.invariants(), "Invariants failed at step 6492"

    # Operation 6493: batch_delete
    keys_to_delete = [5289, 4218, 3539, 1515]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6493"

    # Operation 6494: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6494"

    # Operation 6495: insert
    tree[646] = 'value_341093'
    reference[646] = 'value_341093'
    assert tree.invariants(), "Invariants failed at step 6495"

    # Operation 6496: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6496"

    # Operation 6497: insert
    tree[4091] = 'value_376336'
    reference[4091] = 'value_376336'
    assert tree.invariants(), "Invariants failed at step 6497"

    # Operation 6498: insert
    tree[1905] = 'value_954840'
    reference[1905] = 'value_954840'
    assert tree.invariants(), "Invariants failed at step 6498"

    # Operation 6499: delete
    del tree[1905]
    del reference[1905]
    assert tree.invariants(), "Invariants failed at step 6499"

    # Operation 6500: insert
    tree[249] = 'value_616664'
    reference[249] = 'value_616664'
    assert tree.invariants(), "Invariants failed at step 6500"

    # Operation 6501: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6501"

    # Operation 6502: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6502"

    # Operation 6503: insert
    tree[6179] = 'value_564305'
    reference[6179] = 'value_564305'
    assert tree.invariants(), "Invariants failed at step 6503"

    # Operation 6504: insert
    tree[4947] = 'value_463094'
    reference[4947] = 'value_463094'
    assert tree.invariants(), "Invariants failed at step 6504"

    # Operation 6505: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6505"

    # Operation 6506: delete
    del tree[646]
    del reference[646]
    assert tree.invariants(), "Invariants failed at step 6506"

    # Operation 6507: update
    tree[5407] = 'value_129816'
    reference[5407] = 'value_129816'
    assert tree.invariants(), "Invariants failed at step 6507"

    # Operation 6508: get
    assert tree.invariants(), "Invariants failed at step 6508"

    # Operation 6509: batch_delete
    keys_to_delete = [2242, 7630, 4947, 5781, 249]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6509"

    # Operation 6510: insert
    tree[7464] = 'value_616512'
    reference[7464] = 'value_616512'
    assert tree.invariants(), "Invariants failed at step 6510"

    # Operation 6511: get
    assert tree.invariants(), "Invariants failed at step 6511"

    # Operation 6512: delete
    del tree[6179]
    del reference[6179]
    assert tree.invariants(), "Invariants failed at step 6512"

    # Operation 6513: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6513"

    # Operation 6514: get
    assert tree.invariants(), "Invariants failed at step 6514"

    # Operation 6515: insert
    tree[1842] = 'value_710071'
    reference[1842] = 'value_710071'
    assert tree.invariants(), "Invariants failed at step 6515"

    # Operation 6516: get
    assert tree.invariants(), "Invariants failed at step 6516"

    # Operation 6517: insert
    tree[9746] = 'value_410248'
    reference[9746] = 'value_410248'
    assert tree.invariants(), "Invariants failed at step 6517"

    # Operation 6518: batch_delete
    keys_to_delete = [1568, 7464, 9746, 1842, 6970]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6518"

    # Operation 6519: get
    assert tree.invariants(), "Invariants failed at step 6519"

    # Operation 6520: update
    tree[4091] = 'value_607525'
    reference[4091] = 'value_607525'
    assert tree.invariants(), "Invariants failed at step 6520"

    # Operation 6521: insert
    tree[7359] = 'value_360521'
    reference[7359] = 'value_360521'
    assert tree.invariants(), "Invariants failed at step 6521"

    # Operation 6522: delete
    del tree[4722]
    del reference[4722]
    assert tree.invariants(), "Invariants failed at step 6522"

    # Operation 6523: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6523"

    # Operation 6524: insert
    tree[164] = 'value_476195'
    reference[164] = 'value_476195'
    assert tree.invariants(), "Invariants failed at step 6524"

    # Operation 6525: delete
    del tree[5407]
    del reference[5407]
    assert tree.invariants(), "Invariants failed at step 6525"

    # Operation 6526: delete
    del tree[164]
    del reference[164]
    assert tree.invariants(), "Invariants failed at step 6526"

    # Operation 6527: get
    assert tree.invariants(), "Invariants failed at step 6527"

    # Operation 6528: get
    assert tree.invariants(), "Invariants failed at step 6528"

    # Operation 6529: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6529"

    # Operation 6530: get
    assert tree.invariants(), "Invariants failed at step 6530"

    # Operation 6531: insert
    tree[4211] = 'value_220014'
    reference[4211] = 'value_220014'
    assert tree.invariants(), "Invariants failed at step 6531"

    # Operation 6532: delete
    del tree[4091]
    del reference[4091]
    assert tree.invariants(), "Invariants failed at step 6532"

    # Operation 6533: get
    assert tree.invariants(), "Invariants failed at step 6533"

    # Operation 6534: update
    tree[4211] = 'value_39185'
    reference[4211] = 'value_39185'
    assert tree.invariants(), "Invariants failed at step 6534"

    # Operation 6535: get
    assert tree.invariants(), "Invariants failed at step 6535"

    # Operation 6536: delete
    del tree[4211]
    del reference[4211]
    assert tree.invariants(), "Invariants failed at step 6536"

    # Operation 6537: update
    tree[7359] = 'value_563001'
    reference[7359] = 'value_563001'
    assert tree.invariants(), "Invariants failed at step 6537"

    # Operation 6538: update
    tree[7359] = 'value_226863'
    reference[7359] = 'value_226863'
    assert tree.invariants(), "Invariants failed at step 6538"

    # Operation 6539: update
    tree[7359] = 'value_381697'
    reference[7359] = 'value_381697'
    assert tree.invariants(), "Invariants failed at step 6539"

    # Operation 6540: delete
    del tree[7359]
    del reference[7359]
    assert tree.invariants(), "Invariants failed at step 6540"

    # Operation 6541: insert
    tree[5060] = 'value_654543'
    reference[5060] = 'value_654543'
    assert tree.invariants(), "Invariants failed at step 6541"

    # Operation 6542: insert
    tree[8748] = 'value_220827'
    reference[8748] = 'value_220827'
    assert tree.invariants(), "Invariants failed at step 6542"

    # Operation 6543: delete
    del tree[8748]
    del reference[8748]
    assert tree.invariants(), "Invariants failed at step 6543"

    # Operation 6544: delete
    del tree[5060]
    del reference[5060]
    assert tree.invariants(), "Invariants failed at step 6544"

    # Operation 6545: get
    assert tree.invariants(), "Invariants failed at step 6545"

    # Operation 6546: get
    assert tree.invariants(), "Invariants failed at step 6546"

    # Operation 6547: insert
    tree[5031] = 'value_436864'
    reference[5031] = 'value_436864'
    assert tree.invariants(), "Invariants failed at step 6547"

    # Operation 6548: insert
    tree[464] = 'value_66236'
    reference[464] = 'value_66236'
    assert tree.invariants(), "Invariants failed at step 6548"

    # Operation 6549: insert
    tree[1004] = 'value_224355'
    reference[1004] = 'value_224355'
    assert tree.invariants(), "Invariants failed at step 6549"

    # Operation 6550: insert
    tree[6088] = 'value_459459'
    reference[6088] = 'value_459459'
    assert tree.invariants(), "Invariants failed at step 6550"

    # Operation 6551: get
    assert tree.invariants(), "Invariants failed at step 6551"

    # Operation 6552: update
    tree[5031] = 'value_830723'
    reference[5031] = 'value_830723'
    assert tree.invariants(), "Invariants failed at step 6552"

    # Operation 6553: delete
    del tree[6088]
    del reference[6088]
    assert tree.invariants(), "Invariants failed at step 6553"

    # Operation 6554: delete
    del tree[464]
    del reference[464]
    assert tree.invariants(), "Invariants failed at step 6554"

    # Operation 6555: get
    assert tree.invariants(), "Invariants failed at step 6555"

    # Operation 6556: get
    assert tree.invariants(), "Invariants failed at step 6556"

    # Operation 6557: delete
    del tree[1004]
    del reference[1004]
    assert tree.invariants(), "Invariants failed at step 6557"

    # Operation 6558: delete
    del tree[5031]
    del reference[5031]
    assert tree.invariants(), "Invariants failed at step 6558"

    # Operation 6559: insert
    tree[7254] = 'value_109371'
    reference[7254] = 'value_109371'
    assert tree.invariants(), "Invariants failed at step 6559"

    # Operation 6560: insert
    tree[8116] = 'value_129035'
    reference[8116] = 'value_129035'
    assert tree.invariants(), "Invariants failed at step 6560"

    # Operation 6561: insert
    tree[2731] = 'value_541010'
    reference[2731] = 'value_541010'
    assert tree.invariants(), "Invariants failed at step 6561"

    # Operation 6562: delete
    del tree[2731]
    del reference[2731]
    assert tree.invariants(), "Invariants failed at step 6562"

    # Operation 6563: get
    assert tree.invariants(), "Invariants failed at step 6563"

    # Operation 6564: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6564"

    # Operation 6565: delete
    del tree[8116]
    del reference[8116]
    assert tree.invariants(), "Invariants failed at step 6565"

    # Operation 6566: get
    assert tree.invariants(), "Invariants failed at step 6566"

    # Operation 6567: update
    tree[7254] = 'value_597699'
    reference[7254] = 'value_597699'
    assert tree.invariants(), "Invariants failed at step 6567"

    # Operation 6568: delete
    del tree[7254]
    del reference[7254]
    assert tree.invariants(), "Invariants failed at step 6568"

    # Operation 6569: insert
    tree[675] = 'value_518800'
    reference[675] = 'value_518800'
    assert tree.invariants(), "Invariants failed at step 6569"

    # Operation 6570: insert
    tree[6] = 'value_703316'
    reference[6] = 'value_703316'
    assert tree.invariants(), "Invariants failed at step 6570"

    # Operation 6571: get
    assert tree.invariants(), "Invariants failed at step 6571"

    # Operation 6572: insert
    tree[3689] = 'value_214684'
    reference[3689] = 'value_214684'
    assert tree.invariants(), "Invariants failed at step 6572"

    # Operation 6573: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6573"

    # Operation 6574: get
    assert tree.invariants(), "Invariants failed at step 6574"

    # Operation 6575: insert
    tree[4939] = 'value_986590'
    reference[4939] = 'value_986590'
    assert tree.invariants(), "Invariants failed at step 6575"

    # Operation 6576: insert
    tree[8283] = 'value_592126'
    reference[8283] = 'value_592126'
    assert tree.invariants(), "Invariants failed at step 6576"

    # Operation 6577: delete
    del tree[3689]
    del reference[3689]
    assert tree.invariants(), "Invariants failed at step 6577"

    # Operation 6578: get
    assert tree.invariants(), "Invariants failed at step 6578"

    # Operation 6579: insert
    tree[776] = 'value_83750'
    reference[776] = 'value_83750'
    assert tree.invariants(), "Invariants failed at step 6579"

    # Operation 6580: insert
    tree[932] = 'value_940115'
    reference[932] = 'value_940115'
    assert tree.invariants(), "Invariants failed at step 6580"

    # Operation 6581: insert
    tree[7330] = 'value_406227'
    reference[7330] = 'value_406227'
    assert tree.invariants(), "Invariants failed at step 6581"

    # Operation 6582: update
    tree[932] = 'value_360350'
    reference[932] = 'value_360350'
    assert tree.invariants(), "Invariants failed at step 6582"

    # Operation 6583: update
    tree[8283] = 'value_371405'
    reference[8283] = 'value_371405'
    assert tree.invariants(), "Invariants failed at step 6583"

    # Operation 6584: get
    assert tree.invariants(), "Invariants failed at step 6584"

    # Operation 6585: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6585"

    # Operation 6586: delete
    del tree[4939]
    del reference[4939]
    assert tree.invariants(), "Invariants failed at step 6586"

    # Operation 6587: insert
    tree[9583] = 'value_542365'
    reference[9583] = 'value_542365'
    assert tree.invariants(), "Invariants failed at step 6587"

    # Operation 6588: delete
    del tree[6]
    del reference[6]
    assert tree.invariants(), "Invariants failed at step 6588"

    # Operation 6589: delete
    del tree[8283]
    del reference[8283]
    assert tree.invariants(), "Invariants failed at step 6589"

    # Operation 6590: get
    assert tree.invariants(), "Invariants failed at step 6590"

    # Operation 6591: delete
    del tree[932]
    del reference[932]
    assert tree.invariants(), "Invariants failed at step 6591"

    # Operation 6592: get
    assert tree.invariants(), "Invariants failed at step 6592"

    # Operation 6593: get
    assert tree.invariants(), "Invariants failed at step 6593"

    # Operation 6594: update
    tree[776] = 'value_428291'
    reference[776] = 'value_428291'
    assert tree.invariants(), "Invariants failed at step 6594"

    # Operation 6595: update
    tree[9583] = 'value_9298'
    reference[9583] = 'value_9298'
    assert tree.invariants(), "Invariants failed at step 6595"

    # Operation 6596: delete
    del tree[9583]
    del reference[9583]
    assert tree.invariants(), "Invariants failed at step 6596"

    # Operation 6597: update
    tree[675] = 'value_743858'
    reference[675] = 'value_743858'
    assert tree.invariants(), "Invariants failed at step 6597"

    # Operation 6598: update
    tree[675] = 'value_899025'
    reference[675] = 'value_899025'
    assert tree.invariants(), "Invariants failed at step 6598"

    # Operation 6599: insert
    tree[6720] = 'value_189520'
    reference[6720] = 'value_189520'
    assert tree.invariants(), "Invariants failed at step 6599"

    # Operation 6600: insert
    tree[7200] = 'value_515339'
    reference[7200] = 'value_515339'
    assert tree.invariants(), "Invariants failed at step 6600"

    # Operation 6601: get
    assert tree.invariants(), "Invariants failed at step 6601"

    # Operation 6602: get
    assert tree.invariants(), "Invariants failed at step 6602"

    # Operation 6603: batch_delete
    keys_to_delete = [7200, 9177, 7330, 9414]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6603"

    # Operation 6604: insert
    tree[9363] = 'value_828321'
    reference[9363] = 'value_828321'
    assert tree.invariants(), "Invariants failed at step 6604"

    # Operation 6605: get
    assert tree.invariants(), "Invariants failed at step 6605"

    # Operation 6606: delete
    del tree[776]
    del reference[776]
    assert tree.invariants(), "Invariants failed at step 6606"

    # Operation 6607: get
    assert tree.invariants(), "Invariants failed at step 6607"

    # Operation 6608: insert
    tree[4107] = 'value_51522'
    reference[4107] = 'value_51522'
    assert tree.invariants(), "Invariants failed at step 6608"

    # Operation 6609: insert
    tree[2964] = 'value_502276'
    reference[2964] = 'value_502276'
    assert tree.invariants(), "Invariants failed at step 6609"

    # Operation 6610: delete
    del tree[6720]
    del reference[6720]
    assert tree.invariants(), "Invariants failed at step 6610"

    # Operation 6611: delete
    del tree[675]
    del reference[675]
    assert tree.invariants(), "Invariants failed at step 6611"

    # Operation 6612: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6612"

    # Operation 6613: insert
    tree[7831] = 'value_107846'
    reference[7831] = 'value_107846'
    assert tree.invariants(), "Invariants failed at step 6613"

    # Operation 6614: get
    assert tree.invariants(), "Invariants failed at step 6614"

    # Operation 6615: delete
    del tree[9363]
    del reference[9363]
    assert tree.invariants(), "Invariants failed at step 6615"

    # Operation 6616: get
    assert tree.invariants(), "Invariants failed at step 6616"

    # Operation 6617: get
    assert tree.invariants(), "Invariants failed at step 6617"

    # Operation 6618: delete
    del tree[7831]
    del reference[7831]
    assert tree.invariants(), "Invariants failed at step 6618"

    # Operation 6619: insert
    tree[4342] = 'value_188741'
    reference[4342] = 'value_188741'
    assert tree.invariants(), "Invariants failed at step 6619"

    # Operation 6620: get
    assert tree.invariants(), "Invariants failed at step 6620"

    # Operation 6621: delete
    del tree[4342]
    del reference[4342]
    assert tree.invariants(), "Invariants failed at step 6621"

    # Operation 6622: delete
    del tree[4107]
    del reference[4107]
    assert tree.invariants(), "Invariants failed at step 6622"

    # Operation 6623: delete
    del tree[2964]
    del reference[2964]
    assert tree.invariants(), "Invariants failed at step 6623"

    # Operation 6624: get
    assert tree.invariants(), "Invariants failed at step 6624"

    # Operation 6625: insert
    tree[138] = 'value_781693'
    reference[138] = 'value_781693'
    assert tree.invariants(), "Invariants failed at step 6625"

    # Operation 6626: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6626"

    # Operation 6627: get
    assert tree.invariants(), "Invariants failed at step 6627"

    # Operation 6628: update
    tree[138] = 'value_599704'
    reference[138] = 'value_599704'
    assert tree.invariants(), "Invariants failed at step 6628"

    # Operation 6629: update
    tree[138] = 'value_339987'
    reference[138] = 'value_339987'
    assert tree.invariants(), "Invariants failed at step 6629"

    # Operation 6630: delete
    del tree[138]
    del reference[138]
    assert tree.invariants(), "Invariants failed at step 6630"

    # Operation 6631: get
    assert tree.invariants(), "Invariants failed at step 6631"

    # Operation 6632: insert
    tree[3262] = 'value_499747'
    reference[3262] = 'value_499747'
    assert tree.invariants(), "Invariants failed at step 6632"

    # Operation 6633: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6633"

    # Operation 6634: insert
    tree[2563] = 'value_476867'
    reference[2563] = 'value_476867'
    assert tree.invariants(), "Invariants failed at step 6634"

    # Operation 6635: delete
    del tree[2563]
    del reference[2563]
    assert tree.invariants(), "Invariants failed at step 6635"

    # Operation 6636: get
    assert tree.invariants(), "Invariants failed at step 6636"

    # Operation 6637: get
    assert tree.invariants(), "Invariants failed at step 6637"

    # Operation 6638: delete
    del tree[3262]
    del reference[3262]
    assert tree.invariants(), "Invariants failed at step 6638"

    # Operation 6639: get
    assert tree.invariants(), "Invariants failed at step 6639"

    # Operation 6640: insert
    tree[7010] = 'value_786718'
    reference[7010] = 'value_786718'
    assert tree.invariants(), "Invariants failed at step 6640"

    # Operation 6641: insert
    tree[9759] = 'value_387985'
    reference[9759] = 'value_387985'
    assert tree.invariants(), "Invariants failed at step 6641"

    # Operation 6642: get
    assert tree.invariants(), "Invariants failed at step 6642"

    # Operation 6643: update
    tree[9759] = 'value_980471'
    reference[9759] = 'value_980471'
    assert tree.invariants(), "Invariants failed at step 6643"

    # Operation 6644: insert
    tree[8593] = 'value_999543'
    reference[8593] = 'value_999543'
    assert tree.invariants(), "Invariants failed at step 6644"

    # Operation 6645: delete
    del tree[7010]
    del reference[7010]
    assert tree.invariants(), "Invariants failed at step 6645"

    # Operation 6646: delete
    del tree[9759]
    del reference[9759]
    assert tree.invariants(), "Invariants failed at step 6646"

    # Operation 6647: get
    assert tree.invariants(), "Invariants failed at step 6647"

    # Operation 6648: insert
    tree[5960] = 'value_831556'
    reference[5960] = 'value_831556'
    assert tree.invariants(), "Invariants failed at step 6648"

    # Operation 6649: get
    assert tree.invariants(), "Invariants failed at step 6649"

    # Operation 6650: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6650"

    # Operation 6651: delete
    del tree[8593]
    del reference[8593]
    assert tree.invariants(), "Invariants failed at step 6651"

    # Operation 6652: insert
    tree[1831] = 'value_839497'
    reference[1831] = 'value_839497'
    assert tree.invariants(), "Invariants failed at step 6652"

    # Operation 6653: delete
    del tree[1831]
    del reference[1831]
    assert tree.invariants(), "Invariants failed at step 6653"

    # Operation 6654: get
    assert tree.invariants(), "Invariants failed at step 6654"

    # Operation 6655: get
    assert tree.invariants(), "Invariants failed at step 6655"

    # Operation 6656: insert
    tree[3714] = 'value_697951'
    reference[3714] = 'value_697951'
    assert tree.invariants(), "Invariants failed at step 6656"

    # Operation 6657: get
    assert tree.invariants(), "Invariants failed at step 6657"

    # Operation 6658: delete
    del tree[5960]
    del reference[5960]
    assert tree.invariants(), "Invariants failed at step 6658"

    # Operation 6659: get
    assert tree.invariants(), "Invariants failed at step 6659"

    # Operation 6660: delete
    del tree[3714]
    del reference[3714]
    assert tree.invariants(), "Invariants failed at step 6660"

    # Operation 6661: insert
    tree[1529] = 'value_19592'
    reference[1529] = 'value_19592'
    assert tree.invariants(), "Invariants failed at step 6661"

    # Operation 6662: get
    assert tree.invariants(), "Invariants failed at step 6662"

    # Operation 6663: delete
    del tree[1529]
    del reference[1529]
    assert tree.invariants(), "Invariants failed at step 6663"

    # Operation 6664: get
    assert tree.invariants(), "Invariants failed at step 6664"

    # Operation 6665: insert
    tree[6398] = 'value_556941'
    reference[6398] = 'value_556941'
    assert tree.invariants(), "Invariants failed at step 6665"

    # Operation 6666: delete
    del tree[6398]
    del reference[6398]
    assert tree.invariants(), "Invariants failed at step 6666"

    # Operation 6667: insert
    tree[9760] = 'value_651478'
    reference[9760] = 'value_651478'
    assert tree.invariants(), "Invariants failed at step 6667"

    # Operation 6668: delete
    del tree[9760]
    del reference[9760]
    assert tree.invariants(), "Invariants failed at step 6668"

    # Operation 6669: get
    assert tree.invariants(), "Invariants failed at step 6669"

    # Operation 6670: insert
    tree[977] = 'value_687530'
    reference[977] = 'value_687530'
    assert tree.invariants(), "Invariants failed at step 6670"

    # Operation 6671: delete
    del tree[977]
    del reference[977]
    assert tree.invariants(), "Invariants failed at step 6671"

    # Operation 6672: insert
    tree[406] = 'value_534010'
    reference[406] = 'value_534010'
    assert tree.invariants(), "Invariants failed at step 6672"

    # Operation 6673: delete
    del tree[406]
    del reference[406]
    assert tree.invariants(), "Invariants failed at step 6673"

    # Operation 6674: insert
    tree[5354] = 'value_481266'
    reference[5354] = 'value_481266'
    assert tree.invariants(), "Invariants failed at step 6674"

    # Operation 6675: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6675"

    # Operation 6676: update
    tree[5354] = 'value_556308'
    reference[5354] = 'value_556308'
    assert tree.invariants(), "Invariants failed at step 6676"

    # Operation 6677: delete
    del tree[5354]
    del reference[5354]
    assert tree.invariants(), "Invariants failed at step 6677"

    # Operation 6678: insert
    tree[4835] = 'value_288617'
    reference[4835] = 'value_288617'
    assert tree.invariants(), "Invariants failed at step 6678"

    # Operation 6679: get
    assert tree.invariants(), "Invariants failed at step 6679"

    # Operation 6680: update
    tree[4835] = 'value_592987'
    reference[4835] = 'value_592987'
    assert tree.invariants(), "Invariants failed at step 6680"

    # Operation 6681: get
    assert tree.invariants(), "Invariants failed at step 6681"

    # Operation 6682: delete
    del tree[4835]
    del reference[4835]
    assert tree.invariants(), "Invariants failed at step 6682"

    # Operation 6683: insert
    tree[6232] = 'value_188803'
    reference[6232] = 'value_188803'
    assert tree.invariants(), "Invariants failed at step 6683"

    # Operation 6684: insert
    tree[2134] = 'value_295194'
    reference[2134] = 'value_295194'
    assert tree.invariants(), "Invariants failed at step 6684"

    # Operation 6685: get
    assert tree.invariants(), "Invariants failed at step 6685"

    # Operation 6686: get
    assert tree.invariants(), "Invariants failed at step 6686"

    # Operation 6687: update
    tree[6232] = 'value_896589'
    reference[6232] = 'value_896589'
    assert tree.invariants(), "Invariants failed at step 6687"

    # Operation 6688: get
    assert tree.invariants(), "Invariants failed at step 6688"

    # Operation 6689: insert
    tree[7951] = 'value_557799'
    reference[7951] = 'value_557799'
    assert tree.invariants(), "Invariants failed at step 6689"

    # Operation 6690: insert
    tree[5657] = 'value_473744'
    reference[5657] = 'value_473744'
    assert tree.invariants(), "Invariants failed at step 6690"

    # Operation 6691: update
    tree[7951] = 'value_708868'
    reference[7951] = 'value_708868'
    assert tree.invariants(), "Invariants failed at step 6691"

    # Operation 6692: delete
    del tree[7951]
    del reference[7951]
    assert tree.invariants(), "Invariants failed at step 6692"

    # Operation 6693: insert
    tree[8352] = 'value_103803'
    reference[8352] = 'value_103803'
    assert tree.invariants(), "Invariants failed at step 6693"

    # Operation 6694: insert
    tree[3393] = 'value_167796'
    reference[3393] = 'value_167796'
    assert tree.invariants(), "Invariants failed at step 6694"

    # Operation 6695: get
    assert tree.invariants(), "Invariants failed at step 6695"

    # Operation 6696: get
    assert tree.invariants(), "Invariants failed at step 6696"

    # Operation 6697: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6697"

    # Operation 6698: delete
    del tree[6232]
    del reference[6232]
    assert tree.invariants(), "Invariants failed at step 6698"

    # Operation 6699: delete
    del tree[8352]
    del reference[8352]
    assert tree.invariants(), "Invariants failed at step 6699"

    # Operation 6700: get
    assert tree.invariants(), "Invariants failed at step 6700"

    # Operation 6701: get
    assert tree.invariants(), "Invariants failed at step 6701"

    # Operation 6702: get
    assert tree.invariants(), "Invariants failed at step 6702"

    # Operation 6703: update
    tree[2134] = 'value_900609'
    reference[2134] = 'value_900609'
    assert tree.invariants(), "Invariants failed at step 6703"

    # Operation 6704: insert
    tree[4184] = 'value_22326'
    reference[4184] = 'value_22326'
    assert tree.invariants(), "Invariants failed at step 6704"

    # Operation 6705: insert
    tree[8682] = 'value_49970'
    reference[8682] = 'value_49970'
    assert tree.invariants(), "Invariants failed at step 6705"

    # Operation 6706: get
    assert tree.invariants(), "Invariants failed at step 6706"

    # Operation 6707: get
    assert tree.invariants(), "Invariants failed at step 6707"

    # Operation 6708: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6708"

    # Operation 6709: get
    assert tree.invariants(), "Invariants failed at step 6709"

    # Operation 6710: delete
    del tree[4184]
    del reference[4184]
    assert tree.invariants(), "Invariants failed at step 6710"

    # Operation 6711: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6711"

    # Operation 6712: update
    tree[5657] = 'value_177260'
    reference[5657] = 'value_177260'
    assert tree.invariants(), "Invariants failed at step 6712"

    # Operation 6713: delete
    del tree[3393]
    del reference[3393]
    assert tree.invariants(), "Invariants failed at step 6713"

    # Operation 6714: insert
    tree[100] = 'value_631309'
    reference[100] = 'value_631309'
    assert tree.invariants(), "Invariants failed at step 6714"

    # Operation 6715: delete
    del tree[2134]
    del reference[2134]
    assert tree.invariants(), "Invariants failed at step 6715"

    # Operation 6716: update
    tree[8682] = 'value_78045'
    reference[8682] = 'value_78045'
    assert tree.invariants(), "Invariants failed at step 6716"

    # Operation 6717: insert
    tree[4358] = 'value_587938'
    reference[4358] = 'value_587938'
    assert tree.invariants(), "Invariants failed at step 6717"

    # Operation 6718: get
    assert tree.invariants(), "Invariants failed at step 6718"

    # Operation 6719: update
    tree[100] = 'value_871703'
    reference[100] = 'value_871703'
    assert tree.invariants(), "Invariants failed at step 6719"

    # Operation 6720: insert
    tree[8857] = 'value_158632'
    reference[8857] = 'value_158632'
    assert tree.invariants(), "Invariants failed at step 6720"

    # Operation 6721: delete
    del tree[100]
    del reference[100]
    assert tree.invariants(), "Invariants failed at step 6721"

    # Operation 6722: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6722"

    # Operation 6723: get
    assert tree.invariants(), "Invariants failed at step 6723"

    # Operation 6724: delete
    del tree[8682]
    del reference[8682]
    assert tree.invariants(), "Invariants failed at step 6724"

    # Operation 6725: update
    tree[4358] = 'value_13728'
    reference[4358] = 'value_13728'
    assert tree.invariants(), "Invariants failed at step 6725"

    # Operation 6726: delete
    del tree[8857]
    del reference[8857]
    assert tree.invariants(), "Invariants failed at step 6726"

    # Operation 6727: update
    tree[5657] = 'value_175209'
    reference[5657] = 'value_175209'
    assert tree.invariants(), "Invariants failed at step 6727"

    # Operation 6728: get
    assert tree.invariants(), "Invariants failed at step 6728"

    # Operation 6729: get
    assert tree.invariants(), "Invariants failed at step 6729"

    # Operation 6730: delete
    del tree[5657]
    del reference[5657]
    assert tree.invariants(), "Invariants failed at step 6730"

    # Operation 6731: insert
    tree[8053] = 'value_814024'
    reference[8053] = 'value_814024'
    assert tree.invariants(), "Invariants failed at step 6731"

    # Operation 6732: delete
    del tree[4358]
    del reference[4358]
    assert tree.invariants(), "Invariants failed at step 6732"

    # Operation 6733: delete
    del tree[8053]
    del reference[8053]
    assert tree.invariants(), "Invariants failed at step 6733"

    # Operation 6734: insert
    tree[280] = 'value_322534'
    reference[280] = 'value_322534'
    assert tree.invariants(), "Invariants failed at step 6734"

    # Operation 6735: update
    tree[280] = 'value_912476'
    reference[280] = 'value_912476'
    assert tree.invariants(), "Invariants failed at step 6735"

    # Operation 6736: update
    tree[280] = 'value_512235'
    reference[280] = 'value_512235'
    assert tree.invariants(), "Invariants failed at step 6736"

    # Operation 6737: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6737"

    # Operation 6738: delete
    del tree[280]
    del reference[280]
    assert tree.invariants(), "Invariants failed at step 6738"

    # Operation 6739: get
    assert tree.invariants(), "Invariants failed at step 6739"

    # Operation 6740: insert
    tree[3182] = 'value_193610'
    reference[3182] = 'value_193610'
    assert tree.invariants(), "Invariants failed at step 6740"

    # Operation 6741: get
    assert tree.invariants(), "Invariants failed at step 6741"

    # Operation 6742: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6742"

    # Operation 6743: insert
    tree[5897] = 'value_162114'
    reference[5897] = 'value_162114'
    assert tree.invariants(), "Invariants failed at step 6743"

    # Operation 6744: get
    assert tree.invariants(), "Invariants failed at step 6744"

    # Operation 6745: update
    tree[5897] = 'value_895173'
    reference[5897] = 'value_895173'
    assert tree.invariants(), "Invariants failed at step 6745"

    # Operation 6746: delete
    del tree[3182]
    del reference[3182]
    assert tree.invariants(), "Invariants failed at step 6746"

    # Operation 6747: update
    tree[5897] = 'value_30777'
    reference[5897] = 'value_30777'
    assert tree.invariants(), "Invariants failed at step 6747"

    # Operation 6748: insert
    tree[917] = 'value_948963'
    reference[917] = 'value_948963'
    assert tree.invariants(), "Invariants failed at step 6748"

    # Operation 6749: update
    tree[917] = 'value_510826'
    reference[917] = 'value_510826'
    assert tree.invariants(), "Invariants failed at step 6749"

    # Operation 6750: delete
    del tree[5897]
    del reference[5897]
    assert tree.invariants(), "Invariants failed at step 6750"

    # Operation 6751: insert
    tree[38] = 'value_106875'
    reference[38] = 'value_106875'
    assert tree.invariants(), "Invariants failed at step 6751"

    # Operation 6752: delete
    del tree[917]
    del reference[917]
    assert tree.invariants(), "Invariants failed at step 6752"

    # Operation 6753: insert
    tree[3724] = 'value_924273'
    reference[3724] = 'value_924273'
    assert tree.invariants(), "Invariants failed at step 6753"

    # Operation 6754: delete
    del tree[38]
    del reference[38]
    assert tree.invariants(), "Invariants failed at step 6754"

    # Operation 6755: insert
    tree[1993] = 'value_324605'
    reference[1993] = 'value_324605'
    assert tree.invariants(), "Invariants failed at step 6755"

    # Operation 6756: update
    tree[1993] = 'value_196513'
    reference[1993] = 'value_196513'
    assert tree.invariants(), "Invariants failed at step 6756"

    # Operation 6757: delete
    del tree[1993]
    del reference[1993]
    assert tree.invariants(), "Invariants failed at step 6757"

    # Operation 6758: insert
    tree[8280] = 'value_496816'
    reference[8280] = 'value_496816'
    assert tree.invariants(), "Invariants failed at step 6758"

    # Operation 6759: get
    assert tree.invariants(), "Invariants failed at step 6759"

    # Operation 6760: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6760"

    # Operation 6761: insert
    tree[9139] = 'value_845586'
    reference[9139] = 'value_845586'
    assert tree.invariants(), "Invariants failed at step 6761"

    # Operation 6762: insert
    tree[8356] = 'value_504716'
    reference[8356] = 'value_504716'
    assert tree.invariants(), "Invariants failed at step 6762"

    # Operation 6763: delete
    del tree[3724]
    del reference[3724]
    assert tree.invariants(), "Invariants failed at step 6763"

    # Operation 6764: get
    assert tree.invariants(), "Invariants failed at step 6764"

    # Operation 6765: get
    assert tree.invariants(), "Invariants failed at step 6765"

    # Operation 6766: insert
    tree[5095] = 'value_799152'
    reference[5095] = 'value_799152'
    assert tree.invariants(), "Invariants failed at step 6766"

    # Operation 6767: insert
    tree[1024] = 'value_167529'
    reference[1024] = 'value_167529'
    assert tree.invariants(), "Invariants failed at step 6767"

    # Operation 6768: delete
    del tree[1024]
    del reference[1024]
    assert tree.invariants(), "Invariants failed at step 6768"

    # Operation 6769: get
    assert tree.invariants(), "Invariants failed at step 6769"

    # Operation 6770: delete
    del tree[9139]
    del reference[9139]
    assert tree.invariants(), "Invariants failed at step 6770"

    # Operation 6771: insert
    tree[1123] = 'value_114050'
    reference[1123] = 'value_114050'
    assert tree.invariants(), "Invariants failed at step 6771"

    # Operation 6772: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6772"

    # Operation 6773: get
    assert tree.invariants(), "Invariants failed at step 6773"

    # Operation 6774: delete
    del tree[1123]
    del reference[1123]
    assert tree.invariants(), "Invariants failed at step 6774"

    # Operation 6775: get
    assert tree.invariants(), "Invariants failed at step 6775"

    # Operation 6776: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6776"

    # Operation 6777: update
    tree[5095] = 'value_781123'
    reference[5095] = 'value_781123'
    assert tree.invariants(), "Invariants failed at step 6777"

    # Operation 6778: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6778"

    # Operation 6779: get
    assert tree.invariants(), "Invariants failed at step 6779"

    # Operation 6780: update
    tree[5095] = 'value_276036'
    reference[5095] = 'value_276036'
    assert tree.invariants(), "Invariants failed at step 6780"

    # Operation 6781: insert
    tree[8150] = 'value_637928'
    reference[8150] = 'value_637928'
    assert tree.invariants(), "Invariants failed at step 6781"

    # Operation 6782: insert
    tree[1981] = 'value_15767'
    reference[1981] = 'value_15767'
    assert tree.invariants(), "Invariants failed at step 6782"

    # Operation 6783: batch_delete
    keys_to_delete = [2608, 8356, 1981, 2158]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6783"

    # Operation 6784: insert
    tree[5127] = 'value_779124'
    reference[5127] = 'value_779124'
    assert tree.invariants(), "Invariants failed at step 6784"

    # Operation 6785: delete
    del tree[5127]
    del reference[5127]
    assert tree.invariants(), "Invariants failed at step 6785"

    # Operation 6786: get
    assert tree.invariants(), "Invariants failed at step 6786"

    # Operation 6787: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6787"

    # Operation 6788: insert
    tree[7596] = 'value_938065'
    reference[7596] = 'value_938065'
    assert tree.invariants(), "Invariants failed at step 6788"

    # Operation 6789: get
    assert tree.invariants(), "Invariants failed at step 6789"

    # Operation 6790: get
    assert tree.invariants(), "Invariants failed at step 6790"

    # Operation 6791: insert
    tree[4633] = 'value_543219'
    reference[4633] = 'value_543219'
    assert tree.invariants(), "Invariants failed at step 6791"

    # Operation 6792: insert
    tree[3899] = 'value_851152'
    reference[3899] = 'value_851152'
    assert tree.invariants(), "Invariants failed at step 6792"

    # Operation 6793: batch_delete
    keys_to_delete = [9486, 8150, 8280, 4633, 7004]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6793"

    # Operation 6794: get
    assert tree.invariants(), "Invariants failed at step 6794"

    # Operation 6795: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6795"

    # Operation 6796: insert
    tree[8797] = 'value_757564'
    reference[8797] = 'value_757564'
    assert tree.invariants(), "Invariants failed at step 6796"

    # Operation 6797: get
    assert tree.invariants(), "Invariants failed at step 6797"

    # Operation 6798: get
    assert tree.invariants(), "Invariants failed at step 6798"

    # Operation 6799: insert
    tree[2437] = 'value_487263'
    reference[2437] = 'value_487263'
    assert tree.invariants(), "Invariants failed at step 6799"

    # Operation 6800: batch_delete
    keys_to_delete = [704, 2681, 3899, 2437]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6800"

    # Operation 6801: insert
    tree[2538] = 'value_786476'
    reference[2538] = 'value_786476'
    assert tree.invariants(), "Invariants failed at step 6801"

    # Operation 6802: get
    assert tree.invariants(), "Invariants failed at step 6802"

    # Operation 6803: update
    tree[5095] = 'value_541747'
    reference[5095] = 'value_541747'
    assert tree.invariants(), "Invariants failed at step 6803"

    # Operation 6804: delete
    del tree[7596]
    del reference[7596]
    assert tree.invariants(), "Invariants failed at step 6804"

    # Operation 6805: get
    assert tree.invariants(), "Invariants failed at step 6805"

    # Operation 6806: insert
    tree[7143] = 'value_84422'
    reference[7143] = 'value_84422'
    assert tree.invariants(), "Invariants failed at step 6806"

    # Operation 6807: delete
    del tree[2538]
    del reference[2538]
    assert tree.invariants(), "Invariants failed at step 6807"

    # Operation 6808: delete
    del tree[8797]
    del reference[8797]
    assert tree.invariants(), "Invariants failed at step 6808"

    # Operation 6809: delete
    del tree[7143]
    del reference[7143]
    assert tree.invariants(), "Invariants failed at step 6809"

    # Operation 6810: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6810"

    # Operation 6811: delete
    del tree[5095]
    del reference[5095]
    assert tree.invariants(), "Invariants failed at step 6811"

    # Operation 6812: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6812"

    # Operation 6813: get
    assert tree.invariants(), "Invariants failed at step 6813"

    # Operation 6814: insert
    tree[8648] = 'value_994559'
    reference[8648] = 'value_994559'
    assert tree.invariants(), "Invariants failed at step 6814"

    # Operation 6815: get
    assert tree.invariants(), "Invariants failed at step 6815"

    # Operation 6816: insert
    tree[6075] = 'value_179097'
    reference[6075] = 'value_179097'
    assert tree.invariants(), "Invariants failed at step 6816"

    # Operation 6817: get
    assert tree.invariants(), "Invariants failed at step 6817"

    # Operation 6818: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6818"

    # Operation 6819: get
    assert tree.invariants(), "Invariants failed at step 6819"

    # Operation 6820: insert
    tree[3495] = 'value_487552'
    reference[3495] = 'value_487552'
    assert tree.invariants(), "Invariants failed at step 6820"

    # Operation 6821: delete
    del tree[8648]
    del reference[8648]
    assert tree.invariants(), "Invariants failed at step 6821"

    # Operation 6822: get
    assert tree.invariants(), "Invariants failed at step 6822"

    # Operation 6823: delete
    del tree[3495]
    del reference[3495]
    assert tree.invariants(), "Invariants failed at step 6823"

    # Operation 6824: delete
    del tree[6075]
    del reference[6075]
    assert tree.invariants(), "Invariants failed at step 6824"

    # Operation 6825: insert
    tree[9142] = 'value_444574'
    reference[9142] = 'value_444574'
    assert tree.invariants(), "Invariants failed at step 6825"

    # Operation 6826: insert
    tree[2285] = 'value_177608'
    reference[2285] = 'value_177608'
    assert tree.invariants(), "Invariants failed at step 6826"

    # Operation 6827: update
    tree[2285] = 'value_351566'
    reference[2285] = 'value_351566'
    assert tree.invariants(), "Invariants failed at step 6827"

    # Operation 6828: insert
    tree[2852] = 'value_633504'
    reference[2852] = 'value_633504'
    assert tree.invariants(), "Invariants failed at step 6828"

    # Operation 6829: delete
    del tree[2852]
    del reference[2852]
    assert tree.invariants(), "Invariants failed at step 6829"

    # Operation 6830: update
    tree[2285] = 'value_58166'
    reference[2285] = 'value_58166'
    assert tree.invariants(), "Invariants failed at step 6830"

    # Operation 6831: delete
    del tree[2285]
    del reference[2285]
    assert tree.invariants(), "Invariants failed at step 6831"

    # Operation 6832: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6832"

    # Operation 6833: delete
    del tree[9142]
    del reference[9142]
    assert tree.invariants(), "Invariants failed at step 6833"

    # Operation 6834: get
    assert tree.invariants(), "Invariants failed at step 6834"

    # Operation 6835: insert
    tree[3965] = 'value_714832'
    reference[3965] = 'value_714832'
    assert tree.invariants(), "Invariants failed at step 6835"

    # Operation 6836: delete
    del tree[3965]
    del reference[3965]
    assert tree.invariants(), "Invariants failed at step 6836"

    # Operation 6837: insert
    tree[2642] = 'value_771281'
    reference[2642] = 'value_771281'
    assert tree.invariants(), "Invariants failed at step 6837"

    # Operation 6838: insert
    tree[2302] = 'value_124135'
    reference[2302] = 'value_124135'
    assert tree.invariants(), "Invariants failed at step 6838"

    # Operation 6839: update
    tree[2642] = 'value_399921'
    reference[2642] = 'value_399921'
    assert tree.invariants(), "Invariants failed at step 6839"

    # Operation 6840: update
    tree[2642] = 'value_519821'
    reference[2642] = 'value_519821'
    assert tree.invariants(), "Invariants failed at step 6840"

    # Operation 6841: delete
    del tree[2642]
    del reference[2642]
    assert tree.invariants(), "Invariants failed at step 6841"

    # Operation 6842: insert
    tree[7003] = 'value_914382'
    reference[7003] = 'value_914382'
    assert tree.invariants(), "Invariants failed at step 6842"

    # Operation 6843: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6843"

    # Operation 6844: delete
    del tree[2302]
    del reference[2302]
    assert tree.invariants(), "Invariants failed at step 6844"

    # Operation 6845: get
    assert tree.invariants(), "Invariants failed at step 6845"

    # Operation 6846: delete
    del tree[7003]
    del reference[7003]
    assert tree.invariants(), "Invariants failed at step 6846"

    # Operation 6847: insert
    tree[7680] = 'value_347543'
    reference[7680] = 'value_347543'
    assert tree.invariants(), "Invariants failed at step 6847"

    # Operation 6848: delete
    del tree[7680]
    del reference[7680]
    assert tree.invariants(), "Invariants failed at step 6848"

    # Operation 6849: get
    assert tree.invariants(), "Invariants failed at step 6849"

    # Operation 6850: insert
    tree[6922] = 'value_843010'
    reference[6922] = 'value_843010'
    assert tree.invariants(), "Invariants failed at step 6850"

    # Operation 6851: insert
    tree[9371] = 'value_365128'
    reference[9371] = 'value_365128'
    assert tree.invariants(), "Invariants failed at step 6851"

    # Operation 6852: get
    assert tree.invariants(), "Invariants failed at step 6852"

    # Operation 6853: update
    tree[9371] = 'value_304295'
    reference[9371] = 'value_304295'
    assert tree.invariants(), "Invariants failed at step 6853"

    # Operation 6854: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6854"

    # Operation 6855: insert
    tree[885] = 'value_514891'
    reference[885] = 'value_514891'
    assert tree.invariants(), "Invariants failed at step 6855"

    # Operation 6856: get
    assert tree.invariants(), "Invariants failed at step 6856"

    # Operation 6857: get
    assert tree.invariants(), "Invariants failed at step 6857"

    # Operation 6858: delete
    del tree[9371]
    del reference[9371]
    assert tree.invariants(), "Invariants failed at step 6858"

    # Operation 6859: insert
    tree[8818] = 'value_407551'
    reference[8818] = 'value_407551'
    assert tree.invariants(), "Invariants failed at step 6859"

    # Operation 6860: get
    assert tree.invariants(), "Invariants failed at step 6860"

    # Operation 6861: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6861"

    # Operation 6862: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6862"

    # Operation 6863: insert
    tree[1877] = 'value_507525'
    reference[1877] = 'value_507525'
    assert tree.invariants(), "Invariants failed at step 6863"

    # Operation 6864: get
    assert tree.invariants(), "Invariants failed at step 6864"

    # Operation 6865: get
    assert tree.invariants(), "Invariants failed at step 6865"

    # Operation 6866: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6866"

    # Operation 6867: delete
    del tree[6922]
    del reference[6922]
    assert tree.invariants(), "Invariants failed at step 6867"

    # Operation 6868: get
    assert tree.invariants(), "Invariants failed at step 6868"

    # Operation 6869: insert
    tree[1601] = 'value_969970'
    reference[1601] = 'value_969970'
    assert tree.invariants(), "Invariants failed at step 6869"

    # Operation 6870: delete
    del tree[1601]
    del reference[1601]
    assert tree.invariants(), "Invariants failed at step 6870"

    # Operation 6871: delete
    del tree[8818]
    del reference[8818]
    assert tree.invariants(), "Invariants failed at step 6871"

    # Operation 6872: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6872"

    # Operation 6873: update
    tree[1877] = 'value_464107'
    reference[1877] = 'value_464107'
    assert tree.invariants(), "Invariants failed at step 6873"

    # Operation 6874: insert
    tree[1167] = 'value_862453'
    reference[1167] = 'value_862453'
    assert tree.invariants(), "Invariants failed at step 6874"

    # Operation 6875: insert
    tree[1164] = 'value_945320'
    reference[1164] = 'value_945320'
    assert tree.invariants(), "Invariants failed at step 6875"

    # Operation 6876: update
    tree[1164] = 'value_360725'
    reference[1164] = 'value_360725'
    assert tree.invariants(), "Invariants failed at step 6876"

    # Operation 6877: get
    assert tree.invariants(), "Invariants failed at step 6877"

    # Operation 6878: insert
    tree[4259] = 'value_942330'
    reference[4259] = 'value_942330'
    assert tree.invariants(), "Invariants failed at step 6878"

    # Operation 6879: get
    assert tree.invariants(), "Invariants failed at step 6879"

    # Operation 6880: delete
    del tree[1877]
    del reference[1877]
    assert tree.invariants(), "Invariants failed at step 6880"

    # Operation 6881: update
    tree[1164] = 'value_423253'
    reference[1164] = 'value_423253'
    assert tree.invariants(), "Invariants failed at step 6881"

    # Operation 6882: insert
    tree[3702] = 'value_244393'
    reference[3702] = 'value_244393'
    assert tree.invariants(), "Invariants failed at step 6882"

    # Operation 6883: insert
    tree[5341] = 'value_901179'
    reference[5341] = 'value_901179'
    assert tree.invariants(), "Invariants failed at step 6883"

    # Operation 6884: get
    assert tree.invariants(), "Invariants failed at step 6884"

    # Operation 6885: insert
    tree[9090] = 'value_396481'
    reference[9090] = 'value_396481'
    assert tree.invariants(), "Invariants failed at step 6885"

    # Operation 6886: update
    tree[4259] = 'value_764973'
    reference[4259] = 'value_764973'
    assert tree.invariants(), "Invariants failed at step 6886"

    # Operation 6887: get
    assert tree.invariants(), "Invariants failed at step 6887"

    # Operation 6888: delete
    del tree[1167]
    del reference[1167]
    assert tree.invariants(), "Invariants failed at step 6888"

    # Operation 6889: insert
    tree[4319] = 'value_572478'
    reference[4319] = 'value_572478'
    assert tree.invariants(), "Invariants failed at step 6889"

    # Operation 6890: delete
    del tree[885]
    del reference[885]
    assert tree.invariants(), "Invariants failed at step 6890"

    # Operation 6891: delete
    del tree[4259]
    del reference[4259]
    assert tree.invariants(), "Invariants failed at step 6891"

    # Operation 6892: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6892"

    # Operation 6893: insert
    tree[7405] = 'value_779842'
    reference[7405] = 'value_779842'
    assert tree.invariants(), "Invariants failed at step 6893"

    # Operation 6894: delete
    del tree[1164]
    del reference[1164]
    assert tree.invariants(), "Invariants failed at step 6894"

    # Operation 6895: delete
    del tree[5341]
    del reference[5341]
    assert tree.invariants(), "Invariants failed at step 6895"

    # Operation 6896: insert
    tree[9929] = 'value_943129'
    reference[9929] = 'value_943129'
    assert tree.invariants(), "Invariants failed at step 6896"

    # Operation 6897: update
    tree[4319] = 'value_717428'
    reference[4319] = 'value_717428'
    assert tree.invariants(), "Invariants failed at step 6897"

    # Operation 6898: get
    assert tree.invariants(), "Invariants failed at step 6898"

    # Operation 6899: get
    assert tree.invariants(), "Invariants failed at step 6899"

    # Operation 6900: get
    assert tree.invariants(), "Invariants failed at step 6900"

    # Operation 6901: delete
    del tree[9090]
    del reference[9090]
    assert tree.invariants(), "Invariants failed at step 6901"

    # Operation 6902: update
    tree[4319] = 'value_741739'
    reference[4319] = 'value_741739'
    assert tree.invariants(), "Invariants failed at step 6902"

    # Operation 6903: delete
    del tree[4319]
    del reference[4319]
    assert tree.invariants(), "Invariants failed at step 6903"

    # Operation 6904: get
    assert tree.invariants(), "Invariants failed at step 6904"

    # Operation 6905: insert
    tree[9997] = 'value_154853'
    reference[9997] = 'value_154853'
    assert tree.invariants(), "Invariants failed at step 6905"

    # Operation 6906: insert
    tree[24] = 'value_56687'
    reference[24] = 'value_56687'
    assert tree.invariants(), "Invariants failed at step 6906"

    # Operation 6907: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6907"

    # Operation 6908: get
    assert tree.invariants(), "Invariants failed at step 6908"

    # Operation 6909: get
    assert tree.invariants(), "Invariants failed at step 6909"

    # Operation 6910: get
    assert tree.invariants(), "Invariants failed at step 6910"

    # Operation 6911: update
    tree[9997] = 'value_427427'
    reference[9997] = 'value_427427'
    assert tree.invariants(), "Invariants failed at step 6911"

    # Operation 6912: update
    tree[9997] = 'value_587823'
    reference[9997] = 'value_587823'
    assert tree.invariants(), "Invariants failed at step 6912"

    # Operation 6913: get
    assert tree.invariants(), "Invariants failed at step 6913"

    # Operation 6914: insert
    tree[9647] = 'value_695353'
    reference[9647] = 'value_695353'
    assert tree.invariants(), "Invariants failed at step 6914"

    # Operation 6915: get
    assert tree.invariants(), "Invariants failed at step 6915"

    # Operation 6916: batch_delete
    keys_to_delete = [9120, 9929, 7405, 9997, 3199]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6916"

    # Operation 6917: insert
    tree[5613] = 'value_279188'
    reference[5613] = 'value_279188'
    assert tree.invariants(), "Invariants failed at step 6917"

    # Operation 6918: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6918"

    # Operation 6919: insert
    tree[9982] = 'value_430183'
    reference[9982] = 'value_430183'
    assert tree.invariants(), "Invariants failed at step 6919"

    # Operation 6920: insert
    tree[1615] = 'value_228292'
    reference[1615] = 'value_228292'
    assert tree.invariants(), "Invariants failed at step 6920"

    # Operation 6921: insert
    tree[8758] = 'value_835192'
    reference[8758] = 'value_835192'
    assert tree.invariants(), "Invariants failed at step 6921"

    # Operation 6922: get
    assert tree.invariants(), "Invariants failed at step 6922"

    # Operation 6923: insert
    tree[2258] = 'value_944370'
    reference[2258] = 'value_944370'
    assert tree.invariants(), "Invariants failed at step 6923"

    # Operation 6924: insert
    tree[4869] = 'value_175353'
    reference[4869] = 'value_175353'
    assert tree.invariants(), "Invariants failed at step 6924"

    # Operation 6925: delete
    del tree[2258]
    del reference[2258]
    assert tree.invariants(), "Invariants failed at step 6925"

    # Operation 6926: update
    tree[1615] = 'value_760993'
    reference[1615] = 'value_760993'
    assert tree.invariants(), "Invariants failed at step 6926"

    # Operation 6927: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6927"

    # Operation 6928: delete
    del tree[9647]
    del reference[9647]
    assert tree.invariants(), "Invariants failed at step 6928"

    # Operation 6929: update
    tree[4869] = 'value_240336'
    reference[4869] = 'value_240336'
    assert tree.invariants(), "Invariants failed at step 6929"

    # Operation 6930: get
    assert tree.invariants(), "Invariants failed at step 6930"

    # Operation 6931: delete
    del tree[3702]
    del reference[3702]
    assert tree.invariants(), "Invariants failed at step 6931"

    # Operation 6932: insert
    tree[7497] = 'value_336375'
    reference[7497] = 'value_336375'
    assert tree.invariants(), "Invariants failed at step 6932"

    # Operation 6933: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6933"

    # Operation 6934: get
    assert tree.invariants(), "Invariants failed at step 6934"

    # Operation 6935: get
    assert tree.invariants(), "Invariants failed at step 6935"

    # Operation 6936: get
    assert tree.invariants(), "Invariants failed at step 6936"

    # Operation 6937: insert
    tree[6286] = 'value_246744'
    reference[6286] = 'value_246744'
    assert tree.invariants(), "Invariants failed at step 6937"

    # Operation 6938: insert
    tree[6888] = 'value_473404'
    reference[6888] = 'value_473404'
    assert tree.invariants(), "Invariants failed at step 6938"

    # Operation 6939: get
    assert tree.invariants(), "Invariants failed at step 6939"

    # Operation 6940: insert
    tree[4606] = 'value_46928'
    reference[4606] = 'value_46928'
    assert tree.invariants(), "Invariants failed at step 6940"

    # Operation 6941: update
    tree[24] = 'value_616256'
    reference[24] = 'value_616256'
    assert tree.invariants(), "Invariants failed at step 6941"

    # Operation 6942: delete
    del tree[6286]
    del reference[6286]
    assert tree.invariants(), "Invariants failed at step 6942"

    # Operation 6943: batch_delete
    keys_to_delete = [4869, 8015, 1615, 24, 9982]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6943"

    # Operation 6944: delete
    del tree[8758]
    del reference[8758]
    assert tree.invariants(), "Invariants failed at step 6944"

    # Operation 6945: insert
    tree[9500] = 'value_762696'
    reference[9500] = 'value_762696'
    assert tree.invariants(), "Invariants failed at step 6945"

    # Operation 6946: get
    assert tree.invariants(), "Invariants failed at step 6946"

    # Operation 6947: get
    assert tree.invariants(), "Invariants failed at step 6947"

    # Operation 6948: delete
    del tree[7497]
    del reference[7497]
    assert tree.invariants(), "Invariants failed at step 6948"

    # Operation 6949: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6949"

    # Operation 6950: delete
    del tree[6888]
    del reference[6888]
    assert tree.invariants(), "Invariants failed at step 6950"

    # Operation 6951: delete
    del tree[9500]
    del reference[9500]
    assert tree.invariants(), "Invariants failed at step 6951"

    # Operation 6952: delete
    del tree[5613]
    del reference[5613]
    assert tree.invariants(), "Invariants failed at step 6952"

    # Operation 6953: update
    tree[4606] = 'value_692162'
    reference[4606] = 'value_692162'
    assert tree.invariants(), "Invariants failed at step 6953"

    # Operation 6954: delete
    del tree[4606]
    del reference[4606]
    assert tree.invariants(), "Invariants failed at step 6954"

    # Operation 6955: insert
    tree[8120] = 'value_949475'
    reference[8120] = 'value_949475'
    assert tree.invariants(), "Invariants failed at step 6955"

    # Operation 6956: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 6956"

    # Operation 6957: insert
    tree[8634] = 'value_28604'
    reference[8634] = 'value_28604'
    assert tree.invariants(), "Invariants failed at step 6957"

    # Operation 6958: get
    assert tree.invariants(), "Invariants failed at step 6958"

    # Operation 6959: update
    tree[8634] = 'value_161733'
    reference[8634] = 'value_161733'
    assert tree.invariants(), "Invariants failed at step 6959"

    # Operation 6960: delete
    del tree[8634]
    del reference[8634]
    assert tree.invariants(), "Invariants failed at step 6960"

    # Operation 6961: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6961"

    # Operation 6962: insert
    tree[9872] = 'value_504012'
    reference[9872] = 'value_504012'
    assert tree.invariants(), "Invariants failed at step 6962"

    # Operation 6963: update
    tree[8120] = 'value_493321'
    reference[8120] = 'value_493321'
    assert tree.invariants(), "Invariants failed at step 6963"

    # Operation 6964: delete
    del tree[9872]
    del reference[9872]
    assert tree.invariants(), "Invariants failed at step 6964"

    # Operation 6965: get
    assert tree.invariants(), "Invariants failed at step 6965"

    # Operation 6966: insert
    tree[4043] = 'value_947938'
    reference[4043] = 'value_947938'
    assert tree.invariants(), "Invariants failed at step 6966"

    # Operation 6967: delete
    del tree[8120]
    del reference[8120]
    assert tree.invariants(), "Invariants failed at step 6967"

    # Operation 6968: delete
    del tree[4043]
    del reference[4043]
    assert tree.invariants(), "Invariants failed at step 6968"

    # Operation 6969: insert
    tree[1564] = 'value_464777'
    reference[1564] = 'value_464777'
    assert tree.invariants(), "Invariants failed at step 6969"

    # Operation 6970: insert
    tree[770] = 'value_697549'
    reference[770] = 'value_697549'
    assert tree.invariants(), "Invariants failed at step 6970"

    # Operation 6971: insert
    tree[5253] = 'value_608032'
    reference[5253] = 'value_608032'
    assert tree.invariants(), "Invariants failed at step 6971"

    # Operation 6972: update
    tree[770] = 'value_400918'
    reference[770] = 'value_400918'
    assert tree.invariants(), "Invariants failed at step 6972"

    # Operation 6973: delete
    del tree[770]
    del reference[770]
    assert tree.invariants(), "Invariants failed at step 6973"

    # Operation 6974: insert
    tree[4600] = 'value_414529'
    reference[4600] = 'value_414529'
    assert tree.invariants(), "Invariants failed at step 6974"

    # Operation 6975: get
    assert tree.invariants(), "Invariants failed at step 6975"

    # Operation 6976: update
    tree[1564] = 'value_991120'
    reference[1564] = 'value_991120'
    assert tree.invariants(), "Invariants failed at step 6976"

    # Operation 6977: insert
    tree[1318] = 'value_938353'
    reference[1318] = 'value_938353'
    assert tree.invariants(), "Invariants failed at step 6977"

    # Operation 6978: insert
    tree[969] = 'value_892825'
    reference[969] = 'value_892825'
    assert tree.invariants(), "Invariants failed at step 6978"

    # Operation 6979: insert
    tree[9221] = 'value_917584'
    reference[9221] = 'value_917584'
    assert tree.invariants(), "Invariants failed at step 6979"

    # Operation 6980: insert
    tree[7165] = 'value_684523'
    reference[7165] = 'value_684523'
    assert tree.invariants(), "Invariants failed at step 6980"

    # Operation 6981: insert
    tree[3404] = 'value_875653'
    reference[3404] = 'value_875653'
    assert tree.invariants(), "Invariants failed at step 6981"

    # Operation 6982: delete
    del tree[1318]
    del reference[1318]
    assert tree.invariants(), "Invariants failed at step 6982"

    # Operation 6983: get
    assert tree.invariants(), "Invariants failed at step 6983"

    # Operation 6984: get
    assert tree.invariants(), "Invariants failed at step 6984"

    # Operation 6985: delete
    del tree[9221]
    del reference[9221]
    assert tree.invariants(), "Invariants failed at step 6985"

    # Operation 6986: insert
    tree[6439] = 'value_935080'
    reference[6439] = 'value_935080'
    assert tree.invariants(), "Invariants failed at step 6986"

    # Operation 6987: batch_delete
    keys_to_delete = [5253, 969, 5776, 4600, 7165]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 6987"

    # Operation 6988: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 6988"

    # Operation 6989: insert
    tree[7359] = 'value_543141'
    reference[7359] = 'value_543141'
    assert tree.invariants(), "Invariants failed at step 6989"

    # Operation 6990: update
    tree[6439] = 'value_550291'
    reference[6439] = 'value_550291'
    assert tree.invariants(), "Invariants failed at step 6990"

    # Operation 6991: delete
    del tree[6439]
    del reference[6439]
    assert tree.invariants(), "Invariants failed at step 6991"

    # Operation 6992: delete
    del tree[7359]
    del reference[7359]
    assert tree.invariants(), "Invariants failed at step 6992"

    # Operation 6993: update
    tree[1564] = 'value_523759'
    reference[1564] = 'value_523759'
    assert tree.invariants(), "Invariants failed at step 6993"

    # Operation 6994: delete
    del tree[1564]
    del reference[1564]
    assert tree.invariants(), "Invariants failed at step 6994"

    # Operation 6995: delete
    del tree[3404]
    del reference[3404]
    assert tree.invariants(), "Invariants failed at step 6995"

    # Operation 6996: insert
    tree[155] = 'value_82410'
    reference[155] = 'value_82410'
    assert tree.invariants(), "Invariants failed at step 6996"

    # Operation 6997: delete
    del tree[155]
    del reference[155]
    assert tree.invariants(), "Invariants failed at step 6997"

    # Operation 6998: insert
    tree[7054] = 'value_196039'
    reference[7054] = 'value_196039'
    assert tree.invariants(), "Invariants failed at step 6998"

    # Operation 6999: delete
    del tree[7054]
    del reference[7054]
    assert tree.invariants(), "Invariants failed at step 6999"

    # Operation 7000: insert
    tree[7929] = 'value_148353'
    reference[7929] = 'value_148353'
    assert tree.invariants(), "Invariants failed at step 7000"

    # Operation 7001: get
    assert tree.invariants(), "Invariants failed at step 7001"

    # Operation 7002: delete
    del tree[7929]
    del reference[7929]
    assert tree.invariants(), "Invariants failed at step 7002"

    # Operation 7003: get
    assert tree.invariants(), "Invariants failed at step 7003"

    # Operation 7004: insert
    tree[9700] = 'value_402916'
    reference[9700] = 'value_402916'
    assert tree.invariants(), "Invariants failed at step 7004"

    # Operation 7005: get
    assert tree.invariants(), "Invariants failed at step 7005"

    # Operation 7006: insert
    tree[7317] = 'value_723158'
    reference[7317] = 'value_723158'
    assert tree.invariants(), "Invariants failed at step 7006"

    # Operation 7007: delete
    del tree[9700]
    del reference[9700]
    assert tree.invariants(), "Invariants failed at step 7007"

    # Operation 7008: update
    tree[7317] = 'value_873463'
    reference[7317] = 'value_873463'
    assert tree.invariants(), "Invariants failed at step 7008"

    # Operation 7009: delete
    del tree[7317]
    del reference[7317]
    assert tree.invariants(), "Invariants failed at step 7009"

    # Operation 7010: insert
    tree[7762] = 'value_887828'
    reference[7762] = 'value_887828'
    assert tree.invariants(), "Invariants failed at step 7010"

    # Operation 7011: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7011"

    # Operation 7012: delete
    del tree[7762]
    del reference[7762]
    assert tree.invariants(), "Invariants failed at step 7012"

    # Operation 7013: insert
    tree[1946] = 'value_711905'
    reference[1946] = 'value_711905'
    assert tree.invariants(), "Invariants failed at step 7013"

    # Operation 7014: delete
    del tree[1946]
    del reference[1946]
    assert tree.invariants(), "Invariants failed at step 7014"

    # Operation 7015: get
    assert tree.invariants(), "Invariants failed at step 7015"

    # Operation 7016: get
    assert tree.invariants(), "Invariants failed at step 7016"

    # Operation 7017: insert
    tree[5719] = 'value_957320'
    reference[5719] = 'value_957320'
    assert tree.invariants(), "Invariants failed at step 7017"

    # Operation 7018: insert
    tree[6967] = 'value_419205'
    reference[6967] = 'value_419205'
    assert tree.invariants(), "Invariants failed at step 7018"

    # Operation 7019: insert
    tree[8574] = 'value_877901'
    reference[8574] = 'value_877901'
    assert tree.invariants(), "Invariants failed at step 7019"

    # Operation 7020: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7020"

    # Operation 7021: get
    assert tree.invariants(), "Invariants failed at step 7021"

    # Operation 7022: get
    assert tree.invariants(), "Invariants failed at step 7022"

    # Operation 7023: get
    assert tree.invariants(), "Invariants failed at step 7023"

    # Operation 7024: insert
    tree[8057] = 'value_592257'
    reference[8057] = 'value_592257'
    assert tree.invariants(), "Invariants failed at step 7024"

    # Operation 7025: delete
    del tree[8574]
    del reference[8574]
    assert tree.invariants(), "Invariants failed at step 7025"

    # Operation 7026: get
    assert tree.invariants(), "Invariants failed at step 7026"

    # Operation 7027: delete
    del tree[5719]
    del reference[5719]
    assert tree.invariants(), "Invariants failed at step 7027"

    # Operation 7028: insert
    tree[6178] = 'value_643357'
    reference[6178] = 'value_643357'
    assert tree.invariants(), "Invariants failed at step 7028"

    # Operation 7029: get
    assert tree.invariants(), "Invariants failed at step 7029"

    # Operation 7030: get
    assert tree.invariants(), "Invariants failed at step 7030"

    # Operation 7031: delete
    del tree[8057]
    del reference[8057]
    assert tree.invariants(), "Invariants failed at step 7031"

    # Operation 7032: delete
    del tree[6178]
    del reference[6178]
    assert tree.invariants(), "Invariants failed at step 7032"

    # Operation 7033: get
    assert tree.invariants(), "Invariants failed at step 7033"

    # Operation 7034: get
    assert tree.invariants(), "Invariants failed at step 7034"

    # Operation 7035: insert
    tree[8975] = 'value_344694'
    reference[8975] = 'value_344694'
    assert tree.invariants(), "Invariants failed at step 7035"

    # Operation 7036: delete
    del tree[8975]
    del reference[8975]
    assert tree.invariants(), "Invariants failed at step 7036"

    # Operation 7037: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7037"

    # Operation 7038: insert
    tree[9688] = 'value_656922'
    reference[9688] = 'value_656922'
    assert tree.invariants(), "Invariants failed at step 7038"

    # Operation 7039: delete
    del tree[9688]
    del reference[9688]
    assert tree.invariants(), "Invariants failed at step 7039"

    # Operation 7040: delete
    del tree[6967]
    del reference[6967]
    assert tree.invariants(), "Invariants failed at step 7040"

    # Operation 7041: insert
    tree[2666] = 'value_347992'
    reference[2666] = 'value_347992'
    assert tree.invariants(), "Invariants failed at step 7041"

    # Operation 7042: delete
    del tree[2666]
    del reference[2666]
    assert tree.invariants(), "Invariants failed at step 7042"

    # Operation 7043: insert
    tree[3563] = 'value_346382'
    reference[3563] = 'value_346382'
    assert tree.invariants(), "Invariants failed at step 7043"

    # Operation 7044: delete
    del tree[3563]
    del reference[3563]
    assert tree.invariants(), "Invariants failed at step 7044"

    # Operation 7045: insert
    tree[8351] = 'value_644955'
    reference[8351] = 'value_644955'
    assert tree.invariants(), "Invariants failed at step 7045"

    # Operation 7046: insert
    tree[2194] = 'value_449470'
    reference[2194] = 'value_449470'
    assert tree.invariants(), "Invariants failed at step 7046"

    # Operation 7047: insert
    tree[3951] = 'value_294967'
    reference[3951] = 'value_294967'
    assert tree.invariants(), "Invariants failed at step 7047"

    # Operation 7048: update
    tree[2194] = 'value_683840'
    reference[2194] = 'value_683840'
    assert tree.invariants(), "Invariants failed at step 7048"

    # Operation 7049: insert
    tree[1683] = 'value_669513'
    reference[1683] = 'value_669513'
    assert tree.invariants(), "Invariants failed at step 7049"

    # Operation 7050: insert
    tree[5541] = 'value_483669'
    reference[5541] = 'value_483669'
    assert tree.invariants(), "Invariants failed at step 7050"

    # Operation 7051: get
    assert tree.invariants(), "Invariants failed at step 7051"

    # Operation 7052: batch_delete
    keys_to_delete = [6155, 1683, 5541, 3951]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7052"

    # Operation 7053: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7053"

    # Operation 7054: update
    tree[8351] = 'value_923899'
    reference[8351] = 'value_923899'
    assert tree.invariants(), "Invariants failed at step 7054"

    # Operation 7055: update
    tree[8351] = 'value_149497'
    reference[8351] = 'value_149497'
    assert tree.invariants(), "Invariants failed at step 7055"

    # Operation 7056: delete
    del tree[2194]
    del reference[2194]
    assert tree.invariants(), "Invariants failed at step 7056"

    # Operation 7057: update
    tree[8351] = 'value_952674'
    reference[8351] = 'value_952674'
    assert tree.invariants(), "Invariants failed at step 7057"

    # Operation 7058: delete
    del tree[8351]
    del reference[8351]
    assert tree.invariants(), "Invariants failed at step 7058"

    # Operation 7059: insert
    tree[3690] = 'value_812133'
    reference[3690] = 'value_812133'
    assert tree.invariants(), "Invariants failed at step 7059"

    # Operation 7060: get
    assert tree.invariants(), "Invariants failed at step 7060"

    # Operation 7061: delete
    del tree[3690]
    del reference[3690]
    assert tree.invariants(), "Invariants failed at step 7061"

    # Operation 7062: insert
    tree[6765] = 'value_353352'
    reference[6765] = 'value_353352'
    assert tree.invariants(), "Invariants failed at step 7062"

    # Operation 7063: get
    assert tree.invariants(), "Invariants failed at step 7063"

    # Operation 7064: update
    tree[6765] = 'value_758592'
    reference[6765] = 'value_758592'
    assert tree.invariants(), "Invariants failed at step 7064"

    # Operation 7065: insert
    tree[5782] = 'value_102938'
    reference[5782] = 'value_102938'
    assert tree.invariants(), "Invariants failed at step 7065"

    # Operation 7066: insert
    tree[1353] = 'value_669411'
    reference[1353] = 'value_669411'
    assert tree.invariants(), "Invariants failed at step 7066"

    # Operation 7067: delete
    del tree[5782]
    del reference[5782]
    assert tree.invariants(), "Invariants failed at step 7067"

    # Operation 7068: update
    tree[1353] = 'value_840520'
    reference[1353] = 'value_840520'
    assert tree.invariants(), "Invariants failed at step 7068"

    # Operation 7069: update
    tree[1353] = 'value_31050'
    reference[1353] = 'value_31050'
    assert tree.invariants(), "Invariants failed at step 7069"

    # Operation 7070: insert
    tree[1271] = 'value_867930'
    reference[1271] = 'value_867930'
    assert tree.invariants(), "Invariants failed at step 7070"

    # Operation 7071: insert
    tree[180] = 'value_773948'
    reference[180] = 'value_773948'
    assert tree.invariants(), "Invariants failed at step 7071"

    # Operation 7072: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7072"

    # Operation 7073: insert
    tree[3051] = 'value_35456'
    reference[3051] = 'value_35456'
    assert tree.invariants(), "Invariants failed at step 7073"

    # Operation 7074: insert
    tree[9601] = 'value_403565'
    reference[9601] = 'value_403565'
    assert tree.invariants(), "Invariants failed at step 7074"

    # Operation 7075: insert
    tree[6214] = 'value_382239'
    reference[6214] = 'value_382239'
    assert tree.invariants(), "Invariants failed at step 7075"

    # Operation 7076: delete
    del tree[9601]
    del reference[9601]
    assert tree.invariants(), "Invariants failed at step 7076"

    # Operation 7077: delete
    del tree[6765]
    del reference[6765]
    assert tree.invariants(), "Invariants failed at step 7077"

    # Operation 7078: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7078"

    # Operation 7079: get
    assert tree.invariants(), "Invariants failed at step 7079"

    # Operation 7080: batch_delete
    keys_to_delete = [1353, 6214, 935]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7080"

    # Operation 7081: insert
    tree[9836] = 'value_842302'
    reference[9836] = 'value_842302'
    assert tree.invariants(), "Invariants failed at step 7081"

    # Operation 7082: insert
    tree[8939] = 'value_429840'
    reference[8939] = 'value_429840'
    assert tree.invariants(), "Invariants failed at step 7082"

    # Operation 7083: delete
    del tree[8939]
    del reference[8939]
    assert tree.invariants(), "Invariants failed at step 7083"

    # Operation 7084: update
    tree[3051] = 'value_854783'
    reference[3051] = 'value_854783'
    assert tree.invariants(), "Invariants failed at step 7084"

    # Operation 7085: delete
    del tree[9836]
    del reference[9836]
    assert tree.invariants(), "Invariants failed at step 7085"

    # Operation 7086: get
    assert tree.invariants(), "Invariants failed at step 7086"

    # Operation 7087: insert
    tree[1212] = 'value_825898'
    reference[1212] = 'value_825898'
    assert tree.invariants(), "Invariants failed at step 7087"

    # Operation 7088: delete
    del tree[180]
    del reference[180]
    assert tree.invariants(), "Invariants failed at step 7088"

    # Operation 7089: insert
    tree[5612] = 'value_569305'
    reference[5612] = 'value_569305'
    assert tree.invariants(), "Invariants failed at step 7089"

    # Operation 7090: update
    tree[5612] = 'value_718621'
    reference[5612] = 'value_718621'
    assert tree.invariants(), "Invariants failed at step 7090"

    # Operation 7091: update
    tree[5612] = 'value_99181'
    reference[5612] = 'value_99181'
    assert tree.invariants(), "Invariants failed at step 7091"

    # Operation 7092: update
    tree[1271] = 'value_938865'
    reference[1271] = 'value_938865'
    assert tree.invariants(), "Invariants failed at step 7092"

    # Operation 7093: delete
    del tree[1271]
    del reference[1271]
    assert tree.invariants(), "Invariants failed at step 7093"

    # Operation 7094: insert
    tree[5908] = 'value_661872'
    reference[5908] = 'value_661872'
    assert tree.invariants(), "Invariants failed at step 7094"

    # Operation 7095: get
    assert tree.invariants(), "Invariants failed at step 7095"

    # Operation 7096: delete
    del tree[5908]
    del reference[5908]
    assert tree.invariants(), "Invariants failed at step 7096"

    # Operation 7097: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7097"

    # Operation 7098: delete
    del tree[5612]
    del reference[5612]
    assert tree.invariants(), "Invariants failed at step 7098"

    # Operation 7099: update
    tree[3051] = 'value_90533'
    reference[3051] = 'value_90533'
    assert tree.invariants(), "Invariants failed at step 7099"

    # Operation 7100: get
    assert tree.invariants(), "Invariants failed at step 7100"

    # Operation 7101: delete
    del tree[3051]
    del reference[3051]
    assert tree.invariants(), "Invariants failed at step 7101"

    # Operation 7102: get
    assert tree.invariants(), "Invariants failed at step 7102"

    # Operation 7103: update
    tree[1212] = 'value_798898'
    reference[1212] = 'value_798898'
    assert tree.invariants(), "Invariants failed at step 7103"

    # Operation 7104: update
    tree[1212] = 'value_198782'
    reference[1212] = 'value_198782'
    assert tree.invariants(), "Invariants failed at step 7104"

    # Operation 7105: get
    assert tree.invariants(), "Invariants failed at step 7105"

    # Operation 7106: insert
    tree[293] = 'value_850375'
    reference[293] = 'value_850375'
    assert tree.invariants(), "Invariants failed at step 7106"

    # Operation 7107: insert
    tree[8078] = 'value_929019'
    reference[8078] = 'value_929019'
    assert tree.invariants(), "Invariants failed at step 7107"

    # Operation 7108: get
    assert tree.invariants(), "Invariants failed at step 7108"

    # Operation 7109: get
    assert tree.invariants(), "Invariants failed at step 7109"

    # Operation 7110: delete
    del tree[293]
    del reference[293]
    assert tree.invariants(), "Invariants failed at step 7110"

    # Operation 7111: delete
    del tree[1212]
    del reference[1212]
    assert tree.invariants(), "Invariants failed at step 7111"

    # Operation 7112: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7112"

    # Operation 7113: delete
    del tree[8078]
    del reference[8078]
    assert tree.invariants(), "Invariants failed at step 7113"

    # Operation 7114: insert
    tree[6018] = 'value_757395'
    reference[6018] = 'value_757395'
    assert tree.invariants(), "Invariants failed at step 7114"

    # Operation 7115: delete
    del tree[6018]
    del reference[6018]
    assert tree.invariants(), "Invariants failed at step 7115"

    # Operation 7116: get
    assert tree.invariants(), "Invariants failed at step 7116"

    # Operation 7117: insert
    tree[8550] = 'value_286659'
    reference[8550] = 'value_286659'
    assert tree.invariants(), "Invariants failed at step 7117"

    # Operation 7118: delete
    del tree[8550]
    del reference[8550]
    assert tree.invariants(), "Invariants failed at step 7118"

    # Operation 7119: insert
    tree[8207] = 'value_323231'
    reference[8207] = 'value_323231'
    assert tree.invariants(), "Invariants failed at step 7119"

    # Operation 7120: insert
    tree[8042] = 'value_738209'
    reference[8042] = 'value_738209'
    assert tree.invariants(), "Invariants failed at step 7120"

    # Operation 7121: delete
    del tree[8207]
    del reference[8207]
    assert tree.invariants(), "Invariants failed at step 7121"

    # Operation 7122: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7122"

    # Operation 7123: get
    assert tree.invariants(), "Invariants failed at step 7123"

    # Operation 7124: get
    assert tree.invariants(), "Invariants failed at step 7124"

    # Operation 7125: get
    assert tree.invariants(), "Invariants failed at step 7125"

    # Operation 7126: update
    tree[8042] = 'value_739333'
    reference[8042] = 'value_739333'
    assert tree.invariants(), "Invariants failed at step 7126"

    # Operation 7127: insert
    tree[169] = 'value_163075'
    reference[169] = 'value_163075'
    assert tree.invariants(), "Invariants failed at step 7127"

    # Operation 7128: insert
    tree[8271] = 'value_979266'
    reference[8271] = 'value_979266'
    assert tree.invariants(), "Invariants failed at step 7128"

    # Operation 7129: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7129"

    # Operation 7130: get
    assert tree.invariants(), "Invariants failed at step 7130"

    # Operation 7131: delete
    del tree[8042]
    del reference[8042]
    assert tree.invariants(), "Invariants failed at step 7131"

    # Operation 7132: delete
    del tree[8271]
    del reference[8271]
    assert tree.invariants(), "Invariants failed at step 7132"

    # Operation 7133: get
    assert tree.invariants(), "Invariants failed at step 7133"

    # Operation 7134: insert
    tree[6197] = 'value_835704'
    reference[6197] = 'value_835704'
    assert tree.invariants(), "Invariants failed at step 7134"

    # Operation 7135: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7135"

    # Operation 7136: insert
    tree[9859] = 'value_80045'
    reference[9859] = 'value_80045'
    assert tree.invariants(), "Invariants failed at step 7136"

    # Operation 7137: insert
    tree[5574] = 'value_673726'
    reference[5574] = 'value_673726'
    assert tree.invariants(), "Invariants failed at step 7137"

    # Operation 7138: delete
    del tree[9859]
    del reference[9859]
    assert tree.invariants(), "Invariants failed at step 7138"

    # Operation 7139: get
    assert tree.invariants(), "Invariants failed at step 7139"

    # Operation 7140: update
    tree[169] = 'value_774695'
    reference[169] = 'value_774695'
    assert tree.invariants(), "Invariants failed at step 7140"

    # Operation 7141: get
    assert tree.invariants(), "Invariants failed at step 7141"

    # Operation 7142: delete
    del tree[169]
    del reference[169]
    assert tree.invariants(), "Invariants failed at step 7142"

    # Operation 7143: delete
    del tree[6197]
    del reference[6197]
    assert tree.invariants(), "Invariants failed at step 7143"

    # Operation 7144: insert
    tree[3236] = 'value_630950'
    reference[3236] = 'value_630950'
    assert tree.invariants(), "Invariants failed at step 7144"

    # Operation 7145: update
    tree[3236] = 'value_761469'
    reference[3236] = 'value_761469'
    assert tree.invariants(), "Invariants failed at step 7145"

    # Operation 7146: delete
    del tree[5574]
    del reference[5574]
    assert tree.invariants(), "Invariants failed at step 7146"

    # Operation 7147: delete
    del tree[3236]
    del reference[3236]
    assert tree.invariants(), "Invariants failed at step 7147"

    # Operation 7148: get
    assert tree.invariants(), "Invariants failed at step 7148"

    # Operation 7149: get
    assert tree.invariants(), "Invariants failed at step 7149"

    # Operation 7150: get
    assert tree.invariants(), "Invariants failed at step 7150"

    # Operation 7151: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7151"

    # Operation 7152: insert
    tree[8859] = 'value_250090'
    reference[8859] = 'value_250090'
    assert tree.invariants(), "Invariants failed at step 7152"

    # Operation 7153: get
    assert tree.invariants(), "Invariants failed at step 7153"

    # Operation 7154: delete
    del tree[8859]
    del reference[8859]
    assert tree.invariants(), "Invariants failed at step 7154"

    # Operation 7155: get
    assert tree.invariants(), "Invariants failed at step 7155"

    # Operation 7156: get
    assert tree.invariants(), "Invariants failed at step 7156"

    # Operation 7157: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7157"

    # Operation 7158: get
    assert tree.invariants(), "Invariants failed at step 7158"

    # Operation 7159: get
    assert tree.invariants(), "Invariants failed at step 7159"

    # Operation 7160: insert
    tree[1277] = 'value_454103'
    reference[1277] = 'value_454103'
    assert tree.invariants(), "Invariants failed at step 7160"

    # Operation 7161: update
    tree[1277] = 'value_113794'
    reference[1277] = 'value_113794'
    assert tree.invariants(), "Invariants failed at step 7161"

    # Operation 7162: update
    tree[1277] = 'value_717141'
    reference[1277] = 'value_717141'
    assert tree.invariants(), "Invariants failed at step 7162"

    # Operation 7163: update
    tree[1277] = 'value_712731'
    reference[1277] = 'value_712731'
    assert tree.invariants(), "Invariants failed at step 7163"

    # Operation 7164: insert
    tree[5510] = 'value_21758'
    reference[5510] = 'value_21758'
    assert tree.invariants(), "Invariants failed at step 7164"

    # Operation 7165: delete
    del tree[1277]
    del reference[1277]
    assert tree.invariants(), "Invariants failed at step 7165"

    # Operation 7166: delete
    del tree[5510]
    del reference[5510]
    assert tree.invariants(), "Invariants failed at step 7166"

    # Operation 7167: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7167"

    # Operation 7168: get
    assert tree.invariants(), "Invariants failed at step 7168"

    # Operation 7169: insert
    tree[8442] = 'value_89853'
    reference[8442] = 'value_89853'
    assert tree.invariants(), "Invariants failed at step 7169"

    # Operation 7170: update
    tree[8442] = 'value_468952'
    reference[8442] = 'value_468952'
    assert tree.invariants(), "Invariants failed at step 7170"

    # Operation 7171: insert
    tree[6524] = 'value_849749'
    reference[6524] = 'value_849749'
    assert tree.invariants(), "Invariants failed at step 7171"

    # Operation 7172: insert
    tree[5267] = 'value_8722'
    reference[5267] = 'value_8722'
    assert tree.invariants(), "Invariants failed at step 7172"

    # Operation 7173: delete
    del tree[6524]
    del reference[6524]
    assert tree.invariants(), "Invariants failed at step 7173"

    # Operation 7174: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7174"

    # Operation 7175: get
    assert tree.invariants(), "Invariants failed at step 7175"

    # Operation 7176: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7176"

    # Operation 7177: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7177"

    # Operation 7178: delete
    del tree[5267]
    del reference[5267]
    assert tree.invariants(), "Invariants failed at step 7178"

    # Operation 7179: get
    assert tree.invariants(), "Invariants failed at step 7179"

    # Operation 7180: update
    tree[8442] = 'value_711374'
    reference[8442] = 'value_711374'
    assert tree.invariants(), "Invariants failed at step 7180"

    # Operation 7181: update
    tree[8442] = 'value_894791'
    reference[8442] = 'value_894791'
    assert tree.invariants(), "Invariants failed at step 7181"

    # Operation 7182: get
    assert tree.invariants(), "Invariants failed at step 7182"

    # Operation 7183: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7183"

    # Operation 7184: insert
    tree[9604] = 'value_889628'
    reference[9604] = 'value_889628'
    assert tree.invariants(), "Invariants failed at step 7184"

    # Operation 7185: insert
    tree[4193] = 'value_103452'
    reference[4193] = 'value_103452'
    assert tree.invariants(), "Invariants failed at step 7185"

    # Operation 7186: update
    tree[8442] = 'value_387354'
    reference[8442] = 'value_387354'
    assert tree.invariants(), "Invariants failed at step 7186"

    # Operation 7187: insert
    tree[5408] = 'value_759689'
    reference[5408] = 'value_759689'
    assert tree.invariants(), "Invariants failed at step 7187"

    # Operation 7188: delete
    del tree[8442]
    del reference[8442]
    assert tree.invariants(), "Invariants failed at step 7188"

    # Operation 7189: insert
    tree[9702] = 'value_738473'
    reference[9702] = 'value_738473'
    assert tree.invariants(), "Invariants failed at step 7189"

    # Operation 7190: get
    assert tree.invariants(), "Invariants failed at step 7190"

    # Operation 7191: update
    tree[5408] = 'value_823920'
    reference[5408] = 'value_823920'
    assert tree.invariants(), "Invariants failed at step 7191"

    # Operation 7192: get
    assert tree.invariants(), "Invariants failed at step 7192"

    # Operation 7193: insert
    tree[7474] = 'value_273105'
    reference[7474] = 'value_273105'
    assert tree.invariants(), "Invariants failed at step 7193"

    # Operation 7194: delete
    del tree[7474]
    del reference[7474]
    assert tree.invariants(), "Invariants failed at step 7194"

    # Operation 7195: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7195"

    # Operation 7196: update
    tree[9604] = 'value_988139'
    reference[9604] = 'value_988139'
    assert tree.invariants(), "Invariants failed at step 7196"

    # Operation 7197: delete
    del tree[9604]
    del reference[9604]
    assert tree.invariants(), "Invariants failed at step 7197"

    # Operation 7198: delete
    del tree[5408]
    del reference[5408]
    assert tree.invariants(), "Invariants failed at step 7198"

    # Operation 7199: update
    tree[9702] = 'value_983615'
    reference[9702] = 'value_983615'
    assert tree.invariants(), "Invariants failed at step 7199"

    # Operation 7200: update
    tree[4193] = 'value_431874'
    reference[4193] = 'value_431874'
    assert tree.invariants(), "Invariants failed at step 7200"

    # Operation 7201: update
    tree[9702] = 'value_367293'
    reference[9702] = 'value_367293'
    assert tree.invariants(), "Invariants failed at step 7201"

    # Operation 7202: insert
    tree[4589] = 'value_540035'
    reference[4589] = 'value_540035'
    assert tree.invariants(), "Invariants failed at step 7202"

    # Operation 7203: insert
    tree[7665] = 'value_968031'
    reference[7665] = 'value_968031'
    assert tree.invariants(), "Invariants failed at step 7203"

    # Operation 7204: insert
    tree[7900] = 'value_358502'
    reference[7900] = 'value_358502'
    assert tree.invariants(), "Invariants failed at step 7204"

    # Operation 7205: delete
    del tree[4589]
    del reference[4589]
    assert tree.invariants(), "Invariants failed at step 7205"

    # Operation 7206: delete
    del tree[7665]
    del reference[7665]
    assert tree.invariants(), "Invariants failed at step 7206"

    # Operation 7207: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7207"

    # Operation 7208: delete
    del tree[7900]
    del reference[7900]
    assert tree.invariants(), "Invariants failed at step 7208"

    # Operation 7209: insert
    tree[8577] = 'value_813033'
    reference[8577] = 'value_813033'
    assert tree.invariants(), "Invariants failed at step 7209"

    # Operation 7210: get
    assert tree.invariants(), "Invariants failed at step 7210"

    # Operation 7211: insert
    tree[2896] = 'value_662811'
    reference[2896] = 'value_662811'
    assert tree.invariants(), "Invariants failed at step 7211"

    # Operation 7212: get
    assert tree.invariants(), "Invariants failed at step 7212"

    # Operation 7213: insert
    tree[9656] = 'value_334010'
    reference[9656] = 'value_334010'
    assert tree.invariants(), "Invariants failed at step 7213"

    # Operation 7214: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7214"

    # Operation 7215: update
    tree[2896] = 'value_631853'
    reference[2896] = 'value_631853'
    assert tree.invariants(), "Invariants failed at step 7215"

    # Operation 7216: update
    tree[8577] = 'value_922719'
    reference[8577] = 'value_922719'
    assert tree.invariants(), "Invariants failed at step 7216"

    # Operation 7217: get
    assert tree.invariants(), "Invariants failed at step 7217"

    # Operation 7218: get
    assert tree.invariants(), "Invariants failed at step 7218"

    # Operation 7219: insert
    tree[2068] = 'value_403169'
    reference[2068] = 'value_403169'
    assert tree.invariants(), "Invariants failed at step 7219"

    # Operation 7220: insert
    tree[623] = 'value_386317'
    reference[623] = 'value_386317'
    assert tree.invariants(), "Invariants failed at step 7220"

    # Operation 7221: insert
    tree[2485] = 'value_279571'
    reference[2485] = 'value_279571'
    assert tree.invariants(), "Invariants failed at step 7221"

    # Operation 7222: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7222"

    # Operation 7223: get
    assert tree.invariants(), "Invariants failed at step 7223"

    # Operation 7224: insert
    tree[2423] = 'value_196940'
    reference[2423] = 'value_196940'
    assert tree.invariants(), "Invariants failed at step 7224"

    # Operation 7225: delete
    del tree[2423]
    del reference[2423]
    assert tree.invariants(), "Invariants failed at step 7225"

    # Operation 7226: delete
    del tree[9702]
    del reference[9702]
    assert tree.invariants(), "Invariants failed at step 7226"

    # Operation 7227: insert
    tree[3517] = 'value_689254'
    reference[3517] = 'value_689254'
    assert tree.invariants(), "Invariants failed at step 7227"

    # Operation 7228: get
    assert tree.invariants(), "Invariants failed at step 7228"

    # Operation 7229: update
    tree[8577] = 'value_434866'
    reference[8577] = 'value_434866'
    assert tree.invariants(), "Invariants failed at step 7229"

    # Operation 7230: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7230"

    # Operation 7231: batch_delete
    keys_to_delete = [9656, 8577, 4257]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7231"

    # Operation 7232: batch_delete
    keys_to_delete = [8265, 2896, 2485, 6237, 3517]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7232"

    # Operation 7233: delete
    del tree[2068]
    del reference[2068]
    assert tree.invariants(), "Invariants failed at step 7233"

    # Operation 7234: delete
    del tree[4193]
    del reference[4193]
    assert tree.invariants(), "Invariants failed at step 7234"

    # Operation 7235: delete
    del tree[623]
    del reference[623]
    assert tree.invariants(), "Invariants failed at step 7235"

    # Operation 7236: insert
    tree[1985] = 'value_793706'
    reference[1985] = 'value_793706'
    assert tree.invariants(), "Invariants failed at step 7236"

    # Operation 7237: update
    tree[1985] = 'value_566993'
    reference[1985] = 'value_566993'
    assert tree.invariants(), "Invariants failed at step 7237"

    # Operation 7238: update
    tree[1985] = 'value_123054'
    reference[1985] = 'value_123054'
    assert tree.invariants(), "Invariants failed at step 7238"

    # Operation 7239: update
    tree[1985] = 'value_117780'
    reference[1985] = 'value_117780'
    assert tree.invariants(), "Invariants failed at step 7239"

    # Operation 7240: delete
    del tree[1985]
    del reference[1985]
    assert tree.invariants(), "Invariants failed at step 7240"

    # Operation 7241: insert
    tree[4866] = 'value_456185'
    reference[4866] = 'value_456185'
    assert tree.invariants(), "Invariants failed at step 7241"

    # Operation 7242: delete
    del tree[4866]
    del reference[4866]
    assert tree.invariants(), "Invariants failed at step 7242"

    # Operation 7243: insert
    tree[5996] = 'value_836328'
    reference[5996] = 'value_836328'
    assert tree.invariants(), "Invariants failed at step 7243"

    # Operation 7244: get
    assert tree.invariants(), "Invariants failed at step 7244"

    # Operation 7245: insert
    tree[4341] = 'value_377883'
    reference[4341] = 'value_377883'
    assert tree.invariants(), "Invariants failed at step 7245"

    # Operation 7246: update
    tree[4341] = 'value_933274'
    reference[4341] = 'value_933274'
    assert tree.invariants(), "Invariants failed at step 7246"

    # Operation 7247: get
    assert tree.invariants(), "Invariants failed at step 7247"

    # Operation 7248: get
    assert tree.invariants(), "Invariants failed at step 7248"

    # Operation 7249: get
    assert tree.invariants(), "Invariants failed at step 7249"

    # Operation 7250: get
    assert tree.invariants(), "Invariants failed at step 7250"

    # Operation 7251: get
    assert tree.invariants(), "Invariants failed at step 7251"

    # Operation 7252: delete
    del tree[5996]
    del reference[5996]
    assert tree.invariants(), "Invariants failed at step 7252"

    # Operation 7253: update
    tree[4341] = 'value_604286'
    reference[4341] = 'value_604286'
    assert tree.invariants(), "Invariants failed at step 7253"

    # Operation 7254: get
    assert tree.invariants(), "Invariants failed at step 7254"

    # Operation 7255: update
    tree[4341] = 'value_722549'
    reference[4341] = 'value_722549'
    assert tree.invariants(), "Invariants failed at step 7255"

    # Operation 7256: update
    tree[4341] = 'value_376377'
    reference[4341] = 'value_376377'
    assert tree.invariants(), "Invariants failed at step 7256"

    # Operation 7257: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7257"

    # Operation 7258: insert
    tree[8068] = 'value_605177'
    reference[8068] = 'value_605177'
    assert tree.invariants(), "Invariants failed at step 7258"

    # Operation 7259: insert
    tree[4358] = 'value_562981'
    reference[4358] = 'value_562981'
    assert tree.invariants(), "Invariants failed at step 7259"

    # Operation 7260: delete
    del tree[8068]
    del reference[8068]
    assert tree.invariants(), "Invariants failed at step 7260"

    # Operation 7261: insert
    tree[7278] = 'value_569127'
    reference[7278] = 'value_569127'
    assert tree.invariants(), "Invariants failed at step 7261"

    # Operation 7262: insert
    tree[6496] = 'value_33145'
    reference[6496] = 'value_33145'
    assert tree.invariants(), "Invariants failed at step 7262"

    # Operation 7263: get
    assert tree.invariants(), "Invariants failed at step 7263"

    # Operation 7264: delete
    del tree[4358]
    del reference[4358]
    assert tree.invariants(), "Invariants failed at step 7264"

    # Operation 7265: get
    assert tree.invariants(), "Invariants failed at step 7265"

    # Operation 7266: insert
    tree[5405] = 'value_1750'
    reference[5405] = 'value_1750'
    assert tree.invariants(), "Invariants failed at step 7266"

    # Operation 7267: get
    assert tree.invariants(), "Invariants failed at step 7267"

    # Operation 7268: get
    assert tree.invariants(), "Invariants failed at step 7268"

    # Operation 7269: get
    assert tree.invariants(), "Invariants failed at step 7269"

    # Operation 7270: update
    tree[6496] = 'value_38178'
    reference[6496] = 'value_38178'
    assert tree.invariants(), "Invariants failed at step 7270"

    # Operation 7271: delete
    del tree[4341]
    del reference[4341]
    assert tree.invariants(), "Invariants failed at step 7271"

    # Operation 7272: delete
    del tree[7278]
    del reference[7278]
    assert tree.invariants(), "Invariants failed at step 7272"

    # Operation 7273: update
    tree[6496] = 'value_809818'
    reference[6496] = 'value_809818'
    assert tree.invariants(), "Invariants failed at step 7273"

    # Operation 7274: get
    assert tree.invariants(), "Invariants failed at step 7274"

    # Operation 7275: update
    tree[6496] = 'value_71236'
    reference[6496] = 'value_71236'
    assert tree.invariants(), "Invariants failed at step 7275"

    # Operation 7276: insert
    tree[1021] = 'value_593496'
    reference[1021] = 'value_593496'
    assert tree.invariants(), "Invariants failed at step 7276"

    # Operation 7277: delete
    del tree[5405]
    del reference[5405]
    assert tree.invariants(), "Invariants failed at step 7277"

    # Operation 7278: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7278"

    # Operation 7279: get
    assert tree.invariants(), "Invariants failed at step 7279"

    # Operation 7280: delete
    del tree[1021]
    del reference[1021]
    assert tree.invariants(), "Invariants failed at step 7280"

    # Operation 7281: get
    assert tree.invariants(), "Invariants failed at step 7281"

    # Operation 7282: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7282"

    # Operation 7283: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7283"

    # Operation 7284: insert
    tree[9904] = 'value_382548'
    reference[9904] = 'value_382548'
    assert tree.invariants(), "Invariants failed at step 7284"

    # Operation 7285: get
    assert tree.invariants(), "Invariants failed at step 7285"

    # Operation 7286: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7286"

    # Operation 7287: get
    assert tree.invariants(), "Invariants failed at step 7287"

    # Operation 7288: delete
    del tree[6496]
    del reference[6496]
    assert tree.invariants(), "Invariants failed at step 7288"

    # Operation 7289: get
    assert tree.invariants(), "Invariants failed at step 7289"

    # Operation 7290: insert
    tree[4755] = 'value_962248'
    reference[4755] = 'value_962248'
    assert tree.invariants(), "Invariants failed at step 7290"

    # Operation 7291: delete
    del tree[9904]
    del reference[9904]
    assert tree.invariants(), "Invariants failed at step 7291"

    # Operation 7292: insert
    tree[1258] = 'value_191953'
    reference[1258] = 'value_191953'
    assert tree.invariants(), "Invariants failed at step 7292"

    # Operation 7293: insert
    tree[7325] = 'value_709321'
    reference[7325] = 'value_709321'
    assert tree.invariants(), "Invariants failed at step 7293"

    # Operation 7294: get
    assert tree.invariants(), "Invariants failed at step 7294"

    # Operation 7295: get
    assert tree.invariants(), "Invariants failed at step 7295"

    # Operation 7296: delete
    del tree[4755]
    del reference[4755]
    assert tree.invariants(), "Invariants failed at step 7296"

    # Operation 7297: insert
    tree[6936] = 'value_572307'
    reference[6936] = 'value_572307'
    assert tree.invariants(), "Invariants failed at step 7297"

    # Operation 7298: get
    assert tree.invariants(), "Invariants failed at step 7298"

    # Operation 7299: get
    assert tree.invariants(), "Invariants failed at step 7299"

    # Operation 7300: delete
    del tree[7325]
    del reference[7325]
    assert tree.invariants(), "Invariants failed at step 7300"

    # Operation 7301: insert
    tree[3254] = 'value_993720'
    reference[3254] = 'value_993720'
    assert tree.invariants(), "Invariants failed at step 7301"

    # Operation 7302: update
    tree[1258] = 'value_366774'
    reference[1258] = 'value_366774'
    assert tree.invariants(), "Invariants failed at step 7302"

    # Operation 7303: get
    assert tree.invariants(), "Invariants failed at step 7303"

    # Operation 7304: insert
    tree[1060] = 'value_168237'
    reference[1060] = 'value_168237'
    assert tree.invariants(), "Invariants failed at step 7304"

    # Operation 7305: insert
    tree[5549] = 'value_212956'
    reference[5549] = 'value_212956'
    assert tree.invariants(), "Invariants failed at step 7305"

    # Operation 7306: update
    tree[6936] = 'value_722819'
    reference[6936] = 'value_722819'
    assert tree.invariants(), "Invariants failed at step 7306"

    # Operation 7307: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7307"

    # Operation 7308: get
    assert tree.invariants(), "Invariants failed at step 7308"

    # Operation 7309: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7309"

    # Operation 7310: delete
    del tree[5549]
    del reference[5549]
    assert tree.invariants(), "Invariants failed at step 7310"

    # Operation 7311: delete
    del tree[3254]
    del reference[3254]
    assert tree.invariants(), "Invariants failed at step 7311"

    # Operation 7312: get
    assert tree.invariants(), "Invariants failed at step 7312"

    # Operation 7313: get
    assert tree.invariants(), "Invariants failed at step 7313"

    # Operation 7314: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7314"

    # Operation 7315: delete
    del tree[1258]
    del reference[1258]
    assert tree.invariants(), "Invariants failed at step 7315"

    # Operation 7316: get
    assert tree.invariants(), "Invariants failed at step 7316"

    # Operation 7317: get
    assert tree.invariants(), "Invariants failed at step 7317"

    # Operation 7318: insert
    tree[6946] = 'value_619033'
    reference[6946] = 'value_619033'
    assert tree.invariants(), "Invariants failed at step 7318"

    # Operation 7319: delete
    del tree[6946]
    del reference[6946]
    assert tree.invariants(), "Invariants failed at step 7319"

    # Operation 7320: delete
    del tree[6936]
    del reference[6936]
    assert tree.invariants(), "Invariants failed at step 7320"

    # Operation 7321: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7321"

    # Operation 7322: delete
    del tree[1060]
    del reference[1060]
    assert tree.invariants(), "Invariants failed at step 7322"

    # Operation 7323: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7323"

    # Operation 7324: insert
    tree[7694] = 'value_756809'
    reference[7694] = 'value_756809'
    assert tree.invariants(), "Invariants failed at step 7324"

    # Operation 7325: update
    tree[7694] = 'value_11229'
    reference[7694] = 'value_11229'
    assert tree.invariants(), "Invariants failed at step 7325"

    # Operation 7326: get
    assert tree.invariants(), "Invariants failed at step 7326"

    # Operation 7327: insert
    tree[1098] = 'value_801984'
    reference[1098] = 'value_801984'
    assert tree.invariants(), "Invariants failed at step 7327"

    # Operation 7328: delete
    del tree[7694]
    del reference[7694]
    assert tree.invariants(), "Invariants failed at step 7328"

    # Operation 7329: delete
    del tree[1098]
    del reference[1098]
    assert tree.invariants(), "Invariants failed at step 7329"

    # Operation 7330: get
    assert tree.invariants(), "Invariants failed at step 7330"

    # Operation 7331: insert
    tree[8520] = 'value_439760'
    reference[8520] = 'value_439760'
    assert tree.invariants(), "Invariants failed at step 7331"

    # Operation 7332: get
    assert tree.invariants(), "Invariants failed at step 7332"

    # Operation 7333: insert
    tree[811] = 'value_639145'
    reference[811] = 'value_639145'
    assert tree.invariants(), "Invariants failed at step 7333"

    # Operation 7334: delete
    del tree[811]
    del reference[811]
    assert tree.invariants(), "Invariants failed at step 7334"

    # Operation 7335: get
    assert tree.invariants(), "Invariants failed at step 7335"

    # Operation 7336: insert
    tree[5057] = 'value_976502'
    reference[5057] = 'value_976502'
    assert tree.invariants(), "Invariants failed at step 7336"

    # Operation 7337: get
    assert tree.invariants(), "Invariants failed at step 7337"

    # Operation 7338: get
    assert tree.invariants(), "Invariants failed at step 7338"

    # Operation 7339: insert
    tree[5334] = 'value_83211'
    reference[5334] = 'value_83211'
    assert tree.invariants(), "Invariants failed at step 7339"

    # Operation 7340: delete
    del tree[5334]
    del reference[5334]
    assert tree.invariants(), "Invariants failed at step 7340"

    # Operation 7341: get
    assert tree.invariants(), "Invariants failed at step 7341"

    # Operation 7342: update
    tree[5057] = 'value_713789'
    reference[5057] = 'value_713789'
    assert tree.invariants(), "Invariants failed at step 7342"

    # Operation 7343: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7343"

    # Operation 7344: get
    assert tree.invariants(), "Invariants failed at step 7344"

    # Operation 7345: update
    tree[5057] = 'value_623069'
    reference[5057] = 'value_623069'
    assert tree.invariants(), "Invariants failed at step 7345"

    # Operation 7346: insert
    tree[9944] = 'value_565337'
    reference[9944] = 'value_565337'
    assert tree.invariants(), "Invariants failed at step 7346"

    # Operation 7347: get
    assert tree.invariants(), "Invariants failed at step 7347"

    # Operation 7348: get
    assert tree.invariants(), "Invariants failed at step 7348"

    # Operation 7349: delete
    del tree[5057]
    del reference[5057]
    assert tree.invariants(), "Invariants failed at step 7349"

    # Operation 7350: insert
    tree[5462] = 'value_781992'
    reference[5462] = 'value_781992'
    assert tree.invariants(), "Invariants failed at step 7350"

    # Operation 7351: insert
    tree[7437] = 'value_543034'
    reference[7437] = 'value_543034'
    assert tree.invariants(), "Invariants failed at step 7351"

    # Operation 7352: delete
    del tree[8520]
    del reference[8520]
    assert tree.invariants(), "Invariants failed at step 7352"

    # Operation 7353: delete
    del tree[5462]
    del reference[5462]
    assert tree.invariants(), "Invariants failed at step 7353"

    # Operation 7354: delete
    del tree[9944]
    del reference[9944]
    assert tree.invariants(), "Invariants failed at step 7354"

    # Operation 7355: get
    assert tree.invariants(), "Invariants failed at step 7355"

    # Operation 7356: insert
    tree[1249] = 'value_54145'
    reference[1249] = 'value_54145'
    assert tree.invariants(), "Invariants failed at step 7356"

    # Operation 7357: delete
    del tree[1249]
    del reference[1249]
    assert tree.invariants(), "Invariants failed at step 7357"

    # Operation 7358: update
    tree[7437] = 'value_157256'
    reference[7437] = 'value_157256'
    assert tree.invariants(), "Invariants failed at step 7358"

    # Operation 7359: delete
    del tree[7437]
    del reference[7437]
    assert tree.invariants(), "Invariants failed at step 7359"

    # Operation 7360: get
    assert tree.invariants(), "Invariants failed at step 7360"

    # Operation 7361: insert
    tree[2580] = 'value_103830'
    reference[2580] = 'value_103830'
    assert tree.invariants(), "Invariants failed at step 7361"

    # Operation 7362: insert
    tree[54] = 'value_747654'
    reference[54] = 'value_747654'
    assert tree.invariants(), "Invariants failed at step 7362"

    # Operation 7363: insert
    tree[1493] = 'value_694338'
    reference[1493] = 'value_694338'
    assert tree.invariants(), "Invariants failed at step 7363"

    # Operation 7364: insert
    tree[5121] = 'value_981531'
    reference[5121] = 'value_981531'
    assert tree.invariants(), "Invariants failed at step 7364"

    # Operation 7365: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7365"

    # Operation 7366: insert
    tree[3033] = 'value_906449'
    reference[3033] = 'value_906449'
    assert tree.invariants(), "Invariants failed at step 7366"

    # Operation 7367: insert
    tree[9549] = 'value_697121'
    reference[9549] = 'value_697121'
    assert tree.invariants(), "Invariants failed at step 7367"

    # Operation 7368: get
    assert tree.invariants(), "Invariants failed at step 7368"

    # Operation 7369: insert
    tree[3366] = 'value_598354'
    reference[3366] = 'value_598354'
    assert tree.invariants(), "Invariants failed at step 7369"

    # Operation 7370: delete
    del tree[3366]
    del reference[3366]
    assert tree.invariants(), "Invariants failed at step 7370"

    # Operation 7371: get
    assert tree.invariants(), "Invariants failed at step 7371"

    # Operation 7372: delete
    del tree[3033]
    del reference[3033]
    assert tree.invariants(), "Invariants failed at step 7372"

    # Operation 7373: delete
    del tree[5121]
    del reference[5121]
    assert tree.invariants(), "Invariants failed at step 7373"

    # Operation 7374: delete
    del tree[2580]
    del reference[2580]
    assert tree.invariants(), "Invariants failed at step 7374"

    # Operation 7375: get
    assert tree.invariants(), "Invariants failed at step 7375"

    # Operation 7376: update
    tree[1493] = 'value_269202'
    reference[1493] = 'value_269202'
    assert tree.invariants(), "Invariants failed at step 7376"

    # Operation 7377: update
    tree[54] = 'value_124936'
    reference[54] = 'value_124936'
    assert tree.invariants(), "Invariants failed at step 7377"

    # Operation 7378: delete
    del tree[54]
    del reference[54]
    assert tree.invariants(), "Invariants failed at step 7378"

    # Operation 7379: update
    tree[1493] = 'value_214844'
    reference[1493] = 'value_214844'
    assert tree.invariants(), "Invariants failed at step 7379"

    # Operation 7380: insert
    tree[2695] = 'value_538689'
    reference[2695] = 'value_538689'
    assert tree.invariants(), "Invariants failed at step 7380"

    # Operation 7381: delete
    del tree[1493]
    del reference[1493]
    assert tree.invariants(), "Invariants failed at step 7381"

    # Operation 7382: insert
    tree[5147] = 'value_552739'
    reference[5147] = 'value_552739'
    assert tree.invariants(), "Invariants failed at step 7382"

    # Operation 7383: insert
    tree[5227] = 'value_94379'
    reference[5227] = 'value_94379'
    assert tree.invariants(), "Invariants failed at step 7383"

    # Operation 7384: insert
    tree[3073] = 'value_334026'
    reference[3073] = 'value_334026'
    assert tree.invariants(), "Invariants failed at step 7384"

    # Operation 7385: batch_delete
    keys_to_delete = [5147, 9549, 3423, 9431]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7385"

    # Operation 7386: update
    tree[5227] = 'value_944695'
    reference[5227] = 'value_944695'
    assert tree.invariants(), "Invariants failed at step 7386"

    # Operation 7387: get
    assert tree.invariants(), "Invariants failed at step 7387"

    # Operation 7388: delete
    del tree[5227]
    del reference[5227]
    assert tree.invariants(), "Invariants failed at step 7388"

    # Operation 7389: get
    assert tree.invariants(), "Invariants failed at step 7389"

    # Operation 7390: get
    assert tree.invariants(), "Invariants failed at step 7390"

    # Operation 7391: insert
    tree[6076] = 'value_817474'
    reference[6076] = 'value_817474'
    assert tree.invariants(), "Invariants failed at step 7391"

    # Operation 7392: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7392"

    # Operation 7393: insert
    tree[949] = 'value_375312'
    reference[949] = 'value_375312'
    assert tree.invariants(), "Invariants failed at step 7393"

    # Operation 7394: insert
    tree[5650] = 'value_44038'
    reference[5650] = 'value_44038'
    assert tree.invariants(), "Invariants failed at step 7394"

    # Operation 7395: insert
    tree[5023] = 'value_721254'
    reference[5023] = 'value_721254'
    assert tree.invariants(), "Invariants failed at step 7395"

    # Operation 7396: insert
    tree[9869] = 'value_782283'
    reference[9869] = 'value_782283'
    assert tree.invariants(), "Invariants failed at step 7396"

    # Operation 7397: delete
    del tree[5023]
    del reference[5023]
    assert tree.invariants(), "Invariants failed at step 7397"

    # Operation 7398: delete
    del tree[6076]
    del reference[6076]
    assert tree.invariants(), "Invariants failed at step 7398"

    # Operation 7399: delete
    del tree[5650]
    del reference[5650]
    assert tree.invariants(), "Invariants failed at step 7399"

    # Operation 7400: delete
    del tree[949]
    del reference[949]
    assert tree.invariants(), "Invariants failed at step 7400"

    # Operation 7401: delete
    del tree[2695]
    del reference[2695]
    assert tree.invariants(), "Invariants failed at step 7401"

    # Operation 7402: delete
    del tree[9869]
    del reference[9869]
    assert tree.invariants(), "Invariants failed at step 7402"

    # Operation 7403: delete
    del tree[3073]
    del reference[3073]
    assert tree.invariants(), "Invariants failed at step 7403"

    # Operation 7404: get
    assert tree.invariants(), "Invariants failed at step 7404"

    # Operation 7405: insert
    tree[6612] = 'value_398804'
    reference[6612] = 'value_398804'
    assert tree.invariants(), "Invariants failed at step 7405"

    # Operation 7406: insert
    tree[5595] = 'value_588986'
    reference[5595] = 'value_588986'
    assert tree.invariants(), "Invariants failed at step 7406"

    # Operation 7407: delete
    del tree[5595]
    del reference[5595]
    assert tree.invariants(), "Invariants failed at step 7407"

    # Operation 7408: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7408"

    # Operation 7409: delete
    del tree[6612]
    del reference[6612]
    assert tree.invariants(), "Invariants failed at step 7409"

    # Operation 7410: get
    assert tree.invariants(), "Invariants failed at step 7410"

    # Operation 7411: insert
    tree[2738] = 'value_336452'
    reference[2738] = 'value_336452'
    assert tree.invariants(), "Invariants failed at step 7411"

    # Operation 7412: insert
    tree[4965] = 'value_356794'
    reference[4965] = 'value_356794'
    assert tree.invariants(), "Invariants failed at step 7412"

    # Operation 7413: insert
    tree[3110] = 'value_735926'
    reference[3110] = 'value_735926'
    assert tree.invariants(), "Invariants failed at step 7413"

    # Operation 7414: insert
    tree[9615] = 'value_635400'
    reference[9615] = 'value_635400'
    assert tree.invariants(), "Invariants failed at step 7414"

    # Operation 7415: get
    assert tree.invariants(), "Invariants failed at step 7415"

    # Operation 7416: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7416"

    # Operation 7417: insert
    tree[9283] = 'value_363383'
    reference[9283] = 'value_363383'
    assert tree.invariants(), "Invariants failed at step 7417"

    # Operation 7418: get
    assert tree.invariants(), "Invariants failed at step 7418"

    # Operation 7419: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7419"

    # Operation 7420: delete
    del tree[9283]
    del reference[9283]
    assert tree.invariants(), "Invariants failed at step 7420"

    # Operation 7421: insert
    tree[9427] = 'value_515354'
    reference[9427] = 'value_515354'
    assert tree.invariants(), "Invariants failed at step 7421"

    # Operation 7422: insert
    tree[5075] = 'value_628740'
    reference[5075] = 'value_628740'
    assert tree.invariants(), "Invariants failed at step 7422"

    # Operation 7423: insert
    tree[6355] = 'value_291885'
    reference[6355] = 'value_291885'
    assert tree.invariants(), "Invariants failed at step 7423"

    # Operation 7424: delete
    del tree[3110]
    del reference[3110]
    assert tree.invariants(), "Invariants failed at step 7424"

    # Operation 7425: insert
    tree[5873] = 'value_715093'
    reference[5873] = 'value_715093'
    assert tree.invariants(), "Invariants failed at step 7425"

    # Operation 7426: insert
    tree[2050] = 'value_116382'
    reference[2050] = 'value_116382'
    assert tree.invariants(), "Invariants failed at step 7426"

    # Operation 7427: insert
    tree[6519] = 'value_889869'
    reference[6519] = 'value_889869'
    assert tree.invariants(), "Invariants failed at step 7427"

    # Operation 7428: get
    assert tree.invariants(), "Invariants failed at step 7428"

    # Operation 7429: batch_delete
    keys_to_delete = [4965, 5873, 9815, 1467, 6519]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7429"

    # Operation 7430: insert
    tree[6889] = 'value_198363'
    reference[6889] = 'value_198363'
    assert tree.invariants(), "Invariants failed at step 7430"

    # Operation 7431: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7431"

    # Operation 7432: update
    tree[2050] = 'value_501206'
    reference[2050] = 'value_501206'
    assert tree.invariants(), "Invariants failed at step 7432"

    # Operation 7433: insert
    tree[8125] = 'value_176392'
    reference[8125] = 'value_176392'
    assert tree.invariants(), "Invariants failed at step 7433"

    # Operation 7434: delete
    del tree[9427]
    del reference[9427]
    assert tree.invariants(), "Invariants failed at step 7434"

    # Operation 7435: update
    tree[6355] = 'value_11368'
    reference[6355] = 'value_11368'
    assert tree.invariants(), "Invariants failed at step 7435"

    # Operation 7436: delete
    del tree[2050]
    del reference[2050]
    assert tree.invariants(), "Invariants failed at step 7436"

    # Operation 7437: insert
    tree[7174] = 'value_394654'
    reference[7174] = 'value_394654'
    assert tree.invariants(), "Invariants failed at step 7437"

    # Operation 7438: insert
    tree[8774] = 'value_538302'
    reference[8774] = 'value_538302'
    assert tree.invariants(), "Invariants failed at step 7438"

    # Operation 7439: update
    tree[7174] = 'value_987138'
    reference[7174] = 'value_987138'
    assert tree.invariants(), "Invariants failed at step 7439"

    # Operation 7440: delete
    del tree[2738]
    del reference[2738]
    assert tree.invariants(), "Invariants failed at step 7440"

    # Operation 7441: update
    tree[7174] = 'value_214788'
    reference[7174] = 'value_214788'
    assert tree.invariants(), "Invariants failed at step 7441"

    # Operation 7442: get
    assert tree.invariants(), "Invariants failed at step 7442"

    # Operation 7443: delete
    del tree[5075]
    del reference[5075]
    assert tree.invariants(), "Invariants failed at step 7443"

    # Operation 7444: get
    assert tree.invariants(), "Invariants failed at step 7444"

    # Operation 7445: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7445"

    # Operation 7446: delete
    del tree[9615]
    del reference[9615]
    assert tree.invariants(), "Invariants failed at step 7446"

    # Operation 7447: delete
    del tree[7174]
    del reference[7174]
    assert tree.invariants(), "Invariants failed at step 7447"

    # Operation 7448: insert
    tree[7709] = 'value_65389'
    reference[7709] = 'value_65389'
    assert tree.invariants(), "Invariants failed at step 7448"

    # Operation 7449: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7449"

    # Operation 7450: insert
    tree[2122] = 'value_232935'
    reference[2122] = 'value_232935'
    assert tree.invariants(), "Invariants failed at step 7450"

    # Operation 7451: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7451"

    # Operation 7452: batch_delete
    keys_to_delete = [4073, 2122, 6355, 2907, 8125]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7452"

    # Operation 7453: insert
    tree[7476] = 'value_304419'
    reference[7476] = 'value_304419'
    assert tree.invariants(), "Invariants failed at step 7453"

    # Operation 7454: delete
    del tree[8774]
    del reference[8774]
    assert tree.invariants(), "Invariants failed at step 7454"

    # Operation 7455: insert
    tree[5722] = 'value_345808'
    reference[5722] = 'value_345808'
    assert tree.invariants(), "Invariants failed at step 7455"

    # Operation 7456: insert
    tree[1864] = 'value_373461'
    reference[1864] = 'value_373461'
    assert tree.invariants(), "Invariants failed at step 7456"

    # Operation 7457: delete
    del tree[1864]
    del reference[1864]
    assert tree.invariants(), "Invariants failed at step 7457"

    # Operation 7458: update
    tree[5722] = 'value_713359'
    reference[5722] = 'value_713359'
    assert tree.invariants(), "Invariants failed at step 7458"

    # Operation 7459: update
    tree[7709] = 'value_945991'
    reference[7709] = 'value_945991'
    assert tree.invariants(), "Invariants failed at step 7459"

    # Operation 7460: delete
    del tree[7476]
    del reference[7476]
    assert tree.invariants(), "Invariants failed at step 7460"

    # Operation 7461: delete
    del tree[6889]
    del reference[6889]
    assert tree.invariants(), "Invariants failed at step 7461"

    # Operation 7462: get
    assert tree.invariants(), "Invariants failed at step 7462"

    # Operation 7463: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7463"

    # Operation 7464: delete
    del tree[5722]
    del reference[5722]
    assert tree.invariants(), "Invariants failed at step 7464"

    # Operation 7465: insert
    tree[7335] = 'value_310619'
    reference[7335] = 'value_310619'
    assert tree.invariants(), "Invariants failed at step 7465"

    # Operation 7466: get
    assert tree.invariants(), "Invariants failed at step 7466"

    # Operation 7467: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7467"

    # Operation 7468: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7468"

    # Operation 7469: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7469"

    # Operation 7470: insert
    tree[2608] = 'value_655105'
    reference[2608] = 'value_655105'
    assert tree.invariants(), "Invariants failed at step 7470"

    # Operation 7471: get
    assert tree.invariants(), "Invariants failed at step 7471"

    # Operation 7472: insert
    tree[6566] = 'value_460380'
    reference[6566] = 'value_460380'
    assert tree.invariants(), "Invariants failed at step 7472"

    # Operation 7473: insert
    tree[8196] = 'value_211282'
    reference[8196] = 'value_211282'
    assert tree.invariants(), "Invariants failed at step 7473"

    # Operation 7474: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7474"

    # Operation 7475: delete
    del tree[7709]
    del reference[7709]
    assert tree.invariants(), "Invariants failed at step 7475"

    # Operation 7476: get
    assert tree.invariants(), "Invariants failed at step 7476"

    # Operation 7477: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7477"

    # Operation 7478: insert
    tree[4372] = 'value_573231'
    reference[4372] = 'value_573231'
    assert tree.invariants(), "Invariants failed at step 7478"

    # Operation 7479: get
    assert tree.invariants(), "Invariants failed at step 7479"

    # Operation 7480: delete
    del tree[8196]
    del reference[8196]
    assert tree.invariants(), "Invariants failed at step 7480"

    # Operation 7481: update
    tree[7335] = 'value_423022'
    reference[7335] = 'value_423022'
    assert tree.invariants(), "Invariants failed at step 7481"

    # Operation 7482: update
    tree[6566] = 'value_612585'
    reference[6566] = 'value_612585'
    assert tree.invariants(), "Invariants failed at step 7482"

    # Operation 7483: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7483"

    # Operation 7484: delete
    del tree[2608]
    del reference[2608]
    assert tree.invariants(), "Invariants failed at step 7484"

    # Operation 7485: delete
    del tree[6566]
    del reference[6566]
    assert tree.invariants(), "Invariants failed at step 7485"

    # Operation 7486: update
    tree[4372] = 'value_503057'
    reference[4372] = 'value_503057'
    assert tree.invariants(), "Invariants failed at step 7486"

    # Operation 7487: insert
    tree[9010] = 'value_802063'
    reference[9010] = 'value_802063'
    assert tree.invariants(), "Invariants failed at step 7487"

    # Operation 7488: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7488"

    # Operation 7489: delete
    del tree[9010]
    del reference[9010]
    assert tree.invariants(), "Invariants failed at step 7489"

    # Operation 7490: insert
    tree[7324] = 'value_688399'
    reference[7324] = 'value_688399'
    assert tree.invariants(), "Invariants failed at step 7490"

    # Operation 7491: insert
    tree[5234] = 'value_147489'
    reference[5234] = 'value_147489'
    assert tree.invariants(), "Invariants failed at step 7491"

    # Operation 7492: get
    assert tree.invariants(), "Invariants failed at step 7492"

    # Operation 7493: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7493"

    # Operation 7494: update
    tree[5234] = 'value_590677'
    reference[5234] = 'value_590677'
    assert tree.invariants(), "Invariants failed at step 7494"

    # Operation 7495: insert
    tree[5218] = 'value_347455'
    reference[5218] = 'value_347455'
    assert tree.invariants(), "Invariants failed at step 7495"

    # Operation 7496: insert
    tree[5359] = 'value_121993'
    reference[5359] = 'value_121993'
    assert tree.invariants(), "Invariants failed at step 7496"

    # Operation 7497: insert
    tree[7575] = 'value_713414'
    reference[7575] = 'value_713414'
    assert tree.invariants(), "Invariants failed at step 7497"

    # Operation 7498: update
    tree[5234] = 'value_762142'
    reference[5234] = 'value_762142'
    assert tree.invariants(), "Invariants failed at step 7498"

    # Operation 7499: insert
    tree[6735] = 'value_625683'
    reference[6735] = 'value_625683'
    assert tree.invariants(), "Invariants failed at step 7499"

    # Operation 7500: insert
    tree[6208] = 'value_653359'
    reference[6208] = 'value_653359'
    assert tree.invariants(), "Invariants failed at step 7500"

    # Operation 7501: insert
    tree[7309] = 'value_38078'
    reference[7309] = 'value_38078'
    assert tree.invariants(), "Invariants failed at step 7501"

    # Operation 7502: batch_delete
    keys_to_delete = [5234, 2540, 5359]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7502"

    # Operation 7503: batch_delete
    keys_to_delete = [5218, 7335, 9419, 7575, 1436, 7324]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7503"

    # Operation 7504: update
    tree[4372] = 'value_237083'
    reference[4372] = 'value_237083'
    assert tree.invariants(), "Invariants failed at step 7504"

    # Operation 7505: insert
    tree[7825] = 'value_818659'
    reference[7825] = 'value_818659'
    assert tree.invariants(), "Invariants failed at step 7505"

    # Operation 7506: delete
    del tree[7309]
    del reference[7309]
    assert tree.invariants(), "Invariants failed at step 7506"

    # Operation 7507: update
    tree[4372] = 'value_38188'
    reference[4372] = 'value_38188'
    assert tree.invariants(), "Invariants failed at step 7507"

    # Operation 7508: get
    assert tree.invariants(), "Invariants failed at step 7508"

    # Operation 7509: update
    tree[4372] = 'value_984635'
    reference[4372] = 'value_984635'
    assert tree.invariants(), "Invariants failed at step 7509"

    # Operation 7510: delete
    del tree[4372]
    del reference[4372]
    assert tree.invariants(), "Invariants failed at step 7510"

    # Operation 7511: update
    tree[6735] = 'value_329034'
    reference[6735] = 'value_329034'
    assert tree.invariants(), "Invariants failed at step 7511"

    # Operation 7512: delete
    del tree[6208]
    del reference[6208]
    assert tree.invariants(), "Invariants failed at step 7512"

    # Operation 7513: update
    tree[7825] = 'value_434082'
    reference[7825] = 'value_434082'
    assert tree.invariants(), "Invariants failed at step 7513"

    # Operation 7514: update
    tree[7825] = 'value_877963'
    reference[7825] = 'value_877963'
    assert tree.invariants(), "Invariants failed at step 7514"

    # Operation 7515: update
    tree[6735] = 'value_682971'
    reference[6735] = 'value_682971'
    assert tree.invariants(), "Invariants failed at step 7515"

    # Operation 7516: update
    tree[7825] = 'value_302571'
    reference[7825] = 'value_302571'
    assert tree.invariants(), "Invariants failed at step 7516"

    # Operation 7517: insert
    tree[9583] = 'value_96451'
    reference[9583] = 'value_96451'
    assert tree.invariants(), "Invariants failed at step 7517"

    # Operation 7518: delete
    del tree[7825]
    del reference[7825]
    assert tree.invariants(), "Invariants failed at step 7518"

    # Operation 7519: get
    assert tree.invariants(), "Invariants failed at step 7519"

    # Operation 7520: delete
    del tree[9583]
    del reference[9583]
    assert tree.invariants(), "Invariants failed at step 7520"

    # Operation 7521: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7521"

    # Operation 7522: delete
    del tree[6735]
    del reference[6735]
    assert tree.invariants(), "Invariants failed at step 7522"

    # Operation 7523: insert
    tree[4502] = 'value_442976'
    reference[4502] = 'value_442976'
    assert tree.invariants(), "Invariants failed at step 7523"

    # Operation 7524: delete
    del tree[4502]
    del reference[4502]
    assert tree.invariants(), "Invariants failed at step 7524"

    # Operation 7525: insert
    tree[7044] = 'value_662863'
    reference[7044] = 'value_662863'
    assert tree.invariants(), "Invariants failed at step 7525"

    # Operation 7526: insert
    tree[5178] = 'value_934619'
    reference[5178] = 'value_934619'
    assert tree.invariants(), "Invariants failed at step 7526"

    # Operation 7527: delete
    del tree[5178]
    del reference[5178]
    assert tree.invariants(), "Invariants failed at step 7527"

    # Operation 7528: delete
    del tree[7044]
    del reference[7044]
    assert tree.invariants(), "Invariants failed at step 7528"

    # Operation 7529: insert
    tree[1460] = 'value_387626'
    reference[1460] = 'value_387626'
    assert tree.invariants(), "Invariants failed at step 7529"

    # Operation 7530: update
    tree[1460] = 'value_513577'
    reference[1460] = 'value_513577'
    assert tree.invariants(), "Invariants failed at step 7530"

    # Operation 7531: update
    tree[1460] = 'value_723338'
    reference[1460] = 'value_723338'
    assert tree.invariants(), "Invariants failed at step 7531"

    # Operation 7532: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7532"

    # Operation 7533: insert
    tree[9946] = 'value_768082'
    reference[9946] = 'value_768082'
    assert tree.invariants(), "Invariants failed at step 7533"

    # Operation 7534: insert
    tree[2256] = 'value_464193'
    reference[2256] = 'value_464193'
    assert tree.invariants(), "Invariants failed at step 7534"

    # Operation 7535: update
    tree[1460] = 'value_142713'
    reference[1460] = 'value_142713'
    assert tree.invariants(), "Invariants failed at step 7535"

    # Operation 7536: insert
    tree[3073] = 'value_606745'
    reference[3073] = 'value_606745'
    assert tree.invariants(), "Invariants failed at step 7536"

    # Operation 7537: delete
    del tree[9946]
    del reference[9946]
    assert tree.invariants(), "Invariants failed at step 7537"

    # Operation 7538: insert
    tree[9337] = 'value_742018'
    reference[9337] = 'value_742018'
    assert tree.invariants(), "Invariants failed at step 7538"

    # Operation 7539: insert
    tree[2107] = 'value_761792'
    reference[2107] = 'value_761792'
    assert tree.invariants(), "Invariants failed at step 7539"

    # Operation 7540: insert
    tree[4013] = 'value_882390'
    reference[4013] = 'value_882390'
    assert tree.invariants(), "Invariants failed at step 7540"

    # Operation 7541: delete
    del tree[2256]
    del reference[2256]
    assert tree.invariants(), "Invariants failed at step 7541"

    # Operation 7542: insert
    tree[6621] = 'value_109287'
    reference[6621] = 'value_109287'
    assert tree.invariants(), "Invariants failed at step 7542"

    # Operation 7543: batch_delete
    keys_to_delete = [3073, 6817, 4658, 1460, 9337]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7543"

    # Operation 7544: update
    tree[4013] = 'value_649601'
    reference[4013] = 'value_649601'
    assert tree.invariants(), "Invariants failed at step 7544"

    # Operation 7545: update
    tree[4013] = 'value_652528'
    reference[4013] = 'value_652528'
    assert tree.invariants(), "Invariants failed at step 7545"

    # Operation 7546: get
    assert tree.invariants(), "Invariants failed at step 7546"

    # Operation 7547: update
    tree[2107] = 'value_738125'
    reference[2107] = 'value_738125'
    assert tree.invariants(), "Invariants failed at step 7547"

    # Operation 7548: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7548"

    # Operation 7549: insert
    tree[1258] = 'value_672576'
    reference[1258] = 'value_672576'
    assert tree.invariants(), "Invariants failed at step 7549"

    # Operation 7550: get
    assert tree.invariants(), "Invariants failed at step 7550"

    # Operation 7551: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7551"

    # Operation 7552: insert
    tree[1270] = 'value_121927'
    reference[1270] = 'value_121927'
    assert tree.invariants(), "Invariants failed at step 7552"

    # Operation 7553: insert
    tree[9982] = 'value_949701'
    reference[9982] = 'value_949701'
    assert tree.invariants(), "Invariants failed at step 7553"

    # Operation 7554: insert
    tree[2785] = 'value_2175'
    reference[2785] = 'value_2175'
    assert tree.invariants(), "Invariants failed at step 7554"

    # Operation 7555: batch_delete
    keys_to_delete = [2785, 1258, 8090, 4444, 9982]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7555"

    # Operation 7556: delete
    del tree[6621]
    del reference[6621]
    assert tree.invariants(), "Invariants failed at step 7556"

    # Operation 7557: delete
    del tree[2107]
    del reference[2107]
    assert tree.invariants(), "Invariants failed at step 7557"

    # Operation 7558: delete
    del tree[1270]
    del reference[1270]
    assert tree.invariants(), "Invariants failed at step 7558"

    # Operation 7559: get
    assert tree.invariants(), "Invariants failed at step 7559"

    # Operation 7560: update
    tree[4013] = 'value_843578'
    reference[4013] = 'value_843578'
    assert tree.invariants(), "Invariants failed at step 7560"

    # Operation 7561: get
    assert tree.invariants(), "Invariants failed at step 7561"

    # Operation 7562: delete
    del tree[4013]
    del reference[4013]
    assert tree.invariants(), "Invariants failed at step 7562"

    # Operation 7563: get
    assert tree.invariants(), "Invariants failed at step 7563"

    # Operation 7564: get
    assert tree.invariants(), "Invariants failed at step 7564"

    # Operation 7565: insert
    tree[15] = 'value_80549'
    reference[15] = 'value_80549'
    assert tree.invariants(), "Invariants failed at step 7565"

    # Operation 7566: delete
    del tree[15]
    del reference[15]
    assert tree.invariants(), "Invariants failed at step 7566"

    # Operation 7567: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7567"

    # Operation 7568: get
    assert tree.invariants(), "Invariants failed at step 7568"

    # Operation 7569: insert
    tree[6872] = 'value_113328'
    reference[6872] = 'value_113328'
    assert tree.invariants(), "Invariants failed at step 7569"

    # Operation 7570: update
    tree[6872] = 'value_984159'
    reference[6872] = 'value_984159'
    assert tree.invariants(), "Invariants failed at step 7570"

    # Operation 7571: update
    tree[6872] = 'value_398535'
    reference[6872] = 'value_398535'
    assert tree.invariants(), "Invariants failed at step 7571"

    # Operation 7572: insert
    tree[6988] = 'value_165164'
    reference[6988] = 'value_165164'
    assert tree.invariants(), "Invariants failed at step 7572"

    # Operation 7573: delete
    del tree[6988]
    del reference[6988]
    assert tree.invariants(), "Invariants failed at step 7573"

    # Operation 7574: update
    tree[6872] = 'value_1510'
    reference[6872] = 'value_1510'
    assert tree.invariants(), "Invariants failed at step 7574"

    # Operation 7575: delete
    del tree[6872]
    del reference[6872]
    assert tree.invariants(), "Invariants failed at step 7575"

    # Operation 7576: get
    assert tree.invariants(), "Invariants failed at step 7576"

    # Operation 7577: insert
    tree[898] = 'value_429227'
    reference[898] = 'value_429227'
    assert tree.invariants(), "Invariants failed at step 7577"

    # Operation 7578: get
    assert tree.invariants(), "Invariants failed at step 7578"

    # Operation 7579: delete
    del tree[898]
    del reference[898]
    assert tree.invariants(), "Invariants failed at step 7579"

    # Operation 7580: insert
    tree[7403] = 'value_191989'
    reference[7403] = 'value_191989'
    assert tree.invariants(), "Invariants failed at step 7580"

    # Operation 7581: get
    assert tree.invariants(), "Invariants failed at step 7581"

    # Operation 7582: insert
    tree[6382] = 'value_394225'
    reference[6382] = 'value_394225'
    assert tree.invariants(), "Invariants failed at step 7582"

    # Operation 7583: delete
    del tree[6382]
    del reference[6382]
    assert tree.invariants(), "Invariants failed at step 7583"

    # Operation 7584: delete
    del tree[7403]
    del reference[7403]
    assert tree.invariants(), "Invariants failed at step 7584"

    # Operation 7585: insert
    tree[7609] = 'value_902484'
    reference[7609] = 'value_902484'
    assert tree.invariants(), "Invariants failed at step 7585"

    # Operation 7586: update
    tree[7609] = 'value_166717'
    reference[7609] = 'value_166717'
    assert tree.invariants(), "Invariants failed at step 7586"

    # Operation 7587: delete
    del tree[7609]
    del reference[7609]
    assert tree.invariants(), "Invariants failed at step 7587"

    # Operation 7588: insert
    tree[7212] = 'value_303645'
    reference[7212] = 'value_303645'
    assert tree.invariants(), "Invariants failed at step 7588"

    # Operation 7589: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7589"

    # Operation 7590: update
    tree[7212] = 'value_667940'
    reference[7212] = 'value_667940'
    assert tree.invariants(), "Invariants failed at step 7590"

    # Operation 7591: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7591"

    # Operation 7592: insert
    tree[1543] = 'value_887314'
    reference[1543] = 'value_887314'
    assert tree.invariants(), "Invariants failed at step 7592"

    # Operation 7593: delete
    del tree[7212]
    del reference[7212]
    assert tree.invariants(), "Invariants failed at step 7593"

    # Operation 7594: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7594"

    # Operation 7595: delete
    del tree[1543]
    del reference[1543]
    assert tree.invariants(), "Invariants failed at step 7595"

    # Operation 7596: insert
    tree[6055] = 'value_525051'
    reference[6055] = 'value_525051'
    assert tree.invariants(), "Invariants failed at step 7596"

    # Operation 7597: delete
    del tree[6055]
    del reference[6055]
    assert tree.invariants(), "Invariants failed at step 7597"

    # Operation 7598: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7598"

    # Operation 7599: insert
    tree[630] = 'value_559353'
    reference[630] = 'value_559353'
    assert tree.invariants(), "Invariants failed at step 7599"

    # Operation 7600: delete
    del tree[630]
    del reference[630]
    assert tree.invariants(), "Invariants failed at step 7600"

    # Operation 7601: insert
    tree[5666] = 'value_824496'
    reference[5666] = 'value_824496'
    assert tree.invariants(), "Invariants failed at step 7601"

    # Operation 7602: delete
    del tree[5666]
    del reference[5666]
    assert tree.invariants(), "Invariants failed at step 7602"

    # Operation 7603: insert
    tree[838] = 'value_233961'
    reference[838] = 'value_233961'
    assert tree.invariants(), "Invariants failed at step 7603"

    # Operation 7604: delete
    del tree[838]
    del reference[838]
    assert tree.invariants(), "Invariants failed at step 7604"

    # Operation 7605: insert
    tree[6528] = 'value_578155'
    reference[6528] = 'value_578155'
    assert tree.invariants(), "Invariants failed at step 7605"

    # Operation 7606: insert
    tree[5403] = 'value_876627'
    reference[5403] = 'value_876627'
    assert tree.invariants(), "Invariants failed at step 7606"

    # Operation 7607: delete
    del tree[5403]
    del reference[5403]
    assert tree.invariants(), "Invariants failed at step 7607"

    # Operation 7608: delete
    del tree[6528]
    del reference[6528]
    assert tree.invariants(), "Invariants failed at step 7608"

    # Operation 7609: get
    assert tree.invariants(), "Invariants failed at step 7609"

    # Operation 7610: insert
    tree[2967] = 'value_578203'
    reference[2967] = 'value_578203'
    assert tree.invariants(), "Invariants failed at step 7610"

    # Operation 7611: update
    tree[2967] = 'value_525613'
    reference[2967] = 'value_525613'
    assert tree.invariants(), "Invariants failed at step 7611"

    # Operation 7612: insert
    tree[3012] = 'value_28961'
    reference[3012] = 'value_28961'
    assert tree.invariants(), "Invariants failed at step 7612"

    # Operation 7613: delete
    del tree[2967]
    del reference[2967]
    assert tree.invariants(), "Invariants failed at step 7613"

    # Operation 7614: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7614"

    # Operation 7615: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7615"

    # Operation 7616: insert
    tree[1946] = 'value_467998'
    reference[1946] = 'value_467998'
    assert tree.invariants(), "Invariants failed at step 7616"

    # Operation 7617: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7617"

    # Operation 7618: get
    assert tree.invariants(), "Invariants failed at step 7618"

    # Operation 7619: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7619"

    # Operation 7620: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7620"

    # Operation 7621: get
    assert tree.invariants(), "Invariants failed at step 7621"

    # Operation 7622: update
    tree[1946] = 'value_27814'
    reference[1946] = 'value_27814'
    assert tree.invariants(), "Invariants failed at step 7622"

    # Operation 7623: delete
    del tree[1946]
    del reference[1946]
    assert tree.invariants(), "Invariants failed at step 7623"

    # Operation 7624: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7624"

    # Operation 7625: get
    assert tree.invariants(), "Invariants failed at step 7625"

    # Operation 7626: update
    tree[3012] = 'value_920711'
    reference[3012] = 'value_920711'
    assert tree.invariants(), "Invariants failed at step 7626"

    # Operation 7627: insert
    tree[9931] = 'value_902532'
    reference[9931] = 'value_902532'
    assert tree.invariants(), "Invariants failed at step 7627"

    # Operation 7628: insert
    tree[7579] = 'value_979638'
    reference[7579] = 'value_979638'
    assert tree.invariants(), "Invariants failed at step 7628"

    # Operation 7629: delete
    del tree[7579]
    del reference[7579]
    assert tree.invariants(), "Invariants failed at step 7629"

    # Operation 7630: delete
    del tree[9931]
    del reference[9931]
    assert tree.invariants(), "Invariants failed at step 7630"

    # Operation 7631: insert
    tree[9812] = 'value_532148'
    reference[9812] = 'value_532148'
    assert tree.invariants(), "Invariants failed at step 7631"

    # Operation 7632: get
    assert tree.invariants(), "Invariants failed at step 7632"

    # Operation 7633: update
    tree[9812] = 'value_25129'
    reference[9812] = 'value_25129'
    assert tree.invariants(), "Invariants failed at step 7633"

    # Operation 7634: delete
    del tree[3012]
    del reference[3012]
    assert tree.invariants(), "Invariants failed at step 7634"

    # Operation 7635: insert
    tree[1823] = 'value_408539'
    reference[1823] = 'value_408539'
    assert tree.invariants(), "Invariants failed at step 7635"

    # Operation 7636: update
    tree[9812] = 'value_720920'
    reference[9812] = 'value_720920'
    assert tree.invariants(), "Invariants failed at step 7636"

    # Operation 7637: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7637"

    # Operation 7638: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7638"

    # Operation 7639: insert
    tree[7276] = 'value_756858'
    reference[7276] = 'value_756858'
    assert tree.invariants(), "Invariants failed at step 7639"

    # Operation 7640: get
    assert tree.invariants(), "Invariants failed at step 7640"

    # Operation 7641: insert
    tree[6052] = 'value_969046'
    reference[6052] = 'value_969046'
    assert tree.invariants(), "Invariants failed at step 7641"

    # Operation 7642: delete
    del tree[6052]
    del reference[6052]
    assert tree.invariants(), "Invariants failed at step 7642"

    # Operation 7643: get
    assert tree.invariants(), "Invariants failed at step 7643"

    # Operation 7644: delete
    del tree[1823]
    del reference[1823]
    assert tree.invariants(), "Invariants failed at step 7644"

    # Operation 7645: insert
    tree[7766] = 'value_564832'
    reference[7766] = 'value_564832'
    assert tree.invariants(), "Invariants failed at step 7645"

    # Operation 7646: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7646"

    # Operation 7647: update
    tree[7276] = 'value_195511'
    reference[7276] = 'value_195511'
    assert tree.invariants(), "Invariants failed at step 7647"

    # Operation 7648: delete
    del tree[9812]
    del reference[9812]
    assert tree.invariants(), "Invariants failed at step 7648"

    # Operation 7649: update
    tree[7766] = 'value_286746'
    reference[7766] = 'value_286746'
    assert tree.invariants(), "Invariants failed at step 7649"

    # Operation 7650: insert
    tree[1062] = 'value_381676'
    reference[1062] = 'value_381676'
    assert tree.invariants(), "Invariants failed at step 7650"

    # Operation 7651: insert
    tree[6041] = 'value_575841'
    reference[6041] = 'value_575841'
    assert tree.invariants(), "Invariants failed at step 7651"

    # Operation 7652: delete
    del tree[1062]
    del reference[1062]
    assert tree.invariants(), "Invariants failed at step 7652"

    # Operation 7653: get
    assert tree.invariants(), "Invariants failed at step 7653"

    # Operation 7654: delete
    del tree[7276]
    del reference[7276]
    assert tree.invariants(), "Invariants failed at step 7654"

    # Operation 7655: update
    tree[7766] = 'value_891535'
    reference[7766] = 'value_891535'
    assert tree.invariants(), "Invariants failed at step 7655"

    # Operation 7656: get
    assert tree.invariants(), "Invariants failed at step 7656"

    # Operation 7657: insert
    tree[3514] = 'value_537678'
    reference[3514] = 'value_537678'
    assert tree.invariants(), "Invariants failed at step 7657"

    # Operation 7658: insert
    tree[8673] = 'value_693034'
    reference[8673] = 'value_693034'
    assert tree.invariants(), "Invariants failed at step 7658"

    # Operation 7659: delete
    del tree[3514]
    del reference[3514]
    assert tree.invariants(), "Invariants failed at step 7659"

    # Operation 7660: insert
    tree[2938] = 'value_562413'
    reference[2938] = 'value_562413'
    assert tree.invariants(), "Invariants failed at step 7660"

    # Operation 7661: update
    tree[2938] = 'value_968229'
    reference[2938] = 'value_968229'
    assert tree.invariants(), "Invariants failed at step 7661"

    # Operation 7662: get
    assert tree.invariants(), "Invariants failed at step 7662"

    # Operation 7663: update
    tree[8673] = 'value_727889'
    reference[8673] = 'value_727889'
    assert tree.invariants(), "Invariants failed at step 7663"

    # Operation 7664: insert
    tree[7205] = 'value_633754'
    reference[7205] = 'value_633754'
    assert tree.invariants(), "Invariants failed at step 7664"

    # Operation 7665: delete
    del tree[2938]
    del reference[2938]
    assert tree.invariants(), "Invariants failed at step 7665"

    # Operation 7666: get
    assert tree.invariants(), "Invariants failed at step 7666"

    # Operation 7667: delete
    del tree[6041]
    del reference[6041]
    assert tree.invariants(), "Invariants failed at step 7667"

    # Operation 7668: delete
    del tree[8673]
    del reference[8673]
    assert tree.invariants(), "Invariants failed at step 7668"

    # Operation 7669: insert
    tree[699] = 'value_25177'
    reference[699] = 'value_25177'
    assert tree.invariants(), "Invariants failed at step 7669"

    # Operation 7670: delete
    del tree[699]
    del reference[699]
    assert tree.invariants(), "Invariants failed at step 7670"

    # Operation 7671: insert
    tree[5114] = 'value_595340'
    reference[5114] = 'value_595340'
    assert tree.invariants(), "Invariants failed at step 7671"

    # Operation 7672: insert
    tree[9493] = 'value_52919'
    reference[9493] = 'value_52919'
    assert tree.invariants(), "Invariants failed at step 7672"

    # Operation 7673: get
    assert tree.invariants(), "Invariants failed at step 7673"

    # Operation 7674: update
    tree[5114] = 'value_605431'
    reference[5114] = 'value_605431'
    assert tree.invariants(), "Invariants failed at step 7674"

    # Operation 7675: update
    tree[7766] = 'value_594052'
    reference[7766] = 'value_594052'
    assert tree.invariants(), "Invariants failed at step 7675"

    # Operation 7676: delete
    del tree[5114]
    del reference[5114]
    assert tree.invariants(), "Invariants failed at step 7676"

    # Operation 7677: get
    assert tree.invariants(), "Invariants failed at step 7677"

    # Operation 7678: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7678"

    # Operation 7679: insert
    tree[931] = 'value_693120'
    reference[931] = 'value_693120'
    assert tree.invariants(), "Invariants failed at step 7679"

    # Operation 7680: insert
    tree[8616] = 'value_943464'
    reference[8616] = 'value_943464'
    assert tree.invariants(), "Invariants failed at step 7680"

    # Operation 7681: insert
    tree[3508] = 'value_385967'
    reference[3508] = 'value_385967'
    assert tree.invariants(), "Invariants failed at step 7681"

    # Operation 7682: insert
    tree[6768] = 'value_55774'
    reference[6768] = 'value_55774'
    assert tree.invariants(), "Invariants failed at step 7682"

    # Operation 7683: insert
    tree[4092] = 'value_781692'
    reference[4092] = 'value_781692'
    assert tree.invariants(), "Invariants failed at step 7683"

    # Operation 7684: get
    assert tree.invariants(), "Invariants failed at step 7684"

    # Operation 7685: get
    assert tree.invariants(), "Invariants failed at step 7685"

    # Operation 7686: delete
    del tree[931]
    del reference[931]
    assert tree.invariants(), "Invariants failed at step 7686"

    # Operation 7687: insert
    tree[2179] = 'value_840513'
    reference[2179] = 'value_840513'
    assert tree.invariants(), "Invariants failed at step 7687"

    # Operation 7688: update
    tree[6768] = 'value_53013'
    reference[6768] = 'value_53013'
    assert tree.invariants(), "Invariants failed at step 7688"

    # Operation 7689: delete
    del tree[2179]
    del reference[2179]
    assert tree.invariants(), "Invariants failed at step 7689"

    # Operation 7690: delete
    del tree[9493]
    del reference[9493]
    assert tree.invariants(), "Invariants failed at step 7690"

    # Operation 7691: batch_delete
    keys_to_delete = [6768, 3508, 4092, 7766]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7691"

    # Operation 7692: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7692"

    # Operation 7693: get
    assert tree.invariants(), "Invariants failed at step 7693"

    # Operation 7694: delete
    del tree[7205]
    del reference[7205]
    assert tree.invariants(), "Invariants failed at step 7694"

    # Operation 7695: insert
    tree[1124] = 'value_587298'
    reference[1124] = 'value_587298'
    assert tree.invariants(), "Invariants failed at step 7695"

    # Operation 7696: insert
    tree[5995] = 'value_910225'
    reference[5995] = 'value_910225'
    assert tree.invariants(), "Invariants failed at step 7696"

    # Operation 7697: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7697"

    # Operation 7698: get
    assert tree.invariants(), "Invariants failed at step 7698"

    # Operation 7699: insert
    tree[9791] = 'value_621583'
    reference[9791] = 'value_621583'
    assert tree.invariants(), "Invariants failed at step 7699"

    # Operation 7700: get
    assert tree.invariants(), "Invariants failed at step 7700"

    # Operation 7701: update
    tree[9791] = 'value_65797'
    reference[9791] = 'value_65797'
    assert tree.invariants(), "Invariants failed at step 7701"

    # Operation 7702: insert
    tree[400] = 'value_888867'
    reference[400] = 'value_888867'
    assert tree.invariants(), "Invariants failed at step 7702"

    # Operation 7703: delete
    del tree[1124]
    del reference[1124]
    assert tree.invariants(), "Invariants failed at step 7703"

    # Operation 7704: update
    tree[5995] = 'value_203231'
    reference[5995] = 'value_203231'
    assert tree.invariants(), "Invariants failed at step 7704"

    # Operation 7705: get
    assert tree.invariants(), "Invariants failed at step 7705"

    # Operation 7706: delete
    del tree[5995]
    del reference[5995]
    assert tree.invariants(), "Invariants failed at step 7706"

    # Operation 7707: update
    tree[400] = 'value_997819'
    reference[400] = 'value_997819'
    assert tree.invariants(), "Invariants failed at step 7707"

    # Operation 7708: get
    assert tree.invariants(), "Invariants failed at step 7708"

    # Operation 7709: get
    assert tree.invariants(), "Invariants failed at step 7709"

    # Operation 7710: insert
    tree[8750] = 'value_704021'
    reference[8750] = 'value_704021'
    assert tree.invariants(), "Invariants failed at step 7710"

    # Operation 7711: get
    assert tree.invariants(), "Invariants failed at step 7711"

    # Operation 7712: get
    assert tree.invariants(), "Invariants failed at step 7712"

    # Operation 7713: insert
    tree[4029] = 'value_836197'
    reference[4029] = 'value_836197'
    assert tree.invariants(), "Invariants failed at step 7713"

    # Operation 7714: insert
    tree[2432] = 'value_905681'
    reference[2432] = 'value_905681'
    assert tree.invariants(), "Invariants failed at step 7714"

    # Operation 7715: insert
    tree[6759] = 'value_561266'
    reference[6759] = 'value_561266'
    assert tree.invariants(), "Invariants failed at step 7715"

    # Operation 7716: delete
    del tree[4029]
    del reference[4029]
    assert tree.invariants(), "Invariants failed at step 7716"

    # Operation 7717: insert
    tree[2497] = 'value_201048'
    reference[2497] = 'value_201048'
    assert tree.invariants(), "Invariants failed at step 7717"

    # Operation 7718: delete
    del tree[8750]
    del reference[8750]
    assert tree.invariants(), "Invariants failed at step 7718"

    # Operation 7719: get
    assert tree.invariants(), "Invariants failed at step 7719"

    # Operation 7720: delete
    del tree[6759]
    del reference[6759]
    assert tree.invariants(), "Invariants failed at step 7720"

    # Operation 7721: insert
    tree[8523] = 'value_266811'
    reference[8523] = 'value_266811'
    assert tree.invariants(), "Invariants failed at step 7721"

    # Operation 7722: insert
    tree[8202] = 'value_437821'
    reference[8202] = 'value_437821'
    assert tree.invariants(), "Invariants failed at step 7722"

    # Operation 7723: batch_delete
    keys_to_delete = [2497, 7147, 400, 6708, 9791]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7723"

    # Operation 7724: get
    assert tree.invariants(), "Invariants failed at step 7724"

    # Operation 7725: delete
    del tree[8616]
    del reference[8616]
    assert tree.invariants(), "Invariants failed at step 7725"

    # Operation 7726: delete
    del tree[8202]
    del reference[8202]
    assert tree.invariants(), "Invariants failed at step 7726"

    # Operation 7727: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7727"

    # Operation 7728: delete
    del tree[8523]
    del reference[8523]
    assert tree.invariants(), "Invariants failed at step 7728"

    # Operation 7729: get
    assert tree.invariants(), "Invariants failed at step 7729"

    # Operation 7730: delete
    del tree[2432]
    del reference[2432]
    assert tree.invariants(), "Invariants failed at step 7730"

    # Operation 7731: insert
    tree[9182] = 'value_652344'
    reference[9182] = 'value_652344'
    assert tree.invariants(), "Invariants failed at step 7731"

    # Operation 7732: insert
    tree[2136] = 'value_295777'
    reference[2136] = 'value_295777'
    assert tree.invariants(), "Invariants failed at step 7732"

    # Operation 7733: delete
    del tree[9182]
    del reference[9182]
    assert tree.invariants(), "Invariants failed at step 7733"

    # Operation 7734: update
    tree[2136] = 'value_4482'
    reference[2136] = 'value_4482'
    assert tree.invariants(), "Invariants failed at step 7734"

    # Operation 7735: insert
    tree[2926] = 'value_916077'
    reference[2926] = 'value_916077'
    assert tree.invariants(), "Invariants failed at step 7735"

    # Operation 7736: get
    assert tree.invariants(), "Invariants failed at step 7736"

    # Operation 7737: get
    assert tree.invariants(), "Invariants failed at step 7737"

    # Operation 7738: insert
    tree[2037] = 'value_240805'
    reference[2037] = 'value_240805'
    assert tree.invariants(), "Invariants failed at step 7738"

    # Operation 7739: delete
    del tree[2136]
    del reference[2136]
    assert tree.invariants(), "Invariants failed at step 7739"

    # Operation 7740: insert
    tree[3230] = 'value_382797'
    reference[3230] = 'value_382797'
    assert tree.invariants(), "Invariants failed at step 7740"

    # Operation 7741: delete
    del tree[2037]
    del reference[2037]
    assert tree.invariants(), "Invariants failed at step 7741"

    # Operation 7742: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7742"

    # Operation 7743: delete
    del tree[2926]
    del reference[2926]
    assert tree.invariants(), "Invariants failed at step 7743"

    # Operation 7744: insert
    tree[1596] = 'value_274667'
    reference[1596] = 'value_274667'
    assert tree.invariants(), "Invariants failed at step 7744"

    # Operation 7745: insert
    tree[7484] = 'value_652065'
    reference[7484] = 'value_652065'
    assert tree.invariants(), "Invariants failed at step 7745"

    # Operation 7746: get
    assert tree.invariants(), "Invariants failed at step 7746"

    # Operation 7747: delete
    del tree[7484]
    del reference[7484]
    assert tree.invariants(), "Invariants failed at step 7747"

    # Operation 7748: get
    assert tree.invariants(), "Invariants failed at step 7748"

    # Operation 7749: insert
    tree[2700] = 'value_602320'
    reference[2700] = 'value_602320'
    assert tree.invariants(), "Invariants failed at step 7749"

    # Operation 7750: get
    assert tree.invariants(), "Invariants failed at step 7750"

    # Operation 7751: get
    assert tree.invariants(), "Invariants failed at step 7751"

    # Operation 7752: get
    assert tree.invariants(), "Invariants failed at step 7752"

    # Operation 7753: delete
    del tree[1596]
    del reference[1596]
    assert tree.invariants(), "Invariants failed at step 7753"

    # Operation 7754: delete
    del tree[2700]
    del reference[2700]
    assert tree.invariants(), "Invariants failed at step 7754"

    # Operation 7755: get
    assert tree.invariants(), "Invariants failed at step 7755"

    # Operation 7756: update
    tree[3230] = 'value_200069'
    reference[3230] = 'value_200069'
    assert tree.invariants(), "Invariants failed at step 7756"

    # Operation 7757: insert
    tree[9595] = 'value_501101'
    reference[9595] = 'value_501101'
    assert tree.invariants(), "Invariants failed at step 7757"

    # Operation 7758: get
    assert tree.invariants(), "Invariants failed at step 7758"

    # Operation 7759: delete
    del tree[3230]
    del reference[3230]
    assert tree.invariants(), "Invariants failed at step 7759"

    # Operation 7760: get
    assert tree.invariants(), "Invariants failed at step 7760"

    # Operation 7761: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7761"

    # Operation 7762: delete
    del tree[9595]
    del reference[9595]
    assert tree.invariants(), "Invariants failed at step 7762"

    # Operation 7763: insert
    tree[1551] = 'value_469485'
    reference[1551] = 'value_469485'
    assert tree.invariants(), "Invariants failed at step 7763"

    # Operation 7764: insert
    tree[409] = 'value_417592'
    reference[409] = 'value_417592'
    assert tree.invariants(), "Invariants failed at step 7764"

    # Operation 7765: insert
    tree[114] = 'value_966041'
    reference[114] = 'value_966041'
    assert tree.invariants(), "Invariants failed at step 7765"

    # Operation 7766: insert
    tree[5342] = 'value_456573'
    reference[5342] = 'value_456573'
    assert tree.invariants(), "Invariants failed at step 7766"

    # Operation 7767: insert
    tree[2903] = 'value_573208'
    reference[2903] = 'value_573208'
    assert tree.invariants(), "Invariants failed at step 7767"

    # Operation 7768: get
    assert tree.invariants(), "Invariants failed at step 7768"

    # Operation 7769: update
    tree[1551] = 'value_732434'
    reference[1551] = 'value_732434'
    assert tree.invariants(), "Invariants failed at step 7769"

    # Operation 7770: insert
    tree[3453] = 'value_507390'
    reference[3453] = 'value_507390'
    assert tree.invariants(), "Invariants failed at step 7770"

    # Operation 7771: delete
    del tree[1551]
    del reference[1551]
    assert tree.invariants(), "Invariants failed at step 7771"

    # Operation 7772: insert
    tree[7465] = 'value_641891'
    reference[7465] = 'value_641891'
    assert tree.invariants(), "Invariants failed at step 7772"

    # Operation 7773: delete
    del tree[2903]
    del reference[2903]
    assert tree.invariants(), "Invariants failed at step 7773"

    # Operation 7774: get
    assert tree.invariants(), "Invariants failed at step 7774"

    # Operation 7775: batch_delete
    keys_to_delete = [409, 6827, 718, 5342]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7775"

    # Operation 7776: insert
    tree[6366] = 'value_345864'
    reference[6366] = 'value_345864'
    assert tree.invariants(), "Invariants failed at step 7776"

    # Operation 7777: get
    assert tree.invariants(), "Invariants failed at step 7777"

    # Operation 7778: insert
    tree[8184] = 'value_542016'
    reference[8184] = 'value_542016'
    assert tree.invariants(), "Invariants failed at step 7778"

    # Operation 7779: get
    assert tree.invariants(), "Invariants failed at step 7779"

    # Operation 7780: update
    tree[114] = 'value_797479'
    reference[114] = 'value_797479'
    assert tree.invariants(), "Invariants failed at step 7780"

    # Operation 7781: update
    tree[3453] = 'value_800890'
    reference[3453] = 'value_800890'
    assert tree.invariants(), "Invariants failed at step 7781"

    # Operation 7782: update
    tree[7465] = 'value_729825'
    reference[7465] = 'value_729825'
    assert tree.invariants(), "Invariants failed at step 7782"

    # Operation 7783: insert
    tree[8489] = 'value_703386'
    reference[8489] = 'value_703386'
    assert tree.invariants(), "Invariants failed at step 7783"

    # Operation 7784: insert
    tree[6284] = 'value_19248'
    reference[6284] = 'value_19248'
    assert tree.invariants(), "Invariants failed at step 7784"

    # Operation 7785: get
    assert tree.invariants(), "Invariants failed at step 7785"

    # Operation 7786: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7786"

    # Operation 7787: insert
    tree[546] = 'value_171643'
    reference[546] = 'value_171643'
    assert tree.invariants(), "Invariants failed at step 7787"

    # Operation 7788: batch_delete
    keys_to_delete = [546, 1891, 8489, 114, 8250, 3453]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7788"

    # Operation 7789: update
    tree[6366] = 'value_860961'
    reference[6366] = 'value_860961'
    assert tree.invariants(), "Invariants failed at step 7789"

    # Operation 7790: insert
    tree[5577] = 'value_166733'
    reference[5577] = 'value_166733'
    assert tree.invariants(), "Invariants failed at step 7790"

    # Operation 7791: insert
    tree[728] = 'value_367884'
    reference[728] = 'value_367884'
    assert tree.invariants(), "Invariants failed at step 7791"

    # Operation 7792: delete
    del tree[728]
    del reference[728]
    assert tree.invariants(), "Invariants failed at step 7792"

    # Operation 7793: get
    assert tree.invariants(), "Invariants failed at step 7793"

    # Operation 7794: batch_delete
    keys_to_delete = [4136, 9401, 6284, 6366]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7794"

    # Operation 7795: insert
    tree[5464] = 'value_958583'
    reference[5464] = 'value_958583'
    assert tree.invariants(), "Invariants failed at step 7795"

    # Operation 7796: insert
    tree[6345] = 'value_824121'
    reference[6345] = 'value_824121'
    assert tree.invariants(), "Invariants failed at step 7796"

    # Operation 7797: insert
    tree[4694] = 'value_48506'
    reference[4694] = 'value_48506'
    assert tree.invariants(), "Invariants failed at step 7797"

    # Operation 7798: insert
    tree[5802] = 'value_245341'
    reference[5802] = 'value_245341'
    assert tree.invariants(), "Invariants failed at step 7798"

    # Operation 7799: insert
    tree[4888] = 'value_788564'
    reference[4888] = 'value_788564'
    assert tree.invariants(), "Invariants failed at step 7799"

    # Operation 7800: insert
    tree[9179] = 'value_62560'
    reference[9179] = 'value_62560'
    assert tree.invariants(), "Invariants failed at step 7800"

    # Operation 7801: delete
    del tree[4888]
    del reference[4888]
    assert tree.invariants(), "Invariants failed at step 7801"

    # Operation 7802: delete
    del tree[9179]
    del reference[9179]
    assert tree.invariants(), "Invariants failed at step 7802"

    # Operation 7803: get
    assert tree.invariants(), "Invariants failed at step 7803"

    # Operation 7804: insert
    tree[996] = 'value_21485'
    reference[996] = 'value_21485'
    assert tree.invariants(), "Invariants failed at step 7804"

    # Operation 7805: get
    assert tree.invariants(), "Invariants failed at step 7805"

    # Operation 7806: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7806"

    # Operation 7807: get
    assert tree.invariants(), "Invariants failed at step 7807"

    # Operation 7808: insert
    tree[7087] = 'value_65790'
    reference[7087] = 'value_65790'
    assert tree.invariants(), "Invariants failed at step 7808"

    # Operation 7809: delete
    del tree[7465]
    del reference[7465]
    assert tree.invariants(), "Invariants failed at step 7809"

    # Operation 7810: batch_delete
    keys_to_delete = [8184, 963, 6345, 7087, 695, 5464]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7810"

    # Operation 7811: delete
    del tree[5802]
    del reference[5802]
    assert tree.invariants(), "Invariants failed at step 7811"

    # Operation 7812: update
    tree[996] = 'value_606665'
    reference[996] = 'value_606665'
    assert tree.invariants(), "Invariants failed at step 7812"

    # Operation 7813: get
    assert tree.invariants(), "Invariants failed at step 7813"

    # Operation 7814: delete
    del tree[5577]
    del reference[5577]
    assert tree.invariants(), "Invariants failed at step 7814"

    # Operation 7815: delete
    del tree[996]
    del reference[996]
    assert tree.invariants(), "Invariants failed at step 7815"

    # Operation 7816: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7816"

    # Operation 7817: delete
    del tree[4694]
    del reference[4694]
    assert tree.invariants(), "Invariants failed at step 7817"

    # Operation 7818: insert
    tree[1596] = 'value_101370'
    reference[1596] = 'value_101370'
    assert tree.invariants(), "Invariants failed at step 7818"

    # Operation 7819: get
    assert tree.invariants(), "Invariants failed at step 7819"

    # Operation 7820: insert
    tree[1828] = 'value_667312'
    reference[1828] = 'value_667312'
    assert tree.invariants(), "Invariants failed at step 7820"

    # Operation 7821: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7821"

    # Operation 7822: delete
    del tree[1596]
    del reference[1596]
    assert tree.invariants(), "Invariants failed at step 7822"

    # Operation 7823: insert
    tree[9317] = 'value_972954'
    reference[9317] = 'value_972954'
    assert tree.invariants(), "Invariants failed at step 7823"

    # Operation 7824: insert
    tree[7693] = 'value_383625'
    reference[7693] = 'value_383625'
    assert tree.invariants(), "Invariants failed at step 7824"

    # Operation 7825: insert
    tree[7041] = 'value_780729'
    reference[7041] = 'value_780729'
    assert tree.invariants(), "Invariants failed at step 7825"

    # Operation 7826: insert
    tree[5736] = 'value_415909'
    reference[5736] = 'value_415909'
    assert tree.invariants(), "Invariants failed at step 7826"

    # Operation 7827: delete
    del tree[9317]
    del reference[9317]
    assert tree.invariants(), "Invariants failed at step 7827"

    # Operation 7828: delete
    del tree[5736]
    del reference[5736]
    assert tree.invariants(), "Invariants failed at step 7828"

    # Operation 7829: get
    assert tree.invariants(), "Invariants failed at step 7829"

    # Operation 7830: delete
    del tree[1828]
    del reference[1828]
    assert tree.invariants(), "Invariants failed at step 7830"

    # Operation 7831: get
    assert tree.invariants(), "Invariants failed at step 7831"

    # Operation 7832: update
    tree[7693] = 'value_546838'
    reference[7693] = 'value_546838'
    assert tree.invariants(), "Invariants failed at step 7832"

    # Operation 7833: get
    assert tree.invariants(), "Invariants failed at step 7833"

    # Operation 7834: delete
    del tree[7693]
    del reference[7693]
    assert tree.invariants(), "Invariants failed at step 7834"

    # Operation 7835: get
    assert tree.invariants(), "Invariants failed at step 7835"

    # Operation 7836: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7836"

    # Operation 7837: insert
    tree[8577] = 'value_80730'
    reference[8577] = 'value_80730'
    assert tree.invariants(), "Invariants failed at step 7837"

    # Operation 7838: insert
    tree[2136] = 'value_358158'
    reference[2136] = 'value_358158'
    assert tree.invariants(), "Invariants failed at step 7838"

    # Operation 7839: delete
    del tree[2136]
    del reference[2136]
    assert tree.invariants(), "Invariants failed at step 7839"

    # Operation 7840: insert
    tree[4610] = 'value_719227'
    reference[4610] = 'value_719227'
    assert tree.invariants(), "Invariants failed at step 7840"

    # Operation 7841: insert
    tree[3717] = 'value_873793'
    reference[3717] = 'value_873793'
    assert tree.invariants(), "Invariants failed at step 7841"

    # Operation 7842: insert
    tree[7370] = 'value_557194'
    reference[7370] = 'value_557194'
    assert tree.invariants(), "Invariants failed at step 7842"

    # Operation 7843: get
    assert tree.invariants(), "Invariants failed at step 7843"

    # Operation 7844: get
    assert tree.invariants(), "Invariants failed at step 7844"

    # Operation 7845: get
    assert tree.invariants(), "Invariants failed at step 7845"

    # Operation 7846: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7846"

    # Operation 7847: delete
    del tree[3717]
    del reference[3717]
    assert tree.invariants(), "Invariants failed at step 7847"

    # Operation 7848: insert
    tree[4344] = 'value_247500'
    reference[4344] = 'value_247500'
    assert tree.invariants(), "Invariants failed at step 7848"

    # Operation 7849: insert
    tree[1260] = 'value_45918'
    reference[1260] = 'value_45918'
    assert tree.invariants(), "Invariants failed at step 7849"

    # Operation 7850: delete
    del tree[4344]
    del reference[4344]
    assert tree.invariants(), "Invariants failed at step 7850"

    # Operation 7851: batch_delete
    keys_to_delete = [7041, 4610, 3772, 7957]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7851"

    # Operation 7852: update
    tree[7370] = 'value_280712'
    reference[7370] = 'value_280712'
    assert tree.invariants(), "Invariants failed at step 7852"

    # Operation 7853: delete
    del tree[1260]
    del reference[1260]
    assert tree.invariants(), "Invariants failed at step 7853"

    # Operation 7854: get
    assert tree.invariants(), "Invariants failed at step 7854"

    # Operation 7855: get
    assert tree.invariants(), "Invariants failed at step 7855"

    # Operation 7856: insert
    tree[6163] = 'value_963335'
    reference[6163] = 'value_963335'
    assert tree.invariants(), "Invariants failed at step 7856"

    # Operation 7857: insert
    tree[3577] = 'value_88287'
    reference[3577] = 'value_88287'
    assert tree.invariants(), "Invariants failed at step 7857"

    # Operation 7858: insert
    tree[739] = 'value_910857'
    reference[739] = 'value_910857'
    assert tree.invariants(), "Invariants failed at step 7858"

    # Operation 7859: insert
    tree[667] = 'value_628989'
    reference[667] = 'value_628989'
    assert tree.invariants(), "Invariants failed at step 7859"

    # Operation 7860: batch_delete
    keys_to_delete = [739, 7370, 8463, 7733, 667]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7860"

    # Operation 7861: insert
    tree[6859] = 'value_798757'
    reference[6859] = 'value_798757'
    assert tree.invariants(), "Invariants failed at step 7861"

    # Operation 7862: insert
    tree[4077] = 'value_421296'
    reference[4077] = 'value_421296'
    assert tree.invariants(), "Invariants failed at step 7862"

    # Operation 7863: insert
    tree[4270] = 'value_699378'
    reference[4270] = 'value_699378'
    assert tree.invariants(), "Invariants failed at step 7863"

    # Operation 7864: batch_delete
    keys_to_delete = [4077, 4270, 6163, 3577, 8863]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7864"

    # Operation 7865: delete
    del tree[8577]
    del reference[8577]
    assert tree.invariants(), "Invariants failed at step 7865"

    # Operation 7866: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7866"

    # Operation 7867: get
    assert tree.invariants(), "Invariants failed at step 7867"

    # Operation 7868: delete
    del tree[6859]
    del reference[6859]
    assert tree.invariants(), "Invariants failed at step 7868"

    # Operation 7869: get
    assert tree.invariants(), "Invariants failed at step 7869"

    # Operation 7870: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7870"

    # Operation 7871: insert
    tree[8094] = 'value_608822'
    reference[8094] = 'value_608822'
    assert tree.invariants(), "Invariants failed at step 7871"

    # Operation 7872: delete
    del tree[8094]
    del reference[8094]
    assert tree.invariants(), "Invariants failed at step 7872"

    # Operation 7873: get
    assert tree.invariants(), "Invariants failed at step 7873"

    # Operation 7874: insert
    tree[8498] = 'value_659129'
    reference[8498] = 'value_659129'
    assert tree.invariants(), "Invariants failed at step 7874"

    # Operation 7875: update
    tree[8498] = 'value_144274'
    reference[8498] = 'value_144274'
    assert tree.invariants(), "Invariants failed at step 7875"

    # Operation 7876: delete
    del tree[8498]
    del reference[8498]
    assert tree.invariants(), "Invariants failed at step 7876"

    # Operation 7877: insert
    tree[9515] = 'value_747256'
    reference[9515] = 'value_747256'
    assert tree.invariants(), "Invariants failed at step 7877"

    # Operation 7878: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7878"

    # Operation 7879: get
    assert tree.invariants(), "Invariants failed at step 7879"

    # Operation 7880: delete
    del tree[9515]
    del reference[9515]
    assert tree.invariants(), "Invariants failed at step 7880"

    # Operation 7881: insert
    tree[5177] = 'value_771422'
    reference[5177] = 'value_771422'
    assert tree.invariants(), "Invariants failed at step 7881"

    # Operation 7882: insert
    tree[9784] = 'value_843280'
    reference[9784] = 'value_843280'
    assert tree.invariants(), "Invariants failed at step 7882"

    # Operation 7883: delete
    del tree[9784]
    del reference[9784]
    assert tree.invariants(), "Invariants failed at step 7883"

    # Operation 7884: get
    assert tree.invariants(), "Invariants failed at step 7884"

    # Operation 7885: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7885"

    # Operation 7886: delete
    del tree[5177]
    del reference[5177]
    assert tree.invariants(), "Invariants failed at step 7886"

    # Operation 7887: get
    assert tree.invariants(), "Invariants failed at step 7887"

    # Operation 7888: get
    assert tree.invariants(), "Invariants failed at step 7888"

    # Operation 7889: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7889"

    # Operation 7890: insert
    tree[9915] = 'value_498001'
    reference[9915] = 'value_498001'
    assert tree.invariants(), "Invariants failed at step 7890"

    # Operation 7891: get
    assert tree.invariants(), "Invariants failed at step 7891"

    # Operation 7892: delete
    del tree[9915]
    del reference[9915]
    assert tree.invariants(), "Invariants failed at step 7892"

    # Operation 7893: get
    assert tree.invariants(), "Invariants failed at step 7893"

    # Operation 7894: insert
    tree[7618] = 'value_555205'
    reference[7618] = 'value_555205'
    assert tree.invariants(), "Invariants failed at step 7894"

    # Operation 7895: insert
    tree[9560] = 'value_706305'
    reference[9560] = 'value_706305'
    assert tree.invariants(), "Invariants failed at step 7895"

    # Operation 7896: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7896"

    # Operation 7897: insert
    tree[8201] = 'value_488678'
    reference[8201] = 'value_488678'
    assert tree.invariants(), "Invariants failed at step 7897"

    # Operation 7898: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7898"

    # Operation 7899: delete
    del tree[9560]
    del reference[9560]
    assert tree.invariants(), "Invariants failed at step 7899"

    # Operation 7900: insert
    tree[1682] = 'value_260466'
    reference[1682] = 'value_260466'
    assert tree.invariants(), "Invariants failed at step 7900"

    # Operation 7901: update
    tree[8201] = 'value_786274'
    reference[8201] = 'value_786274'
    assert tree.invariants(), "Invariants failed at step 7901"

    # Operation 7902: delete
    del tree[1682]
    del reference[1682]
    assert tree.invariants(), "Invariants failed at step 7902"

    # Operation 7903: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7903"

    # Operation 7904: insert
    tree[3047] = 'value_472690'
    reference[3047] = 'value_472690'
    assert tree.invariants(), "Invariants failed at step 7904"

    # Operation 7905: insert
    tree[7084] = 'value_575578'
    reference[7084] = 'value_575578'
    assert tree.invariants(), "Invariants failed at step 7905"

    # Operation 7906: insert
    tree[8986] = 'value_593062'
    reference[8986] = 'value_593062'
    assert tree.invariants(), "Invariants failed at step 7906"

    # Operation 7907: delete
    del tree[8201]
    del reference[8201]
    assert tree.invariants(), "Invariants failed at step 7907"

    # Operation 7908: insert
    tree[4806] = 'value_893860'
    reference[4806] = 'value_893860'
    assert tree.invariants(), "Invariants failed at step 7908"

    # Operation 7909: delete
    del tree[7618]
    del reference[7618]
    assert tree.invariants(), "Invariants failed at step 7909"

    # Operation 7910: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7910"

    # Operation 7911: insert
    tree[8656] = 'value_942305'
    reference[8656] = 'value_942305'
    assert tree.invariants(), "Invariants failed at step 7911"

    # Operation 7912: batch_delete
    keys_to_delete = [1633, 7084, 5446, 3047]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7912"

    # Operation 7913: get
    assert tree.invariants(), "Invariants failed at step 7913"

    # Operation 7914: insert
    tree[3217] = 'value_630119'
    reference[3217] = 'value_630119'
    assert tree.invariants(), "Invariants failed at step 7914"

    # Operation 7915: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 7915"

    # Operation 7916: get
    assert tree.invariants(), "Invariants failed at step 7916"

    # Operation 7917: delete
    del tree[3217]
    del reference[3217]
    assert tree.invariants(), "Invariants failed at step 7917"

    # Operation 7918: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7918"

    # Operation 7919: insert
    tree[7392] = 'value_366289'
    reference[7392] = 'value_366289'
    assert tree.invariants(), "Invariants failed at step 7919"

    # Operation 7920: get
    assert tree.invariants(), "Invariants failed at step 7920"

    # Operation 7921: update
    tree[7392] = 'value_148131'
    reference[7392] = 'value_148131'
    assert tree.invariants(), "Invariants failed at step 7921"

    # Operation 7922: insert
    tree[209] = 'value_497057'
    reference[209] = 'value_497057'
    assert tree.invariants(), "Invariants failed at step 7922"

    # Operation 7923: insert
    tree[7733] = 'value_223773'
    reference[7733] = 'value_223773'
    assert tree.invariants(), "Invariants failed at step 7923"

    # Operation 7924: insert
    tree[7589] = 'value_650720'
    reference[7589] = 'value_650720'
    assert tree.invariants(), "Invariants failed at step 7924"

    # Operation 7925: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7925"

    # Operation 7926: batch_delete
    keys_to_delete = [7589, 209, 9879, 1529, 8986]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7926"

    # Operation 7927: delete
    del tree[4806]
    del reference[4806]
    assert tree.invariants(), "Invariants failed at step 7927"

    # Operation 7928: insert
    tree[8069] = 'value_714770'
    reference[8069] = 'value_714770'
    assert tree.invariants(), "Invariants failed at step 7928"

    # Operation 7929: get
    assert tree.invariants(), "Invariants failed at step 7929"

    # Operation 7930: update
    tree[7733] = 'value_235109'
    reference[7733] = 'value_235109'
    assert tree.invariants(), "Invariants failed at step 7930"

    # Operation 7931: delete
    del tree[8656]
    del reference[8656]
    assert tree.invariants(), "Invariants failed at step 7931"

    # Operation 7932: delete
    del tree[8069]
    del reference[8069]
    assert tree.invariants(), "Invariants failed at step 7932"

    # Operation 7933: delete
    del tree[7733]
    del reference[7733]
    assert tree.invariants(), "Invariants failed at step 7933"

    # Operation 7934: insert
    tree[1013] = 'value_212493'
    reference[1013] = 'value_212493'
    assert tree.invariants(), "Invariants failed at step 7934"

    # Operation 7935: delete
    del tree[1013]
    del reference[1013]
    assert tree.invariants(), "Invariants failed at step 7935"

    # Operation 7936: get
    assert tree.invariants(), "Invariants failed at step 7936"

    # Operation 7937: get
    assert tree.invariants(), "Invariants failed at step 7937"

    # Operation 7938: get
    assert tree.invariants(), "Invariants failed at step 7938"

    # Operation 7939: get
    assert tree.invariants(), "Invariants failed at step 7939"

    # Operation 7940: update
    tree[7392] = 'value_309946'
    reference[7392] = 'value_309946'
    assert tree.invariants(), "Invariants failed at step 7940"

    # Operation 7941: insert
    tree[3589] = 'value_711297'
    reference[3589] = 'value_711297'
    assert tree.invariants(), "Invariants failed at step 7941"

    # Operation 7942: delete
    del tree[3589]
    del reference[3589]
    assert tree.invariants(), "Invariants failed at step 7942"

    # Operation 7943: delete
    del tree[7392]
    del reference[7392]
    assert tree.invariants(), "Invariants failed at step 7943"

    # Operation 7944: get
    assert tree.invariants(), "Invariants failed at step 7944"

    # Operation 7945: get
    assert tree.invariants(), "Invariants failed at step 7945"

    # Operation 7946: insert
    tree[9105] = 'value_652546'
    reference[9105] = 'value_652546'
    assert tree.invariants(), "Invariants failed at step 7946"

    # Operation 7947: update
    tree[9105] = 'value_451289'
    reference[9105] = 'value_451289'
    assert tree.invariants(), "Invariants failed at step 7947"

    # Operation 7948: insert
    tree[40] = 'value_54352'
    reference[40] = 'value_54352'
    assert tree.invariants(), "Invariants failed at step 7948"

    # Operation 7949: insert
    tree[9010] = 'value_133109'
    reference[9010] = 'value_133109'
    assert tree.invariants(), "Invariants failed at step 7949"

    # Operation 7950: delete
    del tree[9105]
    del reference[9105]
    assert tree.invariants(), "Invariants failed at step 7950"

    # Operation 7951: get
    assert tree.invariants(), "Invariants failed at step 7951"

    # Operation 7952: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7952"

    # Operation 7953: insert
    tree[563] = 'value_752763'
    reference[563] = 'value_752763'
    assert tree.invariants(), "Invariants failed at step 7953"

    # Operation 7954: delete
    del tree[9010]
    del reference[9010]
    assert tree.invariants(), "Invariants failed at step 7954"

    # Operation 7955: insert
    tree[1847] = 'value_351170'
    reference[1847] = 'value_351170'
    assert tree.invariants(), "Invariants failed at step 7955"

    # Operation 7956: delete
    del tree[1847]
    del reference[1847]
    assert tree.invariants(), "Invariants failed at step 7956"

    # Operation 7957: insert
    tree[2338] = 'value_165838'
    reference[2338] = 'value_165838'
    assert tree.invariants(), "Invariants failed at step 7957"

    # Operation 7958: delete
    del tree[563]
    del reference[563]
    assert tree.invariants(), "Invariants failed at step 7958"

    # Operation 7959: insert
    tree[1997] = 'value_499408'
    reference[1997] = 'value_499408'
    assert tree.invariants(), "Invariants failed at step 7959"

    # Operation 7960: delete
    del tree[40]
    del reference[40]
    assert tree.invariants(), "Invariants failed at step 7960"

    # Operation 7961: delete
    del tree[2338]
    del reference[2338]
    assert tree.invariants(), "Invariants failed at step 7961"

    # Operation 7962: delete
    del tree[1997]
    del reference[1997]
    assert tree.invariants(), "Invariants failed at step 7962"

    # Operation 7963: insert
    tree[9231] = 'value_361573'
    reference[9231] = 'value_361573'
    assert tree.invariants(), "Invariants failed at step 7963"

    # Operation 7964: insert
    tree[4304] = 'value_688236'
    reference[4304] = 'value_688236'
    assert tree.invariants(), "Invariants failed at step 7964"

    # Operation 7965: delete
    del tree[9231]
    del reference[9231]
    assert tree.invariants(), "Invariants failed at step 7965"

    # Operation 7966: insert
    tree[6643] = 'value_199466'
    reference[6643] = 'value_199466'
    assert tree.invariants(), "Invariants failed at step 7966"

    # Operation 7967: delete
    del tree[4304]
    del reference[4304]
    assert tree.invariants(), "Invariants failed at step 7967"

    # Operation 7968: insert
    tree[8548] = 'value_2833'
    reference[8548] = 'value_2833'
    assert tree.invariants(), "Invariants failed at step 7968"

    # Operation 7969: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7969"

    # Operation 7970: insert
    tree[4009] = 'value_770866'
    reference[4009] = 'value_770866'
    assert tree.invariants(), "Invariants failed at step 7970"

    # Operation 7971: delete
    del tree[4009]
    del reference[4009]
    assert tree.invariants(), "Invariants failed at step 7971"

    # Operation 7972: insert
    tree[5226] = 'value_496094'
    reference[5226] = 'value_496094'
    assert tree.invariants(), "Invariants failed at step 7972"

    # Operation 7973: update
    tree[8548] = 'value_941922'
    reference[8548] = 'value_941922'
    assert tree.invariants(), "Invariants failed at step 7973"

    # Operation 7974: delete
    del tree[6643]
    del reference[6643]
    assert tree.invariants(), "Invariants failed at step 7974"

    # Operation 7975: get
    assert tree.invariants(), "Invariants failed at step 7975"

    # Operation 7976: get
    assert tree.invariants(), "Invariants failed at step 7976"

    # Operation 7977: insert
    tree[8092] = 'value_463596'
    reference[8092] = 'value_463596'
    assert tree.invariants(), "Invariants failed at step 7977"

    # Operation 7978: get
    assert tree.invariants(), "Invariants failed at step 7978"

    # Operation 7979: insert
    tree[9578] = 'value_184328'
    reference[9578] = 'value_184328'
    assert tree.invariants(), "Invariants failed at step 7979"

    # Operation 7980: insert
    tree[1643] = 'value_553956'
    reference[1643] = 'value_553956'
    assert tree.invariants(), "Invariants failed at step 7980"

    # Operation 7981: insert
    tree[925] = 'value_450571'
    reference[925] = 'value_450571'
    assert tree.invariants(), "Invariants failed at step 7981"

    # Operation 7982: get
    assert tree.invariants(), "Invariants failed at step 7982"

    # Operation 7983: insert
    tree[7481] = 'value_313106'
    reference[7481] = 'value_313106'
    assert tree.invariants(), "Invariants failed at step 7983"

    # Operation 7984: get
    assert tree.invariants(), "Invariants failed at step 7984"

    # Operation 7985: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 7985"

    # Operation 7986: insert
    tree[6597] = 'value_528738'
    reference[6597] = 'value_528738'
    assert tree.invariants(), "Invariants failed at step 7986"

    # Operation 7987: batch_delete
    keys_to_delete = [6597, 8092, 9578, 5226, 7481, 4220]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 7987"

    # Operation 7988: delete
    del tree[1643]
    del reference[1643]
    assert tree.invariants(), "Invariants failed at step 7988"

    # Operation 7989: delete
    del tree[8548]
    del reference[8548]
    assert tree.invariants(), "Invariants failed at step 7989"

    # Operation 7990: get
    assert tree.invariants(), "Invariants failed at step 7990"

    # Operation 7991: delete
    del tree[925]
    del reference[925]
    assert tree.invariants(), "Invariants failed at step 7991"

    # Operation 7992: insert
    tree[9895] = 'value_596587'
    reference[9895] = 'value_596587'
    assert tree.invariants(), "Invariants failed at step 7992"

    # Operation 7993: get
    assert tree.invariants(), "Invariants failed at step 7993"

    # Operation 7994: delete
    del tree[9895]
    del reference[9895]
    assert tree.invariants(), "Invariants failed at step 7994"

    # Operation 7995: insert
    tree[394] = 'value_116627'
    reference[394] = 'value_116627'
    assert tree.invariants(), "Invariants failed at step 7995"

    # Operation 7996: insert
    tree[6331] = 'value_587370'
    reference[6331] = 'value_587370'
    assert tree.invariants(), "Invariants failed at step 7996"

    # Operation 7997: insert
    tree[3282] = 'value_872852'
    reference[3282] = 'value_872852'
    assert tree.invariants(), "Invariants failed at step 7997"

    # Operation 7998: delete
    del tree[3282]
    del reference[3282]
    assert tree.invariants(), "Invariants failed at step 7998"

    # Operation 7999: delete
    del tree[394]
    del reference[394]
    assert tree.invariants(), "Invariants failed at step 7999"

    # Operation 8000: insert
    tree[4585] = 'value_614766'
    reference[4585] = 'value_614766'
    assert tree.invariants(), "Invariants failed at step 8000"

    # Operation 8001: delete
    del tree[4585]
    del reference[4585]
    assert tree.invariants(), "Invariants failed at step 8001"

    # Operation 8002: update
    tree[6331] = 'value_253610'
    reference[6331] = 'value_253610'
    assert tree.invariants(), "Invariants failed at step 8002"

    # Operation 8003: get
    assert tree.invariants(), "Invariants failed at step 8003"

    # Operation 8004: insert
    tree[3457] = 'value_655'
    reference[3457] = 'value_655'
    assert tree.invariants(), "Invariants failed at step 8004"

    # Operation 8005: delete
    del tree[6331]
    del reference[6331]
    assert tree.invariants(), "Invariants failed at step 8005"

    # Operation 8006: get
    assert tree.invariants(), "Invariants failed at step 8006"

    # Operation 8007: insert
    tree[8425] = 'value_371816'
    reference[8425] = 'value_371816'
    assert tree.invariants(), "Invariants failed at step 8007"

    # Operation 8008: get
    assert tree.invariants(), "Invariants failed at step 8008"

    # Operation 8009: delete
    del tree[3457]
    del reference[3457]
    assert tree.invariants(), "Invariants failed at step 8009"

    # Operation 8010: get
    assert tree.invariants(), "Invariants failed at step 8010"

    # Operation 8011: delete
    del tree[8425]
    del reference[8425]
    assert tree.invariants(), "Invariants failed at step 8011"

    # Operation 8012: insert
    tree[4419] = 'value_369235'
    reference[4419] = 'value_369235'
    assert tree.invariants(), "Invariants failed at step 8012"

    # Operation 8013: get
    assert tree.invariants(), "Invariants failed at step 8013"

    # Operation 8014: delete
    del tree[4419]
    del reference[4419]
    assert tree.invariants(), "Invariants failed at step 8014"

    # Operation 8015: insert
    tree[3841] = 'value_27944'
    reference[3841] = 'value_27944'
    assert tree.invariants(), "Invariants failed at step 8015"

    # Operation 8016: update
    tree[3841] = 'value_871667'
    reference[3841] = 'value_871667'
    assert tree.invariants(), "Invariants failed at step 8016"

    # Operation 8017: delete
    del tree[3841]
    del reference[3841]
    assert tree.invariants(), "Invariants failed at step 8017"

    # Operation 8018: get
    assert tree.invariants(), "Invariants failed at step 8018"

    # Operation 8019: insert
    tree[1026] = 'value_764837'
    reference[1026] = 'value_764837'
    assert tree.invariants(), "Invariants failed at step 8019"

    # Operation 8020: delete
    del tree[1026]
    del reference[1026]
    assert tree.invariants(), "Invariants failed at step 8020"

    # Operation 8021: insert
    tree[6938] = 'value_435873'
    reference[6938] = 'value_435873'
    assert tree.invariants(), "Invariants failed at step 8021"

    # Operation 8022: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8022"

    # Operation 8023: update
    tree[6938] = 'value_306910'
    reference[6938] = 'value_306910'
    assert tree.invariants(), "Invariants failed at step 8023"

    # Operation 8024: delete
    del tree[6938]
    del reference[6938]
    assert tree.invariants(), "Invariants failed at step 8024"

    # Operation 8025: insert
    tree[59] = 'value_306204'
    reference[59] = 'value_306204'
    assert tree.invariants(), "Invariants failed at step 8025"

    # Operation 8026: insert
    tree[3031] = 'value_941048'
    reference[3031] = 'value_941048'
    assert tree.invariants(), "Invariants failed at step 8026"

    # Operation 8027: insert
    tree[7584] = 'value_284243'
    reference[7584] = 'value_284243'
    assert tree.invariants(), "Invariants failed at step 8027"

    # Operation 8028: get
    assert tree.invariants(), "Invariants failed at step 8028"

    # Operation 8029: delete
    del tree[59]
    del reference[59]
    assert tree.invariants(), "Invariants failed at step 8029"

    # Operation 8030: insert
    tree[3752] = 'value_185353'
    reference[3752] = 'value_185353'
    assert tree.invariants(), "Invariants failed at step 8030"

    # Operation 8031: delete
    del tree[3031]
    del reference[3031]
    assert tree.invariants(), "Invariants failed at step 8031"

    # Operation 8032: insert
    tree[6686] = 'value_890105'
    reference[6686] = 'value_890105'
    assert tree.invariants(), "Invariants failed at step 8032"

    # Operation 8033: insert
    tree[3191] = 'value_409647'
    reference[3191] = 'value_409647'
    assert tree.invariants(), "Invariants failed at step 8033"

    # Operation 8034: insert
    tree[7834] = 'value_361183'
    reference[7834] = 'value_361183'
    assert tree.invariants(), "Invariants failed at step 8034"

    # Operation 8035: delete
    del tree[6686]
    del reference[6686]
    assert tree.invariants(), "Invariants failed at step 8035"

    # Operation 8036: insert
    tree[1001] = 'value_340016'
    reference[1001] = 'value_340016'
    assert tree.invariants(), "Invariants failed at step 8036"

    # Operation 8037: delete
    del tree[1001]
    del reference[1001]
    assert tree.invariants(), "Invariants failed at step 8037"

    # Operation 8038: get
    assert tree.invariants(), "Invariants failed at step 8038"

    # Operation 8039: insert
    tree[8823] = 'value_804312'
    reference[8823] = 'value_804312'
    assert tree.invariants(), "Invariants failed at step 8039"

    # Operation 8040: delete
    del tree[7834]
    del reference[7834]
    assert tree.invariants(), "Invariants failed at step 8040"

    # Operation 8041: update
    tree[8823] = 'value_804512'
    reference[8823] = 'value_804512'
    assert tree.invariants(), "Invariants failed at step 8041"

    # Operation 8042: get
    assert tree.invariants(), "Invariants failed at step 8042"

    # Operation 8043: delete
    del tree[8823]
    del reference[8823]
    assert tree.invariants(), "Invariants failed at step 8043"

    # Operation 8044: insert
    tree[6325] = 'value_61123'
    reference[6325] = 'value_61123'
    assert tree.invariants(), "Invariants failed at step 8044"

    # Operation 8045: delete
    del tree[3752]
    del reference[3752]
    assert tree.invariants(), "Invariants failed at step 8045"

    # Operation 8046: delete
    del tree[7584]
    del reference[7584]
    assert tree.invariants(), "Invariants failed at step 8046"

    # Operation 8047: insert
    tree[3230] = 'value_847787'
    reference[3230] = 'value_847787'
    assert tree.invariants(), "Invariants failed at step 8047"

    # Operation 8048: get
    assert tree.invariants(), "Invariants failed at step 8048"

    # Operation 8049: get
    assert tree.invariants(), "Invariants failed at step 8049"

    # Operation 8050: update
    tree[3191] = 'value_562766'
    reference[3191] = 'value_562766'
    assert tree.invariants(), "Invariants failed at step 8050"

    # Operation 8051: delete
    del tree[6325]
    del reference[6325]
    assert tree.invariants(), "Invariants failed at step 8051"

    # Operation 8052: delete
    del tree[3230]
    del reference[3230]
    assert tree.invariants(), "Invariants failed at step 8052"

    # Operation 8053: insert
    tree[9800] = 'value_919767'
    reference[9800] = 'value_919767'
    assert tree.invariants(), "Invariants failed at step 8053"

    # Operation 8054: delete
    del tree[9800]
    del reference[9800]
    assert tree.invariants(), "Invariants failed at step 8054"

    # Operation 8055: delete
    del tree[3191]
    del reference[3191]
    assert tree.invariants(), "Invariants failed at step 8055"

    # Operation 8056: insert
    tree[871] = 'value_375643'
    reference[871] = 'value_375643'
    assert tree.invariants(), "Invariants failed at step 8056"

    # Operation 8057: insert
    tree[857] = 'value_156358'
    reference[857] = 'value_156358'
    assert tree.invariants(), "Invariants failed at step 8057"

    # Operation 8058: insert
    tree[1448] = 'value_912569'
    reference[1448] = 'value_912569'
    assert tree.invariants(), "Invariants failed at step 8058"

    # Operation 8059: get
    assert tree.invariants(), "Invariants failed at step 8059"

    # Operation 8060: insert
    tree[7248] = 'value_5842'
    reference[7248] = 'value_5842'
    assert tree.invariants(), "Invariants failed at step 8060"

    # Operation 8061: get
    assert tree.invariants(), "Invariants failed at step 8061"

    # Operation 8062: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8062"

    # Operation 8063: get
    assert tree.invariants(), "Invariants failed at step 8063"

    # Operation 8064: insert
    tree[3368] = 'value_628392'
    reference[3368] = 'value_628392'
    assert tree.invariants(), "Invariants failed at step 8064"

    # Operation 8065: delete
    del tree[3368]
    del reference[3368]
    assert tree.invariants(), "Invariants failed at step 8065"

    # Operation 8066: get
    assert tree.invariants(), "Invariants failed at step 8066"

    # Operation 8067: insert
    tree[1637] = 'value_753023'
    reference[1637] = 'value_753023'
    assert tree.invariants(), "Invariants failed at step 8067"

    # Operation 8068: get
    assert tree.invariants(), "Invariants failed at step 8068"

    # Operation 8069: get
    assert tree.invariants(), "Invariants failed at step 8069"

    # Operation 8070: delete
    del tree[1637]
    del reference[1637]
    assert tree.invariants(), "Invariants failed at step 8070"

    # Operation 8071: delete
    del tree[1448]
    del reference[1448]
    assert tree.invariants(), "Invariants failed at step 8071"

    # Operation 8072: delete
    del tree[7248]
    del reference[7248]
    assert tree.invariants(), "Invariants failed at step 8072"

    # Operation 8073: update
    tree[871] = 'value_71399'
    reference[871] = 'value_71399'
    assert tree.invariants(), "Invariants failed at step 8073"

    # Operation 8074: insert
    tree[1630] = 'value_323309'
    reference[1630] = 'value_323309'
    assert tree.invariants(), "Invariants failed at step 8074"

    # Operation 8075: get
    assert tree.invariants(), "Invariants failed at step 8075"

    # Operation 8076: delete
    del tree[1630]
    del reference[1630]
    assert tree.invariants(), "Invariants failed at step 8076"

    # Operation 8077: delete
    del tree[871]
    del reference[871]
    assert tree.invariants(), "Invariants failed at step 8077"

    # Operation 8078: insert
    tree[1125] = 'value_934836'
    reference[1125] = 'value_934836'
    assert tree.invariants(), "Invariants failed at step 8078"

    # Operation 8079: insert
    tree[1075] = 'value_777740'
    reference[1075] = 'value_777740'
    assert tree.invariants(), "Invariants failed at step 8079"

    # Operation 8080: insert
    tree[2969] = 'value_793245'
    reference[2969] = 'value_793245'
    assert tree.invariants(), "Invariants failed at step 8080"

    # Operation 8081: delete
    del tree[2969]
    del reference[2969]
    assert tree.invariants(), "Invariants failed at step 8081"

    # Operation 8082: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8082"

    # Operation 8083: insert
    tree[6602] = 'value_764916'
    reference[6602] = 'value_764916'
    assert tree.invariants(), "Invariants failed at step 8083"

    # Operation 8084: delete
    del tree[857]
    del reference[857]
    assert tree.invariants(), "Invariants failed at step 8084"

    # Operation 8085: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8085"

    # Operation 8086: delete
    del tree[6602]
    del reference[6602]
    assert tree.invariants(), "Invariants failed at step 8086"

    # Operation 8087: get
    assert tree.invariants(), "Invariants failed at step 8087"

    # Operation 8088: delete
    del tree[1125]
    del reference[1125]
    assert tree.invariants(), "Invariants failed at step 8088"

    # Operation 8089: update
    tree[1075] = 'value_389780'
    reference[1075] = 'value_389780'
    assert tree.invariants(), "Invariants failed at step 8089"

    # Operation 8090: get
    assert tree.invariants(), "Invariants failed at step 8090"

    # Operation 8091: delete
    del tree[1075]
    del reference[1075]
    assert tree.invariants(), "Invariants failed at step 8091"

    # Operation 8092: insert
    tree[3511] = 'value_894868'
    reference[3511] = 'value_894868'
    assert tree.invariants(), "Invariants failed at step 8092"

    # Operation 8093: insert
    tree[9032] = 'value_755027'
    reference[9032] = 'value_755027'
    assert tree.invariants(), "Invariants failed at step 8093"

    # Operation 8094: insert
    tree[3257] = 'value_642718'
    reference[3257] = 'value_642718'
    assert tree.invariants(), "Invariants failed at step 8094"

    # Operation 8095: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8095"

    # Operation 8096: update
    tree[3511] = 'value_80625'
    reference[3511] = 'value_80625'
    assert tree.invariants(), "Invariants failed at step 8096"

    # Operation 8097: delete
    del tree[9032]
    del reference[9032]
    assert tree.invariants(), "Invariants failed at step 8097"

    # Operation 8098: insert
    tree[3253] = 'value_148216'
    reference[3253] = 'value_148216'
    assert tree.invariants(), "Invariants failed at step 8098"

    # Operation 8099: get
    assert tree.invariants(), "Invariants failed at step 8099"

    # Operation 8100: update
    tree[3511] = 'value_580657'
    reference[3511] = 'value_580657'
    assert tree.invariants(), "Invariants failed at step 8100"

    # Operation 8101: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8101"

    # Operation 8102: delete
    del tree[3257]
    del reference[3257]
    assert tree.invariants(), "Invariants failed at step 8102"

    # Operation 8103: insert
    tree[6610] = 'value_830527'
    reference[6610] = 'value_830527'
    assert tree.invariants(), "Invariants failed at step 8103"

    # Operation 8104: delete
    del tree[3253]
    del reference[3253]
    assert tree.invariants(), "Invariants failed at step 8104"

    # Operation 8105: update
    tree[3511] = 'value_237879'
    reference[3511] = 'value_237879'
    assert tree.invariants(), "Invariants failed at step 8105"

    # Operation 8106: delete
    del tree[6610]
    del reference[6610]
    assert tree.invariants(), "Invariants failed at step 8106"

    # Operation 8107: delete
    del tree[3511]
    del reference[3511]
    assert tree.invariants(), "Invariants failed at step 8107"

    # Operation 8108: insert
    tree[2934] = 'value_142568'
    reference[2934] = 'value_142568'
    assert tree.invariants(), "Invariants failed at step 8108"

    # Operation 8109: get
    assert tree.invariants(), "Invariants failed at step 8109"

    # Operation 8110: insert
    tree[8973] = 'value_764371'
    reference[8973] = 'value_764371'
    assert tree.invariants(), "Invariants failed at step 8110"

    # Operation 8111: get
    assert tree.invariants(), "Invariants failed at step 8111"

    # Operation 8112: get
    assert tree.invariants(), "Invariants failed at step 8112"

    # Operation 8113: insert
    tree[6690] = 'value_852457'
    reference[6690] = 'value_852457'
    assert tree.invariants(), "Invariants failed at step 8113"

    # Operation 8114: delete
    del tree[8973]
    del reference[8973]
    assert tree.invariants(), "Invariants failed at step 8114"

    # Operation 8115: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8115"

    # Operation 8116: get
    assert tree.invariants(), "Invariants failed at step 8116"

    # Operation 8117: insert
    tree[1621] = 'value_125510'
    reference[1621] = 'value_125510'
    assert tree.invariants(), "Invariants failed at step 8117"

    # Operation 8118: insert
    tree[9734] = 'value_883296'
    reference[9734] = 'value_883296'
    assert tree.invariants(), "Invariants failed at step 8118"

    # Operation 8119: delete
    del tree[2934]
    del reference[2934]
    assert tree.invariants(), "Invariants failed at step 8119"

    # Operation 8120: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8120"

    # Operation 8121: delete
    del tree[6690]
    del reference[6690]
    assert tree.invariants(), "Invariants failed at step 8121"

    # Operation 8122: delete
    del tree[9734]
    del reference[9734]
    assert tree.invariants(), "Invariants failed at step 8122"

    # Operation 8123: get
    assert tree.invariants(), "Invariants failed at step 8123"

    # Operation 8124: get
    assert tree.invariants(), "Invariants failed at step 8124"

    # Operation 8125: insert
    tree[3394] = 'value_286457'
    reference[3394] = 'value_286457'
    assert tree.invariants(), "Invariants failed at step 8125"

    # Operation 8126: get
    assert tree.invariants(), "Invariants failed at step 8126"

    # Operation 8127: delete
    del tree[3394]
    del reference[3394]
    assert tree.invariants(), "Invariants failed at step 8127"

    # Operation 8128: get
    assert tree.invariants(), "Invariants failed at step 8128"

    # Operation 8129: insert
    tree[7825] = 'value_93112'
    reference[7825] = 'value_93112'
    assert tree.invariants(), "Invariants failed at step 8129"

    # Operation 8130: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8130"

    # Operation 8131: insert
    tree[887] = 'value_730735'
    reference[887] = 'value_730735'
    assert tree.invariants(), "Invariants failed at step 8131"

    # Operation 8132: update
    tree[1621] = 'value_280726'
    reference[1621] = 'value_280726'
    assert tree.invariants(), "Invariants failed at step 8132"

    # Operation 8133: insert
    tree[1136] = 'value_997123'
    reference[1136] = 'value_997123'
    assert tree.invariants(), "Invariants failed at step 8133"

    # Operation 8134: insert
    tree[8061] = 'value_10501'
    reference[8061] = 'value_10501'
    assert tree.invariants(), "Invariants failed at step 8134"

    # Operation 8135: batch_delete
    keys_to_delete = [9719, 8652, 8061, 887]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8135"

    # Operation 8136: delete
    del tree[7825]
    del reference[7825]
    assert tree.invariants(), "Invariants failed at step 8136"

    # Operation 8137: delete
    del tree[1621]
    del reference[1621]
    assert tree.invariants(), "Invariants failed at step 8137"

    # Operation 8138: insert
    tree[7701] = 'value_207480'
    reference[7701] = 'value_207480'
    assert tree.invariants(), "Invariants failed at step 8138"

    # Operation 8139: get
    assert tree.invariants(), "Invariants failed at step 8139"

    # Operation 8140: get
    assert tree.invariants(), "Invariants failed at step 8140"

    # Operation 8141: insert
    tree[2261] = 'value_447380'
    reference[2261] = 'value_447380'
    assert tree.invariants(), "Invariants failed at step 8141"

    # Operation 8142: insert
    tree[8654] = 'value_959634'
    reference[8654] = 'value_959634'
    assert tree.invariants(), "Invariants failed at step 8142"

    # Operation 8143: update
    tree[2261] = 'value_310793'
    reference[2261] = 'value_310793'
    assert tree.invariants(), "Invariants failed at step 8143"

    # Operation 8144: delete
    del tree[8654]
    del reference[8654]
    assert tree.invariants(), "Invariants failed at step 8144"

    # Operation 8145: get
    assert tree.invariants(), "Invariants failed at step 8145"

    # Operation 8146: insert
    tree[5922] = 'value_999365'
    reference[5922] = 'value_999365'
    assert tree.invariants(), "Invariants failed at step 8146"

    # Operation 8147: delete
    del tree[7701]
    del reference[7701]
    assert tree.invariants(), "Invariants failed at step 8147"

    # Operation 8148: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8148"

    # Operation 8149: get
    assert tree.invariants(), "Invariants failed at step 8149"

    # Operation 8150: get
    assert tree.invariants(), "Invariants failed at step 8150"

    # Operation 8151: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8151"

    # Operation 8152: get
    assert tree.invariants(), "Invariants failed at step 8152"

    # Operation 8153: get
    assert tree.invariants(), "Invariants failed at step 8153"

    # Operation 8154: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8154"

    # Operation 8155: get
    assert tree.invariants(), "Invariants failed at step 8155"

    # Operation 8156: insert
    tree[9067] = 'value_820084'
    reference[9067] = 'value_820084'
    assert tree.invariants(), "Invariants failed at step 8156"

    # Operation 8157: insert
    tree[3668] = 'value_441590'
    reference[3668] = 'value_441590'
    assert tree.invariants(), "Invariants failed at step 8157"

    # Operation 8158: insert
    tree[5582] = 'value_545966'
    reference[5582] = 'value_545966'
    assert tree.invariants(), "Invariants failed at step 8158"

    # Operation 8159: insert
    tree[2837] = 'value_265183'
    reference[2837] = 'value_265183'
    assert tree.invariants(), "Invariants failed at step 8159"

    # Operation 8160: get
    assert tree.invariants(), "Invariants failed at step 8160"

    # Operation 8161: batch_delete
    keys_to_delete = [5922, 866, 8683, 1136, 2837]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8161"

    # Operation 8162: insert
    tree[5767] = 'value_573615'
    reference[5767] = 'value_573615'
    assert tree.invariants(), "Invariants failed at step 8162"

    # Operation 8163: insert
    tree[3675] = 'value_110646'
    reference[3675] = 'value_110646'
    assert tree.invariants(), "Invariants failed at step 8163"

    # Operation 8164: get
    assert tree.invariants(), "Invariants failed at step 8164"

    # Operation 8165: get
    assert tree.invariants(), "Invariants failed at step 8165"

    # Operation 8166: delete
    del tree[3668]
    del reference[3668]
    assert tree.invariants(), "Invariants failed at step 8166"

    # Operation 8167: get
    assert tree.invariants(), "Invariants failed at step 8167"

    # Operation 8168: delete
    del tree[9067]
    del reference[9067]
    assert tree.invariants(), "Invariants failed at step 8168"

    # Operation 8169: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8169"

    # Operation 8170: delete
    del tree[2261]
    del reference[2261]
    assert tree.invariants(), "Invariants failed at step 8170"

    # Operation 8171: delete
    del tree[5582]
    del reference[5582]
    assert tree.invariants(), "Invariants failed at step 8171"

    # Operation 8172: update
    tree[3675] = 'value_67466'
    reference[3675] = 'value_67466'
    assert tree.invariants(), "Invariants failed at step 8172"

    # Operation 8173: insert
    tree[8848] = 'value_912990'
    reference[8848] = 'value_912990'
    assert tree.invariants(), "Invariants failed at step 8173"

    # Operation 8174: insert
    tree[3488] = 'value_91112'
    reference[3488] = 'value_91112'
    assert tree.invariants(), "Invariants failed at step 8174"

    # Operation 8175: insert
    tree[4286] = 'value_109363'
    reference[4286] = 'value_109363'
    assert tree.invariants(), "Invariants failed at step 8175"

    # Operation 8176: update
    tree[3675] = 'value_106437'
    reference[3675] = 'value_106437'
    assert tree.invariants(), "Invariants failed at step 8176"

    # Operation 8177: get
    assert tree.invariants(), "Invariants failed at step 8177"

    # Operation 8178: insert
    tree[1770] = 'value_304328'
    reference[1770] = 'value_304328'
    assert tree.invariants(), "Invariants failed at step 8178"

    # Operation 8179: insert
    tree[9831] = 'value_406670'
    reference[9831] = 'value_406670'
    assert tree.invariants(), "Invariants failed at step 8179"

    # Operation 8180: update
    tree[3675] = 'value_546406'
    reference[3675] = 'value_546406'
    assert tree.invariants(), "Invariants failed at step 8180"

    # Operation 8181: get
    assert tree.invariants(), "Invariants failed at step 8181"

    # Operation 8182: update
    tree[1770] = 'value_842537'
    reference[1770] = 'value_842537'
    assert tree.invariants(), "Invariants failed at step 8182"

    # Operation 8183: update
    tree[1770] = 'value_882050'
    reference[1770] = 'value_882050'
    assert tree.invariants(), "Invariants failed at step 8183"

    # Operation 8184: get
    assert tree.invariants(), "Invariants failed at step 8184"

    # Operation 8185: insert
    tree[7496] = 'value_81928'
    reference[7496] = 'value_81928'
    assert tree.invariants(), "Invariants failed at step 8185"

    # Operation 8186: batch_delete
    keys_to_delete = [3488, 9831, 5767, 8848, 9684, 6391]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8186"

    # Operation 8187: update
    tree[1770] = 'value_977942'
    reference[1770] = 'value_977942'
    assert tree.invariants(), "Invariants failed at step 8187"

    # Operation 8188: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8188"

    # Operation 8189: update
    tree[1770] = 'value_1728'
    reference[1770] = 'value_1728'
    assert tree.invariants(), "Invariants failed at step 8189"

    # Operation 8190: delete
    del tree[4286]
    del reference[4286]
    assert tree.invariants(), "Invariants failed at step 8190"

    # Operation 8191: get
    assert tree.invariants(), "Invariants failed at step 8191"

    # Operation 8192: get
    assert tree.invariants(), "Invariants failed at step 8192"

    # Operation 8193: delete
    del tree[7496]
    del reference[7496]
    assert tree.invariants(), "Invariants failed at step 8193"

    # Operation 8194: get
    assert tree.invariants(), "Invariants failed at step 8194"

    # Operation 8195: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8195"

    # Operation 8196: insert
    tree[5414] = 'value_870872'
    reference[5414] = 'value_870872'
    assert tree.invariants(), "Invariants failed at step 8196"

    # Operation 8197: delete
    del tree[1770]
    del reference[1770]
    assert tree.invariants(), "Invariants failed at step 8197"

    # Operation 8198: insert
    tree[2635] = 'value_723044'
    reference[2635] = 'value_723044'
    assert tree.invariants(), "Invariants failed at step 8198"

    # Operation 8199: insert
    tree[6619] = 'value_229290'
    reference[6619] = 'value_229290'
    assert tree.invariants(), "Invariants failed at step 8199"

    # Operation 8200: insert
    tree[613] = 'value_483160'
    reference[613] = 'value_483160'
    assert tree.invariants(), "Invariants failed at step 8200"

    # Operation 8201: batch_delete
    keys_to_delete = [2635, 7820, 613, 6619]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8201"

    # Operation 8202: insert
    tree[2933] = 'value_562765'
    reference[2933] = 'value_562765'
    assert tree.invariants(), "Invariants failed at step 8202"

    # Operation 8203: get
    assert tree.invariants(), "Invariants failed at step 8203"

    # Operation 8204: get
    assert tree.invariants(), "Invariants failed at step 8204"

    # Operation 8205: get
    assert tree.invariants(), "Invariants failed at step 8205"

    # Operation 8206: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8206"

    # Operation 8207: delete
    del tree[3675]
    del reference[3675]
    assert tree.invariants(), "Invariants failed at step 8207"

    # Operation 8208: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8208"

    # Operation 8209: delete
    del tree[2933]
    del reference[2933]
    assert tree.invariants(), "Invariants failed at step 8209"

    # Operation 8210: delete
    del tree[5414]
    del reference[5414]
    assert tree.invariants(), "Invariants failed at step 8210"

    # Operation 8211: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8211"

    # Operation 8212: insert
    tree[3567] = 'value_111178'
    reference[3567] = 'value_111178'
    assert tree.invariants(), "Invariants failed at step 8212"

    # Operation 8213: insert
    tree[2113] = 'value_730005'
    reference[2113] = 'value_730005'
    assert tree.invariants(), "Invariants failed at step 8213"

    # Operation 8214: delete
    del tree[2113]
    del reference[2113]
    assert tree.invariants(), "Invariants failed at step 8214"

    # Operation 8215: update
    tree[3567] = 'value_512318'
    reference[3567] = 'value_512318'
    assert tree.invariants(), "Invariants failed at step 8215"

    # Operation 8216: insert
    tree[5489] = 'value_471511'
    reference[5489] = 'value_471511'
    assert tree.invariants(), "Invariants failed at step 8216"

    # Operation 8217: insert
    tree[5707] = 'value_918443'
    reference[5707] = 'value_918443'
    assert tree.invariants(), "Invariants failed at step 8217"

    # Operation 8218: insert
    tree[3532] = 'value_594381'
    reference[3532] = 'value_594381'
    assert tree.invariants(), "Invariants failed at step 8218"

    # Operation 8219: get
    assert tree.invariants(), "Invariants failed at step 8219"

    # Operation 8220: get
    assert tree.invariants(), "Invariants failed at step 8220"

    # Operation 8221: delete
    del tree[5707]
    del reference[5707]
    assert tree.invariants(), "Invariants failed at step 8221"

    # Operation 8222: get
    assert tree.invariants(), "Invariants failed at step 8222"

    # Operation 8223: insert
    tree[4442] = 'value_233882'
    reference[4442] = 'value_233882'
    assert tree.invariants(), "Invariants failed at step 8223"

    # Operation 8224: delete
    del tree[3567]
    del reference[3567]
    assert tree.invariants(), "Invariants failed at step 8224"

    # Operation 8225: delete
    del tree[4442]
    del reference[4442]
    assert tree.invariants(), "Invariants failed at step 8225"

    # Operation 8226: update
    tree[3532] = 'value_137447'
    reference[3532] = 'value_137447'
    assert tree.invariants(), "Invariants failed at step 8226"

    # Operation 8227: insert
    tree[2123] = 'value_111605'
    reference[2123] = 'value_111605'
    assert tree.invariants(), "Invariants failed at step 8227"

    # Operation 8228: insert
    tree[6239] = 'value_466784'
    reference[6239] = 'value_466784'
    assert tree.invariants(), "Invariants failed at step 8228"

    # Operation 8229: insert
    tree[8852] = 'value_942774'
    reference[8852] = 'value_942774'
    assert tree.invariants(), "Invariants failed at step 8229"

    # Operation 8230: update
    tree[5489] = 'value_240054'
    reference[5489] = 'value_240054'
    assert tree.invariants(), "Invariants failed at step 8230"

    # Operation 8231: delete
    del tree[6239]
    del reference[6239]
    assert tree.invariants(), "Invariants failed at step 8231"

    # Operation 8232: get
    assert tree.invariants(), "Invariants failed at step 8232"

    # Operation 8233: get
    assert tree.invariants(), "Invariants failed at step 8233"

    # Operation 8234: insert
    tree[6845] = 'value_252954'
    reference[6845] = 'value_252954'
    assert tree.invariants(), "Invariants failed at step 8234"

    # Operation 8235: delete
    del tree[8852]
    del reference[8852]
    assert tree.invariants(), "Invariants failed at step 8235"

    # Operation 8236: update
    tree[3532] = 'value_451625'
    reference[3532] = 'value_451625'
    assert tree.invariants(), "Invariants failed at step 8236"

    # Operation 8237: get
    assert tree.invariants(), "Invariants failed at step 8237"

    # Operation 8238: get
    assert tree.invariants(), "Invariants failed at step 8238"

    # Operation 8239: insert
    tree[6557] = 'value_205685'
    reference[6557] = 'value_205685'
    assert tree.invariants(), "Invariants failed at step 8239"

    # Operation 8240: batch_delete
    keys_to_delete = [849, 5489, 3532, 6526]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8240"

    # Operation 8241: get
    assert tree.invariants(), "Invariants failed at step 8241"

    # Operation 8242: insert
    tree[1276] = 'value_401392'
    reference[1276] = 'value_401392'
    assert tree.invariants(), "Invariants failed at step 8242"

    # Operation 8243: insert
    tree[7944] = 'value_4564'
    reference[7944] = 'value_4564'
    assert tree.invariants(), "Invariants failed at step 8243"

    # Operation 8244: get
    assert tree.invariants(), "Invariants failed at step 8244"

    # Operation 8245: insert
    tree[8431] = 'value_624039'
    reference[8431] = 'value_624039'
    assert tree.invariants(), "Invariants failed at step 8245"

    # Operation 8246: delete
    del tree[7944]
    del reference[7944]
    assert tree.invariants(), "Invariants failed at step 8246"

    # Operation 8247: delete
    del tree[6557]
    del reference[6557]
    assert tree.invariants(), "Invariants failed at step 8247"

    # Operation 8248: delete
    del tree[8431]
    del reference[8431]
    assert tree.invariants(), "Invariants failed at step 8248"

    # Operation 8249: insert
    tree[589] = 'value_522172'
    reference[589] = 'value_522172'
    assert tree.invariants(), "Invariants failed at step 8249"

    # Operation 8250: delete
    del tree[6845]
    del reference[6845]
    assert tree.invariants(), "Invariants failed at step 8250"

    # Operation 8251: insert
    tree[3643] = 'value_181317'
    reference[3643] = 'value_181317'
    assert tree.invariants(), "Invariants failed at step 8251"

    # Operation 8252: update
    tree[3643] = 'value_23133'
    reference[3643] = 'value_23133'
    assert tree.invariants(), "Invariants failed at step 8252"

    # Operation 8253: delete
    del tree[3643]
    del reference[3643]
    assert tree.invariants(), "Invariants failed at step 8253"

    # Operation 8254: update
    tree[1276] = 'value_248872'
    reference[1276] = 'value_248872'
    assert tree.invariants(), "Invariants failed at step 8254"

    # Operation 8255: update
    tree[2123] = 'value_891358'
    reference[2123] = 'value_891358'
    assert tree.invariants(), "Invariants failed at step 8255"

    # Operation 8256: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8256"

    # Operation 8257: update
    tree[2123] = 'value_617580'
    reference[2123] = 'value_617580'
    assert tree.invariants(), "Invariants failed at step 8257"

    # Operation 8258: delete
    del tree[2123]
    del reference[2123]
    assert tree.invariants(), "Invariants failed at step 8258"

    # Operation 8259: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8259"

    # Operation 8260: get
    assert tree.invariants(), "Invariants failed at step 8260"

    # Operation 8261: delete
    del tree[589]
    del reference[589]
    assert tree.invariants(), "Invariants failed at step 8261"

    # Operation 8262: insert
    tree[8869] = 'value_927925'
    reference[8869] = 'value_927925'
    assert tree.invariants(), "Invariants failed at step 8262"

    # Operation 8263: delete
    del tree[1276]
    del reference[1276]
    assert tree.invariants(), "Invariants failed at step 8263"

    # Operation 8264: get
    assert tree.invariants(), "Invariants failed at step 8264"

    # Operation 8265: get
    assert tree.invariants(), "Invariants failed at step 8265"

    # Operation 8266: delete
    del tree[8869]
    del reference[8869]
    assert tree.invariants(), "Invariants failed at step 8266"

    # Operation 8267: insert
    tree[7620] = 'value_235679'
    reference[7620] = 'value_235679'
    assert tree.invariants(), "Invariants failed at step 8267"

    # Operation 8268: insert
    tree[4960] = 'value_788430'
    reference[4960] = 'value_788430'
    assert tree.invariants(), "Invariants failed at step 8268"

    # Operation 8269: insert
    tree[2648] = 'value_633732'
    reference[2648] = 'value_633732'
    assert tree.invariants(), "Invariants failed at step 8269"

    # Operation 8270: delete
    del tree[7620]
    del reference[7620]
    assert tree.invariants(), "Invariants failed at step 8270"

    # Operation 8271: update
    tree[4960] = 'value_290945'
    reference[4960] = 'value_290945'
    assert tree.invariants(), "Invariants failed at step 8271"

    # Operation 8272: get
    assert tree.invariants(), "Invariants failed at step 8272"

    # Operation 8273: insert
    tree[6730] = 'value_936540'
    reference[6730] = 'value_936540'
    assert tree.invariants(), "Invariants failed at step 8273"

    # Operation 8274: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8274"

    # Operation 8275: insert
    tree[4108] = 'value_618630'
    reference[4108] = 'value_618630'
    assert tree.invariants(), "Invariants failed at step 8275"

    # Operation 8276: get
    assert tree.invariants(), "Invariants failed at step 8276"

    # Operation 8277: insert
    tree[3924] = 'value_274723'
    reference[3924] = 'value_274723'
    assert tree.invariants(), "Invariants failed at step 8277"

    # Operation 8278: insert
    tree[1444] = 'value_831842'
    reference[1444] = 'value_831842'
    assert tree.invariants(), "Invariants failed at step 8278"

    # Operation 8279: insert
    tree[8717] = 'value_239975'
    reference[8717] = 'value_239975'
    assert tree.invariants(), "Invariants failed at step 8279"

    # Operation 8280: batch_delete
    keys_to_delete = [8521, 5129, 6730, 4108, 8717]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8280"

    # Operation 8281: delete
    del tree[1444]
    del reference[1444]
    assert tree.invariants(), "Invariants failed at step 8281"

    # Operation 8282: insert
    tree[8690] = 'value_32003'
    reference[8690] = 'value_32003'
    assert tree.invariants(), "Invariants failed at step 8282"

    # Operation 8283: update
    tree[2648] = 'value_513343'
    reference[2648] = 'value_513343'
    assert tree.invariants(), "Invariants failed at step 8283"

    # Operation 8284: insert
    tree[4189] = 'value_152659'
    reference[4189] = 'value_152659'
    assert tree.invariants(), "Invariants failed at step 8284"

    # Operation 8285: insert
    tree[3371] = 'value_459482'
    reference[3371] = 'value_459482'
    assert tree.invariants(), "Invariants failed at step 8285"

    # Operation 8286: delete
    del tree[3371]
    del reference[3371]
    assert tree.invariants(), "Invariants failed at step 8286"

    # Operation 8287: get
    assert tree.invariants(), "Invariants failed at step 8287"

    # Operation 8288: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8288"

    # Operation 8289: insert
    tree[8054] = 'value_82082'
    reference[8054] = 'value_82082'
    assert tree.invariants(), "Invariants failed at step 8289"

    # Operation 8290: delete
    del tree[3924]
    del reference[3924]
    assert tree.invariants(), "Invariants failed at step 8290"

    # Operation 8291: get
    assert tree.invariants(), "Invariants failed at step 8291"

    # Operation 8292: get
    assert tree.invariants(), "Invariants failed at step 8292"

    # Operation 8293: insert
    tree[2248] = 'value_763469'
    reference[2248] = 'value_763469'
    assert tree.invariants(), "Invariants failed at step 8293"

    # Operation 8294: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8294"

    # Operation 8295: insert
    tree[5817] = 'value_56057'
    reference[5817] = 'value_56057'
    assert tree.invariants(), "Invariants failed at step 8295"

    # Operation 8296: delete
    del tree[8690]
    del reference[8690]
    assert tree.invariants(), "Invariants failed at step 8296"

    # Operation 8297: update
    tree[2248] = 'value_38721'
    reference[2248] = 'value_38721'
    assert tree.invariants(), "Invariants failed at step 8297"

    # Operation 8298: delete
    del tree[4189]
    del reference[4189]
    assert tree.invariants(), "Invariants failed at step 8298"

    # Operation 8299: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8299"

    # Operation 8300: batch_delete
    keys_to_delete = [5817, 506, 1398, 8054]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8300"

    # Operation 8301: get
    assert tree.invariants(), "Invariants failed at step 8301"

    # Operation 8302: get
    assert tree.invariants(), "Invariants failed at step 8302"

    # Operation 8303: insert
    tree[2159] = 'value_550629'
    reference[2159] = 'value_550629'
    assert tree.invariants(), "Invariants failed at step 8303"

    # Operation 8304: insert
    tree[5290] = 'value_987766'
    reference[5290] = 'value_987766'
    assert tree.invariants(), "Invariants failed at step 8304"

    # Operation 8305: delete
    del tree[4960]
    del reference[4960]
    assert tree.invariants(), "Invariants failed at step 8305"

    # Operation 8306: insert
    tree[9729] = 'value_733123'
    reference[9729] = 'value_733123'
    assert tree.invariants(), "Invariants failed at step 8306"

    # Operation 8307: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8307"

    # Operation 8308: batch_delete
    keys_to_delete = [2439, 9729, 8361, 2159]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8308"

    # Operation 8309: insert
    tree[7511] = 'value_916759'
    reference[7511] = 'value_916759'
    assert tree.invariants(), "Invariants failed at step 8309"

    # Operation 8310: insert
    tree[1301] = 'value_829117'
    reference[1301] = 'value_829117'
    assert tree.invariants(), "Invariants failed at step 8310"

    # Operation 8311: delete
    del tree[5290]
    del reference[5290]
    assert tree.invariants(), "Invariants failed at step 8311"

    # Operation 8312: get
    assert tree.invariants(), "Invariants failed at step 8312"

    # Operation 8313: update
    tree[2248] = 'value_818670'
    reference[2248] = 'value_818670'
    assert tree.invariants(), "Invariants failed at step 8313"

    # Operation 8314: delete
    del tree[1301]
    del reference[1301]
    assert tree.invariants(), "Invariants failed at step 8314"

    # Operation 8315: insert
    tree[5439] = 'value_460337'
    reference[5439] = 'value_460337'
    assert tree.invariants(), "Invariants failed at step 8315"

    # Operation 8316: delete
    del tree[7511]
    del reference[7511]
    assert tree.invariants(), "Invariants failed at step 8316"

    # Operation 8317: update
    tree[5439] = 'value_369161'
    reference[5439] = 'value_369161'
    assert tree.invariants(), "Invariants failed at step 8317"

    # Operation 8318: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8318"

    # Operation 8319: get
    assert tree.invariants(), "Invariants failed at step 8319"

    # Operation 8320: delete
    del tree[2248]
    del reference[2248]
    assert tree.invariants(), "Invariants failed at step 8320"

    # Operation 8321: insert
    tree[4844] = 'value_342772'
    reference[4844] = 'value_342772'
    assert tree.invariants(), "Invariants failed at step 8321"

    # Operation 8322: get
    assert tree.invariants(), "Invariants failed at step 8322"

    # Operation 8323: delete
    del tree[4844]
    del reference[4844]
    assert tree.invariants(), "Invariants failed at step 8323"

    # Operation 8324: get
    assert tree.invariants(), "Invariants failed at step 8324"

    # Operation 8325: delete
    del tree[2648]
    del reference[2648]
    assert tree.invariants(), "Invariants failed at step 8325"

    # Operation 8326: update
    tree[5439] = 'value_817205'
    reference[5439] = 'value_817205'
    assert tree.invariants(), "Invariants failed at step 8326"

    # Operation 8327: insert
    tree[4888] = 'value_327611'
    reference[4888] = 'value_327611'
    assert tree.invariants(), "Invariants failed at step 8327"

    # Operation 8328: insert
    tree[9586] = 'value_641373'
    reference[9586] = 'value_641373'
    assert tree.invariants(), "Invariants failed at step 8328"

    # Operation 8329: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8329"

    # Operation 8330: update
    tree[9586] = 'value_825765'
    reference[9586] = 'value_825765'
    assert tree.invariants(), "Invariants failed at step 8330"

    # Operation 8331: insert
    tree[8873] = 'value_592012'
    reference[8873] = 'value_592012'
    assert tree.invariants(), "Invariants failed at step 8331"

    # Operation 8332: delete
    del tree[9586]
    del reference[9586]
    assert tree.invariants(), "Invariants failed at step 8332"

    # Operation 8333: update
    tree[4888] = 'value_493072'
    reference[4888] = 'value_493072'
    assert tree.invariants(), "Invariants failed at step 8333"

    # Operation 8334: insert
    tree[3640] = 'value_235177'
    reference[3640] = 'value_235177'
    assert tree.invariants(), "Invariants failed at step 8334"

    # Operation 8335: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8335"

    # Operation 8336: delete
    del tree[8873]
    del reference[8873]
    assert tree.invariants(), "Invariants failed at step 8336"

    # Operation 8337: delete
    del tree[4888]
    del reference[4888]
    assert tree.invariants(), "Invariants failed at step 8337"

    # Operation 8338: insert
    tree[238] = 'value_495997'
    reference[238] = 'value_495997'
    assert tree.invariants(), "Invariants failed at step 8338"

    # Operation 8339: insert
    tree[4734] = 'value_188473'
    reference[4734] = 'value_188473'
    assert tree.invariants(), "Invariants failed at step 8339"

    # Operation 8340: get
    assert tree.invariants(), "Invariants failed at step 8340"

    # Operation 8341: delete
    del tree[5439]
    del reference[5439]
    assert tree.invariants(), "Invariants failed at step 8341"

    # Operation 8342: get
    assert tree.invariants(), "Invariants failed at step 8342"

    # Operation 8343: delete
    del tree[3640]
    del reference[3640]
    assert tree.invariants(), "Invariants failed at step 8343"

    # Operation 8344: delete
    del tree[4734]
    del reference[4734]
    assert tree.invariants(), "Invariants failed at step 8344"

    # Operation 8345: update
    tree[238] = 'value_333653'
    reference[238] = 'value_333653'
    assert tree.invariants(), "Invariants failed at step 8345"

    # Operation 8346: delete
    del tree[238]
    del reference[238]
    assert tree.invariants(), "Invariants failed at step 8346"

    # Operation 8347: get
    assert tree.invariants(), "Invariants failed at step 8347"

    # Operation 8348: get
    assert tree.invariants(), "Invariants failed at step 8348"

    # Operation 8349: insert
    tree[2233] = 'value_300561'
    reference[2233] = 'value_300561'
    assert tree.invariants(), "Invariants failed at step 8349"

    # Operation 8350: get
    assert tree.invariants(), "Invariants failed at step 8350"

    # Operation 8351: get
    assert tree.invariants(), "Invariants failed at step 8351"

    # Operation 8352: delete
    del tree[2233]
    del reference[2233]
    assert tree.invariants(), "Invariants failed at step 8352"

    # Operation 8353: get
    assert tree.invariants(), "Invariants failed at step 8353"

    # Operation 8354: insert
    tree[1306] = 'value_457026'
    reference[1306] = 'value_457026'
    assert tree.invariants(), "Invariants failed at step 8354"

    # Operation 8355: insert
    tree[2545] = 'value_373551'
    reference[2545] = 'value_373551'
    assert tree.invariants(), "Invariants failed at step 8355"

    # Operation 8356: get
    assert tree.invariants(), "Invariants failed at step 8356"

    # Operation 8357: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8357"

    # Operation 8358: delete
    del tree[2545]
    del reference[2545]
    assert tree.invariants(), "Invariants failed at step 8358"

    # Operation 8359: insert
    tree[485] = 'value_460850'
    reference[485] = 'value_460850'
    assert tree.invariants(), "Invariants failed at step 8359"

    # Operation 8360: insert
    tree[9958] = 'value_938779'
    reference[9958] = 'value_938779'
    assert tree.invariants(), "Invariants failed at step 8360"

    # Operation 8361: delete
    del tree[9958]
    del reference[9958]
    assert tree.invariants(), "Invariants failed at step 8361"

    # Operation 8362: insert
    tree[5713] = 'value_262393'
    reference[5713] = 'value_262393'
    assert tree.invariants(), "Invariants failed at step 8362"

    # Operation 8363: get
    assert tree.invariants(), "Invariants failed at step 8363"

    # Operation 8364: insert
    tree[8628] = 'value_290060'
    reference[8628] = 'value_290060'
    assert tree.invariants(), "Invariants failed at step 8364"

    # Operation 8365: insert
    tree[2690] = 'value_165234'
    reference[2690] = 'value_165234'
    assert tree.invariants(), "Invariants failed at step 8365"

    # Operation 8366: get
    assert tree.invariants(), "Invariants failed at step 8366"

    # Operation 8367: delete
    del tree[8628]
    del reference[8628]
    assert tree.invariants(), "Invariants failed at step 8367"

    # Operation 8368: insert
    tree[6358] = 'value_530297'
    reference[6358] = 'value_530297'
    assert tree.invariants(), "Invariants failed at step 8368"

    # Operation 8369: get
    assert tree.invariants(), "Invariants failed at step 8369"

    # Operation 8370: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8370"

    # Operation 8371: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8371"

    # Operation 8372: get
    assert tree.invariants(), "Invariants failed at step 8372"

    # Operation 8373: get
    assert tree.invariants(), "Invariants failed at step 8373"

    # Operation 8374: delete
    del tree[5713]
    del reference[5713]
    assert tree.invariants(), "Invariants failed at step 8374"

    # Operation 8375: get
    assert tree.invariants(), "Invariants failed at step 8375"

    # Operation 8376: get
    assert tree.invariants(), "Invariants failed at step 8376"

    # Operation 8377: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8377"

    # Operation 8378: get
    assert tree.invariants(), "Invariants failed at step 8378"

    # Operation 8379: update
    tree[485] = 'value_348308'
    reference[485] = 'value_348308'
    assert tree.invariants(), "Invariants failed at step 8379"

    # Operation 8380: insert
    tree[3106] = 'value_92120'
    reference[3106] = 'value_92120'
    assert tree.invariants(), "Invariants failed at step 8380"

    # Operation 8381: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8381"

    # Operation 8382: insert
    tree[4968] = 'value_624067'
    reference[4968] = 'value_624067'
    assert tree.invariants(), "Invariants failed at step 8382"

    # Operation 8383: insert
    tree[2075] = 'value_498580'
    reference[2075] = 'value_498580'
    assert tree.invariants(), "Invariants failed at step 8383"

    # Operation 8384: delete
    del tree[1306]
    del reference[1306]
    assert tree.invariants(), "Invariants failed at step 8384"

    # Operation 8385: insert
    tree[3770] = 'value_906651'
    reference[3770] = 'value_906651'
    assert tree.invariants(), "Invariants failed at step 8385"

    # Operation 8386: delete
    del tree[3106]
    del reference[3106]
    assert tree.invariants(), "Invariants failed at step 8386"

    # Operation 8387: insert
    tree[3900] = 'value_1766'
    reference[3900] = 'value_1766'
    assert tree.invariants(), "Invariants failed at step 8387"

    # Operation 8388: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8388"

    # Operation 8389: insert
    tree[6254] = 'value_738865'
    reference[6254] = 'value_738865'
    assert tree.invariants(), "Invariants failed at step 8389"

    # Operation 8390: delete
    del tree[485]
    del reference[485]
    assert tree.invariants(), "Invariants failed at step 8390"

    # Operation 8391: update
    tree[2690] = 'value_688666'
    reference[2690] = 'value_688666'
    assert tree.invariants(), "Invariants failed at step 8391"

    # Operation 8392: insert
    tree[8830] = 'value_875931'
    reference[8830] = 'value_875931'
    assert tree.invariants(), "Invariants failed at step 8392"

    # Operation 8393: delete
    del tree[6254]
    del reference[6254]
    assert tree.invariants(), "Invariants failed at step 8393"

    # Operation 8394: batch_delete
    keys_to_delete = [4549, 6358, 4343, 3770, 3900]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8394"

    # Operation 8395: update
    tree[4968] = 'value_549388'
    reference[4968] = 'value_549388'
    assert tree.invariants(), "Invariants failed at step 8395"

    # Operation 8396: get
    assert tree.invariants(), "Invariants failed at step 8396"

    # Operation 8397: update
    tree[2075] = 'value_613326'
    reference[2075] = 'value_613326'
    assert tree.invariants(), "Invariants failed at step 8397"

    # Operation 8398: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8398"

    # Operation 8399: update
    tree[2075] = 'value_40710'
    reference[2075] = 'value_40710'
    assert tree.invariants(), "Invariants failed at step 8399"

    # Operation 8400: insert
    tree[4839] = 'value_457965'
    reference[4839] = 'value_457965'
    assert tree.invariants(), "Invariants failed at step 8400"

    # Operation 8401: delete
    del tree[4839]
    del reference[4839]
    assert tree.invariants(), "Invariants failed at step 8401"

    # Operation 8402: get
    assert tree.invariants(), "Invariants failed at step 8402"

    # Operation 8403: get
    assert tree.invariants(), "Invariants failed at step 8403"

    # Operation 8404: insert
    tree[6115] = 'value_829352'
    reference[6115] = 'value_829352'
    assert tree.invariants(), "Invariants failed at step 8404"

    # Operation 8405: insert
    tree[6932] = 'value_701807'
    reference[6932] = 'value_701807'
    assert tree.invariants(), "Invariants failed at step 8405"

    # Operation 8406: insert
    tree[3176] = 'value_682704'
    reference[3176] = 'value_682704'
    assert tree.invariants(), "Invariants failed at step 8406"

    # Operation 8407: update
    tree[8830] = 'value_704118'
    reference[8830] = 'value_704118'
    assert tree.invariants(), "Invariants failed at step 8407"

    # Operation 8408: insert
    tree[5066] = 'value_5761'
    reference[5066] = 'value_5761'
    assert tree.invariants(), "Invariants failed at step 8408"

    # Operation 8409: get
    assert tree.invariants(), "Invariants failed at step 8409"

    # Operation 8410: get
    assert tree.invariants(), "Invariants failed at step 8410"

    # Operation 8411: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8411"

    # Operation 8412: get
    assert tree.invariants(), "Invariants failed at step 8412"

    # Operation 8413: get
    assert tree.invariants(), "Invariants failed at step 8413"

    # Operation 8414: get
    assert tree.invariants(), "Invariants failed at step 8414"

    # Operation 8415: batch_delete
    keys_to_delete = [6115, 4968, 4237, 6932, 8830]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8415"

    # Operation 8416: delete
    del tree[2075]
    del reference[2075]
    assert tree.invariants(), "Invariants failed at step 8416"

    # Operation 8417: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8417"

    # Operation 8418: get
    assert tree.invariants(), "Invariants failed at step 8418"

    # Operation 8419: insert
    tree[7118] = 'value_308694'
    reference[7118] = 'value_308694'
    assert tree.invariants(), "Invariants failed at step 8419"

    # Operation 8420: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8420"

    # Operation 8421: insert
    tree[9786] = 'value_638207'
    reference[9786] = 'value_638207'
    assert tree.invariants(), "Invariants failed at step 8421"

    # Operation 8422: delete
    del tree[2690]
    del reference[2690]
    assert tree.invariants(), "Invariants failed at step 8422"

    # Operation 8423: insert
    tree[5261] = 'value_192321'
    reference[5261] = 'value_192321'
    assert tree.invariants(), "Invariants failed at step 8423"

    # Operation 8424: insert
    tree[563] = 'value_134296'
    reference[563] = 'value_134296'
    assert tree.invariants(), "Invariants failed at step 8424"

    # Operation 8425: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8425"

    # Operation 8426: get
    assert tree.invariants(), "Invariants failed at step 8426"

    # Operation 8427: insert
    tree[7786] = 'value_98394'
    reference[7786] = 'value_98394'
    assert tree.invariants(), "Invariants failed at step 8427"

    # Operation 8428: get
    assert tree.invariants(), "Invariants failed at step 8428"

    # Operation 8429: delete
    del tree[563]
    del reference[563]
    assert tree.invariants(), "Invariants failed at step 8429"

    # Operation 8430: insert
    tree[3254] = 'value_24172'
    reference[3254] = 'value_24172'
    assert tree.invariants(), "Invariants failed at step 8430"

    # Operation 8431: insert
    tree[2496] = 'value_802554'
    reference[2496] = 'value_802554'
    assert tree.invariants(), "Invariants failed at step 8431"

    # Operation 8432: batch_delete
    keys_to_delete = [6596, 6823, 5066, 7786, 7118]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8432"

    # Operation 8433: insert
    tree[7861] = 'value_617688'
    reference[7861] = 'value_617688'
    assert tree.invariants(), "Invariants failed at step 8433"

    # Operation 8434: get
    assert tree.invariants(), "Invariants failed at step 8434"

    # Operation 8435: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8435"

    # Operation 8436: get
    assert tree.invariants(), "Invariants failed at step 8436"

    # Operation 8437: delete
    del tree[2496]
    del reference[2496]
    assert tree.invariants(), "Invariants failed at step 8437"

    # Operation 8438: get
    assert tree.invariants(), "Invariants failed at step 8438"

    # Operation 8439: update
    tree[3254] = 'value_37026'
    reference[3254] = 'value_37026'
    assert tree.invariants(), "Invariants failed at step 8439"

    # Operation 8440: update
    tree[7861] = 'value_695303'
    reference[7861] = 'value_695303'
    assert tree.invariants(), "Invariants failed at step 8440"

    # Operation 8441: get
    assert tree.invariants(), "Invariants failed at step 8441"

    # Operation 8442: update
    tree[5261] = 'value_30400'
    reference[5261] = 'value_30400'
    assert tree.invariants(), "Invariants failed at step 8442"

    # Operation 8443: update
    tree[9786] = 'value_307763'
    reference[9786] = 'value_307763'
    assert tree.invariants(), "Invariants failed at step 8443"

    # Operation 8444: insert
    tree[5290] = 'value_410'
    reference[5290] = 'value_410'
    assert tree.invariants(), "Invariants failed at step 8444"

    # Operation 8445: delete
    del tree[5290]
    del reference[5290]
    assert tree.invariants(), "Invariants failed at step 8445"

    # Operation 8446: insert
    tree[1163] = 'value_137286'
    reference[1163] = 'value_137286'
    assert tree.invariants(), "Invariants failed at step 8446"

    # Operation 8447: update
    tree[5261] = 'value_903365'
    reference[5261] = 'value_903365'
    assert tree.invariants(), "Invariants failed at step 8447"

    # Operation 8448: insert
    tree[1581] = 'value_375639'
    reference[1581] = 'value_375639'
    assert tree.invariants(), "Invariants failed at step 8448"

    # Operation 8449: get
    assert tree.invariants(), "Invariants failed at step 8449"

    # Operation 8450: update
    tree[5261] = 'value_685605'
    reference[5261] = 'value_685605'
    assert tree.invariants(), "Invariants failed at step 8450"

    # Operation 8451: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8451"

    # Operation 8452: insert
    tree[3010] = 'value_813681'
    reference[3010] = 'value_813681'
    assert tree.invariants(), "Invariants failed at step 8452"

    # Operation 8453: update
    tree[1163] = 'value_754556'
    reference[1163] = 'value_754556'
    assert tree.invariants(), "Invariants failed at step 8453"

    # Operation 8454: insert
    tree[6596] = 'value_992957'
    reference[6596] = 'value_992957'
    assert tree.invariants(), "Invariants failed at step 8454"

    # Operation 8455: get
    assert tree.invariants(), "Invariants failed at step 8455"

    # Operation 8456: insert
    tree[906] = 'value_127485'
    reference[906] = 'value_127485'
    assert tree.invariants(), "Invariants failed at step 8456"

    # Operation 8457: batch_delete
    keys_to_delete = [3176, 8809, 906, 5261, 1655]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8457"

    # Operation 8458: delete
    del tree[3254]
    del reference[3254]
    assert tree.invariants(), "Invariants failed at step 8458"

    # Operation 8459: delete
    del tree[3010]
    del reference[3010]
    assert tree.invariants(), "Invariants failed at step 8459"

    # Operation 8460: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8460"

    # Operation 8461: batch_delete
    keys_to_delete = [8729, 8986, 1163, 1581]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8461"

    # Operation 8462: delete
    del tree[6596]
    del reference[6596]
    assert tree.invariants(), "Invariants failed at step 8462"

    # Operation 8463: delete
    del tree[7861]
    del reference[7861]
    assert tree.invariants(), "Invariants failed at step 8463"

    # Operation 8464: update
    tree[9786] = 'value_453205'
    reference[9786] = 'value_453205'
    assert tree.invariants(), "Invariants failed at step 8464"

    # Operation 8465: delete
    del tree[9786]
    del reference[9786]
    assert tree.invariants(), "Invariants failed at step 8465"

    # Operation 8466: get
    assert tree.invariants(), "Invariants failed at step 8466"

    # Operation 8467: insert
    tree[3457] = 'value_404126'
    reference[3457] = 'value_404126'
    assert tree.invariants(), "Invariants failed at step 8467"

    # Operation 8468: get
    assert tree.invariants(), "Invariants failed at step 8468"

    # Operation 8469: insert
    tree[4607] = 'value_149169'
    reference[4607] = 'value_149169'
    assert tree.invariants(), "Invariants failed at step 8469"

    # Operation 8470: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8470"

    # Operation 8471: delete
    del tree[3457]
    del reference[3457]
    assert tree.invariants(), "Invariants failed at step 8471"

    # Operation 8472: insert
    tree[8916] = 'value_664539'
    reference[8916] = 'value_664539'
    assert tree.invariants(), "Invariants failed at step 8472"

    # Operation 8473: update
    tree[8916] = 'value_47901'
    reference[8916] = 'value_47901'
    assert tree.invariants(), "Invariants failed at step 8473"

    # Operation 8474: insert
    tree[9309] = 'value_765260'
    reference[9309] = 'value_765260'
    assert tree.invariants(), "Invariants failed at step 8474"

    # Operation 8475: insert
    tree[7414] = 'value_349295'
    reference[7414] = 'value_349295'
    assert tree.invariants(), "Invariants failed at step 8475"

    # Operation 8476: insert
    tree[9783] = 'value_489271'
    reference[9783] = 'value_489271'
    assert tree.invariants(), "Invariants failed at step 8476"

    # Operation 8477: insert
    tree[5909] = 'value_343882'
    reference[5909] = 'value_343882'
    assert tree.invariants(), "Invariants failed at step 8477"

    # Operation 8478: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8478"

    # Operation 8479: insert
    tree[5432] = 'value_27147'
    reference[5432] = 'value_27147'
    assert tree.invariants(), "Invariants failed at step 8479"

    # Operation 8480: batch_delete
    keys_to_delete = [5901, 8916, 5494, 7414, 4607]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8480"

    # Operation 8481: get
    assert tree.invariants(), "Invariants failed at step 8481"

    # Operation 8482: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8482"

    # Operation 8483: update
    tree[9309] = 'value_36908'
    reference[9309] = 'value_36908'
    assert tree.invariants(), "Invariants failed at step 8483"

    # Operation 8484: update
    tree[9309] = 'value_647177'
    reference[9309] = 'value_647177'
    assert tree.invariants(), "Invariants failed at step 8484"

    # Operation 8485: get
    assert tree.invariants(), "Invariants failed at step 8485"

    # Operation 8486: delete
    del tree[5432]
    del reference[5432]
    assert tree.invariants(), "Invariants failed at step 8486"

    # Operation 8487: delete
    del tree[9783]
    del reference[9783]
    assert tree.invariants(), "Invariants failed at step 8487"

    # Operation 8488: get
    assert tree.invariants(), "Invariants failed at step 8488"

    # Operation 8489: insert
    tree[5279] = 'value_856343'
    reference[5279] = 'value_856343'
    assert tree.invariants(), "Invariants failed at step 8489"

    # Operation 8490: insert
    tree[215] = 'value_551580'
    reference[215] = 'value_551580'
    assert tree.invariants(), "Invariants failed at step 8490"

    # Operation 8491: insert
    tree[3416] = 'value_8185'
    reference[3416] = 'value_8185'
    assert tree.invariants(), "Invariants failed at step 8491"

    # Operation 8492: delete
    del tree[3416]
    del reference[3416]
    assert tree.invariants(), "Invariants failed at step 8492"

    # Operation 8493: insert
    tree[5490] = 'value_669463'
    reference[5490] = 'value_669463'
    assert tree.invariants(), "Invariants failed at step 8493"

    # Operation 8494: get
    assert tree.invariants(), "Invariants failed at step 8494"

    # Operation 8495: get
    assert tree.invariants(), "Invariants failed at step 8495"

    # Operation 8496: delete
    del tree[5909]
    del reference[5909]
    assert tree.invariants(), "Invariants failed at step 8496"

    # Operation 8497: get
    assert tree.invariants(), "Invariants failed at step 8497"

    # Operation 8498: delete
    del tree[9309]
    del reference[9309]
    assert tree.invariants(), "Invariants failed at step 8498"

    # Operation 8499: insert
    tree[5171] = 'value_460631'
    reference[5171] = 'value_460631'
    assert tree.invariants(), "Invariants failed at step 8499"

    # Operation 8500: update
    tree[5279] = 'value_211390'
    reference[5279] = 'value_211390'
    assert tree.invariants(), "Invariants failed at step 8500"

    # Operation 8501: insert
    tree[1729] = 'value_958237'
    reference[1729] = 'value_958237'
    assert tree.invariants(), "Invariants failed at step 8501"

    # Operation 8502: get
    assert tree.invariants(), "Invariants failed at step 8502"

    # Operation 8503: get
    assert tree.invariants(), "Invariants failed at step 8503"

    # Operation 8504: delete
    del tree[5279]
    del reference[5279]
    assert tree.invariants(), "Invariants failed at step 8504"

    # Operation 8505: delete
    del tree[215]
    del reference[215]
    assert tree.invariants(), "Invariants failed at step 8505"

    # Operation 8506: get
    assert tree.invariants(), "Invariants failed at step 8506"

    # Operation 8507: update
    tree[1729] = 'value_450437'
    reference[1729] = 'value_450437'
    assert tree.invariants(), "Invariants failed at step 8507"

    # Operation 8508: insert
    tree[3734] = 'value_150171'
    reference[3734] = 'value_150171'
    assert tree.invariants(), "Invariants failed at step 8508"

    # Operation 8509: update
    tree[5171] = 'value_75328'
    reference[5171] = 'value_75328'
    assert tree.invariants(), "Invariants failed at step 8509"

    # Operation 8510: update
    tree[3734] = 'value_33231'
    reference[3734] = 'value_33231'
    assert tree.invariants(), "Invariants failed at step 8510"

    # Operation 8511: insert
    tree[5148] = 'value_838388'
    reference[5148] = 'value_838388'
    assert tree.invariants(), "Invariants failed at step 8511"

    # Operation 8512: insert
    tree[6050] = 'value_318405'
    reference[6050] = 'value_318405'
    assert tree.invariants(), "Invariants failed at step 8512"

    # Operation 8513: delete
    del tree[5171]
    del reference[5171]
    assert tree.invariants(), "Invariants failed at step 8513"

    # Operation 8514: get
    assert tree.invariants(), "Invariants failed at step 8514"

    # Operation 8515: get
    assert tree.invariants(), "Invariants failed at step 8515"

    # Operation 8516: insert
    tree[3893] = 'value_41716'
    reference[3893] = 'value_41716'
    assert tree.invariants(), "Invariants failed at step 8516"

    # Operation 8517: get
    assert tree.invariants(), "Invariants failed at step 8517"

    # Operation 8518: get
    assert tree.invariants(), "Invariants failed at step 8518"

    # Operation 8519: delete
    del tree[6050]
    del reference[6050]
    assert tree.invariants(), "Invariants failed at step 8519"

    # Operation 8520: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8520"

    # Operation 8521: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8521"

    # Operation 8522: batch_delete
    keys_to_delete = [5490, 1035, 3893, 8046]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8522"

    # Operation 8523: insert
    tree[6694] = 'value_704791'
    reference[6694] = 'value_704791'
    assert tree.invariants(), "Invariants failed at step 8523"

    # Operation 8524: delete
    del tree[1729]
    del reference[1729]
    assert tree.invariants(), "Invariants failed at step 8524"

    # Operation 8525: delete
    del tree[3734]
    del reference[3734]
    assert tree.invariants(), "Invariants failed at step 8525"

    # Operation 8526: insert
    tree[4150] = 'value_601634'
    reference[4150] = 'value_601634'
    assert tree.invariants(), "Invariants failed at step 8526"

    # Operation 8527: get
    assert tree.invariants(), "Invariants failed at step 8527"

    # Operation 8528: delete
    del tree[5148]
    del reference[5148]
    assert tree.invariants(), "Invariants failed at step 8528"

    # Operation 8529: insert
    tree[9272] = 'value_755678'
    reference[9272] = 'value_755678'
    assert tree.invariants(), "Invariants failed at step 8529"

    # Operation 8530: insert
    tree[9654] = 'value_332565'
    reference[9654] = 'value_332565'
    assert tree.invariants(), "Invariants failed at step 8530"

    # Operation 8531: insert
    tree[3643] = 'value_533625'
    reference[3643] = 'value_533625'
    assert tree.invariants(), "Invariants failed at step 8531"

    # Operation 8532: delete
    del tree[9272]
    del reference[9272]
    assert tree.invariants(), "Invariants failed at step 8532"

    # Operation 8533: insert
    tree[6263] = 'value_145214'
    reference[6263] = 'value_145214'
    assert tree.invariants(), "Invariants failed at step 8533"

    # Operation 8534: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8534"

    # Operation 8535: insert
    tree[1269] = 'value_744686'
    reference[1269] = 'value_744686'
    assert tree.invariants(), "Invariants failed at step 8535"

    # Operation 8536: update
    tree[3643] = 'value_491063'
    reference[3643] = 'value_491063'
    assert tree.invariants(), "Invariants failed at step 8536"

    # Operation 8537: delete
    del tree[6263]
    del reference[6263]
    assert tree.invariants(), "Invariants failed at step 8537"

    # Operation 8538: batch_delete
    keys_to_delete = [1770, 6674, 1269, 4150]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8538"

    # Operation 8539: delete
    del tree[3643]
    del reference[3643]
    assert tree.invariants(), "Invariants failed at step 8539"

    # Operation 8540: insert
    tree[4480] = 'value_688110'
    reference[4480] = 'value_688110'
    assert tree.invariants(), "Invariants failed at step 8540"

    # Operation 8541: delete
    del tree[9654]
    del reference[9654]
    assert tree.invariants(), "Invariants failed at step 8541"

    # Operation 8542: get
    assert tree.invariants(), "Invariants failed at step 8542"

    # Operation 8543: get
    assert tree.invariants(), "Invariants failed at step 8543"

    # Operation 8544: insert
    tree[3579] = 'value_640196'
    reference[3579] = 'value_640196'
    assert tree.invariants(), "Invariants failed at step 8544"

    # Operation 8545: get
    assert tree.invariants(), "Invariants failed at step 8545"

    # Operation 8546: delete
    del tree[6694]
    del reference[6694]
    assert tree.invariants(), "Invariants failed at step 8546"

    # Operation 8547: delete
    del tree[3579]
    del reference[3579]
    assert tree.invariants(), "Invariants failed at step 8547"

    # Operation 8548: insert
    tree[6542] = 'value_448876'
    reference[6542] = 'value_448876'
    assert tree.invariants(), "Invariants failed at step 8548"

    # Operation 8549: delete
    del tree[4480]
    del reference[4480]
    assert tree.invariants(), "Invariants failed at step 8549"

    # Operation 8550: insert
    tree[2589] = 'value_262866'
    reference[2589] = 'value_262866'
    assert tree.invariants(), "Invariants failed at step 8550"

    # Operation 8551: get
    assert tree.invariants(), "Invariants failed at step 8551"

    # Operation 8552: delete
    del tree[6542]
    del reference[6542]
    assert tree.invariants(), "Invariants failed at step 8552"

    # Operation 8553: insert
    tree[9682] = 'value_800577'
    reference[9682] = 'value_800577'
    assert tree.invariants(), "Invariants failed at step 8553"

    # Operation 8554: insert
    tree[4497] = 'value_27849'
    reference[4497] = 'value_27849'
    assert tree.invariants(), "Invariants failed at step 8554"

    # Operation 8555: delete
    del tree[4497]
    del reference[4497]
    assert tree.invariants(), "Invariants failed at step 8555"

    # Operation 8556: get
    assert tree.invariants(), "Invariants failed at step 8556"

    # Operation 8557: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8557"

    # Operation 8558: get
    assert tree.invariants(), "Invariants failed at step 8558"

    # Operation 8559: insert
    tree[9123] = 'value_975072'
    reference[9123] = 'value_975072'
    assert tree.invariants(), "Invariants failed at step 8559"

    # Operation 8560: insert
    tree[8377] = 'value_435451'
    reference[8377] = 'value_435451'
    assert tree.invariants(), "Invariants failed at step 8560"

    # Operation 8561: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8561"

    # Operation 8562: delete
    del tree[9123]
    del reference[9123]
    assert tree.invariants(), "Invariants failed at step 8562"

    # Operation 8563: update
    tree[2589] = 'value_871727'
    reference[2589] = 'value_871727'
    assert tree.invariants(), "Invariants failed at step 8563"

    # Operation 8564: insert
    tree[8206] = 'value_284657'
    reference[8206] = 'value_284657'
    assert tree.invariants(), "Invariants failed at step 8564"

    # Operation 8565: delete
    del tree[2589]
    del reference[2589]
    assert tree.invariants(), "Invariants failed at step 8565"

    # Operation 8566: get
    assert tree.invariants(), "Invariants failed at step 8566"

    # Operation 8567: update
    tree[9682] = 'value_156816'
    reference[9682] = 'value_156816'
    assert tree.invariants(), "Invariants failed at step 8567"

    # Operation 8568: get
    assert tree.invariants(), "Invariants failed at step 8568"

    # Operation 8569: get
    assert tree.invariants(), "Invariants failed at step 8569"

    # Operation 8570: delete
    del tree[8206]
    del reference[8206]
    assert tree.invariants(), "Invariants failed at step 8570"

    # Operation 8571: get
    assert tree.invariants(), "Invariants failed at step 8571"

    # Operation 8572: insert
    tree[2479] = 'value_313729'
    reference[2479] = 'value_313729'
    assert tree.invariants(), "Invariants failed at step 8572"

    # Operation 8573: get
    assert tree.invariants(), "Invariants failed at step 8573"

    # Operation 8574: insert
    tree[7414] = 'value_277853'
    reference[7414] = 'value_277853'
    assert tree.invariants(), "Invariants failed at step 8574"

    # Operation 8575: delete
    del tree[9682]
    del reference[9682]
    assert tree.invariants(), "Invariants failed at step 8575"

    # Operation 8576: update
    tree[7414] = 'value_461121'
    reference[7414] = 'value_461121'
    assert tree.invariants(), "Invariants failed at step 8576"

    # Operation 8577: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8577"

    # Operation 8578: insert
    tree[9710] = 'value_86285'
    reference[9710] = 'value_86285'
    assert tree.invariants(), "Invariants failed at step 8578"

    # Operation 8579: update
    tree[8377] = 'value_145281'
    reference[8377] = 'value_145281'
    assert tree.invariants(), "Invariants failed at step 8579"

    # Operation 8580: insert
    tree[2204] = 'value_825117'
    reference[2204] = 'value_825117'
    assert tree.invariants(), "Invariants failed at step 8580"

    # Operation 8581: update
    tree[7414] = 'value_170566'
    reference[7414] = 'value_170566'
    assert tree.invariants(), "Invariants failed at step 8581"

    # Operation 8582: delete
    del tree[8377]
    del reference[8377]
    assert tree.invariants(), "Invariants failed at step 8582"

    # Operation 8583: insert
    tree[1302] = 'value_829038'
    reference[1302] = 'value_829038'
    assert tree.invariants(), "Invariants failed at step 8583"

    # Operation 8584: insert
    tree[5150] = 'value_418429'
    reference[5150] = 'value_418429'
    assert tree.invariants(), "Invariants failed at step 8584"

    # Operation 8585: batch_delete
    keys_to_delete = [8234, 7343, 7414, 1302, 2204]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8585"

    # Operation 8586: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8586"

    # Operation 8587: update
    tree[5150] = 'value_450362'
    reference[5150] = 'value_450362'
    assert tree.invariants(), "Invariants failed at step 8587"

    # Operation 8588: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8588"

    # Operation 8589: get
    assert tree.invariants(), "Invariants failed at step 8589"

    # Operation 8590: update
    tree[9710] = 'value_551837'
    reference[9710] = 'value_551837'
    assert tree.invariants(), "Invariants failed at step 8590"

    # Operation 8591: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8591"

    # Operation 8592: get
    assert tree.invariants(), "Invariants failed at step 8592"

    # Operation 8593: insert
    tree[707] = 'value_944455'
    reference[707] = 'value_944455'
    assert tree.invariants(), "Invariants failed at step 8593"

    # Operation 8594: get
    assert tree.invariants(), "Invariants failed at step 8594"

    # Operation 8595: delete
    del tree[2479]
    del reference[2479]
    assert tree.invariants(), "Invariants failed at step 8595"

    # Operation 8596: insert
    tree[7126] = 'value_626684'
    reference[7126] = 'value_626684'
    assert tree.invariants(), "Invariants failed at step 8596"

    # Operation 8597: insert
    tree[9788] = 'value_520668'
    reference[9788] = 'value_520668'
    assert tree.invariants(), "Invariants failed at step 8597"

    # Operation 8598: insert
    tree[9795] = 'value_135923'
    reference[9795] = 'value_135923'
    assert tree.invariants(), "Invariants failed at step 8598"

    # Operation 8599: insert
    tree[1233] = 'value_316464'
    reference[1233] = 'value_316464'
    assert tree.invariants(), "Invariants failed at step 8599"

    # Operation 8600: delete
    del tree[5150]
    del reference[5150]
    assert tree.invariants(), "Invariants failed at step 8600"

    # Operation 8601: insert
    tree[4896] = 'value_971459'
    reference[4896] = 'value_971459'
    assert tree.invariants(), "Invariants failed at step 8601"

    # Operation 8602: insert
    tree[7606] = 'value_572448'
    reference[7606] = 'value_572448'
    assert tree.invariants(), "Invariants failed at step 8602"

    # Operation 8603: insert
    tree[5847] = 'value_188960'
    reference[5847] = 'value_188960'
    assert tree.invariants(), "Invariants failed at step 8603"

    # Operation 8604: get
    assert tree.invariants(), "Invariants failed at step 8604"

    # Operation 8605: insert
    tree[6100] = 'value_727282'
    reference[6100] = 'value_727282'
    assert tree.invariants(), "Invariants failed at step 8605"

    # Operation 8606: insert
    tree[6860] = 'value_544648'
    reference[6860] = 'value_544648'
    assert tree.invariants(), "Invariants failed at step 8606"

    # Operation 8607: get
    assert tree.invariants(), "Invariants failed at step 8607"

    # Operation 8608: update
    tree[9710] = 'value_543416'
    reference[9710] = 'value_543416'
    assert tree.invariants(), "Invariants failed at step 8608"

    # Operation 8609: get
    assert tree.invariants(), "Invariants failed at step 8609"

    # Operation 8610: delete
    del tree[7126]
    del reference[7126]
    assert tree.invariants(), "Invariants failed at step 8610"

    # Operation 8611: insert
    tree[320] = 'value_726340'
    reference[320] = 'value_726340'
    assert tree.invariants(), "Invariants failed at step 8611"

    # Operation 8612: delete
    del tree[707]
    del reference[707]
    assert tree.invariants(), "Invariants failed at step 8612"

    # Operation 8613: update
    tree[4896] = 'value_861126'
    reference[4896] = 'value_861126'
    assert tree.invariants(), "Invariants failed at step 8613"

    # Operation 8614: insert
    tree[6475] = 'value_657856'
    reference[6475] = 'value_657856'
    assert tree.invariants(), "Invariants failed at step 8614"

    # Operation 8615: delete
    del tree[6860]
    del reference[6860]
    assert tree.invariants(), "Invariants failed at step 8615"

    # Operation 8616: get
    assert tree.invariants(), "Invariants failed at step 8616"

    # Operation 8617: delete
    del tree[9795]
    del reference[9795]
    assert tree.invariants(), "Invariants failed at step 8617"

    # Operation 8618: get
    assert tree.invariants(), "Invariants failed at step 8618"

    # Operation 8619: delete
    del tree[9710]
    del reference[9710]
    assert tree.invariants(), "Invariants failed at step 8619"

    # Operation 8620: update
    tree[5847] = 'value_189325'
    reference[5847] = 'value_189325'
    assert tree.invariants(), "Invariants failed at step 8620"

    # Operation 8621: delete
    del tree[1233]
    del reference[1233]
    assert tree.invariants(), "Invariants failed at step 8621"

    # Operation 8622: delete
    del tree[6475]
    del reference[6475]
    assert tree.invariants(), "Invariants failed at step 8622"

    # Operation 8623: update
    tree[4896] = 'value_728228'
    reference[4896] = 'value_728228'
    assert tree.invariants(), "Invariants failed at step 8623"

    # Operation 8624: update
    tree[9788] = 'value_51626'
    reference[9788] = 'value_51626'
    assert tree.invariants(), "Invariants failed at step 8624"

    # Operation 8625: get
    assert tree.invariants(), "Invariants failed at step 8625"

    # Operation 8626: update
    tree[4896] = 'value_576164'
    reference[4896] = 'value_576164'
    assert tree.invariants(), "Invariants failed at step 8626"

    # Operation 8627: delete
    del tree[4896]
    del reference[4896]
    assert tree.invariants(), "Invariants failed at step 8627"

    # Operation 8628: insert
    tree[4020] = 'value_391291'
    reference[4020] = 'value_391291'
    assert tree.invariants(), "Invariants failed at step 8628"

    # Operation 8629: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8629"

    # Operation 8630: get
    assert tree.invariants(), "Invariants failed at step 8630"

    # Operation 8631: get
    assert tree.invariants(), "Invariants failed at step 8631"

    # Operation 8632: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8632"

    # Operation 8633: insert
    tree[8451] = 'value_601808'
    reference[8451] = 'value_601808'
    assert tree.invariants(), "Invariants failed at step 8633"

    # Operation 8634: insert
    tree[9108] = 'value_282749'
    reference[9108] = 'value_282749'
    assert tree.invariants(), "Invariants failed at step 8634"

    # Operation 8635: batch_delete
    keys_to_delete = [320, 8451, 4020, 5847, 5625, 6459]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8635"

    # Operation 8636: insert
    tree[6139] = 'value_279202'
    reference[6139] = 'value_279202'
    assert tree.invariants(), "Invariants failed at step 8636"

    # Operation 8637: insert
    tree[5935] = 'value_270826'
    reference[5935] = 'value_270826'
    assert tree.invariants(), "Invariants failed at step 8637"

    # Operation 8638: update
    tree[7606] = 'value_474003'
    reference[7606] = 'value_474003'
    assert tree.invariants(), "Invariants failed at step 8638"

    # Operation 8639: delete
    del tree[5935]
    del reference[5935]
    assert tree.invariants(), "Invariants failed at step 8639"

    # Operation 8640: update
    tree[6139] = 'value_850761'
    reference[6139] = 'value_850761'
    assert tree.invariants(), "Invariants failed at step 8640"

    # Operation 8641: batch_delete
    keys_to_delete = [2471, 6139, 7606, 7943]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8641"

    # Operation 8642: insert
    tree[3587] = 'value_249696'
    reference[3587] = 'value_249696'
    assert tree.invariants(), "Invariants failed at step 8642"

    # Operation 8643: get
    assert tree.invariants(), "Invariants failed at step 8643"

    # Operation 8644: insert
    tree[3212] = 'value_249217'
    reference[3212] = 'value_249217'
    assert tree.invariants(), "Invariants failed at step 8644"

    # Operation 8645: batch_delete
    keys_to_delete = [4060, 1236, 3587, 6100]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8645"

    # Operation 8646: delete
    del tree[9788]
    del reference[9788]
    assert tree.invariants(), "Invariants failed at step 8646"

    # Operation 8647: insert
    tree[8741] = 'value_805051'
    reference[8741] = 'value_805051'
    assert tree.invariants(), "Invariants failed at step 8647"

    # Operation 8648: insert
    tree[5960] = 'value_694507'
    reference[5960] = 'value_694507'
    assert tree.invariants(), "Invariants failed at step 8648"

    # Operation 8649: delete
    del tree[5960]
    del reference[5960]
    assert tree.invariants(), "Invariants failed at step 8649"

    # Operation 8650: get
    assert tree.invariants(), "Invariants failed at step 8650"

    # Operation 8651: get
    assert tree.invariants(), "Invariants failed at step 8651"

    # Operation 8652: insert
    tree[8852] = 'value_820971'
    reference[8852] = 'value_820971'
    assert tree.invariants(), "Invariants failed at step 8652"

    # Operation 8653: insert
    tree[3230] = 'value_672933'
    reference[3230] = 'value_672933'
    assert tree.invariants(), "Invariants failed at step 8653"

    # Operation 8654: insert
    tree[9843] = 'value_591064'
    reference[9843] = 'value_591064'
    assert tree.invariants(), "Invariants failed at step 8654"

    # Operation 8655: insert
    tree[1347] = 'value_830914'
    reference[1347] = 'value_830914'
    assert tree.invariants(), "Invariants failed at step 8655"

    # Operation 8656: update
    tree[9108] = 'value_481148'
    reference[9108] = 'value_481148'
    assert tree.invariants(), "Invariants failed at step 8656"

    # Operation 8657: insert
    tree[6468] = 'value_757258'
    reference[6468] = 'value_757258'
    assert tree.invariants(), "Invariants failed at step 8657"

    # Operation 8658: insert
    tree[3022] = 'value_166637'
    reference[3022] = 'value_166637'
    assert tree.invariants(), "Invariants failed at step 8658"

    # Operation 8659: insert
    tree[3044] = 'value_916049'
    reference[3044] = 'value_916049'
    assert tree.invariants(), "Invariants failed at step 8659"

    # Operation 8660: get
    assert tree.invariants(), "Invariants failed at step 8660"

    # Operation 8661: batch_delete
    keys_to_delete = [7151, 9843, 8852, 667, 3230]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8661"

    # Operation 8662: delete
    del tree[3022]
    del reference[3022]
    assert tree.invariants(), "Invariants failed at step 8662"

    # Operation 8663: insert
    tree[5749] = 'value_497006'
    reference[5749] = 'value_497006'
    assert tree.invariants(), "Invariants failed at step 8663"

    # Operation 8664: delete
    del tree[3212]
    del reference[3212]
    assert tree.invariants(), "Invariants failed at step 8664"

    # Operation 8665: insert
    tree[3530] = 'value_364379'
    reference[3530] = 'value_364379'
    assert tree.invariants(), "Invariants failed at step 8665"

    # Operation 8666: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8666"

    # Operation 8667: insert
    tree[8825] = 'value_243737'
    reference[8825] = 'value_243737'
    assert tree.invariants(), "Invariants failed at step 8667"

    # Operation 8668: insert
    tree[2688] = 'value_196986'
    reference[2688] = 'value_196986'
    assert tree.invariants(), "Invariants failed at step 8668"

    # Operation 8669: insert
    tree[660] = 'value_780401'
    reference[660] = 'value_780401'
    assert tree.invariants(), "Invariants failed at step 8669"

    # Operation 8670: insert
    tree[4310] = 'value_456172'
    reference[4310] = 'value_456172'
    assert tree.invariants(), "Invariants failed at step 8670"

    # Operation 8671: update
    tree[9108] = 'value_803152'
    reference[9108] = 'value_803152'
    assert tree.invariants(), "Invariants failed at step 8671"

    # Operation 8672: get
    assert tree.invariants(), "Invariants failed at step 8672"

    # Operation 8673: insert
    tree[6588] = 'value_509925'
    reference[6588] = 'value_509925'
    assert tree.invariants(), "Invariants failed at step 8673"

    # Operation 8674: insert
    tree[2744] = 'value_441139'
    reference[2744] = 'value_441139'
    assert tree.invariants(), "Invariants failed at step 8674"

    # Operation 8675: insert
    tree[6042] = 'value_981206'
    reference[6042] = 'value_981206'
    assert tree.invariants(), "Invariants failed at step 8675"

    # Operation 8676: batch_delete
    keys_to_delete = [2688, 6468, 3530, 9166, 660, 9108, 2744, 6042, 9368]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8676"

    # Operation 8677: get
    assert tree.invariants(), "Invariants failed at step 8677"

    # Operation 8678: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8678"

    # Operation 8679: insert
    tree[7188] = 'value_923892'
    reference[7188] = 'value_923892'
    assert tree.invariants(), "Invariants failed at step 8679"

    # Operation 8680: update
    tree[3044] = 'value_429801'
    reference[3044] = 'value_429801'
    assert tree.invariants(), "Invariants failed at step 8680"

    # Operation 8681: update
    tree[3044] = 'value_512213'
    reference[3044] = 'value_512213'
    assert tree.invariants(), "Invariants failed at step 8681"

    # Operation 8682: batch_delete
    keys_to_delete = [1347, 3044, 8741, 8825, 5627]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8682"

    # Operation 8683: update
    tree[6588] = 'value_995712'
    reference[6588] = 'value_995712'
    assert tree.invariants(), "Invariants failed at step 8683"

    # Operation 8684: insert
    tree[3202] = 'value_246517'
    reference[3202] = 'value_246517'
    assert tree.invariants(), "Invariants failed at step 8684"

    # Operation 8685: delete
    del tree[6588]
    del reference[6588]
    assert tree.invariants(), "Invariants failed at step 8685"

    # Operation 8686: update
    tree[3202] = 'value_923267'
    reference[3202] = 'value_923267'
    assert tree.invariants(), "Invariants failed at step 8686"

    # Operation 8687: get
    assert tree.invariants(), "Invariants failed at step 8687"

    # Operation 8688: delete
    del tree[3202]
    del reference[3202]
    assert tree.invariants(), "Invariants failed at step 8688"

    # Operation 8689: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8689"

    # Operation 8690: insert
    tree[3176] = 'value_938246'
    reference[3176] = 'value_938246'
    assert tree.invariants(), "Invariants failed at step 8690"

    # Operation 8691: delete
    del tree[4310]
    del reference[4310]
    assert tree.invariants(), "Invariants failed at step 8691"

    # Operation 8692: delete
    del tree[7188]
    del reference[7188]
    assert tree.invariants(), "Invariants failed at step 8692"

    # Operation 8693: update
    tree[3176] = 'value_161159'
    reference[3176] = 'value_161159'
    assert tree.invariants(), "Invariants failed at step 8693"

    # Operation 8694: insert
    tree[1933] = 'value_726076'
    reference[1933] = 'value_726076'
    assert tree.invariants(), "Invariants failed at step 8694"

    # Operation 8695: delete
    del tree[1933]
    del reference[1933]
    assert tree.invariants(), "Invariants failed at step 8695"

    # Operation 8696: update
    tree[3176] = 'value_802304'
    reference[3176] = 'value_802304'
    assert tree.invariants(), "Invariants failed at step 8696"

    # Operation 8697: insert
    tree[3533] = 'value_76471'
    reference[3533] = 'value_76471'
    assert tree.invariants(), "Invariants failed at step 8697"

    # Operation 8698: delete
    del tree[3533]
    del reference[3533]
    assert tree.invariants(), "Invariants failed at step 8698"

    # Operation 8699: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8699"

    # Operation 8700: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8700"

    # Operation 8701: insert
    tree[5058] = 'value_929170'
    reference[5058] = 'value_929170'
    assert tree.invariants(), "Invariants failed at step 8701"

    # Operation 8702: delete
    del tree[5749]
    del reference[5749]
    assert tree.invariants(), "Invariants failed at step 8702"

    # Operation 8703: delete
    del tree[5058]
    del reference[5058]
    assert tree.invariants(), "Invariants failed at step 8703"

    # Operation 8704: insert
    tree[5568] = 'value_268053'
    reference[5568] = 'value_268053'
    assert tree.invariants(), "Invariants failed at step 8704"

    # Operation 8705: insert
    tree[5012] = 'value_418959'
    reference[5012] = 'value_418959'
    assert tree.invariants(), "Invariants failed at step 8705"

    # Operation 8706: get
    assert tree.invariants(), "Invariants failed at step 8706"

    # Operation 8707: insert
    tree[9418] = 'value_60418'
    reference[9418] = 'value_60418'
    assert tree.invariants(), "Invariants failed at step 8707"

    # Operation 8708: update
    tree[5568] = 'value_636318'
    reference[5568] = 'value_636318'
    assert tree.invariants(), "Invariants failed at step 8708"

    # Operation 8709: insert
    tree[4114] = 'value_478358'
    reference[4114] = 'value_478358'
    assert tree.invariants(), "Invariants failed at step 8709"

    # Operation 8710: update
    tree[5012] = 'value_430573'
    reference[5012] = 'value_430573'
    assert tree.invariants(), "Invariants failed at step 8710"

    # Operation 8711: delete
    del tree[9418]
    del reference[9418]
    assert tree.invariants(), "Invariants failed at step 8711"

    # Operation 8712: get
    assert tree.invariants(), "Invariants failed at step 8712"

    # Operation 8713: update
    tree[4114] = 'value_76368'
    reference[4114] = 'value_76368'
    assert tree.invariants(), "Invariants failed at step 8713"

    # Operation 8714: update
    tree[3176] = 'value_958851'
    reference[3176] = 'value_958851'
    assert tree.invariants(), "Invariants failed at step 8714"

    # Operation 8715: delete
    del tree[5568]
    del reference[5568]
    assert tree.invariants(), "Invariants failed at step 8715"

    # Operation 8716: insert
    tree[7219] = 'value_259244'
    reference[7219] = 'value_259244'
    assert tree.invariants(), "Invariants failed at step 8716"

    # Operation 8717: insert
    tree[3155] = 'value_249704'
    reference[3155] = 'value_249704'
    assert tree.invariants(), "Invariants failed at step 8717"

    # Operation 8718: insert
    tree[5389] = 'value_527264'
    reference[5389] = 'value_527264'
    assert tree.invariants(), "Invariants failed at step 8718"

    # Operation 8719: delete
    del tree[5012]
    del reference[5012]
    assert tree.invariants(), "Invariants failed at step 8719"

    # Operation 8720: delete
    del tree[4114]
    del reference[4114]
    assert tree.invariants(), "Invariants failed at step 8720"

    # Operation 8721: get
    assert tree.invariants(), "Invariants failed at step 8721"

    # Operation 8722: insert
    tree[1290] = 'value_296562'
    reference[1290] = 'value_296562'
    assert tree.invariants(), "Invariants failed at step 8722"

    # Operation 8723: insert
    tree[3592] = 'value_441885'
    reference[3592] = 'value_441885'
    assert tree.invariants(), "Invariants failed at step 8723"

    # Operation 8724: insert
    tree[6730] = 'value_108951'
    reference[6730] = 'value_108951'
    assert tree.invariants(), "Invariants failed at step 8724"

    # Operation 8725: get
    assert tree.invariants(), "Invariants failed at step 8725"

    # Operation 8726: batch_delete
    keys_to_delete = [3592, 1290, 7219, 5075, 7770]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8726"

    # Operation 8727: get
    assert tree.invariants(), "Invariants failed at step 8727"

    # Operation 8728: delete
    del tree[3176]
    del reference[3176]
    assert tree.invariants(), "Invariants failed at step 8728"

    # Operation 8729: delete
    del tree[3155]
    del reference[3155]
    assert tree.invariants(), "Invariants failed at step 8729"

    # Operation 8730: update
    tree[5389] = 'value_571324'
    reference[5389] = 'value_571324'
    assert tree.invariants(), "Invariants failed at step 8730"

    # Operation 8731: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8731"

    # Operation 8732: get
    assert tree.invariants(), "Invariants failed at step 8732"

    # Operation 8733: delete
    del tree[6730]
    del reference[6730]
    assert tree.invariants(), "Invariants failed at step 8733"

    # Operation 8734: update
    tree[5389] = 'value_556561'
    reference[5389] = 'value_556561'
    assert tree.invariants(), "Invariants failed at step 8734"

    # Operation 8735: update
    tree[5389] = 'value_785282'
    reference[5389] = 'value_785282'
    assert tree.invariants(), "Invariants failed at step 8735"

    # Operation 8736: update
    tree[5389] = 'value_63289'
    reference[5389] = 'value_63289'
    assert tree.invariants(), "Invariants failed at step 8736"

    # Operation 8737: delete
    del tree[5389]
    del reference[5389]
    assert tree.invariants(), "Invariants failed at step 8737"

    # Operation 8738: insert
    tree[8107] = 'value_635071'
    reference[8107] = 'value_635071'
    assert tree.invariants(), "Invariants failed at step 8738"

    # Operation 8739: get
    assert tree.invariants(), "Invariants failed at step 8739"

    # Operation 8740: insert
    tree[9087] = 'value_681196'
    reference[9087] = 'value_681196'
    assert tree.invariants(), "Invariants failed at step 8740"

    # Operation 8741: get
    assert tree.invariants(), "Invariants failed at step 8741"

    # Operation 8742: delete
    del tree[9087]
    del reference[9087]
    assert tree.invariants(), "Invariants failed at step 8742"

    # Operation 8743: delete
    del tree[8107]
    del reference[8107]
    assert tree.invariants(), "Invariants failed at step 8743"

    # Operation 8744: insert
    tree[3694] = 'value_277950'
    reference[3694] = 'value_277950'
    assert tree.invariants(), "Invariants failed at step 8744"

    # Operation 8745: delete
    del tree[3694]
    del reference[3694]
    assert tree.invariants(), "Invariants failed at step 8745"

    # Operation 8746: insert
    tree[6115] = 'value_948906'
    reference[6115] = 'value_948906'
    assert tree.invariants(), "Invariants failed at step 8746"

    # Operation 8747: update
    tree[6115] = 'value_556312'
    reference[6115] = 'value_556312'
    assert tree.invariants(), "Invariants failed at step 8747"

    # Operation 8748: get
    assert tree.invariants(), "Invariants failed at step 8748"

    # Operation 8749: insert
    tree[6675] = 'value_175510'
    reference[6675] = 'value_175510'
    assert tree.invariants(), "Invariants failed at step 8749"

    # Operation 8750: insert
    tree[2368] = 'value_761897'
    reference[2368] = 'value_761897'
    assert tree.invariants(), "Invariants failed at step 8750"

    # Operation 8751: update
    tree[6675] = 'value_84974'
    reference[6675] = 'value_84974'
    assert tree.invariants(), "Invariants failed at step 8751"

    # Operation 8752: delete
    del tree[6115]
    del reference[6115]
    assert tree.invariants(), "Invariants failed at step 8752"

    # Operation 8753: get
    assert tree.invariants(), "Invariants failed at step 8753"

    # Operation 8754: delete
    del tree[6675]
    del reference[6675]
    assert tree.invariants(), "Invariants failed at step 8754"

    # Operation 8755: insert
    tree[9317] = 'value_952867'
    reference[9317] = 'value_952867'
    assert tree.invariants(), "Invariants failed at step 8755"

    # Operation 8756: get
    assert tree.invariants(), "Invariants failed at step 8756"

    # Operation 8757: get
    assert tree.invariants(), "Invariants failed at step 8757"

    # Operation 8758: insert
    tree[2443] = 'value_236821'
    reference[2443] = 'value_236821'
    assert tree.invariants(), "Invariants failed at step 8758"

    # Operation 8759: get
    assert tree.invariants(), "Invariants failed at step 8759"

    # Operation 8760: get
    assert tree.invariants(), "Invariants failed at step 8760"

    # Operation 8761: delete
    del tree[2443]
    del reference[2443]
    assert tree.invariants(), "Invariants failed at step 8761"

    # Operation 8762: insert
    tree[4206] = 'value_729237'
    reference[4206] = 'value_729237'
    assert tree.invariants(), "Invariants failed at step 8762"

    # Operation 8763: insert
    tree[1672] = 'value_132886'
    reference[1672] = 'value_132886'
    assert tree.invariants(), "Invariants failed at step 8763"

    # Operation 8764: insert
    tree[649] = 'value_574211'
    reference[649] = 'value_574211'
    assert tree.invariants(), "Invariants failed at step 8764"

    # Operation 8765: batch_delete
    keys_to_delete = [1672, 649, 3998, 7950]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8765"

    # Operation 8766: insert
    tree[9768] = 'value_151196'
    reference[9768] = 'value_151196'
    assert tree.invariants(), "Invariants failed at step 8766"

    # Operation 8767: get
    assert tree.invariants(), "Invariants failed at step 8767"

    # Operation 8768: insert
    tree[5530] = 'value_51850'
    reference[5530] = 'value_51850'
    assert tree.invariants(), "Invariants failed at step 8768"

    # Operation 8769: update
    tree[2368] = 'value_97029'
    reference[2368] = 'value_97029'
    assert tree.invariants(), "Invariants failed at step 8769"

    # Operation 8770: get
    assert tree.invariants(), "Invariants failed at step 8770"

    # Operation 8771: insert
    tree[6266] = 'value_185564'
    reference[6266] = 'value_185564'
    assert tree.invariants(), "Invariants failed at step 8771"

    # Operation 8772: delete
    del tree[9768]
    del reference[9768]
    assert tree.invariants(), "Invariants failed at step 8772"

    # Operation 8773: get
    assert tree.invariants(), "Invariants failed at step 8773"

    # Operation 8774: update
    tree[9317] = 'value_206678'
    reference[9317] = 'value_206678'
    assert tree.invariants(), "Invariants failed at step 8774"

    # Operation 8775: get
    assert tree.invariants(), "Invariants failed at step 8775"

    # Operation 8776: delete
    del tree[4206]
    del reference[4206]
    assert tree.invariants(), "Invariants failed at step 8776"

    # Operation 8777: update
    tree[2368] = 'value_295371'
    reference[2368] = 'value_295371'
    assert tree.invariants(), "Invariants failed at step 8777"

    # Operation 8778: update
    tree[9317] = 'value_838529'
    reference[9317] = 'value_838529'
    assert tree.invariants(), "Invariants failed at step 8778"

    # Operation 8779: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8779"

    # Operation 8780: delete
    del tree[2368]
    del reference[2368]
    assert tree.invariants(), "Invariants failed at step 8780"

    # Operation 8781: get
    assert tree.invariants(), "Invariants failed at step 8781"

    # Operation 8782: delete
    del tree[9317]
    del reference[9317]
    assert tree.invariants(), "Invariants failed at step 8782"

    # Operation 8783: get
    assert tree.invariants(), "Invariants failed at step 8783"

    # Operation 8784: get
    assert tree.invariants(), "Invariants failed at step 8784"

    # Operation 8785: get
    assert tree.invariants(), "Invariants failed at step 8785"

    # Operation 8786: insert
    tree[5960] = 'value_921658'
    reference[5960] = 'value_921658'
    assert tree.invariants(), "Invariants failed at step 8786"

    # Operation 8787: delete
    del tree[5530]
    del reference[5530]
    assert tree.invariants(), "Invariants failed at step 8787"

    # Operation 8788: delete
    del tree[6266]
    del reference[6266]
    assert tree.invariants(), "Invariants failed at step 8788"

    # Operation 8789: delete
    del tree[5960]
    del reference[5960]
    assert tree.invariants(), "Invariants failed at step 8789"

    # Operation 8790: insert
    tree[9073] = 'value_612791'
    reference[9073] = 'value_612791'
    assert tree.invariants(), "Invariants failed at step 8790"

    # Operation 8791: insert
    tree[8596] = 'value_921754'
    reference[8596] = 'value_921754'
    assert tree.invariants(), "Invariants failed at step 8791"

    # Operation 8792: delete
    del tree[8596]
    del reference[8596]
    assert tree.invariants(), "Invariants failed at step 8792"

    # Operation 8793: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8793"

    # Operation 8794: get
    assert tree.invariants(), "Invariants failed at step 8794"

    # Operation 8795: delete
    del tree[9073]
    del reference[9073]
    assert tree.invariants(), "Invariants failed at step 8795"

    # Operation 8796: insert
    tree[2851] = 'value_468669'
    reference[2851] = 'value_468669'
    assert tree.invariants(), "Invariants failed at step 8796"

    # Operation 8797: insert
    tree[3485] = 'value_158759'
    reference[3485] = 'value_158759'
    assert tree.invariants(), "Invariants failed at step 8797"

    # Operation 8798: insert
    tree[8778] = 'value_298255'
    reference[8778] = 'value_298255'
    assert tree.invariants(), "Invariants failed at step 8798"

    # Operation 8799: delete
    del tree[8778]
    del reference[8778]
    assert tree.invariants(), "Invariants failed at step 8799"

    # Operation 8800: insert
    tree[7029] = 'value_684566'
    reference[7029] = 'value_684566'
    assert tree.invariants(), "Invariants failed at step 8800"

    # Operation 8801: delete
    del tree[7029]
    del reference[7029]
    assert tree.invariants(), "Invariants failed at step 8801"

    # Operation 8802: insert
    tree[3568] = 'value_338263'
    reference[3568] = 'value_338263'
    assert tree.invariants(), "Invariants failed at step 8802"

    # Operation 8803: get
    assert tree.invariants(), "Invariants failed at step 8803"

    # Operation 8804: delete
    del tree[2851]
    del reference[2851]
    assert tree.invariants(), "Invariants failed at step 8804"

    # Operation 8805: delete
    del tree[3485]
    del reference[3485]
    assert tree.invariants(), "Invariants failed at step 8805"

    # Operation 8806: update
    tree[3568] = 'value_788533'
    reference[3568] = 'value_788533'
    assert tree.invariants(), "Invariants failed at step 8806"

    # Operation 8807: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8807"

    # Operation 8808: get
    assert tree.invariants(), "Invariants failed at step 8808"

    # Operation 8809: insert
    tree[1670] = 'value_162128'
    reference[1670] = 'value_162128'
    assert tree.invariants(), "Invariants failed at step 8809"

    # Operation 8810: insert
    tree[1356] = 'value_758951'
    reference[1356] = 'value_758951'
    assert tree.invariants(), "Invariants failed at step 8810"

    # Operation 8811: get
    assert tree.invariants(), "Invariants failed at step 8811"

    # Operation 8812: insert
    tree[1134] = 'value_823823'
    reference[1134] = 'value_823823'
    assert tree.invariants(), "Invariants failed at step 8812"

    # Operation 8813: delete
    del tree[1670]
    del reference[1670]
    assert tree.invariants(), "Invariants failed at step 8813"

    # Operation 8814: get
    assert tree.invariants(), "Invariants failed at step 8814"

    # Operation 8815: insert
    tree[3571] = 'value_952324'
    reference[3571] = 'value_952324'
    assert tree.invariants(), "Invariants failed at step 8815"

    # Operation 8816: get
    assert tree.invariants(), "Invariants failed at step 8816"

    # Operation 8817: get
    assert tree.invariants(), "Invariants failed at step 8817"

    # Operation 8818: insert
    tree[8838] = 'value_31441'
    reference[8838] = 'value_31441'
    assert tree.invariants(), "Invariants failed at step 8818"

    # Operation 8819: get
    assert tree.invariants(), "Invariants failed at step 8819"

    # Operation 8820: get
    assert tree.invariants(), "Invariants failed at step 8820"

    # Operation 8821: insert
    tree[2090] = 'value_502966'
    reference[2090] = 'value_502966'
    assert tree.invariants(), "Invariants failed at step 8821"

    # Operation 8822: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8822"

    # Operation 8823: batch_delete
    keys_to_delete = [3568, 2362, 3571, 5645]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8823"

    # Operation 8824: delete
    del tree[2090]
    del reference[2090]
    assert tree.invariants(), "Invariants failed at step 8824"

    # Operation 8825: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8825"

    # Operation 8826: delete
    del tree[1134]
    del reference[1134]
    assert tree.invariants(), "Invariants failed at step 8826"

    # Operation 8827: insert
    tree[9794] = 'value_917510'
    reference[9794] = 'value_917510'
    assert tree.invariants(), "Invariants failed at step 8827"

    # Operation 8828: delete
    del tree[9794]
    del reference[9794]
    assert tree.invariants(), "Invariants failed at step 8828"

    # Operation 8829: update
    tree[8838] = 'value_404728'
    reference[8838] = 'value_404728'
    assert tree.invariants(), "Invariants failed at step 8829"

    # Operation 8830: delete
    del tree[1356]
    del reference[1356]
    assert tree.invariants(), "Invariants failed at step 8830"

    # Operation 8831: get
    assert tree.invariants(), "Invariants failed at step 8831"

    # Operation 8832: delete
    del tree[8838]
    del reference[8838]
    assert tree.invariants(), "Invariants failed at step 8832"

    # Operation 8833: insert
    tree[7814] = 'value_387712'
    reference[7814] = 'value_387712'
    assert tree.invariants(), "Invariants failed at step 8833"

    # Operation 8834: get
    assert tree.invariants(), "Invariants failed at step 8834"

    # Operation 8835: get
    assert tree.invariants(), "Invariants failed at step 8835"

    # Operation 8836: delete
    del tree[7814]
    del reference[7814]
    assert tree.invariants(), "Invariants failed at step 8836"

    # Operation 8837: get
    assert tree.invariants(), "Invariants failed at step 8837"

    # Operation 8838: insert
    tree[7101] = 'value_579329'
    reference[7101] = 'value_579329'
    assert tree.invariants(), "Invariants failed at step 8838"

    # Operation 8839: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8839"

    # Operation 8840: get
    assert tree.invariants(), "Invariants failed at step 8840"

    # Operation 8841: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8841"

    # Operation 8842: insert
    tree[3372] = 'value_504344'
    reference[3372] = 'value_504344'
    assert tree.invariants(), "Invariants failed at step 8842"

    # Operation 8843: get
    assert tree.invariants(), "Invariants failed at step 8843"

    # Operation 8844: delete
    del tree[7101]
    del reference[7101]
    assert tree.invariants(), "Invariants failed at step 8844"

    # Operation 8845: insert
    tree[7793] = 'value_694267'
    reference[7793] = 'value_694267'
    assert tree.invariants(), "Invariants failed at step 8845"

    # Operation 8846: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8846"

    # Operation 8847: get
    assert tree.invariants(), "Invariants failed at step 8847"

    # Operation 8848: get
    assert tree.invariants(), "Invariants failed at step 8848"

    # Operation 8849: delete
    del tree[3372]
    del reference[3372]
    assert tree.invariants(), "Invariants failed at step 8849"

    # Operation 8850: get
    assert tree.invariants(), "Invariants failed at step 8850"

    # Operation 8851: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8851"

    # Operation 8852: delete
    del tree[7793]
    del reference[7793]
    assert tree.invariants(), "Invariants failed at step 8852"

    # Operation 8853: get
    assert tree.invariants(), "Invariants failed at step 8853"

    # Operation 8854: insert
    tree[4667] = 'value_148148'
    reference[4667] = 'value_148148'
    assert tree.invariants(), "Invariants failed at step 8854"

    # Operation 8855: update
    tree[4667] = 'value_524468'
    reference[4667] = 'value_524468'
    assert tree.invariants(), "Invariants failed at step 8855"

    # Operation 8856: insert
    tree[6583] = 'value_403321'
    reference[6583] = 'value_403321'
    assert tree.invariants(), "Invariants failed at step 8856"

    # Operation 8857: update
    tree[6583] = 'value_374629'
    reference[6583] = 'value_374629'
    assert tree.invariants(), "Invariants failed at step 8857"

    # Operation 8858: get
    assert tree.invariants(), "Invariants failed at step 8858"

    # Operation 8859: update
    tree[6583] = 'value_921433'
    reference[6583] = 'value_921433'
    assert tree.invariants(), "Invariants failed at step 8859"

    # Operation 8860: insert
    tree[7326] = 'value_674986'
    reference[7326] = 'value_674986'
    assert tree.invariants(), "Invariants failed at step 8860"

    # Operation 8861: delete
    del tree[6583]
    del reference[6583]
    assert tree.invariants(), "Invariants failed at step 8861"

    # Operation 8862: get
    assert tree.invariants(), "Invariants failed at step 8862"

    # Operation 8863: insert
    tree[2206] = 'value_81344'
    reference[2206] = 'value_81344'
    assert tree.invariants(), "Invariants failed at step 8863"

    # Operation 8864: get
    assert tree.invariants(), "Invariants failed at step 8864"

    # Operation 8865: update
    tree[4667] = 'value_817904'
    reference[4667] = 'value_817904'
    assert tree.invariants(), "Invariants failed at step 8865"

    # Operation 8866: delete
    del tree[2206]
    del reference[2206]
    assert tree.invariants(), "Invariants failed at step 8866"

    # Operation 8867: get
    assert tree.invariants(), "Invariants failed at step 8867"

    # Operation 8868: get
    assert tree.invariants(), "Invariants failed at step 8868"

    # Operation 8869: update
    tree[7326] = 'value_351703'
    reference[7326] = 'value_351703'
    assert tree.invariants(), "Invariants failed at step 8869"

    # Operation 8870: get
    assert tree.invariants(), "Invariants failed at step 8870"

    # Operation 8871: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8871"

    # Operation 8872: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8872"

    # Operation 8873: insert
    tree[8093] = 'value_158957'
    reference[8093] = 'value_158957'
    assert tree.invariants(), "Invariants failed at step 8873"

    # Operation 8874: insert
    tree[5782] = 'value_192387'
    reference[5782] = 'value_192387'
    assert tree.invariants(), "Invariants failed at step 8874"

    # Operation 8875: delete
    del tree[8093]
    del reference[8093]
    assert tree.invariants(), "Invariants failed at step 8875"

    # Operation 8876: update
    tree[4667] = 'value_971145'
    reference[4667] = 'value_971145'
    assert tree.invariants(), "Invariants failed at step 8876"

    # Operation 8877: get
    assert tree.invariants(), "Invariants failed at step 8877"

    # Operation 8878: delete
    del tree[7326]
    del reference[7326]
    assert tree.invariants(), "Invariants failed at step 8878"

    # Operation 8879: delete
    del tree[4667]
    del reference[4667]
    assert tree.invariants(), "Invariants failed at step 8879"

    # Operation 8880: insert
    tree[4673] = 'value_334400'
    reference[4673] = 'value_334400'
    assert tree.invariants(), "Invariants failed at step 8880"

    # Operation 8881: delete
    del tree[5782]
    del reference[5782]
    assert tree.invariants(), "Invariants failed at step 8881"

    # Operation 8882: insert
    tree[8726] = 'value_847820'
    reference[8726] = 'value_847820'
    assert tree.invariants(), "Invariants failed at step 8882"

    # Operation 8883: delete
    del tree[4673]
    del reference[4673]
    assert tree.invariants(), "Invariants failed at step 8883"

    # Operation 8884: get
    assert tree.invariants(), "Invariants failed at step 8884"

    # Operation 8885: insert
    tree[9163] = 'value_51988'
    reference[9163] = 'value_51988'
    assert tree.invariants(), "Invariants failed at step 8885"

    # Operation 8886: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8886"

    # Operation 8887: get
    assert tree.invariants(), "Invariants failed at step 8887"

    # Operation 8888: delete
    del tree[9163]
    del reference[9163]
    assert tree.invariants(), "Invariants failed at step 8888"

    # Operation 8889: insert
    tree[2883] = 'value_254584'
    reference[2883] = 'value_254584'
    assert tree.invariants(), "Invariants failed at step 8889"

    # Operation 8890: insert
    tree[4909] = 'value_603697'
    reference[4909] = 'value_603697'
    assert tree.invariants(), "Invariants failed at step 8890"

    # Operation 8891: insert
    tree[9006] = 'value_263341'
    reference[9006] = 'value_263341'
    assert tree.invariants(), "Invariants failed at step 8891"

    # Operation 8892: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8892"

    # Operation 8893: insert
    tree[8895] = 'value_196193'
    reference[8895] = 'value_196193'
    assert tree.invariants(), "Invariants failed at step 8893"

    # Operation 8894: delete
    del tree[8726]
    del reference[8726]
    assert tree.invariants(), "Invariants failed at step 8894"

    # Operation 8895: get
    assert tree.invariants(), "Invariants failed at step 8895"

    # Operation 8896: delete
    del tree[4909]
    del reference[4909]
    assert tree.invariants(), "Invariants failed at step 8896"

    # Operation 8897: delete
    del tree[8895]
    del reference[8895]
    assert tree.invariants(), "Invariants failed at step 8897"

    # Operation 8898: delete
    del tree[9006]
    del reference[9006]
    assert tree.invariants(), "Invariants failed at step 8898"

    # Operation 8899: update
    tree[2883] = 'value_560204'
    reference[2883] = 'value_560204'
    assert tree.invariants(), "Invariants failed at step 8899"

    # Operation 8900: delete
    del tree[2883]
    del reference[2883]
    assert tree.invariants(), "Invariants failed at step 8900"

    # Operation 8901: insert
    tree[8974] = 'value_665942'
    reference[8974] = 'value_665942'
    assert tree.invariants(), "Invariants failed at step 8901"

    # Operation 8902: get
    assert tree.invariants(), "Invariants failed at step 8902"

    # Operation 8903: get
    assert tree.invariants(), "Invariants failed at step 8903"

    # Operation 8904: update
    tree[8974] = 'value_436759'
    reference[8974] = 'value_436759'
    assert tree.invariants(), "Invariants failed at step 8904"

    # Operation 8905: insert
    tree[9254] = 'value_51079'
    reference[9254] = 'value_51079'
    assert tree.invariants(), "Invariants failed at step 8905"

    # Operation 8906: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8906"

    # Operation 8907: insert
    tree[8633] = 'value_714152'
    reference[8633] = 'value_714152'
    assert tree.invariants(), "Invariants failed at step 8907"

    # Operation 8908: update
    tree[8974] = 'value_740343'
    reference[8974] = 'value_740343'
    assert tree.invariants(), "Invariants failed at step 8908"

    # Operation 8909: delete
    del tree[8974]
    del reference[8974]
    assert tree.invariants(), "Invariants failed at step 8909"

    # Operation 8910: delete
    del tree[8633]
    del reference[8633]
    assert tree.invariants(), "Invariants failed at step 8910"

    # Operation 8911: update
    tree[9254] = 'value_695874'
    reference[9254] = 'value_695874'
    assert tree.invariants(), "Invariants failed at step 8911"

    # Operation 8912: delete
    del tree[9254]
    del reference[9254]
    assert tree.invariants(), "Invariants failed at step 8912"

    # Operation 8913: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8913"

    # Operation 8914: get
    assert tree.invariants(), "Invariants failed at step 8914"

    # Operation 8915: get
    assert tree.invariants(), "Invariants failed at step 8915"

    # Operation 8916: insert
    tree[5339] = 'value_154206'
    reference[5339] = 'value_154206'
    assert tree.invariants(), "Invariants failed at step 8916"

    # Operation 8917: update
    tree[5339] = 'value_212062'
    reference[5339] = 'value_212062'
    assert tree.invariants(), "Invariants failed at step 8917"

    # Operation 8918: get
    assert tree.invariants(), "Invariants failed at step 8918"

    # Operation 8919: get
    assert tree.invariants(), "Invariants failed at step 8919"

    # Operation 8920: insert
    tree[4760] = 'value_451998'
    reference[4760] = 'value_451998'
    assert tree.invariants(), "Invariants failed at step 8920"

    # Operation 8921: delete
    del tree[5339]
    del reference[5339]
    assert tree.invariants(), "Invariants failed at step 8921"

    # Operation 8922: update
    tree[4760] = 'value_301405'
    reference[4760] = 'value_301405'
    assert tree.invariants(), "Invariants failed at step 8922"

    # Operation 8923: insert
    tree[1823] = 'value_51193'
    reference[1823] = 'value_51193'
    assert tree.invariants(), "Invariants failed at step 8923"

    # Operation 8924: insert
    tree[9493] = 'value_295678'
    reference[9493] = 'value_295678'
    assert tree.invariants(), "Invariants failed at step 8924"

    # Operation 8925: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8925"

    # Operation 8926: insert
    tree[7225] = 'value_944176'
    reference[7225] = 'value_944176'
    assert tree.invariants(), "Invariants failed at step 8926"

    # Operation 8927: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8927"

    # Operation 8928: get
    assert tree.invariants(), "Invariants failed at step 8928"

    # Operation 8929: delete
    del tree[7225]
    del reference[7225]
    assert tree.invariants(), "Invariants failed at step 8929"

    # Operation 8930: insert
    tree[6127] = 'value_792064'
    reference[6127] = 'value_792064'
    assert tree.invariants(), "Invariants failed at step 8930"

    # Operation 8931: insert
    tree[2525] = 'value_964363'
    reference[2525] = 'value_964363'
    assert tree.invariants(), "Invariants failed at step 8931"

    # Operation 8932: batch_delete
    keys_to_delete = [4760, 9493, 7512, 2525]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8932"

    # Operation 8933: get
    assert tree.invariants(), "Invariants failed at step 8933"

    # Operation 8934: update
    tree[1823] = 'value_67814'
    reference[1823] = 'value_67814'
    assert tree.invariants(), "Invariants failed at step 8934"

    # Operation 8935: insert
    tree[6394] = 'value_728987'
    reference[6394] = 'value_728987'
    assert tree.invariants(), "Invariants failed at step 8935"

    # Operation 8936: get
    assert tree.invariants(), "Invariants failed at step 8936"

    # Operation 8937: get
    assert tree.invariants(), "Invariants failed at step 8937"

    # Operation 8938: insert
    tree[802] = 'value_457662'
    reference[802] = 'value_457662'
    assert tree.invariants(), "Invariants failed at step 8938"

    # Operation 8939: update
    tree[6394] = 'value_556897'
    reference[6394] = 'value_556897'
    assert tree.invariants(), "Invariants failed at step 8939"

    # Operation 8940: update
    tree[802] = 'value_852439'
    reference[802] = 'value_852439'
    assert tree.invariants(), "Invariants failed at step 8940"

    # Operation 8941: insert
    tree[6899] = 'value_725319'
    reference[6899] = 'value_725319'
    assert tree.invariants(), "Invariants failed at step 8941"

    # Operation 8942: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8942"

    # Operation 8943: get
    assert tree.invariants(), "Invariants failed at step 8943"

    # Operation 8944: insert
    tree[4260] = 'value_278792'
    reference[4260] = 'value_278792'
    assert tree.invariants(), "Invariants failed at step 8944"

    # Operation 8945: insert
    tree[2430] = 'value_322335'
    reference[2430] = 'value_322335'
    assert tree.invariants(), "Invariants failed at step 8945"

    # Operation 8946: insert
    tree[8790] = 'value_386256'
    reference[8790] = 'value_386256'
    assert tree.invariants(), "Invariants failed at step 8946"

    # Operation 8947: insert
    tree[3624] = 'value_966466'
    reference[3624] = 'value_966466'
    assert tree.invariants(), "Invariants failed at step 8947"

    # Operation 8948: get
    assert tree.invariants(), "Invariants failed at step 8948"

    # Operation 8949: delete
    del tree[8790]
    del reference[8790]
    assert tree.invariants(), "Invariants failed at step 8949"

    # Operation 8950: delete
    del tree[6394]
    del reference[6394]
    assert tree.invariants(), "Invariants failed at step 8950"

    # Operation 8951: update
    tree[1823] = 'value_658112'
    reference[1823] = 'value_658112'
    assert tree.invariants(), "Invariants failed at step 8951"

    # Operation 8952: get
    assert tree.invariants(), "Invariants failed at step 8952"

    # Operation 8953: delete
    del tree[3624]
    del reference[3624]
    assert tree.invariants(), "Invariants failed at step 8953"

    # Operation 8954: delete
    del tree[2430]
    del reference[2430]
    assert tree.invariants(), "Invariants failed at step 8954"

    # Operation 8955: get
    assert tree.invariants(), "Invariants failed at step 8955"

    # Operation 8956: insert
    tree[4672] = 'value_989607'
    reference[4672] = 'value_989607'
    assert tree.invariants(), "Invariants failed at step 8956"

    # Operation 8957: batch_delete
    keys_to_delete = [802, 6899, 1823]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8957"

    # Operation 8958: insert
    tree[3204] = 'value_806080'
    reference[3204] = 'value_806080'
    assert tree.invariants(), "Invariants failed at step 8958"

    # Operation 8959: update
    tree[3204] = 'value_970983'
    reference[3204] = 'value_970983'
    assert tree.invariants(), "Invariants failed at step 8959"

    # Operation 8960: insert
    tree[2713] = 'value_236880'
    reference[2713] = 'value_236880'
    assert tree.invariants(), "Invariants failed at step 8960"

    # Operation 8961: batch_delete
    keys_to_delete = [1561, 2713, 4260, 6127]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8961"

    # Operation 8962: get
    assert tree.invariants(), "Invariants failed at step 8962"

    # Operation 8963: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 8963"

    # Operation 8964: insert
    tree[5362] = 'value_355905'
    reference[5362] = 'value_355905'
    assert tree.invariants(), "Invariants failed at step 8964"

    # Operation 8965: delete
    del tree[5362]
    del reference[5362]
    assert tree.invariants(), "Invariants failed at step 8965"

    # Operation 8966: insert
    tree[4336] = 'value_97292'
    reference[4336] = 'value_97292'
    assert tree.invariants(), "Invariants failed at step 8966"

    # Operation 8967: insert
    tree[5378] = 'value_107112'
    reference[5378] = 'value_107112'
    assert tree.invariants(), "Invariants failed at step 8967"

    # Operation 8968: get
    assert tree.invariants(), "Invariants failed at step 8968"

    # Operation 8969: insert
    tree[432] = 'value_412489'
    reference[432] = 'value_412489'
    assert tree.invariants(), "Invariants failed at step 8969"

    # Operation 8970: get
    assert tree.invariants(), "Invariants failed at step 8970"

    # Operation 8971: insert
    tree[7424] = 'value_502431'
    reference[7424] = 'value_502431'
    assert tree.invariants(), "Invariants failed at step 8971"

    # Operation 8972: update
    tree[3204] = 'value_448092'
    reference[3204] = 'value_448092'
    assert tree.invariants(), "Invariants failed at step 8972"

    # Operation 8973: delete
    del tree[7424]
    del reference[7424]
    assert tree.invariants(), "Invariants failed at step 8973"

    # Operation 8974: update
    tree[4336] = 'value_297089'
    reference[4336] = 'value_297089'
    assert tree.invariants(), "Invariants failed at step 8974"

    # Operation 8975: insert
    tree[5871] = 'value_90603'
    reference[5871] = 'value_90603'
    assert tree.invariants(), "Invariants failed at step 8975"

    # Operation 8976: insert
    tree[8473] = 'value_268604'
    reference[8473] = 'value_268604'
    assert tree.invariants(), "Invariants failed at step 8976"

    # Operation 8977: get
    assert tree.invariants(), "Invariants failed at step 8977"

    # Operation 8978: insert
    tree[8069] = 'value_222661'
    reference[8069] = 'value_222661'
    assert tree.invariants(), "Invariants failed at step 8978"

    # Operation 8979: batch_delete
    keys_to_delete = [5378, 3204, 8069, 5871, 2672]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 8979"

    # Operation 8980: delete
    del tree[432]
    del reference[432]
    assert tree.invariants(), "Invariants failed at step 8980"

    # Operation 8981: delete
    del tree[4672]
    del reference[4672]
    assert tree.invariants(), "Invariants failed at step 8981"

    # Operation 8982: insert
    tree[7212] = 'value_792309'
    reference[7212] = 'value_792309'
    assert tree.invariants(), "Invariants failed at step 8982"

    # Operation 8983: insert
    tree[3341] = 'value_529283'
    reference[3341] = 'value_529283'
    assert tree.invariants(), "Invariants failed at step 8983"

    # Operation 8984: get
    assert tree.invariants(), "Invariants failed at step 8984"

    # Operation 8985: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 8985"

    # Operation 8986: get
    assert tree.invariants(), "Invariants failed at step 8986"

    # Operation 8987: delete
    del tree[8473]
    del reference[8473]
    assert tree.invariants(), "Invariants failed at step 8987"

    # Operation 8988: insert
    tree[6680] = 'value_995643'
    reference[6680] = 'value_995643'
    assert tree.invariants(), "Invariants failed at step 8988"

    # Operation 8989: delete
    del tree[4336]
    del reference[4336]
    assert tree.invariants(), "Invariants failed at step 8989"

    # Operation 8990: delete
    del tree[7212]
    del reference[7212]
    assert tree.invariants(), "Invariants failed at step 8990"

    # Operation 8991: get
    assert tree.invariants(), "Invariants failed at step 8991"

    # Operation 8992: get
    assert tree.invariants(), "Invariants failed at step 8992"

    # Operation 8993: delete
    del tree[3341]
    del reference[3341]
    assert tree.invariants(), "Invariants failed at step 8993"

    # Operation 8994: insert
    tree[1444] = 'value_648511'
    reference[1444] = 'value_648511'
    assert tree.invariants(), "Invariants failed at step 8994"

    # Operation 8995: get
    assert tree.invariants(), "Invariants failed at step 8995"

    # Operation 8996: insert
    tree[1511] = 'value_732471'
    reference[1511] = 'value_732471'
    assert tree.invariants(), "Invariants failed at step 8996"

    # Operation 8997: delete
    del tree[1511]
    del reference[1511]
    assert tree.invariants(), "Invariants failed at step 8997"

    # Operation 8998: insert
    tree[4955] = 'value_202927'
    reference[4955] = 'value_202927'
    assert tree.invariants(), "Invariants failed at step 8998"

    # Operation 8999: insert
    tree[7580] = 'value_299054'
    reference[7580] = 'value_299054'
    assert tree.invariants(), "Invariants failed at step 8999"

    # Operation 9000: delete
    del tree[4955]
    del reference[4955]
    assert tree.invariants(), "Invariants failed at step 9000"

    # Operation 9001: delete
    del tree[7580]
    del reference[7580]
    assert tree.invariants(), "Invariants failed at step 9001"

    # Operation 9002: insert
    tree[4995] = 'value_363058'
    reference[4995] = 'value_363058'
    assert tree.invariants(), "Invariants failed at step 9002"

    # Operation 9003: delete
    del tree[4995]
    del reference[4995]
    assert tree.invariants(), "Invariants failed at step 9003"

    # Operation 9004: update
    tree[6680] = 'value_14280'
    reference[6680] = 'value_14280'
    assert tree.invariants(), "Invariants failed at step 9004"

    # Operation 9005: delete
    del tree[6680]
    del reference[6680]
    assert tree.invariants(), "Invariants failed at step 9005"

    # Operation 9006: delete
    del tree[1444]
    del reference[1444]
    assert tree.invariants(), "Invariants failed at step 9006"

    # Operation 9007: insert
    tree[2999] = 'value_151958'
    reference[2999] = 'value_151958'
    assert tree.invariants(), "Invariants failed at step 9007"

    # Operation 9008: insert
    tree[9802] = 'value_346405'
    reference[9802] = 'value_346405'
    assert tree.invariants(), "Invariants failed at step 9008"

    # Operation 9009: insert
    tree[3923] = 'value_855157'
    reference[3923] = 'value_855157'
    assert tree.invariants(), "Invariants failed at step 9009"

    # Operation 9010: get
    assert tree.invariants(), "Invariants failed at step 9010"

    # Operation 9011: get
    assert tree.invariants(), "Invariants failed at step 9011"

    # Operation 9012: delete
    del tree[3923]
    del reference[3923]
    assert tree.invariants(), "Invariants failed at step 9012"

    # Operation 9013: insert
    tree[9390] = 'value_782547'
    reference[9390] = 'value_782547'
    assert tree.invariants(), "Invariants failed at step 9013"

    # Operation 9014: delete
    del tree[2999]
    del reference[2999]
    assert tree.invariants(), "Invariants failed at step 9014"

    # Operation 9015: insert
    tree[7311] = 'value_424453'
    reference[7311] = 'value_424453'
    assert tree.invariants(), "Invariants failed at step 9015"

    # Operation 9016: update
    tree[9802] = 'value_48730'
    reference[9802] = 'value_48730'
    assert tree.invariants(), "Invariants failed at step 9016"

    # Operation 9017: delete
    del tree[9390]
    del reference[9390]
    assert tree.invariants(), "Invariants failed at step 9017"

    # Operation 9018: update
    tree[9802] = 'value_547228'
    reference[9802] = 'value_547228'
    assert tree.invariants(), "Invariants failed at step 9018"

    # Operation 9019: update
    tree[9802] = 'value_979685'
    reference[9802] = 'value_979685'
    assert tree.invariants(), "Invariants failed at step 9019"

    # Operation 9020: insert
    tree[6791] = 'value_605519'
    reference[6791] = 'value_605519'
    assert tree.invariants(), "Invariants failed at step 9020"

    # Operation 9021: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9021"

    # Operation 9022: insert
    tree[3229] = 'value_604246'
    reference[3229] = 'value_604246'
    assert tree.invariants(), "Invariants failed at step 9022"

    # Operation 9023: update
    tree[9802] = 'value_30488'
    reference[9802] = 'value_30488'
    assert tree.invariants(), "Invariants failed at step 9023"

    # Operation 9024: delete
    del tree[9802]
    del reference[9802]
    assert tree.invariants(), "Invariants failed at step 9024"

    # Operation 9025: insert
    tree[5150] = 'value_731318'
    reference[5150] = 'value_731318'
    assert tree.invariants(), "Invariants failed at step 9025"

    # Operation 9026: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9026"

    # Operation 9027: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9027"

    # Operation 9028: update
    tree[5150] = 'value_24427'
    reference[5150] = 'value_24427'
    assert tree.invariants(), "Invariants failed at step 9028"

    # Operation 9029: insert
    tree[2444] = 'value_881884'
    reference[2444] = 'value_881884'
    assert tree.invariants(), "Invariants failed at step 9029"

    # Operation 9030: delete
    del tree[6791]
    del reference[6791]
    assert tree.invariants(), "Invariants failed at step 9030"

    # Operation 9031: delete
    del tree[5150]
    del reference[5150]
    assert tree.invariants(), "Invariants failed at step 9031"

    # Operation 9032: delete
    del tree[7311]
    del reference[7311]
    assert tree.invariants(), "Invariants failed at step 9032"

    # Operation 9033: update
    tree[2444] = 'value_668633'
    reference[2444] = 'value_668633'
    assert tree.invariants(), "Invariants failed at step 9033"

    # Operation 9034: update
    tree[3229] = 'value_392540'
    reference[3229] = 'value_392540'
    assert tree.invariants(), "Invariants failed at step 9034"

    # Operation 9035: insert
    tree[1292] = 'value_353568'
    reference[1292] = 'value_353568'
    assert tree.invariants(), "Invariants failed at step 9035"

    # Operation 9036: get
    assert tree.invariants(), "Invariants failed at step 9036"

    # Operation 9037: delete
    del tree[2444]
    del reference[2444]
    assert tree.invariants(), "Invariants failed at step 9037"

    # Operation 9038: update
    tree[1292] = 'value_880031'
    reference[1292] = 'value_880031'
    assert tree.invariants(), "Invariants failed at step 9038"

    # Operation 9039: insert
    tree[6743] = 'value_284687'
    reference[6743] = 'value_284687'
    assert tree.invariants(), "Invariants failed at step 9039"

    # Operation 9040: delete
    del tree[3229]
    del reference[3229]
    assert tree.invariants(), "Invariants failed at step 9040"

    # Operation 9041: get
    assert tree.invariants(), "Invariants failed at step 9041"

    # Operation 9042: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9042"

    # Operation 9043: update
    tree[1292] = 'value_394317'
    reference[1292] = 'value_394317'
    assert tree.invariants(), "Invariants failed at step 9043"

    # Operation 9044: delete
    del tree[6743]
    del reference[6743]
    assert tree.invariants(), "Invariants failed at step 9044"

    # Operation 9045: get
    assert tree.invariants(), "Invariants failed at step 9045"

    # Operation 9046: delete
    del tree[1292]
    del reference[1292]
    assert tree.invariants(), "Invariants failed at step 9046"

    # Operation 9047: insert
    tree[9254] = 'value_368138'
    reference[9254] = 'value_368138'
    assert tree.invariants(), "Invariants failed at step 9047"

    # Operation 9048: update
    tree[9254] = 'value_774179'
    reference[9254] = 'value_774179'
    assert tree.invariants(), "Invariants failed at step 9048"

    # Operation 9049: update
    tree[9254] = 'value_970548'
    reference[9254] = 'value_970548'
    assert tree.invariants(), "Invariants failed at step 9049"

    # Operation 9050: insert
    tree[2261] = 'value_890253'
    reference[2261] = 'value_890253'
    assert tree.invariants(), "Invariants failed at step 9050"

    # Operation 9051: insert
    tree[394] = 'value_968303'
    reference[394] = 'value_968303'
    assert tree.invariants(), "Invariants failed at step 9051"

    # Operation 9052: insert
    tree[6013] = 'value_305582'
    reference[6013] = 'value_305582'
    assert tree.invariants(), "Invariants failed at step 9052"

    # Operation 9053: get
    assert tree.invariants(), "Invariants failed at step 9053"

    # Operation 9054: get
    assert tree.invariants(), "Invariants failed at step 9054"

    # Operation 9055: delete
    del tree[2261]
    del reference[2261]
    assert tree.invariants(), "Invariants failed at step 9055"

    # Operation 9056: update
    tree[394] = 'value_988044'
    reference[394] = 'value_988044'
    assert tree.invariants(), "Invariants failed at step 9056"

    # Operation 9057: insert
    tree[6154] = 'value_221405'
    reference[6154] = 'value_221405'
    assert tree.invariants(), "Invariants failed at step 9057"

    # Operation 9058: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9058"

    # Operation 9059: delete
    del tree[6154]
    del reference[6154]
    assert tree.invariants(), "Invariants failed at step 9059"

    # Operation 9060: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9060"

    # Operation 9061: insert
    tree[638] = 'value_302379'
    reference[638] = 'value_302379'
    assert tree.invariants(), "Invariants failed at step 9061"

    # Operation 9062: insert
    tree[5746] = 'value_611223'
    reference[5746] = 'value_611223'
    assert tree.invariants(), "Invariants failed at step 9062"

    # Operation 9063: insert
    tree[5412] = 'value_24603'
    reference[5412] = 'value_24603'
    assert tree.invariants(), "Invariants failed at step 9063"

    # Operation 9064: get
    assert tree.invariants(), "Invariants failed at step 9064"

    # Operation 9065: insert
    tree[7986] = 'value_227491'
    reference[7986] = 'value_227491'
    assert tree.invariants(), "Invariants failed at step 9065"

    # Operation 9066: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9066"

    # Operation 9067: batch_delete
    keys_to_delete = [5412, 394, 5746, 6738, 638]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9067"

    # Operation 9068: insert
    tree[5332] = 'value_571653'
    reference[5332] = 'value_571653'
    assert tree.invariants(), "Invariants failed at step 9068"

    # Operation 9069: insert
    tree[786] = 'value_785064'
    reference[786] = 'value_785064'
    assert tree.invariants(), "Invariants failed at step 9069"

    # Operation 9070: insert
    tree[943] = 'value_157032'
    reference[943] = 'value_157032'
    assert tree.invariants(), "Invariants failed at step 9070"

    # Operation 9071: get
    assert tree.invariants(), "Invariants failed at step 9071"

    # Operation 9072: insert
    tree[105] = 'value_158425'
    reference[105] = 'value_158425'
    assert tree.invariants(), "Invariants failed at step 9072"

    # Operation 9073: delete
    del tree[786]
    del reference[786]
    assert tree.invariants(), "Invariants failed at step 9073"

    # Operation 9074: insert
    tree[7554] = 'value_693953'
    reference[7554] = 'value_693953'
    assert tree.invariants(), "Invariants failed at step 9074"

    # Operation 9075: batch_delete
    keys_to_delete = [7554, 5958, 9254, 7637, 6013]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9075"

    # Operation 9076: insert
    tree[5434] = 'value_759738'
    reference[5434] = 'value_759738'
    assert tree.invariants(), "Invariants failed at step 9076"

    # Operation 9077: batch_delete
    keys_to_delete = [4712, 5434, 7986, 47]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9077"

    # Operation 9078: get
    assert tree.invariants(), "Invariants failed at step 9078"

    # Operation 9079: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9079"

    # Operation 9080: delete
    del tree[5332]
    del reference[5332]
    assert tree.invariants(), "Invariants failed at step 9080"

    # Operation 9081: delete
    del tree[105]
    del reference[105]
    assert tree.invariants(), "Invariants failed at step 9081"

    # Operation 9082: delete
    del tree[943]
    del reference[943]
    assert tree.invariants(), "Invariants failed at step 9082"

    # Operation 9083: insert
    tree[9230] = 'value_908663'
    reference[9230] = 'value_908663'
    assert tree.invariants(), "Invariants failed at step 9083"

    # Operation 9084: update
    tree[9230] = 'value_837613'
    reference[9230] = 'value_837613'
    assert tree.invariants(), "Invariants failed at step 9084"

    # Operation 9085: update
    tree[9230] = 'value_170026'
    reference[9230] = 'value_170026'
    assert tree.invariants(), "Invariants failed at step 9085"

    # Operation 9086: delete
    del tree[9230]
    del reference[9230]
    assert tree.invariants(), "Invariants failed at step 9086"

    # Operation 9087: insert
    tree[4949] = 'value_809531'
    reference[4949] = 'value_809531'
    assert tree.invariants(), "Invariants failed at step 9087"

    # Operation 9088: delete
    del tree[4949]
    del reference[4949]
    assert tree.invariants(), "Invariants failed at step 9088"

    # Operation 9089: insert
    tree[1148] = 'value_524094'
    reference[1148] = 'value_524094'
    assert tree.invariants(), "Invariants failed at step 9089"

    # Operation 9090: update
    tree[1148] = 'value_387821'
    reference[1148] = 'value_387821'
    assert tree.invariants(), "Invariants failed at step 9090"

    # Operation 9091: get
    assert tree.invariants(), "Invariants failed at step 9091"

    # Operation 9092: delete
    del tree[1148]
    del reference[1148]
    assert tree.invariants(), "Invariants failed at step 9092"

    # Operation 9093: insert
    tree[8176] = 'value_275110'
    reference[8176] = 'value_275110'
    assert tree.invariants(), "Invariants failed at step 9093"

    # Operation 9094: update
    tree[8176] = 'value_726125'
    reference[8176] = 'value_726125'
    assert tree.invariants(), "Invariants failed at step 9094"

    # Operation 9095: insert
    tree[4873] = 'value_404216'
    reference[4873] = 'value_404216'
    assert tree.invariants(), "Invariants failed at step 9095"

    # Operation 9096: insert
    tree[1423] = 'value_248508'
    reference[1423] = 'value_248508'
    assert tree.invariants(), "Invariants failed at step 9096"

    # Operation 9097: get
    assert tree.invariants(), "Invariants failed at step 9097"

    # Operation 9098: get
    assert tree.invariants(), "Invariants failed at step 9098"

    # Operation 9099: get
    assert tree.invariants(), "Invariants failed at step 9099"

    # Operation 9100: delete
    del tree[4873]
    del reference[4873]
    assert tree.invariants(), "Invariants failed at step 9100"

    # Operation 9101: delete
    del tree[8176]
    del reference[8176]
    assert tree.invariants(), "Invariants failed at step 9101"

    # Operation 9102: get
    assert tree.invariants(), "Invariants failed at step 9102"

    # Operation 9103: get
    assert tree.invariants(), "Invariants failed at step 9103"

    # Operation 9104: delete
    del tree[1423]
    del reference[1423]
    assert tree.invariants(), "Invariants failed at step 9104"

    # Operation 9105: get
    assert tree.invariants(), "Invariants failed at step 9105"

    # Operation 9106: insert
    tree[3154] = 'value_785607'
    reference[3154] = 'value_785607'
    assert tree.invariants(), "Invariants failed at step 9106"

    # Operation 9107: delete
    del tree[3154]
    del reference[3154]
    assert tree.invariants(), "Invariants failed at step 9107"

    # Operation 9108: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9108"

    # Operation 9109: insert
    tree[1239] = 'value_567033'
    reference[1239] = 'value_567033'
    assert tree.invariants(), "Invariants failed at step 9109"

    # Operation 9110: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9110"

    # Operation 9111: insert
    tree[2509] = 'value_157685'
    reference[2509] = 'value_157685'
    assert tree.invariants(), "Invariants failed at step 9111"

    # Operation 9112: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9112"

    # Operation 9113: insert
    tree[6869] = 'value_173794'
    reference[6869] = 'value_173794'
    assert tree.invariants(), "Invariants failed at step 9113"

    # Operation 9114: insert
    tree[6856] = 'value_113502'
    reference[6856] = 'value_113502'
    assert tree.invariants(), "Invariants failed at step 9114"

    # Operation 9115: delete
    del tree[2509]
    del reference[2509]
    assert tree.invariants(), "Invariants failed at step 9115"

    # Operation 9116: delete
    del tree[1239]
    del reference[1239]
    assert tree.invariants(), "Invariants failed at step 9116"

    # Operation 9117: delete
    del tree[6856]
    del reference[6856]
    assert tree.invariants(), "Invariants failed at step 9117"

    # Operation 9118: insert
    tree[1278] = 'value_430715'
    reference[1278] = 'value_430715'
    assert tree.invariants(), "Invariants failed at step 9118"

    # Operation 9119: insert
    tree[3198] = 'value_366702'
    reference[3198] = 'value_366702'
    assert tree.invariants(), "Invariants failed at step 9119"

    # Operation 9120: insert
    tree[1870] = 'value_514707'
    reference[1870] = 'value_514707'
    assert tree.invariants(), "Invariants failed at step 9120"

    # Operation 9121: insert
    tree[6780] = 'value_143265'
    reference[6780] = 'value_143265'
    assert tree.invariants(), "Invariants failed at step 9121"

    # Operation 9122: insert
    tree[9286] = 'value_889547'
    reference[9286] = 'value_889547'
    assert tree.invariants(), "Invariants failed at step 9122"

    # Operation 9123: delete
    del tree[1278]
    del reference[1278]
    assert tree.invariants(), "Invariants failed at step 9123"

    # Operation 9124: insert
    tree[4241] = 'value_883184'
    reference[4241] = 'value_883184'
    assert tree.invariants(), "Invariants failed at step 9124"

    # Operation 9125: get
    assert tree.invariants(), "Invariants failed at step 9125"

    # Operation 9126: batch_delete
    keys_to_delete = [9191, 1870, 1199, 6780, 3198]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9126"

    # Operation 9127: insert
    tree[5501] = 'value_880832'
    reference[5501] = 'value_880832'
    assert tree.invariants(), "Invariants failed at step 9127"

    # Operation 9128: delete
    del tree[5501]
    del reference[5501]
    assert tree.invariants(), "Invariants failed at step 9128"

    # Operation 9129: delete
    del tree[4241]
    del reference[4241]
    assert tree.invariants(), "Invariants failed at step 9129"

    # Operation 9130: insert
    tree[3359] = 'value_174208'
    reference[3359] = 'value_174208'
    assert tree.invariants(), "Invariants failed at step 9130"

    # Operation 9131: delete
    del tree[9286]
    del reference[9286]
    assert tree.invariants(), "Invariants failed at step 9131"

    # Operation 9132: get
    assert tree.invariants(), "Invariants failed at step 9132"

    # Operation 9133: insert
    tree[4917] = 'value_317428'
    reference[4917] = 'value_317428'
    assert tree.invariants(), "Invariants failed at step 9133"

    # Operation 9134: delete
    del tree[6869]
    del reference[6869]
    assert tree.invariants(), "Invariants failed at step 9134"

    # Operation 9135: update
    tree[4917] = 'value_669963'
    reference[4917] = 'value_669963'
    assert tree.invariants(), "Invariants failed at step 9135"

    # Operation 9136: insert
    tree[3666] = 'value_558705'
    reference[3666] = 'value_558705'
    assert tree.invariants(), "Invariants failed at step 9136"

    # Operation 9137: insert
    tree[4529] = 'value_656564'
    reference[4529] = 'value_656564'
    assert tree.invariants(), "Invariants failed at step 9137"

    # Operation 9138: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9138"

    # Operation 9139: get
    assert tree.invariants(), "Invariants failed at step 9139"

    # Operation 9140: update
    tree[3359] = 'value_958516'
    reference[3359] = 'value_958516'
    assert tree.invariants(), "Invariants failed at step 9140"

    # Operation 9141: insert
    tree[4963] = 'value_316640'
    reference[4963] = 'value_316640'
    assert tree.invariants(), "Invariants failed at step 9141"

    # Operation 9142: insert
    tree[4206] = 'value_315368'
    reference[4206] = 'value_315368'
    assert tree.invariants(), "Invariants failed at step 9142"

    # Operation 9143: insert
    tree[1889] = 'value_151241'
    reference[1889] = 'value_151241'
    assert tree.invariants(), "Invariants failed at step 9143"

    # Operation 9144: get
    assert tree.invariants(), "Invariants failed at step 9144"

    # Operation 9145: get
    assert tree.invariants(), "Invariants failed at step 9145"

    # Operation 9146: insert
    tree[1781] = 'value_50118'
    reference[1781] = 'value_50118'
    assert tree.invariants(), "Invariants failed at step 9146"

    # Operation 9147: insert
    tree[6765] = 'value_170858'
    reference[6765] = 'value_170858'
    assert tree.invariants(), "Invariants failed at step 9147"

    # Operation 9148: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9148"

    # Operation 9149: get
    assert tree.invariants(), "Invariants failed at step 9149"

    # Operation 9150: insert
    tree[5064] = 'value_711295'
    reference[5064] = 'value_711295'
    assert tree.invariants(), "Invariants failed at step 9150"

    # Operation 9151: delete
    del tree[5064]
    del reference[5064]
    assert tree.invariants(), "Invariants failed at step 9151"

    # Operation 9152: insert
    tree[8284] = 'value_852039'
    reference[8284] = 'value_852039'
    assert tree.invariants(), "Invariants failed at step 9152"

    # Operation 9153: delete
    del tree[4917]
    del reference[4917]
    assert tree.invariants(), "Invariants failed at step 9153"

    # Operation 9154: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9154"

    # Operation 9155: insert
    tree[5060] = 'value_470465'
    reference[5060] = 'value_470465'
    assert tree.invariants(), "Invariants failed at step 9155"

    # Operation 9156: delete
    del tree[4529]
    del reference[4529]
    assert tree.invariants(), "Invariants failed at step 9156"

    # Operation 9157: update
    tree[6765] = 'value_896943'
    reference[6765] = 'value_896943'
    assert tree.invariants(), "Invariants failed at step 9157"

    # Operation 9158: get
    assert tree.invariants(), "Invariants failed at step 9158"

    # Operation 9159: delete
    del tree[6765]
    del reference[6765]
    assert tree.invariants(), "Invariants failed at step 9159"

    # Operation 9160: insert
    tree[936] = 'value_901574'
    reference[936] = 'value_901574'
    assert tree.invariants(), "Invariants failed at step 9160"

    # Operation 9161: get
    assert tree.invariants(), "Invariants failed at step 9161"

    # Operation 9162: update
    tree[4963] = 'value_340874'
    reference[4963] = 'value_340874'
    assert tree.invariants(), "Invariants failed at step 9162"

    # Operation 9163: get
    assert tree.invariants(), "Invariants failed at step 9163"

    # Operation 9164: update
    tree[1781] = 'value_938199'
    reference[1781] = 'value_938199'
    assert tree.invariants(), "Invariants failed at step 9164"

    # Operation 9165: batch_delete
    keys_to_delete = [1889, 936, 9805, 3254, 8284, 3359]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9165"

    # Operation 9166: get
    assert tree.invariants(), "Invariants failed at step 9166"

    # Operation 9167: insert
    tree[6332] = 'value_421463'
    reference[6332] = 'value_421463'
    assert tree.invariants(), "Invariants failed at step 9167"

    # Operation 9168: delete
    del tree[3666]
    del reference[3666]
    assert tree.invariants(), "Invariants failed at step 9168"

    # Operation 9169: get
    assert tree.invariants(), "Invariants failed at step 9169"

    # Operation 9170: batch_delete
    keys_to_delete = [9615, 4963, 4206, 9750]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9170"

    # Operation 9171: insert
    tree[2925] = 'value_123507'
    reference[2925] = 'value_123507'
    assert tree.invariants(), "Invariants failed at step 9171"

    # Operation 9172: delete
    del tree[6332]
    del reference[6332]
    assert tree.invariants(), "Invariants failed at step 9172"

    # Operation 9173: insert
    tree[9344] = 'value_67681'
    reference[9344] = 'value_67681'
    assert tree.invariants(), "Invariants failed at step 9173"

    # Operation 9174: insert
    tree[839] = 'value_479335'
    reference[839] = 'value_479335'
    assert tree.invariants(), "Invariants failed at step 9174"

    # Operation 9175: get
    assert tree.invariants(), "Invariants failed at step 9175"

    # Operation 9176: insert
    tree[2770] = 'value_826016'
    reference[2770] = 'value_826016'
    assert tree.invariants(), "Invariants failed at step 9176"

    # Operation 9177: insert
    tree[9919] = 'value_273881'
    reference[9919] = 'value_273881'
    assert tree.invariants(), "Invariants failed at step 9177"

    # Operation 9178: insert
    tree[7574] = 'value_116484'
    reference[7574] = 'value_116484'
    assert tree.invariants(), "Invariants failed at step 9178"

    # Operation 9179: delete
    del tree[5060]
    del reference[5060]
    assert tree.invariants(), "Invariants failed at step 9179"

    # Operation 9180: delete
    del tree[2925]
    del reference[2925]
    assert tree.invariants(), "Invariants failed at step 9180"

    # Operation 9181: batch_delete
    keys_to_delete = [2152, 1781, 7574, 839]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9181"

    # Operation 9182: update
    tree[2770] = 'value_800463'
    reference[2770] = 'value_800463'
    assert tree.invariants(), "Invariants failed at step 9182"

    # Operation 9183: get
    assert tree.invariants(), "Invariants failed at step 9183"

    # Operation 9184: update
    tree[9919] = 'value_983644'
    reference[9919] = 'value_983644'
    assert tree.invariants(), "Invariants failed at step 9184"

    # Operation 9185: get
    assert tree.invariants(), "Invariants failed at step 9185"

    # Operation 9186: insert
    tree[2120] = 'value_233364'
    reference[2120] = 'value_233364'
    assert tree.invariants(), "Invariants failed at step 9186"

    # Operation 9187: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9187"

    # Operation 9188: insert
    tree[7251] = 'value_685077'
    reference[7251] = 'value_685077'
    assert tree.invariants(), "Invariants failed at step 9188"

    # Operation 9189: insert
    tree[9697] = 'value_886469'
    reference[9697] = 'value_886469'
    assert tree.invariants(), "Invariants failed at step 9189"

    # Operation 9190: insert
    tree[4155] = 'value_677691'
    reference[4155] = 'value_677691'
    assert tree.invariants(), "Invariants failed at step 9190"

    # Operation 9191: insert
    tree[2800] = 'value_762045'
    reference[2800] = 'value_762045'
    assert tree.invariants(), "Invariants failed at step 9191"

    # Operation 9192: update
    tree[9919] = 'value_693680'
    reference[9919] = 'value_693680'
    assert tree.invariants(), "Invariants failed at step 9192"

    # Operation 9193: insert
    tree[1629] = 'value_299674'
    reference[1629] = 'value_299674'
    assert tree.invariants(), "Invariants failed at step 9193"

    # Operation 9194: delete
    del tree[2770]
    del reference[2770]
    assert tree.invariants(), "Invariants failed at step 9194"

    # Operation 9195: delete
    del tree[4155]
    del reference[4155]
    assert tree.invariants(), "Invariants failed at step 9195"

    # Operation 9196: get
    assert tree.invariants(), "Invariants failed at step 9196"

    # Operation 9197: insert
    tree[4520] = 'value_993936'
    reference[4520] = 'value_993936'
    assert tree.invariants(), "Invariants failed at step 9197"

    # Operation 9198: get
    assert tree.invariants(), "Invariants failed at step 9198"

    # Operation 9199: update
    tree[2800] = 'value_757691'
    reference[2800] = 'value_757691'
    assert tree.invariants(), "Invariants failed at step 9199"

    # Operation 9200: delete
    del tree[2800]
    del reference[2800]
    assert tree.invariants(), "Invariants failed at step 9200"

    # Operation 9201: update
    tree[9697] = 'value_976677'
    reference[9697] = 'value_976677'
    assert tree.invariants(), "Invariants failed at step 9201"

    # Operation 9202: batch_delete
    keys_to_delete = [4358, 4520, 9919, 1629, 4607]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9202"

    # Operation 9203: update
    tree[2120] = 'value_144761'
    reference[2120] = 'value_144761'
    assert tree.invariants(), "Invariants failed at step 9203"

    # Operation 9204: get
    assert tree.invariants(), "Invariants failed at step 9204"

    # Operation 9205: delete
    del tree[9697]
    del reference[9697]
    assert tree.invariants(), "Invariants failed at step 9205"

    # Operation 9206: insert
    tree[4485] = 'value_999081'
    reference[4485] = 'value_999081'
    assert tree.invariants(), "Invariants failed at step 9206"

    # Operation 9207: insert
    tree[1703] = 'value_31748'
    reference[1703] = 'value_31748'
    assert tree.invariants(), "Invariants failed at step 9207"

    # Operation 9208: delete
    del tree[7251]
    del reference[7251]
    assert tree.invariants(), "Invariants failed at step 9208"

    # Operation 9209: insert
    tree[5909] = 'value_459191'
    reference[5909] = 'value_459191'
    assert tree.invariants(), "Invariants failed at step 9209"

    # Operation 9210: update
    tree[4485] = 'value_198237'
    reference[4485] = 'value_198237'
    assert tree.invariants(), "Invariants failed at step 9210"

    # Operation 9211: get
    assert tree.invariants(), "Invariants failed at step 9211"

    # Operation 9212: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9212"

    # Operation 9213: insert
    tree[1155] = 'value_93398'
    reference[1155] = 'value_93398'
    assert tree.invariants(), "Invariants failed at step 9213"

    # Operation 9214: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9214"

    # Operation 9215: get
    assert tree.invariants(), "Invariants failed at step 9215"

    # Operation 9216: delete
    del tree[2120]
    del reference[2120]
    assert tree.invariants(), "Invariants failed at step 9216"

    # Operation 9217: delete
    del tree[9344]
    del reference[9344]
    assert tree.invariants(), "Invariants failed at step 9217"

    # Operation 9218: insert
    tree[4945] = 'value_127273'
    reference[4945] = 'value_127273'
    assert tree.invariants(), "Invariants failed at step 9218"

    # Operation 9219: delete
    del tree[5909]
    del reference[5909]
    assert tree.invariants(), "Invariants failed at step 9219"

    # Operation 9220: get
    assert tree.invariants(), "Invariants failed at step 9220"

    # Operation 9221: delete
    del tree[1155]
    del reference[1155]
    assert tree.invariants(), "Invariants failed at step 9221"

    # Operation 9222: update
    tree[4945] = 'value_950377'
    reference[4945] = 'value_950377'
    assert tree.invariants(), "Invariants failed at step 9222"

    # Operation 9223: update
    tree[4485] = 'value_286785'
    reference[4485] = 'value_286785'
    assert tree.invariants(), "Invariants failed at step 9223"

    # Operation 9224: get
    assert tree.invariants(), "Invariants failed at step 9224"

    # Operation 9225: insert
    tree[5707] = 'value_558198'
    reference[5707] = 'value_558198'
    assert tree.invariants(), "Invariants failed at step 9225"

    # Operation 9226: get
    assert tree.invariants(), "Invariants failed at step 9226"

    # Operation 9227: delete
    del tree[1703]
    del reference[1703]
    assert tree.invariants(), "Invariants failed at step 9227"

    # Operation 9228: delete
    del tree[4945]
    del reference[4945]
    assert tree.invariants(), "Invariants failed at step 9228"

    # Operation 9229: get
    assert tree.invariants(), "Invariants failed at step 9229"

    # Operation 9230: delete
    del tree[5707]
    del reference[5707]
    assert tree.invariants(), "Invariants failed at step 9230"

    # Operation 9231: insert
    tree[8679] = 'value_618595'
    reference[8679] = 'value_618595'
    assert tree.invariants(), "Invariants failed at step 9231"

    # Operation 9232: insert
    tree[3318] = 'value_889419'
    reference[3318] = 'value_889419'
    assert tree.invariants(), "Invariants failed at step 9232"

    # Operation 9233: insert
    tree[8048] = 'value_51830'
    reference[8048] = 'value_51830'
    assert tree.invariants(), "Invariants failed at step 9233"

    # Operation 9234: insert
    tree[9572] = 'value_599091'
    reference[9572] = 'value_599091'
    assert tree.invariants(), "Invariants failed at step 9234"

    # Operation 9235: insert
    tree[9429] = 'value_301506'
    reference[9429] = 'value_301506'
    assert tree.invariants(), "Invariants failed at step 9235"

    # Operation 9236: update
    tree[9572] = 'value_366440'
    reference[9572] = 'value_366440'
    assert tree.invariants(), "Invariants failed at step 9236"

    # Operation 9237: delete
    del tree[3318]
    del reference[3318]
    assert tree.invariants(), "Invariants failed at step 9237"

    # Operation 9238: insert
    tree[8603] = 'value_811865'
    reference[8603] = 'value_811865'
    assert tree.invariants(), "Invariants failed at step 9238"

    # Operation 9239: delete
    del tree[4485]
    del reference[4485]
    assert tree.invariants(), "Invariants failed at step 9239"

    # Operation 9240: get
    assert tree.invariants(), "Invariants failed at step 9240"

    # Operation 9241: insert
    tree[5623] = 'value_256033'
    reference[5623] = 'value_256033'
    assert tree.invariants(), "Invariants failed at step 9241"

    # Operation 9242: get
    assert tree.invariants(), "Invariants failed at step 9242"

    # Operation 9243: get
    assert tree.invariants(), "Invariants failed at step 9243"

    # Operation 9244: insert
    tree[7764] = 'value_158431'
    reference[7764] = 'value_158431'
    assert tree.invariants(), "Invariants failed at step 9244"

    # Operation 9245: delete
    del tree[8603]
    del reference[8603]
    assert tree.invariants(), "Invariants failed at step 9245"

    # Operation 9246: delete
    del tree[8679]
    del reference[8679]
    assert tree.invariants(), "Invariants failed at step 9246"

    # Operation 9247: get
    assert tree.invariants(), "Invariants failed at step 9247"

    # Operation 9248: delete
    del tree[8048]
    del reference[8048]
    assert tree.invariants(), "Invariants failed at step 9248"

    # Operation 9249: get
    assert tree.invariants(), "Invariants failed at step 9249"

    # Operation 9250: delete
    del tree[5623]
    del reference[5623]
    assert tree.invariants(), "Invariants failed at step 9250"

    # Operation 9251: insert
    tree[4607] = 'value_910943'
    reference[4607] = 'value_910943'
    assert tree.invariants(), "Invariants failed at step 9251"

    # Operation 9252: insert
    tree[7927] = 'value_228870'
    reference[7927] = 'value_228870'
    assert tree.invariants(), "Invariants failed at step 9252"

    # Operation 9253: get
    assert tree.invariants(), "Invariants failed at step 9253"

    # Operation 9254: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9254"

    # Operation 9255: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9255"

    # Operation 9256: delete
    del tree[9572]
    del reference[9572]
    assert tree.invariants(), "Invariants failed at step 9256"

    # Operation 9257: update
    tree[7764] = 'value_564116'
    reference[7764] = 'value_564116'
    assert tree.invariants(), "Invariants failed at step 9257"

    # Operation 9258: get
    assert tree.invariants(), "Invariants failed at step 9258"

    # Operation 9259: get
    assert tree.invariants(), "Invariants failed at step 9259"

    # Operation 9260: delete
    del tree[9429]
    del reference[9429]
    assert tree.invariants(), "Invariants failed at step 9260"

    # Operation 9261: update
    tree[7927] = 'value_317078'
    reference[7927] = 'value_317078'
    assert tree.invariants(), "Invariants failed at step 9261"

    # Operation 9262: delete
    del tree[4607]
    del reference[4607]
    assert tree.invariants(), "Invariants failed at step 9262"

    # Operation 9263: get
    assert tree.invariants(), "Invariants failed at step 9263"

    # Operation 9264: insert
    tree[280] = 'value_472131'
    reference[280] = 'value_472131'
    assert tree.invariants(), "Invariants failed at step 9264"

    # Operation 9265: insert
    tree[7401] = 'value_557195'
    reference[7401] = 'value_557195'
    assert tree.invariants(), "Invariants failed at step 9265"

    # Operation 9266: get
    assert tree.invariants(), "Invariants failed at step 9266"

    # Operation 9267: delete
    del tree[280]
    del reference[280]
    assert tree.invariants(), "Invariants failed at step 9267"

    # Operation 9268: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9268"

    # Operation 9269: update
    tree[7764] = 'value_670164'
    reference[7764] = 'value_670164'
    assert tree.invariants(), "Invariants failed at step 9269"

    # Operation 9270: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9270"

    # Operation 9271: get
    assert tree.invariants(), "Invariants failed at step 9271"

    # Operation 9272: get
    assert tree.invariants(), "Invariants failed at step 9272"

    # Operation 9273: get
    assert tree.invariants(), "Invariants failed at step 9273"

    # Operation 9274: insert
    tree[7888] = 'value_211114'
    reference[7888] = 'value_211114'
    assert tree.invariants(), "Invariants failed at step 9274"

    # Operation 9275: delete
    del tree[7764]
    del reference[7764]
    assert tree.invariants(), "Invariants failed at step 9275"

    # Operation 9276: get
    assert tree.invariants(), "Invariants failed at step 9276"

    # Operation 9277: delete
    del tree[7888]
    del reference[7888]
    assert tree.invariants(), "Invariants failed at step 9277"

    # Operation 9278: delete
    del tree[7401]
    del reference[7401]
    assert tree.invariants(), "Invariants failed at step 9278"

    # Operation 9279: insert
    tree[5195] = 'value_578081'
    reference[5195] = 'value_578081'
    assert tree.invariants(), "Invariants failed at step 9279"

    # Operation 9280: delete
    del tree[7927]
    del reference[7927]
    assert tree.invariants(), "Invariants failed at step 9280"

    # Operation 9281: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9281"

    # Operation 9282: update
    tree[5195] = 'value_94765'
    reference[5195] = 'value_94765'
    assert tree.invariants(), "Invariants failed at step 9282"

    # Operation 9283: get
    assert tree.invariants(), "Invariants failed at step 9283"

    # Operation 9284: get
    assert tree.invariants(), "Invariants failed at step 9284"

    # Operation 9285: insert
    tree[6009] = 'value_129409'
    reference[6009] = 'value_129409'
    assert tree.invariants(), "Invariants failed at step 9285"

    # Operation 9286: delete
    del tree[5195]
    del reference[5195]
    assert tree.invariants(), "Invariants failed at step 9286"

    # Operation 9287: delete
    del tree[6009]
    del reference[6009]
    assert tree.invariants(), "Invariants failed at step 9287"

    # Operation 9288: get
    assert tree.invariants(), "Invariants failed at step 9288"

    # Operation 9289: get
    assert tree.invariants(), "Invariants failed at step 9289"

    # Operation 9290: insert
    tree[217] = 'value_389904'
    reference[217] = 'value_389904'
    assert tree.invariants(), "Invariants failed at step 9290"

    # Operation 9291: insert
    tree[7066] = 'value_628082'
    reference[7066] = 'value_628082'
    assert tree.invariants(), "Invariants failed at step 9291"

    # Operation 9292: insert
    tree[8900] = 'value_997608'
    reference[8900] = 'value_997608'
    assert tree.invariants(), "Invariants failed at step 9292"

    # Operation 9293: delete
    del tree[7066]
    del reference[7066]
    assert tree.invariants(), "Invariants failed at step 9293"

    # Operation 9294: get
    assert tree.invariants(), "Invariants failed at step 9294"

    # Operation 9295: insert
    tree[2944] = 'value_554264'
    reference[2944] = 'value_554264'
    assert tree.invariants(), "Invariants failed at step 9295"

    # Operation 9296: insert
    tree[5974] = 'value_495224'
    reference[5974] = 'value_495224'
    assert tree.invariants(), "Invariants failed at step 9296"

    # Operation 9297: insert
    tree[5740] = 'value_973521'
    reference[5740] = 'value_973521'
    assert tree.invariants(), "Invariants failed at step 9297"

    # Operation 9298: delete
    del tree[8900]
    del reference[8900]
    assert tree.invariants(), "Invariants failed at step 9298"

    # Operation 9299: insert
    tree[1133] = 'value_628938'
    reference[1133] = 'value_628938'
    assert tree.invariants(), "Invariants failed at step 9299"

    # Operation 9300: get
    assert tree.invariants(), "Invariants failed at step 9300"

    # Operation 9301: get
    assert tree.invariants(), "Invariants failed at step 9301"

    # Operation 9302: batch_delete
    keys_to_delete = [217, 161, 1133, 5974]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9302"

    # Operation 9303: get
    assert tree.invariants(), "Invariants failed at step 9303"

    # Operation 9304: insert
    tree[794] = 'value_790370'
    reference[794] = 'value_790370'
    assert tree.invariants(), "Invariants failed at step 9304"

    # Operation 9305: get
    assert tree.invariants(), "Invariants failed at step 9305"

    # Operation 9306: delete
    del tree[5740]
    del reference[5740]
    assert tree.invariants(), "Invariants failed at step 9306"

    # Operation 9307: delete
    del tree[2944]
    del reference[2944]
    assert tree.invariants(), "Invariants failed at step 9307"

    # Operation 9308: delete
    del tree[794]
    del reference[794]
    assert tree.invariants(), "Invariants failed at step 9308"

    # Operation 9309: insert
    tree[2805] = 'value_618007'
    reference[2805] = 'value_618007'
    assert tree.invariants(), "Invariants failed at step 9309"

    # Operation 9310: insert
    tree[30] = 'value_286559'
    reference[30] = 'value_286559'
    assert tree.invariants(), "Invariants failed at step 9310"

    # Operation 9311: get
    assert tree.invariants(), "Invariants failed at step 9311"

    # Operation 9312: get
    assert tree.invariants(), "Invariants failed at step 9312"

    # Operation 9313: delete
    del tree[30]
    del reference[30]
    assert tree.invariants(), "Invariants failed at step 9313"

    # Operation 9314: insert
    tree[2606] = 'value_869195'
    reference[2606] = 'value_869195'
    assert tree.invariants(), "Invariants failed at step 9314"

    # Operation 9315: insert
    tree[8565] = 'value_375500'
    reference[8565] = 'value_375500'
    assert tree.invariants(), "Invariants failed at step 9315"

    # Operation 9316: get
    assert tree.invariants(), "Invariants failed at step 9316"

    # Operation 9317: delete
    del tree[2606]
    del reference[2606]
    assert tree.invariants(), "Invariants failed at step 9317"

    # Operation 9318: get
    assert tree.invariants(), "Invariants failed at step 9318"

    # Operation 9319: insert
    tree[2408] = 'value_55724'
    reference[2408] = 'value_55724'
    assert tree.invariants(), "Invariants failed at step 9319"

    # Operation 9320: get
    assert tree.invariants(), "Invariants failed at step 9320"

    # Operation 9321: delete
    del tree[8565]
    del reference[8565]
    assert tree.invariants(), "Invariants failed at step 9321"

    # Operation 9322: get
    assert tree.invariants(), "Invariants failed at step 9322"

    # Operation 9323: delete
    del tree[2805]
    del reference[2805]
    assert tree.invariants(), "Invariants failed at step 9323"

    # Operation 9324: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9324"

    # Operation 9325: delete
    del tree[2408]
    del reference[2408]
    assert tree.invariants(), "Invariants failed at step 9325"

    # Operation 9326: insert
    tree[9499] = 'value_69806'
    reference[9499] = 'value_69806'
    assert tree.invariants(), "Invariants failed at step 9326"

    # Operation 9327: insert
    tree[1936] = 'value_28591'
    reference[1936] = 'value_28591'
    assert tree.invariants(), "Invariants failed at step 9327"

    # Operation 9328: update
    tree[1936] = 'value_575220'
    reference[1936] = 'value_575220'
    assert tree.invariants(), "Invariants failed at step 9328"

    # Operation 9329: get
    assert tree.invariants(), "Invariants failed at step 9329"

    # Operation 9330: delete
    del tree[9499]
    del reference[9499]
    assert tree.invariants(), "Invariants failed at step 9330"

    # Operation 9331: insert
    tree[549] = 'value_64779'
    reference[549] = 'value_64779'
    assert tree.invariants(), "Invariants failed at step 9331"

    # Operation 9332: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9332"

    # Operation 9333: delete
    del tree[1936]
    del reference[1936]
    assert tree.invariants(), "Invariants failed at step 9333"

    # Operation 9334: insert
    tree[676] = 'value_527759'
    reference[676] = 'value_527759'
    assert tree.invariants(), "Invariants failed at step 9334"

    # Operation 9335: update
    tree[549] = 'value_397031'
    reference[549] = 'value_397031'
    assert tree.invariants(), "Invariants failed at step 9335"

    # Operation 9336: insert
    tree[3838] = 'value_456627'
    reference[3838] = 'value_456627'
    assert tree.invariants(), "Invariants failed at step 9336"

    # Operation 9337: get
    assert tree.invariants(), "Invariants failed at step 9337"

    # Operation 9338: get
    assert tree.invariants(), "Invariants failed at step 9338"

    # Operation 9339: delete
    del tree[676]
    del reference[676]
    assert tree.invariants(), "Invariants failed at step 9339"

    # Operation 9340: insert
    tree[1021] = 'value_948764'
    reference[1021] = 'value_948764'
    assert tree.invariants(), "Invariants failed at step 9340"

    # Operation 9341: insert
    tree[9719] = 'value_950487'
    reference[9719] = 'value_950487'
    assert tree.invariants(), "Invariants failed at step 9341"

    # Operation 9342: delete
    del tree[549]
    del reference[549]
    assert tree.invariants(), "Invariants failed at step 9342"

    # Operation 9343: insert
    tree[1148] = 'value_11950'
    reference[1148] = 'value_11950'
    assert tree.invariants(), "Invariants failed at step 9343"

    # Operation 9344: insert
    tree[3683] = 'value_82031'
    reference[3683] = 'value_82031'
    assert tree.invariants(), "Invariants failed at step 9344"

    # Operation 9345: batch_delete
    keys_to_delete = [1, 4203, 1021, 3838]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9345"

    # Operation 9346: insert
    tree[2440] = 'value_15413'
    reference[2440] = 'value_15413'
    assert tree.invariants(), "Invariants failed at step 9346"

    # Operation 9347: insert
    tree[396] = 'value_146732'
    reference[396] = 'value_146732'
    assert tree.invariants(), "Invariants failed at step 9347"

    # Operation 9348: delete
    del tree[2440]
    del reference[2440]
    assert tree.invariants(), "Invariants failed at step 9348"

    # Operation 9349: get
    assert tree.invariants(), "Invariants failed at step 9349"

    # Operation 9350: delete
    del tree[1148]
    del reference[1148]
    assert tree.invariants(), "Invariants failed at step 9350"

    # Operation 9351: update
    tree[3683] = 'value_506731'
    reference[3683] = 'value_506731'
    assert tree.invariants(), "Invariants failed at step 9351"

    # Operation 9352: insert
    tree[7573] = 'value_268094'
    reference[7573] = 'value_268094'
    assert tree.invariants(), "Invariants failed at step 9352"

    # Operation 9353: update
    tree[396] = 'value_810531'
    reference[396] = 'value_810531'
    assert tree.invariants(), "Invariants failed at step 9353"

    # Operation 9354: insert
    tree[2036] = 'value_384408'
    reference[2036] = 'value_384408'
    assert tree.invariants(), "Invariants failed at step 9354"

    # Operation 9355: delete
    del tree[2036]
    del reference[2036]
    assert tree.invariants(), "Invariants failed at step 9355"

    # Operation 9356: insert
    tree[3628] = 'value_866821'
    reference[3628] = 'value_866821'
    assert tree.invariants(), "Invariants failed at step 9356"

    # Operation 9357: update
    tree[396] = 'value_150653'
    reference[396] = 'value_150653'
    assert tree.invariants(), "Invariants failed at step 9357"

    # Operation 9358: delete
    del tree[3628]
    del reference[3628]
    assert tree.invariants(), "Invariants failed at step 9358"

    # Operation 9359: insert
    tree[1246] = 'value_806615'
    reference[1246] = 'value_806615'
    assert tree.invariants(), "Invariants failed at step 9359"

    # Operation 9360: insert
    tree[11] = 'value_910947'
    reference[11] = 'value_910947'
    assert tree.invariants(), "Invariants failed at step 9360"

    # Operation 9361: batch_delete
    keys_to_delete = [6081, 3683, 396, 5276, 1246]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9361"

    # Operation 9362: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9362"

    # Operation 9363: insert
    tree[5679] = 'value_350556'
    reference[5679] = 'value_350556'
    assert tree.invariants(), "Invariants failed at step 9363"

    # Operation 9364: get
    assert tree.invariants(), "Invariants failed at step 9364"

    # Operation 9365: delete
    del tree[11]
    del reference[11]
    assert tree.invariants(), "Invariants failed at step 9365"

    # Operation 9366: insert
    tree[538] = 'value_373663'
    reference[538] = 'value_373663'
    assert tree.invariants(), "Invariants failed at step 9366"

    # Operation 9367: insert
    tree[483] = 'value_329641'
    reference[483] = 'value_329641'
    assert tree.invariants(), "Invariants failed at step 9367"

    # Operation 9368: batch_delete
    keys_to_delete = [483, 740, 7573, 4383]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9368"

    # Operation 9369: insert
    tree[3993] = 'value_832921'
    reference[3993] = 'value_832921'
    assert tree.invariants(), "Invariants failed at step 9369"

    # Operation 9370: delete
    del tree[9719]
    del reference[9719]
    assert tree.invariants(), "Invariants failed at step 9370"

    # Operation 9371: insert
    tree[540] = 'value_348630'
    reference[540] = 'value_348630'
    assert tree.invariants(), "Invariants failed at step 9371"

    # Operation 9372: delete
    del tree[5679]
    del reference[5679]
    assert tree.invariants(), "Invariants failed at step 9372"

    # Operation 9373: delete
    del tree[538]
    del reference[538]
    assert tree.invariants(), "Invariants failed at step 9373"

    # Operation 9374: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9374"

    # Operation 9375: insert
    tree[4332] = 'value_541309'
    reference[4332] = 'value_541309'
    assert tree.invariants(), "Invariants failed at step 9375"

    # Operation 9376: update
    tree[4332] = 'value_525398'
    reference[4332] = 'value_525398'
    assert tree.invariants(), "Invariants failed at step 9376"

    # Operation 9377: insert
    tree[4478] = 'value_833414'
    reference[4478] = 'value_833414'
    assert tree.invariants(), "Invariants failed at step 9377"

    # Operation 9378: delete
    del tree[4478]
    del reference[4478]
    assert tree.invariants(), "Invariants failed at step 9378"

    # Operation 9379: delete
    del tree[4332]
    del reference[4332]
    assert tree.invariants(), "Invariants failed at step 9379"

    # Operation 9380: delete
    del tree[3993]
    del reference[3993]
    assert tree.invariants(), "Invariants failed at step 9380"

    # Operation 9381: delete
    del tree[540]
    del reference[540]
    assert tree.invariants(), "Invariants failed at step 9381"

    # Operation 9382: insert
    tree[3418] = 'value_945914'
    reference[3418] = 'value_945914'
    assert tree.invariants(), "Invariants failed at step 9382"

    # Operation 9383: update
    tree[3418] = 'value_409851'
    reference[3418] = 'value_409851'
    assert tree.invariants(), "Invariants failed at step 9383"

    # Operation 9384: get
    assert tree.invariants(), "Invariants failed at step 9384"

    # Operation 9385: delete
    del tree[3418]
    del reference[3418]
    assert tree.invariants(), "Invariants failed at step 9385"

    # Operation 9386: insert
    tree[7512] = 'value_337423'
    reference[7512] = 'value_337423'
    assert tree.invariants(), "Invariants failed at step 9386"

    # Operation 9387: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9387"

    # Operation 9388: get
    assert tree.invariants(), "Invariants failed at step 9388"

    # Operation 9389: update
    tree[7512] = 'value_702818'
    reference[7512] = 'value_702818'
    assert tree.invariants(), "Invariants failed at step 9389"

    # Operation 9390: delete
    del tree[7512]
    del reference[7512]
    assert tree.invariants(), "Invariants failed at step 9390"

    # Operation 9391: insert
    tree[5372] = 'value_24807'
    reference[5372] = 'value_24807'
    assert tree.invariants(), "Invariants failed at step 9391"

    # Operation 9392: insert
    tree[4551] = 'value_482343'
    reference[4551] = 'value_482343'
    assert tree.invariants(), "Invariants failed at step 9392"

    # Operation 9393: insert
    tree[9318] = 'value_544006'
    reference[9318] = 'value_544006'
    assert tree.invariants(), "Invariants failed at step 9393"

    # Operation 9394: insert
    tree[7041] = 'value_369676'
    reference[7041] = 'value_369676'
    assert tree.invariants(), "Invariants failed at step 9394"

    # Operation 9395: insert
    tree[5161] = 'value_250300'
    reference[5161] = 'value_250300'
    assert tree.invariants(), "Invariants failed at step 9395"

    # Operation 9396: insert
    tree[8505] = 'value_487711'
    reference[8505] = 'value_487711'
    assert tree.invariants(), "Invariants failed at step 9396"

    # Operation 9397: insert
    tree[8808] = 'value_917829'
    reference[8808] = 'value_917829'
    assert tree.invariants(), "Invariants failed at step 9397"

    # Operation 9398: insert
    tree[1219] = 'value_392530'
    reference[1219] = 'value_392530'
    assert tree.invariants(), "Invariants failed at step 9398"

    # Operation 9399: batch_delete
    keys_to_delete = [1219, 4551, 8808, 9515, 5372, 6910]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9399"

    # Operation 9400: insert
    tree[6179] = 'value_708424'
    reference[6179] = 'value_708424'
    assert tree.invariants(), "Invariants failed at step 9400"

    # Operation 9401: delete
    del tree[7041]
    del reference[7041]
    assert tree.invariants(), "Invariants failed at step 9401"

    # Operation 9402: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9402"

    # Operation 9403: get
    assert tree.invariants(), "Invariants failed at step 9403"

    # Operation 9404: delete
    del tree[5161]
    del reference[5161]
    assert tree.invariants(), "Invariants failed at step 9404"

    # Operation 9405: insert
    tree[4358] = 'value_653733'
    reference[4358] = 'value_653733'
    assert tree.invariants(), "Invariants failed at step 9405"

    # Operation 9406: insert
    tree[7639] = 'value_844605'
    reference[7639] = 'value_844605'
    assert tree.invariants(), "Invariants failed at step 9406"

    # Operation 9407: delete
    del tree[6179]
    del reference[6179]
    assert tree.invariants(), "Invariants failed at step 9407"

    # Operation 9408: get
    assert tree.invariants(), "Invariants failed at step 9408"

    # Operation 9409: delete
    del tree[4358]
    del reference[4358]
    assert tree.invariants(), "Invariants failed at step 9409"

    # Operation 9410: delete
    del tree[7639]
    del reference[7639]
    assert tree.invariants(), "Invariants failed at step 9410"

    # Operation 9411: delete
    del tree[8505]
    del reference[8505]
    assert tree.invariants(), "Invariants failed at step 9411"

    # Operation 9412: update
    tree[9318] = 'value_8301'
    reference[9318] = 'value_8301'
    assert tree.invariants(), "Invariants failed at step 9412"

    # Operation 9413: delete
    del tree[9318]
    del reference[9318]
    assert tree.invariants(), "Invariants failed at step 9413"

    # Operation 9414: insert
    tree[2722] = 'value_745021'
    reference[2722] = 'value_745021'
    assert tree.invariants(), "Invariants failed at step 9414"

    # Operation 9415: insert
    tree[2948] = 'value_222726'
    reference[2948] = 'value_222726'
    assert tree.invariants(), "Invariants failed at step 9415"

    # Operation 9416: get
    assert tree.invariants(), "Invariants failed at step 9416"

    # Operation 9417: update
    tree[2722] = 'value_185409'
    reference[2722] = 'value_185409'
    assert tree.invariants(), "Invariants failed at step 9417"

    # Operation 9418: update
    tree[2948] = 'value_993988'
    reference[2948] = 'value_993988'
    assert tree.invariants(), "Invariants failed at step 9418"

    # Operation 9419: delete
    del tree[2948]
    del reference[2948]
    assert tree.invariants(), "Invariants failed at step 9419"

    # Operation 9420: insert
    tree[6452] = 'value_541396'
    reference[6452] = 'value_541396'
    assert tree.invariants(), "Invariants failed at step 9420"

    # Operation 9421: update
    tree[6452] = 'value_621234'
    reference[6452] = 'value_621234'
    assert tree.invariants(), "Invariants failed at step 9421"

    # Operation 9422: delete
    del tree[2722]
    del reference[2722]
    assert tree.invariants(), "Invariants failed at step 9422"

    # Operation 9423: insert
    tree[2392] = 'value_208001'
    reference[2392] = 'value_208001'
    assert tree.invariants(), "Invariants failed at step 9423"

    # Operation 9424: insert
    tree[3148] = 'value_167953'
    reference[3148] = 'value_167953'
    assert tree.invariants(), "Invariants failed at step 9424"

    # Operation 9425: insert
    tree[127] = 'value_285610'
    reference[127] = 'value_285610'
    assert tree.invariants(), "Invariants failed at step 9425"

    # Operation 9426: insert
    tree[2908] = 'value_335947'
    reference[2908] = 'value_335947'
    assert tree.invariants(), "Invariants failed at step 9426"

    # Operation 9427: insert
    tree[2335] = 'value_888216'
    reference[2335] = 'value_888216'
    assert tree.invariants(), "Invariants failed at step 9427"

    # Operation 9428: delete
    del tree[2908]
    del reference[2908]
    assert tree.invariants(), "Invariants failed at step 9428"

    # Operation 9429: update
    tree[2392] = 'value_323407'
    reference[2392] = 'value_323407'
    assert tree.invariants(), "Invariants failed at step 9429"

    # Operation 9430: delete
    del tree[127]
    del reference[127]
    assert tree.invariants(), "Invariants failed at step 9430"

    # Operation 9431: delete
    del tree[2335]
    del reference[2335]
    assert tree.invariants(), "Invariants failed at step 9431"

    # Operation 9432: get
    assert tree.invariants(), "Invariants failed at step 9432"

    # Operation 9433: get
    assert tree.invariants(), "Invariants failed at step 9433"

    # Operation 9434: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9434"

    # Operation 9435: update
    tree[6452] = 'value_268405'
    reference[6452] = 'value_268405'
    assert tree.invariants(), "Invariants failed at step 9435"

    # Operation 9436: insert
    tree[6251] = 'value_241043'
    reference[6251] = 'value_241043'
    assert tree.invariants(), "Invariants failed at step 9436"

    # Operation 9437: delete
    del tree[3148]
    del reference[3148]
    assert tree.invariants(), "Invariants failed at step 9437"

    # Operation 9438: get
    assert tree.invariants(), "Invariants failed at step 9438"

    # Operation 9439: delete
    del tree[2392]
    del reference[2392]
    assert tree.invariants(), "Invariants failed at step 9439"

    # Operation 9440: insert
    tree[6240] = 'value_787288'
    reference[6240] = 'value_787288'
    assert tree.invariants(), "Invariants failed at step 9440"

    # Operation 9441: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9441"

    # Operation 9442: get
    assert tree.invariants(), "Invariants failed at step 9442"

    # Operation 9443: insert
    tree[1246] = 'value_929592'
    reference[1246] = 'value_929592'
    assert tree.invariants(), "Invariants failed at step 9443"

    # Operation 9444: insert
    tree[4689] = 'value_17665'
    reference[4689] = 'value_17665'
    assert tree.invariants(), "Invariants failed at step 9444"

    # Operation 9445: get
    assert tree.invariants(), "Invariants failed at step 9445"

    # Operation 9446: delete
    del tree[1246]
    del reference[1246]
    assert tree.invariants(), "Invariants failed at step 9446"

    # Operation 9447: update
    tree[6240] = 'value_886546'
    reference[6240] = 'value_886546'
    assert tree.invariants(), "Invariants failed at step 9447"

    # Operation 9448: get
    assert tree.invariants(), "Invariants failed at step 9448"

    # Operation 9449: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9449"

    # Operation 9450: insert
    tree[9498] = 'value_441242'
    reference[9498] = 'value_441242'
    assert tree.invariants(), "Invariants failed at step 9450"

    # Operation 9451: get
    assert tree.invariants(), "Invariants failed at step 9451"

    # Operation 9452: update
    tree[6452] = 'value_311377'
    reference[6452] = 'value_311377'
    assert tree.invariants(), "Invariants failed at step 9452"

    # Operation 9453: delete
    del tree[6452]
    del reference[6452]
    assert tree.invariants(), "Invariants failed at step 9453"

    # Operation 9454: insert
    tree[6331] = 'value_292846'
    reference[6331] = 'value_292846'
    assert tree.invariants(), "Invariants failed at step 9454"

    # Operation 9455: update
    tree[6331] = 'value_718914'
    reference[6331] = 'value_718914'
    assert tree.invariants(), "Invariants failed at step 9455"

    # Operation 9456: insert
    tree[4645] = 'value_139609'
    reference[4645] = 'value_139609'
    assert tree.invariants(), "Invariants failed at step 9456"

    # Operation 9457: get
    assert tree.invariants(), "Invariants failed at step 9457"

    # Operation 9458: get
    assert tree.invariants(), "Invariants failed at step 9458"

    # Operation 9459: delete
    del tree[4689]
    del reference[4689]
    assert tree.invariants(), "Invariants failed at step 9459"

    # Operation 9460: insert
    tree[1380] = 'value_338722'
    reference[1380] = 'value_338722'
    assert tree.invariants(), "Invariants failed at step 9460"

    # Operation 9461: get
    assert tree.invariants(), "Invariants failed at step 9461"

    # Operation 9462: update
    tree[6251] = 'value_937796'
    reference[6251] = 'value_937796'
    assert tree.invariants(), "Invariants failed at step 9462"

    # Operation 9463: insert
    tree[7885] = 'value_995481'
    reference[7885] = 'value_995481'
    assert tree.invariants(), "Invariants failed at step 9463"

    # Operation 9464: delete
    del tree[4645]
    del reference[4645]
    assert tree.invariants(), "Invariants failed at step 9464"

    # Operation 9465: get
    assert tree.invariants(), "Invariants failed at step 9465"

    # Operation 9466: get
    assert tree.invariants(), "Invariants failed at step 9466"

    # Operation 9467: insert
    tree[3468] = 'value_735058'
    reference[3468] = 'value_735058'
    assert tree.invariants(), "Invariants failed at step 9467"

    # Operation 9468: insert
    tree[4395] = 'value_716657'
    reference[4395] = 'value_716657'
    assert tree.invariants(), "Invariants failed at step 9468"

    # Operation 9469: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9469"

    # Operation 9470: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9470"

    # Operation 9471: delete
    del tree[3468]
    del reference[3468]
    assert tree.invariants(), "Invariants failed at step 9471"

    # Operation 9472: get
    assert tree.invariants(), "Invariants failed at step 9472"

    # Operation 9473: insert
    tree[7152] = 'value_983389'
    reference[7152] = 'value_983389'
    assert tree.invariants(), "Invariants failed at step 9473"

    # Operation 9474: delete
    del tree[9498]
    del reference[9498]
    assert tree.invariants(), "Invariants failed at step 9474"

    # Operation 9475: update
    tree[4395] = 'value_303597'
    reference[4395] = 'value_303597'
    assert tree.invariants(), "Invariants failed at step 9475"

    # Operation 9476: update
    tree[1380] = 'value_122522'
    reference[1380] = 'value_122522'
    assert tree.invariants(), "Invariants failed at step 9476"

    # Operation 9477: delete
    del tree[7885]
    del reference[7885]
    assert tree.invariants(), "Invariants failed at step 9477"

    # Operation 9478: batch_delete
    keys_to_delete = [6240, 1380, 4936, 436, 6331]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9478"

    # Operation 9479: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9479"

    # Operation 9480: update
    tree[7152] = 'value_688406'
    reference[7152] = 'value_688406'
    assert tree.invariants(), "Invariants failed at step 9480"

    # Operation 9481: update
    tree[4395] = 'value_655570'
    reference[4395] = 'value_655570'
    assert tree.invariants(), "Invariants failed at step 9481"

    # Operation 9482: insert
    tree[8865] = 'value_617204'
    reference[8865] = 'value_617204'
    assert tree.invariants(), "Invariants failed at step 9482"

    # Operation 9483: insert
    tree[9584] = 'value_161547'
    reference[9584] = 'value_161547'
    assert tree.invariants(), "Invariants failed at step 9483"

    # Operation 9484: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9484"

    # Operation 9485: get
    assert tree.invariants(), "Invariants failed at step 9485"

    # Operation 9486: batch_delete
    keys_to_delete = [9584, 9561, 4395, 6257]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9486"

    # Operation 9487: get
    assert tree.invariants(), "Invariants failed at step 9487"

    # Operation 9488: get
    assert tree.invariants(), "Invariants failed at step 9488"

    # Operation 9489: get
    assert tree.invariants(), "Invariants failed at step 9489"

    # Operation 9490: get
    assert tree.invariants(), "Invariants failed at step 9490"

    # Operation 9491: update
    tree[8865] = 'value_461473'
    reference[8865] = 'value_461473'
    assert tree.invariants(), "Invariants failed at step 9491"

    # Operation 9492: delete
    del tree[8865]
    del reference[8865]
    assert tree.invariants(), "Invariants failed at step 9492"

    # Operation 9493: get
    assert tree.invariants(), "Invariants failed at step 9493"

    # Operation 9494: delete
    del tree[6251]
    del reference[6251]
    assert tree.invariants(), "Invariants failed at step 9494"

    # Operation 9495: update
    tree[7152] = 'value_667053'
    reference[7152] = 'value_667053'
    assert tree.invariants(), "Invariants failed at step 9495"

    # Operation 9496: delete
    del tree[7152]
    del reference[7152]
    assert tree.invariants(), "Invariants failed at step 9496"

    # Operation 9497: insert
    tree[1855] = 'value_766517'
    reference[1855] = 'value_766517'
    assert tree.invariants(), "Invariants failed at step 9497"

    # Operation 9498: insert
    tree[1340] = 'value_705041'
    reference[1340] = 'value_705041'
    assert tree.invariants(), "Invariants failed at step 9498"

    # Operation 9499: insert
    tree[8947] = 'value_715865'
    reference[8947] = 'value_715865'
    assert tree.invariants(), "Invariants failed at step 9499"

    # Operation 9500: insert
    tree[7639] = 'value_82617'
    reference[7639] = 'value_82617'
    assert tree.invariants(), "Invariants failed at step 9500"

    # Operation 9501: update
    tree[1340] = 'value_577068'
    reference[1340] = 'value_577068'
    assert tree.invariants(), "Invariants failed at step 9501"

    # Operation 9502: delete
    del tree[1340]
    del reference[1340]
    assert tree.invariants(), "Invariants failed at step 9502"

    # Operation 9503: update
    tree[7639] = 'value_392241'
    reference[7639] = 'value_392241'
    assert tree.invariants(), "Invariants failed at step 9503"

    # Operation 9504: get
    assert tree.invariants(), "Invariants failed at step 9504"

    # Operation 9505: insert
    tree[1895] = 'value_984276'
    reference[1895] = 'value_984276'
    assert tree.invariants(), "Invariants failed at step 9505"

    # Operation 9506: insert
    tree[9365] = 'value_379597'
    reference[9365] = 'value_379597'
    assert tree.invariants(), "Invariants failed at step 9506"

    # Operation 9507: get
    assert tree.invariants(), "Invariants failed at step 9507"

    # Operation 9508: insert
    tree[9786] = 'value_156408'
    reference[9786] = 'value_156408'
    assert tree.invariants(), "Invariants failed at step 9508"

    # Operation 9509: insert
    tree[4870] = 'value_745597'
    reference[4870] = 'value_745597'
    assert tree.invariants(), "Invariants failed at step 9509"

    # Operation 9510: insert
    tree[3637] = 'value_453265'
    reference[3637] = 'value_453265'
    assert tree.invariants(), "Invariants failed at step 9510"

    # Operation 9511: update
    tree[3637] = 'value_972611'
    reference[3637] = 'value_972611'
    assert tree.invariants(), "Invariants failed at step 9511"

    # Operation 9512: insert
    tree[6807] = 'value_544410'
    reference[6807] = 'value_544410'
    assert tree.invariants(), "Invariants failed at step 9512"

    # Operation 9513: batch_delete
    keys_to_delete = [8015, 8947, 9365, 7639, 2425, 1855]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9513"

    # Operation 9514: insert
    tree[2750] = 'value_836362'
    reference[2750] = 'value_836362'
    assert tree.invariants(), "Invariants failed at step 9514"

    # Operation 9515: insert
    tree[9415] = 'value_362522'
    reference[9415] = 'value_362522'
    assert tree.invariants(), "Invariants failed at step 9515"

    # Operation 9516: delete
    del tree[6807]
    del reference[6807]
    assert tree.invariants(), "Invariants failed at step 9516"

    # Operation 9517: delete
    del tree[2750]
    del reference[2750]
    assert tree.invariants(), "Invariants failed at step 9517"

    # Operation 9518: insert
    tree[8296] = 'value_193301'
    reference[8296] = 'value_193301'
    assert tree.invariants(), "Invariants failed at step 9518"

    # Operation 9519: update
    tree[3637] = 'value_134784'
    reference[3637] = 'value_134784'
    assert tree.invariants(), "Invariants failed at step 9519"

    # Operation 9520: update
    tree[9415] = 'value_179840'
    reference[9415] = 'value_179840'
    assert tree.invariants(), "Invariants failed at step 9520"

    # Operation 9521: batch_delete
    keys_to_delete = [3305, 9786, 9415, 1895]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9521"

    # Operation 9522: update
    tree[4870] = 'value_8136'
    reference[4870] = 'value_8136'
    assert tree.invariants(), "Invariants failed at step 9522"

    # Operation 9523: insert
    tree[8061] = 'value_774655'
    reference[8061] = 'value_774655'
    assert tree.invariants(), "Invariants failed at step 9523"

    # Operation 9524: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9524"

    # Operation 9525: delete
    del tree[8296]
    del reference[8296]
    assert tree.invariants(), "Invariants failed at step 9525"

    # Operation 9526: get
    assert tree.invariants(), "Invariants failed at step 9526"

    # Operation 9527: insert
    tree[3077] = 'value_762978'
    reference[3077] = 'value_762978'
    assert tree.invariants(), "Invariants failed at step 9527"

    # Operation 9528: insert
    tree[2636] = 'value_259481'
    reference[2636] = 'value_259481'
    assert tree.invariants(), "Invariants failed at step 9528"

    # Operation 9529: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9529"

    # Operation 9530: delete
    del tree[2636]
    del reference[2636]
    assert tree.invariants(), "Invariants failed at step 9530"

    # Operation 9531: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9531"

    # Operation 9532: delete
    del tree[3077]
    del reference[3077]
    assert tree.invariants(), "Invariants failed at step 9532"

    # Operation 9533: insert
    tree[673] = 'value_2547'
    reference[673] = 'value_2547'
    assert tree.invariants(), "Invariants failed at step 9533"

    # Operation 9534: get
    assert tree.invariants(), "Invariants failed at step 9534"

    # Operation 9535: update
    tree[8061] = 'value_695571'
    reference[8061] = 'value_695571'
    assert tree.invariants(), "Invariants failed at step 9535"

    # Operation 9536: insert
    tree[6742] = 'value_750773'
    reference[6742] = 'value_750773'
    assert tree.invariants(), "Invariants failed at step 9536"

    # Operation 9537: batch_delete
    keys_to_delete = [673, 4482, 5620, 6742]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9537"

    # Operation 9538: insert
    tree[7369] = 'value_435196'
    reference[7369] = 'value_435196'
    assert tree.invariants(), "Invariants failed at step 9538"

    # Operation 9539: insert
    tree[1631] = 'value_952648'
    reference[1631] = 'value_952648'
    assert tree.invariants(), "Invariants failed at step 9539"

    # Operation 9540: get
    assert tree.invariants(), "Invariants failed at step 9540"

    # Operation 9541: batch_delete
    keys_to_delete = [1445, 7895, 8061, 1631]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9541"

    # Operation 9542: update
    tree[3637] = 'value_133447'
    reference[3637] = 'value_133447'
    assert tree.invariants(), "Invariants failed at step 9542"

    # Operation 9543: update
    tree[3637] = 'value_661865'
    reference[3637] = 'value_661865'
    assert tree.invariants(), "Invariants failed at step 9543"

    # Operation 9544: delete
    del tree[7369]
    del reference[7369]
    assert tree.invariants(), "Invariants failed at step 9544"

    # Operation 9545: update
    tree[3637] = 'value_374432'
    reference[3637] = 'value_374432'
    assert tree.invariants(), "Invariants failed at step 9545"

    # Operation 9546: get
    assert tree.invariants(), "Invariants failed at step 9546"

    # Operation 9547: update
    tree[4870] = 'value_365303'
    reference[4870] = 'value_365303'
    assert tree.invariants(), "Invariants failed at step 9547"

    # Operation 9548: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9548"

    # Operation 9549: insert
    tree[6667] = 'value_224496'
    reference[6667] = 'value_224496'
    assert tree.invariants(), "Invariants failed at step 9549"

    # Operation 9550: update
    tree[4870] = 'value_753979'
    reference[4870] = 'value_753979'
    assert tree.invariants(), "Invariants failed at step 9550"

    # Operation 9551: insert
    tree[3531] = 'value_601401'
    reference[3531] = 'value_601401'
    assert tree.invariants(), "Invariants failed at step 9551"

    # Operation 9552: delete
    del tree[3637]
    del reference[3637]
    assert tree.invariants(), "Invariants failed at step 9552"

    # Operation 9553: insert
    tree[1468] = 'value_260739'
    reference[1468] = 'value_260739'
    assert tree.invariants(), "Invariants failed at step 9553"

    # Operation 9554: insert
    tree[3758] = 'value_132222'
    reference[3758] = 'value_132222'
    assert tree.invariants(), "Invariants failed at step 9554"

    # Operation 9555: insert
    tree[556] = 'value_418711'
    reference[556] = 'value_418711'
    assert tree.invariants(), "Invariants failed at step 9555"

    # Operation 9556: insert
    tree[3378] = 'value_844977'
    reference[3378] = 'value_844977'
    assert tree.invariants(), "Invariants failed at step 9556"

    # Operation 9557: delete
    del tree[556]
    del reference[556]
    assert tree.invariants(), "Invariants failed at step 9557"

    # Operation 9558: delete
    del tree[4870]
    del reference[4870]
    assert tree.invariants(), "Invariants failed at step 9558"

    # Operation 9559: insert
    tree[3441] = 'value_388710'
    reference[3441] = 'value_388710'
    assert tree.invariants(), "Invariants failed at step 9559"

    # Operation 9560: get
    assert tree.invariants(), "Invariants failed at step 9560"

    # Operation 9561: delete
    del tree[3378]
    del reference[3378]
    assert tree.invariants(), "Invariants failed at step 9561"

    # Operation 9562: update
    tree[1468] = 'value_478041'
    reference[1468] = 'value_478041'
    assert tree.invariants(), "Invariants failed at step 9562"

    # Operation 9563: delete
    del tree[3531]
    del reference[3531]
    assert tree.invariants(), "Invariants failed at step 9563"

    # Operation 9564: get
    assert tree.invariants(), "Invariants failed at step 9564"

    # Operation 9565: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9565"

    # Operation 9566: delete
    del tree[1468]
    del reference[1468]
    assert tree.invariants(), "Invariants failed at step 9566"

    # Operation 9567: insert
    tree[736] = 'value_904822'
    reference[736] = 'value_904822'
    assert tree.invariants(), "Invariants failed at step 9567"

    # Operation 9568: insert
    tree[5217] = 'value_608355'
    reference[5217] = 'value_608355'
    assert tree.invariants(), "Invariants failed at step 9568"

    # Operation 9569: get
    assert tree.invariants(), "Invariants failed at step 9569"

    # Operation 9570: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9570"

    # Operation 9571: get
    assert tree.invariants(), "Invariants failed at step 9571"

    # Operation 9572: update
    tree[736] = 'value_956058'
    reference[736] = 'value_956058'
    assert tree.invariants(), "Invariants failed at step 9572"

    # Operation 9573: update
    tree[6667] = 'value_432110'
    reference[6667] = 'value_432110'
    assert tree.invariants(), "Invariants failed at step 9573"

    # Operation 9574: delete
    del tree[5217]
    del reference[5217]
    assert tree.invariants(), "Invariants failed at step 9574"

    # Operation 9575: get
    assert tree.invariants(), "Invariants failed at step 9575"

    # Operation 9576: delete
    del tree[6667]
    del reference[6667]
    assert tree.invariants(), "Invariants failed at step 9576"

    # Operation 9577: insert
    tree[2632] = 'value_457617'
    reference[2632] = 'value_457617'
    assert tree.invariants(), "Invariants failed at step 9577"

    # Operation 9578: update
    tree[2632] = 'value_302125'
    reference[2632] = 'value_302125'
    assert tree.invariants(), "Invariants failed at step 9578"

    # Operation 9579: insert
    tree[7320] = 'value_89942'
    reference[7320] = 'value_89942'
    assert tree.invariants(), "Invariants failed at step 9579"

    # Operation 9580: update
    tree[2632] = 'value_797084'
    reference[2632] = 'value_797084'
    assert tree.invariants(), "Invariants failed at step 9580"

    # Operation 9581: insert
    tree[946] = 'value_810753'
    reference[946] = 'value_810753'
    assert tree.invariants(), "Invariants failed at step 9581"

    # Operation 9582: delete
    del tree[3441]
    del reference[3441]
    assert tree.invariants(), "Invariants failed at step 9582"

    # Operation 9583: batch_delete
    keys_to_delete = [7320, 1473, 3758, 736]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9583"

    # Operation 9584: get
    assert tree.invariants(), "Invariants failed at step 9584"

    # Operation 9585: insert
    tree[5960] = 'value_889117'
    reference[5960] = 'value_889117'
    assert tree.invariants(), "Invariants failed at step 9585"

    # Operation 9586: get
    assert tree.invariants(), "Invariants failed at step 9586"

    # Operation 9587: delete
    del tree[5960]
    del reference[5960]
    assert tree.invariants(), "Invariants failed at step 9587"

    # Operation 9588: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9588"

    # Operation 9589: insert
    tree[404] = 'value_921416'
    reference[404] = 'value_921416'
    assert tree.invariants(), "Invariants failed at step 9589"

    # Operation 9590: get
    assert tree.invariants(), "Invariants failed at step 9590"

    # Operation 9591: insert
    tree[6442] = 'value_560958'
    reference[6442] = 'value_560958'
    assert tree.invariants(), "Invariants failed at step 9591"

    # Operation 9592: delete
    del tree[6442]
    del reference[6442]
    assert tree.invariants(), "Invariants failed at step 9592"

    # Operation 9593: insert
    tree[9725] = 'value_478027'
    reference[9725] = 'value_478027'
    assert tree.invariants(), "Invariants failed at step 9593"

    # Operation 9594: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9594"

    # Operation 9595: insert
    tree[521] = 'value_633491'
    reference[521] = 'value_633491'
    assert tree.invariants(), "Invariants failed at step 9595"

    # Operation 9596: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9596"

    # Operation 9597: get
    assert tree.invariants(), "Invariants failed at step 9597"

    # Operation 9598: update
    tree[2632] = 'value_2193'
    reference[2632] = 'value_2193'
    assert tree.invariants(), "Invariants failed at step 9598"

    # Operation 9599: insert
    tree[2059] = 'value_818806'
    reference[2059] = 'value_818806'
    assert tree.invariants(), "Invariants failed at step 9599"

    # Operation 9600: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9600"

    # Operation 9601: insert
    tree[9675] = 'value_14981'
    reference[9675] = 'value_14981'
    assert tree.invariants(), "Invariants failed at step 9601"

    # Operation 9602: delete
    del tree[521]
    del reference[521]
    assert tree.invariants(), "Invariants failed at step 9602"

    # Operation 9603: insert
    tree[3914] = 'value_946099'
    reference[3914] = 'value_946099'
    assert tree.invariants(), "Invariants failed at step 9603"

    # Operation 9604: delete
    del tree[3914]
    del reference[3914]
    assert tree.invariants(), "Invariants failed at step 9604"

    # Operation 9605: delete
    del tree[2632]
    del reference[2632]
    assert tree.invariants(), "Invariants failed at step 9605"

    # Operation 9606: get
    assert tree.invariants(), "Invariants failed at step 9606"

    # Operation 9607: insert
    tree[4957] = 'value_428928'
    reference[4957] = 'value_428928'
    assert tree.invariants(), "Invariants failed at step 9607"

    # Operation 9608: get
    assert tree.invariants(), "Invariants failed at step 9608"

    # Operation 9609: batch_delete
    keys_to_delete = [9288, 4402, 946, 404, 9725]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9609"

    # Operation 9610: update
    tree[2059] = 'value_553908'
    reference[2059] = 'value_553908'
    assert tree.invariants(), "Invariants failed at step 9610"

    # Operation 9611: delete
    del tree[9675]
    del reference[9675]
    assert tree.invariants(), "Invariants failed at step 9611"

    # Operation 9612: delete
    del tree[2059]
    del reference[2059]
    assert tree.invariants(), "Invariants failed at step 9612"

    # Operation 9613: insert
    tree[9842] = 'value_334416'
    reference[9842] = 'value_334416'
    assert tree.invariants(), "Invariants failed at step 9613"

    # Operation 9614: insert
    tree[1494] = 'value_966864'
    reference[1494] = 'value_966864'
    assert tree.invariants(), "Invariants failed at step 9614"

    # Operation 9615: update
    tree[9842] = 'value_645016'
    reference[9842] = 'value_645016'
    assert tree.invariants(), "Invariants failed at step 9615"

    # Operation 9616: insert
    tree[693] = 'value_88461'
    reference[693] = 'value_88461'
    assert tree.invariants(), "Invariants failed at step 9616"

    # Operation 9617: get
    assert tree.invariants(), "Invariants failed at step 9617"

    # Operation 9618: insert
    tree[5861] = 'value_794518'
    reference[5861] = 'value_794518'
    assert tree.invariants(), "Invariants failed at step 9618"

    # Operation 9619: insert
    tree[6527] = 'value_918616'
    reference[6527] = 'value_918616'
    assert tree.invariants(), "Invariants failed at step 9619"

    # Operation 9620: update
    tree[5861] = 'value_876415'
    reference[5861] = 'value_876415'
    assert tree.invariants(), "Invariants failed at step 9620"

    # Operation 9621: update
    tree[693] = 'value_408383'
    reference[693] = 'value_408383'
    assert tree.invariants(), "Invariants failed at step 9621"

    # Operation 9622: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9622"

    # Operation 9623: update
    tree[9842] = 'value_667989'
    reference[9842] = 'value_667989'
    assert tree.invariants(), "Invariants failed at step 9623"

    # Operation 9624: insert
    tree[5894] = 'value_58408'
    reference[5894] = 'value_58408'
    assert tree.invariants(), "Invariants failed at step 9624"

    # Operation 9625: insert
    tree[1371] = 'value_681924'
    reference[1371] = 'value_681924'
    assert tree.invariants(), "Invariants failed at step 9625"

    # Operation 9626: get
    assert tree.invariants(), "Invariants failed at step 9626"

    # Operation 9627: get
    assert tree.invariants(), "Invariants failed at step 9627"

    # Operation 9628: update
    tree[5861] = 'value_628873'
    reference[5861] = 'value_628873'
    assert tree.invariants(), "Invariants failed at step 9628"

    # Operation 9629: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9629"

    # Operation 9630: delete
    del tree[4957]
    del reference[4957]
    assert tree.invariants(), "Invariants failed at step 9630"

    # Operation 9631: insert
    tree[2575] = 'value_388927'
    reference[2575] = 'value_388927'
    assert tree.invariants(), "Invariants failed at step 9631"

    # Operation 9632: update
    tree[1494] = 'value_441668'
    reference[1494] = 'value_441668'
    assert tree.invariants(), "Invariants failed at step 9632"

    # Operation 9633: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9633"

    # Operation 9634: insert
    tree[9859] = 'value_158968'
    reference[9859] = 'value_158968'
    assert tree.invariants(), "Invariants failed at step 9634"

    # Operation 9635: delete
    del tree[9859]
    del reference[9859]
    assert tree.invariants(), "Invariants failed at step 9635"

    # Operation 9636: batch_delete
    keys_to_delete = [3331, 6382, 2575, 9842, 693, 6527]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9636"

    # Operation 9637: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9637"

    # Operation 9638: update
    tree[5894] = 'value_686208'
    reference[5894] = 'value_686208'
    assert tree.invariants(), "Invariants failed at step 9638"

    # Operation 9639: update
    tree[5894] = 'value_510247'
    reference[5894] = 'value_510247'
    assert tree.invariants(), "Invariants failed at step 9639"

    # Operation 9640: insert
    tree[8130] = 'value_927140'
    reference[8130] = 'value_927140'
    assert tree.invariants(), "Invariants failed at step 9640"

    # Operation 9641: update
    tree[1494] = 'value_399271'
    reference[1494] = 'value_399271'
    assert tree.invariants(), "Invariants failed at step 9641"

    # Operation 9642: batch_delete
    keys_to_delete = [3161, 8130, 1494, 1063]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9642"

    # Operation 9643: insert
    tree[4893] = 'value_979936'
    reference[4893] = 'value_979936'
    assert tree.invariants(), "Invariants failed at step 9643"

    # Operation 9644: insert
    tree[5153] = 'value_110084'
    reference[5153] = 'value_110084'
    assert tree.invariants(), "Invariants failed at step 9644"

    # Operation 9645: delete
    del tree[1371]
    del reference[1371]
    assert tree.invariants(), "Invariants failed at step 9645"

    # Operation 9646: get
    assert tree.invariants(), "Invariants failed at step 9646"

    # Operation 9647: delete
    del tree[5861]
    del reference[5861]
    assert tree.invariants(), "Invariants failed at step 9647"

    # Operation 9648: insert
    tree[731] = 'value_104186'
    reference[731] = 'value_104186'
    assert tree.invariants(), "Invariants failed at step 9648"

    # Operation 9649: insert
    tree[7091] = 'value_724091'
    reference[7091] = 'value_724091'
    assert tree.invariants(), "Invariants failed at step 9649"

    # Operation 9650: insert
    tree[5551] = 'value_328521'
    reference[5551] = 'value_328521'
    assert tree.invariants(), "Invariants failed at step 9650"

    # Operation 9651: update
    tree[5551] = 'value_222347'
    reference[5551] = 'value_222347'
    assert tree.invariants(), "Invariants failed at step 9651"

    # Operation 9652: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9652"

    # Operation 9653: delete
    del tree[5153]
    del reference[5153]
    assert tree.invariants(), "Invariants failed at step 9653"

    # Operation 9654: get
    assert tree.invariants(), "Invariants failed at step 9654"

    # Operation 9655: insert
    tree[689] = 'value_20401'
    reference[689] = 'value_20401'
    assert tree.invariants(), "Invariants failed at step 9655"

    # Operation 9656: insert
    tree[8633] = 'value_376672'
    reference[8633] = 'value_376672'
    assert tree.invariants(), "Invariants failed at step 9656"

    # Operation 9657: delete
    del tree[4893]
    del reference[4893]
    assert tree.invariants(), "Invariants failed at step 9657"

    # Operation 9658: get
    assert tree.invariants(), "Invariants failed at step 9658"

    # Operation 9659: delete
    del tree[731]
    del reference[731]
    assert tree.invariants(), "Invariants failed at step 9659"

    # Operation 9660: insert
    tree[3423] = 'value_642422'
    reference[3423] = 'value_642422'
    assert tree.invariants(), "Invariants failed at step 9660"

    # Operation 9661: delete
    del tree[689]
    del reference[689]
    assert tree.invariants(), "Invariants failed at step 9661"

    # Operation 9662: insert
    tree[3716] = 'value_95892'
    reference[3716] = 'value_95892'
    assert tree.invariants(), "Invariants failed at step 9662"

    # Operation 9663: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9663"

    # Operation 9664: update
    tree[7091] = 'value_882521'
    reference[7091] = 'value_882521'
    assert tree.invariants(), "Invariants failed at step 9664"

    # Operation 9665: batch_delete
    keys_to_delete = [3716, 5894, 9449, 206, 5551]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9665"

    # Operation 9666: update
    tree[3423] = 'value_833291'
    reference[3423] = 'value_833291'
    assert tree.invariants(), "Invariants failed at step 9666"

    # Operation 9667: insert
    tree[1310] = 'value_663915'
    reference[1310] = 'value_663915'
    assert tree.invariants(), "Invariants failed at step 9667"

    # Operation 9668: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9668"

    # Operation 9669: delete
    del tree[8633]
    del reference[8633]
    assert tree.invariants(), "Invariants failed at step 9669"

    # Operation 9670: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9670"

    # Operation 9671: update
    tree[1310] = 'value_481241'
    reference[1310] = 'value_481241'
    assert tree.invariants(), "Invariants failed at step 9671"

    # Operation 9672: get
    assert tree.invariants(), "Invariants failed at step 9672"

    # Operation 9673: delete
    del tree[3423]
    del reference[3423]
    assert tree.invariants(), "Invariants failed at step 9673"

    # Operation 9674: update
    tree[1310] = 'value_535931'
    reference[1310] = 'value_535931'
    assert tree.invariants(), "Invariants failed at step 9674"

    # Operation 9675: get
    assert tree.invariants(), "Invariants failed at step 9675"

    # Operation 9676: update
    tree[1310] = 'value_933700'
    reference[1310] = 'value_933700'
    assert tree.invariants(), "Invariants failed at step 9676"

    # Operation 9677: delete
    del tree[1310]
    del reference[1310]
    assert tree.invariants(), "Invariants failed at step 9677"

    # Operation 9678: insert
    tree[291] = 'value_729535'
    reference[291] = 'value_729535'
    assert tree.invariants(), "Invariants failed at step 9678"

    # Operation 9679: insert
    tree[9764] = 'value_165158'
    reference[9764] = 'value_165158'
    assert tree.invariants(), "Invariants failed at step 9679"

    # Operation 9680: insert
    tree[2152] = 'value_215725'
    reference[2152] = 'value_215725'
    assert tree.invariants(), "Invariants failed at step 9680"

    # Operation 9681: get
    assert tree.invariants(), "Invariants failed at step 9681"

    # Operation 9682: delete
    del tree[2152]
    del reference[2152]
    assert tree.invariants(), "Invariants failed at step 9682"

    # Operation 9683: update
    tree[9764] = 'value_309130'
    reference[9764] = 'value_309130'
    assert tree.invariants(), "Invariants failed at step 9683"

    # Operation 9684: delete
    del tree[9764]
    del reference[9764]
    assert tree.invariants(), "Invariants failed at step 9684"

    # Operation 9685: insert
    tree[8499] = 'value_434797'
    reference[8499] = 'value_434797'
    assert tree.invariants(), "Invariants failed at step 9685"

    # Operation 9686: insert
    tree[3753] = 'value_865962'
    reference[3753] = 'value_865962'
    assert tree.invariants(), "Invariants failed at step 9686"

    # Operation 9687: delete
    del tree[8499]
    del reference[8499]
    assert tree.invariants(), "Invariants failed at step 9687"

    # Operation 9688: delete
    del tree[3753]
    del reference[3753]
    assert tree.invariants(), "Invariants failed at step 9688"

    # Operation 9689: delete
    del tree[7091]
    del reference[7091]
    assert tree.invariants(), "Invariants failed at step 9689"

    # Operation 9690: delete
    del tree[291]
    del reference[291]
    assert tree.invariants(), "Invariants failed at step 9690"

    # Operation 9691: get
    assert tree.invariants(), "Invariants failed at step 9691"

    # Operation 9692: insert
    tree[9518] = 'value_673431'
    reference[9518] = 'value_673431'
    assert tree.invariants(), "Invariants failed at step 9692"

    # Operation 9693: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9693"

    # Operation 9694: delete
    del tree[9518]
    del reference[9518]
    assert tree.invariants(), "Invariants failed at step 9694"

    # Operation 9695: get
    assert tree.invariants(), "Invariants failed at step 9695"

    # Operation 9696: insert
    tree[1892] = 'value_286827'
    reference[1892] = 'value_286827'
    assert tree.invariants(), "Invariants failed at step 9696"

    # Operation 9697: delete
    del tree[1892]
    del reference[1892]
    assert tree.invariants(), "Invariants failed at step 9697"

    # Operation 9698: insert
    tree[3201] = 'value_377924'
    reference[3201] = 'value_377924'
    assert tree.invariants(), "Invariants failed at step 9698"

    # Operation 9699: insert
    tree[1535] = 'value_489831'
    reference[1535] = 'value_489831'
    assert tree.invariants(), "Invariants failed at step 9699"

    # Operation 9700: delete
    del tree[1535]
    del reference[1535]
    assert tree.invariants(), "Invariants failed at step 9700"

    # Operation 9701: delete
    del tree[3201]
    del reference[3201]
    assert tree.invariants(), "Invariants failed at step 9701"

    # Operation 9702: get
    assert tree.invariants(), "Invariants failed at step 9702"

    # Operation 9703: insert
    tree[2723] = 'value_325827'
    reference[2723] = 'value_325827'
    assert tree.invariants(), "Invariants failed at step 9703"

    # Operation 9704: get
    assert tree.invariants(), "Invariants failed at step 9704"

    # Operation 9705: get
    assert tree.invariants(), "Invariants failed at step 9705"

    # Operation 9706: delete
    del tree[2723]
    del reference[2723]
    assert tree.invariants(), "Invariants failed at step 9706"

    # Operation 9707: get
    assert tree.invariants(), "Invariants failed at step 9707"

    # Operation 9708: insert
    tree[5259] = 'value_490833'
    reference[5259] = 'value_490833'
    assert tree.invariants(), "Invariants failed at step 9708"

    # Operation 9709: get
    assert tree.invariants(), "Invariants failed at step 9709"

    # Operation 9710: delete
    del tree[5259]
    del reference[5259]
    assert tree.invariants(), "Invariants failed at step 9710"

    # Operation 9711: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9711"

    # Operation 9712: get
    assert tree.invariants(), "Invariants failed at step 9712"

    # Operation 9713: insert
    tree[3602] = 'value_100681'
    reference[3602] = 'value_100681'
    assert tree.invariants(), "Invariants failed at step 9713"

    # Operation 9714: insert
    tree[9468] = 'value_170197'
    reference[9468] = 'value_170197'
    assert tree.invariants(), "Invariants failed at step 9714"

    # Operation 9715: insert
    tree[410] = 'value_335291'
    reference[410] = 'value_335291'
    assert tree.invariants(), "Invariants failed at step 9715"

    # Operation 9716: get
    assert tree.invariants(), "Invariants failed at step 9716"

    # Operation 9717: insert
    tree[8114] = 'value_442303'
    reference[8114] = 'value_442303'
    assert tree.invariants(), "Invariants failed at step 9717"

    # Operation 9718: get
    assert tree.invariants(), "Invariants failed at step 9718"

    # Operation 9719: get
    assert tree.invariants(), "Invariants failed at step 9719"

    # Operation 9720: delete
    del tree[8114]
    del reference[8114]
    assert tree.invariants(), "Invariants failed at step 9720"

    # Operation 9721: insert
    tree[2033] = 'value_545392'
    reference[2033] = 'value_545392'
    assert tree.invariants(), "Invariants failed at step 9721"

    # Operation 9722: get
    assert tree.invariants(), "Invariants failed at step 9722"

    # Operation 9723: delete
    del tree[2033]
    del reference[2033]
    assert tree.invariants(), "Invariants failed at step 9723"

    # Operation 9724: delete
    del tree[410]
    del reference[410]
    assert tree.invariants(), "Invariants failed at step 9724"

    # Operation 9725: delete
    del tree[3602]
    del reference[3602]
    assert tree.invariants(), "Invariants failed at step 9725"

    # Operation 9726: delete
    del tree[9468]
    del reference[9468]
    assert tree.invariants(), "Invariants failed at step 9726"

    # Operation 9727: insert
    tree[1470] = 'value_392020'
    reference[1470] = 'value_392020'
    assert tree.invariants(), "Invariants failed at step 9727"

    # Operation 9728: insert
    tree[7673] = 'value_349875'
    reference[7673] = 'value_349875'
    assert tree.invariants(), "Invariants failed at step 9728"

    # Operation 9729: insert
    tree[8266] = 'value_761353'
    reference[8266] = 'value_761353'
    assert tree.invariants(), "Invariants failed at step 9729"

    # Operation 9730: get
    assert tree.invariants(), "Invariants failed at step 9730"

    # Operation 9731: update
    tree[1470] = 'value_912254'
    reference[1470] = 'value_912254'
    assert tree.invariants(), "Invariants failed at step 9731"

    # Operation 9732: delete
    del tree[1470]
    del reference[1470]
    assert tree.invariants(), "Invariants failed at step 9732"

    # Operation 9733: update
    tree[7673] = 'value_713301'
    reference[7673] = 'value_713301'
    assert tree.invariants(), "Invariants failed at step 9733"

    # Operation 9734: delete
    del tree[8266]
    del reference[8266]
    assert tree.invariants(), "Invariants failed at step 9734"

    # Operation 9735: delete
    del tree[7673]
    del reference[7673]
    assert tree.invariants(), "Invariants failed at step 9735"

    # Operation 9736: get
    assert tree.invariants(), "Invariants failed at step 9736"

    # Operation 9737: insert
    tree[8316] = 'value_164766'
    reference[8316] = 'value_164766'
    assert tree.invariants(), "Invariants failed at step 9737"

    # Operation 9738: insert
    tree[5712] = 'value_72340'
    reference[5712] = 'value_72340'
    assert tree.invariants(), "Invariants failed at step 9738"

    # Operation 9739: insert
    tree[6365] = 'value_753771'
    reference[6365] = 'value_753771'
    assert tree.invariants(), "Invariants failed at step 9739"

    # Operation 9740: insert
    tree[9801] = 'value_14081'
    reference[9801] = 'value_14081'
    assert tree.invariants(), "Invariants failed at step 9740"

    # Operation 9741: insert
    tree[6296] = 'value_425764'
    reference[6296] = 'value_425764'
    assert tree.invariants(), "Invariants failed at step 9741"

    # Operation 9742: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9742"

    # Operation 9743: get
    assert tree.invariants(), "Invariants failed at step 9743"

    # Operation 9744: update
    tree[9801] = 'value_185009'
    reference[9801] = 'value_185009'
    assert tree.invariants(), "Invariants failed at step 9744"

    # Operation 9745: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9745"

    # Operation 9746: batch_delete
    keys_to_delete = [5712, 9801, 3184, 6365]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9746"

    # Operation 9747: insert
    tree[5742] = 'value_312964'
    reference[5742] = 'value_312964'
    assert tree.invariants(), "Invariants failed at step 9747"

    # Operation 9748: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9748"

    # Operation 9749: insert
    tree[4918] = 'value_723243'
    reference[4918] = 'value_723243'
    assert tree.invariants(), "Invariants failed at step 9749"

    # Operation 9750: insert
    tree[3405] = 'value_199862'
    reference[3405] = 'value_199862'
    assert tree.invariants(), "Invariants failed at step 9750"

    # Operation 9751: delete
    del tree[5742]
    del reference[5742]
    assert tree.invariants(), "Invariants failed at step 9751"

    # Operation 9752: insert
    tree[8801] = 'value_395254'
    reference[8801] = 'value_395254'
    assert tree.invariants(), "Invariants failed at step 9752"

    # Operation 9753: update
    tree[8316] = 'value_145942'
    reference[8316] = 'value_145942'
    assert tree.invariants(), "Invariants failed at step 9753"

    # Operation 9754: insert
    tree[5687] = 'value_360373'
    reference[5687] = 'value_360373'
    assert tree.invariants(), "Invariants failed at step 9754"

    # Operation 9755: delete
    del tree[8801]
    del reference[8801]
    assert tree.invariants(), "Invariants failed at step 9755"

    # Operation 9756: delete
    del tree[6296]
    del reference[6296]
    assert tree.invariants(), "Invariants failed at step 9756"

    # Operation 9757: get
    assert tree.invariants(), "Invariants failed at step 9757"

    # Operation 9758: insert
    tree[4867] = 'value_895057'
    reference[4867] = 'value_895057'
    assert tree.invariants(), "Invariants failed at step 9758"

    # Operation 9759: insert
    tree[6351] = 'value_20781'
    reference[6351] = 'value_20781'
    assert tree.invariants(), "Invariants failed at step 9759"

    # Operation 9760: batch_delete
    keys_to_delete = [5687, 7500, 6351, 6647, 8316]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9760"

    # Operation 9761: insert
    tree[7705] = 'value_707535'
    reference[7705] = 'value_707535'
    assert tree.invariants(), "Invariants failed at step 9761"

    # Operation 9762: insert
    tree[3249] = 'value_774232'
    reference[3249] = 'value_774232'
    assert tree.invariants(), "Invariants failed at step 9762"

    # Operation 9763: delete
    del tree[3405]
    del reference[3405]
    assert tree.invariants(), "Invariants failed at step 9763"

    # Operation 9764: delete
    del tree[4918]
    del reference[4918]
    assert tree.invariants(), "Invariants failed at step 9764"

    # Operation 9765: insert
    tree[1203] = 'value_687301'
    reference[1203] = 'value_687301'
    assert tree.invariants(), "Invariants failed at step 9765"

    # Operation 9766: get
    assert tree.invariants(), "Invariants failed at step 9766"

    # Operation 9767: delete
    del tree[7705]
    del reference[7705]
    assert tree.invariants(), "Invariants failed at step 9767"

    # Operation 9768: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9768"

    # Operation 9769: get
    assert tree.invariants(), "Invariants failed at step 9769"

    # Operation 9770: get
    assert tree.invariants(), "Invariants failed at step 9770"

    # Operation 9771: delete
    del tree[1203]
    del reference[1203]
    assert tree.invariants(), "Invariants failed at step 9771"

    # Operation 9772: delete
    del tree[4867]
    del reference[4867]
    assert tree.invariants(), "Invariants failed at step 9772"

    # Operation 9773: insert
    tree[3573] = 'value_150957'
    reference[3573] = 'value_150957'
    assert tree.invariants(), "Invariants failed at step 9773"

    # Operation 9774: insert
    tree[1222] = 'value_314013'
    reference[1222] = 'value_314013'
    assert tree.invariants(), "Invariants failed at step 9774"

    # Operation 9775: get
    assert tree.invariants(), "Invariants failed at step 9775"

    # Operation 9776: delete
    del tree[3573]
    del reference[3573]
    assert tree.invariants(), "Invariants failed at step 9776"

    # Operation 9777: get
    assert tree.invariants(), "Invariants failed at step 9777"

    # Operation 9778: get
    assert tree.invariants(), "Invariants failed at step 9778"

    # Operation 9779: get
    assert tree.invariants(), "Invariants failed at step 9779"

    # Operation 9780: delete
    del tree[3249]
    del reference[3249]
    assert tree.invariants(), "Invariants failed at step 9780"

    # Operation 9781: get
    assert tree.invariants(), "Invariants failed at step 9781"

    # Operation 9782: update
    tree[1222] = 'value_311605'
    reference[1222] = 'value_311605'
    assert tree.invariants(), "Invariants failed at step 9782"

    # Operation 9783: insert
    tree[1144] = 'value_228913'
    reference[1144] = 'value_228913'
    assert tree.invariants(), "Invariants failed at step 9783"

    # Operation 9784: insert
    tree[1399] = 'value_615372'
    reference[1399] = 'value_615372'
    assert tree.invariants(), "Invariants failed at step 9784"

    # Operation 9785: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9785"

    # Operation 9786: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9786"

    # Operation 9787: delete
    del tree[1222]
    del reference[1222]
    assert tree.invariants(), "Invariants failed at step 9787"

    # Operation 9788: update
    tree[1399] = 'value_369958'
    reference[1399] = 'value_369958'
    assert tree.invariants(), "Invariants failed at step 9788"

    # Operation 9789: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9789"

    # Operation 9790: delete
    del tree[1144]
    del reference[1144]
    assert tree.invariants(), "Invariants failed at step 9790"

    # Operation 9791: insert
    tree[316] = 'value_511599'
    reference[316] = 'value_511599'
    assert tree.invariants(), "Invariants failed at step 9791"

    # Operation 9792: insert
    tree[783] = 'value_516678'
    reference[783] = 'value_516678'
    assert tree.invariants(), "Invariants failed at step 9792"

    # Operation 9793: insert
    tree[8569] = 'value_690611'
    reference[8569] = 'value_690611'
    assert tree.invariants(), "Invariants failed at step 9793"

    # Operation 9794: insert
    tree[9393] = 'value_889398'
    reference[9393] = 'value_889398'
    assert tree.invariants(), "Invariants failed at step 9794"

    # Operation 9795: update
    tree[9393] = 'value_50455'
    reference[9393] = 'value_50455'
    assert tree.invariants(), "Invariants failed at step 9795"

    # Operation 9796: batch_delete
    keys_to_delete = [9393, 4387, 316, 5550]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9796"

    # Operation 9797: get
    assert tree.invariants(), "Invariants failed at step 9797"

    # Operation 9798: delete
    del tree[1399]
    del reference[1399]
    assert tree.invariants(), "Invariants failed at step 9798"

    # Operation 9799: get
    assert tree.invariants(), "Invariants failed at step 9799"

    # Operation 9800: delete
    del tree[783]
    del reference[783]
    assert tree.invariants(), "Invariants failed at step 9800"

    # Operation 9801: delete
    del tree[8569]
    del reference[8569]
    assert tree.invariants(), "Invariants failed at step 9801"

    # Operation 9802: insert
    tree[298] = 'value_576083'
    reference[298] = 'value_576083'
    assert tree.invariants(), "Invariants failed at step 9802"

    # Operation 9803: get
    assert tree.invariants(), "Invariants failed at step 9803"

    # Operation 9804: delete
    del tree[298]
    del reference[298]
    assert tree.invariants(), "Invariants failed at step 9804"

    # Operation 9805: insert
    tree[7087] = 'value_515730'
    reference[7087] = 'value_515730'
    assert tree.invariants(), "Invariants failed at step 9805"

    # Operation 9806: delete
    del tree[7087]
    del reference[7087]
    assert tree.invariants(), "Invariants failed at step 9806"

    # Operation 9807: insert
    tree[4747] = 'value_42284'
    reference[4747] = 'value_42284'
    assert tree.invariants(), "Invariants failed at step 9807"

    # Operation 9808: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9808"

    # Operation 9809: update
    tree[4747] = 'value_491086'
    reference[4747] = 'value_491086'
    assert tree.invariants(), "Invariants failed at step 9809"

    # Operation 9810: delete
    del tree[4747]
    del reference[4747]
    assert tree.invariants(), "Invariants failed at step 9810"

    # Operation 9811: get
    assert tree.invariants(), "Invariants failed at step 9811"

    # Operation 9812: insert
    tree[8523] = 'value_325633'
    reference[8523] = 'value_325633'
    assert tree.invariants(), "Invariants failed at step 9812"

    # Operation 9813: get
    assert tree.invariants(), "Invariants failed at step 9813"

    # Operation 9814: get
    assert tree.invariants(), "Invariants failed at step 9814"

    # Operation 9815: update
    tree[8523] = 'value_789583'
    reference[8523] = 'value_789583'
    assert tree.invariants(), "Invariants failed at step 9815"

    # Operation 9816: insert
    tree[4943] = 'value_251100'
    reference[4943] = 'value_251100'
    assert tree.invariants(), "Invariants failed at step 9816"

    # Operation 9817: get
    assert tree.invariants(), "Invariants failed at step 9817"

    # Operation 9818: delete
    del tree[4943]
    del reference[4943]
    assert tree.invariants(), "Invariants failed at step 9818"

    # Operation 9819: get
    assert tree.invariants(), "Invariants failed at step 9819"

    # Operation 9820: update
    tree[8523] = 'value_97656'
    reference[8523] = 'value_97656'
    assert tree.invariants(), "Invariants failed at step 9820"

    # Operation 9821: delete
    del tree[8523]
    del reference[8523]
    assert tree.invariants(), "Invariants failed at step 9821"

    # Operation 9822: insert
    tree[3111] = 'value_10080'
    reference[3111] = 'value_10080'
    assert tree.invariants(), "Invariants failed at step 9822"

    # Operation 9823: insert
    tree[8919] = 'value_562015'
    reference[8919] = 'value_562015'
    assert tree.invariants(), "Invariants failed at step 9823"

    # Operation 9824: insert
    tree[1345] = 'value_534586'
    reference[1345] = 'value_534586'
    assert tree.invariants(), "Invariants failed at step 9824"

    # Operation 9825: delete
    del tree[8919]
    del reference[8919]
    assert tree.invariants(), "Invariants failed at step 9825"

    # Operation 9826: get
    assert tree.invariants(), "Invariants failed at step 9826"

    # Operation 9827: insert
    tree[1017] = 'value_687037'
    reference[1017] = 'value_687037'
    assert tree.invariants(), "Invariants failed at step 9827"

    # Operation 9828: insert
    tree[9033] = 'value_406804'
    reference[9033] = 'value_406804'
    assert tree.invariants(), "Invariants failed at step 9828"

    # Operation 9829: delete
    del tree[9033]
    del reference[9033]
    assert tree.invariants(), "Invariants failed at step 9829"

    # Operation 9830: delete
    del tree[1345]
    del reference[1345]
    assert tree.invariants(), "Invariants failed at step 9830"

    # Operation 9831: update
    tree[3111] = 'value_945563'
    reference[3111] = 'value_945563'
    assert tree.invariants(), "Invariants failed at step 9831"

    # Operation 9832: get
    assert tree.invariants(), "Invariants failed at step 9832"

    # Operation 9833: update
    tree[1017] = 'value_929140'
    reference[1017] = 'value_929140'
    assert tree.invariants(), "Invariants failed at step 9833"

    # Operation 9834: delete
    del tree[1017]
    del reference[1017]
    assert tree.invariants(), "Invariants failed at step 9834"

    # Operation 9835: delete
    del tree[3111]
    del reference[3111]
    assert tree.invariants(), "Invariants failed at step 9835"

    # Operation 9836: get
    assert tree.invariants(), "Invariants failed at step 9836"

    # Operation 9837: get
    assert tree.invariants(), "Invariants failed at step 9837"

    # Operation 9838: get
    assert tree.invariants(), "Invariants failed at step 9838"

    # Operation 9839: get
    assert tree.invariants(), "Invariants failed at step 9839"

    # Operation 9840: insert
    tree[9341] = 'value_702424'
    reference[9341] = 'value_702424'
    assert tree.invariants(), "Invariants failed at step 9840"

    # Operation 9841: get
    assert tree.invariants(), "Invariants failed at step 9841"

    # Operation 9842: insert
    tree[5158] = 'value_942318'
    reference[5158] = 'value_942318'
    assert tree.invariants(), "Invariants failed at step 9842"

    # Operation 9843: get
    assert tree.invariants(), "Invariants failed at step 9843"

    # Operation 9844: update
    tree[9341] = 'value_239618'
    reference[9341] = 'value_239618'
    assert tree.invariants(), "Invariants failed at step 9844"

    # Operation 9845: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9845"

    # Operation 9846: update
    tree[9341] = 'value_755132'
    reference[9341] = 'value_755132'
    assert tree.invariants(), "Invariants failed at step 9846"

    # Operation 9847: delete
    del tree[9341]
    del reference[9341]
    assert tree.invariants(), "Invariants failed at step 9847"

    # Operation 9848: insert
    tree[2756] = 'value_654826'
    reference[2756] = 'value_654826'
    assert tree.invariants(), "Invariants failed at step 9848"

    # Operation 9849: delete
    del tree[5158]
    del reference[5158]
    assert tree.invariants(), "Invariants failed at step 9849"

    # Operation 9850: delete
    del tree[2756]
    del reference[2756]
    assert tree.invariants(), "Invariants failed at step 9850"

    # Operation 9851: insert
    tree[337] = 'value_626110'
    reference[337] = 'value_626110'
    assert tree.invariants(), "Invariants failed at step 9851"

    # Operation 9852: delete
    del tree[337]
    del reference[337]
    assert tree.invariants(), "Invariants failed at step 9852"

    # Operation 9853: insert
    tree[4790] = 'value_449218'
    reference[4790] = 'value_449218'
    assert tree.invariants(), "Invariants failed at step 9853"

    # Operation 9854: insert
    tree[9401] = 'value_569659'
    reference[9401] = 'value_569659'
    assert tree.invariants(), "Invariants failed at step 9854"

    # Operation 9855: insert
    tree[2932] = 'value_841962'
    reference[2932] = 'value_841962'
    assert tree.invariants(), "Invariants failed at step 9855"

    # Operation 9856: update
    tree[2932] = 'value_142684'
    reference[2932] = 'value_142684'
    assert tree.invariants(), "Invariants failed at step 9856"

    # Operation 9857: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9857"

    # Operation 9858: insert
    tree[5953] = 'value_611798'
    reference[5953] = 'value_611798'
    assert tree.invariants(), "Invariants failed at step 9858"

    # Operation 9859: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9859"

    # Operation 9860: insert
    tree[3313] = 'value_478538'
    reference[3313] = 'value_478538'
    assert tree.invariants(), "Invariants failed at step 9860"

    # Operation 9861: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9861"

    # Operation 9862: delete
    del tree[3313]
    del reference[3313]
    assert tree.invariants(), "Invariants failed at step 9862"

    # Operation 9863: get
    assert tree.invariants(), "Invariants failed at step 9863"

    # Operation 9864: insert
    tree[9696] = 'value_3675'
    reference[9696] = 'value_3675'
    assert tree.invariants(), "Invariants failed at step 9864"

    # Operation 9865: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9865"

    # Operation 9866: insert
    tree[6350] = 'value_291731'
    reference[6350] = 'value_291731'
    assert tree.invariants(), "Invariants failed at step 9866"

    # Operation 9867: insert
    tree[800] = 'value_276392'
    reference[800] = 'value_276392'
    assert tree.invariants(), "Invariants failed at step 9867"

    # Operation 9868: batch_delete
    keys_to_delete = [9696, 3211, 6350, 7539, 2932]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9868"

    # Operation 9869: get
    assert tree.invariants(), "Invariants failed at step 9869"

    # Operation 9870: delete
    del tree[9401]
    del reference[9401]
    assert tree.invariants(), "Invariants failed at step 9870"

    # Operation 9871: insert
    tree[6941] = 'value_680095'
    reference[6941] = 'value_680095'
    assert tree.invariants(), "Invariants failed at step 9871"

    # Operation 9872: delete
    del tree[4790]
    del reference[4790]
    assert tree.invariants(), "Invariants failed at step 9872"

    # Operation 9873: update
    tree[6941] = 'value_994335'
    reference[6941] = 'value_994335'
    assert tree.invariants(), "Invariants failed at step 9873"

    # Operation 9874: insert
    tree[7077] = 'value_750187'
    reference[7077] = 'value_750187'
    assert tree.invariants(), "Invariants failed at step 9874"

    # Operation 9875: update
    tree[6941] = 'value_151096'
    reference[6941] = 'value_151096'
    assert tree.invariants(), "Invariants failed at step 9875"

    # Operation 9876: get
    assert tree.invariants(), "Invariants failed at step 9876"

    # Operation 9877: get
    assert tree.invariants(), "Invariants failed at step 9877"

    # Operation 9878: delete
    del tree[5953]
    del reference[5953]
    assert tree.invariants(), "Invariants failed at step 9878"

    # Operation 9879: delete
    del tree[800]
    del reference[800]
    assert tree.invariants(), "Invariants failed at step 9879"

    # Operation 9880: delete
    del tree[7077]
    del reference[7077]
    assert tree.invariants(), "Invariants failed at step 9880"

    # Operation 9881: delete
    del tree[6941]
    del reference[6941]
    assert tree.invariants(), "Invariants failed at step 9881"

    # Operation 9882: insert
    tree[4246] = 'value_7957'
    reference[4246] = 'value_7957'
    assert tree.invariants(), "Invariants failed at step 9882"

    # Operation 9883: insert
    tree[9259] = 'value_854237'
    reference[9259] = 'value_854237'
    assert tree.invariants(), "Invariants failed at step 9883"

    # Operation 9884: insert
    tree[7473] = 'value_559539'
    reference[7473] = 'value_559539'
    assert tree.invariants(), "Invariants failed at step 9884"

    # Operation 9885: delete
    del tree[4246]
    del reference[4246]
    assert tree.invariants(), "Invariants failed at step 9885"

    # Operation 9886: insert
    tree[7256] = 'value_65252'
    reference[7256] = 'value_65252'
    assert tree.invariants(), "Invariants failed at step 9886"

    # Operation 9887: update
    tree[9259] = 'value_139171'
    reference[9259] = 'value_139171'
    assert tree.invariants(), "Invariants failed at step 9887"

    # Operation 9888: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9888"

    # Operation 9889: delete
    del tree[7256]
    del reference[7256]
    assert tree.invariants(), "Invariants failed at step 9889"

    # Operation 9890: insert
    tree[778] = 'value_917722'
    reference[778] = 'value_917722'
    assert tree.invariants(), "Invariants failed at step 9890"

    # Operation 9891: get
    assert tree.invariants(), "Invariants failed at step 9891"

    # Operation 9892: insert
    tree[2272] = 'value_293494'
    reference[2272] = 'value_293494'
    assert tree.invariants(), "Invariants failed at step 9892"

    # Operation 9893: insert
    tree[3972] = 'value_640537'
    reference[3972] = 'value_640537'
    assert tree.invariants(), "Invariants failed at step 9893"

    # Operation 9894: batch_delete
    keys_to_delete = [2272, 385, 4578, 3972]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9894"

    # Operation 9895: delete
    del tree[778]
    del reference[778]
    assert tree.invariants(), "Invariants failed at step 9895"

    # Operation 9896: delete
    del tree[7473]
    del reference[7473]
    assert tree.invariants(), "Invariants failed at step 9896"

    # Operation 9897: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9897"

    # Operation 9898: update
    tree[9259] = 'value_138119'
    reference[9259] = 'value_138119'
    assert tree.invariants(), "Invariants failed at step 9898"

    # Operation 9899: delete
    del tree[9259]
    del reference[9259]
    assert tree.invariants(), "Invariants failed at step 9899"

    # Operation 9900: get
    assert tree.invariants(), "Invariants failed at step 9900"

    # Operation 9901: get
    assert tree.invariants(), "Invariants failed at step 9901"

    # Operation 9902: insert
    tree[7575] = 'value_510931'
    reference[7575] = 'value_510931'
    assert tree.invariants(), "Invariants failed at step 9902"

    # Operation 9903: insert
    tree[8150] = 'value_83715'
    reference[8150] = 'value_83715'
    assert tree.invariants(), "Invariants failed at step 9903"

    # Operation 9904: get
    assert tree.invariants(), "Invariants failed at step 9904"

    # Operation 9905: delete
    del tree[8150]
    del reference[8150]
    assert tree.invariants(), "Invariants failed at step 9905"

    # Operation 9906: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9906"

    # Operation 9907: insert
    tree[6912] = 'value_827683'
    reference[6912] = 'value_827683'
    assert tree.invariants(), "Invariants failed at step 9907"

    # Operation 9908: insert
    tree[1521] = 'value_272287'
    reference[1521] = 'value_272287'
    assert tree.invariants(), "Invariants failed at step 9908"

    # Operation 9909: delete
    del tree[6912]
    del reference[6912]
    assert tree.invariants(), "Invariants failed at step 9909"

    # Operation 9910: get
    assert tree.invariants(), "Invariants failed at step 9910"

    # Operation 9911: delete
    del tree[7575]
    del reference[7575]
    assert tree.invariants(), "Invariants failed at step 9911"

    # Operation 9912: insert
    tree[5524] = 'value_847572'
    reference[5524] = 'value_847572'
    assert tree.invariants(), "Invariants failed at step 9912"

    # Operation 9913: get
    assert tree.invariants(), "Invariants failed at step 9913"

    # Operation 9914: get
    assert tree.invariants(), "Invariants failed at step 9914"

    # Operation 9915: get
    assert tree.invariants(), "Invariants failed at step 9915"

    # Operation 9916: get
    assert tree.invariants(), "Invariants failed at step 9916"

    # Operation 9917: insert
    tree[3017] = 'value_612220'
    reference[3017] = 'value_612220'
    assert tree.invariants(), "Invariants failed at step 9917"

    # Operation 9918: get
    assert tree.invariants(), "Invariants failed at step 9918"

    # Operation 9919: get
    assert tree.invariants(), "Invariants failed at step 9919"

    # Operation 9920: update
    tree[5524] = 'value_821824'
    reference[5524] = 'value_821824'
    assert tree.invariants(), "Invariants failed at step 9920"

    # Operation 9921: update
    tree[1521] = 'value_45372'
    reference[1521] = 'value_45372'
    assert tree.invariants(), "Invariants failed at step 9921"

    # Operation 9922: insert
    tree[8755] = 'value_474563'
    reference[8755] = 'value_474563'
    assert tree.invariants(), "Invariants failed at step 9922"

    # Operation 9923: insert
    tree[3075] = 'value_528652'
    reference[3075] = 'value_528652'
    assert tree.invariants(), "Invariants failed at step 9923"

    # Operation 9924: insert
    tree[4314] = 'value_70560'
    reference[4314] = 'value_70560'
    assert tree.invariants(), "Invariants failed at step 9924"

    # Operation 9925: get
    assert tree.invariants(), "Invariants failed at step 9925"

    # Operation 9926: get
    assert tree.invariants(), "Invariants failed at step 9926"

    # Operation 9927: insert
    tree[5849] = 'value_837704'
    reference[5849] = 'value_837704'
    assert tree.invariants(), "Invariants failed at step 9927"

    # Operation 9928: insert
    tree[7739] = 'value_514495'
    reference[7739] = 'value_514495'
    assert tree.invariants(), "Invariants failed at step 9928"

    # Operation 9929: get
    assert tree.invariants(), "Invariants failed at step 9929"

    # Operation 9930: insert
    tree[8150] = 'value_97438'
    reference[8150] = 'value_97438'
    assert tree.invariants(), "Invariants failed at step 9930"

    # Operation 9931: get
    assert tree.invariants(), "Invariants failed at step 9931"

    # Operation 9932: delete
    del tree[5849]
    del reference[5849]
    assert tree.invariants(), "Invariants failed at step 9932"

    # Operation 9933: update
    tree[7739] = 'value_436881'
    reference[7739] = 'value_436881'
    assert tree.invariants(), "Invariants failed at step 9933"

    # Operation 9934: insert
    tree[2989] = 'value_294935'
    reference[2989] = 'value_294935'
    assert tree.invariants(), "Invariants failed at step 9934"

    # Operation 9935: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9935"

    # Operation 9936: insert
    tree[4034] = 'value_229078'
    reference[4034] = 'value_229078'
    assert tree.invariants(), "Invariants failed at step 9936"

    # Operation 9937: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 9937"

    # Operation 9938: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9938"

    # Operation 9939: update
    tree[2989] = 'value_949037'
    reference[2989] = 'value_949037'
    assert tree.invariants(), "Invariants failed at step 9939"

    # Operation 9940: insert
    tree[2718] = 'value_743219'
    reference[2718] = 'value_743219'
    assert tree.invariants(), "Invariants failed at step 9940"

    # Operation 9941: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9941"

    # Operation 9942: get
    assert tree.invariants(), "Invariants failed at step 9942"

    # Operation 9943: delete
    del tree[4314]
    del reference[4314]
    assert tree.invariants(), "Invariants failed at step 9943"

    # Operation 9944: insert
    tree[7482] = 'value_454617'
    reference[7482] = 'value_454617'
    assert tree.invariants(), "Invariants failed at step 9944"

    # Operation 9945: delete
    del tree[4034]
    del reference[4034]
    assert tree.invariants(), "Invariants failed at step 9945"

    # Operation 9946: insert
    tree[4536] = 'value_278173'
    reference[4536] = 'value_278173'
    assert tree.invariants(), "Invariants failed at step 9946"

    # Operation 9947: batch_delete
    keys_to_delete = [2718, 3075, 8150, 9327]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9947"

    # Operation 9948: insert
    tree[8533] = 'value_192715'
    reference[8533] = 'value_192715'
    assert tree.invariants(), "Invariants failed at step 9948"

    # Operation 9949: get
    assert tree.invariants(), "Invariants failed at step 9949"

    # Operation 9950: batch_delete
    keys_to_delete = [9350, 3017, 2989, 3281, 4536, 7739]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9950"

    # Operation 9951: get
    assert tree.invariants(), "Invariants failed at step 9951"

    # Operation 9952: batch_delete
    keys_to_delete = [8968, 8755, 8533, 2583]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9952"

    # Operation 9953: get
    assert tree.invariants(), "Invariants failed at step 9953"

    # Operation 9954: insert
    tree[610] = 'value_434186'
    reference[610] = 'value_434186'
    assert tree.invariants(), "Invariants failed at step 9954"

    # Operation 9955: delete
    del tree[5524]
    del reference[5524]
    assert tree.invariants(), "Invariants failed at step 9955"

    # Operation 9956: insert
    tree[4187] = 'value_257538'
    reference[4187] = 'value_257538'
    assert tree.invariants(), "Invariants failed at step 9956"

    # Operation 9957: get
    assert tree.invariants(), "Invariants failed at step 9957"

    # Operation 9958: delete
    del tree[7482]
    del reference[7482]
    assert tree.invariants(), "Invariants failed at step 9958"

    # Operation 9959: insert
    tree[7985] = 'value_192937'
    reference[7985] = 'value_192937'
    assert tree.invariants(), "Invariants failed at step 9959"

    # Operation 9960: update
    tree[7985] = 'value_893567'
    reference[7985] = 'value_893567'
    assert tree.invariants(), "Invariants failed at step 9960"

    # Operation 9961: insert
    tree[8370] = 'value_775464'
    reference[8370] = 'value_775464'
    assert tree.invariants(), "Invariants failed at step 9961"

    # Operation 9962: get
    assert tree.invariants(), "Invariants failed at step 9962"

    # Operation 9963: insert
    tree[1867] = 'value_178223'
    reference[1867] = 'value_178223'
    assert tree.invariants(), "Invariants failed at step 9963"

    # Operation 9964: delete
    del tree[1867]
    del reference[1867]
    assert tree.invariants(), "Invariants failed at step 9964"

    # Operation 9965: update
    tree[610] = 'value_375190'
    reference[610] = 'value_375190'
    assert tree.invariants(), "Invariants failed at step 9965"

    # Operation 9966: update
    tree[4187] = 'value_518129'
    reference[4187] = 'value_518129'
    assert tree.invariants(), "Invariants failed at step 9966"

    # Operation 9967: insert
    tree[4073] = 'value_377402'
    reference[4073] = 'value_377402'
    assert tree.invariants(), "Invariants failed at step 9967"

    # Operation 9968: insert
    tree[4792] = 'value_947396'
    reference[4792] = 'value_947396'
    assert tree.invariants(), "Invariants failed at step 9968"

    # Operation 9969: update
    tree[4073] = 'value_263512'
    reference[4073] = 'value_263512'
    assert tree.invariants(), "Invariants failed at step 9969"

    # Operation 9970: delete
    del tree[1521]
    del reference[1521]
    assert tree.invariants(), "Invariants failed at step 9970"

    # Operation 9971: get
    assert tree.invariants(), "Invariants failed at step 9971"

    # Operation 9972: insert
    tree[1641] = 'value_416148'
    reference[1641] = 'value_416148'
    assert tree.invariants(), "Invariants failed at step 9972"

    # Operation 9973: delete
    del tree[4187]
    del reference[4187]
    assert tree.invariants(), "Invariants failed at step 9973"

    # Operation 9974: delete
    del tree[610]
    del reference[610]
    assert tree.invariants(), "Invariants failed at step 9974"

    # Operation 9975: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 9975"

    # Operation 9976: insert
    tree[7541] = 'value_599232'
    reference[7541] = 'value_599232'
    assert tree.invariants(), "Invariants failed at step 9976"

    # Operation 9977: batch_delete
    keys_to_delete = [280, 7985, 8370, 4792, 5854]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9977"

    # Operation 9978: insert
    tree[5032] = 'value_320379'
    reference[5032] = 'value_320379'
    assert tree.invariants(), "Invariants failed at step 9978"

    # Operation 9979: get
    assert tree.invariants(), "Invariants failed at step 9979"

    # Operation 9980: insert
    tree[2578] = 'value_661182'
    reference[2578] = 'value_661182'
    assert tree.invariants(), "Invariants failed at step 9980"

    # Operation 9981: update
    tree[2578] = 'value_170484'
    reference[2578] = 'value_170484'
    assert tree.invariants(), "Invariants failed at step 9981"

    # Operation 9982: get
    assert tree.invariants(), "Invariants failed at step 9982"

    # Operation 9983: get
    assert tree.invariants(), "Invariants failed at step 9983"

    # Operation 9984: delete
    del tree[2578]
    del reference[2578]
    assert tree.invariants(), "Invariants failed at step 9984"

    # Operation 9985: get
    assert tree.invariants(), "Invariants failed at step 9985"

    # Operation 9986: insert
    tree[8739] = 'value_641231'
    reference[8739] = 'value_641231'
    assert tree.invariants(), "Invariants failed at step 9986"

    # Operation 9987: update
    tree[4073] = 'value_297652'
    reference[4073] = 'value_297652'
    assert tree.invariants(), "Invariants failed at step 9987"

    # Operation 9988: insert
    tree[3302] = 'value_156002'
    reference[3302] = 'value_156002'
    assert tree.invariants(), "Invariants failed at step 9988"

    # Operation 9989: get
    assert tree.invariants(), "Invariants failed at step 9989"

    # Operation 9990: batch_delete
    keys_to_delete = [9826, 8739, 5032, 4073, 1182]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 9990"

    # Operation 9991: insert
    tree[5079] = 'value_21079'
    reference[5079] = 'value_21079'
    assert tree.invariants(), "Invariants failed at step 9991"

    # Operation 9992: delete
    del tree[1641]
    del reference[1641]
    assert tree.invariants(), "Invariants failed at step 9992"

    # Operation 9993: get
    assert tree.invariants(), "Invariants failed at step 9993"

    # Operation 9994: delete
    del tree[7541]
    del reference[7541]
    assert tree.invariants(), "Invariants failed at step 9994"

    # Operation 9995: insert
    tree[3758] = 'value_594108'
    reference[3758] = 'value_594108'
    assert tree.invariants(), "Invariants failed at step 9995"

    # Operation 9996: get
    assert tree.invariants(), "Invariants failed at step 9996"

    # Operation 9997: insert
    tree[7871] = 'value_609980'
    reference[7871] = 'value_609980'
    assert tree.invariants(), "Invariants failed at step 9997"

    # Operation 9998: delete
    del tree[7871]
    del reference[7871]
    assert tree.invariants(), "Invariants failed at step 9998"

    # Operation 9999: get
    assert tree.invariants(), "Invariants failed at step 9999"

    # Operation 10000: get
    assert tree.invariants(), "Invariants failed at step 10000"

    # Operation 10001: delete
    del tree[5079]
    del reference[5079]
    assert tree.invariants(), "Invariants failed at step 10001"

    # Operation 10002: get
    assert tree.invariants(), "Invariants failed at step 10002"

    # Operation 10003: insert
    tree[5585] = 'value_836682'
    reference[5585] = 'value_836682'
    assert tree.invariants(), "Invariants failed at step 10003"

    # Operation 10004: delete
    del tree[5585]
    del reference[5585]
    assert tree.invariants(), "Invariants failed at step 10004"

    # Operation 10005: delete
    del tree[3302]
    del reference[3302]
    assert tree.invariants(), "Invariants failed at step 10005"

    # Operation 10006: insert
    tree[2811] = 'value_370820'
    reference[2811] = 'value_370820'
    assert tree.invariants(), "Invariants failed at step 10006"

    # Operation 10007: update
    tree[3758] = 'value_49944'
    reference[3758] = 'value_49944'
    assert tree.invariants(), "Invariants failed at step 10007"

    # Operation 10008: delete
    del tree[2811]
    del reference[2811]
    assert tree.invariants(), "Invariants failed at step 10008"

    # Operation 10009: insert
    tree[3760] = 'value_762965'
    reference[3760] = 'value_762965'
    assert tree.invariants(), "Invariants failed at step 10009"

    # Operation 10010: update
    tree[3760] = 'value_163359'
    reference[3760] = 'value_163359'
    assert tree.invariants(), "Invariants failed at step 10010"

    # Operation 10011: delete
    del tree[3758]
    del reference[3758]
    assert tree.invariants(), "Invariants failed at step 10011"

    # Operation 10012: insert
    tree[8691] = 'value_940252'
    reference[8691] = 'value_940252'
    assert tree.invariants(), "Invariants failed at step 10012"

    # Operation 10013: get
    assert tree.invariants(), "Invariants failed at step 10013"

    # Operation 10014: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10014"

    # Operation 10015: get
    assert tree.invariants(), "Invariants failed at step 10015"

    # Operation 10016: insert
    tree[3363] = 'value_778970'
    reference[3363] = 'value_778970'
    assert tree.invariants(), "Invariants failed at step 10016"

    # Operation 10017: delete
    del tree[3363]
    del reference[3363]
    assert tree.invariants(), "Invariants failed at step 10017"

    # Operation 10018: get
    assert tree.invariants(), "Invariants failed at step 10018"

    # Operation 10019: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10019"

    # Operation 10020: delete
    del tree[3760]
    del reference[3760]
    assert tree.invariants(), "Invariants failed at step 10020"

    # Operation 10021: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10021"

    # Operation 10022: insert
    tree[5821] = 'value_836530'
    reference[5821] = 'value_836530'
    assert tree.invariants(), "Invariants failed at step 10022"

    # Operation 10023: get
    assert tree.invariants(), "Invariants failed at step 10023"

    # Operation 10024: insert
    tree[6828] = 'value_111577'
    reference[6828] = 'value_111577'
    assert tree.invariants(), "Invariants failed at step 10024"

    # Operation 10025: update
    tree[6828] = 'value_706607'
    reference[6828] = 'value_706607'
    assert tree.invariants(), "Invariants failed at step 10025"

    # Operation 10026: insert
    tree[617] = 'value_782433'
    reference[617] = 'value_782433'
    assert tree.invariants(), "Invariants failed at step 10026"

    # Operation 10027: get
    assert tree.invariants(), "Invariants failed at step 10027"

    # Operation 10028: get
    assert tree.invariants(), "Invariants failed at step 10028"

    # Operation 10029: insert
    tree[1058] = 'value_579098'
    reference[1058] = 'value_579098'
    assert tree.invariants(), "Invariants failed at step 10029"

    # Operation 10030: insert
    tree[9038] = 'value_6983'
    reference[9038] = 'value_6983'
    assert tree.invariants(), "Invariants failed at step 10030"

    # Operation 10031: update
    tree[1058] = 'value_878999'
    reference[1058] = 'value_878999'
    assert tree.invariants(), "Invariants failed at step 10031"

    # Operation 10032: delete
    del tree[8691]
    del reference[8691]
    assert tree.invariants(), "Invariants failed at step 10032"

    # Operation 10033: update
    tree[5821] = 'value_122208'
    reference[5821] = 'value_122208'
    assert tree.invariants(), "Invariants failed at step 10033"

    # Operation 10034: get
    assert tree.invariants(), "Invariants failed at step 10034"

    # Operation 10035: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10035"

    # Operation 10036: get
    assert tree.invariants(), "Invariants failed at step 10036"

    # Operation 10037: get
    assert tree.invariants(), "Invariants failed at step 10037"

    # Operation 10038: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10038"

    # Operation 10039: delete
    del tree[5821]
    del reference[5821]
    assert tree.invariants(), "Invariants failed at step 10039"

    # Operation 10040: delete
    del tree[6828]
    del reference[6828]
    assert tree.invariants(), "Invariants failed at step 10040"

    # Operation 10041: update
    tree[1058] = 'value_266839'
    reference[1058] = 'value_266839'
    assert tree.invariants(), "Invariants failed at step 10041"

    # Operation 10042: insert
    tree[2129] = 'value_477598'
    reference[2129] = 'value_477598'
    assert tree.invariants(), "Invariants failed at step 10042"

    # Operation 10043: insert
    tree[598] = 'value_630078'
    reference[598] = 'value_630078'
    assert tree.invariants(), "Invariants failed at step 10043"

    # Operation 10044: get
    assert tree.invariants(), "Invariants failed at step 10044"

    # Operation 10045: delete
    del tree[617]
    del reference[617]
    assert tree.invariants(), "Invariants failed at step 10045"

    # Operation 10046: insert
    tree[1922] = 'value_75289'
    reference[1922] = 'value_75289'
    assert tree.invariants(), "Invariants failed at step 10046"

    # Operation 10047: delete
    del tree[598]
    del reference[598]
    assert tree.invariants(), "Invariants failed at step 10047"

    # Operation 10048: get
    assert tree.invariants(), "Invariants failed at step 10048"

    # Operation 10049: insert
    tree[9639] = 'value_482816'
    reference[9639] = 'value_482816'
    assert tree.invariants(), "Invariants failed at step 10049"

    # Operation 10050: get
    assert tree.invariants(), "Invariants failed at step 10050"

    # Operation 10051: batch_delete
    keys_to_delete = [2425, 1725, 9038, 9639]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10051"

    # Operation 10052: insert
    tree[4393] = 'value_480844'
    reference[4393] = 'value_480844'
    assert tree.invariants(), "Invariants failed at step 10052"

    # Operation 10053: insert
    tree[499] = 'value_90758'
    reference[499] = 'value_90758'
    assert tree.invariants(), "Invariants failed at step 10053"

    # Operation 10054: get
    assert tree.invariants(), "Invariants failed at step 10054"

    # Operation 10055: get
    assert tree.invariants(), "Invariants failed at step 10055"

    # Operation 10056: update
    tree[1922] = 'value_936288'
    reference[1922] = 'value_936288'
    assert tree.invariants(), "Invariants failed at step 10056"

    # Operation 10057: get
    assert tree.invariants(), "Invariants failed at step 10057"

    # Operation 10058: insert
    tree[4481] = 'value_627729'
    reference[4481] = 'value_627729'
    assert tree.invariants(), "Invariants failed at step 10058"

    # Operation 10059: get
    assert tree.invariants(), "Invariants failed at step 10059"

    # Operation 10060: update
    tree[4481] = 'value_31956'
    reference[4481] = 'value_31956'
    assert tree.invariants(), "Invariants failed at step 10060"

    # Operation 10061: delete
    del tree[4393]
    del reference[4393]
    assert tree.invariants(), "Invariants failed at step 10061"

    # Operation 10062: insert
    tree[3711] = 'value_547392'
    reference[3711] = 'value_547392'
    assert tree.invariants(), "Invariants failed at step 10062"

    # Operation 10063: get
    assert tree.invariants(), "Invariants failed at step 10063"

    # Operation 10064: delete
    del tree[1058]
    del reference[1058]
    assert tree.invariants(), "Invariants failed at step 10064"

    # Operation 10065: get
    assert tree.invariants(), "Invariants failed at step 10065"

    # Operation 10066: update
    tree[2129] = 'value_926341'
    reference[2129] = 'value_926341'
    assert tree.invariants(), "Invariants failed at step 10066"

    # Operation 10067: insert
    tree[3910] = 'value_605609'
    reference[3910] = 'value_605609'
    assert tree.invariants(), "Invariants failed at step 10067"

    # Operation 10068: batch_delete
    keys_to_delete = [2129, 499, 7517, 3910]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10068"

    # Operation 10069: delete
    del tree[1922]
    del reference[1922]
    assert tree.invariants(), "Invariants failed at step 10069"

    # Operation 10070: insert
    tree[9515] = 'value_710251'
    reference[9515] = 'value_710251'
    assert tree.invariants(), "Invariants failed at step 10070"

    # Operation 10071: insert
    tree[9611] = 'value_608413'
    reference[9611] = 'value_608413'
    assert tree.invariants(), "Invariants failed at step 10071"

    # Operation 10072: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10072"

    # Operation 10073: delete
    del tree[9611]
    del reference[9611]
    assert tree.invariants(), "Invariants failed at step 10073"

    # Operation 10074: update
    tree[4481] = 'value_346191'
    reference[4481] = 'value_346191'
    assert tree.invariants(), "Invariants failed at step 10074"

    # Operation 10075: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10075"

    # Operation 10076: delete
    del tree[9515]
    del reference[9515]
    assert tree.invariants(), "Invariants failed at step 10076"

    # Operation 10077: delete
    del tree[4481]
    del reference[4481]
    assert tree.invariants(), "Invariants failed at step 10077"

    # Operation 10078: insert
    tree[7003] = 'value_206887'
    reference[7003] = 'value_206887'
    assert tree.invariants(), "Invariants failed at step 10078"

    # Operation 10079: insert
    tree[9646] = 'value_762146'
    reference[9646] = 'value_762146'
    assert tree.invariants(), "Invariants failed at step 10079"

    # Operation 10080: insert
    tree[2301] = 'value_497126'
    reference[2301] = 'value_497126'
    assert tree.invariants(), "Invariants failed at step 10080"

    # Operation 10081: insert
    tree[2185] = 'value_866906'
    reference[2185] = 'value_866906'
    assert tree.invariants(), "Invariants failed at step 10081"

    # Operation 10082: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10082"

    # Operation 10083: delete
    del tree[2185]
    del reference[2185]
    assert tree.invariants(), "Invariants failed at step 10083"

    # Operation 10084: update
    tree[9646] = 'value_361553'
    reference[9646] = 'value_361553'
    assert tree.invariants(), "Invariants failed at step 10084"

    # Operation 10085: update
    tree[2301] = 'value_531822'
    reference[2301] = 'value_531822'
    assert tree.invariants(), "Invariants failed at step 10085"

    # Operation 10086: delete
    del tree[2301]
    del reference[2301]
    assert tree.invariants(), "Invariants failed at step 10086"

    # Operation 10087: delete
    del tree[7003]
    del reference[7003]
    assert tree.invariants(), "Invariants failed at step 10087"

    # Operation 10088: insert
    tree[4834] = 'value_400114'
    reference[4834] = 'value_400114'
    assert tree.invariants(), "Invariants failed at step 10088"

    # Operation 10089: get
    assert tree.invariants(), "Invariants failed at step 10089"

    # Operation 10090: insert
    tree[2667] = 'value_768505'
    reference[2667] = 'value_768505'
    assert tree.invariants(), "Invariants failed at step 10090"

    # Operation 10091: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10091"

    # Operation 10092: update
    tree[3711] = 'value_345352'
    reference[3711] = 'value_345352'
    assert tree.invariants(), "Invariants failed at step 10092"

    # Operation 10093: delete
    del tree[3711]
    del reference[3711]
    assert tree.invariants(), "Invariants failed at step 10093"

    # Operation 10094: insert
    tree[9970] = 'value_282092'
    reference[9970] = 'value_282092'
    assert tree.invariants(), "Invariants failed at step 10094"

    # Operation 10095: delete
    del tree[4834]
    del reference[4834]
    assert tree.invariants(), "Invariants failed at step 10095"

    # Operation 10096: get
    assert tree.invariants(), "Invariants failed at step 10096"

    # Operation 10097: get
    assert tree.invariants(), "Invariants failed at step 10097"

    # Operation 10098: delete
    del tree[9646]
    del reference[9646]
    assert tree.invariants(), "Invariants failed at step 10098"

    # Operation 10099: insert
    tree[3946] = 'value_306629'
    reference[3946] = 'value_306629'
    assert tree.invariants(), "Invariants failed at step 10099"

    # Operation 10100: insert
    tree[8704] = 'value_803218'
    reference[8704] = 'value_803218'
    assert tree.invariants(), "Invariants failed at step 10100"

    # Operation 10101: insert
    tree[830] = 'value_62962'
    reference[830] = 'value_62962'
    assert tree.invariants(), "Invariants failed at step 10101"

    # Operation 10102: insert
    tree[9673] = 'value_103696'
    reference[9673] = 'value_103696'
    assert tree.invariants(), "Invariants failed at step 10102"

    # Operation 10103: delete
    del tree[9970]
    del reference[9970]
    assert tree.invariants(), "Invariants failed at step 10103"

    # Operation 10104: get
    assert tree.invariants(), "Invariants failed at step 10104"

    # Operation 10105: delete
    del tree[9673]
    del reference[9673]
    assert tree.invariants(), "Invariants failed at step 10105"

    # Operation 10106: delete
    del tree[8704]
    del reference[8704]
    assert tree.invariants(), "Invariants failed at step 10106"

    # Operation 10107: update
    tree[3946] = 'value_15661'
    reference[3946] = 'value_15661'
    assert tree.invariants(), "Invariants failed at step 10107"

    # Operation 10108: insert
    tree[3006] = 'value_474968'
    reference[3006] = 'value_474968'
    assert tree.invariants(), "Invariants failed at step 10108"

    # Operation 10109: insert
    tree[1469] = 'value_706301'
    reference[1469] = 'value_706301'
    assert tree.invariants(), "Invariants failed at step 10109"

    # Operation 10110: get
    assert tree.invariants(), "Invariants failed at step 10110"

    # Operation 10111: batch_delete
    keys_to_delete = [3946, 2219, 1469, 2999]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10111"

    # Operation 10112: delete
    del tree[2667]
    del reference[2667]
    assert tree.invariants(), "Invariants failed at step 10112"

    # Operation 10113: delete
    del tree[830]
    del reference[830]
    assert tree.invariants(), "Invariants failed at step 10113"

    # Operation 10114: insert
    tree[7259] = 'value_963986'
    reference[7259] = 'value_963986'
    assert tree.invariants(), "Invariants failed at step 10114"

    # Operation 10115: get
    assert tree.invariants(), "Invariants failed at step 10115"

    # Operation 10116: delete
    del tree[3006]
    del reference[3006]
    assert tree.invariants(), "Invariants failed at step 10116"

    # Operation 10117: insert
    tree[9840] = 'value_690907'
    reference[9840] = 'value_690907'
    assert tree.invariants(), "Invariants failed at step 10117"

    # Operation 10118: delete
    del tree[9840]
    del reference[9840]
    assert tree.invariants(), "Invariants failed at step 10118"

    # Operation 10119: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10119"

    # Operation 10120: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10120"

    # Operation 10121: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10121"

    # Operation 10122: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10122"

    # Operation 10123: delete
    del tree[7259]
    del reference[7259]
    assert tree.invariants(), "Invariants failed at step 10123"

    # Operation 10124: insert
    tree[6595] = 'value_755520'
    reference[6595] = 'value_755520'
    assert tree.invariants(), "Invariants failed at step 10124"

    # Operation 10125: get
    assert tree.invariants(), "Invariants failed at step 10125"

    # Operation 10126: get
    assert tree.invariants(), "Invariants failed at step 10126"

    # Operation 10127: update
    tree[6595] = 'value_603989'
    reference[6595] = 'value_603989'
    assert tree.invariants(), "Invariants failed at step 10127"

    # Operation 10128: insert
    tree[8068] = 'value_269057'
    reference[8068] = 'value_269057'
    assert tree.invariants(), "Invariants failed at step 10128"

    # Operation 10129: insert
    tree[6458] = 'value_401781'
    reference[6458] = 'value_401781'
    assert tree.invariants(), "Invariants failed at step 10129"

    # Operation 10130: insert
    tree[4391] = 'value_75467'
    reference[4391] = 'value_75467'
    assert tree.invariants(), "Invariants failed at step 10130"

    # Operation 10131: delete
    del tree[8068]
    del reference[8068]
    assert tree.invariants(), "Invariants failed at step 10131"

    # Operation 10132: update
    tree[4391] = 'value_665848'
    reference[4391] = 'value_665848'
    assert tree.invariants(), "Invariants failed at step 10132"

    # Operation 10133: delete
    del tree[6595]
    del reference[6595]
    assert tree.invariants(), "Invariants failed at step 10133"

    # Operation 10134: insert
    tree[8119] = 'value_720788'
    reference[8119] = 'value_720788'
    assert tree.invariants(), "Invariants failed at step 10134"

    # Operation 10135: insert
    tree[2580] = 'value_940715'
    reference[2580] = 'value_940715'
    assert tree.invariants(), "Invariants failed at step 10135"

    # Operation 10136: insert
    tree[154] = 'value_886238'
    reference[154] = 'value_886238'
    assert tree.invariants(), "Invariants failed at step 10136"

    # Operation 10137: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10137"

    # Operation 10138: update
    tree[154] = 'value_201905'
    reference[154] = 'value_201905'
    assert tree.invariants(), "Invariants failed at step 10138"

    # Operation 10139: delete
    del tree[154]
    del reference[154]
    assert tree.invariants(), "Invariants failed at step 10139"

    # Operation 10140: insert
    tree[8238] = 'value_334401'
    reference[8238] = 'value_334401'
    assert tree.invariants(), "Invariants failed at step 10140"

    # Operation 10141: batch_delete
    keys_to_delete = [457, 2580, 4297, 8119]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10141"

    # Operation 10142: get
    assert tree.invariants(), "Invariants failed at step 10142"

    # Operation 10143: insert
    tree[7445] = 'value_703458'
    reference[7445] = 'value_703458'
    assert tree.invariants(), "Invariants failed at step 10143"

    # Operation 10144: delete
    del tree[6458]
    del reference[6458]
    assert tree.invariants(), "Invariants failed at step 10144"

    # Operation 10145: insert
    tree[6978] = 'value_665260'
    reference[6978] = 'value_665260'
    assert tree.invariants(), "Invariants failed at step 10145"

    # Operation 10146: get
    assert tree.invariants(), "Invariants failed at step 10146"

    # Operation 10147: insert
    tree[8055] = 'value_351171'
    reference[8055] = 'value_351171'
    assert tree.invariants(), "Invariants failed at step 10147"

    # Operation 10148: insert
    tree[3629] = 'value_696784'
    reference[3629] = 'value_696784'
    assert tree.invariants(), "Invariants failed at step 10148"

    # Operation 10149: delete
    del tree[4391]
    del reference[4391]
    assert tree.invariants(), "Invariants failed at step 10149"

    # Operation 10150: delete
    del tree[3629]
    del reference[3629]
    assert tree.invariants(), "Invariants failed at step 10150"

    # Operation 10151: insert
    tree[5397] = 'value_120275'
    reference[5397] = 'value_120275'
    assert tree.invariants(), "Invariants failed at step 10151"

    # Operation 10152: insert
    tree[5589] = 'value_356018'
    reference[5589] = 'value_356018'
    assert tree.invariants(), "Invariants failed at step 10152"

    # Operation 10153: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10153"

    # Operation 10154: batch_delete
    keys_to_delete = [7601, 6978, 5397, 8238]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10154"

    # Operation 10155: delete
    del tree[8055]
    del reference[8055]
    assert tree.invariants(), "Invariants failed at step 10155"

    # Operation 10156: get
    assert tree.invariants(), "Invariants failed at step 10156"

    # Operation 10157: get
    assert tree.invariants(), "Invariants failed at step 10157"

    # Operation 10158: insert
    tree[4671] = 'value_82399'
    reference[4671] = 'value_82399'
    assert tree.invariants(), "Invariants failed at step 10158"

    # Operation 10159: insert
    tree[1186] = 'value_976998'
    reference[1186] = 'value_976998'
    assert tree.invariants(), "Invariants failed at step 10159"

    # Operation 10160: update
    tree[7445] = 'value_242985'
    reference[7445] = 'value_242985'
    assert tree.invariants(), "Invariants failed at step 10160"

    # Operation 10161: update
    tree[7445] = 'value_147040'
    reference[7445] = 'value_147040'
    assert tree.invariants(), "Invariants failed at step 10161"

    # Operation 10162: insert
    tree[9418] = 'value_554648'
    reference[9418] = 'value_554648'
    assert tree.invariants(), "Invariants failed at step 10162"

    # Operation 10163: update
    tree[1186] = 'value_440572'
    reference[1186] = 'value_440572'
    assert tree.invariants(), "Invariants failed at step 10163"

    # Operation 10164: delete
    del tree[5589]
    del reference[5589]
    assert tree.invariants(), "Invariants failed at step 10164"

    # Operation 10165: insert
    tree[1751] = 'value_562698'
    reference[1751] = 'value_562698'
    assert tree.invariants(), "Invariants failed at step 10165"

    # Operation 10166: delete
    del tree[4671]
    del reference[4671]
    assert tree.invariants(), "Invariants failed at step 10166"

    # Operation 10167: delete
    del tree[1751]
    del reference[1751]
    assert tree.invariants(), "Invariants failed at step 10167"

    # Operation 10168: insert
    tree[9721] = 'value_827753'
    reference[9721] = 'value_827753'
    assert tree.invariants(), "Invariants failed at step 10168"

    # Operation 10169: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10169"

    # Operation 10170: delete
    del tree[7445]
    del reference[7445]
    assert tree.invariants(), "Invariants failed at step 10170"

    # Operation 10171: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10171"

    # Operation 10172: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10172"

    # Operation 10173: insert
    tree[2286] = 'value_884050'
    reference[2286] = 'value_884050'
    assert tree.invariants(), "Invariants failed at step 10173"

    # Operation 10174: insert
    tree[79] = 'value_48087'
    reference[79] = 'value_48087'
    assert tree.invariants(), "Invariants failed at step 10174"

    # Operation 10175: batch_delete
    keys_to_delete = [1186, 4893, 1063, 79]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10175"

    # Operation 10176: update
    tree[9721] = 'value_652647'
    reference[9721] = 'value_652647'
    assert tree.invariants(), "Invariants failed at step 10176"

    # Operation 10177: delete
    del tree[9721]
    del reference[9721]
    assert tree.invariants(), "Invariants failed at step 10177"

    # Operation 10178: insert
    tree[3798] = 'value_439891'
    reference[3798] = 'value_439891'
    assert tree.invariants(), "Invariants failed at step 10178"

    # Operation 10179: insert
    tree[3013] = 'value_339515'
    reference[3013] = 'value_339515'
    assert tree.invariants(), "Invariants failed at step 10179"

    # Operation 10180: insert
    tree[2005] = 'value_229917'
    reference[2005] = 'value_229917'
    assert tree.invariants(), "Invariants failed at step 10180"

    # Operation 10181: insert
    tree[2215] = 'value_182090'
    reference[2215] = 'value_182090'
    assert tree.invariants(), "Invariants failed at step 10181"

    # Operation 10182: insert
    tree[6280] = 'value_251072'
    reference[6280] = 'value_251072'
    assert tree.invariants(), "Invariants failed at step 10182"

    # Operation 10183: insert
    tree[7468] = 'value_486919'
    reference[7468] = 'value_486919'
    assert tree.invariants(), "Invariants failed at step 10183"

    # Operation 10184: delete
    del tree[9418]
    del reference[9418]
    assert tree.invariants(), "Invariants failed at step 10184"

    # Operation 10185: insert
    tree[9916] = 'value_969717'
    reference[9916] = 'value_969717'
    assert tree.invariants(), "Invariants failed at step 10185"

    # Operation 10186: update
    tree[3013] = 'value_467383'
    reference[3013] = 'value_467383'
    assert tree.invariants(), "Invariants failed at step 10186"

    # Operation 10187: get
    assert tree.invariants(), "Invariants failed at step 10187"

    # Operation 10188: update
    tree[3013] = 'value_979918'
    reference[3013] = 'value_979918'
    assert tree.invariants(), "Invariants failed at step 10188"

    # Operation 10189: delete
    del tree[9916]
    del reference[9916]
    assert tree.invariants(), "Invariants failed at step 10189"

    # Operation 10190: update
    tree[6280] = 'value_344255'
    reference[6280] = 'value_344255'
    assert tree.invariants(), "Invariants failed at step 10190"

    # Operation 10191: batch_delete
    keys_to_delete = [2215, 2286, 1874, 2005, 3798]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10191"

    # Operation 10192: delete
    del tree[3013]
    del reference[3013]
    assert tree.invariants(), "Invariants failed at step 10192"

    # Operation 10193: insert
    tree[7514] = 'value_21935'
    reference[7514] = 'value_21935'
    assert tree.invariants(), "Invariants failed at step 10193"

    # Operation 10194: insert
    tree[6038] = 'value_933061'
    reference[6038] = 'value_933061'
    assert tree.invariants(), "Invariants failed at step 10194"

    # Operation 10195: insert
    tree[1653] = 'value_820206'
    reference[1653] = 'value_820206'
    assert tree.invariants(), "Invariants failed at step 10195"

    # Operation 10196: delete
    del tree[1653]
    del reference[1653]
    assert tree.invariants(), "Invariants failed at step 10196"

    # Operation 10197: insert
    tree[4024] = 'value_379842'
    reference[4024] = 'value_379842'
    assert tree.invariants(), "Invariants failed at step 10197"

    # Operation 10198: insert
    tree[1447] = 'value_847101'
    reference[1447] = 'value_847101'
    assert tree.invariants(), "Invariants failed at step 10198"

    # Operation 10199: insert
    tree[6543] = 'value_41217'
    reference[6543] = 'value_41217'
    assert tree.invariants(), "Invariants failed at step 10199"

    # Operation 10200: batch_delete
    keys_to_delete = [2951, 7468, 5394, 6038, 7514]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10200"

    # Operation 10201: delete
    del tree[1447]
    del reference[1447]
    assert tree.invariants(), "Invariants failed at step 10201"

    # Operation 10202: insert
    tree[2574] = 'value_379851'
    reference[2574] = 'value_379851'
    assert tree.invariants(), "Invariants failed at step 10202"

    # Operation 10203: insert
    tree[5983] = 'value_958002'
    reference[5983] = 'value_958002'
    assert tree.invariants(), "Invariants failed at step 10203"

    # Operation 10204: get
    assert tree.invariants(), "Invariants failed at step 10204"

    # Operation 10205: insert
    tree[6278] = 'value_325746'
    reference[6278] = 'value_325746'
    assert tree.invariants(), "Invariants failed at step 10205"

    # Operation 10206: update
    tree[5983] = 'value_883930'
    reference[5983] = 'value_883930'
    assert tree.invariants(), "Invariants failed at step 10206"

    # Operation 10207: delete
    del tree[6543]
    del reference[6543]
    assert tree.invariants(), "Invariants failed at step 10207"

    # Operation 10208: delete
    del tree[5983]
    del reference[5983]
    assert tree.invariants(), "Invariants failed at step 10208"

    # Operation 10209: insert
    tree[7500] = 'value_598695'
    reference[7500] = 'value_598695'
    assert tree.invariants(), "Invariants failed at step 10209"

    # Operation 10210: insert
    tree[2181] = 'value_495392'
    reference[2181] = 'value_495392'
    assert tree.invariants(), "Invariants failed at step 10210"

    # Operation 10211: batch_delete
    keys_to_delete = [6149, 2181, 7500, 6935, 4024]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10211"

    # Operation 10212: delete
    del tree[6280]
    del reference[6280]
    assert tree.invariants(), "Invariants failed at step 10212"

    # Operation 10213: delete
    del tree[2574]
    del reference[2574]
    assert tree.invariants(), "Invariants failed at step 10213"

    # Operation 10214: get
    assert tree.invariants(), "Invariants failed at step 10214"

    # Operation 10215: delete
    del tree[6278]
    del reference[6278]
    assert tree.invariants(), "Invariants failed at step 10215"

    # Operation 10216: insert
    tree[8306] = 'value_106785'
    reference[8306] = 'value_106785'
    assert tree.invariants(), "Invariants failed at step 10216"

    # Operation 10217: insert
    tree[5619] = 'value_307697'
    reference[5619] = 'value_307697'
    assert tree.invariants(), "Invariants failed at step 10217"

    # Operation 10218: delete
    del tree[5619]
    del reference[5619]
    assert tree.invariants(), "Invariants failed at step 10218"

    # Operation 10219: get
    assert tree.invariants(), "Invariants failed at step 10219"

    # Operation 10220: insert
    tree[6728] = 'value_497274'
    reference[6728] = 'value_497274'
    assert tree.invariants(), "Invariants failed at step 10220"

    # Operation 10221: get
    assert tree.invariants(), "Invariants failed at step 10221"

    # Operation 10222: delete
    del tree[8306]
    del reference[8306]
    assert tree.invariants(), "Invariants failed at step 10222"

    # Operation 10223: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10223"

    # Operation 10224: insert
    tree[4038] = 'value_399312'
    reference[4038] = 'value_399312'
    assert tree.invariants(), "Invariants failed at step 10224"

    # Operation 10225: insert
    tree[2065] = 'value_980782'
    reference[2065] = 'value_980782'
    assert tree.invariants(), "Invariants failed at step 10225"

    # Operation 10226: delete
    del tree[6728]
    del reference[6728]
    assert tree.invariants(), "Invariants failed at step 10226"

    # Operation 10227: delete
    del tree[4038]
    del reference[4038]
    assert tree.invariants(), "Invariants failed at step 10227"

    # Operation 10228: update
    tree[2065] = 'value_179632'
    reference[2065] = 'value_179632'
    assert tree.invariants(), "Invariants failed at step 10228"

    # Operation 10229: insert
    tree[7454] = 'value_228298'
    reference[7454] = 'value_228298'
    assert tree.invariants(), "Invariants failed at step 10229"

    # Operation 10230: delete
    del tree[2065]
    del reference[2065]
    assert tree.invariants(), "Invariants failed at step 10230"

    # Operation 10231: get
    assert tree.invariants(), "Invariants failed at step 10231"

    # Operation 10232: delete
    del tree[7454]
    del reference[7454]
    assert tree.invariants(), "Invariants failed at step 10232"

    # Operation 10233: insert
    tree[2426] = 'value_361553'
    reference[2426] = 'value_361553'
    assert tree.invariants(), "Invariants failed at step 10233"

    # Operation 10234: update
    tree[2426] = 'value_970975'
    reference[2426] = 'value_970975'
    assert tree.invariants(), "Invariants failed at step 10234"

    # Operation 10235: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10235"

    # Operation 10236: delete
    del tree[2426]
    del reference[2426]
    assert tree.invariants(), "Invariants failed at step 10236"

    # Operation 10237: get
    assert tree.invariants(), "Invariants failed at step 10237"

    # Operation 10238: insert
    tree[9170] = 'value_157240'
    reference[9170] = 'value_157240'
    assert tree.invariants(), "Invariants failed at step 10238"

    # Operation 10239: get
    assert tree.invariants(), "Invariants failed at step 10239"

    # Operation 10240: insert
    tree[3032] = 'value_817026'
    reference[3032] = 'value_817026'
    assert tree.invariants(), "Invariants failed at step 10240"

    # Operation 10241: insert
    tree[7120] = 'value_349749'
    reference[7120] = 'value_349749'
    assert tree.invariants(), "Invariants failed at step 10241"

    # Operation 10242: insert
    tree[4200] = 'value_185055'
    reference[4200] = 'value_185055'
    assert tree.invariants(), "Invariants failed at step 10242"

    # Operation 10243: insert
    tree[2995] = 'value_26776'
    reference[2995] = 'value_26776'
    assert tree.invariants(), "Invariants failed at step 10243"

    # Operation 10244: insert
    tree[8737] = 'value_203966'
    reference[8737] = 'value_203966'
    assert tree.invariants(), "Invariants failed at step 10244"

    # Operation 10245: get
    assert tree.invariants(), "Invariants failed at step 10245"

    # Operation 10246: delete
    del tree[4200]
    del reference[4200]
    assert tree.invariants(), "Invariants failed at step 10246"

    # Operation 10247: update
    tree[9170] = 'value_593273'
    reference[9170] = 'value_593273'
    assert tree.invariants(), "Invariants failed at step 10247"

    # Operation 10248: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10248"

    # Operation 10249: delete
    del tree[7120]
    del reference[7120]
    assert tree.invariants(), "Invariants failed at step 10249"

    # Operation 10250: insert
    tree[2450] = 'value_996579'
    reference[2450] = 'value_996579'
    assert tree.invariants(), "Invariants failed at step 10250"

    # Operation 10251: batch_delete
    keys_to_delete = [3032, 9170, 1958, 2450]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10251"

    # Operation 10252: delete
    del tree[2995]
    del reference[2995]
    assert tree.invariants(), "Invariants failed at step 10252"

    # Operation 10253: delete
    del tree[8737]
    del reference[8737]
    assert tree.invariants(), "Invariants failed at step 10253"

    # Operation 10254: insert
    tree[7174] = 'value_261180'
    reference[7174] = 'value_261180'
    assert tree.invariants(), "Invariants failed at step 10254"

    # Operation 10255: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10255"

    # Operation 10256: delete
    del tree[7174]
    del reference[7174]
    assert tree.invariants(), "Invariants failed at step 10256"

    # Operation 10257: insert
    tree[9485] = 'value_234193'
    reference[9485] = 'value_234193'
    assert tree.invariants(), "Invariants failed at step 10257"

    # Operation 10258: insert
    tree[4891] = 'value_600674'
    reference[4891] = 'value_600674'
    assert tree.invariants(), "Invariants failed at step 10258"

    # Operation 10259: insert
    tree[4483] = 'value_812458'
    reference[4483] = 'value_812458'
    assert tree.invariants(), "Invariants failed at step 10259"

    # Operation 10260: insert
    tree[9845] = 'value_187299'
    reference[9845] = 'value_187299'
    assert tree.invariants(), "Invariants failed at step 10260"

    # Operation 10261: get
    assert tree.invariants(), "Invariants failed at step 10261"

    # Operation 10262: insert
    tree[6769] = 'value_659701'
    reference[6769] = 'value_659701'
    assert tree.invariants(), "Invariants failed at step 10262"

    # Operation 10263: delete
    del tree[9845]
    del reference[9845]
    assert tree.invariants(), "Invariants failed at step 10263"

    # Operation 10264: delete
    del tree[9485]
    del reference[9485]
    assert tree.invariants(), "Invariants failed at step 10264"

    # Operation 10265: get
    assert tree.invariants(), "Invariants failed at step 10265"

    # Operation 10266: get
    assert tree.invariants(), "Invariants failed at step 10266"

    # Operation 10267: insert
    tree[6027] = 'value_351028'
    reference[6027] = 'value_351028'
    assert tree.invariants(), "Invariants failed at step 10267"

    # Operation 10268: insert
    tree[2522] = 'value_863084'
    reference[2522] = 'value_863084'
    assert tree.invariants(), "Invariants failed at step 10268"

    # Operation 10269: delete
    del tree[4483]
    del reference[4483]
    assert tree.invariants(), "Invariants failed at step 10269"

    # Operation 10270: delete
    del tree[4891]
    del reference[4891]
    assert tree.invariants(), "Invariants failed at step 10270"

    # Operation 10271: get
    assert tree.invariants(), "Invariants failed at step 10271"

    # Operation 10272: insert
    tree[8858] = 'value_588899'
    reference[8858] = 'value_588899'
    assert tree.invariants(), "Invariants failed at step 10272"

    # Operation 10273: insert
    tree[4622] = 'value_248843'
    reference[4622] = 'value_248843'
    assert tree.invariants(), "Invariants failed at step 10273"

    # Operation 10274: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10274"

    # Operation 10275: update
    tree[6769] = 'value_976661'
    reference[6769] = 'value_976661'
    assert tree.invariants(), "Invariants failed at step 10275"

    # Operation 10276: insert
    tree[6508] = 'value_555735'
    reference[6508] = 'value_555735'
    assert tree.invariants(), "Invariants failed at step 10276"

    # Operation 10277: insert
    tree[3783] = 'value_100771'
    reference[3783] = 'value_100771'
    assert tree.invariants(), "Invariants failed at step 10277"

    # Operation 10278: insert
    tree[7117] = 'value_320237'
    reference[7117] = 'value_320237'
    assert tree.invariants(), "Invariants failed at step 10278"

    # Operation 10279: insert
    tree[8444] = 'value_712292'
    reference[8444] = 'value_712292'
    assert tree.invariants(), "Invariants failed at step 10279"

    # Operation 10280: update
    tree[6769] = 'value_657713'
    reference[6769] = 'value_657713'
    assert tree.invariants(), "Invariants failed at step 10280"

    # Operation 10281: delete
    del tree[8444]
    del reference[8444]
    assert tree.invariants(), "Invariants failed at step 10281"

    # Operation 10282: update
    tree[7117] = 'value_46745'
    reference[7117] = 'value_46745'
    assert tree.invariants(), "Invariants failed at step 10282"

    # Operation 10283: batch_delete
    keys_to_delete = [1033, 6027, 7117, 4622, 784, 8858]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10283"

    # Operation 10284: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10284"

    # Operation 10285: insert
    tree[2214] = 'value_860250'
    reference[2214] = 'value_860250'
    assert tree.invariants(), "Invariants failed at step 10285"

    # Operation 10286: batch_delete
    keys_to_delete = [6769, 2522, 7452, 2895]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10286"

    # Operation 10287: get
    assert tree.invariants(), "Invariants failed at step 10287"

    # Operation 10288: insert
    tree[1260] = 'value_660809'
    reference[1260] = 'value_660809'
    assert tree.invariants(), "Invariants failed at step 10288"

    # Operation 10289: get
    assert tree.invariants(), "Invariants failed at step 10289"

    # Operation 10290: delete
    del tree[2214]
    del reference[2214]
    assert tree.invariants(), "Invariants failed at step 10290"

    # Operation 10291: update
    tree[3783] = 'value_548367'
    reference[3783] = 'value_548367'
    assert tree.invariants(), "Invariants failed at step 10291"

    # Operation 10292: delete
    del tree[1260]
    del reference[1260]
    assert tree.invariants(), "Invariants failed at step 10292"

    # Operation 10293: delete
    del tree[3783]
    del reference[3783]
    assert tree.invariants(), "Invariants failed at step 10293"

    # Operation 10294: update
    tree[6508] = 'value_294277'
    reference[6508] = 'value_294277'
    assert tree.invariants(), "Invariants failed at step 10294"

    # Operation 10295: delete
    del tree[6508]
    del reference[6508]
    assert tree.invariants(), "Invariants failed at step 10295"

    # Operation 10296: get
    assert tree.invariants(), "Invariants failed at step 10296"

    # Operation 10297: insert
    tree[7403] = 'value_625781'
    reference[7403] = 'value_625781'
    assert tree.invariants(), "Invariants failed at step 10297"

    # Operation 10298: delete
    del tree[7403]
    del reference[7403]
    assert tree.invariants(), "Invariants failed at step 10298"

    # Operation 10299: insert
    tree[5983] = 'value_665822'
    reference[5983] = 'value_665822'
    assert tree.invariants(), "Invariants failed at step 10299"

    # Operation 10300: insert
    tree[5252] = 'value_375638'
    reference[5252] = 'value_375638'
    assert tree.invariants(), "Invariants failed at step 10300"

    # Operation 10301: delete
    del tree[5983]
    del reference[5983]
    assert tree.invariants(), "Invariants failed at step 10301"

    # Operation 10302: update
    tree[5252] = 'value_385198'
    reference[5252] = 'value_385198'
    assert tree.invariants(), "Invariants failed at step 10302"

    # Operation 10303: delete
    del tree[5252]
    del reference[5252]
    assert tree.invariants(), "Invariants failed at step 10303"

    # Operation 10304: insert
    tree[2252] = 'value_717061'
    reference[2252] = 'value_717061'
    assert tree.invariants(), "Invariants failed at step 10304"

    # Operation 10305: delete
    del tree[2252]
    del reference[2252]
    assert tree.invariants(), "Invariants failed at step 10305"

    # Operation 10306: insert
    tree[882] = 'value_564863'
    reference[882] = 'value_564863'
    assert tree.invariants(), "Invariants failed at step 10306"

    # Operation 10307: insert
    tree[5156] = 'value_791254'
    reference[5156] = 'value_791254'
    assert tree.invariants(), "Invariants failed at step 10307"

    # Operation 10308: get
    assert tree.invariants(), "Invariants failed at step 10308"

    # Operation 10309: get
    assert tree.invariants(), "Invariants failed at step 10309"

    # Operation 10310: delete
    del tree[882]
    del reference[882]
    assert tree.invariants(), "Invariants failed at step 10310"

    # Operation 10311: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10311"

    # Operation 10312: delete
    del tree[5156]
    del reference[5156]
    assert tree.invariants(), "Invariants failed at step 10312"

    # Operation 10313: get
    assert tree.invariants(), "Invariants failed at step 10313"

    # Operation 10314: get
    assert tree.invariants(), "Invariants failed at step 10314"

    # Operation 10315: insert
    tree[6507] = 'value_152538'
    reference[6507] = 'value_152538'
    assert tree.invariants(), "Invariants failed at step 10315"

    # Operation 10316: update
    tree[6507] = 'value_266164'
    reference[6507] = 'value_266164'
    assert tree.invariants(), "Invariants failed at step 10316"

    # Operation 10317: insert
    tree[8246] = 'value_291187'
    reference[8246] = 'value_291187'
    assert tree.invariants(), "Invariants failed at step 10317"

    # Operation 10318: delete
    del tree[8246]
    del reference[8246]
    assert tree.invariants(), "Invariants failed at step 10318"

    # Operation 10319: insert
    tree[5094] = 'value_940255'
    reference[5094] = 'value_940255'
    assert tree.invariants(), "Invariants failed at step 10319"

    # Operation 10320: insert
    tree[4310] = 'value_300857'
    reference[4310] = 'value_300857'
    assert tree.invariants(), "Invariants failed at step 10320"

    # Operation 10321: insert
    tree[4813] = 'value_903483'
    reference[4813] = 'value_903483'
    assert tree.invariants(), "Invariants failed at step 10321"

    # Operation 10322: get
    assert tree.invariants(), "Invariants failed at step 10322"

    # Operation 10323: delete
    del tree[5094]
    del reference[5094]
    assert tree.invariants(), "Invariants failed at step 10323"

    # Operation 10324: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10324"

    # Operation 10325: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10325"

    # Operation 10326: update
    tree[4310] = 'value_946051'
    reference[4310] = 'value_946051'
    assert tree.invariants(), "Invariants failed at step 10326"

    # Operation 10327: insert
    tree[5170] = 'value_476761'
    reference[5170] = 'value_476761'
    assert tree.invariants(), "Invariants failed at step 10327"

    # Operation 10328: insert
    tree[7093] = 'value_405019'
    reference[7093] = 'value_405019'
    assert tree.invariants(), "Invariants failed at step 10328"

    # Operation 10329: delete
    del tree[4310]
    del reference[4310]
    assert tree.invariants(), "Invariants failed at step 10329"

    # Operation 10330: insert
    tree[3909] = 'value_319619'
    reference[3909] = 'value_319619'
    assert tree.invariants(), "Invariants failed at step 10330"

    # Operation 10331: get
    assert tree.invariants(), "Invariants failed at step 10331"

    # Operation 10332: get
    assert tree.invariants(), "Invariants failed at step 10332"

    # Operation 10333: get
    assert tree.invariants(), "Invariants failed at step 10333"

    # Operation 10334: update
    tree[6507] = 'value_352'
    reference[6507] = 'value_352'
    assert tree.invariants(), "Invariants failed at step 10334"

    # Operation 10335: delete
    del tree[3909]
    del reference[3909]
    assert tree.invariants(), "Invariants failed at step 10335"

    # Operation 10336: insert
    tree[7882] = 'value_828834'
    reference[7882] = 'value_828834'
    assert tree.invariants(), "Invariants failed at step 10336"

    # Operation 10337: get
    assert tree.invariants(), "Invariants failed at step 10337"

    # Operation 10338: update
    tree[7093] = 'value_414041'
    reference[7093] = 'value_414041'
    assert tree.invariants(), "Invariants failed at step 10338"

    # Operation 10339: insert
    tree[3164] = 'value_912884'
    reference[3164] = 'value_912884'
    assert tree.invariants(), "Invariants failed at step 10339"

    # Operation 10340: update
    tree[6507] = 'value_665935'
    reference[6507] = 'value_665935'
    assert tree.invariants(), "Invariants failed at step 10340"

    # Operation 10341: insert
    tree[6939] = 'value_238230'
    reference[6939] = 'value_238230'
    assert tree.invariants(), "Invariants failed at step 10341"

    # Operation 10342: get
    assert tree.invariants(), "Invariants failed at step 10342"

    # Operation 10343: delete
    del tree[7882]
    del reference[7882]
    assert tree.invariants(), "Invariants failed at step 10343"

    # Operation 10344: update
    tree[4813] = 'value_771310'
    reference[4813] = 'value_771310'
    assert tree.invariants(), "Invariants failed at step 10344"

    # Operation 10345: delete
    del tree[6939]
    del reference[6939]
    assert tree.invariants(), "Invariants failed at step 10345"

    # Operation 10346: insert
    tree[3103] = 'value_742136'
    reference[3103] = 'value_742136'
    assert tree.invariants(), "Invariants failed at step 10346"

    # Operation 10347: get
    assert tree.invariants(), "Invariants failed at step 10347"

    # Operation 10348: insert
    tree[2388] = 'value_160070'
    reference[2388] = 'value_160070'
    assert tree.invariants(), "Invariants failed at step 10348"

    # Operation 10349: get
    assert tree.invariants(), "Invariants failed at step 10349"

    # Operation 10350: batch_delete
    keys_to_delete = [675, 4813, 2388, 1468, 3103]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10350"

    # Operation 10351: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10351"

    # Operation 10352: delete
    del tree[6507]
    del reference[6507]
    assert tree.invariants(), "Invariants failed at step 10352"

    # Operation 10353: get
    assert tree.invariants(), "Invariants failed at step 10353"

    # Operation 10354: update
    tree[7093] = 'value_545399'
    reference[7093] = 'value_545399'
    assert tree.invariants(), "Invariants failed at step 10354"

    # Operation 10355: insert
    tree[1158] = 'value_868199'
    reference[1158] = 'value_868199'
    assert tree.invariants(), "Invariants failed at step 10355"

    # Operation 10356: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10356"

    # Operation 10357: delete
    del tree[3164]
    del reference[3164]
    assert tree.invariants(), "Invariants failed at step 10357"

    # Operation 10358: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10358"

    # Operation 10359: insert
    tree[7205] = 'value_347829'
    reference[7205] = 'value_347829'
    assert tree.invariants(), "Invariants failed at step 10359"

    # Operation 10360: insert
    tree[3478] = 'value_839153'
    reference[3478] = 'value_839153'
    assert tree.invariants(), "Invariants failed at step 10360"

    # Operation 10361: get
    assert tree.invariants(), "Invariants failed at step 10361"

    # Operation 10362: delete
    del tree[7205]
    del reference[7205]
    assert tree.invariants(), "Invariants failed at step 10362"

    # Operation 10363: insert
    tree[2231] = 'value_838816'
    reference[2231] = 'value_838816'
    assert tree.invariants(), "Invariants failed at step 10363"

    # Operation 10364: get
    assert tree.invariants(), "Invariants failed at step 10364"

    # Operation 10365: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10365"

    # Operation 10366: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10366"

    # Operation 10367: insert
    tree[697] = 'value_232038'
    reference[697] = 'value_232038'
    assert tree.invariants(), "Invariants failed at step 10367"

    # Operation 10368: insert
    tree[9919] = 'value_617369'
    reference[9919] = 'value_617369'
    assert tree.invariants(), "Invariants failed at step 10368"

    # Operation 10369: update
    tree[2231] = 'value_298466'
    reference[2231] = 'value_298466'
    assert tree.invariants(), "Invariants failed at step 10369"

    # Operation 10370: delete
    del tree[1158]
    del reference[1158]
    assert tree.invariants(), "Invariants failed at step 10370"

    # Operation 10371: insert
    tree[4080] = 'value_553929'
    reference[4080] = 'value_553929'
    assert tree.invariants(), "Invariants failed at step 10371"

    # Operation 10372: batch_delete
    keys_to_delete = [3887, 7093, 3478, 9919]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10372"

    # Operation 10373: delete
    del tree[4080]
    del reference[4080]
    assert tree.invariants(), "Invariants failed at step 10373"

    # Operation 10374: get
    assert tree.invariants(), "Invariants failed at step 10374"

    # Operation 10375: get
    assert tree.invariants(), "Invariants failed at step 10375"

    # Operation 10376: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10376"

    # Operation 10377: update
    tree[5170] = 'value_888694'
    reference[5170] = 'value_888694'
    assert tree.invariants(), "Invariants failed at step 10377"

    # Operation 10378: delete
    del tree[5170]
    del reference[5170]
    assert tree.invariants(), "Invariants failed at step 10378"

    # Operation 10379: insert
    tree[3490] = 'value_467162'
    reference[3490] = 'value_467162'
    assert tree.invariants(), "Invariants failed at step 10379"

    # Operation 10380: delete
    del tree[2231]
    del reference[2231]
    assert tree.invariants(), "Invariants failed at step 10380"

    # Operation 10381: get
    assert tree.invariants(), "Invariants failed at step 10381"

    # Operation 10382: get
    assert tree.invariants(), "Invariants failed at step 10382"

    # Operation 10383: delete
    del tree[3490]
    del reference[3490]
    assert tree.invariants(), "Invariants failed at step 10383"

    # Operation 10384: insert
    tree[6229] = 'value_10593'
    reference[6229] = 'value_10593'
    assert tree.invariants(), "Invariants failed at step 10384"

    # Operation 10385: delete
    del tree[6229]
    del reference[6229]
    assert tree.invariants(), "Invariants failed at step 10385"

    # Operation 10386: delete
    del tree[697]
    del reference[697]
    assert tree.invariants(), "Invariants failed at step 10386"

    # Operation 10387: insert
    tree[2537] = 'value_691827'
    reference[2537] = 'value_691827'
    assert tree.invariants(), "Invariants failed at step 10387"

    # Operation 10388: get
    assert tree.invariants(), "Invariants failed at step 10388"

    # Operation 10389: delete
    del tree[2537]
    del reference[2537]
    assert tree.invariants(), "Invariants failed at step 10389"

    # Operation 10390: get
    assert tree.invariants(), "Invariants failed at step 10390"

    # Operation 10391: get
    assert tree.invariants(), "Invariants failed at step 10391"

    # Operation 10392: insert
    tree[228] = 'value_229021'
    reference[228] = 'value_229021'
    assert tree.invariants(), "Invariants failed at step 10392"

    # Operation 10393: insert
    tree[998] = 'value_76770'
    reference[998] = 'value_76770'
    assert tree.invariants(), "Invariants failed at step 10393"

    # Operation 10394: get
    assert tree.invariants(), "Invariants failed at step 10394"

    # Operation 10395: get
    assert tree.invariants(), "Invariants failed at step 10395"

    # Operation 10396: get
    assert tree.invariants(), "Invariants failed at step 10396"

    # Operation 10397: delete
    del tree[228]
    del reference[228]
    assert tree.invariants(), "Invariants failed at step 10397"

    # Operation 10398: get
    assert tree.invariants(), "Invariants failed at step 10398"

    # Operation 10399: delete
    del tree[998]
    del reference[998]
    assert tree.invariants(), "Invariants failed at step 10399"

    # Operation 10400: insert
    tree[1397] = 'value_379452'
    reference[1397] = 'value_379452'
    assert tree.invariants(), "Invariants failed at step 10400"

    # Operation 10401: delete
    del tree[1397]
    del reference[1397]
    assert tree.invariants(), "Invariants failed at step 10401"

    # Operation 10402: get
    assert tree.invariants(), "Invariants failed at step 10402"

    # Operation 10403: get
    assert tree.invariants(), "Invariants failed at step 10403"

    # Operation 10404: insert
    tree[2236] = 'value_425598'
    reference[2236] = 'value_425598'
    assert tree.invariants(), "Invariants failed at step 10404"

    # Operation 10405: delete
    del tree[2236]
    del reference[2236]
    assert tree.invariants(), "Invariants failed at step 10405"

    # Operation 10406: insert
    tree[1049] = 'value_68669'
    reference[1049] = 'value_68669'
    assert tree.invariants(), "Invariants failed at step 10406"

    # Operation 10407: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10407"

    # Operation 10408: get
    assert tree.invariants(), "Invariants failed at step 10408"

    # Operation 10409: insert
    tree[351] = 'value_84260'
    reference[351] = 'value_84260'
    assert tree.invariants(), "Invariants failed at step 10409"

    # Operation 10410: delete
    del tree[1049]
    del reference[1049]
    assert tree.invariants(), "Invariants failed at step 10410"

    # Operation 10411: insert
    tree[8788] = 'value_354861'
    reference[8788] = 'value_354861'
    assert tree.invariants(), "Invariants failed at step 10411"

    # Operation 10412: insert
    tree[2770] = 'value_863364'
    reference[2770] = 'value_863364'
    assert tree.invariants(), "Invariants failed at step 10412"

    # Operation 10413: delete
    del tree[8788]
    del reference[8788]
    assert tree.invariants(), "Invariants failed at step 10413"

    # Operation 10414: get
    assert tree.invariants(), "Invariants failed at step 10414"

    # Operation 10415: insert
    tree[5472] = 'value_771595'
    reference[5472] = 'value_771595'
    assert tree.invariants(), "Invariants failed at step 10415"

    # Operation 10416: get
    assert tree.invariants(), "Invariants failed at step 10416"

    # Operation 10417: delete
    del tree[351]
    del reference[351]
    assert tree.invariants(), "Invariants failed at step 10417"

    # Operation 10418: delete
    del tree[5472]
    del reference[5472]
    assert tree.invariants(), "Invariants failed at step 10418"

    # Operation 10419: delete
    del tree[2770]
    del reference[2770]
    assert tree.invariants(), "Invariants failed at step 10419"

    # Operation 10420: insert
    tree[5258] = 'value_71992'
    reference[5258] = 'value_71992'
    assert tree.invariants(), "Invariants failed at step 10420"

    # Operation 10421: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10421"

    # Operation 10422: update
    tree[5258] = 'value_223801'
    reference[5258] = 'value_223801'
    assert tree.invariants(), "Invariants failed at step 10422"

    # Operation 10423: delete
    del tree[5258]
    del reference[5258]
    assert tree.invariants(), "Invariants failed at step 10423"

    # Operation 10424: insert
    tree[8355] = 'value_723223'
    reference[8355] = 'value_723223'
    assert tree.invariants(), "Invariants failed at step 10424"

    # Operation 10425: delete
    del tree[8355]
    del reference[8355]
    assert tree.invariants(), "Invariants failed at step 10425"

    # Operation 10426: get
    assert tree.invariants(), "Invariants failed at step 10426"

    # Operation 10427: get
    assert tree.invariants(), "Invariants failed at step 10427"

    # Operation 10428: insert
    tree[1382] = 'value_657652'
    reference[1382] = 'value_657652'
    assert tree.invariants(), "Invariants failed at step 10428"

    # Operation 10429: get
    assert tree.invariants(), "Invariants failed at step 10429"

    # Operation 10430: insert
    tree[5444] = 'value_673148'
    reference[5444] = 'value_673148'
    assert tree.invariants(), "Invariants failed at step 10430"

    # Operation 10431: insert
    tree[6469] = 'value_687732'
    reference[6469] = 'value_687732'
    assert tree.invariants(), "Invariants failed at step 10431"

    # Operation 10432: get
    assert tree.invariants(), "Invariants failed at step 10432"

    # Operation 10433: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10433"

    # Operation 10434: get
    assert tree.invariants(), "Invariants failed at step 10434"

    # Operation 10435: delete
    del tree[5444]
    del reference[5444]
    assert tree.invariants(), "Invariants failed at step 10435"

    # Operation 10436: delete
    del tree[6469]
    del reference[6469]
    assert tree.invariants(), "Invariants failed at step 10436"

    # Operation 10437: insert
    tree[271] = 'value_378766'
    reference[271] = 'value_378766'
    assert tree.invariants(), "Invariants failed at step 10437"

    # Operation 10438: get
    assert tree.invariants(), "Invariants failed at step 10438"

    # Operation 10439: update
    tree[271] = 'value_62163'
    reference[271] = 'value_62163'
    assert tree.invariants(), "Invariants failed at step 10439"

    # Operation 10440: insert
    tree[9306] = 'value_729471'
    reference[9306] = 'value_729471'
    assert tree.invariants(), "Invariants failed at step 10440"

    # Operation 10441: get
    assert tree.invariants(), "Invariants failed at step 10441"

    # Operation 10442: insert
    tree[7167] = 'value_733982'
    reference[7167] = 'value_733982'
    assert tree.invariants(), "Invariants failed at step 10442"

    # Operation 10443: delete
    del tree[7167]
    del reference[7167]
    assert tree.invariants(), "Invariants failed at step 10443"

    # Operation 10444: get
    assert tree.invariants(), "Invariants failed at step 10444"

    # Operation 10445: update
    tree[271] = 'value_257258'
    reference[271] = 'value_257258'
    assert tree.invariants(), "Invariants failed at step 10445"

    # Operation 10446: delete
    del tree[9306]
    del reference[9306]
    assert tree.invariants(), "Invariants failed at step 10446"

    # Operation 10447: insert
    tree[4732] = 'value_904921'
    reference[4732] = 'value_904921'
    assert tree.invariants(), "Invariants failed at step 10447"

    # Operation 10448: get
    assert tree.invariants(), "Invariants failed at step 10448"

    # Operation 10449: update
    tree[4732] = 'value_377783'
    reference[4732] = 'value_377783'
    assert tree.invariants(), "Invariants failed at step 10449"

    # Operation 10450: insert
    tree[8183] = 'value_852979'
    reference[8183] = 'value_852979'
    assert tree.invariants(), "Invariants failed at step 10450"

    # Operation 10451: delete
    del tree[4732]
    del reference[4732]
    assert tree.invariants(), "Invariants failed at step 10451"

    # Operation 10452: insert
    tree[9588] = 'value_464529'
    reference[9588] = 'value_464529'
    assert tree.invariants(), "Invariants failed at step 10452"

    # Operation 10453: delete
    del tree[8183]
    del reference[8183]
    assert tree.invariants(), "Invariants failed at step 10453"

    # Operation 10454: delete
    del tree[9588]
    del reference[9588]
    assert tree.invariants(), "Invariants failed at step 10454"

    # Operation 10455: insert
    tree[5050] = 'value_735317'
    reference[5050] = 'value_735317'
    assert tree.invariants(), "Invariants failed at step 10455"

    # Operation 10456: insert
    tree[2640] = 'value_264228'
    reference[2640] = 'value_264228'
    assert tree.invariants(), "Invariants failed at step 10456"

    # Operation 10457: get
    assert tree.invariants(), "Invariants failed at step 10457"

    # Operation 10458: insert
    tree[7328] = 'value_357413'
    reference[7328] = 'value_357413'
    assert tree.invariants(), "Invariants failed at step 10458"

    # Operation 10459: batch_delete
    keys_to_delete = [7328, 7771, 2963, 271]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10459"

    # Operation 10460: get
    assert tree.invariants(), "Invariants failed at step 10460"

    # Operation 10461: insert
    tree[6275] = 'value_152717'
    reference[6275] = 'value_152717'
    assert tree.invariants(), "Invariants failed at step 10461"

    # Operation 10462: get
    assert tree.invariants(), "Invariants failed at step 10462"

    # Operation 10463: insert
    tree[2131] = 'value_929668'
    reference[2131] = 'value_929668'
    assert tree.invariants(), "Invariants failed at step 10463"

    # Operation 10464: update
    tree[6275] = 'value_737435'
    reference[6275] = 'value_737435'
    assert tree.invariants(), "Invariants failed at step 10464"

    # Operation 10465: update
    tree[2131] = 'value_705018'
    reference[2131] = 'value_705018'
    assert tree.invariants(), "Invariants failed at step 10465"

    # Operation 10466: delete
    del tree[2640]
    del reference[2640]
    assert tree.invariants(), "Invariants failed at step 10466"

    # Operation 10467: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10467"

    # Operation 10468: get
    assert tree.invariants(), "Invariants failed at step 10468"

    # Operation 10469: update
    tree[6275] = 'value_654310'
    reference[6275] = 'value_654310'
    assert tree.invariants(), "Invariants failed at step 10469"

    # Operation 10470: delete
    del tree[1382]
    del reference[1382]
    assert tree.invariants(), "Invariants failed at step 10470"

    # Operation 10471: get
    assert tree.invariants(), "Invariants failed at step 10471"

    # Operation 10472: get
    assert tree.invariants(), "Invariants failed at step 10472"

    # Operation 10473: delete
    del tree[2131]
    del reference[2131]
    assert tree.invariants(), "Invariants failed at step 10473"

    # Operation 10474: delete
    del tree[6275]
    del reference[6275]
    assert tree.invariants(), "Invariants failed at step 10474"

    # Operation 10475: get
    assert tree.invariants(), "Invariants failed at step 10475"

    # Operation 10476: insert
    tree[7218] = 'value_565990'
    reference[7218] = 'value_565990'
    assert tree.invariants(), "Invariants failed at step 10476"

    # Operation 10477: delete
    del tree[5050]
    del reference[5050]
    assert tree.invariants(), "Invariants failed at step 10477"

    # Operation 10478: delete
    del tree[7218]
    del reference[7218]
    assert tree.invariants(), "Invariants failed at step 10478"

    # Operation 10479: get
    assert tree.invariants(), "Invariants failed at step 10479"

    # Operation 10480: insert
    tree[5840] = 'value_499501'
    reference[5840] = 'value_499501'
    assert tree.invariants(), "Invariants failed at step 10480"

    # Operation 10481: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10481"

    # Operation 10482: insert
    tree[6061] = 'value_255635'
    reference[6061] = 'value_255635'
    assert tree.invariants(), "Invariants failed at step 10482"

    # Operation 10483: insert
    tree[8943] = 'value_165840'
    reference[8943] = 'value_165840'
    assert tree.invariants(), "Invariants failed at step 10483"

    # Operation 10484: delete
    del tree[5840]
    del reference[5840]
    assert tree.invariants(), "Invariants failed at step 10484"

    # Operation 10485: insert
    tree[7126] = 'value_75221'
    reference[7126] = 'value_75221'
    assert tree.invariants(), "Invariants failed at step 10485"

    # Operation 10486: get
    assert tree.invariants(), "Invariants failed at step 10486"

    # Operation 10487: update
    tree[8943] = 'value_122966'
    reference[8943] = 'value_122966'
    assert tree.invariants(), "Invariants failed at step 10487"

    # Operation 10488: delete
    del tree[8943]
    del reference[8943]
    assert tree.invariants(), "Invariants failed at step 10488"

    # Operation 10489: update
    tree[7126] = 'value_995190'
    reference[7126] = 'value_995190'
    assert tree.invariants(), "Invariants failed at step 10489"

    # Operation 10490: insert
    tree[5868] = 'value_290207'
    reference[5868] = 'value_290207'
    assert tree.invariants(), "Invariants failed at step 10490"

    # Operation 10491: update
    tree[5868] = 'value_933720'
    reference[5868] = 'value_933720'
    assert tree.invariants(), "Invariants failed at step 10491"

    # Operation 10492: delete
    del tree[6061]
    del reference[6061]
    assert tree.invariants(), "Invariants failed at step 10492"

    # Operation 10493: delete
    del tree[5868]
    del reference[5868]
    assert tree.invariants(), "Invariants failed at step 10493"

    # Operation 10494: delete
    del tree[7126]
    del reference[7126]
    assert tree.invariants(), "Invariants failed at step 10494"

    # Operation 10495: insert
    tree[9673] = 'value_629112'
    reference[9673] = 'value_629112'
    assert tree.invariants(), "Invariants failed at step 10495"

    # Operation 10496: insert
    tree[5896] = 'value_293999'
    reference[5896] = 'value_293999'
    assert tree.invariants(), "Invariants failed at step 10496"

    # Operation 10497: insert
    tree[7114] = 'value_224752'
    reference[7114] = 'value_224752'
    assert tree.invariants(), "Invariants failed at step 10497"

    # Operation 10498: insert
    tree[4120] = 'value_68685'
    reference[4120] = 'value_68685'
    assert tree.invariants(), "Invariants failed at step 10498"

    # Operation 10499: insert
    tree[8259] = 'value_97717'
    reference[8259] = 'value_97717'
    assert tree.invariants(), "Invariants failed at step 10499"

    # Operation 10500: insert
    tree[2379] = 'value_249478'
    reference[2379] = 'value_249478'
    assert tree.invariants(), "Invariants failed at step 10500"

    # Operation 10501: insert
    tree[1719] = 'value_653831'
    reference[1719] = 'value_653831'
    assert tree.invariants(), "Invariants failed at step 10501"

    # Operation 10502: get
    assert tree.invariants(), "Invariants failed at step 10502"

    # Operation 10503: insert
    tree[4466] = 'value_497962'
    reference[4466] = 'value_497962'
    assert tree.invariants(), "Invariants failed at step 10503"

    # Operation 10504: delete
    del tree[2379]
    del reference[2379]
    assert tree.invariants(), "Invariants failed at step 10504"

    # Operation 10505: update
    tree[8259] = 'value_760714'
    reference[8259] = 'value_760714'
    assert tree.invariants(), "Invariants failed at step 10505"

    # Operation 10506: insert
    tree[9738] = 'value_225579'
    reference[9738] = 'value_225579'
    assert tree.invariants(), "Invariants failed at step 10506"

    # Operation 10507: batch_delete
    keys_to_delete = [8259, 400, 4466, 4120, 5018]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10507"

    # Operation 10508: insert
    tree[6440] = 'value_457708'
    reference[6440] = 'value_457708'
    assert tree.invariants(), "Invariants failed at step 10508"

    # Operation 10509: get
    assert tree.invariants(), "Invariants failed at step 10509"

    # Operation 10510: delete
    del tree[6440]
    del reference[6440]
    assert tree.invariants(), "Invariants failed at step 10510"

    # Operation 10511: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10511"

    # Operation 10512: get
    assert tree.invariants(), "Invariants failed at step 10512"

    # Operation 10513: update
    tree[9673] = 'value_432255'
    reference[9673] = 'value_432255'
    assert tree.invariants(), "Invariants failed at step 10513"

    # Operation 10514: get
    assert tree.invariants(), "Invariants failed at step 10514"

    # Operation 10515: batch_delete
    keys_to_delete = [9673, 9738, 8643, 4811]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10515"

    # Operation 10516: update
    tree[5896] = 'value_440390'
    reference[5896] = 'value_440390'
    assert tree.invariants(), "Invariants failed at step 10516"

    # Operation 10517: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10517"

    # Operation 10518: delete
    del tree[1719]
    del reference[1719]
    assert tree.invariants(), "Invariants failed at step 10518"

    # Operation 10519: update
    tree[5896] = 'value_661434'
    reference[5896] = 'value_661434'
    assert tree.invariants(), "Invariants failed at step 10519"

    # Operation 10520: update
    tree[7114] = 'value_72031'
    reference[7114] = 'value_72031'
    assert tree.invariants(), "Invariants failed at step 10520"

    # Operation 10521: delete
    del tree[5896]
    del reference[5896]
    assert tree.invariants(), "Invariants failed at step 10521"

    # Operation 10522: update
    tree[7114] = 'value_534425'
    reference[7114] = 'value_534425'
    assert tree.invariants(), "Invariants failed at step 10522"

    # Operation 10523: get
    assert tree.invariants(), "Invariants failed at step 10523"

    # Operation 10524: update
    tree[7114] = 'value_97545'
    reference[7114] = 'value_97545'
    assert tree.invariants(), "Invariants failed at step 10524"

    # Operation 10525: update
    tree[7114] = 'value_35100'
    reference[7114] = 'value_35100'
    assert tree.invariants(), "Invariants failed at step 10525"

    # Operation 10526: delete
    del tree[7114]
    del reference[7114]
    assert tree.invariants(), "Invariants failed at step 10526"

    # Operation 10527: get
    assert tree.invariants(), "Invariants failed at step 10527"

    # Operation 10528: get
    assert tree.invariants(), "Invariants failed at step 10528"

    # Operation 10529: get
    assert tree.invariants(), "Invariants failed at step 10529"

    # Operation 10530: insert
    tree[2938] = 'value_537860'
    reference[2938] = 'value_537860'
    assert tree.invariants(), "Invariants failed at step 10530"

    # Operation 10531: insert
    tree[7882] = 'value_461807'
    reference[7882] = 'value_461807'
    assert tree.invariants(), "Invariants failed at step 10531"

    # Operation 10532: insert
    tree[340] = 'value_310255'
    reference[340] = 'value_310255'
    assert tree.invariants(), "Invariants failed at step 10532"

    # Operation 10533: update
    tree[2938] = 'value_608623'
    reference[2938] = 'value_608623'
    assert tree.invariants(), "Invariants failed at step 10533"

    # Operation 10534: get
    assert tree.invariants(), "Invariants failed at step 10534"

    # Operation 10535: get
    assert tree.invariants(), "Invariants failed at step 10535"

    # Operation 10536: update
    tree[7882] = 'value_266058'
    reference[7882] = 'value_266058'
    assert tree.invariants(), "Invariants failed at step 10536"

    # Operation 10537: delete
    del tree[7882]
    del reference[7882]
    assert tree.invariants(), "Invariants failed at step 10537"

    # Operation 10538: update
    tree[340] = 'value_983881'
    reference[340] = 'value_983881'
    assert tree.invariants(), "Invariants failed at step 10538"

    # Operation 10539: delete
    del tree[2938]
    del reference[2938]
    assert tree.invariants(), "Invariants failed at step 10539"

    # Operation 10540: get
    assert tree.invariants(), "Invariants failed at step 10540"

    # Operation 10541: get
    assert tree.invariants(), "Invariants failed at step 10541"

    # Operation 10542: get
    assert tree.invariants(), "Invariants failed at step 10542"

    # Operation 10543: get
    assert tree.invariants(), "Invariants failed at step 10543"

    # Operation 10544: insert
    tree[6686] = 'value_220080'
    reference[6686] = 'value_220080'
    assert tree.invariants(), "Invariants failed at step 10544"

    # Operation 10545: get
    assert tree.invariants(), "Invariants failed at step 10545"

    # Operation 10546: update
    tree[340] = 'value_709340'
    reference[340] = 'value_709340'
    assert tree.invariants(), "Invariants failed at step 10546"

    # Operation 10547: get
    assert tree.invariants(), "Invariants failed at step 10547"

    # Operation 10548: delete
    del tree[6686]
    del reference[6686]
    assert tree.invariants(), "Invariants failed at step 10548"

    # Operation 10549: update
    tree[340] = 'value_833243'
    reference[340] = 'value_833243'
    assert tree.invariants(), "Invariants failed at step 10549"

    # Operation 10550: delete
    del tree[340]
    del reference[340]
    assert tree.invariants(), "Invariants failed at step 10550"

    # Operation 10551: get
    assert tree.invariants(), "Invariants failed at step 10551"

    # Operation 10552: get
    assert tree.invariants(), "Invariants failed at step 10552"

    # Operation 10553: insert
    tree[8861] = 'value_638297'
    reference[8861] = 'value_638297'
    assert tree.invariants(), "Invariants failed at step 10553"

    # Operation 10554: insert
    tree[2212] = 'value_883430'
    reference[2212] = 'value_883430'
    assert tree.invariants(), "Invariants failed at step 10554"

    # Operation 10555: delete
    del tree[8861]
    del reference[8861]
    assert tree.invariants(), "Invariants failed at step 10555"

    # Operation 10556: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10556"

    # Operation 10557: update
    tree[2212] = 'value_73933'
    reference[2212] = 'value_73933'
    assert tree.invariants(), "Invariants failed at step 10557"

    # Operation 10558: delete
    del tree[2212]
    del reference[2212]
    assert tree.invariants(), "Invariants failed at step 10558"

    # Operation 10559: get
    assert tree.invariants(), "Invariants failed at step 10559"

    # Operation 10560: insert
    tree[8139] = 'value_818990'
    reference[8139] = 'value_818990'
    assert tree.invariants(), "Invariants failed at step 10560"

    # Operation 10561: insert
    tree[3897] = 'value_993253'
    reference[3897] = 'value_993253'
    assert tree.invariants(), "Invariants failed at step 10561"

    # Operation 10562: insert
    tree[7934] = 'value_140536'
    reference[7934] = 'value_140536'
    assert tree.invariants(), "Invariants failed at step 10562"

    # Operation 10563: delete
    del tree[7934]
    del reference[7934]
    assert tree.invariants(), "Invariants failed at step 10563"

    # Operation 10564: update
    tree[3897] = 'value_721112'
    reference[3897] = 'value_721112'
    assert tree.invariants(), "Invariants failed at step 10564"

    # Operation 10565: insert
    tree[4231] = 'value_735010'
    reference[4231] = 'value_735010'
    assert tree.invariants(), "Invariants failed at step 10565"

    # Operation 10566: get
    assert tree.invariants(), "Invariants failed at step 10566"

    # Operation 10567: delete
    del tree[3897]
    del reference[3897]
    assert tree.invariants(), "Invariants failed at step 10567"

    # Operation 10568: insert
    tree[603] = 'value_984848'
    reference[603] = 'value_984848'
    assert tree.invariants(), "Invariants failed at step 10568"

    # Operation 10569: insert
    tree[9189] = 'value_72739'
    reference[9189] = 'value_72739'
    assert tree.invariants(), "Invariants failed at step 10569"

    # Operation 10570: get
    assert tree.invariants(), "Invariants failed at step 10570"

    # Operation 10571: insert
    tree[1751] = 'value_57147'
    reference[1751] = 'value_57147'
    assert tree.invariants(), "Invariants failed at step 10571"

    # Operation 10572: delete
    del tree[8139]
    del reference[8139]
    assert tree.invariants(), "Invariants failed at step 10572"

    # Operation 10573: insert
    tree[89] = 'value_973798'
    reference[89] = 'value_973798'
    assert tree.invariants(), "Invariants failed at step 10573"

    # Operation 10574: insert
    tree[3339] = 'value_358153'
    reference[3339] = 'value_358153'
    assert tree.invariants(), "Invariants failed at step 10574"

    # Operation 10575: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10575"

    # Operation 10576: batch_delete
    keys_to_delete = [4231, 2249, 3339, 7535, 1751]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10576"

    # Operation 10577: update
    tree[9189] = 'value_638256'
    reference[9189] = 'value_638256'
    assert tree.invariants(), "Invariants failed at step 10577"

    # Operation 10578: delete
    del tree[603]
    del reference[603]
    assert tree.invariants(), "Invariants failed at step 10578"

    # Operation 10579: insert
    tree[3473] = 'value_17276'
    reference[3473] = 'value_17276'
    assert tree.invariants(), "Invariants failed at step 10579"

    # Operation 10580: insert
    tree[2035] = 'value_424233'
    reference[2035] = 'value_424233'
    assert tree.invariants(), "Invariants failed at step 10580"

    # Operation 10581: get
    assert tree.invariants(), "Invariants failed at step 10581"

    # Operation 10582: get
    assert tree.invariants(), "Invariants failed at step 10582"

    # Operation 10583: delete
    del tree[9189]
    del reference[9189]
    assert tree.invariants(), "Invariants failed at step 10583"

    # Operation 10584: delete
    del tree[3473]
    del reference[3473]
    assert tree.invariants(), "Invariants failed at step 10584"

    # Operation 10585: update
    tree[2035] = 'value_168999'
    reference[2035] = 'value_168999'
    assert tree.invariants(), "Invariants failed at step 10585"

    # Operation 10586: update
    tree[89] = 'value_397291'
    reference[89] = 'value_397291'
    assert tree.invariants(), "Invariants failed at step 10586"

    # Operation 10587: delete
    del tree[89]
    del reference[89]
    assert tree.invariants(), "Invariants failed at step 10587"

    # Operation 10588: update
    tree[2035] = 'value_757103'
    reference[2035] = 'value_757103'
    assert tree.invariants(), "Invariants failed at step 10588"

    # Operation 10589: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10589"

    # Operation 10590: insert
    tree[3490] = 'value_815288'
    reference[3490] = 'value_815288'
    assert tree.invariants(), "Invariants failed at step 10590"

    # Operation 10591: delete
    del tree[3490]
    del reference[3490]
    assert tree.invariants(), "Invariants failed at step 10591"

    # Operation 10592: delete
    del tree[2035]
    del reference[2035]
    assert tree.invariants(), "Invariants failed at step 10592"

    # Operation 10593: insert
    tree[4522] = 'value_459688'
    reference[4522] = 'value_459688'
    assert tree.invariants(), "Invariants failed at step 10593"

    # Operation 10594: delete
    del tree[4522]
    del reference[4522]
    assert tree.invariants(), "Invariants failed at step 10594"

    # Operation 10595: insert
    tree[5321] = 'value_365876'
    reference[5321] = 'value_365876'
    assert tree.invariants(), "Invariants failed at step 10595"

    # Operation 10596: insert
    tree[2452] = 'value_729197'
    reference[2452] = 'value_729197'
    assert tree.invariants(), "Invariants failed at step 10596"

    # Operation 10597: update
    tree[5321] = 'value_316983'
    reference[5321] = 'value_316983'
    assert tree.invariants(), "Invariants failed at step 10597"

    # Operation 10598: delete
    del tree[5321]
    del reference[5321]
    assert tree.invariants(), "Invariants failed at step 10598"

    # Operation 10599: get
    assert tree.invariants(), "Invariants failed at step 10599"

    # Operation 10600: delete
    del tree[2452]
    del reference[2452]
    assert tree.invariants(), "Invariants failed at step 10600"

    # Operation 10601: get
    assert tree.invariants(), "Invariants failed at step 10601"

    # Operation 10602: insert
    tree[4566] = 'value_632919'
    reference[4566] = 'value_632919'
    assert tree.invariants(), "Invariants failed at step 10602"

    # Operation 10603: insert
    tree[665] = 'value_955145'
    reference[665] = 'value_955145'
    assert tree.invariants(), "Invariants failed at step 10603"

    # Operation 10604: insert
    tree[9922] = 'value_578962'
    reference[9922] = 'value_578962'
    assert tree.invariants(), "Invariants failed at step 10604"

    # Operation 10605: insert
    tree[1234] = 'value_278417'
    reference[1234] = 'value_278417'
    assert tree.invariants(), "Invariants failed at step 10605"

    # Operation 10606: delete
    del tree[665]
    del reference[665]
    assert tree.invariants(), "Invariants failed at step 10606"

    # Operation 10607: insert
    tree[2631] = 'value_763905'
    reference[2631] = 'value_763905'
    assert tree.invariants(), "Invariants failed at step 10607"

    # Operation 10608: get
    assert tree.invariants(), "Invariants failed at step 10608"

    # Operation 10609: delete
    del tree[2631]
    del reference[2631]
    assert tree.invariants(), "Invariants failed at step 10609"

    # Operation 10610: delete
    del tree[1234]
    del reference[1234]
    assert tree.invariants(), "Invariants failed at step 10610"

    # Operation 10611: delete
    del tree[4566]
    del reference[4566]
    assert tree.invariants(), "Invariants failed at step 10611"

    # Operation 10612: get
    assert tree.invariants(), "Invariants failed at step 10612"

    # Operation 10613: insert
    tree[8045] = 'value_559901'
    reference[8045] = 'value_559901'
    assert tree.invariants(), "Invariants failed at step 10613"

    # Operation 10614: insert
    tree[3777] = 'value_458623'
    reference[3777] = 'value_458623'
    assert tree.invariants(), "Invariants failed at step 10614"

    # Operation 10615: update
    tree[3777] = 'value_627736'
    reference[3777] = 'value_627736'
    assert tree.invariants(), "Invariants failed at step 10615"

    # Operation 10616: update
    tree[9922] = 'value_810057'
    reference[9922] = 'value_810057'
    assert tree.invariants(), "Invariants failed at step 10616"

    # Operation 10617: insert
    tree[9997] = 'value_190809'
    reference[9997] = 'value_190809'
    assert tree.invariants(), "Invariants failed at step 10617"

    # Operation 10618: insert
    tree[9961] = 'value_862747'
    reference[9961] = 'value_862747'
    assert tree.invariants(), "Invariants failed at step 10618"

    # Operation 10619: insert
    tree[2915] = 'value_302631'
    reference[2915] = 'value_302631'
    assert tree.invariants(), "Invariants failed at step 10619"

    # Operation 10620: get
    assert tree.invariants(), "Invariants failed at step 10620"

    # Operation 10621: insert
    tree[6329] = 'value_390447'
    reference[6329] = 'value_390447'
    assert tree.invariants(), "Invariants failed at step 10621"

    # Operation 10622: get
    assert tree.invariants(), "Invariants failed at step 10622"

    # Operation 10623: get
    assert tree.invariants(), "Invariants failed at step 10623"

    # Operation 10624: update
    tree[8045] = 'value_752937'
    reference[8045] = 'value_752937'
    assert tree.invariants(), "Invariants failed at step 10624"

    # Operation 10625: update
    tree[9922] = 'value_118334'
    reference[9922] = 'value_118334'
    assert tree.invariants(), "Invariants failed at step 10625"

    # Operation 10626: delete
    del tree[9997]
    del reference[9997]
    assert tree.invariants(), "Invariants failed at step 10626"

    # Operation 10627: insert
    tree[6225] = 'value_178932'
    reference[6225] = 'value_178932'
    assert tree.invariants(), "Invariants failed at step 10627"

    # Operation 10628: insert
    tree[6257] = 'value_250921'
    reference[6257] = 'value_250921'
    assert tree.invariants(), "Invariants failed at step 10628"

    # Operation 10629: delete
    del tree[9922]
    del reference[9922]
    assert tree.invariants(), "Invariants failed at step 10629"

    # Operation 10630: insert
    tree[5204] = 'value_911721'
    reference[5204] = 'value_911721'
    assert tree.invariants(), "Invariants failed at step 10630"

    # Operation 10631: batch_delete
    keys_to_delete = [2915, 9961, 6225, 6257, 7093, 6263]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10631"

    # Operation 10632: get
    assert tree.invariants(), "Invariants failed at step 10632"

    # Operation 10633: get
    assert tree.invariants(), "Invariants failed at step 10633"

    # Operation 10634: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10634"

    # Operation 10635: delete
    del tree[5204]
    del reference[5204]
    assert tree.invariants(), "Invariants failed at step 10635"

    # Operation 10636: update
    tree[6329] = 'value_779610'
    reference[6329] = 'value_779610'
    assert tree.invariants(), "Invariants failed at step 10636"

    # Operation 10637: delete
    del tree[8045]
    del reference[8045]
    assert tree.invariants(), "Invariants failed at step 10637"

    # Operation 10638: delete
    del tree[6329]
    del reference[6329]
    assert tree.invariants(), "Invariants failed at step 10638"

    # Operation 10639: get
    assert tree.invariants(), "Invariants failed at step 10639"

    # Operation 10640: insert
    tree[7390] = 'value_853491'
    reference[7390] = 'value_853491'
    assert tree.invariants(), "Invariants failed at step 10640"

    # Operation 10641: insert
    tree[8510] = 'value_981304'
    reference[8510] = 'value_981304'
    assert tree.invariants(), "Invariants failed at step 10641"

    # Operation 10642: get
    assert tree.invariants(), "Invariants failed at step 10642"

    # Operation 10643: insert
    tree[2224] = 'value_868821'
    reference[2224] = 'value_868821'
    assert tree.invariants(), "Invariants failed at step 10643"

    # Operation 10644: insert
    tree[7240] = 'value_207017'
    reference[7240] = 'value_207017'
    assert tree.invariants(), "Invariants failed at step 10644"

    # Operation 10645: delete
    del tree[2224]
    del reference[2224]
    assert tree.invariants(), "Invariants failed at step 10645"

    # Operation 10646: insert
    tree[4227] = 'value_397128'
    reference[4227] = 'value_397128'
    assert tree.invariants(), "Invariants failed at step 10646"

    # Operation 10647: delete
    del tree[4227]
    del reference[4227]
    assert tree.invariants(), "Invariants failed at step 10647"

    # Operation 10648: get
    assert tree.invariants(), "Invariants failed at step 10648"

    # Operation 10649: insert
    tree[9875] = 'value_237049'
    reference[9875] = 'value_237049'
    assert tree.invariants(), "Invariants failed at step 10649"

    # Operation 10650: get
    assert tree.invariants(), "Invariants failed at step 10650"

    # Operation 10651: update
    tree[9875] = 'value_113636'
    reference[9875] = 'value_113636'
    assert tree.invariants(), "Invariants failed at step 10651"

    # Operation 10652: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10652"

    # Operation 10653: update
    tree[9875] = 'value_985134'
    reference[9875] = 'value_985134'
    assert tree.invariants(), "Invariants failed at step 10653"

    # Operation 10654: get
    assert tree.invariants(), "Invariants failed at step 10654"

    # Operation 10655: insert
    tree[1982] = 'value_935762'
    reference[1982] = 'value_935762'
    assert tree.invariants(), "Invariants failed at step 10655"

    # Operation 10656: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10656"

    # Operation 10657: delete
    del tree[7240]
    del reference[7240]
    assert tree.invariants(), "Invariants failed at step 10657"

    # Operation 10658: batch_delete
    keys_to_delete = [232, 3777, 1982, 1145]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10658"

    # Operation 10659: delete
    del tree[7390]
    del reference[7390]
    assert tree.invariants(), "Invariants failed at step 10659"

    # Operation 10660: insert
    tree[3088] = 'value_860449'
    reference[3088] = 'value_860449'
    assert tree.invariants(), "Invariants failed at step 10660"

    # Operation 10661: update
    tree[9875] = 'value_565066'
    reference[9875] = 'value_565066'
    assert tree.invariants(), "Invariants failed at step 10661"

    # Operation 10662: insert
    tree[994] = 'value_626068'
    reference[994] = 'value_626068'
    assert tree.invariants(), "Invariants failed at step 10662"

    # Operation 10663: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10663"

    # Operation 10664: delete
    del tree[3088]
    del reference[3088]
    assert tree.invariants(), "Invariants failed at step 10664"

    # Operation 10665: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10665"

    # Operation 10666: delete
    del tree[994]
    del reference[994]
    assert tree.invariants(), "Invariants failed at step 10666"

    # Operation 10667: update
    tree[9875] = 'value_205177'
    reference[9875] = 'value_205177'
    assert tree.invariants(), "Invariants failed at step 10667"

    # Operation 10668: update
    tree[8510] = 'value_781765'
    reference[8510] = 'value_781765'
    assert tree.invariants(), "Invariants failed at step 10668"

    # Operation 10669: delete
    del tree[8510]
    del reference[8510]
    assert tree.invariants(), "Invariants failed at step 10669"

    # Operation 10670: get
    assert tree.invariants(), "Invariants failed at step 10670"

    # Operation 10671: delete
    del tree[9875]
    del reference[9875]
    assert tree.invariants(), "Invariants failed at step 10671"

    # Operation 10672: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10672"

    # Operation 10673: get
    assert tree.invariants(), "Invariants failed at step 10673"

    # Operation 10674: insert
    tree[9989] = 'value_392601'
    reference[9989] = 'value_392601'
    assert tree.invariants(), "Invariants failed at step 10674"

    # Operation 10675: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10675"

    # Operation 10676: update
    tree[9989] = 'value_76369'
    reference[9989] = 'value_76369'
    assert tree.invariants(), "Invariants failed at step 10676"

    # Operation 10677: insert
    tree[2472] = 'value_599960'
    reference[2472] = 'value_599960'
    assert tree.invariants(), "Invariants failed at step 10677"

    # Operation 10678: insert
    tree[8465] = 'value_553581'
    reference[8465] = 'value_553581'
    assert tree.invariants(), "Invariants failed at step 10678"

    # Operation 10679: insert
    tree[7798] = 'value_704768'
    reference[7798] = 'value_704768'
    assert tree.invariants(), "Invariants failed at step 10679"

    # Operation 10680: update
    tree[7798] = 'value_395403'
    reference[7798] = 'value_395403'
    assert tree.invariants(), "Invariants failed at step 10680"

    # Operation 10681: delete
    del tree[9989]
    del reference[9989]
    assert tree.invariants(), "Invariants failed at step 10681"

    # Operation 10682: delete
    del tree[7798]
    del reference[7798]
    assert tree.invariants(), "Invariants failed at step 10682"

    # Operation 10683: get
    assert tree.invariants(), "Invariants failed at step 10683"

    # Operation 10684: insert
    tree[2722] = 'value_134528'
    reference[2722] = 'value_134528'
    assert tree.invariants(), "Invariants failed at step 10684"

    # Operation 10685: delete
    del tree[2472]
    del reference[2472]
    assert tree.invariants(), "Invariants failed at step 10685"

    # Operation 10686: insert
    tree[7522] = 'value_677946'
    reference[7522] = 'value_677946'
    assert tree.invariants(), "Invariants failed at step 10686"

    # Operation 10687: delete
    del tree[8465]
    del reference[8465]
    assert tree.invariants(), "Invariants failed at step 10687"

    # Operation 10688: insert
    tree[1494] = 'value_252911'
    reference[1494] = 'value_252911'
    assert tree.invariants(), "Invariants failed at step 10688"

    # Operation 10689: insert
    tree[1471] = 'value_700690'
    reference[1471] = 'value_700690'
    assert tree.invariants(), "Invariants failed at step 10689"

    # Operation 10690: insert
    tree[5298] = 'value_462773'
    reference[5298] = 'value_462773'
    assert tree.invariants(), "Invariants failed at step 10690"

    # Operation 10691: delete
    del tree[2722]
    del reference[2722]
    assert tree.invariants(), "Invariants failed at step 10691"

    # Operation 10692: get
    assert tree.invariants(), "Invariants failed at step 10692"

    # Operation 10693: delete
    del tree[1471]
    del reference[1471]
    assert tree.invariants(), "Invariants failed at step 10693"

    # Operation 10694: insert
    tree[2983] = 'value_761601'
    reference[2983] = 'value_761601'
    assert tree.invariants(), "Invariants failed at step 10694"

    # Operation 10695: delete
    del tree[2983]
    del reference[2983]
    assert tree.invariants(), "Invariants failed at step 10695"

    # Operation 10696: delete
    del tree[1494]
    del reference[1494]
    assert tree.invariants(), "Invariants failed at step 10696"

    # Operation 10697: update
    tree[7522] = 'value_283859'
    reference[7522] = 'value_283859'
    assert tree.invariants(), "Invariants failed at step 10697"

    # Operation 10698: insert
    tree[8197] = 'value_307000'
    reference[8197] = 'value_307000'
    assert tree.invariants(), "Invariants failed at step 10698"

    # Operation 10699: delete
    del tree[8197]
    del reference[8197]
    assert tree.invariants(), "Invariants failed at step 10699"

    # Operation 10700: delete
    del tree[5298]
    del reference[5298]
    assert tree.invariants(), "Invariants failed at step 10700"

    # Operation 10701: get
    assert tree.invariants(), "Invariants failed at step 10701"

    # Operation 10702: delete
    del tree[7522]
    del reference[7522]
    assert tree.invariants(), "Invariants failed at step 10702"

    # Operation 10703: insert
    tree[1654] = 'value_131648'
    reference[1654] = 'value_131648'
    assert tree.invariants(), "Invariants failed at step 10703"

    # Operation 10704: get
    assert tree.invariants(), "Invariants failed at step 10704"

    # Operation 10705: get
    assert tree.invariants(), "Invariants failed at step 10705"

    # Operation 10706: insert
    tree[681] = 'value_202077'
    reference[681] = 'value_202077'
    assert tree.invariants(), "Invariants failed at step 10706"

    # Operation 10707: get
    assert tree.invariants(), "Invariants failed at step 10707"

    # Operation 10708: insert
    tree[1159] = 'value_947976'
    reference[1159] = 'value_947976'
    assert tree.invariants(), "Invariants failed at step 10708"

    # Operation 10709: get
    assert tree.invariants(), "Invariants failed at step 10709"

    # Operation 10710: insert
    tree[6647] = 'value_551139'
    reference[6647] = 'value_551139'
    assert tree.invariants(), "Invariants failed at step 10710"

    # Operation 10711: delete
    del tree[681]
    del reference[681]
    assert tree.invariants(), "Invariants failed at step 10711"

    # Operation 10712: insert
    tree[2289] = 'value_996523'
    reference[2289] = 'value_996523'
    assert tree.invariants(), "Invariants failed at step 10712"

    # Operation 10713: get
    assert tree.invariants(), "Invariants failed at step 10713"

    # Operation 10714: get
    assert tree.invariants(), "Invariants failed at step 10714"

    # Operation 10715: insert
    tree[5166] = 'value_881485'
    reference[5166] = 'value_881485'
    assert tree.invariants(), "Invariants failed at step 10715"

    # Operation 10716: delete
    del tree[2289]
    del reference[2289]
    assert tree.invariants(), "Invariants failed at step 10716"

    # Operation 10717: insert
    tree[9070] = 'value_523011'
    reference[9070] = 'value_523011'
    assert tree.invariants(), "Invariants failed at step 10717"

    # Operation 10718: insert
    tree[6965] = 'value_778740'
    reference[6965] = 'value_778740'
    assert tree.invariants(), "Invariants failed at step 10718"

    # Operation 10719: update
    tree[9070] = 'value_28965'
    reference[9070] = 'value_28965'
    assert tree.invariants(), "Invariants failed at step 10719"

    # Operation 10720: update
    tree[5166] = 'value_373496'
    reference[5166] = 'value_373496'
    assert tree.invariants(), "Invariants failed at step 10720"

    # Operation 10721: delete
    del tree[9070]
    del reference[9070]
    assert tree.invariants(), "Invariants failed at step 10721"

    # Operation 10722: update
    tree[5166] = 'value_299735'
    reference[5166] = 'value_299735'
    assert tree.invariants(), "Invariants failed at step 10722"

    # Operation 10723: get
    assert tree.invariants(), "Invariants failed at step 10723"

    # Operation 10724: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10724"

    # Operation 10725: update
    tree[5166] = 'value_621747'
    reference[5166] = 'value_621747'
    assert tree.invariants(), "Invariants failed at step 10725"

    # Operation 10726: delete
    del tree[6965]
    del reference[6965]
    assert tree.invariants(), "Invariants failed at step 10726"

    # Operation 10727: insert
    tree[5855] = 'value_543749'
    reference[5855] = 'value_543749'
    assert tree.invariants(), "Invariants failed at step 10727"

    # Operation 10728: delete
    del tree[1159]
    del reference[1159]
    assert tree.invariants(), "Invariants failed at step 10728"

    # Operation 10729: insert
    tree[1361] = 'value_957940'
    reference[1361] = 'value_957940'
    assert tree.invariants(), "Invariants failed at step 10729"

    # Operation 10730: insert
    tree[5307] = 'value_263007'
    reference[5307] = 'value_263007'
    assert tree.invariants(), "Invariants failed at step 10730"

    # Operation 10731: update
    tree[5855] = 'value_550886'
    reference[5855] = 'value_550886'
    assert tree.invariants(), "Invariants failed at step 10731"

    # Operation 10732: delete
    del tree[5307]
    del reference[5307]
    assert tree.invariants(), "Invariants failed at step 10732"

    # Operation 10733: delete
    del tree[1654]
    del reference[1654]
    assert tree.invariants(), "Invariants failed at step 10733"

    # Operation 10734: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10734"

    # Operation 10735: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10735"

    # Operation 10736: delete
    del tree[1361]
    del reference[1361]
    assert tree.invariants(), "Invariants failed at step 10736"

    # Operation 10737: delete
    del tree[5855]
    del reference[5855]
    assert tree.invariants(), "Invariants failed at step 10737"

    # Operation 10738: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10738"

    # Operation 10739: get
    assert tree.invariants(), "Invariants failed at step 10739"

    # Operation 10740: insert
    tree[5010] = 'value_609776'
    reference[5010] = 'value_609776'
    assert tree.invariants(), "Invariants failed at step 10740"

    # Operation 10741: update
    tree[5166] = 'value_225995'
    reference[5166] = 'value_225995'
    assert tree.invariants(), "Invariants failed at step 10741"

    # Operation 10742: insert
    tree[7208] = 'value_467665'
    reference[7208] = 'value_467665'
    assert tree.invariants(), "Invariants failed at step 10742"

    # Operation 10743: get
    assert tree.invariants(), "Invariants failed at step 10743"

    # Operation 10744: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10744"

    # Operation 10745: get
    assert tree.invariants(), "Invariants failed at step 10745"

    # Operation 10746: get
    assert tree.invariants(), "Invariants failed at step 10746"

    # Operation 10747: delete
    del tree[7208]
    del reference[7208]
    assert tree.invariants(), "Invariants failed at step 10747"

    # Operation 10748: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10748"

    # Operation 10749: insert
    tree[1346] = 'value_396005'
    reference[1346] = 'value_396005'
    assert tree.invariants(), "Invariants failed at step 10749"

    # Operation 10750: get
    assert tree.invariants(), "Invariants failed at step 10750"

    # Operation 10751: update
    tree[5166] = 'value_487685'
    reference[5166] = 'value_487685'
    assert tree.invariants(), "Invariants failed at step 10751"

    # Operation 10752: insert
    tree[9111] = 'value_652186'
    reference[9111] = 'value_652186'
    assert tree.invariants(), "Invariants failed at step 10752"

    # Operation 10753: batch_delete
    keys_to_delete = [6647, 925, 7806, 9111]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10753"

    # Operation 10754: update
    tree[1346] = 'value_227845'
    reference[1346] = 'value_227845'
    assert tree.invariants(), "Invariants failed at step 10754"

    # Operation 10755: get
    assert tree.invariants(), "Invariants failed at step 10755"

    # Operation 10756: insert
    tree[3934] = 'value_226723'
    reference[3934] = 'value_226723'
    assert tree.invariants(), "Invariants failed at step 10756"

    # Operation 10757: insert
    tree[7362] = 'value_862354'
    reference[7362] = 'value_862354'
    assert tree.invariants(), "Invariants failed at step 10757"

    # Operation 10758: delete
    del tree[7362]
    del reference[7362]
    assert tree.invariants(), "Invariants failed at step 10758"

    # Operation 10759: update
    tree[5010] = 'value_987681'
    reference[5010] = 'value_987681'
    assert tree.invariants(), "Invariants failed at step 10759"

    # Operation 10760: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10760"

    # Operation 10761: insert
    tree[407] = 'value_712589'
    reference[407] = 'value_712589'
    assert tree.invariants(), "Invariants failed at step 10761"

    # Operation 10762: insert
    tree[5703] = 'value_309074'
    reference[5703] = 'value_309074'
    assert tree.invariants(), "Invariants failed at step 10762"

    # Operation 10763: batch_delete
    keys_to_delete = [1346, 3934, 407, 4189, 3166]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10763"

    # Operation 10764: delete
    del tree[5166]
    del reference[5166]
    assert tree.invariants(), "Invariants failed at step 10764"

    # Operation 10765: update
    tree[5010] = 'value_40592'
    reference[5010] = 'value_40592'
    assert tree.invariants(), "Invariants failed at step 10765"

    # Operation 10766: delete
    del tree[5703]
    del reference[5703]
    assert tree.invariants(), "Invariants failed at step 10766"

    # Operation 10767: insert
    tree[8357] = 'value_193942'
    reference[8357] = 'value_193942'
    assert tree.invariants(), "Invariants failed at step 10767"

    # Operation 10768: get
    assert tree.invariants(), "Invariants failed at step 10768"

    # Operation 10769: delete
    del tree[5010]
    del reference[5010]
    assert tree.invariants(), "Invariants failed at step 10769"

    # Operation 10770: delete
    del tree[8357]
    del reference[8357]
    assert tree.invariants(), "Invariants failed at step 10770"

    # Operation 10771: insert
    tree[7285] = 'value_831812'
    reference[7285] = 'value_831812'
    assert tree.invariants(), "Invariants failed at step 10771"

    # Operation 10772: insert
    tree[353] = 'value_772463'
    reference[353] = 'value_772463'
    assert tree.invariants(), "Invariants failed at step 10772"

    # Operation 10773: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10773"

    # Operation 10774: get
    assert tree.invariants(), "Invariants failed at step 10774"

    # Operation 10775: insert
    tree[9987] = 'value_585296'
    reference[9987] = 'value_585296'
    assert tree.invariants(), "Invariants failed at step 10775"

    # Operation 10776: get
    assert tree.invariants(), "Invariants failed at step 10776"

    # Operation 10777: delete
    del tree[7285]
    del reference[7285]
    assert tree.invariants(), "Invariants failed at step 10777"

    # Operation 10778: delete
    del tree[9987]
    del reference[9987]
    assert tree.invariants(), "Invariants failed at step 10778"

    # Operation 10779: insert
    tree[3779] = 'value_683675'
    reference[3779] = 'value_683675'
    assert tree.invariants(), "Invariants failed at step 10779"

    # Operation 10780: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10780"

    # Operation 10781: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10781"

    # Operation 10782: insert
    tree[4331] = 'value_933252'
    reference[4331] = 'value_933252'
    assert tree.invariants(), "Invariants failed at step 10782"

    # Operation 10783: get
    assert tree.invariants(), "Invariants failed at step 10783"

    # Operation 10784: insert
    tree[5328] = 'value_118646'
    reference[5328] = 'value_118646'
    assert tree.invariants(), "Invariants failed at step 10784"

    # Operation 10785: update
    tree[4331] = 'value_865433'
    reference[4331] = 'value_865433'
    assert tree.invariants(), "Invariants failed at step 10785"

    # Operation 10786: insert
    tree[4603] = 'value_91884'
    reference[4603] = 'value_91884'
    assert tree.invariants(), "Invariants failed at step 10786"

    # Operation 10787: batch_delete
    keys_to_delete = [2177, 4603, 9900, 3779]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10787"

    # Operation 10788: delete
    del tree[4331]
    del reference[4331]
    assert tree.invariants(), "Invariants failed at step 10788"

    # Operation 10789: delete
    del tree[5328]
    del reference[5328]
    assert tree.invariants(), "Invariants failed at step 10789"

    # Operation 10790: update
    tree[353] = 'value_131164'
    reference[353] = 'value_131164'
    assert tree.invariants(), "Invariants failed at step 10790"

    # Operation 10791: delete
    del tree[353]
    del reference[353]
    assert tree.invariants(), "Invariants failed at step 10791"

    # Operation 10792: get
    assert tree.invariants(), "Invariants failed at step 10792"

    # Operation 10793: insert
    tree[1659] = 'value_145140'
    reference[1659] = 'value_145140'
    assert tree.invariants(), "Invariants failed at step 10793"

    # Operation 10794: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10794"

    # Operation 10795: delete
    del tree[1659]
    del reference[1659]
    assert tree.invariants(), "Invariants failed at step 10795"

    # Operation 10796: insert
    tree[9852] = 'value_261822'
    reference[9852] = 'value_261822'
    assert tree.invariants(), "Invariants failed at step 10796"

    # Operation 10797: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10797"

    # Operation 10798: insert
    tree[5744] = 'value_613578'
    reference[5744] = 'value_613578'
    assert tree.invariants(), "Invariants failed at step 10798"

    # Operation 10799: update
    tree[5744] = 'value_508852'
    reference[5744] = 'value_508852'
    assert tree.invariants(), "Invariants failed at step 10799"

    # Operation 10800: delete
    del tree[5744]
    del reference[5744]
    assert tree.invariants(), "Invariants failed at step 10800"

    # Operation 10801: get
    assert tree.invariants(), "Invariants failed at step 10801"

    # Operation 10802: delete
    del tree[9852]
    del reference[9852]
    assert tree.invariants(), "Invariants failed at step 10802"

    # Operation 10803: get
    assert tree.invariants(), "Invariants failed at step 10803"

    # Operation 10804: get
    assert tree.invariants(), "Invariants failed at step 10804"

    # Operation 10805: insert
    tree[5761] = 'value_603677'
    reference[5761] = 'value_603677'
    assert tree.invariants(), "Invariants failed at step 10805"

    # Operation 10806: get
    assert tree.invariants(), "Invariants failed at step 10806"

    # Operation 10807: get
    assert tree.invariants(), "Invariants failed at step 10807"

    # Operation 10808: get
    assert tree.invariants(), "Invariants failed at step 10808"

    # Operation 10809: delete
    del tree[5761]
    del reference[5761]
    assert tree.invariants(), "Invariants failed at step 10809"

    # Operation 10810: insert
    tree[1372] = 'value_433270'
    reference[1372] = 'value_433270'
    assert tree.invariants(), "Invariants failed at step 10810"

    # Operation 10811: get
    assert tree.invariants(), "Invariants failed at step 10811"

    # Operation 10812: insert
    tree[6627] = 'value_337045'
    reference[6627] = 'value_337045'
    assert tree.invariants(), "Invariants failed at step 10812"

    # Operation 10813: get
    assert tree.invariants(), "Invariants failed at step 10813"

    # Operation 10814: get
    assert tree.invariants(), "Invariants failed at step 10814"

    # Operation 10815: delete
    del tree[1372]
    del reference[1372]
    assert tree.invariants(), "Invariants failed at step 10815"

    # Operation 10816: insert
    tree[993] = 'value_987348'
    reference[993] = 'value_987348'
    assert tree.invariants(), "Invariants failed at step 10816"

    # Operation 10817: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10817"

    # Operation 10818: insert
    tree[4613] = 'value_25382'
    reference[4613] = 'value_25382'
    assert tree.invariants(), "Invariants failed at step 10818"

    # Operation 10819: insert
    tree[7106] = 'value_768765'
    reference[7106] = 'value_768765'
    assert tree.invariants(), "Invariants failed at step 10819"

    # Operation 10820: insert
    tree[1340] = 'value_952087'
    reference[1340] = 'value_952087'
    assert tree.invariants(), "Invariants failed at step 10820"

    # Operation 10821: get
    assert tree.invariants(), "Invariants failed at step 10821"

    # Operation 10822: insert
    tree[999] = 'value_697940'
    reference[999] = 'value_697940'
    assert tree.invariants(), "Invariants failed at step 10822"

    # Operation 10823: get
    assert tree.invariants(), "Invariants failed at step 10823"

    # Operation 10824: get
    assert tree.invariants(), "Invariants failed at step 10824"

    # Operation 10825: batch_delete
    keys_to_delete = [993, 7106, 9219, 8802, 4613]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10825"

    # Operation 10826: get
    assert tree.invariants(), "Invariants failed at step 10826"

    # Operation 10827: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10827"

    # Operation 10828: get
    assert tree.invariants(), "Invariants failed at step 10828"

    # Operation 10829: insert
    tree[3662] = 'value_881314'
    reference[3662] = 'value_881314'
    assert tree.invariants(), "Invariants failed at step 10829"

    # Operation 10830: insert
    tree[7036] = 'value_504799'
    reference[7036] = 'value_504799'
    assert tree.invariants(), "Invariants failed at step 10830"

    # Operation 10831: delete
    del tree[3662]
    del reference[3662]
    assert tree.invariants(), "Invariants failed at step 10831"

    # Operation 10832: delete
    del tree[1340]
    del reference[1340]
    assert tree.invariants(), "Invariants failed at step 10832"

    # Operation 10833: delete
    del tree[999]
    del reference[999]
    assert tree.invariants(), "Invariants failed at step 10833"

    # Operation 10834: update
    tree[7036] = 'value_235430'
    reference[7036] = 'value_235430'
    assert tree.invariants(), "Invariants failed at step 10834"

    # Operation 10835: get
    assert tree.invariants(), "Invariants failed at step 10835"

    # Operation 10836: get
    assert tree.invariants(), "Invariants failed at step 10836"

    # Operation 10837: insert
    tree[7710] = 'value_946409'
    reference[7710] = 'value_946409'
    assert tree.invariants(), "Invariants failed at step 10837"

    # Operation 10838: update
    tree[6627] = 'value_294939'
    reference[6627] = 'value_294939'
    assert tree.invariants(), "Invariants failed at step 10838"

    # Operation 10839: delete
    del tree[7036]
    del reference[7036]
    assert tree.invariants(), "Invariants failed at step 10839"

    # Operation 10840: insert
    tree[9381] = 'value_148782'
    reference[9381] = 'value_148782'
    assert tree.invariants(), "Invariants failed at step 10840"

    # Operation 10841: delete
    del tree[7710]
    del reference[7710]
    assert tree.invariants(), "Invariants failed at step 10841"

    # Operation 10842: insert
    tree[6745] = 'value_868004'
    reference[6745] = 'value_868004'
    assert tree.invariants(), "Invariants failed at step 10842"

    # Operation 10843: delete
    del tree[6745]
    del reference[6745]
    assert tree.invariants(), "Invariants failed at step 10843"

    # Operation 10844: update
    tree[6627] = 'value_206789'
    reference[6627] = 'value_206789'
    assert tree.invariants(), "Invariants failed at step 10844"

    # Operation 10845: update
    tree[6627] = 'value_691575'
    reference[6627] = 'value_691575'
    assert tree.invariants(), "Invariants failed at step 10845"

    # Operation 10846: get
    assert tree.invariants(), "Invariants failed at step 10846"

    # Operation 10847: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10847"

    # Operation 10848: insert
    tree[9154] = 'value_896111'
    reference[9154] = 'value_896111'
    assert tree.invariants(), "Invariants failed at step 10848"

    # Operation 10849: delete
    del tree[9154]
    del reference[9154]
    assert tree.invariants(), "Invariants failed at step 10849"

    # Operation 10850: insert
    tree[9541] = 'value_456495'
    reference[9541] = 'value_456495'
    assert tree.invariants(), "Invariants failed at step 10850"

    # Operation 10851: insert
    tree[2330] = 'value_93956'
    reference[2330] = 'value_93956'
    assert tree.invariants(), "Invariants failed at step 10851"

    # Operation 10852: delete
    del tree[6627]
    del reference[6627]
    assert tree.invariants(), "Invariants failed at step 10852"

    # Operation 10853: insert
    tree[7343] = 'value_418108'
    reference[7343] = 'value_418108'
    assert tree.invariants(), "Invariants failed at step 10853"

    # Operation 10854: insert
    tree[2413] = 'value_700871'
    reference[2413] = 'value_700871'
    assert tree.invariants(), "Invariants failed at step 10854"

    # Operation 10855: insert
    tree[9882] = 'value_130937'
    reference[9882] = 'value_130937'
    assert tree.invariants(), "Invariants failed at step 10855"

    # Operation 10856: delete
    del tree[2330]
    del reference[2330]
    assert tree.invariants(), "Invariants failed at step 10856"

    # Operation 10857: delete
    del tree[9541]
    del reference[9541]
    assert tree.invariants(), "Invariants failed at step 10857"

    # Operation 10858: insert
    tree[1033] = 'value_179327'
    reference[1033] = 'value_179327'
    assert tree.invariants(), "Invariants failed at step 10858"

    # Operation 10859: insert
    tree[6899] = 'value_25705'
    reference[6899] = 'value_25705'
    assert tree.invariants(), "Invariants failed at step 10859"

    # Operation 10860: get
    assert tree.invariants(), "Invariants failed at step 10860"

    # Operation 10861: update
    tree[7343] = 'value_634177'
    reference[7343] = 'value_634177'
    assert tree.invariants(), "Invariants failed at step 10861"

    # Operation 10862: get
    assert tree.invariants(), "Invariants failed at step 10862"

    # Operation 10863: update
    tree[9882] = 'value_657581'
    reference[9882] = 'value_657581'
    assert tree.invariants(), "Invariants failed at step 10863"

    # Operation 10864: update
    tree[9381] = 'value_380510'
    reference[9381] = 'value_380510'
    assert tree.invariants(), "Invariants failed at step 10864"

    # Operation 10865: insert
    tree[2329] = 'value_581934'
    reference[2329] = 'value_581934'
    assert tree.invariants(), "Invariants failed at step 10865"

    # Operation 10866: update
    tree[7343] = 'value_415238'
    reference[7343] = 'value_415238'
    assert tree.invariants(), "Invariants failed at step 10866"

    # Operation 10867: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10867"

    # Operation 10868: get
    assert tree.invariants(), "Invariants failed at step 10868"

    # Operation 10869: update
    tree[6899] = 'value_132486'
    reference[6899] = 'value_132486'
    assert tree.invariants(), "Invariants failed at step 10869"

    # Operation 10870: delete
    del tree[2329]
    del reference[2329]
    assert tree.invariants(), "Invariants failed at step 10870"

    # Operation 10871: delete
    del tree[9381]
    del reference[9381]
    assert tree.invariants(), "Invariants failed at step 10871"

    # Operation 10872: insert
    tree[5277] = 'value_224455'
    reference[5277] = 'value_224455'
    assert tree.invariants(), "Invariants failed at step 10872"

    # Operation 10873: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10873"

    # Operation 10874: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10874"

    # Operation 10875: insert
    tree[6869] = 'value_432832'
    reference[6869] = 'value_432832'
    assert tree.invariants(), "Invariants failed at step 10875"

    # Operation 10876: get
    assert tree.invariants(), "Invariants failed at step 10876"

    # Operation 10877: update
    tree[6869] = 'value_402306'
    reference[6869] = 'value_402306'
    assert tree.invariants(), "Invariants failed at step 10877"

    # Operation 10878: delete
    del tree[1033]
    del reference[1033]
    assert tree.invariants(), "Invariants failed at step 10878"

    # Operation 10879: get
    assert tree.invariants(), "Invariants failed at step 10879"

    # Operation 10880: delete
    del tree[7343]
    del reference[7343]
    assert tree.invariants(), "Invariants failed at step 10880"

    # Operation 10881: update
    tree[2413] = 'value_370161'
    reference[2413] = 'value_370161'
    assert tree.invariants(), "Invariants failed at step 10881"

    # Operation 10882: delete
    del tree[6869]
    del reference[6869]
    assert tree.invariants(), "Invariants failed at step 10882"

    # Operation 10883: insert
    tree[2401] = 'value_328711'
    reference[2401] = 'value_328711'
    assert tree.invariants(), "Invariants failed at step 10883"

    # Operation 10884: get
    assert tree.invariants(), "Invariants failed at step 10884"

    # Operation 10885: update
    tree[5277] = 'value_487266'
    reference[5277] = 'value_487266'
    assert tree.invariants(), "Invariants failed at step 10885"

    # Operation 10886: update
    tree[5277] = 'value_273662'
    reference[5277] = 'value_273662'
    assert tree.invariants(), "Invariants failed at step 10886"

    # Operation 10887: batch_delete
    keys_to_delete = [9808, 2401, 2413, 758]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10887"

    # Operation 10888: insert
    tree[9610] = 'value_891191'
    reference[9610] = 'value_891191'
    assert tree.invariants(), "Invariants failed at step 10888"

    # Operation 10889: update
    tree[9610] = 'value_426945'
    reference[9610] = 'value_426945'
    assert tree.invariants(), "Invariants failed at step 10889"

    # Operation 10890: delete
    del tree[6899]
    del reference[6899]
    assert tree.invariants(), "Invariants failed at step 10890"

    # Operation 10891: get
    assert tree.invariants(), "Invariants failed at step 10891"

    # Operation 10892: delete
    del tree[9610]
    del reference[9610]
    assert tree.invariants(), "Invariants failed at step 10892"

    # Operation 10893: get
    assert tree.invariants(), "Invariants failed at step 10893"

    # Operation 10894: get
    assert tree.invariants(), "Invariants failed at step 10894"

    # Operation 10895: get
    assert tree.invariants(), "Invariants failed at step 10895"

    # Operation 10896: insert
    tree[336] = 'value_749019'
    reference[336] = 'value_749019'
    assert tree.invariants(), "Invariants failed at step 10896"

    # Operation 10897: get
    assert tree.invariants(), "Invariants failed at step 10897"

    # Operation 10898: update
    tree[336] = 'value_206757'
    reference[336] = 'value_206757'
    assert tree.invariants(), "Invariants failed at step 10898"

    # Operation 10899: insert
    tree[3465] = 'value_178225'
    reference[3465] = 'value_178225'
    assert tree.invariants(), "Invariants failed at step 10899"

    # Operation 10900: get
    assert tree.invariants(), "Invariants failed at step 10900"

    # Operation 10901: insert
    tree[1614] = 'value_601084'
    reference[1614] = 'value_601084'
    assert tree.invariants(), "Invariants failed at step 10901"

    # Operation 10902: get
    assert tree.invariants(), "Invariants failed at step 10902"

    # Operation 10903: insert
    tree[6083] = 'value_457361'
    reference[6083] = 'value_457361'
    assert tree.invariants(), "Invariants failed at step 10903"

    # Operation 10904: get
    assert tree.invariants(), "Invariants failed at step 10904"

    # Operation 10905: get
    assert tree.invariants(), "Invariants failed at step 10905"

    # Operation 10906: delete
    del tree[3465]
    del reference[3465]
    assert tree.invariants(), "Invariants failed at step 10906"

    # Operation 10907: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10907"

    # Operation 10908: insert
    tree[2179] = 'value_989006'
    reference[2179] = 'value_989006'
    assert tree.invariants(), "Invariants failed at step 10908"

    # Operation 10909: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10909"

    # Operation 10910: batch_delete
    keys_to_delete = [6083, 336, 9461, 9749, 5277]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10910"

    # Operation 10911: insert
    tree[9997] = 'value_731708'
    reference[9997] = 'value_731708'
    assert tree.invariants(), "Invariants failed at step 10911"

    # Operation 10912: insert
    tree[900] = 'value_787954'
    reference[900] = 'value_787954'
    assert tree.invariants(), "Invariants failed at step 10912"

    # Operation 10913: get
    assert tree.invariants(), "Invariants failed at step 10913"

    # Operation 10914: update
    tree[9882] = 'value_76834'
    reference[9882] = 'value_76834'
    assert tree.invariants(), "Invariants failed at step 10914"

    # Operation 10915: get
    assert tree.invariants(), "Invariants failed at step 10915"

    # Operation 10916: get
    assert tree.invariants(), "Invariants failed at step 10916"

    # Operation 10917: insert
    tree[450] = 'value_625777'
    reference[450] = 'value_625777'
    assert tree.invariants(), "Invariants failed at step 10917"

    # Operation 10918: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10918"

    # Operation 10919: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10919"

    # Operation 10920: batch_delete
    keys_to_delete = [9882, 2179, 9997, 9343]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10920"

    # Operation 10921: get
    assert tree.invariants(), "Invariants failed at step 10921"

    # Operation 10922: get
    assert tree.invariants(), "Invariants failed at step 10922"

    # Operation 10923: get
    assert tree.invariants(), "Invariants failed at step 10923"

    # Operation 10924: get
    assert tree.invariants(), "Invariants failed at step 10924"

    # Operation 10925: get
    assert tree.invariants(), "Invariants failed at step 10925"

    # Operation 10926: update
    tree[900] = 'value_1757'
    reference[900] = 'value_1757'
    assert tree.invariants(), "Invariants failed at step 10926"

    # Operation 10927: update
    tree[450] = 'value_896681'
    reference[450] = 'value_896681'
    assert tree.invariants(), "Invariants failed at step 10927"

    # Operation 10928: insert
    tree[7498] = 'value_76937'
    reference[7498] = 'value_76937'
    assert tree.invariants(), "Invariants failed at step 10928"

    # Operation 10929: insert
    tree[253] = 'value_263700'
    reference[253] = 'value_263700'
    assert tree.invariants(), "Invariants failed at step 10929"

    # Operation 10930: insert
    tree[6985] = 'value_993718'
    reference[6985] = 'value_993718'
    assert tree.invariants(), "Invariants failed at step 10930"

    # Operation 10931: update
    tree[1614] = 'value_791790'
    reference[1614] = 'value_791790'
    assert tree.invariants(), "Invariants failed at step 10931"

    # Operation 10932: insert
    tree[3783] = 'value_849429'
    reference[3783] = 'value_849429'
    assert tree.invariants(), "Invariants failed at step 10932"

    # Operation 10933: get
    assert tree.invariants(), "Invariants failed at step 10933"

    # Operation 10934: batch_delete
    keys_to_delete = [450, 6211, 900, 3783, 253]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10934"

    # Operation 10935: insert
    tree[669] = 'value_387013'
    reference[669] = 'value_387013'
    assert tree.invariants(), "Invariants failed at step 10935"

    # Operation 10936: insert
    tree[6873] = 'value_775886'
    reference[6873] = 'value_775886'
    assert tree.invariants(), "Invariants failed at step 10936"

    # Operation 10937: batch_delete
    keys_to_delete = [4968, 6873, 7498, 7710]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 10937"

    # Operation 10938: get
    assert tree.invariants(), "Invariants failed at step 10938"

    # Operation 10939: get
    assert tree.invariants(), "Invariants failed at step 10939"

    # Operation 10940: get
    assert tree.invariants(), "Invariants failed at step 10940"

    # Operation 10941: delete
    del tree[6985]
    del reference[6985]
    assert tree.invariants(), "Invariants failed at step 10941"

    # Operation 10942: update
    tree[669] = 'value_48861'
    reference[669] = 'value_48861'
    assert tree.invariants(), "Invariants failed at step 10942"

    # Operation 10943: delete
    del tree[669]
    del reference[669]
    assert tree.invariants(), "Invariants failed at step 10943"

    # Operation 10944: update
    tree[1614] = 'value_377494'
    reference[1614] = 'value_377494'
    assert tree.invariants(), "Invariants failed at step 10944"

    # Operation 10945: insert
    tree[5942] = 'value_689386'
    reference[5942] = 'value_689386'
    assert tree.invariants(), "Invariants failed at step 10945"

    # Operation 10946: get
    assert tree.invariants(), "Invariants failed at step 10946"

    # Operation 10947: delete
    del tree[1614]
    del reference[1614]
    assert tree.invariants(), "Invariants failed at step 10947"

    # Operation 10948: delete
    del tree[5942]
    del reference[5942]
    assert tree.invariants(), "Invariants failed at step 10948"

    # Operation 10949: insert
    tree[6077] = 'value_654856'
    reference[6077] = 'value_654856'
    assert tree.invariants(), "Invariants failed at step 10949"

    # Operation 10950: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10950"

    # Operation 10951: update
    tree[6077] = 'value_724892'
    reference[6077] = 'value_724892'
    assert tree.invariants(), "Invariants failed at step 10951"

    # Operation 10952: insert
    tree[7257] = 'value_772106'
    reference[7257] = 'value_772106'
    assert tree.invariants(), "Invariants failed at step 10952"

    # Operation 10953: update
    tree[7257] = 'value_313974'
    reference[7257] = 'value_313974'
    assert tree.invariants(), "Invariants failed at step 10953"

    # Operation 10954: insert
    tree[434] = 'value_722445'
    reference[434] = 'value_722445'
    assert tree.invariants(), "Invariants failed at step 10954"

    # Operation 10955: delete
    del tree[7257]
    del reference[7257]
    assert tree.invariants(), "Invariants failed at step 10955"

    # Operation 10956: delete
    del tree[6077]
    del reference[6077]
    assert tree.invariants(), "Invariants failed at step 10956"

    # Operation 10957: update
    tree[434] = 'value_103424'
    reference[434] = 'value_103424'
    assert tree.invariants(), "Invariants failed at step 10957"

    # Operation 10958: get
    assert tree.invariants(), "Invariants failed at step 10958"

    # Operation 10959: get
    assert tree.invariants(), "Invariants failed at step 10959"

    # Operation 10960: get
    assert tree.invariants(), "Invariants failed at step 10960"

    # Operation 10961: delete
    del tree[434]
    del reference[434]
    assert tree.invariants(), "Invariants failed at step 10961"

    # Operation 10962: get
    assert tree.invariants(), "Invariants failed at step 10962"

    # Operation 10963: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 10963"

    # Operation 10964: insert
    tree[5929] = 'value_380691'
    reference[5929] = 'value_380691'
    assert tree.invariants(), "Invariants failed at step 10964"

    # Operation 10965: delete
    del tree[5929]
    del reference[5929]
    assert tree.invariants(), "Invariants failed at step 10965"

    # Operation 10966: insert
    tree[8326] = 'value_266728'
    reference[8326] = 'value_266728'
    assert tree.invariants(), "Invariants failed at step 10966"

    # Operation 10967: update
    tree[8326] = 'value_14522'
    reference[8326] = 'value_14522'
    assert tree.invariants(), "Invariants failed at step 10967"

    # Operation 10968: update
    tree[8326] = 'value_240755'
    reference[8326] = 'value_240755'
    assert tree.invariants(), "Invariants failed at step 10968"

    # Operation 10969: update
    tree[8326] = 'value_21363'
    reference[8326] = 'value_21363'
    assert tree.invariants(), "Invariants failed at step 10969"

    # Operation 10970: insert
    tree[3535] = 'value_142716'
    reference[3535] = 'value_142716'
    assert tree.invariants(), "Invariants failed at step 10970"

    # Operation 10971: get
    assert tree.invariants(), "Invariants failed at step 10971"

    # Operation 10972: insert
    tree[4472] = 'value_986526'
    reference[4472] = 'value_986526'
    assert tree.invariants(), "Invariants failed at step 10972"

    # Operation 10973: insert
    tree[4629] = 'value_156399'
    reference[4629] = 'value_156399'
    assert tree.invariants(), "Invariants failed at step 10973"

    # Operation 10974: insert
    tree[3027] = 'value_362640'
    reference[3027] = 'value_362640'
    assert tree.invariants(), "Invariants failed at step 10974"

    # Operation 10975: delete
    del tree[4472]
    del reference[4472]
    assert tree.invariants(), "Invariants failed at step 10975"

    # Operation 10976: delete
    del tree[8326]
    del reference[8326]
    assert tree.invariants(), "Invariants failed at step 10976"

    # Operation 10977: get
    assert tree.invariants(), "Invariants failed at step 10977"

    # Operation 10978: get
    assert tree.invariants(), "Invariants failed at step 10978"

    # Operation 10979: delete
    del tree[3535]
    del reference[3535]
    assert tree.invariants(), "Invariants failed at step 10979"

    # Operation 10980: get
    assert tree.invariants(), "Invariants failed at step 10980"

    # Operation 10981: get
    assert tree.invariants(), "Invariants failed at step 10981"

    # Operation 10982: insert
    tree[6255] = 'value_360347'
    reference[6255] = 'value_360347'
    assert tree.invariants(), "Invariants failed at step 10982"

    # Operation 10983: get
    assert tree.invariants(), "Invariants failed at step 10983"

    # Operation 10984: insert
    tree[8176] = 'value_130693'
    reference[8176] = 'value_130693'
    assert tree.invariants(), "Invariants failed at step 10984"

    # Operation 10985: delete
    del tree[6255]
    del reference[6255]
    assert tree.invariants(), "Invariants failed at step 10985"

    # Operation 10986: get
    assert tree.invariants(), "Invariants failed at step 10986"

    # Operation 10987: update
    tree[8176] = 'value_716465'
    reference[8176] = 'value_716465'
    assert tree.invariants(), "Invariants failed at step 10987"

    # Operation 10988: insert
    tree[4332] = 'value_330078'
    reference[4332] = 'value_330078'
    assert tree.invariants(), "Invariants failed at step 10988"

    # Operation 10989: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 10989"

    # Operation 10990: delete
    del tree[8176]
    del reference[8176]
    assert tree.invariants(), "Invariants failed at step 10990"

    # Operation 10991: insert
    tree[7016] = 'value_584296'
    reference[7016] = 'value_584296'
    assert tree.invariants(), "Invariants failed at step 10991"

    # Operation 10992: insert
    tree[9141] = 'value_714008'
    reference[9141] = 'value_714008'
    assert tree.invariants(), "Invariants failed at step 10992"

    # Operation 10993: get
    assert tree.invariants(), "Invariants failed at step 10993"

    # Operation 10994: update
    tree[3027] = 'value_463313'
    reference[3027] = 'value_463313'
    assert tree.invariants(), "Invariants failed at step 10994"

    # Operation 10995: delete
    del tree[4332]
    del reference[4332]
    assert tree.invariants(), "Invariants failed at step 10995"

    # Operation 10996: delete
    del tree[9141]
    del reference[9141]
    assert tree.invariants(), "Invariants failed at step 10996"

    # Operation 10997: insert
    tree[472] = 'value_611701'
    reference[472] = 'value_611701'
    assert tree.invariants(), "Invariants failed at step 10997"

    # Operation 10998: get
    assert tree.invariants(), "Invariants failed at step 10998"

    # Operation 10999: delete
    del tree[4629]
    del reference[4629]
    assert tree.invariants(), "Invariants failed at step 10999"

    # Operation 11000: insert
    tree[5502] = 'value_942189'
    reference[5502] = 'value_942189'
    assert tree.invariants(), "Invariants failed at step 11000"

    # Operation 11001: get
    assert tree.invariants(), "Invariants failed at step 11001"

    # Operation 11002: get
    assert tree.invariants(), "Invariants failed at step 11002"

    # Operation 11003: delete
    del tree[3027]
    del reference[3027]
    assert tree.invariants(), "Invariants failed at step 11003"

    # Operation 11004: update
    tree[7016] = 'value_438716'
    reference[7016] = 'value_438716'
    assert tree.invariants(), "Invariants failed at step 11004"

    # Operation 11005: delete
    del tree[5502]
    del reference[5502]
    assert tree.invariants(), "Invariants failed at step 11005"

    # Operation 11006: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11006"

    # Operation 11007: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11007"

    # Operation 11008: insert
    tree[5014] = 'value_9184'
    reference[5014] = 'value_9184'
    assert tree.invariants(), "Invariants failed at step 11008"

    # Operation 11009: delete
    del tree[7016]
    del reference[7016]
    assert tree.invariants(), "Invariants failed at step 11009"

    # Operation 11010: update
    tree[5014] = 'value_551200'
    reference[5014] = 'value_551200'
    assert tree.invariants(), "Invariants failed at step 11010"

    # Operation 11011: delete
    del tree[5014]
    del reference[5014]
    assert tree.invariants(), "Invariants failed at step 11011"

    # Operation 11012: insert
    tree[6898] = 'value_718496'
    reference[6898] = 'value_718496'
    assert tree.invariants(), "Invariants failed at step 11012"

    # Operation 11013: get
    assert tree.invariants(), "Invariants failed at step 11013"

    # Operation 11014: get
    assert tree.invariants(), "Invariants failed at step 11014"

    # Operation 11015: update
    tree[472] = 'value_687049'
    reference[472] = 'value_687049'
    assert tree.invariants(), "Invariants failed at step 11015"

    # Operation 11016: get
    assert tree.invariants(), "Invariants failed at step 11016"

    # Operation 11017: get
    assert tree.invariants(), "Invariants failed at step 11017"

    # Operation 11018: insert
    tree[9195] = 'value_307960'
    reference[9195] = 'value_307960'
    assert tree.invariants(), "Invariants failed at step 11018"

    # Operation 11019: update
    tree[472] = 'value_215563'
    reference[472] = 'value_215563'
    assert tree.invariants(), "Invariants failed at step 11019"

    # Operation 11020: get
    assert tree.invariants(), "Invariants failed at step 11020"

    # Operation 11021: delete
    del tree[6898]
    del reference[6898]
    assert tree.invariants(), "Invariants failed at step 11021"

    # Operation 11022: delete
    del tree[9195]
    del reference[9195]
    assert tree.invariants(), "Invariants failed at step 11022"

    # Operation 11023: insert
    tree[6437] = 'value_137702'
    reference[6437] = 'value_137702'
    assert tree.invariants(), "Invariants failed at step 11023"

    # Operation 11024: delete
    del tree[472]
    del reference[472]
    assert tree.invariants(), "Invariants failed at step 11024"

    # Operation 11025: insert
    tree[5851] = 'value_549642'
    reference[5851] = 'value_549642'
    assert tree.invariants(), "Invariants failed at step 11025"

    # Operation 11026: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11026"

    # Operation 11027: update
    tree[5851] = 'value_838983'
    reference[5851] = 'value_838983'
    assert tree.invariants(), "Invariants failed at step 11027"

    # Operation 11028: insert
    tree[2677] = 'value_85209'
    reference[2677] = 'value_85209'
    assert tree.invariants(), "Invariants failed at step 11028"

    # Operation 11029: get
    assert tree.invariants(), "Invariants failed at step 11029"

    # Operation 11030: delete
    del tree[6437]
    del reference[6437]
    assert tree.invariants(), "Invariants failed at step 11030"

    # Operation 11031: insert
    tree[3536] = 'value_795102'
    reference[3536] = 'value_795102'
    assert tree.invariants(), "Invariants failed at step 11031"

    # Operation 11032: insert
    tree[4964] = 'value_23803'
    reference[4964] = 'value_23803'
    assert tree.invariants(), "Invariants failed at step 11032"

    # Operation 11033: get
    assert tree.invariants(), "Invariants failed at step 11033"

    # Operation 11034: update
    tree[5851] = 'value_260137'
    reference[5851] = 'value_260137'
    assert tree.invariants(), "Invariants failed at step 11034"

    # Operation 11035: insert
    tree[4258] = 'value_115012'
    reference[4258] = 'value_115012'
    assert tree.invariants(), "Invariants failed at step 11035"

    # Operation 11036: batch_delete
    keys_to_delete = [5851, 4964, 8859, 5071]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11036"

    # Operation 11037: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11037"

    # Operation 11038: delete
    del tree[2677]
    del reference[2677]
    assert tree.invariants(), "Invariants failed at step 11038"

    # Operation 11039: update
    tree[4258] = 'value_862971'
    reference[4258] = 'value_862971'
    assert tree.invariants(), "Invariants failed at step 11039"

    # Operation 11040: update
    tree[4258] = 'value_863415'
    reference[4258] = 'value_863415'
    assert tree.invariants(), "Invariants failed at step 11040"

    # Operation 11041: get
    assert tree.invariants(), "Invariants failed at step 11041"

    # Operation 11042: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11042"

    # Operation 11043: get
    assert tree.invariants(), "Invariants failed at step 11043"

    # Operation 11044: delete
    del tree[4258]
    del reference[4258]
    assert tree.invariants(), "Invariants failed at step 11044"

    # Operation 11045: get
    assert tree.invariants(), "Invariants failed at step 11045"

    # Operation 11046: delete
    del tree[3536]
    del reference[3536]
    assert tree.invariants(), "Invariants failed at step 11046"

    # Operation 11047: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11047"

    # Operation 11048: get
    assert tree.invariants(), "Invariants failed at step 11048"

    # Operation 11049: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11049"

    # Operation 11050: get
    assert tree.invariants(), "Invariants failed at step 11050"

    # Operation 11051: insert
    tree[5841] = 'value_776525'
    reference[5841] = 'value_776525'
    assert tree.invariants(), "Invariants failed at step 11051"

    # Operation 11052: insert
    tree[7943] = 'value_717948'
    reference[7943] = 'value_717948'
    assert tree.invariants(), "Invariants failed at step 11052"

    # Operation 11053: insert
    tree[9787] = 'value_22603'
    reference[9787] = 'value_22603'
    assert tree.invariants(), "Invariants failed at step 11053"

    # Operation 11054: insert
    tree[2318] = 'value_36363'
    reference[2318] = 'value_36363'
    assert tree.invariants(), "Invariants failed at step 11054"

    # Operation 11055: insert
    tree[3524] = 'value_846100'
    reference[3524] = 'value_846100'
    assert tree.invariants(), "Invariants failed at step 11055"

    # Operation 11056: delete
    del tree[9787]
    del reference[9787]
    assert tree.invariants(), "Invariants failed at step 11056"

    # Operation 11057: get
    assert tree.invariants(), "Invariants failed at step 11057"

    # Operation 11058: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11058"

    # Operation 11059: delete
    del tree[2318]
    del reference[2318]
    assert tree.invariants(), "Invariants failed at step 11059"

    # Operation 11060: insert
    tree[9078] = 'value_162285'
    reference[9078] = 'value_162285'
    assert tree.invariants(), "Invariants failed at step 11060"

    # Operation 11061: delete
    del tree[9078]
    del reference[9078]
    assert tree.invariants(), "Invariants failed at step 11061"

    # Operation 11062: delete
    del tree[7943]
    del reference[7943]
    assert tree.invariants(), "Invariants failed at step 11062"

    # Operation 11063: insert
    tree[5252] = 'value_737772'
    reference[5252] = 'value_737772'
    assert tree.invariants(), "Invariants failed at step 11063"

    # Operation 11064: insert
    tree[962] = 'value_467979'
    reference[962] = 'value_467979'
    assert tree.invariants(), "Invariants failed at step 11064"

    # Operation 11065: insert
    tree[6651] = 'value_477802'
    reference[6651] = 'value_477802'
    assert tree.invariants(), "Invariants failed at step 11065"

    # Operation 11066: get
    assert tree.invariants(), "Invariants failed at step 11066"

    # Operation 11067: get
    assert tree.invariants(), "Invariants failed at step 11067"

    # Operation 11068: batch_delete
    keys_to_delete = [5252, 962, 3524, 6333]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11068"

    # Operation 11069: insert
    tree[8561] = 'value_853052'
    reference[8561] = 'value_853052'
    assert tree.invariants(), "Invariants failed at step 11069"

    # Operation 11070: insert
    tree[6643] = 'value_164903'
    reference[6643] = 'value_164903'
    assert tree.invariants(), "Invariants failed at step 11070"

    # Operation 11071: delete
    del tree[6651]
    del reference[6651]
    assert tree.invariants(), "Invariants failed at step 11071"

    # Operation 11072: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11072"

    # Operation 11073: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11073"

    # Operation 11074: delete
    del tree[6643]
    del reference[6643]
    assert tree.invariants(), "Invariants failed at step 11074"

    # Operation 11075: get
    assert tree.invariants(), "Invariants failed at step 11075"

    # Operation 11076: get
    assert tree.invariants(), "Invariants failed at step 11076"

    # Operation 11077: delete
    del tree[8561]
    del reference[8561]
    assert tree.invariants(), "Invariants failed at step 11077"

    # Operation 11078: update
    tree[5841] = 'value_976953'
    reference[5841] = 'value_976953'
    assert tree.invariants(), "Invariants failed at step 11078"

    # Operation 11079: insert
    tree[2411] = 'value_838296'
    reference[2411] = 'value_838296'
    assert tree.invariants(), "Invariants failed at step 11079"

    # Operation 11080: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11080"

    # Operation 11081: insert
    tree[9870] = 'value_944440'
    reference[9870] = 'value_944440'
    assert tree.invariants(), "Invariants failed at step 11081"

    # Operation 11082: update
    tree[9870] = 'value_42030'
    reference[9870] = 'value_42030'
    assert tree.invariants(), "Invariants failed at step 11082"

    # Operation 11083: delete
    del tree[2411]
    del reference[2411]
    assert tree.invariants(), "Invariants failed at step 11083"

    # Operation 11084: get
    assert tree.invariants(), "Invariants failed at step 11084"

    # Operation 11085: update
    tree[5841] = 'value_481948'
    reference[5841] = 'value_481948'
    assert tree.invariants(), "Invariants failed at step 11085"

    # Operation 11086: update
    tree[9870] = 'value_808398'
    reference[9870] = 'value_808398'
    assert tree.invariants(), "Invariants failed at step 11086"

    # Operation 11087: insert
    tree[3426] = 'value_637466'
    reference[3426] = 'value_637466'
    assert tree.invariants(), "Invariants failed at step 11087"

    # Operation 11088: insert
    tree[568] = 'value_460412'
    reference[568] = 'value_460412'
    assert tree.invariants(), "Invariants failed at step 11088"

    # Operation 11089: insert
    tree[3022] = 'value_699302'
    reference[3022] = 'value_699302'
    assert tree.invariants(), "Invariants failed at step 11089"

    # Operation 11090: batch_delete
    keys_to_delete = [3426, 3130, 1013, 9870]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11090"

    # Operation 11091: insert
    tree[9539] = 'value_612429'
    reference[9539] = 'value_612429'
    assert tree.invariants(), "Invariants failed at step 11091"

    # Operation 11092: get
    assert tree.invariants(), "Invariants failed at step 11092"

    # Operation 11093: delete
    del tree[5841]
    del reference[5841]
    assert tree.invariants(), "Invariants failed at step 11093"

    # Operation 11094: insert
    tree[2821] = 'value_286389'
    reference[2821] = 'value_286389'
    assert tree.invariants(), "Invariants failed at step 11094"

    # Operation 11095: delete
    del tree[3022]
    del reference[3022]
    assert tree.invariants(), "Invariants failed at step 11095"

    # Operation 11096: insert
    tree[7151] = 'value_706314'
    reference[7151] = 'value_706314'
    assert tree.invariants(), "Invariants failed at step 11096"

    # Operation 11097: insert
    tree[881] = 'value_489453'
    reference[881] = 'value_489453'
    assert tree.invariants(), "Invariants failed at step 11097"

    # Operation 11098: insert
    tree[1179] = 'value_902681'
    reference[1179] = 'value_902681'
    assert tree.invariants(), "Invariants failed at step 11098"

    # Operation 11099: get
    assert tree.invariants(), "Invariants failed at step 11099"

    # Operation 11100: get
    assert tree.invariants(), "Invariants failed at step 11100"

    # Operation 11101: insert
    tree[8093] = 'value_610922'
    reference[8093] = 'value_610922'
    assert tree.invariants(), "Invariants failed at step 11101"

    # Operation 11102: update
    tree[2821] = 'value_622816'
    reference[2821] = 'value_622816'
    assert tree.invariants(), "Invariants failed at step 11102"

    # Operation 11103: update
    tree[8093] = 'value_148445'
    reference[8093] = 'value_148445'
    assert tree.invariants(), "Invariants failed at step 11103"

    # Operation 11104: delete
    del tree[568]
    del reference[568]
    assert tree.invariants(), "Invariants failed at step 11104"

    # Operation 11105: get
    assert tree.invariants(), "Invariants failed at step 11105"

    # Operation 11106: delete
    del tree[7151]
    del reference[7151]
    assert tree.invariants(), "Invariants failed at step 11106"

    # Operation 11107: update
    tree[881] = 'value_512728'
    reference[881] = 'value_512728'
    assert tree.invariants(), "Invariants failed at step 11107"

    # Operation 11108: insert
    tree[1306] = 'value_583918'
    reference[1306] = 'value_583918'
    assert tree.invariants(), "Invariants failed at step 11108"

    # Operation 11109: batch_delete
    keys_to_delete = [7683, 9539, 9424, 881, 1306]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11109"

    # Operation 11110: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11110"

    # Operation 11111: update
    tree[8093] = 'value_624691'
    reference[8093] = 'value_624691'
    assert tree.invariants(), "Invariants failed at step 11111"

    # Operation 11112: delete
    del tree[1179]
    del reference[1179]
    assert tree.invariants(), "Invariants failed at step 11112"

    # Operation 11113: delete
    del tree[2821]
    del reference[2821]
    assert tree.invariants(), "Invariants failed at step 11113"

    # Operation 11114: insert
    tree[2056] = 'value_27334'
    reference[2056] = 'value_27334'
    assert tree.invariants(), "Invariants failed at step 11114"

    # Operation 11115: insert
    tree[8310] = 'value_441388'
    reference[8310] = 'value_441388'
    assert tree.invariants(), "Invariants failed at step 11115"

    # Operation 11116: delete
    del tree[2056]
    del reference[2056]
    assert tree.invariants(), "Invariants failed at step 11116"

    # Operation 11117: delete
    del tree[8310]
    del reference[8310]
    assert tree.invariants(), "Invariants failed at step 11117"

    # Operation 11118: insert
    tree[806] = 'value_355338'
    reference[806] = 'value_355338'
    assert tree.invariants(), "Invariants failed at step 11118"

    # Operation 11119: delete
    del tree[8093]
    del reference[8093]
    assert tree.invariants(), "Invariants failed at step 11119"

    # Operation 11120: insert
    tree[5404] = 'value_636847'
    reference[5404] = 'value_636847'
    assert tree.invariants(), "Invariants failed at step 11120"

    # Operation 11121: update
    tree[5404] = 'value_852180'
    reference[5404] = 'value_852180'
    assert tree.invariants(), "Invariants failed at step 11121"

    # Operation 11122: insert
    tree[9164] = 'value_111210'
    reference[9164] = 'value_111210'
    assert tree.invariants(), "Invariants failed at step 11122"

    # Operation 11123: get
    assert tree.invariants(), "Invariants failed at step 11123"

    # Operation 11124: update
    tree[806] = 'value_801329'
    reference[806] = 'value_801329'
    assert tree.invariants(), "Invariants failed at step 11124"

    # Operation 11125: insert
    tree[7298] = 'value_271607'
    reference[7298] = 'value_271607'
    assert tree.invariants(), "Invariants failed at step 11125"

    # Operation 11126: insert
    tree[9112] = 'value_438543'
    reference[9112] = 'value_438543'
    assert tree.invariants(), "Invariants failed at step 11126"

    # Operation 11127: get
    assert tree.invariants(), "Invariants failed at step 11127"

    # Operation 11128: delete
    del tree[7298]
    del reference[7298]
    assert tree.invariants(), "Invariants failed at step 11128"

    # Operation 11129: get
    assert tree.invariants(), "Invariants failed at step 11129"

    # Operation 11130: insert
    tree[766] = 'value_280666'
    reference[766] = 'value_280666'
    assert tree.invariants(), "Invariants failed at step 11130"

    # Operation 11131: delete
    del tree[806]
    del reference[806]
    assert tree.invariants(), "Invariants failed at step 11131"

    # Operation 11132: insert
    tree[9491] = 'value_834701'
    reference[9491] = 'value_834701'
    assert tree.invariants(), "Invariants failed at step 11132"

    # Operation 11133: update
    tree[5404] = 'value_983873'
    reference[5404] = 'value_983873'
    assert tree.invariants(), "Invariants failed at step 11133"

    # Operation 11134: delete
    del tree[9112]
    del reference[9112]
    assert tree.invariants(), "Invariants failed at step 11134"

    # Operation 11135: insert
    tree[180] = 'value_980624'
    reference[180] = 'value_980624'
    assert tree.invariants(), "Invariants failed at step 11135"

    # Operation 11136: insert
    tree[3469] = 'value_422345'
    reference[3469] = 'value_422345'
    assert tree.invariants(), "Invariants failed at step 11136"

    # Operation 11137: insert
    tree[4040] = 'value_840632'
    reference[4040] = 'value_840632'
    assert tree.invariants(), "Invariants failed at step 11137"

    # Operation 11138: insert
    tree[6685] = 'value_446100'
    reference[6685] = 'value_446100'
    assert tree.invariants(), "Invariants failed at step 11138"

    # Operation 11139: delete
    del tree[6685]
    del reference[6685]
    assert tree.invariants(), "Invariants failed at step 11139"

    # Operation 11140: insert
    tree[1477] = 'value_616525'
    reference[1477] = 'value_616525'
    assert tree.invariants(), "Invariants failed at step 11140"

    # Operation 11141: delete
    del tree[180]
    del reference[180]
    assert tree.invariants(), "Invariants failed at step 11141"

    # Operation 11142: insert
    tree[5908] = 'value_771082'
    reference[5908] = 'value_771082'
    assert tree.invariants(), "Invariants failed at step 11142"

    # Operation 11143: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11143"

    # Operation 11144: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11144"

    # Operation 11145: delete
    del tree[5908]
    del reference[5908]
    assert tree.invariants(), "Invariants failed at step 11145"

    # Operation 11146: delete
    del tree[9491]
    del reference[9491]
    assert tree.invariants(), "Invariants failed at step 11146"

    # Operation 11147: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11147"

    # Operation 11148: get
    assert tree.invariants(), "Invariants failed at step 11148"

    # Operation 11149: delete
    del tree[1477]
    del reference[1477]
    assert tree.invariants(), "Invariants failed at step 11149"

    # Operation 11150: insert
    tree[719] = 'value_48287'
    reference[719] = 'value_48287'
    assert tree.invariants(), "Invariants failed at step 11150"

    # Operation 11151: insert
    tree[2061] = 'value_600743'
    reference[2061] = 'value_600743'
    assert tree.invariants(), "Invariants failed at step 11151"

    # Operation 11152: insert
    tree[5828] = 'value_668803'
    reference[5828] = 'value_668803'
    assert tree.invariants(), "Invariants failed at step 11152"

    # Operation 11153: insert
    tree[3054] = 'value_350413'
    reference[3054] = 'value_350413'
    assert tree.invariants(), "Invariants failed at step 11153"

    # Operation 11154: insert
    tree[6366] = 'value_909545'
    reference[6366] = 'value_909545'
    assert tree.invariants(), "Invariants failed at step 11154"

    # Operation 11155: insert
    tree[4619] = 'value_509547'
    reference[4619] = 'value_509547'
    assert tree.invariants(), "Invariants failed at step 11155"

    # Operation 11156: get
    assert tree.invariants(), "Invariants failed at step 11156"

    # Operation 11157: get
    assert tree.invariants(), "Invariants failed at step 11157"

    # Operation 11158: get
    assert tree.invariants(), "Invariants failed at step 11158"

    # Operation 11159: insert
    tree[789] = 'value_741632'
    reference[789] = 'value_741632'
    assert tree.invariants(), "Invariants failed at step 11159"

    # Operation 11160: delete
    del tree[3469]
    del reference[3469]
    assert tree.invariants(), "Invariants failed at step 11160"

    # Operation 11161: delete
    del tree[719]
    del reference[719]
    assert tree.invariants(), "Invariants failed at step 11161"

    # Operation 11162: insert
    tree[6251] = 'value_950404'
    reference[6251] = 'value_950404'
    assert tree.invariants(), "Invariants failed at step 11162"

    # Operation 11163: delete
    del tree[9164]
    del reference[9164]
    assert tree.invariants(), "Invariants failed at step 11163"

    # Operation 11164: insert
    tree[9576] = 'value_599703'
    reference[9576] = 'value_599703'
    assert tree.invariants(), "Invariants failed at step 11164"

    # Operation 11165: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11165"

    # Operation 11166: insert
    tree[6428] = 'value_504820'
    reference[6428] = 'value_504820'
    assert tree.invariants(), "Invariants failed at step 11166"

    # Operation 11167: insert
    tree[9161] = 'value_93733'
    reference[9161] = 'value_93733'
    assert tree.invariants(), "Invariants failed at step 11167"

    # Operation 11168: insert
    tree[6116] = 'value_427152'
    reference[6116] = 'value_427152'
    assert tree.invariants(), "Invariants failed at step 11168"

    # Operation 11169: get
    assert tree.invariants(), "Invariants failed at step 11169"

    # Operation 11170: update
    tree[5828] = 'value_655491'
    reference[5828] = 'value_655491'
    assert tree.invariants(), "Invariants failed at step 11170"

    # Operation 11171: insert
    tree[4573] = 'value_935597'
    reference[4573] = 'value_935597'
    assert tree.invariants(), "Invariants failed at step 11171"

    # Operation 11172: batch_delete
    keys_to_delete = [6116, 9161, 6251, 4619, 5229, 766, 660, 789, 6366]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11172"

    # Operation 11173: insert
    tree[1378] = 'value_151258'
    reference[1378] = 'value_151258'
    assert tree.invariants(), "Invariants failed at step 11173"

    # Operation 11174: insert
    tree[5508] = 'value_683179'
    reference[5508] = 'value_683179'
    assert tree.invariants(), "Invariants failed at step 11174"

    # Operation 11175: insert
    tree[7173] = 'value_491963'
    reference[7173] = 'value_491963'
    assert tree.invariants(), "Invariants failed at step 11175"

    # Operation 11176: delete
    del tree[4040]
    del reference[4040]
    assert tree.invariants(), "Invariants failed at step 11176"

    # Operation 11177: insert
    tree[4863] = 'value_664256'
    reference[4863] = 'value_664256'
    assert tree.invariants(), "Invariants failed at step 11177"

    # Operation 11178: delete
    del tree[4573]
    del reference[4573]
    assert tree.invariants(), "Invariants failed at step 11178"

    # Operation 11179: insert
    tree[6580] = 'value_97768'
    reference[6580] = 'value_97768'
    assert tree.invariants(), "Invariants failed at step 11179"

    # Operation 11180: delete
    del tree[6580]
    del reference[6580]
    assert tree.invariants(), "Invariants failed at step 11180"

    # Operation 11181: insert
    tree[6453] = 'value_437172'
    reference[6453] = 'value_437172'
    assert tree.invariants(), "Invariants failed at step 11181"

    # Operation 11182: get
    assert tree.invariants(), "Invariants failed at step 11182"

    # Operation 11183: delete
    del tree[3054]
    del reference[3054]
    assert tree.invariants(), "Invariants failed at step 11183"

    # Operation 11184: batch_delete
    keys_to_delete = [5828, 7529, 4653, 2061, 6453]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11184"

    # Operation 11185: get
    assert tree.invariants(), "Invariants failed at step 11185"

    # Operation 11186: insert
    tree[8767] = 'value_210539'
    reference[8767] = 'value_210539'
    assert tree.invariants(), "Invariants failed at step 11186"

    # Operation 11187: update
    tree[4863] = 'value_234817'
    reference[4863] = 'value_234817'
    assert tree.invariants(), "Invariants failed at step 11187"

    # Operation 11188: delete
    del tree[5508]
    del reference[5508]
    assert tree.invariants(), "Invariants failed at step 11188"

    # Operation 11189: delete
    del tree[6428]
    del reference[6428]
    assert tree.invariants(), "Invariants failed at step 11189"

    # Operation 11190: insert
    tree[2659] = 'value_988648'
    reference[2659] = 'value_988648'
    assert tree.invariants(), "Invariants failed at step 11190"

    # Operation 11191: insert
    tree[9653] = 'value_485984'
    reference[9653] = 'value_485984'
    assert tree.invariants(), "Invariants failed at step 11191"

    # Operation 11192: insert
    tree[1770] = 'value_468901'
    reference[1770] = 'value_468901'
    assert tree.invariants(), "Invariants failed at step 11192"

    # Operation 11193: insert
    tree[9226] = 'value_246803'
    reference[9226] = 'value_246803'
    assert tree.invariants(), "Invariants failed at step 11193"

    # Operation 11194: delete
    del tree[2659]
    del reference[2659]
    assert tree.invariants(), "Invariants failed at step 11194"

    # Operation 11195: batch_delete
    keys_to_delete = [1378, 6916, 4229, 9576, 9226, 4863]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11195"

    # Operation 11196: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11196"

    # Operation 11197: batch_delete
    keys_to_delete = [1770, 9653, 7150, 7581]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11197"

    # Operation 11198: delete
    del tree[8767]
    del reference[8767]
    assert tree.invariants(), "Invariants failed at step 11198"

    # Operation 11199: get
    assert tree.invariants(), "Invariants failed at step 11199"

    # Operation 11200: get
    assert tree.invariants(), "Invariants failed at step 11200"

    # Operation 11201: insert
    tree[7950] = 'value_204780'
    reference[7950] = 'value_204780'
    assert tree.invariants(), "Invariants failed at step 11201"

    # Operation 11202: insert
    tree[6813] = 'value_67721'
    reference[6813] = 'value_67721'
    assert tree.invariants(), "Invariants failed at step 11202"

    # Operation 11203: update
    tree[5404] = 'value_894440'
    reference[5404] = 'value_894440'
    assert tree.invariants(), "Invariants failed at step 11203"

    # Operation 11204: insert
    tree[7861] = 'value_345055'
    reference[7861] = 'value_345055'
    assert tree.invariants(), "Invariants failed at step 11204"

    # Operation 11205: get
    assert tree.invariants(), "Invariants failed at step 11205"

    # Operation 11206: batch_delete
    keys_to_delete = [33, 6813, 7173, 7950]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11206"

    # Operation 11207: delete
    del tree[7861]
    del reference[7861]
    assert tree.invariants(), "Invariants failed at step 11207"

    # Operation 11208: update
    tree[5404] = 'value_251946'
    reference[5404] = 'value_251946'
    assert tree.invariants(), "Invariants failed at step 11208"

    # Operation 11209: get
    assert tree.invariants(), "Invariants failed at step 11209"

    # Operation 11210: update
    tree[5404] = 'value_429789'
    reference[5404] = 'value_429789'
    assert tree.invariants(), "Invariants failed at step 11210"

    # Operation 11211: insert
    tree[3758] = 'value_296774'
    reference[3758] = 'value_296774'
    assert tree.invariants(), "Invariants failed at step 11211"

    # Operation 11212: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11212"

    # Operation 11213: delete
    del tree[3758]
    del reference[3758]
    assert tree.invariants(), "Invariants failed at step 11213"

    # Operation 11214: delete
    del tree[5404]
    del reference[5404]
    assert tree.invariants(), "Invariants failed at step 11214"

    # Operation 11215: insert
    tree[6115] = 'value_586238'
    reference[6115] = 'value_586238'
    assert tree.invariants(), "Invariants failed at step 11215"

    # Operation 11216: get
    assert tree.invariants(), "Invariants failed at step 11216"

    # Operation 11217: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11217"

    # Operation 11218: update
    tree[6115] = 'value_115823'
    reference[6115] = 'value_115823'
    assert tree.invariants(), "Invariants failed at step 11218"

    # Operation 11219: delete
    del tree[6115]
    del reference[6115]
    assert tree.invariants(), "Invariants failed at step 11219"

    # Operation 11220: get
    assert tree.invariants(), "Invariants failed at step 11220"

    # Operation 11221: insert
    tree[4846] = 'value_157495'
    reference[4846] = 'value_157495'
    assert tree.invariants(), "Invariants failed at step 11221"

    # Operation 11222: get
    assert tree.invariants(), "Invariants failed at step 11222"

    # Operation 11223: insert
    tree[2966] = 'value_10782'
    reference[2966] = 'value_10782'
    assert tree.invariants(), "Invariants failed at step 11223"

    # Operation 11224: update
    tree[4846] = 'value_531143'
    reference[4846] = 'value_531143'
    assert tree.invariants(), "Invariants failed at step 11224"

    # Operation 11225: insert
    tree[4340] = 'value_735261'
    reference[4340] = 'value_735261'
    assert tree.invariants(), "Invariants failed at step 11225"

    # Operation 11226: get
    assert tree.invariants(), "Invariants failed at step 11226"

    # Operation 11227: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11227"

    # Operation 11228: insert
    tree[7552] = 'value_538760'
    reference[7552] = 'value_538760'
    assert tree.invariants(), "Invariants failed at step 11228"

    # Operation 11229: update
    tree[7552] = 'value_702639'
    reference[7552] = 'value_702639'
    assert tree.invariants(), "Invariants failed at step 11229"

    # Operation 11230: get
    assert tree.invariants(), "Invariants failed at step 11230"

    # Operation 11231: delete
    del tree[7552]
    del reference[7552]
    assert tree.invariants(), "Invariants failed at step 11231"

    # Operation 11232: insert
    tree[4466] = 'value_90557'
    reference[4466] = 'value_90557'
    assert tree.invariants(), "Invariants failed at step 11232"

    # Operation 11233: insert
    tree[8161] = 'value_780513'
    reference[8161] = 'value_780513'
    assert tree.invariants(), "Invariants failed at step 11233"

    # Operation 11234: get
    assert tree.invariants(), "Invariants failed at step 11234"

    # Operation 11235: delete
    del tree[2966]
    del reference[2966]
    assert tree.invariants(), "Invariants failed at step 11235"

    # Operation 11236: get
    assert tree.invariants(), "Invariants failed at step 11236"

    # Operation 11237: insert
    tree[4856] = 'value_274402'
    reference[4856] = 'value_274402'
    assert tree.invariants(), "Invariants failed at step 11237"

    # Operation 11238: get
    assert tree.invariants(), "Invariants failed at step 11238"

    # Operation 11239: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11239"

    # Operation 11240: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11240"

    # Operation 11241: delete
    del tree[4856]
    del reference[4856]
    assert tree.invariants(), "Invariants failed at step 11241"

    # Operation 11242: insert
    tree[2462] = 'value_777807'
    reference[2462] = 'value_777807'
    assert tree.invariants(), "Invariants failed at step 11242"

    # Operation 11243: delete
    del tree[4846]
    del reference[4846]
    assert tree.invariants(), "Invariants failed at step 11243"

    # Operation 11244: get
    assert tree.invariants(), "Invariants failed at step 11244"

    # Operation 11245: delete
    del tree[4340]
    del reference[4340]
    assert tree.invariants(), "Invariants failed at step 11245"

    # Operation 11246: delete
    del tree[2462]
    del reference[2462]
    assert tree.invariants(), "Invariants failed at step 11246"

    # Operation 11247: delete
    del tree[8161]
    del reference[8161]
    assert tree.invariants(), "Invariants failed at step 11247"

    # Operation 11248: delete
    del tree[4466]
    del reference[4466]
    assert tree.invariants(), "Invariants failed at step 11248"

    # Operation 11249: insert
    tree[9525] = 'value_379069'
    reference[9525] = 'value_379069'
    assert tree.invariants(), "Invariants failed at step 11249"

    # Operation 11250: insert
    tree[5950] = 'value_36479'
    reference[5950] = 'value_36479'
    assert tree.invariants(), "Invariants failed at step 11250"

    # Operation 11251: insert
    tree[7111] = 'value_419636'
    reference[7111] = 'value_419636'
    assert tree.invariants(), "Invariants failed at step 11251"

    # Operation 11252: get
    assert tree.invariants(), "Invariants failed at step 11252"

    # Operation 11253: delete
    del tree[5950]
    del reference[5950]
    assert tree.invariants(), "Invariants failed at step 11253"

    # Operation 11254: update
    tree[7111] = 'value_885647'
    reference[7111] = 'value_885647'
    assert tree.invariants(), "Invariants failed at step 11254"

    # Operation 11255: get
    assert tree.invariants(), "Invariants failed at step 11255"

    # Operation 11256: get
    assert tree.invariants(), "Invariants failed at step 11256"

    # Operation 11257: delete
    del tree[7111]
    del reference[7111]
    assert tree.invariants(), "Invariants failed at step 11257"

    # Operation 11258: update
    tree[9525] = 'value_634520'
    reference[9525] = 'value_634520'
    assert tree.invariants(), "Invariants failed at step 11258"

    # Operation 11259: insert
    tree[9834] = 'value_796856'
    reference[9834] = 'value_796856'
    assert tree.invariants(), "Invariants failed at step 11259"

    # Operation 11260: delete
    del tree[9525]
    del reference[9525]
    assert tree.invariants(), "Invariants failed at step 11260"

    # Operation 11261: get
    assert tree.invariants(), "Invariants failed at step 11261"

    # Operation 11262: insert
    tree[8690] = 'value_266177'
    reference[8690] = 'value_266177'
    assert tree.invariants(), "Invariants failed at step 11262"

    # Operation 11263: insert
    tree[16] = 'value_832840'
    reference[16] = 'value_832840'
    assert tree.invariants(), "Invariants failed at step 11263"

    # Operation 11264: insert
    tree[677] = 'value_384451'
    reference[677] = 'value_384451'
    assert tree.invariants(), "Invariants failed at step 11264"

    # Operation 11265: delete
    del tree[8690]
    del reference[8690]
    assert tree.invariants(), "Invariants failed at step 11265"

    # Operation 11266: delete
    del tree[16]
    del reference[16]
    assert tree.invariants(), "Invariants failed at step 11266"

    # Operation 11267: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11267"

    # Operation 11268: insert
    tree[9181] = 'value_728605'
    reference[9181] = 'value_728605'
    assert tree.invariants(), "Invariants failed at step 11268"

    # Operation 11269: get
    assert tree.invariants(), "Invariants failed at step 11269"

    # Operation 11270: update
    tree[9181] = 'value_885243'
    reference[9181] = 'value_885243'
    assert tree.invariants(), "Invariants failed at step 11270"

    # Operation 11271: delete
    del tree[9834]
    del reference[9834]
    assert tree.invariants(), "Invariants failed at step 11271"

    # Operation 11272: update
    tree[9181] = 'value_969619'
    reference[9181] = 'value_969619'
    assert tree.invariants(), "Invariants failed at step 11272"

    # Operation 11273: insert
    tree[703] = 'value_492087'
    reference[703] = 'value_492087'
    assert tree.invariants(), "Invariants failed at step 11273"

    # Operation 11274: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11274"

    # Operation 11275: get
    assert tree.invariants(), "Invariants failed at step 11275"

    # Operation 11276: insert
    tree[8304] = 'value_241573'
    reference[8304] = 'value_241573'
    assert tree.invariants(), "Invariants failed at step 11276"

    # Operation 11277: update
    tree[9181] = 'value_132828'
    reference[9181] = 'value_132828'
    assert tree.invariants(), "Invariants failed at step 11277"

    # Operation 11278: get
    assert tree.invariants(), "Invariants failed at step 11278"

    # Operation 11279: insert
    tree[7204] = 'value_15993'
    reference[7204] = 'value_15993'
    assert tree.invariants(), "Invariants failed at step 11279"

    # Operation 11280: insert
    tree[8047] = 'value_233065'
    reference[8047] = 'value_233065'
    assert tree.invariants(), "Invariants failed at step 11280"

    # Operation 11281: update
    tree[9181] = 'value_949772'
    reference[9181] = 'value_949772'
    assert tree.invariants(), "Invariants failed at step 11281"

    # Operation 11282: delete
    del tree[9181]
    del reference[9181]
    assert tree.invariants(), "Invariants failed at step 11282"

    # Operation 11283: get
    assert tree.invariants(), "Invariants failed at step 11283"

    # Operation 11284: insert
    tree[2847] = 'value_859589'
    reference[2847] = 'value_859589'
    assert tree.invariants(), "Invariants failed at step 11284"

    # Operation 11285: delete
    del tree[703]
    del reference[703]
    assert tree.invariants(), "Invariants failed at step 11285"

    # Operation 11286: delete
    del tree[8304]
    del reference[8304]
    assert tree.invariants(), "Invariants failed at step 11286"

    # Operation 11287: insert
    tree[2098] = 'value_390670'
    reference[2098] = 'value_390670'
    assert tree.invariants(), "Invariants failed at step 11287"

    # Operation 11288: delete
    del tree[8047]
    del reference[8047]
    assert tree.invariants(), "Invariants failed at step 11288"

    # Operation 11289: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11289"

    # Operation 11290: insert
    tree[7752] = 'value_559394'
    reference[7752] = 'value_559394'
    assert tree.invariants(), "Invariants failed at step 11290"

    # Operation 11291: update
    tree[7752] = 'value_22039'
    reference[7752] = 'value_22039'
    assert tree.invariants(), "Invariants failed at step 11291"

    # Operation 11292: insert
    tree[4059] = 'value_259929'
    reference[4059] = 'value_259929'
    assert tree.invariants(), "Invariants failed at step 11292"

    # Operation 11293: get
    assert tree.invariants(), "Invariants failed at step 11293"

    # Operation 11294: get
    assert tree.invariants(), "Invariants failed at step 11294"

    # Operation 11295: get
    assert tree.invariants(), "Invariants failed at step 11295"

    # Operation 11296: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11296"

    # Operation 11297: delete
    del tree[7204]
    del reference[7204]
    assert tree.invariants(), "Invariants failed at step 11297"

    # Operation 11298: insert
    tree[6267] = 'value_186573'
    reference[6267] = 'value_186573'
    assert tree.invariants(), "Invariants failed at step 11298"

    # Operation 11299: get
    assert tree.invariants(), "Invariants failed at step 11299"

    # Operation 11300: batch_delete
    keys_to_delete = [3104, 4283, 2098, 6267, 2847]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11300"

    # Operation 11301: delete
    del tree[7752]
    del reference[7752]
    assert tree.invariants(), "Invariants failed at step 11301"

    # Operation 11302: insert
    tree[9120] = 'value_308435'
    reference[9120] = 'value_308435'
    assert tree.invariants(), "Invariants failed at step 11302"

    # Operation 11303: update
    tree[4059] = 'value_803642'
    reference[4059] = 'value_803642'
    assert tree.invariants(), "Invariants failed at step 11303"

    # Operation 11304: get
    assert tree.invariants(), "Invariants failed at step 11304"

    # Operation 11305: update
    tree[9120] = 'value_521456'
    reference[9120] = 'value_521456'
    assert tree.invariants(), "Invariants failed at step 11305"

    # Operation 11306: get
    assert tree.invariants(), "Invariants failed at step 11306"

    # Operation 11307: insert
    tree[2995] = 'value_264054'
    reference[2995] = 'value_264054'
    assert tree.invariants(), "Invariants failed at step 11307"

    # Operation 11308: delete
    del tree[9120]
    del reference[9120]
    assert tree.invariants(), "Invariants failed at step 11308"

    # Operation 11309: delete
    del tree[4059]
    del reference[4059]
    assert tree.invariants(), "Invariants failed at step 11309"

    # Operation 11310: delete
    del tree[677]
    del reference[677]
    assert tree.invariants(), "Invariants failed at step 11310"

    # Operation 11311: delete
    del tree[2995]
    del reference[2995]
    assert tree.invariants(), "Invariants failed at step 11311"

    # Operation 11312: insert
    tree[321] = 'value_646634'
    reference[321] = 'value_646634'
    assert tree.invariants(), "Invariants failed at step 11312"

    # Operation 11313: delete
    del tree[321]
    del reference[321]
    assert tree.invariants(), "Invariants failed at step 11313"

    # Operation 11314: insert
    tree[9762] = 'value_406949'
    reference[9762] = 'value_406949'
    assert tree.invariants(), "Invariants failed at step 11314"

    # Operation 11315: insert
    tree[1687] = 'value_401623'
    reference[1687] = 'value_401623'
    assert tree.invariants(), "Invariants failed at step 11315"

    # Operation 11316: update
    tree[9762] = 'value_299055'
    reference[9762] = 'value_299055'
    assert tree.invariants(), "Invariants failed at step 11316"

    # Operation 11317: delete
    del tree[9762]
    del reference[9762]
    assert tree.invariants(), "Invariants failed at step 11317"

    # Operation 11318: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11318"

    # Operation 11319: get
    assert tree.invariants(), "Invariants failed at step 11319"

    # Operation 11320: delete
    del tree[1687]
    del reference[1687]
    assert tree.invariants(), "Invariants failed at step 11320"

    # Operation 11321: get
    assert tree.invariants(), "Invariants failed at step 11321"

    # Operation 11322: insert
    tree[6834] = 'value_446837'
    reference[6834] = 'value_446837'
    assert tree.invariants(), "Invariants failed at step 11322"

    # Operation 11323: insert
    tree[426] = 'value_925699'
    reference[426] = 'value_925699'
    assert tree.invariants(), "Invariants failed at step 11323"

    # Operation 11324: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11324"

    # Operation 11325: get
    assert tree.invariants(), "Invariants failed at step 11325"

    # Operation 11326: get
    assert tree.invariants(), "Invariants failed at step 11326"

    # Operation 11327: delete
    del tree[6834]
    del reference[6834]
    assert tree.invariants(), "Invariants failed at step 11327"

    # Operation 11328: delete
    del tree[426]
    del reference[426]
    assert tree.invariants(), "Invariants failed at step 11328"

    # Operation 11329: get
    assert tree.invariants(), "Invariants failed at step 11329"

    # Operation 11330: get
    assert tree.invariants(), "Invariants failed at step 11330"

    # Operation 11331: get
    assert tree.invariants(), "Invariants failed at step 11331"

    # Operation 11332: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11332"

    # Operation 11333: insert
    tree[7308] = 'value_731671'
    reference[7308] = 'value_731671'
    assert tree.invariants(), "Invariants failed at step 11333"

    # Operation 11334: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11334"

    # Operation 11335: get
    assert tree.invariants(), "Invariants failed at step 11335"

    # Operation 11336: delete
    del tree[7308]
    del reference[7308]
    assert tree.invariants(), "Invariants failed at step 11336"

    # Operation 11337: get
    assert tree.invariants(), "Invariants failed at step 11337"

    # Operation 11338: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11338"

    # Operation 11339: insert
    tree[2443] = 'value_425348'
    reference[2443] = 'value_425348'
    assert tree.invariants(), "Invariants failed at step 11339"

    # Operation 11340: get
    assert tree.invariants(), "Invariants failed at step 11340"

    # Operation 11341: get
    assert tree.invariants(), "Invariants failed at step 11341"

    # Operation 11342: insert
    tree[1537] = 'value_803304'
    reference[1537] = 'value_803304'
    assert tree.invariants(), "Invariants failed at step 11342"

    # Operation 11343: insert
    tree[861] = 'value_691296'
    reference[861] = 'value_691296'
    assert tree.invariants(), "Invariants failed at step 11343"

    # Operation 11344: insert
    tree[5470] = 'value_474051'
    reference[5470] = 'value_474051'
    assert tree.invariants(), "Invariants failed at step 11344"

    # Operation 11345: delete
    del tree[2443]
    del reference[2443]
    assert tree.invariants(), "Invariants failed at step 11345"

    # Operation 11346: get
    assert tree.invariants(), "Invariants failed at step 11346"

    # Operation 11347: delete
    del tree[5470]
    del reference[5470]
    assert tree.invariants(), "Invariants failed at step 11347"

    # Operation 11348: get
    assert tree.invariants(), "Invariants failed at step 11348"

    # Operation 11349: insert
    tree[7886] = 'value_880856'
    reference[7886] = 'value_880856'
    assert tree.invariants(), "Invariants failed at step 11349"

    # Operation 11350: get
    assert tree.invariants(), "Invariants failed at step 11350"

    # Operation 11351: delete
    del tree[861]
    del reference[861]
    assert tree.invariants(), "Invariants failed at step 11351"

    # Operation 11352: update
    tree[1537] = 'value_940841'
    reference[1537] = 'value_940841'
    assert tree.invariants(), "Invariants failed at step 11352"

    # Operation 11353: get
    assert tree.invariants(), "Invariants failed at step 11353"

    # Operation 11354: get
    assert tree.invariants(), "Invariants failed at step 11354"

    # Operation 11355: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11355"

    # Operation 11356: insert
    tree[3123] = 'value_247333'
    reference[3123] = 'value_247333'
    assert tree.invariants(), "Invariants failed at step 11356"

    # Operation 11357: insert
    tree[4562] = 'value_605627'
    reference[4562] = 'value_605627'
    assert tree.invariants(), "Invariants failed at step 11357"

    # Operation 11358: insert
    tree[5120] = 'value_151358'
    reference[5120] = 'value_151358'
    assert tree.invariants(), "Invariants failed at step 11358"

    # Operation 11359: delete
    del tree[4562]
    del reference[4562]
    assert tree.invariants(), "Invariants failed at step 11359"

    # Operation 11360: delete
    del tree[1537]
    del reference[1537]
    assert tree.invariants(), "Invariants failed at step 11360"

    # Operation 11361: get
    assert tree.invariants(), "Invariants failed at step 11361"

    # Operation 11362: insert
    tree[2334] = 'value_936585'
    reference[2334] = 'value_936585'
    assert tree.invariants(), "Invariants failed at step 11362"

    # Operation 11363: insert
    tree[5491] = 'value_81148'
    reference[5491] = 'value_81148'
    assert tree.invariants(), "Invariants failed at step 11363"

    # Operation 11364: insert
    tree[2928] = 'value_815347'
    reference[2928] = 'value_815347'
    assert tree.invariants(), "Invariants failed at step 11364"

    # Operation 11365: insert
    tree[9017] = 'value_193985'
    reference[9017] = 'value_193985'
    assert tree.invariants(), "Invariants failed at step 11365"

    # Operation 11366: insert
    tree[9495] = 'value_258637'
    reference[9495] = 'value_258637'
    assert tree.invariants(), "Invariants failed at step 11366"

    # Operation 11367: get
    assert tree.invariants(), "Invariants failed at step 11367"

    # Operation 11368: get
    assert tree.invariants(), "Invariants failed at step 11368"

    # Operation 11369: batch_delete
    keys_to_delete = [5120, 7971, 3881, 9495, 9017, 2334]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11369"

    # Operation 11370: delete
    del tree[3123]
    del reference[3123]
    assert tree.invariants(), "Invariants failed at step 11370"

    # Operation 11371: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11371"

    # Operation 11372: delete
    del tree[2928]
    del reference[2928]
    assert tree.invariants(), "Invariants failed at step 11372"

    # Operation 11373: insert
    tree[1163] = 'value_343125'
    reference[1163] = 'value_343125'
    assert tree.invariants(), "Invariants failed at step 11373"

    # Operation 11374: delete
    del tree[5491]
    del reference[5491]
    assert tree.invariants(), "Invariants failed at step 11374"

    # Operation 11375: insert
    tree[1031] = 'value_182575'
    reference[1031] = 'value_182575'
    assert tree.invariants(), "Invariants failed at step 11375"

    # Operation 11376: update
    tree[1163] = 'value_473401'
    reference[1163] = 'value_473401'
    assert tree.invariants(), "Invariants failed at step 11376"

    # Operation 11377: insert
    tree[2469] = 'value_582128'
    reference[2469] = 'value_582128'
    assert tree.invariants(), "Invariants failed at step 11377"

    # Operation 11378: delete
    del tree[1031]
    del reference[1031]
    assert tree.invariants(), "Invariants failed at step 11378"

    # Operation 11379: get
    assert tree.invariants(), "Invariants failed at step 11379"

    # Operation 11380: insert
    tree[998] = 'value_464024'
    reference[998] = 'value_464024'
    assert tree.invariants(), "Invariants failed at step 11380"

    # Operation 11381: insert
    tree[1965] = 'value_412409'
    reference[1965] = 'value_412409'
    assert tree.invariants(), "Invariants failed at step 11381"

    # Operation 11382: insert
    tree[473] = 'value_538713'
    reference[473] = 'value_538713'
    assert tree.invariants(), "Invariants failed at step 11382"

    # Operation 11383: get
    assert tree.invariants(), "Invariants failed at step 11383"

    # Operation 11384: get
    assert tree.invariants(), "Invariants failed at step 11384"

    # Operation 11385: get
    assert tree.invariants(), "Invariants failed at step 11385"

    # Operation 11386: delete
    del tree[2469]
    del reference[2469]
    assert tree.invariants(), "Invariants failed at step 11386"

    # Operation 11387: update
    tree[7886] = 'value_390166'
    reference[7886] = 'value_390166'
    assert tree.invariants(), "Invariants failed at step 11387"

    # Operation 11388: delete
    del tree[1163]
    del reference[1163]
    assert tree.invariants(), "Invariants failed at step 11388"

    # Operation 11389: update
    tree[1965] = 'value_480087'
    reference[1965] = 'value_480087'
    assert tree.invariants(), "Invariants failed at step 11389"

    # Operation 11390: delete
    del tree[998]
    del reference[998]
    assert tree.invariants(), "Invariants failed at step 11390"

    # Operation 11391: get
    assert tree.invariants(), "Invariants failed at step 11391"

    # Operation 11392: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11392"

    # Operation 11393: update
    tree[7886] = 'value_15593'
    reference[7886] = 'value_15593'
    assert tree.invariants(), "Invariants failed at step 11393"

    # Operation 11394: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11394"

    # Operation 11395: get
    assert tree.invariants(), "Invariants failed at step 11395"

    # Operation 11396: get
    assert tree.invariants(), "Invariants failed at step 11396"

    # Operation 11397: insert
    tree[1677] = 'value_918024'
    reference[1677] = 'value_918024'
    assert tree.invariants(), "Invariants failed at step 11397"

    # Operation 11398: get
    assert tree.invariants(), "Invariants failed at step 11398"

    # Operation 11399: get
    assert tree.invariants(), "Invariants failed at step 11399"

    # Operation 11400: insert
    tree[179] = 'value_415537'
    reference[179] = 'value_415537'
    assert tree.invariants(), "Invariants failed at step 11400"

    # Operation 11401: delete
    del tree[473]
    del reference[473]
    assert tree.invariants(), "Invariants failed at step 11401"

    # Operation 11402: insert
    tree[7922] = 'value_166154'
    reference[7922] = 'value_166154'
    assert tree.invariants(), "Invariants failed at step 11402"

    # Operation 11403: insert
    tree[8075] = 'value_903022'
    reference[8075] = 'value_903022'
    assert tree.invariants(), "Invariants failed at step 11403"

    # Operation 11404: delete
    del tree[179]
    del reference[179]
    assert tree.invariants(), "Invariants failed at step 11404"

    # Operation 11405: get
    assert tree.invariants(), "Invariants failed at step 11405"

    # Operation 11406: update
    tree[1677] = 'value_360517'
    reference[1677] = 'value_360517'
    assert tree.invariants(), "Invariants failed at step 11406"

    # Operation 11407: get
    assert tree.invariants(), "Invariants failed at step 11407"

    # Operation 11408: update
    tree[1965] = 'value_729481'
    reference[1965] = 'value_729481'
    assert tree.invariants(), "Invariants failed at step 11408"

    # Operation 11409: delete
    del tree[7922]
    del reference[7922]
    assert tree.invariants(), "Invariants failed at step 11409"

    # Operation 11410: insert
    tree[5887] = 'value_141102'
    reference[5887] = 'value_141102'
    assert tree.invariants(), "Invariants failed at step 11410"

    # Operation 11411: delete
    del tree[7886]
    del reference[7886]
    assert tree.invariants(), "Invariants failed at step 11411"

    # Operation 11412: update
    tree[1677] = 'value_581852'
    reference[1677] = 'value_581852'
    assert tree.invariants(), "Invariants failed at step 11412"

    # Operation 11413: update
    tree[1677] = 'value_844558'
    reference[1677] = 'value_844558'
    assert tree.invariants(), "Invariants failed at step 11413"

    # Operation 11414: insert
    tree[9686] = 'value_914194'
    reference[9686] = 'value_914194'
    assert tree.invariants(), "Invariants failed at step 11414"

    # Operation 11415: delete
    del tree[9686]
    del reference[9686]
    assert tree.invariants(), "Invariants failed at step 11415"

    # Operation 11416: get
    assert tree.invariants(), "Invariants failed at step 11416"

    # Operation 11417: delete
    del tree[5887]
    del reference[5887]
    assert tree.invariants(), "Invariants failed at step 11417"

    # Operation 11418: insert
    tree[2487] = 'value_879302'
    reference[2487] = 'value_879302'
    assert tree.invariants(), "Invariants failed at step 11418"

    # Operation 11419: insert
    tree[7309] = 'value_512262'
    reference[7309] = 'value_512262'
    assert tree.invariants(), "Invariants failed at step 11419"

    # Operation 11420: get
    assert tree.invariants(), "Invariants failed at step 11420"

    # Operation 11421: insert
    tree[6013] = 'value_939464'
    reference[6013] = 'value_939464'
    assert tree.invariants(), "Invariants failed at step 11421"

    # Operation 11422: delete
    del tree[1965]
    del reference[1965]
    assert tree.invariants(), "Invariants failed at step 11422"

    # Operation 11423: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11423"

    # Operation 11424: update
    tree[8075] = 'value_45455'
    reference[8075] = 'value_45455'
    assert tree.invariants(), "Invariants failed at step 11424"

    # Operation 11425: insert
    tree[3599] = 'value_502544'
    reference[3599] = 'value_502544'
    assert tree.invariants(), "Invariants failed at step 11425"

    # Operation 11426: update
    tree[2487] = 'value_744057'
    reference[2487] = 'value_744057'
    assert tree.invariants(), "Invariants failed at step 11426"

    # Operation 11427: delete
    del tree[7309]
    del reference[7309]
    assert tree.invariants(), "Invariants failed at step 11427"

    # Operation 11428: insert
    tree[1525] = 'value_666496'
    reference[1525] = 'value_666496'
    assert tree.invariants(), "Invariants failed at step 11428"

    # Operation 11429: insert
    tree[1868] = 'value_22544'
    reference[1868] = 'value_22544'
    assert tree.invariants(), "Invariants failed at step 11429"

    # Operation 11430: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11430"

    # Operation 11431: delete
    del tree[8075]
    del reference[8075]
    assert tree.invariants(), "Invariants failed at step 11431"

    # Operation 11432: insert
    tree[8426] = 'value_631883'
    reference[8426] = 'value_631883'
    assert tree.invariants(), "Invariants failed at step 11432"

    # Operation 11433: delete
    del tree[6013]
    del reference[6013]
    assert tree.invariants(), "Invariants failed at step 11433"

    # Operation 11434: delete
    del tree[2487]
    del reference[2487]
    assert tree.invariants(), "Invariants failed at step 11434"

    # Operation 11435: delete
    del tree[1525]
    del reference[1525]
    assert tree.invariants(), "Invariants failed at step 11435"

    # Operation 11436: delete
    del tree[8426]
    del reference[8426]
    assert tree.invariants(), "Invariants failed at step 11436"

    # Operation 11437: get
    assert tree.invariants(), "Invariants failed at step 11437"

    # Operation 11438: get
    assert tree.invariants(), "Invariants failed at step 11438"

    # Operation 11439: delete
    del tree[1868]
    del reference[1868]
    assert tree.invariants(), "Invariants failed at step 11439"

    # Operation 11440: delete
    del tree[3599]
    del reference[3599]
    assert tree.invariants(), "Invariants failed at step 11440"

    # Operation 11441: insert
    tree[2440] = 'value_771689'
    reference[2440] = 'value_771689'
    assert tree.invariants(), "Invariants failed at step 11441"

    # Operation 11442: insert
    tree[6695] = 'value_311629'
    reference[6695] = 'value_311629'
    assert tree.invariants(), "Invariants failed at step 11442"

    # Operation 11443: delete
    del tree[2440]
    del reference[2440]
    assert tree.invariants(), "Invariants failed at step 11443"

    # Operation 11444: delete
    del tree[6695]
    del reference[6695]
    assert tree.invariants(), "Invariants failed at step 11444"

    # Operation 11445: update
    tree[1677] = 'value_299795'
    reference[1677] = 'value_299795'
    assert tree.invariants(), "Invariants failed at step 11445"

    # Operation 11446: get
    assert tree.invariants(), "Invariants failed at step 11446"

    # Operation 11447: delete
    del tree[1677]
    del reference[1677]
    assert tree.invariants(), "Invariants failed at step 11447"

    # Operation 11448: insert
    tree[5760] = 'value_710436'
    reference[5760] = 'value_710436'
    assert tree.invariants(), "Invariants failed at step 11448"

    # Operation 11449: insert
    tree[395] = 'value_177107'
    reference[395] = 'value_177107'
    assert tree.invariants(), "Invariants failed at step 11449"

    # Operation 11450: delete
    del tree[395]
    del reference[395]
    assert tree.invariants(), "Invariants failed at step 11450"

    # Operation 11451: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11451"

    # Operation 11452: insert
    tree[5948] = 'value_893835'
    reference[5948] = 'value_893835'
    assert tree.invariants(), "Invariants failed at step 11452"

    # Operation 11453: insert
    tree[3466] = 'value_875825'
    reference[3466] = 'value_875825'
    assert tree.invariants(), "Invariants failed at step 11453"

    # Operation 11454: get
    assert tree.invariants(), "Invariants failed at step 11454"

    # Operation 11455: insert
    tree[8580] = 'value_834869'
    reference[8580] = 'value_834869'
    assert tree.invariants(), "Invariants failed at step 11455"

    # Operation 11456: get
    assert tree.invariants(), "Invariants failed at step 11456"

    # Operation 11457: delete
    del tree[5948]
    del reference[5948]
    assert tree.invariants(), "Invariants failed at step 11457"

    # Operation 11458: insert
    tree[3615] = 'value_105517'
    reference[3615] = 'value_105517'
    assert tree.invariants(), "Invariants failed at step 11458"

    # Operation 11459: get
    assert tree.invariants(), "Invariants failed at step 11459"

    # Operation 11460: get
    assert tree.invariants(), "Invariants failed at step 11460"

    # Operation 11461: get
    assert tree.invariants(), "Invariants failed at step 11461"

    # Operation 11462: insert
    tree[1191] = 'value_448545'
    reference[1191] = 'value_448545'
    assert tree.invariants(), "Invariants failed at step 11462"

    # Operation 11463: delete
    del tree[1191]
    del reference[1191]
    assert tree.invariants(), "Invariants failed at step 11463"

    # Operation 11464: delete
    del tree[5760]
    del reference[5760]
    assert tree.invariants(), "Invariants failed at step 11464"

    # Operation 11465: insert
    tree[3933] = 'value_460801'
    reference[3933] = 'value_460801'
    assert tree.invariants(), "Invariants failed at step 11465"

    # Operation 11466: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11466"

    # Operation 11467: update
    tree[8580] = 'value_165210'
    reference[8580] = 'value_165210'
    assert tree.invariants(), "Invariants failed at step 11467"

    # Operation 11468: insert
    tree[8668] = 'value_807851'
    reference[8668] = 'value_807851'
    assert tree.invariants(), "Invariants failed at step 11468"

    # Operation 11469: delete
    del tree[8668]
    del reference[8668]
    assert tree.invariants(), "Invariants failed at step 11469"

    # Operation 11470: insert
    tree[9147] = 'value_113658'
    reference[9147] = 'value_113658'
    assert tree.invariants(), "Invariants failed at step 11470"

    # Operation 11471: insert
    tree[4471] = 'value_890704'
    reference[4471] = 'value_890704'
    assert tree.invariants(), "Invariants failed at step 11471"

    # Operation 11472: delete
    del tree[3933]
    del reference[3933]
    assert tree.invariants(), "Invariants failed at step 11472"

    # Operation 11473: delete
    del tree[4471]
    del reference[4471]
    assert tree.invariants(), "Invariants failed at step 11473"

    # Operation 11474: insert
    tree[9210] = 'value_195647'
    reference[9210] = 'value_195647'
    assert tree.invariants(), "Invariants failed at step 11474"

    # Operation 11475: update
    tree[9147] = 'value_535539'
    reference[9147] = 'value_535539'
    assert tree.invariants(), "Invariants failed at step 11475"

    # Operation 11476: delete
    del tree[3466]
    del reference[3466]
    assert tree.invariants(), "Invariants failed at step 11476"

    # Operation 11477: delete
    del tree[8580]
    del reference[8580]
    assert tree.invariants(), "Invariants failed at step 11477"

    # Operation 11478: get
    assert tree.invariants(), "Invariants failed at step 11478"

    # Operation 11479: insert
    tree[2291] = 'value_779275'
    reference[2291] = 'value_779275'
    assert tree.invariants(), "Invariants failed at step 11479"

    # Operation 11480: insert
    tree[8043] = 'value_533427'
    reference[8043] = 'value_533427'
    assert tree.invariants(), "Invariants failed at step 11480"

    # Operation 11481: insert
    tree[3502] = 'value_983605'
    reference[3502] = 'value_983605'
    assert tree.invariants(), "Invariants failed at step 11481"

    # Operation 11482: get
    assert tree.invariants(), "Invariants failed at step 11482"

    # Operation 11483: delete
    del tree[9210]
    del reference[9210]
    assert tree.invariants(), "Invariants failed at step 11483"

    # Operation 11484: delete
    del tree[8043]
    del reference[8043]
    assert tree.invariants(), "Invariants failed at step 11484"

    # Operation 11485: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11485"

    # Operation 11486: get
    assert tree.invariants(), "Invariants failed at step 11486"

    # Operation 11487: get
    assert tree.invariants(), "Invariants failed at step 11487"

    # Operation 11488: delete
    del tree[3615]
    del reference[3615]
    assert tree.invariants(), "Invariants failed at step 11488"

    # Operation 11489: insert
    tree[7700] = 'value_701386'
    reference[7700] = 'value_701386'
    assert tree.invariants(), "Invariants failed at step 11489"

    # Operation 11490: insert
    tree[1446] = 'value_223608'
    reference[1446] = 'value_223608'
    assert tree.invariants(), "Invariants failed at step 11490"

    # Operation 11491: insert
    tree[3732] = 'value_955417'
    reference[3732] = 'value_955417'
    assert tree.invariants(), "Invariants failed at step 11491"

    # Operation 11492: insert
    tree[9719] = 'value_488388'
    reference[9719] = 'value_488388'
    assert tree.invariants(), "Invariants failed at step 11492"

    # Operation 11493: get
    assert tree.invariants(), "Invariants failed at step 11493"

    # Operation 11494: delete
    del tree[3502]
    del reference[3502]
    assert tree.invariants(), "Invariants failed at step 11494"

    # Operation 11495: update
    tree[2291] = 'value_398173'
    reference[2291] = 'value_398173'
    assert tree.invariants(), "Invariants failed at step 11495"

    # Operation 11496: delete
    del tree[3732]
    del reference[3732]
    assert tree.invariants(), "Invariants failed at step 11496"

    # Operation 11497: batch_delete
    keys_to_delete = [2291, 7951, 1446, 4823]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11497"

    # Operation 11498: delete
    del tree[9147]
    del reference[9147]
    assert tree.invariants(), "Invariants failed at step 11498"

    # Operation 11499: get
    assert tree.invariants(), "Invariants failed at step 11499"

    # Operation 11500: insert
    tree[9961] = 'value_244064'
    reference[9961] = 'value_244064'
    assert tree.invariants(), "Invariants failed at step 11500"

    # Operation 11501: delete
    del tree[9961]
    del reference[9961]
    assert tree.invariants(), "Invariants failed at step 11501"

    # Operation 11502: insert
    tree[2232] = 'value_106823'
    reference[2232] = 'value_106823'
    assert tree.invariants(), "Invariants failed at step 11502"

    # Operation 11503: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11503"

    # Operation 11504: insert
    tree[5626] = 'value_447991'
    reference[5626] = 'value_447991'
    assert tree.invariants(), "Invariants failed at step 11504"

    # Operation 11505: insert
    tree[8567] = 'value_841927'
    reference[8567] = 'value_841927'
    assert tree.invariants(), "Invariants failed at step 11505"

    # Operation 11506: delete
    del tree[9719]
    del reference[9719]
    assert tree.invariants(), "Invariants failed at step 11506"

    # Operation 11507: insert
    tree[4657] = 'value_58216'
    reference[4657] = 'value_58216'
    assert tree.invariants(), "Invariants failed at step 11507"

    # Operation 11508: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11508"

    # Operation 11509: insert
    tree[4860] = 'value_689615'
    reference[4860] = 'value_689615'
    assert tree.invariants(), "Invariants failed at step 11509"

    # Operation 11510: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11510"

    # Operation 11511: insert
    tree[4191] = 'value_751506'
    reference[4191] = 'value_751506'
    assert tree.invariants(), "Invariants failed at step 11511"

    # Operation 11512: batch_delete
    keys_to_delete = [7214, 6032, 4657, 7700, 2232]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11512"

    # Operation 11513: get
    assert tree.invariants(), "Invariants failed at step 11513"

    # Operation 11514: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11514"

    # Operation 11515: delete
    del tree[8567]
    del reference[8567]
    assert tree.invariants(), "Invariants failed at step 11515"

    # Operation 11516: update
    tree[5626] = 'value_62569'
    reference[5626] = 'value_62569'
    assert tree.invariants(), "Invariants failed at step 11516"

    # Operation 11517: get
    assert tree.invariants(), "Invariants failed at step 11517"

    # Operation 11518: get
    assert tree.invariants(), "Invariants failed at step 11518"

    # Operation 11519: insert
    tree[6479] = 'value_679754'
    reference[6479] = 'value_679754'
    assert tree.invariants(), "Invariants failed at step 11519"

    # Operation 11520: delete
    del tree[6479]
    del reference[6479]
    assert tree.invariants(), "Invariants failed at step 11520"

    # Operation 11521: insert
    tree[4748] = 'value_154347'
    reference[4748] = 'value_154347'
    assert tree.invariants(), "Invariants failed at step 11521"

    # Operation 11522: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11522"

    # Operation 11523: get
    assert tree.invariants(), "Invariants failed at step 11523"

    # Operation 11524: get
    assert tree.invariants(), "Invariants failed at step 11524"

    # Operation 11525: get
    assert tree.invariants(), "Invariants failed at step 11525"

    # Operation 11526: get
    assert tree.invariants(), "Invariants failed at step 11526"

    # Operation 11527: insert
    tree[1506] = 'value_298786'
    reference[1506] = 'value_298786'
    assert tree.invariants(), "Invariants failed at step 11527"

    # Operation 11528: delete
    del tree[4748]
    del reference[4748]
    assert tree.invariants(), "Invariants failed at step 11528"

    # Operation 11529: insert
    tree[2925] = 'value_798679'
    reference[2925] = 'value_798679'
    assert tree.invariants(), "Invariants failed at step 11529"

    # Operation 11530: update
    tree[2925] = 'value_441296'
    reference[2925] = 'value_441296'
    assert tree.invariants(), "Invariants failed at step 11530"

    # Operation 11531: get
    assert tree.invariants(), "Invariants failed at step 11531"

    # Operation 11532: insert
    tree[7106] = 'value_898927'
    reference[7106] = 'value_898927'
    assert tree.invariants(), "Invariants failed at step 11532"

    # Operation 11533: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11533"

    # Operation 11534: delete
    del tree[4191]
    del reference[4191]
    assert tree.invariants(), "Invariants failed at step 11534"

    # Operation 11535: insert
    tree[2700] = 'value_21954'
    reference[2700] = 'value_21954'
    assert tree.invariants(), "Invariants failed at step 11535"

    # Operation 11536: insert
    tree[2121] = 'value_939785'
    reference[2121] = 'value_939785'
    assert tree.invariants(), "Invariants failed at step 11536"

    # Operation 11537: batch_delete
    keys_to_delete = [7106, 2121, 2700, 7568, 7539]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11537"

    # Operation 11538: delete
    del tree[5626]
    del reference[5626]
    assert tree.invariants(), "Invariants failed at step 11538"

    # Operation 11539: get
    assert tree.invariants(), "Invariants failed at step 11539"

    # Operation 11540: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11540"

    # Operation 11541: insert
    tree[4179] = 'value_481777'
    reference[4179] = 'value_481777'
    assert tree.invariants(), "Invariants failed at step 11541"

    # Operation 11542: insert
    tree[3722] = 'value_8554'
    reference[3722] = 'value_8554'
    assert tree.invariants(), "Invariants failed at step 11542"

    # Operation 11543: insert
    tree[9291] = 'value_811163'
    reference[9291] = 'value_811163'
    assert tree.invariants(), "Invariants failed at step 11543"

    # Operation 11544: get
    assert tree.invariants(), "Invariants failed at step 11544"

    # Operation 11545: insert
    tree[3803] = 'value_470679'
    reference[3803] = 'value_470679'
    assert tree.invariants(), "Invariants failed at step 11545"

    # Operation 11546: get
    assert tree.invariants(), "Invariants failed at step 11546"

    # Operation 11547: batch_delete
    keys_to_delete = [1506, 4119, 5498, 3803, 4860]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11547"

    # Operation 11548: delete
    del tree[9291]
    del reference[9291]
    assert tree.invariants(), "Invariants failed at step 11548"

    # Operation 11549: insert
    tree[801] = 'value_242792'
    reference[801] = 'value_242792'
    assert tree.invariants(), "Invariants failed at step 11549"

    # Operation 11550: update
    tree[801] = 'value_710455'
    reference[801] = 'value_710455'
    assert tree.invariants(), "Invariants failed at step 11550"

    # Operation 11551: delete
    del tree[2925]
    del reference[2925]
    assert tree.invariants(), "Invariants failed at step 11551"

    # Operation 11552: insert
    tree[1129] = 'value_786315'
    reference[1129] = 'value_786315'
    assert tree.invariants(), "Invariants failed at step 11552"

    # Operation 11553: get
    assert tree.invariants(), "Invariants failed at step 11553"

    # Operation 11554: get
    assert tree.invariants(), "Invariants failed at step 11554"

    # Operation 11555: insert
    tree[2551] = 'value_274945'
    reference[2551] = 'value_274945'
    assert tree.invariants(), "Invariants failed at step 11555"

    # Operation 11556: get
    assert tree.invariants(), "Invariants failed at step 11556"

    # Operation 11557: update
    tree[1129] = 'value_868639'
    reference[1129] = 'value_868639'
    assert tree.invariants(), "Invariants failed at step 11557"

    # Operation 11558: insert
    tree[2372] = 'value_977550'
    reference[2372] = 'value_977550'
    assert tree.invariants(), "Invariants failed at step 11558"

    # Operation 11559: insert
    tree[3883] = 'value_913785'
    reference[3883] = 'value_913785'
    assert tree.invariants(), "Invariants failed at step 11559"

    # Operation 11560: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11560"

    # Operation 11561: delete
    del tree[2551]
    del reference[2551]
    assert tree.invariants(), "Invariants failed at step 11561"

    # Operation 11562: insert
    tree[2055] = 'value_878743'
    reference[2055] = 'value_878743'
    assert tree.invariants(), "Invariants failed at step 11562"

    # Operation 11563: insert
    tree[8884] = 'value_765409'
    reference[8884] = 'value_765409'
    assert tree.invariants(), "Invariants failed at step 11563"

    # Operation 11564: insert
    tree[9157] = 'value_615242'
    reference[9157] = 'value_615242'
    assert tree.invariants(), "Invariants failed at step 11564"

    # Operation 11565: get
    assert tree.invariants(), "Invariants failed at step 11565"

    # Operation 11566: get
    assert tree.invariants(), "Invariants failed at step 11566"

    # Operation 11567: batch_delete
    keys_to_delete = [801, 9157, 2055, 4179, 8884, 404]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11567"

    # Operation 11568: get
    assert tree.invariants(), "Invariants failed at step 11568"

    # Operation 11569: update
    tree[2372] = 'value_448667'
    reference[2372] = 'value_448667'
    assert tree.invariants(), "Invariants failed at step 11569"

    # Operation 11570: get
    assert tree.invariants(), "Invariants failed at step 11570"

    # Operation 11571: get
    assert tree.invariants(), "Invariants failed at step 11571"

    # Operation 11572: delete
    del tree[1129]
    del reference[1129]
    assert tree.invariants(), "Invariants failed at step 11572"

    # Operation 11573: update
    tree[3722] = 'value_602320'
    reference[3722] = 'value_602320'
    assert tree.invariants(), "Invariants failed at step 11573"

    # Operation 11574: insert
    tree[94] = 'value_507003'
    reference[94] = 'value_507003'
    assert tree.invariants(), "Invariants failed at step 11574"

    # Operation 11575: insert
    tree[2277] = 'value_148113'
    reference[2277] = 'value_148113'
    assert tree.invariants(), "Invariants failed at step 11575"

    # Operation 11576: insert
    tree[8076] = 'value_765199'
    reference[8076] = 'value_765199'
    assert tree.invariants(), "Invariants failed at step 11576"

    # Operation 11577: update
    tree[2277] = 'value_138229'
    reference[2277] = 'value_138229'
    assert tree.invariants(), "Invariants failed at step 11577"

    # Operation 11578: delete
    del tree[2372]
    del reference[2372]
    assert tree.invariants(), "Invariants failed at step 11578"

    # Operation 11579: get
    assert tree.invariants(), "Invariants failed at step 11579"

    # Operation 11580: get
    assert tree.invariants(), "Invariants failed at step 11580"

    # Operation 11581: insert
    tree[965] = 'value_4564'
    reference[965] = 'value_4564'
    assert tree.invariants(), "Invariants failed at step 11581"

    # Operation 11582: batch_delete
    keys_to_delete = [2277, 965, 5287, 3722, 7219]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11582"

    # Operation 11583: delete
    del tree[3883]
    del reference[3883]
    assert tree.invariants(), "Invariants failed at step 11583"

    # Operation 11584: update
    tree[94] = 'value_659766'
    reference[94] = 'value_659766'
    assert tree.invariants(), "Invariants failed at step 11584"

    # Operation 11585: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11585"

    # Operation 11586: insert
    tree[8455] = 'value_707505'
    reference[8455] = 'value_707505'
    assert tree.invariants(), "Invariants failed at step 11586"

    # Operation 11587: delete
    del tree[8076]
    del reference[8076]
    assert tree.invariants(), "Invariants failed at step 11587"

    # Operation 11588: delete
    del tree[8455]
    del reference[8455]
    assert tree.invariants(), "Invariants failed at step 11588"

    # Operation 11589: get
    assert tree.invariants(), "Invariants failed at step 11589"

    # Operation 11590: insert
    tree[7572] = 'value_131668'
    reference[7572] = 'value_131668'
    assert tree.invariants(), "Invariants failed at step 11590"

    # Operation 11591: get
    assert tree.invariants(), "Invariants failed at step 11591"

    # Operation 11592: delete
    del tree[7572]
    del reference[7572]
    assert tree.invariants(), "Invariants failed at step 11592"

    # Operation 11593: delete
    del tree[94]
    del reference[94]
    assert tree.invariants(), "Invariants failed at step 11593"

    # Operation 11594: insert
    tree[6387] = 'value_138325'
    reference[6387] = 'value_138325'
    assert tree.invariants(), "Invariants failed at step 11594"

    # Operation 11595: delete
    del tree[6387]
    del reference[6387]
    assert tree.invariants(), "Invariants failed at step 11595"

    # Operation 11596: insert
    tree[5763] = 'value_242178'
    reference[5763] = 'value_242178'
    assert tree.invariants(), "Invariants failed at step 11596"

    # Operation 11597: insert
    tree[2014] = 'value_508187'
    reference[2014] = 'value_508187'
    assert tree.invariants(), "Invariants failed at step 11597"

    # Operation 11598: get
    assert tree.invariants(), "Invariants failed at step 11598"

    # Operation 11599: insert
    tree[9055] = 'value_692725'
    reference[9055] = 'value_692725'
    assert tree.invariants(), "Invariants failed at step 11599"

    # Operation 11600: delete
    del tree[2014]
    del reference[2014]
    assert tree.invariants(), "Invariants failed at step 11600"

    # Operation 11601: insert
    tree[9812] = 'value_23326'
    reference[9812] = 'value_23326'
    assert tree.invariants(), "Invariants failed at step 11601"

    # Operation 11602: delete
    del tree[9812]
    del reference[9812]
    assert tree.invariants(), "Invariants failed at step 11602"

    # Operation 11603: update
    tree[5763] = 'value_31767'
    reference[5763] = 'value_31767'
    assert tree.invariants(), "Invariants failed at step 11603"

    # Operation 11604: update
    tree[5763] = 'value_639437'
    reference[5763] = 'value_639437'
    assert tree.invariants(), "Invariants failed at step 11604"

    # Operation 11605: get
    assert tree.invariants(), "Invariants failed at step 11605"

    # Operation 11606: update
    tree[5763] = 'value_873678'
    reference[5763] = 'value_873678'
    assert tree.invariants(), "Invariants failed at step 11606"

    # Operation 11607: delete
    del tree[9055]
    del reference[9055]
    assert tree.invariants(), "Invariants failed at step 11607"

    # Operation 11608: insert
    tree[9252] = 'value_765258'
    reference[9252] = 'value_765258'
    assert tree.invariants(), "Invariants failed at step 11608"

    # Operation 11609: insert
    tree[7398] = 'value_561548'
    reference[7398] = 'value_561548'
    assert tree.invariants(), "Invariants failed at step 11609"

    # Operation 11610: get
    assert tree.invariants(), "Invariants failed at step 11610"

    # Operation 11611: delete
    del tree[5763]
    del reference[5763]
    assert tree.invariants(), "Invariants failed at step 11611"

    # Operation 11612: insert
    tree[9763] = 'value_66111'
    reference[9763] = 'value_66111'
    assert tree.invariants(), "Invariants failed at step 11612"

    # Operation 11613: insert
    tree[5421] = 'value_135909'
    reference[5421] = 'value_135909'
    assert tree.invariants(), "Invariants failed at step 11613"

    # Operation 11614: update
    tree[9763] = 'value_672723'
    reference[9763] = 'value_672723'
    assert tree.invariants(), "Invariants failed at step 11614"

    # Operation 11615: insert
    tree[4694] = 'value_41747'
    reference[4694] = 'value_41747'
    assert tree.invariants(), "Invariants failed at step 11615"

    # Operation 11616: update
    tree[9252] = 'value_92333'
    reference[9252] = 'value_92333'
    assert tree.invariants(), "Invariants failed at step 11616"

    # Operation 11617: delete
    del tree[4694]
    del reference[4694]
    assert tree.invariants(), "Invariants failed at step 11617"

    # Operation 11618: delete
    del tree[5421]
    del reference[5421]
    assert tree.invariants(), "Invariants failed at step 11618"

    # Operation 11619: get
    assert tree.invariants(), "Invariants failed at step 11619"

    # Operation 11620: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11620"

    # Operation 11621: insert
    tree[7570] = 'value_13654'
    reference[7570] = 'value_13654'
    assert tree.invariants(), "Invariants failed at step 11621"

    # Operation 11622: get
    assert tree.invariants(), "Invariants failed at step 11622"

    # Operation 11623: delete
    del tree[7570]
    del reference[7570]
    assert tree.invariants(), "Invariants failed at step 11623"

    # Operation 11624: delete
    del tree[7398]
    del reference[7398]
    assert tree.invariants(), "Invariants failed at step 11624"

    # Operation 11625: get
    assert tree.invariants(), "Invariants failed at step 11625"

    # Operation 11626: get
    assert tree.invariants(), "Invariants failed at step 11626"

    # Operation 11627: insert
    tree[3255] = 'value_125209'
    reference[3255] = 'value_125209'
    assert tree.invariants(), "Invariants failed at step 11627"

    # Operation 11628: insert
    tree[9128] = 'value_703342'
    reference[9128] = 'value_703342'
    assert tree.invariants(), "Invariants failed at step 11628"

    # Operation 11629: delete
    del tree[9252]
    del reference[9252]
    assert tree.invariants(), "Invariants failed at step 11629"

    # Operation 11630: delete
    del tree[3255]
    del reference[3255]
    assert tree.invariants(), "Invariants failed at step 11630"

    # Operation 11631: update
    tree[9763] = 'value_587872'
    reference[9763] = 'value_587872'
    assert tree.invariants(), "Invariants failed at step 11631"

    # Operation 11632: get
    assert tree.invariants(), "Invariants failed at step 11632"

    # Operation 11633: get
    assert tree.invariants(), "Invariants failed at step 11633"

    # Operation 11634: delete
    del tree[9763]
    del reference[9763]
    assert tree.invariants(), "Invariants failed at step 11634"

    # Operation 11635: insert
    tree[1601] = 'value_268050'
    reference[1601] = 'value_268050'
    assert tree.invariants(), "Invariants failed at step 11635"

    # Operation 11636: update
    tree[1601] = 'value_837774'
    reference[1601] = 'value_837774'
    assert tree.invariants(), "Invariants failed at step 11636"

    # Operation 11637: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11637"

    # Operation 11638: update
    tree[9128] = 'value_934456'
    reference[9128] = 'value_934456'
    assert tree.invariants(), "Invariants failed at step 11638"

    # Operation 11639: insert
    tree[4969] = 'value_46155'
    reference[4969] = 'value_46155'
    assert tree.invariants(), "Invariants failed at step 11639"

    # Operation 11640: update
    tree[4969] = 'value_56075'
    reference[4969] = 'value_56075'
    assert tree.invariants(), "Invariants failed at step 11640"

    # Operation 11641: update
    tree[9128] = 'value_41701'
    reference[9128] = 'value_41701'
    assert tree.invariants(), "Invariants failed at step 11641"

    # Operation 11642: get
    assert tree.invariants(), "Invariants failed at step 11642"

    # Operation 11643: update
    tree[9128] = 'value_889122'
    reference[9128] = 'value_889122'
    assert tree.invariants(), "Invariants failed at step 11643"

    # Operation 11644: insert
    tree[607] = 'value_98844'
    reference[607] = 'value_98844'
    assert tree.invariants(), "Invariants failed at step 11644"

    # Operation 11645: delete
    del tree[607]
    del reference[607]
    assert tree.invariants(), "Invariants failed at step 11645"

    # Operation 11646: insert
    tree[9100] = 'value_796592'
    reference[9100] = 'value_796592'
    assert tree.invariants(), "Invariants failed at step 11646"

    # Operation 11647: insert
    tree[3202] = 'value_148019'
    reference[3202] = 'value_148019'
    assert tree.invariants(), "Invariants failed at step 11647"

    # Operation 11648: delete
    del tree[4969]
    del reference[4969]
    assert tree.invariants(), "Invariants failed at step 11648"

    # Operation 11649: insert
    tree[4644] = 'value_614830'
    reference[4644] = 'value_614830'
    assert tree.invariants(), "Invariants failed at step 11649"

    # Operation 11650: delete
    del tree[9128]
    del reference[9128]
    assert tree.invariants(), "Invariants failed at step 11650"

    # Operation 11651: insert
    tree[3297] = 'value_151161'
    reference[3297] = 'value_151161'
    assert tree.invariants(), "Invariants failed at step 11651"

    # Operation 11652: batch_delete
    keys_to_delete = [3297, 3202, 6740, 750]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11652"

    # Operation 11653: delete
    del tree[1601]
    del reference[1601]
    assert tree.invariants(), "Invariants failed at step 11653"

    # Operation 11654: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11654"

    # Operation 11655: get
    assert tree.invariants(), "Invariants failed at step 11655"

    # Operation 11656: get
    assert tree.invariants(), "Invariants failed at step 11656"

    # Operation 11657: insert
    tree[3476] = 'value_983516'
    reference[3476] = 'value_983516'
    assert tree.invariants(), "Invariants failed at step 11657"

    # Operation 11658: delete
    del tree[4644]
    del reference[4644]
    assert tree.invariants(), "Invariants failed at step 11658"

    # Operation 11659: delete
    del tree[3476]
    del reference[3476]
    assert tree.invariants(), "Invariants failed at step 11659"

    # Operation 11660: get
    assert tree.invariants(), "Invariants failed at step 11660"

    # Operation 11661: insert
    tree[7408] = 'value_617913'
    reference[7408] = 'value_617913'
    assert tree.invariants(), "Invariants failed at step 11661"

    # Operation 11662: insert
    tree[2427] = 'value_164293'
    reference[2427] = 'value_164293'
    assert tree.invariants(), "Invariants failed at step 11662"

    # Operation 11663: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11663"

    # Operation 11664: insert
    tree[4975] = 'value_986785'
    reference[4975] = 'value_986785'
    assert tree.invariants(), "Invariants failed at step 11664"

    # Operation 11665: delete
    del tree[9100]
    del reference[9100]
    assert tree.invariants(), "Invariants failed at step 11665"

    # Operation 11666: get
    assert tree.invariants(), "Invariants failed at step 11666"

    # Operation 11667: delete
    del tree[7408]
    del reference[7408]
    assert tree.invariants(), "Invariants failed at step 11667"

    # Operation 11668: update
    tree[2427] = 'value_696958'
    reference[2427] = 'value_696958'
    assert tree.invariants(), "Invariants failed at step 11668"

    # Operation 11669: get
    assert tree.invariants(), "Invariants failed at step 11669"

    # Operation 11670: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11670"

    # Operation 11671: insert
    tree[3205] = 'value_996171'
    reference[3205] = 'value_996171'
    assert tree.invariants(), "Invariants failed at step 11671"

    # Operation 11672: insert
    tree[2620] = 'value_812535'
    reference[2620] = 'value_812535'
    assert tree.invariants(), "Invariants failed at step 11672"

    # Operation 11673: update
    tree[2620] = 'value_249401'
    reference[2620] = 'value_249401'
    assert tree.invariants(), "Invariants failed at step 11673"

    # Operation 11674: get
    assert tree.invariants(), "Invariants failed at step 11674"

    # Operation 11675: delete
    del tree[4975]
    del reference[4975]
    assert tree.invariants(), "Invariants failed at step 11675"

    # Operation 11676: update
    tree[3205] = 'value_701366'
    reference[3205] = 'value_701366'
    assert tree.invariants(), "Invariants failed at step 11676"

    # Operation 11677: insert
    tree[2206] = 'value_595275'
    reference[2206] = 'value_595275'
    assert tree.invariants(), "Invariants failed at step 11677"

    # Operation 11678: delete
    del tree[2206]
    del reference[2206]
    assert tree.invariants(), "Invariants failed at step 11678"

    # Operation 11679: delete
    del tree[3205]
    del reference[3205]
    assert tree.invariants(), "Invariants failed at step 11679"

    # Operation 11680: delete
    del tree[2427]
    del reference[2427]
    assert tree.invariants(), "Invariants failed at step 11680"

    # Operation 11681: insert
    tree[2023] = 'value_850378'
    reference[2023] = 'value_850378'
    assert tree.invariants(), "Invariants failed at step 11681"

    # Operation 11682: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11682"

    # Operation 11683: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11683"

    # Operation 11684: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11684"

    # Operation 11685: delete
    del tree[2023]
    del reference[2023]
    assert tree.invariants(), "Invariants failed at step 11685"

    # Operation 11686: delete
    del tree[2620]
    del reference[2620]
    assert tree.invariants(), "Invariants failed at step 11686"

    # Operation 11687: insert
    tree[7861] = 'value_175820'
    reference[7861] = 'value_175820'
    assert tree.invariants(), "Invariants failed at step 11687"

    # Operation 11688: insert
    tree[4175] = 'value_922865'
    reference[4175] = 'value_922865'
    assert tree.invariants(), "Invariants failed at step 11688"

    # Operation 11689: insert
    tree[5835] = 'value_566327'
    reference[5835] = 'value_566327'
    assert tree.invariants(), "Invariants failed at step 11689"

    # Operation 11690: update
    tree[5835] = 'value_445176'
    reference[5835] = 'value_445176'
    assert tree.invariants(), "Invariants failed at step 11690"

    # Operation 11691: insert
    tree[6236] = 'value_485465'
    reference[6236] = 'value_485465'
    assert tree.invariants(), "Invariants failed at step 11691"

    # Operation 11692: delete
    del tree[6236]
    del reference[6236]
    assert tree.invariants(), "Invariants failed at step 11692"

    # Operation 11693: delete
    del tree[4175]
    del reference[4175]
    assert tree.invariants(), "Invariants failed at step 11693"

    # Operation 11694: delete
    del tree[5835]
    del reference[5835]
    assert tree.invariants(), "Invariants failed at step 11694"

    # Operation 11695: insert
    tree[8072] = 'value_454193'
    reference[8072] = 'value_454193'
    assert tree.invariants(), "Invariants failed at step 11695"

    # Operation 11696: insert
    tree[8436] = 'value_546459'
    reference[8436] = 'value_546459'
    assert tree.invariants(), "Invariants failed at step 11696"

    # Operation 11697: delete
    del tree[8072]
    del reference[8072]
    assert tree.invariants(), "Invariants failed at step 11697"

    # Operation 11698: update
    tree[8436] = 'value_180510'
    reference[8436] = 'value_180510'
    assert tree.invariants(), "Invariants failed at step 11698"

    # Operation 11699: delete
    del tree[7861]
    del reference[7861]
    assert tree.invariants(), "Invariants failed at step 11699"

    # Operation 11700: delete
    del tree[8436]
    del reference[8436]
    assert tree.invariants(), "Invariants failed at step 11700"

    # Operation 11701: insert
    tree[2544] = 'value_356004'
    reference[2544] = 'value_356004'
    assert tree.invariants(), "Invariants failed at step 11701"

    # Operation 11702: delete
    del tree[2544]
    del reference[2544]
    assert tree.invariants(), "Invariants failed at step 11702"

    # Operation 11703: insert
    tree[9905] = 'value_223269'
    reference[9905] = 'value_223269'
    assert tree.invariants(), "Invariants failed at step 11703"

    # Operation 11704: insert
    tree[1119] = 'value_575695'
    reference[1119] = 'value_575695'
    assert tree.invariants(), "Invariants failed at step 11704"

    # Operation 11705: update
    tree[9905] = 'value_578891'
    reference[9905] = 'value_578891'
    assert tree.invariants(), "Invariants failed at step 11705"

    # Operation 11706: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11706"

    # Operation 11707: get
    assert tree.invariants(), "Invariants failed at step 11707"

    # Operation 11708: insert
    tree[7576] = 'value_574563'
    reference[7576] = 'value_574563'
    assert tree.invariants(), "Invariants failed at step 11708"

    # Operation 11709: get
    assert tree.invariants(), "Invariants failed at step 11709"

    # Operation 11710: get
    assert tree.invariants(), "Invariants failed at step 11710"

    # Operation 11711: insert
    tree[4819] = 'value_227375'
    reference[4819] = 'value_227375'
    assert tree.invariants(), "Invariants failed at step 11711"

    # Operation 11712: insert
    tree[1673] = 'value_60114'
    reference[1673] = 'value_60114'
    assert tree.invariants(), "Invariants failed at step 11712"

    # Operation 11713: insert
    tree[8071] = 'value_123170'
    reference[8071] = 'value_123170'
    assert tree.invariants(), "Invariants failed at step 11713"

    # Operation 11714: get
    assert tree.invariants(), "Invariants failed at step 11714"

    # Operation 11715: delete
    del tree[8071]
    del reference[8071]
    assert tree.invariants(), "Invariants failed at step 11715"

    # Operation 11716: get
    assert tree.invariants(), "Invariants failed at step 11716"

    # Operation 11717: get
    assert tree.invariants(), "Invariants failed at step 11717"

    # Operation 11718: delete
    del tree[1673]
    del reference[1673]
    assert tree.invariants(), "Invariants failed at step 11718"

    # Operation 11719: insert
    tree[6180] = 'value_384490'
    reference[6180] = 'value_384490'
    assert tree.invariants(), "Invariants failed at step 11719"

    # Operation 11720: update
    tree[7576] = 'value_960895'
    reference[7576] = 'value_960895'
    assert tree.invariants(), "Invariants failed at step 11720"

    # Operation 11721: delete
    del tree[9905]
    del reference[9905]
    assert tree.invariants(), "Invariants failed at step 11721"

    # Operation 11722: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11722"

    # Operation 11723: insert
    tree[6837] = 'value_770442'
    reference[6837] = 'value_770442'
    assert tree.invariants(), "Invariants failed at step 11723"

    # Operation 11724: batch_delete
    keys_to_delete = [7576, 2713, 2026, 6180]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11724"

    # Operation 11725: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11725"

    # Operation 11726: get
    assert tree.invariants(), "Invariants failed at step 11726"

    # Operation 11727: delete
    del tree[1119]
    del reference[1119]
    assert tree.invariants(), "Invariants failed at step 11727"

    # Operation 11728: delete
    del tree[4819]
    del reference[4819]
    assert tree.invariants(), "Invariants failed at step 11728"

    # Operation 11729: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11729"

    # Operation 11730: get
    assert tree.invariants(), "Invariants failed at step 11730"

    # Operation 11731: get
    assert tree.invariants(), "Invariants failed at step 11731"

    # Operation 11732: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11732"

    # Operation 11733: get
    assert tree.invariants(), "Invariants failed at step 11733"

    # Operation 11734: update
    tree[6837] = 'value_308647'
    reference[6837] = 'value_308647'
    assert tree.invariants(), "Invariants failed at step 11734"

    # Operation 11735: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11735"

    # Operation 11736: insert
    tree[3092] = 'value_970744'
    reference[3092] = 'value_970744'
    assert tree.invariants(), "Invariants failed at step 11736"

    # Operation 11737: delete
    del tree[3092]
    del reference[3092]
    assert tree.invariants(), "Invariants failed at step 11737"

    # Operation 11738: get
    assert tree.invariants(), "Invariants failed at step 11738"

    # Operation 11739: insert
    tree[9567] = 'value_524133'
    reference[9567] = 'value_524133'
    assert tree.invariants(), "Invariants failed at step 11739"

    # Operation 11740: delete
    del tree[9567]
    del reference[9567]
    assert tree.invariants(), "Invariants failed at step 11740"

    # Operation 11741: delete
    del tree[6837]
    del reference[6837]
    assert tree.invariants(), "Invariants failed at step 11741"

    # Operation 11742: insert
    tree[4065] = 'value_992048'
    reference[4065] = 'value_992048'
    assert tree.invariants(), "Invariants failed at step 11742"

    # Operation 11743: delete
    del tree[4065]
    del reference[4065]
    assert tree.invariants(), "Invariants failed at step 11743"

    # Operation 11744: get
    assert tree.invariants(), "Invariants failed at step 11744"

    # Operation 11745: get
    assert tree.invariants(), "Invariants failed at step 11745"

    # Operation 11746: insert
    tree[5339] = 'value_347457'
    reference[5339] = 'value_347457'
    assert tree.invariants(), "Invariants failed at step 11746"

    # Operation 11747: delete
    del tree[5339]
    del reference[5339]
    assert tree.invariants(), "Invariants failed at step 11747"

    # Operation 11748: insert
    tree[1194] = 'value_795493'
    reference[1194] = 'value_795493'
    assert tree.invariants(), "Invariants failed at step 11748"

    # Operation 11749: get
    assert tree.invariants(), "Invariants failed at step 11749"

    # Operation 11750: insert
    tree[9528] = 'value_673036'
    reference[9528] = 'value_673036'
    assert tree.invariants(), "Invariants failed at step 11750"

    # Operation 11751: get
    assert tree.invariants(), "Invariants failed at step 11751"

    # Operation 11752: delete
    del tree[1194]
    del reference[1194]
    assert tree.invariants(), "Invariants failed at step 11752"

    # Operation 11753: delete
    del tree[9528]
    del reference[9528]
    assert tree.invariants(), "Invariants failed at step 11753"

    # Operation 11754: insert
    tree[1506] = 'value_410372'
    reference[1506] = 'value_410372'
    assert tree.invariants(), "Invariants failed at step 11754"

    # Operation 11755: get
    assert tree.invariants(), "Invariants failed at step 11755"

    # Operation 11756: update
    tree[1506] = 'value_397797'
    reference[1506] = 'value_397797'
    assert tree.invariants(), "Invariants failed at step 11756"

    # Operation 11757: get
    assert tree.invariants(), "Invariants failed at step 11757"

    # Operation 11758: update
    tree[1506] = 'value_756341'
    reference[1506] = 'value_756341'
    assert tree.invariants(), "Invariants failed at step 11758"

    # Operation 11759: update
    tree[1506] = 'value_430942'
    reference[1506] = 'value_430942'
    assert tree.invariants(), "Invariants failed at step 11759"

    # Operation 11760: get
    assert tree.invariants(), "Invariants failed at step 11760"

    # Operation 11761: update
    tree[1506] = 'value_854684'
    reference[1506] = 'value_854684'
    assert tree.invariants(), "Invariants failed at step 11761"

    # Operation 11762: update
    tree[1506] = 'value_680558'
    reference[1506] = 'value_680558'
    assert tree.invariants(), "Invariants failed at step 11762"

    # Operation 11763: get
    assert tree.invariants(), "Invariants failed at step 11763"

    # Operation 11764: delete
    del tree[1506]
    del reference[1506]
    assert tree.invariants(), "Invariants failed at step 11764"

    # Operation 11765: insert
    tree[8643] = 'value_686312'
    reference[8643] = 'value_686312'
    assert tree.invariants(), "Invariants failed at step 11765"

    # Operation 11766: insert
    tree[192] = 'value_395535'
    reference[192] = 'value_395535'
    assert tree.invariants(), "Invariants failed at step 11766"

    # Operation 11767: insert
    tree[6854] = 'value_837400'
    reference[6854] = 'value_837400'
    assert tree.invariants(), "Invariants failed at step 11767"

    # Operation 11768: delete
    del tree[6854]
    del reference[6854]
    assert tree.invariants(), "Invariants failed at step 11768"

    # Operation 11769: insert
    tree[9595] = 'value_74462'
    reference[9595] = 'value_74462'
    assert tree.invariants(), "Invariants failed at step 11769"

    # Operation 11770: insert
    tree[7295] = 'value_884369'
    reference[7295] = 'value_884369'
    assert tree.invariants(), "Invariants failed at step 11770"

    # Operation 11771: insert
    tree[2378] = 'value_694616'
    reference[2378] = 'value_694616'
    assert tree.invariants(), "Invariants failed at step 11771"

    # Operation 11772: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11772"

    # Operation 11773: batch_delete
    keys_to_delete = [2378, 1444, 836, 7295]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11773"

    # Operation 11774: delete
    del tree[192]
    del reference[192]
    assert tree.invariants(), "Invariants failed at step 11774"

    # Operation 11775: insert
    tree[9941] = 'value_772159'
    reference[9941] = 'value_772159'
    assert tree.invariants(), "Invariants failed at step 11775"

    # Operation 11776: get
    assert tree.invariants(), "Invariants failed at step 11776"

    # Operation 11777: get
    assert tree.invariants(), "Invariants failed at step 11777"

    # Operation 11778: delete
    del tree[9595]
    del reference[9595]
    assert tree.invariants(), "Invariants failed at step 11778"

    # Operation 11779: delete
    del tree[8643]
    del reference[8643]
    assert tree.invariants(), "Invariants failed at step 11779"

    # Operation 11780: insert
    tree[3037] = 'value_231619'
    reference[3037] = 'value_231619'
    assert tree.invariants(), "Invariants failed at step 11780"

    # Operation 11781: delete
    del tree[9941]
    del reference[9941]
    assert tree.invariants(), "Invariants failed at step 11781"

    # Operation 11782: get
    assert tree.invariants(), "Invariants failed at step 11782"

    # Operation 11783: insert
    tree[8630] = 'value_369110'
    reference[8630] = 'value_369110'
    assert tree.invariants(), "Invariants failed at step 11783"

    # Operation 11784: get
    assert tree.invariants(), "Invariants failed at step 11784"

    # Operation 11785: delete
    del tree[8630]
    del reference[8630]
    assert tree.invariants(), "Invariants failed at step 11785"

    # Operation 11786: get
    assert tree.invariants(), "Invariants failed at step 11786"

    # Operation 11787: get
    assert tree.invariants(), "Invariants failed at step 11787"

    # Operation 11788: insert
    tree[2738] = 'value_824899'
    reference[2738] = 'value_824899'
    assert tree.invariants(), "Invariants failed at step 11788"

    # Operation 11789: insert
    tree[1523] = 'value_763493'
    reference[1523] = 'value_763493'
    assert tree.invariants(), "Invariants failed at step 11789"

    # Operation 11790: insert
    tree[8797] = 'value_632024'
    reference[8797] = 'value_632024'
    assert tree.invariants(), "Invariants failed at step 11790"

    # Operation 11791: delete
    del tree[8797]
    del reference[8797]
    assert tree.invariants(), "Invariants failed at step 11791"

    # Operation 11792: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11792"

    # Operation 11793: delete
    del tree[2738]
    del reference[2738]
    assert tree.invariants(), "Invariants failed at step 11793"

    # Operation 11794: insert
    tree[4339] = 'value_642902'
    reference[4339] = 'value_642902'
    assert tree.invariants(), "Invariants failed at step 11794"

    # Operation 11795: get
    assert tree.invariants(), "Invariants failed at step 11795"

    # Operation 11796: delete
    del tree[1523]
    del reference[1523]
    assert tree.invariants(), "Invariants failed at step 11796"

    # Operation 11797: delete
    del tree[3037]
    del reference[3037]
    assert tree.invariants(), "Invariants failed at step 11797"

    # Operation 11798: get
    assert tree.invariants(), "Invariants failed at step 11798"

    # Operation 11799: insert
    tree[9013] = 'value_375744'
    reference[9013] = 'value_375744'
    assert tree.invariants(), "Invariants failed at step 11799"

    # Operation 11800: delete
    del tree[4339]
    del reference[4339]
    assert tree.invariants(), "Invariants failed at step 11800"

    # Operation 11801: insert
    tree[6888] = 'value_514890'
    reference[6888] = 'value_514890'
    assert tree.invariants(), "Invariants failed at step 11801"

    # Operation 11802: delete
    del tree[9013]
    del reference[9013]
    assert tree.invariants(), "Invariants failed at step 11802"

    # Operation 11803: insert
    tree[6945] = 'value_53689'
    reference[6945] = 'value_53689'
    assert tree.invariants(), "Invariants failed at step 11803"

    # Operation 11804: get
    assert tree.invariants(), "Invariants failed at step 11804"

    # Operation 11805: get
    assert tree.invariants(), "Invariants failed at step 11805"

    # Operation 11806: insert
    tree[3935] = 'value_352207'
    reference[3935] = 'value_352207'
    assert tree.invariants(), "Invariants failed at step 11806"

    # Operation 11807: insert
    tree[9405] = 'value_129131'
    reference[9405] = 'value_129131'
    assert tree.invariants(), "Invariants failed at step 11807"

    # Operation 11808: update
    tree[6945] = 'value_103766'
    reference[6945] = 'value_103766'
    assert tree.invariants(), "Invariants failed at step 11808"

    # Operation 11809: delete
    del tree[6945]
    del reference[6945]
    assert tree.invariants(), "Invariants failed at step 11809"

    # Operation 11810: insert
    tree[5859] = 'value_953991'
    reference[5859] = 'value_953991'
    assert tree.invariants(), "Invariants failed at step 11810"

    # Operation 11811: delete
    del tree[6888]
    del reference[6888]
    assert tree.invariants(), "Invariants failed at step 11811"

    # Operation 11812: insert
    tree[3781] = 'value_389758'
    reference[3781] = 'value_389758'
    assert tree.invariants(), "Invariants failed at step 11812"

    # Operation 11813: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11813"

    # Operation 11814: insert
    tree[2899] = 'value_515462'
    reference[2899] = 'value_515462'
    assert tree.invariants(), "Invariants failed at step 11814"

    # Operation 11815: update
    tree[3935] = 'value_540566'
    reference[3935] = 'value_540566'
    assert tree.invariants(), "Invariants failed at step 11815"

    # Operation 11816: batch_delete
    keys_to_delete = [9405, 5859, 3781, 235]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11816"

    # Operation 11817: delete
    del tree[3935]
    del reference[3935]
    assert tree.invariants(), "Invariants failed at step 11817"

    # Operation 11818: insert
    tree[7111] = 'value_213967'
    reference[7111] = 'value_213967'
    assert tree.invariants(), "Invariants failed at step 11818"

    # Operation 11819: update
    tree[2899] = 'value_853140'
    reference[2899] = 'value_853140'
    assert tree.invariants(), "Invariants failed at step 11819"

    # Operation 11820: insert
    tree[6512] = 'value_922735'
    reference[6512] = 'value_922735'
    assert tree.invariants(), "Invariants failed at step 11820"

    # Operation 11821: insert
    tree[6804] = 'value_100906'
    reference[6804] = 'value_100906'
    assert tree.invariants(), "Invariants failed at step 11821"

    # Operation 11822: insert
    tree[3438] = 'value_854362'
    reference[3438] = 'value_854362'
    assert tree.invariants(), "Invariants failed at step 11822"

    # Operation 11823: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11823"

    # Operation 11824: get
    assert tree.invariants(), "Invariants failed at step 11824"

    # Operation 11825: insert
    tree[6467] = 'value_945652'
    reference[6467] = 'value_945652'
    assert tree.invariants(), "Invariants failed at step 11825"

    # Operation 11826: insert
    tree[2704] = 'value_147623'
    reference[2704] = 'value_147623'
    assert tree.invariants(), "Invariants failed at step 11826"

    # Operation 11827: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11827"

    # Operation 11828: batch_delete
    keys_to_delete = [2304, 899, 7111, 3438, 2899]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11828"

    # Operation 11829: get
    assert tree.invariants(), "Invariants failed at step 11829"

    # Operation 11830: get
    assert tree.invariants(), "Invariants failed at step 11830"

    # Operation 11831: insert
    tree[470] = 'value_630894'
    reference[470] = 'value_630894'
    assert tree.invariants(), "Invariants failed at step 11831"

    # Operation 11832: insert
    tree[2209] = 'value_313462'
    reference[2209] = 'value_313462'
    assert tree.invariants(), "Invariants failed at step 11832"

    # Operation 11833: insert
    tree[2553] = 'value_93881'
    reference[2553] = 'value_93881'
    assert tree.invariants(), "Invariants failed at step 11833"

    # Operation 11834: batch_delete
    keys_to_delete = [7841, 6804, 470, 2553, 2522]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11834"

    # Operation 11835: insert
    tree[9358] = 'value_153415'
    reference[9358] = 'value_153415'
    assert tree.invariants(), "Invariants failed at step 11835"

    # Operation 11836: get
    assert tree.invariants(), "Invariants failed at step 11836"

    # Operation 11837: delete
    del tree[9358]
    del reference[9358]
    assert tree.invariants(), "Invariants failed at step 11837"

    # Operation 11838: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11838"

    # Operation 11839: insert
    tree[3504] = 'value_902286'
    reference[3504] = 'value_902286'
    assert tree.invariants(), "Invariants failed at step 11839"

    # Operation 11840: get
    assert tree.invariants(), "Invariants failed at step 11840"

    # Operation 11841: delete
    del tree[6467]
    del reference[6467]
    assert tree.invariants(), "Invariants failed at step 11841"

    # Operation 11842: update
    tree[6512] = 'value_828904'
    reference[6512] = 'value_828904'
    assert tree.invariants(), "Invariants failed at step 11842"

    # Operation 11843: delete
    del tree[2209]
    del reference[2209]
    assert tree.invariants(), "Invariants failed at step 11843"

    # Operation 11844: insert
    tree[159] = 'value_829129'
    reference[159] = 'value_829129'
    assert tree.invariants(), "Invariants failed at step 11844"

    # Operation 11845: delete
    del tree[2704]
    del reference[2704]
    assert tree.invariants(), "Invariants failed at step 11845"

    # Operation 11846: delete
    del tree[3504]
    del reference[3504]
    assert tree.invariants(), "Invariants failed at step 11846"

    # Operation 11847: insert
    tree[9107] = 'value_159089'
    reference[9107] = 'value_159089'
    assert tree.invariants(), "Invariants failed at step 11847"

    # Operation 11848: delete
    del tree[6512]
    del reference[6512]
    assert tree.invariants(), "Invariants failed at step 11848"

    # Operation 11849: delete
    del tree[159]
    del reference[159]
    assert tree.invariants(), "Invariants failed at step 11849"

    # Operation 11850: delete
    del tree[9107]
    del reference[9107]
    assert tree.invariants(), "Invariants failed at step 11850"

    # Operation 11851: insert
    tree[1797] = 'value_648382'
    reference[1797] = 'value_648382'
    assert tree.invariants(), "Invariants failed at step 11851"

    # Operation 11852: delete
    del tree[1797]
    del reference[1797]
    assert tree.invariants(), "Invariants failed at step 11852"

    # Operation 11853: insert
    tree[4859] = 'value_317437'
    reference[4859] = 'value_317437'
    assert tree.invariants(), "Invariants failed at step 11853"

    # Operation 11854: insert
    tree[3168] = 'value_795957'
    reference[3168] = 'value_795957'
    assert tree.invariants(), "Invariants failed at step 11854"

    # Operation 11855: insert
    tree[341] = 'value_573799'
    reference[341] = 'value_573799'
    assert tree.invariants(), "Invariants failed at step 11855"

    # Operation 11856: insert
    tree[2253] = 'value_39593'
    reference[2253] = 'value_39593'
    assert tree.invariants(), "Invariants failed at step 11856"

    # Operation 11857: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11857"

    # Operation 11858: get
    assert tree.invariants(), "Invariants failed at step 11858"

    # Operation 11859: update
    tree[3168] = 'value_359697'
    reference[3168] = 'value_359697'
    assert tree.invariants(), "Invariants failed at step 11859"

    # Operation 11860: insert
    tree[2538] = 'value_791188'
    reference[2538] = 'value_791188'
    assert tree.invariants(), "Invariants failed at step 11860"

    # Operation 11861: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11861"

    # Operation 11862: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11862"

    # Operation 11863: insert
    tree[4968] = 'value_535931'
    reference[4968] = 'value_535931'
    assert tree.invariants(), "Invariants failed at step 11863"

    # Operation 11864: delete
    del tree[2253]
    del reference[2253]
    assert tree.invariants(), "Invariants failed at step 11864"

    # Operation 11865: insert
    tree[2757] = 'value_235880'
    reference[2757] = 'value_235880'
    assert tree.invariants(), "Invariants failed at step 11865"

    # Operation 11866: delete
    del tree[4859]
    del reference[4859]
    assert tree.invariants(), "Invariants failed at step 11866"

    # Operation 11867: insert
    tree[5692] = 'value_272555'
    reference[5692] = 'value_272555'
    assert tree.invariants(), "Invariants failed at step 11867"

    # Operation 11868: insert
    tree[7175] = 'value_556150'
    reference[7175] = 'value_556150'
    assert tree.invariants(), "Invariants failed at step 11868"

    # Operation 11869: insert
    tree[2225] = 'value_337796'
    reference[2225] = 'value_337796'
    assert tree.invariants(), "Invariants failed at step 11869"

    # Operation 11870: insert
    tree[7149] = 'value_827002'
    reference[7149] = 'value_827002'
    assert tree.invariants(), "Invariants failed at step 11870"

    # Operation 11871: get
    assert tree.invariants(), "Invariants failed at step 11871"

    # Operation 11872: insert
    tree[319] = 'value_680285'
    reference[319] = 'value_680285'
    assert tree.invariants(), "Invariants failed at step 11872"

    # Operation 11873: insert
    tree[4496] = 'value_700953'
    reference[4496] = 'value_700953'
    assert tree.invariants(), "Invariants failed at step 11873"

    # Operation 11874: delete
    del tree[4496]
    del reference[4496]
    assert tree.invariants(), "Invariants failed at step 11874"

    # Operation 11875: batch_delete
    keys_to_delete = [3168, 9561, 2538, 9992]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11875"

    # Operation 11876: delete
    del tree[7149]
    del reference[7149]
    assert tree.invariants(), "Invariants failed at step 11876"

    # Operation 11877: get
    assert tree.invariants(), "Invariants failed at step 11877"

    # Operation 11878: get
    assert tree.invariants(), "Invariants failed at step 11878"

    # Operation 11879: get
    assert tree.invariants(), "Invariants failed at step 11879"

    # Operation 11880: batch_delete
    keys_to_delete = [2757, 2225, 6833, 7640, 5692]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11880"

    # Operation 11881: insert
    tree[9993] = 'value_407935'
    reference[9993] = 'value_407935'
    assert tree.invariants(), "Invariants failed at step 11881"

    # Operation 11882: update
    tree[341] = 'value_52970'
    reference[341] = 'value_52970'
    assert tree.invariants(), "Invariants failed at step 11882"

    # Operation 11883: insert
    tree[2398] = 'value_840705'
    reference[2398] = 'value_840705'
    assert tree.invariants(), "Invariants failed at step 11883"

    # Operation 11884: delete
    del tree[4968]
    del reference[4968]
    assert tree.invariants(), "Invariants failed at step 11884"

    # Operation 11885: get
    assert tree.invariants(), "Invariants failed at step 11885"

    # Operation 11886: delete
    del tree[2398]
    del reference[2398]
    assert tree.invariants(), "Invariants failed at step 11886"

    # Operation 11887: update
    tree[341] = 'value_118173'
    reference[341] = 'value_118173'
    assert tree.invariants(), "Invariants failed at step 11887"

    # Operation 11888: update
    tree[7175] = 'value_2206'
    reference[7175] = 'value_2206'
    assert tree.invariants(), "Invariants failed at step 11888"

    # Operation 11889: delete
    del tree[7175]
    del reference[7175]
    assert tree.invariants(), "Invariants failed at step 11889"

    # Operation 11890: get
    assert tree.invariants(), "Invariants failed at step 11890"

    # Operation 11891: delete
    del tree[341]
    del reference[341]
    assert tree.invariants(), "Invariants failed at step 11891"

    # Operation 11892: delete
    del tree[319]
    del reference[319]
    assert tree.invariants(), "Invariants failed at step 11892"

    # Operation 11893: delete
    del tree[9993]
    del reference[9993]
    assert tree.invariants(), "Invariants failed at step 11893"

    # Operation 11894: insert
    tree[9956] = 'value_837918'
    reference[9956] = 'value_837918'
    assert tree.invariants(), "Invariants failed at step 11894"

    # Operation 11895: insert
    tree[652] = 'value_231508'
    reference[652] = 'value_231508'
    assert tree.invariants(), "Invariants failed at step 11895"

    # Operation 11896: insert
    tree[4189] = 'value_483178'
    reference[4189] = 'value_483178'
    assert tree.invariants(), "Invariants failed at step 11896"

    # Operation 11897: delete
    del tree[4189]
    del reference[4189]
    assert tree.invariants(), "Invariants failed at step 11897"

    # Operation 11898: get
    assert tree.invariants(), "Invariants failed at step 11898"

    # Operation 11899: get
    assert tree.invariants(), "Invariants failed at step 11899"

    # Operation 11900: insert
    tree[1089] = 'value_110139'
    reference[1089] = 'value_110139'
    assert tree.invariants(), "Invariants failed at step 11900"

    # Operation 11901: insert
    tree[36] = 'value_587258'
    reference[36] = 'value_587258'
    assert tree.invariants(), "Invariants failed at step 11901"

    # Operation 11902: insert
    tree[7385] = 'value_645411'
    reference[7385] = 'value_645411'
    assert tree.invariants(), "Invariants failed at step 11902"

    # Operation 11903: insert
    tree[1625] = 'value_261119'
    reference[1625] = 'value_261119'
    assert tree.invariants(), "Invariants failed at step 11903"

    # Operation 11904: batch_delete
    keys_to_delete = [1089, 36, 652, 2608, 285]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11904"

    # Operation 11905: delete
    del tree[7385]
    del reference[7385]
    assert tree.invariants(), "Invariants failed at step 11905"

    # Operation 11906: get
    assert tree.invariants(), "Invariants failed at step 11906"

    # Operation 11907: insert
    tree[6270] = 'value_269991'
    reference[6270] = 'value_269991'
    assert tree.invariants(), "Invariants failed at step 11907"

    # Operation 11908: update
    tree[9956] = 'value_238827'
    reference[9956] = 'value_238827'
    assert tree.invariants(), "Invariants failed at step 11908"

    # Operation 11909: delete
    del tree[1625]
    del reference[1625]
    assert tree.invariants(), "Invariants failed at step 11909"

    # Operation 11910: insert
    tree[5741] = 'value_729801'
    reference[5741] = 'value_729801'
    assert tree.invariants(), "Invariants failed at step 11910"

    # Operation 11911: get
    assert tree.invariants(), "Invariants failed at step 11911"

    # Operation 11912: update
    tree[5741] = 'value_64358'
    reference[5741] = 'value_64358'
    assert tree.invariants(), "Invariants failed at step 11912"

    # Operation 11913: insert
    tree[5597] = 'value_106130'
    reference[5597] = 'value_106130'
    assert tree.invariants(), "Invariants failed at step 11913"

    # Operation 11914: update
    tree[5597] = 'value_884322'
    reference[5597] = 'value_884322'
    assert tree.invariants(), "Invariants failed at step 11914"

    # Operation 11915: delete
    del tree[9956]
    del reference[9956]
    assert tree.invariants(), "Invariants failed at step 11915"

    # Operation 11916: insert
    tree[2912] = 'value_747929'
    reference[2912] = 'value_747929'
    assert tree.invariants(), "Invariants failed at step 11916"

    # Operation 11917: delete
    del tree[5597]
    del reference[5597]
    assert tree.invariants(), "Invariants failed at step 11917"

    # Operation 11918: delete
    del tree[2912]
    del reference[2912]
    assert tree.invariants(), "Invariants failed at step 11918"

    # Operation 11919: get
    assert tree.invariants(), "Invariants failed at step 11919"

    # Operation 11920: get
    assert tree.invariants(), "Invariants failed at step 11920"

    # Operation 11921: get
    assert tree.invariants(), "Invariants failed at step 11921"

    # Operation 11922: get
    assert tree.invariants(), "Invariants failed at step 11922"

    # Operation 11923: get
    assert tree.invariants(), "Invariants failed at step 11923"

    # Operation 11924: insert
    tree[1573] = 'value_11978'
    reference[1573] = 'value_11978'
    assert tree.invariants(), "Invariants failed at step 11924"

    # Operation 11925: get
    assert tree.invariants(), "Invariants failed at step 11925"

    # Operation 11926: insert
    tree[8413] = 'value_404736'
    reference[8413] = 'value_404736'
    assert tree.invariants(), "Invariants failed at step 11926"

    # Operation 11927: insert
    tree[1711] = 'value_442372'
    reference[1711] = 'value_442372'
    assert tree.invariants(), "Invariants failed at step 11927"

    # Operation 11928: get
    assert tree.invariants(), "Invariants failed at step 11928"

    # Operation 11929: delete
    del tree[6270]
    del reference[6270]
    assert tree.invariants(), "Invariants failed at step 11929"

    # Operation 11930: insert
    tree[4973] = 'value_787178'
    reference[4973] = 'value_787178'
    assert tree.invariants(), "Invariants failed at step 11930"

    # Operation 11931: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11931"

    # Operation 11932: delete
    del tree[1573]
    del reference[1573]
    assert tree.invariants(), "Invariants failed at step 11932"

    # Operation 11933: insert
    tree[4374] = 'value_358267'
    reference[4374] = 'value_358267'
    assert tree.invariants(), "Invariants failed at step 11933"

    # Operation 11934: batch_delete
    keys_to_delete = [9504, 277, 4374, 1711]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11934"

    # Operation 11935: delete
    del tree[8413]
    del reference[8413]
    assert tree.invariants(), "Invariants failed at step 11935"

    # Operation 11936: update
    tree[5741] = 'value_903442'
    reference[5741] = 'value_903442'
    assert tree.invariants(), "Invariants failed at step 11936"

    # Operation 11937: delete
    del tree[5741]
    del reference[5741]
    assert tree.invariants(), "Invariants failed at step 11937"

    # Operation 11938: get
    assert tree.invariants(), "Invariants failed at step 11938"

    # Operation 11939: delete
    del tree[4973]
    del reference[4973]
    assert tree.invariants(), "Invariants failed at step 11939"

    # Operation 11940: insert
    tree[8277] = 'value_466837'
    reference[8277] = 'value_466837'
    assert tree.invariants(), "Invariants failed at step 11940"

    # Operation 11941: get
    assert tree.invariants(), "Invariants failed at step 11941"

    # Operation 11942: insert
    tree[6267] = 'value_371554'
    reference[6267] = 'value_371554'
    assert tree.invariants(), "Invariants failed at step 11942"

    # Operation 11943: delete
    del tree[6267]
    del reference[6267]
    assert tree.invariants(), "Invariants failed at step 11943"

    # Operation 11944: insert
    tree[878] = 'value_491129'
    reference[878] = 'value_491129'
    assert tree.invariants(), "Invariants failed at step 11944"

    # Operation 11945: update
    tree[8277] = 'value_162674'
    reference[8277] = 'value_162674'
    assert tree.invariants(), "Invariants failed at step 11945"

    # Operation 11946: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11946"

    # Operation 11947: update
    tree[8277] = 'value_898806'
    reference[8277] = 'value_898806'
    assert tree.invariants(), "Invariants failed at step 11947"

    # Operation 11948: update
    tree[8277] = 'value_577192'
    reference[8277] = 'value_577192'
    assert tree.invariants(), "Invariants failed at step 11948"

    # Operation 11949: delete
    del tree[878]
    del reference[878]
    assert tree.invariants(), "Invariants failed at step 11949"

    # Operation 11950: get
    assert tree.invariants(), "Invariants failed at step 11950"

    # Operation 11951: update
    tree[8277] = 'value_242460'
    reference[8277] = 'value_242460'
    assert tree.invariants(), "Invariants failed at step 11951"

    # Operation 11952: insert
    tree[3931] = 'value_262482'
    reference[3931] = 'value_262482'
    assert tree.invariants(), "Invariants failed at step 11952"

    # Operation 11953: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11953"

    # Operation 11954: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11954"

    # Operation 11955: insert
    tree[373] = 'value_348491'
    reference[373] = 'value_348491'
    assert tree.invariants(), "Invariants failed at step 11955"

    # Operation 11956: delete
    del tree[3931]
    del reference[3931]
    assert tree.invariants(), "Invariants failed at step 11956"

    # Operation 11957: delete
    del tree[8277]
    del reference[8277]
    assert tree.invariants(), "Invariants failed at step 11957"

    # Operation 11958: insert
    tree[8035] = 'value_738891'
    reference[8035] = 'value_738891'
    assert tree.invariants(), "Invariants failed at step 11958"

    # Operation 11959: get
    assert tree.invariants(), "Invariants failed at step 11959"

    # Operation 11960: delete
    del tree[373]
    del reference[373]
    assert tree.invariants(), "Invariants failed at step 11960"

    # Operation 11961: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11961"

    # Operation 11962: get
    assert tree.invariants(), "Invariants failed at step 11962"

    # Operation 11963: delete
    del tree[8035]
    del reference[8035]
    assert tree.invariants(), "Invariants failed at step 11963"

    # Operation 11964: insert
    tree[7619] = 'value_205031'
    reference[7619] = 'value_205031'
    assert tree.invariants(), "Invariants failed at step 11964"

    # Operation 11965: update
    tree[7619] = 'value_222515'
    reference[7619] = 'value_222515'
    assert tree.invariants(), "Invariants failed at step 11965"

    # Operation 11966: insert
    tree[8657] = 'value_122355'
    reference[8657] = 'value_122355'
    assert tree.invariants(), "Invariants failed at step 11966"

    # Operation 11967: get
    assert tree.invariants(), "Invariants failed at step 11967"

    # Operation 11968: update
    tree[8657] = 'value_488391'
    reference[8657] = 'value_488391'
    assert tree.invariants(), "Invariants failed at step 11968"

    # Operation 11969: insert
    tree[9436] = 'value_518955'
    reference[9436] = 'value_518955'
    assert tree.invariants(), "Invariants failed at step 11969"

    # Operation 11970: get
    assert tree.invariants(), "Invariants failed at step 11970"

    # Operation 11971: get
    assert tree.invariants(), "Invariants failed at step 11971"

    # Operation 11972: update
    tree[9436] = 'value_544343'
    reference[9436] = 'value_544343'
    assert tree.invariants(), "Invariants failed at step 11972"

    # Operation 11973: get
    assert tree.invariants(), "Invariants failed at step 11973"

    # Operation 11974: get
    assert tree.invariants(), "Invariants failed at step 11974"

    # Operation 11975: insert
    tree[3459] = 'value_196093'
    reference[3459] = 'value_196093'
    assert tree.invariants(), "Invariants failed at step 11975"

    # Operation 11976: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11976"

    # Operation 11977: insert
    tree[2071] = 'value_302988'
    reference[2071] = 'value_302988'
    assert tree.invariants(), "Invariants failed at step 11977"

    # Operation 11978: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 11978"

    # Operation 11979: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 11979"

    # Operation 11980: insert
    tree[8262] = 'value_382863'
    reference[8262] = 'value_382863'
    assert tree.invariants(), "Invariants failed at step 11980"

    # Operation 11981: delete
    del tree[8262]
    del reference[8262]
    assert tree.invariants(), "Invariants failed at step 11981"

    # Operation 11982: insert
    tree[177] = 'value_276233'
    reference[177] = 'value_276233'
    assert tree.invariants(), "Invariants failed at step 11982"

    # Operation 11983: update
    tree[7619] = 'value_36798'
    reference[7619] = 'value_36798'
    assert tree.invariants(), "Invariants failed at step 11983"

    # Operation 11984: insert
    tree[53] = 'value_676309'
    reference[53] = 'value_676309'
    assert tree.invariants(), "Invariants failed at step 11984"

    # Operation 11985: delete
    del tree[3459]
    del reference[3459]
    assert tree.invariants(), "Invariants failed at step 11985"

    # Operation 11986: insert
    tree[9019] = 'value_878737'
    reference[9019] = 'value_878737'
    assert tree.invariants(), "Invariants failed at step 11986"

    # Operation 11987: update
    tree[9436] = 'value_312363'
    reference[9436] = 'value_312363'
    assert tree.invariants(), "Invariants failed at step 11987"

    # Operation 11988: get
    assert tree.invariants(), "Invariants failed at step 11988"

    # Operation 11989: insert
    tree[4128] = 'value_536550'
    reference[4128] = 'value_536550'
    assert tree.invariants(), "Invariants failed at step 11989"

    # Operation 11990: insert
    tree[92] = 'value_912365'
    reference[92] = 'value_912365'
    assert tree.invariants(), "Invariants failed at step 11990"

    # Operation 11991: insert
    tree[9774] = 'value_640824'
    reference[9774] = 'value_640824'
    assert tree.invariants(), "Invariants failed at step 11991"

    # Operation 11992: get
    assert tree.invariants(), "Invariants failed at step 11992"

    # Operation 11993: get
    assert tree.invariants(), "Invariants failed at step 11993"

    # Operation 11994: batch_delete
    keys_to_delete = [7619, 1852, 92, 8657, 2071, 9436, 2269]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11994"

    # Operation 11995: batch_delete
    keys_to_delete = [4128, 53, 157]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 11995"

    # Operation 11996: get
    assert tree.invariants(), "Invariants failed at step 11996"

    # Operation 11997: insert
    tree[7994] = 'value_253799'
    reference[7994] = 'value_253799'
    assert tree.invariants(), "Invariants failed at step 11997"

    # Operation 11998: delete
    del tree[177]
    del reference[177]
    assert tree.invariants(), "Invariants failed at step 11998"

    # Operation 11999: delete
    del tree[9019]
    del reference[9019]
    assert tree.invariants(), "Invariants failed at step 11999"

    # Operation 12000: get
    assert tree.invariants(), "Invariants failed at step 12000"

    # Operation 12001: delete
    del tree[7994]
    del reference[7994]
    assert tree.invariants(), "Invariants failed at step 12001"

    # Operation 12002: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12002"

    # Operation 12003: insert
    tree[569] = 'value_13031'
    reference[569] = 'value_13031'
    assert tree.invariants(), "Invariants failed at step 12003"

    # Operation 12004: update
    tree[9774] = 'value_675265'
    reference[9774] = 'value_675265'
    assert tree.invariants(), "Invariants failed at step 12004"

    # Operation 12005: delete
    del tree[569]
    del reference[569]
    assert tree.invariants(), "Invariants failed at step 12005"

    # Operation 12006: delete
    del tree[9774]
    del reference[9774]
    assert tree.invariants(), "Invariants failed at step 12006"

    # Operation 12007: insert
    tree[4324] = 'value_261102'
    reference[4324] = 'value_261102'
    assert tree.invariants(), "Invariants failed at step 12007"

    # Operation 12008: insert
    tree[7154] = 'value_494351'
    reference[7154] = 'value_494351'
    assert tree.invariants(), "Invariants failed at step 12008"

    # Operation 12009: delete
    del tree[7154]
    del reference[7154]
    assert tree.invariants(), "Invariants failed at step 12009"

    # Operation 12010: insert
    tree[9742] = 'value_385631'
    reference[9742] = 'value_385631'
    assert tree.invariants(), "Invariants failed at step 12010"

    # Operation 12011: get
    assert tree.invariants(), "Invariants failed at step 12011"

    # Operation 12012: insert
    tree[9256] = 'value_995842'
    reference[9256] = 'value_995842'
    assert tree.invariants(), "Invariants failed at step 12012"

    # Operation 12013: delete
    del tree[9742]
    del reference[9742]
    assert tree.invariants(), "Invariants failed at step 12013"

    # Operation 12014: insert
    tree[5172] = 'value_112628'
    reference[5172] = 'value_112628'
    assert tree.invariants(), "Invariants failed at step 12014"

    # Operation 12015: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12015"

    # Operation 12016: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12016"

    # Operation 12017: insert
    tree[7499] = 'value_23612'
    reference[7499] = 'value_23612'
    assert tree.invariants(), "Invariants failed at step 12017"

    # Operation 12018: delete
    del tree[7499]
    del reference[7499]
    assert tree.invariants(), "Invariants failed at step 12018"

    # Operation 12019: insert
    tree[5905] = 'value_885185'
    reference[5905] = 'value_885185'
    assert tree.invariants(), "Invariants failed at step 12019"

    # Operation 12020: get
    assert tree.invariants(), "Invariants failed at step 12020"

    # Operation 12021: insert
    tree[8822] = 'value_111664'
    reference[8822] = 'value_111664'
    assert tree.invariants(), "Invariants failed at step 12021"

    # Operation 12022: update
    tree[5905] = 'value_895428'
    reference[5905] = 'value_895428'
    assert tree.invariants(), "Invariants failed at step 12022"

    # Operation 12023: delete
    del tree[5172]
    del reference[5172]
    assert tree.invariants(), "Invariants failed at step 12023"

    # Operation 12024: get
    assert tree.invariants(), "Invariants failed at step 12024"

    # Operation 12025: get
    assert tree.invariants(), "Invariants failed at step 12025"

    # Operation 12026: get
    assert tree.invariants(), "Invariants failed at step 12026"

    # Operation 12027: update
    tree[4324] = 'value_153787'
    reference[4324] = 'value_153787'
    assert tree.invariants(), "Invariants failed at step 12027"

    # Operation 12028: delete
    del tree[4324]
    del reference[4324]
    assert tree.invariants(), "Invariants failed at step 12028"

    # Operation 12029: insert
    tree[1753] = 'value_836559'
    reference[1753] = 'value_836559'
    assert tree.invariants(), "Invariants failed at step 12029"

    # Operation 12030: delete
    del tree[8822]
    del reference[8822]
    assert tree.invariants(), "Invariants failed at step 12030"

    # Operation 12031: insert
    tree[9851] = 'value_573674'
    reference[9851] = 'value_573674'
    assert tree.invariants(), "Invariants failed at step 12031"

    # Operation 12032: insert
    tree[3741] = 'value_677189'
    reference[3741] = 'value_677189'
    assert tree.invariants(), "Invariants failed at step 12032"

    # Operation 12033: get
    assert tree.invariants(), "Invariants failed at step 12033"

    # Operation 12034: delete
    del tree[9256]
    del reference[9256]
    assert tree.invariants(), "Invariants failed at step 12034"

    # Operation 12035: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12035"

    # Operation 12036: update
    tree[3741] = 'value_238031'
    reference[3741] = 'value_238031'
    assert tree.invariants(), "Invariants failed at step 12036"

    # Operation 12037: get
    assert tree.invariants(), "Invariants failed at step 12037"

    # Operation 12038: get
    assert tree.invariants(), "Invariants failed at step 12038"

    # Operation 12039: insert
    tree[3248] = 'value_480131'
    reference[3248] = 'value_480131'
    assert tree.invariants(), "Invariants failed at step 12039"

    # Operation 12040: delete
    del tree[3248]
    del reference[3248]
    assert tree.invariants(), "Invariants failed at step 12040"

    # Operation 12041: insert
    tree[6551] = 'value_633017'
    reference[6551] = 'value_633017'
    assert tree.invariants(), "Invariants failed at step 12041"

    # Operation 12042: delete_nonexistent
    assert tree.invariants(), "Invariants failed at step 12042"

    # Operation 12043: get
    assert tree.invariants(), "Invariants failed at step 12043"

    # Operation 12044: insert
    tree[358] = 'value_498227'
    reference[358] = 'value_498227'
    assert tree.invariants(), "Invariants failed at step 12044"

    # Operation 12045: get
    assert tree.invariants(), "Invariants failed at step 12045"

    # Operation 12046: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12046"

    # Operation 12047: delete
    del tree[6551]
    del reference[6551]
    assert tree.invariants(), "Invariants failed at step 12047"

    # Operation 12048: get
    assert tree.invariants(), "Invariants failed at step 12048"

    # Operation 12049: delete
    del tree[9851]
    del reference[9851]
    assert tree.invariants(), "Invariants failed at step 12049"

    # Operation 12050: update
    tree[3741] = 'value_563092'
    reference[3741] = 'value_563092'
    assert tree.invariants(), "Invariants failed at step 12050"

    # Operation 12051: update
    tree[3741] = 'value_712090'
    reference[3741] = 'value_712090'
    assert tree.invariants(), "Invariants failed at step 12051"

    # Operation 12052: insert
    tree[8334] = 'value_973141'
    reference[8334] = 'value_973141'
    assert tree.invariants(), "Invariants failed at step 12052"

    # Operation 12053: batch_delete
    keys_to_delete = [5905, 4059, 3741, 8486]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 12053"

    # Operation 12054: update
    tree[1753] = 'value_133246'
    reference[1753] = 'value_133246'
    assert tree.invariants(), "Invariants failed at step 12054"

    # Operation 12055: update
    tree[1753] = 'value_528558'
    reference[1753] = 'value_528558'
    assert tree.invariants(), "Invariants failed at step 12055"

    # Operation 12056: delete
    del tree[8334]
    del reference[8334]
    assert tree.invariants(), "Invariants failed at step 12056"

    # Operation 12057: get
    assert tree.invariants(), "Invariants failed at step 12057"

    # Operation 12058: delete
    del tree[1753]
    del reference[1753]
    assert tree.invariants(), "Invariants failed at step 12058"

    # Operation 12059: update
    tree[358] = 'value_793497'
    reference[358] = 'value_793497'
    assert tree.invariants(), "Invariants failed at step 12059"

    # Operation 12060: get
    assert tree.invariants(), "Invariants failed at step 12060"

    # Operation 12061: get
    assert tree.invariants(), "Invariants failed at step 12061"

    # Operation 12062: insert
    tree[1375] = 'value_714027'
    reference[1375] = 'value_714027'
    assert tree.invariants(), "Invariants failed at step 12062"

    # Operation 12063: get
    assert tree.invariants(), "Invariants failed at step 12063"

    # Operation 12064: delete
    del tree[1375]
    del reference[1375]
    assert tree.invariants(), "Invariants failed at step 12064"

    # Operation 12065: insert
    tree[2850] = 'value_672221'
    reference[2850] = 'value_672221'
    assert tree.invariants(), "Invariants failed at step 12065"

    # Operation 12066: get
    assert tree.invariants(), "Invariants failed at step 12066"

    # Operation 12067: insert
    tree[3316] = 'value_696408'
    reference[3316] = 'value_696408'
    assert tree.invariants(), "Invariants failed at step 12067"

    # Operation 12068: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12068"

    # Operation 12069: delete
    del tree[3316]
    del reference[3316]
    assert tree.invariants(), "Invariants failed at step 12069"

    # Operation 12070: update
    tree[358] = 'value_6982'
    reference[358] = 'value_6982'
    assert tree.invariants(), "Invariants failed at step 12070"

    # Operation 12071: update
    tree[2850] = 'value_91406'
    reference[2850] = 'value_91406'
    assert tree.invariants(), "Invariants failed at step 12071"

    # Operation 12072: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12072"

    # Operation 12073: update
    tree[2850] = 'value_35886'
    reference[2850] = 'value_35886'
    assert tree.invariants(), "Invariants failed at step 12073"

    # Operation 12074: get
    assert tree.invariants(), "Invariants failed at step 12074"

    # Operation 12075: delete
    del tree[358]
    del reference[358]
    assert tree.invariants(), "Invariants failed at step 12075"

    # Operation 12076: delete
    del tree[2850]
    del reference[2850]
    assert tree.invariants(), "Invariants failed at step 12076"

    # Operation 12077: insert
    tree[2947] = 'value_205451'
    reference[2947] = 'value_205451'
    assert tree.invariants(), "Invariants failed at step 12077"

    # Operation 12078: insert
    tree[7853] = 'value_775809'
    reference[7853] = 'value_775809'
    assert tree.invariants(), "Invariants failed at step 12078"

    # Operation 12079: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12079"

    # Operation 12080: get
    assert tree.invariants(), "Invariants failed at step 12080"

    # Operation 12081: delete
    del tree[2947]
    del reference[2947]
    assert tree.invariants(), "Invariants failed at step 12081"

    # Operation 12082: delete
    del tree[7853]
    del reference[7853]
    assert tree.invariants(), "Invariants failed at step 12082"

    # Operation 12083: insert
    tree[2700] = 'value_894284'
    reference[2700] = 'value_894284'
    assert tree.invariants(), "Invariants failed at step 12083"

    # Operation 12084: update
    tree[2700] = 'value_557324'
    reference[2700] = 'value_557324'
    assert tree.invariants(), "Invariants failed at step 12084"

    # Operation 12085: update
    tree[2700] = 'value_225123'
    reference[2700] = 'value_225123'
    assert tree.invariants(), "Invariants failed at step 12085"

    # Operation 12086: get
    assert tree.invariants(), "Invariants failed at step 12086"

    # Operation 12087: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12087"

    # Operation 12088: insert
    tree[4462] = 'value_469546'
    reference[4462] = 'value_469546'
    assert tree.invariants(), "Invariants failed at step 12088"

    # Operation 12089: insert
    tree[8446] = 'value_595314'
    reference[8446] = 'value_595314'
    assert tree.invariants(), "Invariants failed at step 12089"

    # Operation 12090: get
    assert tree.invariants(), "Invariants failed at step 12090"

    # Operation 12091: insert
    tree[4025] = 'value_735457'
    reference[4025] = 'value_735457'
    assert tree.invariants(), "Invariants failed at step 12091"

    # Operation 12092: get
    assert tree.invariants(), "Invariants failed at step 12092"

    # Operation 12093: insert
    tree[3423] = 'value_265553'
    reference[3423] = 'value_265553'
    assert tree.invariants(), "Invariants failed at step 12093"

    # Operation 12094: update
    tree[3423] = 'value_642780'
    reference[3423] = 'value_642780'
    assert tree.invariants(), "Invariants failed at step 12094"

    # Operation 12095: insert
    tree[3462] = 'value_623193'
    reference[3462] = 'value_623193'
    assert tree.invariants(), "Invariants failed at step 12095"

    # Operation 12096: get
    assert tree.invariants(), "Invariants failed at step 12096"

    # Operation 12097: delete
    del tree[3423]
    del reference[3423]
    assert tree.invariants(), "Invariants failed at step 12097"

    # Operation 12098: update
    tree[3462] = 'value_195951'
    reference[3462] = 'value_195951'
    assert tree.invariants(), "Invariants failed at step 12098"

    # Operation 12099: delete
    del tree[4462]
    del reference[4462]
    assert tree.invariants(), "Invariants failed at step 12099"

    # Operation 12100: delete
    del tree[8446]
    del reference[8446]
    assert tree.invariants(), "Invariants failed at step 12100"

    # Operation 12101: update
    tree[2700] = 'value_436337'
    reference[2700] = 'value_436337'
    assert tree.invariants(), "Invariants failed at step 12101"

    # Operation 12102: get
    assert tree.invariants(), "Invariants failed at step 12102"

    # Operation 12103: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12103"

    # Operation 12104: delete
    del tree[4025]
    del reference[4025]
    assert tree.invariants(), "Invariants failed at step 12104"

    # Operation 12105: delete
    del tree[2700]
    del reference[2700]
    assert tree.invariants(), "Invariants failed at step 12105"

    # Operation 12106: get
    assert tree.invariants(), "Invariants failed at step 12106"

    # Operation 12107: delete
    del tree[3462]
    del reference[3462]
    assert tree.invariants(), "Invariants failed at step 12107"

    # Operation 12108: get
    assert tree.invariants(), "Invariants failed at step 12108"

    # Operation 12109: insert
    tree[9451] = 'value_638760'
    reference[9451] = 'value_638760'
    assert tree.invariants(), "Invariants failed at step 12109"

    # Operation 12110: get
    assert tree.invariants(), "Invariants failed at step 12110"

    # Operation 12111: delete
    del tree[9451]
    del reference[9451]
    assert tree.invariants(), "Invariants failed at step 12111"

    # Operation 12112: insert
    tree[6322] = 'value_290326'
    reference[6322] = 'value_290326'
    assert tree.invariants(), "Invariants failed at step 12112"

    # Operation 12113: delete
    del tree[6322]
    del reference[6322]
    assert tree.invariants(), "Invariants failed at step 12113"

    # Operation 12114: insert
    tree[8018] = 'value_788808'
    reference[8018] = 'value_788808'
    assert tree.invariants(), "Invariants failed at step 12114"

    # Operation 12115: insert
    tree[6713] = 'value_371590'
    reference[6713] = 'value_371590'
    assert tree.invariants(), "Invariants failed at step 12115"

    # Operation 12116: get
    assert tree.invariants(), "Invariants failed at step 12116"

    # Operation 12117: get
    assert tree.invariants(), "Invariants failed at step 12117"

    # Operation 12118: insert
    tree[6594] = 'value_765189'
    reference[6594] = 'value_765189'
    assert tree.invariants(), "Invariants failed at step 12118"

    # Operation 12119: insert
    tree[5130] = 'value_216131'
    reference[5130] = 'value_216131'
    assert tree.invariants(), "Invariants failed at step 12119"

    # Operation 12120: insert
    tree[8826] = 'value_488878'
    reference[8826] = 'value_488878'
    assert tree.invariants(), "Invariants failed at step 12120"

    # Operation 12121: insert
    tree[7900] = 'value_216526'
    reference[7900] = 'value_216526'
    assert tree.invariants(), "Invariants failed at step 12121"

    # Operation 12122: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12122"

    # Operation 12123: batch_delete
    keys_to_delete = [6594, 947, 8018, 9637]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 12123"

    # Operation 12124: insert
    tree[1043] = 'value_760688'
    reference[1043] = 'value_760688'
    assert tree.invariants(), "Invariants failed at step 12124"

    # Operation 12125: insert
    tree[6019] = 'value_96664'
    reference[6019] = 'value_96664'
    assert tree.invariants(), "Invariants failed at step 12125"

    # Operation 12126: insert
    tree[3784] = 'value_656212'
    reference[3784] = 'value_656212'
    assert tree.invariants(), "Invariants failed at step 12126"

    # Operation 12127: update
    tree[3784] = 'value_887271'
    reference[3784] = 'value_887271'
    assert tree.invariants(), "Invariants failed at step 12127"

    # Operation 12128: insert
    tree[1356] = 'value_21151'
    reference[1356] = 'value_21151'
    assert tree.invariants(), "Invariants failed at step 12128"

    # Operation 12129: update
    tree[5130] = 'value_919812'
    reference[5130] = 'value_919812'
    assert tree.invariants(), "Invariants failed at step 12129"

    # Operation 12130: get
    assert tree.invariants(), "Invariants failed at step 12130"

    # Operation 12131: batch_delete
    keys_to_delete = [5705, 5130, 850, 6713, 7900]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 12131"

    # Operation 12132: batch_delete
    keys_to_delete = [3784, 9585, 1043, 1726]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 12132"

    # Operation 12133: insert
    tree[8830] = 'value_302716'
    reference[8830] = 'value_302716'
    assert tree.invariants(), "Invariants failed at step 12133"

    # Operation 12134: insert
    tree[51] = 'value_296085'
    reference[51] = 'value_296085'
    assert tree.invariants(), "Invariants failed at step 12134"

    # Operation 12135: batch_delete
    keys_to_delete = [8826, 7410, 1356, 9477]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 12135"

    # Operation 12136: get
    assert tree.invariants(), "Invariants failed at step 12136"

    # Operation 12137: insert
    tree[9340] = 'value_441474'
    reference[9340] = 'value_441474'
    assert tree.invariants(), "Invariants failed at step 12137"

    # Operation 12138: update
    tree[9340] = 'value_671728'
    reference[9340] = 'value_671728'
    assert tree.invariants(), "Invariants failed at step 12138"

    # Operation 12139: get
    assert tree.invariants(), "Invariants failed at step 12139"

    # Operation 12140: insert
    tree[6059] = 'value_144538'
    reference[6059] = 'value_144538'
    assert tree.invariants(), "Invariants failed at step 12140"

    # Operation 12141: insert
    tree[6268] = 'value_296555'
    reference[6268] = 'value_296555'
    assert tree.invariants(), "Invariants failed at step 12141"

    # Operation 12142: update
    tree[6268] = 'value_359774'
    reference[6268] = 'value_359774'
    assert tree.invariants(), "Invariants failed at step 12142"

    # Operation 12143: insert
    tree[9330] = 'value_758355'
    reference[9330] = 'value_758355'
    assert tree.invariants(), "Invariants failed at step 12143"

    # Operation 12144: insert
    tree[3670] = 'value_720490'
    reference[3670] = 'value_720490'
    assert tree.invariants(), "Invariants failed at step 12144"

    # Operation 12145: delete
    del tree[8830]
    del reference[8830]
    assert tree.invariants(), "Invariants failed at step 12145"

    # Operation 12146: get
    assert tree.invariants(), "Invariants failed at step 12146"

    # Operation 12147: get
    assert tree.invariants(), "Invariants failed at step 12147"

    # Operation 12148: update
    tree[51] = 'value_668443'
    reference[51] = 'value_668443'
    assert tree.invariants(), "Invariants failed at step 12148"

    # Operation 12149: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12149"

    # Operation 12150: delete
    del tree[6019]
    del reference[6019]
    assert tree.invariants(), "Invariants failed at step 12150"

    # Operation 12151: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12151"

    # Operation 12152: update
    tree[51] = 'value_809771'
    reference[51] = 'value_809771'
    assert tree.invariants(), "Invariants failed at step 12152"

    # Operation 12153: insert
    tree[2303] = 'value_358742'
    reference[2303] = 'value_358742'
    assert tree.invariants(), "Invariants failed at step 12153"

    # Operation 12154: batch_delete
    keys_to_delete = [5543, 6059, 3672, 9340, 2303]
    tree.delete_batch(keys_to_delete)
    for k in keys_to_delete:
        if k in reference: del reference[k]
    assert tree.invariants(), "Invariants failed at step 12154"

    # Operation 12155: insert
    tree[9158] = 'value_213048'
    reference[9158] = 'value_213048'
    assert tree.invariants(), "Invariants failed at step 12155"

    # Operation 12156: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12156"

    # Operation 12157: insert
    tree[8710] = 'value_62221'
    reference[8710] = 'value_62221'
    assert tree.invariants(), "Invariants failed at step 12157"

    # Operation 12158: insert
    tree[587] = 'value_667264'
    reference[587] = 'value_667264'
    assert tree.invariants(), "Invariants failed at step 12158"

    # Operation 12159: delete
    del tree[9330]
    del reference[9330]
    assert tree.invariants(), "Invariants failed at step 12159"

    # Operation 12160: insert
    tree[1326] = 'value_328518'
    reference[1326] = 'value_328518'
    assert tree.invariants(), "Invariants failed at step 12160"

    # Operation 12161: insert
    tree[563] = 'value_880136'
    reference[563] = 'value_880136'
    assert tree.invariants(), "Invariants failed at step 12161"

    # Operation 12162: insert
    tree[6484] = 'value_861354'
    reference[6484] = 'value_861354'
    assert tree.invariants(), "Invariants failed at step 12162"

    # Operation 12163: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12163"

    # Operation 12164: insert
    tree[761] = 'value_788641'
    reference[761] = 'value_788641'
    assert tree.invariants(), "Invariants failed at step 12164"

    # Operation 12165: delete
    del tree[3670]
    del reference[3670]
    assert tree.invariants(), "Invariants failed at step 12165"

    # Operation 12166: delete
    del tree[51]
    del reference[51]
    assert tree.invariants(), "Invariants failed at step 12166"

    # Operation 12167: insert
    tree[301] = 'value_703149'
    reference[301] = 'value_703149'
    assert tree.invariants(), "Invariants failed at step 12167"

    # Operation 12168: get
    assert tree.invariants(), "Invariants failed at step 12168"

    # Operation 12169: update
    tree[563] = 'value_58665'
    reference[563] = 'value_58665'
    assert tree.invariants(), "Invariants failed at step 12169"

    # Operation 12170: delete
    del tree[1326]
    del reference[1326]
    assert tree.invariants(), "Invariants failed at step 12170"

    # Operation 12171: insert
    tree[8357] = 'value_981355'
    reference[8357] = 'value_981355'
    assert tree.invariants(), "Invariants failed at step 12171"

    # Operation 12172: insert
    tree[8206] = 'value_47014'
    reference[8206] = 'value_47014'
    assert tree.invariants(), "Invariants failed at step 12172"

    # Operation 12173: compact
    tree.compact()
    assert tree.invariants(), "Invariants failed at step 12173"

    # Operation 12174: insert
    tree[495] = 'value_5305'
    reference[495] = 'value_5305'
    assert tree.invariants(), "Invariants failed at step 12174"

    # Operation 12175: insert
    tree[2913] = 'value_197628'
    reference[2913] = 'value_197628'
    assert tree.invariants(), "Invariants failed at step 12175"

    # Operation 12176: get
    assert tree.invariants(), "Invariants failed at step 12176"

    # Operation 12177: update
    tree[2913] = 'value_439494'
    reference[2913] = 'value_439494'
    assert tree.invariants(), "Invariants failed at step 12177"

    # Operation 12178: insert
    tree[6463] = 'value_929788'
    reference[6463] = 'value_929788'
    assert tree.invariants(), "Invariants failed at step 12178"

    # Operation 12179: delete
    del tree[6463]
    del reference[6463]
    assert tree.invariants(), "Invariants failed at step 12179"

    # Operation 12180: delete
    del tree[8710]
    del reference[8710]
    assert tree.invariants(), "Invariants failed at step 12180"

    # Operation 12181: insert
    tree[4543] = 'value_541873'
    reference[4543] = 'value_541873'
    assert tree.invariants(), "Invariants failed at step 12181"

    # Operation 12182: delete
    del tree[4543]
    del reference[4543]
    assert tree.invariants(), "Invariants failed at step 12182"

    # Operation 12183: insert
    tree[9043] = 'value_823369'
    reference[9043] = 'value_823369'
    assert tree.invariants(), "Invariants failed at step 12183"

    # Operation 12184: get
    assert tree.invariants(), "Invariants failed at step 12184"

    # Verify final consistency
    assert len(tree) == len(reference), "Length mismatch"
    for key, value in reference.items():
        assert tree[key] == value, f"Value mismatch for {key}"
    print("Reproduction completed successfully")

if __name__ == "__main__":
    reproduce_failure()
